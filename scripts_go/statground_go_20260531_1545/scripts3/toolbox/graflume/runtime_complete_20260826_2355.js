/*! Graflume v0.1.0-alpha.0 | https://github.com/statground/graflume */
var Graflume = (function (exports) {
    'use strict';

    function quickChart(chartFactory, type, target, data, options) {
        const { x, y, mark, create, ...chartOptions } = options;
        return chartFactory(target, {
            ...chartOptions,
            data,
            mark: { type, ...mark },
            x,
            y,
        }, create);
    }
    function quickCombo(chartFactory, target, data, options) {
        const { layers, create, ...chartOptions } = options;
        return chartFactory(target, { ...chartOptions, data, layers }, create);
    }

    /** Above this bounded row count the chart-oriented scatter API uses the hybrid WebGL renderer. */
    const automaticScatterWebGLThreshold = 5_000;
    function dataInputRowCount(data) {
        if (Array.isArray(data))
            return data.length;
        const columnar = data;
        if (typeof columnar.length === 'number' &&
            Number.isInteger(columnar.length) &&
            columnar.length >= 0) {
            return columnar.length;
        }
        const lengths = Object.values(columnar.columns).map(({ length }) => length);
        return lengths.length === 0 ? 0 : Math.min(...lengths);
    }
    /** Pure, observable dispatch policy shared by default and complete scatter quick APIs. */
    function resolveScatterRendererDispatch(data, preference = 'auto', threshold = automaticScatterWebGLThreshold) {
        if (!Number.isInteger(threshold) || threshold < 1) {
            throw new RangeError('Scatter WebGL threshold must be a positive integer.');
        }
        const rowCount = dataInputRowCount(data);
        if (preference === 'canvas')
            return { renderer: 'canvas', reason: 'explicit-canvas', rowCount, threshold };
        if (preference === 'webgl')
            return { renderer: 'webgl', reason: 'explicit-webgl', rowCount, threshold };
        if (preference !== 'auto') {
            return { renderer: preference, reason: 'explicit-renderer', rowCount, threshold };
        }
        return rowCount >= threshold
            ? { renderer: 'webgl', reason: 'threshold', rowCount, threshold }
            : { renderer: 'canvas', reason: 'small', rowCount, threshold };
    }
    /** Chart-oriented scatter creation with automatic WebGL dispatch and the ordinary Chart API. */
    function quickScatter(chartFactory, target, data, options) {
        const dispatch = resolveScatterRendererDispatch(data, options.renderer);
        return quickChart(chartFactory, 'point', target, data, {
            ...options,
            renderer: dispatch.renderer,
        });
    }

    const family$2 = (id, name, quickApi, mark) => ({
        id,
        name,
        quickApi,
        mark,
    });
    const variant$1 = (id, name, quickApi, mark, familyId, mode = 'default') => ({ id, name, quickApi, mark, familyId, mode });
    /** Distinct families added by the complete entrypoint. */
    const additionalChartTypeCatalog = [
        family$2('polar', 'Polar chart', 'polar', 'polar'),
        family$2('network', 'Network chart', 'network', 'graph'),
        family$2('chord', 'Chord diagram', 'chord', 'chord'),
        family$2('funnel', 'Funnel chart', 'funnel', 'funnel'),
        family$2('parallel', 'Parallel coordinates', 'parallel', 'parallel'),
        family$2('heatmap', 'Heatmap', 'heatmap', 'heatmap'),
        family$2('image', 'Raster image', 'image', 'image'),
        family$2('ternary', 'Ternary chart', 'ternary', 'ternary'),
        family$2('smith', 'Smith chart', 'smith', 'smith'),
        family$2('scatter-matrix', 'Scatter matrix', 'scatterMatrix', 'scatter-matrix'),
        family$2('carpet', 'Carpet chart', 'carpet', 'carpet'),
    ];
    /** Existing advanced names retained as compatible presets. */
    const additionalChartVariantCatalog = [
        variant$1('polar', 'Polar chart', 'polar', 'polar', 'polar'),
        variant$1('radar', 'Radar chart', 'radar', 'radar', 'polar', 'radar'),
        variant$1('polar-line', 'Polar line chart', 'polarLine', 'polar', 'polar', 'line'),
        variant$1('polar-scatter', 'Polar scatter chart', 'polarScatter', 'polar', 'polar', 'scatter'),
        variant$1('polar-bar', 'Polar bar chart', 'polarBar', 'polar', 'polar', 'bar'),
        variant$1('tree', 'Tree chart', 'tree', 'tree', 'hierarchy', 'tree'),
        variant$1('graph', 'Graph chart', 'graph', 'graph', 'network', 'node-link'),
        variant$1('chord', 'Chord diagram', 'chord', 'chord', 'chord'),
        variant$1('funnel', 'Funnel chart', 'funnel', 'funnel', 'funnel'),
        variant$1('funnel-area', 'Funnel area chart', 'funnelArea', 'funnel', 'funnel', 'area'),
        variant$1('parallel', 'Parallel coordinates', 'parallel', 'parallel', 'parallel'),
        variant$1('parallel-categories', 'Parallel categories', 'parallelCategories', 'parallel', 'parallel', 'categories'),
        variant$1('boxplot', 'Boxplot', 'boxplot', 'boxplot', 'distribution', 'boxplot'),
        variant$1('effect-scatter', 'Effect scatter chart', 'effectScatter', 'effect-scatter', 'scatter', 'emphasis'),
        variant$1('lines', 'Connection lines', 'lines', 'lines', 'network', 'connections'),
        variant$1('heatmap', 'Heatmap', 'heatmap', 'heatmap', 'heatmap'),
        variant$1('image', 'Raster image', 'image', 'image', 'image'),
        variant$1('ternary', 'Ternary chart', 'ternary', 'ternary', 'ternary'),
        variant$1('smith', 'Smith chart', 'smith', 'smith', 'smith'),
        variant$1('scatter-matrix', 'Scatter matrix', 'scatterMatrix', 'scatter-matrix', 'scatter-matrix'),
        variant$1('carpet', 'Carpet chart', 'carpet', 'carpet', 'carpet'),
        variant$1('carpet-scatter', 'Carpet scatter overlay', 'carpetScatter', 'carpet', 'carpet', 'scatter'),
        variant$1('carpet-contour', 'Carpet contour overlay', 'carpetContour', 'carpet', 'carpet', 'contour'),
        variant$1('pictorial-bar', 'Pictorial bar chart', 'pictorialBar', 'pictorial-bar', 'bar', 'pictorial'),
        variant$1('theme-river', 'Theme river chart', 'themeRiver', 'theme-river', 'area', 'stream'),
        variant$1('sunburst', 'Sunburst chart', 'sunburst', 'sunburst', 'hierarchy', 'sunburst'),
        variant$1('custom', 'Declarative custom chart', 'custom', 'custom', 'custom'),
    ];

    const family$1 = (id, name, quickApi, mark) => ({
        id,
        name,
        quickApi,
        mark,
    });
    const variant = (id, name, quickApi, mark, familyId, mode = 'default', metadata = {}) => ({ id, name, quickApi, mark, familyId, mode, ...metadata });
    const researchFoundationsIntroduction = {
        introducedIn: 'research-foundations-2026-08-25',
    };
    /** Established chart families shown in discovery surfaces. */
    const chartTypeCatalog = [
        family$1('annotation', 'Annotation chart', 'annotation', 'annotation'),
        family$1('area', 'Area chart', 'area', 'area'),
        family$1('bar', 'Bar chart', 'bar', 'bar'),
        family$1('bubble', 'Bubble chart', 'bubble', 'bubble'),
        family$1('calendar', 'Calendar chart', 'calendar', 'calendar'),
        family$1('candlestick', 'Candlestick chart', 'candlestick', 'candlestick'),
        family$1('combination', 'Combination chart', 'combo', 'multiple'),
        family$1('difference', 'Difference chart', 'diff', 'diff'),
        family$1('pie', 'Pie chart', 'pie', 'pie'),
        family$1('timeline', 'Timeline and range chart', 'timeline', 'timeline'),
        family$1('gauge', 'Gauge chart', 'gauge', 'gauge'),
        family$1('map', 'Map chart', 'map', 'map'),
        family$1('distribution', 'Distribution chart', 'distribution', 'distribution'),
        family$1('interval', 'Interval chart', 'intervals', 'interval'),
        family$1('line', 'Line chart', 'line', 'line'),
        family$1('motion', 'Motion chart', 'motion', 'motion'),
        family$1('hierarchy', 'Hierarchy chart', 'treemap', 'treemap'),
        family$1('flow', 'Flow diagram', 'sankey', 'sankey'),
        family$1('scatter', 'Scatter chart', 'scatter', 'point'),
        family$1('table', 'Table chart', 'table', 'table'),
        family$1('waterfall', 'Waterfall chart', 'waterfall', 'waterfall'),
        family$1('word-tree', 'Word tree', 'wordTree', 'word-tree'),
    ];
    /** Existing Quick API names retained as compatible presets. */
    const chartVariantCatalog = [
        variant('annotation', 'Annotation chart', 'annotation', 'annotation', 'annotation'),
        variant('annotated-timeline', 'Annotated timeline', 'annotatedTimeline', 'annotation', 'annotation', 'timeline'),
        variant('area', 'Area chart', 'area', 'area', 'area'),
        variant('bar', 'Bar chart', 'horizontalBar', 'bar', 'bar', 'horizontal'),
        variant('bubble', 'Bubble chart', 'bubble', 'bubble', 'bubble'),
        variant('calendar', 'Calendar chart', 'calendar', 'calendar', 'calendar'),
        variant('candlestick', 'Candlestick chart', 'candlestick', 'candlestick', 'candlestick'),
        variant('column', 'Column chart', 'column', 'bar', 'bar', 'vertical'),
        variant('combo', 'Combo chart', 'combo', 'multiple', 'combination'),
        variant('diff', 'Diff chart', 'diff', 'diff', 'difference'),
        variant('donut', 'Donut chart', 'donut', 'pie', 'pie', 'donut'),
        variant('gantt', 'Gantt chart', 'gantt', 'gantt', 'timeline', 'gantt'),
        variant('gauge', 'Gauge chart', 'gauge', 'gauge', 'gauge'),
        variant('gauge-number', 'Number indicator', 'gaugeNumber', 'gauge', 'gauge', 'number'),
        variant('gauge-delta', 'Delta indicator', 'gaugeDelta', 'gauge', 'gauge', 'delta'),
        variant('gauge-bullet', 'Bullet gauge', 'gaugeBullet', 'gauge', 'gauge', 'bullet'),
        variant('geo', 'Geographic region chart', 'geo', 'geo', 'map', 'region'),
        variant('distribution', 'Distribution chart', 'distribution', 'distribution', 'distribution', 'histogram'),
        variant('histogram', 'Histogram', 'histogram', 'histogram', 'distribution', 'histogram'),
        variant('ecdf', 'Empirical cumulative distribution', 'ecdf', 'distribution', 'distribution', 'ecdf', researchFoundationsIntroduction),
        variant('ccdf', 'Complementary empirical distribution', 'ccdf', 'distribution', 'distribution', 'ccdf', researchFoundationsIntroduction),
        variant('kde', 'Kernel density estimate', 'kde', 'distribution', 'distribution', 'kde', researchFoundationsIntroduction),
        variant('histogram-2d', 'Bivariate histogram', 'histogram2d', 'distribution', 'distribution', 'histogram-2d'),
        variant('histogram-2d-contour', 'Bivariate density contours', 'histogram2dContour', 'distribution', 'distribution', 'histogram-2d-contour'),
        variant('violin', 'Violin chart', 'violin', 'distribution', 'distribution', 'violin'),
        variant('intervals', 'Intervals', 'intervals', 'interval', 'interval'),
        variant('line', 'Line chart', 'line', 'line', 'line'),
        variant('map', 'Map', 'map', 'map', 'map'),
        variant('motion', 'Motion chart', 'motion', 'motion', 'motion'),
        variant('org', 'Organization chart', 'org', 'org', 'hierarchy', 'organization'),
        variant('pie', 'Pie chart', 'pie', 'pie', 'pie'),
        variant('sankey', 'Sankey diagram', 'sankey', 'sankey', 'flow'),
        variant('scatter', 'Scatter chart', 'scatter', 'point', 'scatter'),
        variant('stepped-area', 'Stepped area chart', 'steppedArea', 'stepped-area', 'area', 'stepped'),
        variant('table', 'Table chart', 'table', 'table', 'table'),
        variant('timeline', 'Timeline', 'timeline', 'timeline', 'timeline'),
        variant('treemap', 'Tree map', 'treemap', 'treemap', 'hierarchy', 'treemap'),
        variant('icicle', 'Icicle chart', 'icicle', 'treemap', 'hierarchy', 'icicle'),
        variant('trendline', 'Trendline', 'trendline', 'trendline', 'line', 'trend'),
        variant('vega', 'Portable adapter chart', 'vegaChart', 'vega', 'custom', 'adapter'),
        variant('waterfall', 'Waterfall chart', 'waterfall', 'waterfall', 'waterfall'),
        variant('word-tree', 'Word tree', 'wordTree', 'word-tree', 'word-tree'),
    ];

    const variantFamilyOverrides = {
        'arc-diagram': 'network',
        'area-range': 'interval',
        'area-spline': 'area',
        'area-spline-range': 'interval',
        'bell-curve': 'distribution',
        bullet: 'bar',
        'column-pyramid': 'bar',
        'column-range': 'interval',
        cylinder: 'bar',
        'dependency-wheel': 'chord',
        dumbbell: 'interval',
        'error-bar': 'interval',
        'event-flags': 'annotation',
        'funnel-3d': 'funnel',
        'heikin-ashi': 'candlestick',
        'high-low-close': 'candlestick',
        'hollow-candlestick': 'candlestick',
        kagi: 'price-blocks',
        lollipop: 'bar',
        'network-graph': 'network',
        'open-high-low-close': 'candlestick',
        'organization-network': 'hierarchy',
        'packed-bubble': 'bubble',
        pareto: 'combination',
        'pictorial-column': 'bar',
        polygon: 'area',
        pyramid: 'funnel',
        'pyramid-3d': 'funnel',
        'scatter-3d': 'scatter',
        'three-line-break': 'price-blocks',
        'solid-gauge': 'gauge',
        spline: 'line',
        streamgraph: 'area',
        'tile-map': 'heatmap',
        'tree-graph': 'hierarchy',
        'variable-pie': 'pie',
        'variable-width': 'bar',
        'range-bars': 'price-blocks',
        vector: 'vector-field',
        'volume-by-price': 'volume-profile',
        'wind-barb': 'vector-field',
        'x-range': 'timeline',
    };
    function familyIdFor(id, category) {
        const override = variantFamilyOverrides[id];
        if (override)
            return override;
        if (category === 'indicator')
            return 'technical-indicator';
        if (category === 'map')
            return 'map';
        if (id === 'point-and-figure' || id === 'renko')
            return 'price-blocks';
        return id;
    }
    const entry = (id, name, quickApi, mark, category, _legacyFamily = id) => {
        const familyId = familyIdFor(id, category);
        return {
            id,
            name,
            quickApi,
            mark,
            category,
            familyId,
            mode: familyId === id ? 'default' : id,
            canonicalFamily: familyId,
        };
    };
    const currentLimitationsRelease = 'current-limitations-2026-08-26';
    const currentReleaseEntry = (...args) => ({
        ...entry(...args),
        introducedIn: currentLimitationsRelease,
    });
    /**
     * Specialized series that extend the established and advanced catalogs.
     * Existing families are represented once and reused through canonical aliases.
     */
    const seriesChartVariantCatalog = [
        entry('arc-diagram', 'Arc diagram', 'arcDiagram', 'arc-diagram', 'relationship'),
        entry('area-range', 'Area range chart', 'areaRange', 'range', 'cartesian'),
        entry('area-spline', 'Smooth area chart', 'areaSpline', 'smooth', 'cartesian'),
        entry('area-spline-range', 'Smooth area range chart', 'areaSplineRange', 'range', 'cartesian'),
        entry('bell-curve', 'Bell curve', 'bellCurve', 'distribution', 'distribution'),
        entry('bullet', 'Bullet chart', 'bullet', 'bullet', 'cartesian'),
        entry('column-pyramid', 'Column pyramid chart', 'columnPyramid', 'pyramid', 'cartesian'),
        entry('column-range', 'Column range chart', 'columnRange', 'range', 'cartesian'),
        entry('contour', 'Contour chart', 'contour', 'contour', 'distribution'),
        entry('cylinder', 'Cylinder chart', 'cylinder', 'cylinder', 'cartesian'),
        entry('dependency-wheel', 'Dependency wheel', 'dependencyWheel', 'chord', 'relationship', 'chord'),
        entry('dumbbell', 'Dumbbell chart', 'dumbbell', 'range', 'cartesian'),
        entry('error-bar', 'Error bar chart', 'errorBar', 'interval', 'cartesian', 'intervals'),
        entry('funnel-3d', 'Depth funnel chart', 'funnel3d', 'pyramid', 'radial'),
        entry('item', 'Item chart', 'itemChart', 'item', 'radial'),
        entry('lollipop', 'Lollipop chart', 'lollipop', 'lollipop', 'cartesian'),
        entry('network-graph', 'Network graph', 'networkGraph', 'graph', 'relationship', 'graph'),
        entry('organization-network', 'Organization network', 'organizationNetwork', 'org', 'relationship', 'org'),
        entry('packed-bubble', 'Packed bubble chart', 'packedBubble', 'packed-bubble', 'relationship'),
        entry('pareto', 'Pareto chart', 'pareto', 'pareto', 'distribution'),
        entry('pictorial-column', 'Pictorial column chart', 'pictorialColumn', 'pictorial-bar', 'cartesian', 'pictorial-bar'),
        entry('polygon', 'Polygon chart', 'polygon', 'polygon', 'cartesian'),
        entry('pyramid', 'Pyramid chart', 'pyramid', 'pyramid', 'radial'),
        entry('pyramid-3d', 'Depth pyramid chart', 'pyramid3d', 'pyramid', 'radial', 'pyramid'),
        entry('scatter-3d', 'Three-axis scatter chart', 'scatter3d', 'scatter-3d', 'cartesian'),
        entry('solid-gauge', 'Solid gauge', 'solidGauge', 'solid-gauge', 'radial'),
        entry('spline', 'Spline chart', 'spline', 'smooth', 'cartesian'),
        entry('streamgraph', 'Streamgraph', 'streamgraph', 'theme-river', 'distribution', 'theme-river'),
        entry('tile-map', 'Tile map', 'tileMap', 'tilemap', 'map'),
        entry('tree-graph', 'Tree graph', 'treeGraph', 'tree', 'relationship', 'tree'),
        entry('variable-pie', 'Variable radius pie chart', 'variablePie', 'variable-pie', 'radial'),
        entry('variable-width', 'Variable width column chart', 'variableWidth', 'variwide', 'cartesian'),
        entry('vector', 'Vector field chart', 'vector', 'vector', 'cartesian'),
        entry('venn', 'Venn diagram', 'venn', 'venn', 'relationship'),
        entry('wind-barb', 'Wind barb chart', 'windBarb', 'wind-barb', 'cartesian'),
        entry('word-cloud', 'Word cloud', 'wordCloud', 'word-cloud', 'relationship'),
        entry('x-range', 'Horizontal range chart', 'xRange', 'timeline', 'cartesian', 'timeline'),
        entry('acceleration-bands', 'Acceleration bands', 'accelerationBands', 'indicator', 'indicator'),
        entry('awesome-oscillator', 'Awesome oscillator', 'awesomeOscillator', 'indicator', 'indicator'),
        entry('absolute-price-oscillator', 'Absolute price oscillator', 'absolutePriceOscillator', 'indicator', 'indicator'),
        entry('aroon', 'Aroon indicator', 'aroon', 'indicator', 'indicator'),
        entry('aroon-oscillator', 'Aroon oscillator', 'aroonOscillator', 'indicator', 'indicator'),
        entry('average-true-range', 'Average true range', 'averageTrueRange', 'indicator', 'indicator'),
        entry('volatility-bands', 'Volatility bands', 'volatilityBands', 'indicator', 'indicator'),
        entry('commodity-channel-index', 'Commodity channel index', 'commodityChannelIndex', 'indicator', 'indicator'),
        entry('chaikin-oscillator', 'Chaikin oscillator', 'chaikinOscillator', 'indicator', 'indicator'),
        entry('chaikin-money-flow', 'Chaikin money flow', 'chaikinMoneyFlow', 'indicator', 'indicator'),
        entry('chande-momentum-oscillator', 'Chande momentum oscillator', 'chandeMomentumOscillator', 'indicator', 'indicator'),
        entry('double-exponential-average', 'Double exponential moving average', 'doubleExponentialMovingAverage', 'indicator', 'indicator'),
        entry('disparity-index', 'Disparity index', 'disparityIndex', 'indicator', 'indicator'),
        entry('directional-movement-index', 'Directional movement index', 'directionalMovementIndex', 'indicator', 'indicator'),
        entry('detrended-price-oscillator', 'Detrended price oscillator', 'detrendedPriceOscillator', 'indicator', 'indicator'),
        entry('exponential-moving-average', 'Exponential moving average', 'exponentialMovingAverage', 'indicator', 'indicator'),
        entry('event-flags', 'Event flags', 'eventFlags', 'flags', 'financial'),
        entry('heikin-ashi', 'Heikin-Ashi chart', 'heikinAshi', 'financial', 'financial'),
        entry('high-low-close', 'High-low-close chart', 'highLowClose', 'financial', 'financial'),
        entry('hollow-candlestick', 'Hollow candlestick chart', 'hollowCandlestick', 'financial', 'financial'),
        entry('ichimoku-cloud', 'Ichimoku cloud', 'ichimokuCloud', 'indicator', 'indicator'),
        entry('keltner-channels', 'Keltner channels', 'keltnerChannels', 'indicator', 'indicator'),
        entry('klinger-oscillator', 'Klinger oscillator', 'klingerOscillator', 'indicator', 'indicator'),
        entry('linear-regression', 'Linear regression', 'linearRegression', 'indicator', 'indicator'),
        entry('linear-regression-angle', 'Linear regression angle', 'linearRegressionAngle', 'indicator', 'indicator'),
        entry('linear-regression-intercept', 'Linear regression intercept', 'linearRegressionIntercept', 'indicator', 'indicator'),
        entry('linear-regression-slope', 'Linear regression slope', 'linearRegressionSlope', 'indicator', 'indicator'),
        entry('moving-average-convergence-divergence', 'Moving average convergence divergence', 'movingAverageConvergenceDivergence', 'indicator', 'indicator'),
        entry('money-flow-index', 'Money flow index', 'moneyFlowIndex', 'indicator', 'indicator'),
        entry('momentum', 'Momentum indicator', 'momentumIndicator', 'indicator', 'indicator'),
        entry('normalized-average-true-range', 'Normalized average true range', 'normalizedAverageTrueRange', 'indicator', 'indicator'),
        entry('on-balance-volume', 'On-balance volume', 'onBalanceVolume', 'indicator', 'indicator'),
        entry('open-high-low-close', 'Open-high-low-close chart', 'openHighLowClose', 'financial', 'financial'),
        entry('price-channel', 'Price channel', 'priceChannel', 'indicator', 'indicator'),
        entry('pivot-points', 'Pivot points', 'pivotPoints', 'indicator', 'indicator'),
        entry('point-and-figure', 'Point and figure chart', 'pointAndFigure', 'point-figure', 'financial'),
        entry('percentage-price-oscillator', 'Percentage price oscillator', 'percentagePriceOscillator', 'indicator', 'indicator'),
        entry('price-envelopes', 'Price envelopes', 'priceEnvelopes', 'indicator', 'indicator'),
        entry('parabolic-stop-and-reverse', 'Parabolic stop and reverse', 'parabolicStopAndReverse', 'indicator', 'indicator'),
        entry('renko', 'Renko chart', 'renko', 'renko', 'financial'),
        currentReleaseEntry('kagi', 'Kagi chart', 'kagi', 'renko', 'financial'),
        currentReleaseEntry('three-line-break', 'Three line break chart', 'threeLineBreak', 'renko', 'financial'),
        currentReleaseEntry('range-bars', 'Range bars chart', 'rangeBars', 'renko', 'financial'),
        entry('rate-of-change', 'Rate of change', 'rateOfChange', 'indicator', 'indicator'),
        entry('relative-strength-index', 'Relative strength index', 'relativeStrengthIndex', 'indicator', 'indicator'),
        entry('slow-stochastic', 'Slow stochastic oscillator', 'slowStochastic', 'indicator', 'indicator'),
        entry('simple-moving-average', 'Simple moving average', 'simpleMovingAverage', 'indicator', 'indicator'),
        entry('stochastic', 'Stochastic oscillator', 'stochastic', 'indicator', 'indicator'),
        entry('supertrend', 'Supertrend', 'supertrend', 'indicator', 'indicator'),
        entry('triple-exponential-average', 'Triple exponential moving average', 'tripleExponentialMovingAverage', 'indicator', 'indicator'),
        entry('triple-exponential-oscillator', 'Triple exponential average oscillator', 'tripleExponentialAverageOscillator', 'indicator', 'indicator'),
        entry('volume-by-price', 'Volume by price', 'volumeByPrice', 'volume-profile', 'financial'),
        entry('volume-weighted-average-price', 'Volume weighted average price', 'volumeWeightedAveragePrice', 'indicator', 'indicator'),
        entry('williams-range', 'Williams range', 'williamsRange', 'indicator', 'indicator'),
        entry('weighted-moving-average', 'Weighted moving average', 'weightedMovingAverage', 'indicator', 'indicator'),
        entry('zigzag', 'Zigzag indicator', 'zigzag', 'indicator', 'indicator'),
        entry('flow-map', 'Flow map', 'flowMap', 'geo-flow', 'map'),
        entry('geo-heatmap', 'Geographic heatmap', 'geoHeatmap', 'geo-heatmap', 'map'),
        entry('map-bubble', 'Map bubble chart', 'mapBubble', 'map', 'map', 'map'),
        entry('map-line', 'Map line chart', 'mapLine', 'geo-line', 'map'),
        entry('map-point', 'Map point chart', 'mapPoint', 'map', 'map', 'map'),
        entry('tiled-map', 'Tiled map', 'tiledMap', 'tiled-map', 'map'),
    ];
    const family = (id, name, quickApi, mark, category) => ({ id, name, quickApi, mark, category });
    /** Specialized families that add a distinct data model or reading task. */
    const seriesChartTypeCatalog = [
        family('contour', 'Contour chart', 'contour', 'contour', 'distribution'),
        family('item', 'Item chart', 'itemChart', 'item', 'radial'),
        family('vector-field', 'Vector field chart', 'vectorField', 'vector', 'cartesian'),
        family('venn', 'Venn diagram', 'venn', 'venn', 'relationship'),
        family('word-cloud', 'Word cloud', 'wordCloud', 'word-cloud', 'relationship'),
        family('price-blocks', 'Price blocks chart', 'priceBlocks', 'renko', 'financial'),
        family('volume-profile', 'Volume profile chart', 'volumeProfile', 'volume-profile', 'financial'),
        family('technical-indicator', 'Technical indicator chart', 'technicalIndicator', 'indicator', 'indicator'),
    ];
    const directFamilyByVariant = {
        area: 'area',
        bar: 'bar',
        boxplot: 'distribution',
        bubble: 'bubble',
        candlestick: 'candlestick',
        column: 'bar',
        funnel: 'funnel',
        gantt: 'timeline',
        gauge: 'gauge',
        heatmap: 'heatmap',
        histogram: 'distribution',
        line: 'line',
        map: 'map',
        pie: 'pie',
        sankey: 'flow',
        scatter: 'scatter',
        sunburst: 'hierarchy',
        timeline: 'timeline',
        treemap: 'hierarchy',
        trendline: 'line',
        waterfall: 'waterfall',
    };
    const seriesFamilyByVariant = new Map(seriesChartVariantCatalog.map(({ id, familyId }) => [id, familyId]));
    const compatibility = (identifier, variantId) => ({
        identifier,
        familyId: seriesFamilyByVariant.get(variantId) ?? directFamilyByVariant[variantId] ?? variantId,
        variantId,
    });
    /** Public series identifiers and the single catalog family that implements each one. */
    const seriesCompatibilityCatalog = [
        compatibility('abands', 'acceleration-bands'),
        compatibility('ao', 'awesome-oscillator'),
        compatibility('apo', 'absolute-price-oscillator'),
        compatibility('arcdiagram', 'arc-diagram'),
        compatibility('area', 'area'),
        compatibility('arearange', 'area-range'),
        compatibility('areaspline', 'area-spline'),
        compatibility('areasplinerange', 'area-spline-range'),
        compatibility('aroon', 'aroon'),
        compatibility('aroonoscillator', 'aroon-oscillator'),
        compatibility('atr', 'average-true-range'),
        compatibility('bar', 'bar'),
        compatibility('bb', 'volatility-bands'),
        compatibility('bellcurve', 'bell-curve'),
        compatibility('boxplot', 'boxplot'),
        compatibility('bubble', 'bubble'),
        compatibility('bullet', 'bullet'),
        compatibility('candlestick', 'candlestick'),
        compatibility('cci', 'commodity-channel-index'),
        compatibility('chaikin', 'chaikin-oscillator'),
        compatibility('cmf', 'chaikin-money-flow'),
        compatibility('cmo', 'chande-momentum-oscillator'),
        compatibility('column', 'column'),
        compatibility('columnpyramid', 'column-pyramid'),
        compatibility('columnrange', 'column-range'),
        compatibility('contour', 'contour'),
        compatibility('cylinder', 'cylinder'),
        compatibility('dema', 'double-exponential-average'),
        compatibility('dependencywheel', 'dependency-wheel'),
        compatibility('disparityindex', 'disparity-index'),
        compatibility('dmi', 'directional-movement-index'),
        compatibility('dpo', 'detrended-price-oscillator'),
        compatibility('dumbbell', 'dumbbell'),
        compatibility('ema', 'exponential-moving-average'),
        compatibility('errorbar', 'error-bar'),
        compatibility('flags', 'event-flags'),
        compatibility('flowmap', 'flow-map'),
        compatibility('funnel', 'funnel'),
        compatibility('funnel3d', 'funnel-3d'),
        compatibility('gantt', 'gantt'),
        compatibility('gauge', 'gauge'),
        compatibility('geoheatmap', 'geo-heatmap'),
        compatibility('heatmap', 'heatmap'),
        compatibility('heikinashi', 'heikin-ashi'),
        compatibility('histogram', 'histogram'),
        compatibility('hlc', 'high-low-close'),
        compatibility('hollowcandlestick', 'hollow-candlestick'),
        compatibility('ikh', 'ichimoku-cloud'),
        compatibility('item', 'item'),
        compatibility('keltnerchannels', 'keltner-channels'),
        compatibility('kagi', 'kagi'),
        compatibility('klinger', 'klinger-oscillator'),
        compatibility('line', 'line'),
        compatibility('linearregression', 'linear-regression'),
        compatibility('linearregressionangle', 'linear-regression-angle'),
        compatibility('linearregressionintercept', 'linear-regression-intercept'),
        compatibility('linearregressionslope', 'linear-regression-slope'),
        compatibility('lollipop', 'lollipop'),
        compatibility('macd', 'moving-average-convergence-divergence'),
        compatibility('map', 'map'),
        compatibility('mapbubble', 'map-bubble'),
        compatibility('mapline', 'map-line'),
        compatibility('mappoint', 'map-point'),
        compatibility('mfi', 'money-flow-index'),
        compatibility('momentum', 'momentum'),
        compatibility('natr', 'normalized-average-true-range'),
        compatibility('networkgraph', 'network-graph'),
        compatibility('obv', 'on-balance-volume'),
        compatibility('ohlc', 'open-high-low-close'),
        compatibility('organization', 'organization-network'),
        compatibility('packedbubble', 'packed-bubble'),
        compatibility('pareto', 'pareto'),
        compatibility('pc', 'price-channel'),
        compatibility('pictorial', 'pictorial-column'),
        compatibility('pie', 'pie'),
        compatibility('pivotpoints', 'pivot-points'),
        compatibility('pointandfigure', 'point-and-figure'),
        compatibility('polygon', 'polygon'),
        compatibility('ppo', 'percentage-price-oscillator'),
        compatibility('priceenvelopes', 'price-envelopes'),
        compatibility('psar', 'parabolic-stop-and-reverse'),
        compatibility('pyramid', 'pyramid'),
        compatibility('pyramid3d', 'pyramid-3d'),
        compatibility('renko', 'renko'),
        compatibility('rangebars', 'range-bars'),
        compatibility('roc', 'rate-of-change'),
        compatibility('rsi', 'relative-strength-index'),
        compatibility('sankey', 'sankey'),
        compatibility('scatter', 'scatter'),
        compatibility('scatter3d', 'scatter-3d'),
        compatibility('slowstochastic', 'slow-stochastic'),
        compatibility('sma', 'simple-moving-average'),
        compatibility('solidgauge', 'solid-gauge'),
        compatibility('spline', 'spline'),
        compatibility('stochastic', 'stochastic'),
        compatibility('streamgraph', 'streamgraph'),
        compatibility('sunburst', 'sunburst'),
        compatibility('supertrend', 'supertrend'),
        compatibility('tema', 'triple-exponential-average'),
        compatibility('tiledwebmap', 'tiled-map'),
        compatibility('tilemap', 'tile-map'),
        compatibility('timeline', 'timeline'),
        compatibility('threelinebreak', 'three-line-break'),
        compatibility('treegraph', 'tree-graph'),
        compatibility('treemap', 'treemap'),
        compatibility('trendline', 'trendline'),
        compatibility('trix', 'triple-exponential-oscillator'),
        compatibility('variablepie', 'variable-pie'),
        compatibility('variwide', 'variable-width'),
        compatibility('vbp', 'volume-by-price'),
        compatibility('vector', 'vector'),
        compatibility('venn', 'venn'),
        compatibility('vwap', 'volume-weighted-average-price'),
        compatibility('waterfall', 'waterfall'),
        compatibility('williamsr', 'williams-range'),
        compatibility('windbarb', 'wind-barb'),
        compatibility('wma', 'weighted-moving-average'),
        compatibility('wordcloud', 'word-cloud'),
        compatibility('xrange', 'x-range'),
        compatibility('zigzag', 'zigzag'),
    ];
    const seriesCompatibilityIds = seriesCompatibilityCatalog.map(({ identifier }) => identifier);
    const compatibilityByIdentifier = new Map(seriesCompatibilityCatalog.map((item) => [item.identifier, item]));
    function resolveSeriesType(identifier) {
        const normalized = identifier
            .trim()
            .toLowerCase()
            .replaceAll(/[^a-z0-9]/g, '');
        return compatibilityByIdentifier.get(normalized);
    }

    const SETTINGS = {
        standard: {
            profile: 'standard',
            maxLinePoints: 100_000,
            maxPointMarks: 25_000,
            maxBarMarks: 25_000,
            enableHitTesting: true,
            enableAnimation: true,
        },
        large: {
            profile: 'large',
            maxLinePoints: 30_000,
            maxPointMarks: 20_000,
            maxBarMarks: 12_000,
            enableHitTesting: false,
            enableAnimation: false,
        },
        ultra: {
            profile: 'ultra',
            maxLinePoints: 8_000,
            maxPointMarks: 8_000,
            maxBarMarks: 5_000,
            enableHitTesting: false,
            enableAnimation: false,
        },
    };
    function resolvePerformanceSettings(preference, rowCount, viewportWidth) {
        const profile = preference === 'auto'
            ? rowCount < 50_000
                ? 'standard'
                : rowCount < 1_000_000
                    ? 'large'
                    : 'ultra'
            : preference;
        const base = SETTINGS[profile];
        if (profile === 'standard')
            return base;
        const pointsPerPixel = profile === 'ultra' ? 2 : 4;
        const minimum = profile === 'ultra' ? 2_000 : 10_000;
        const pixelAwareLineLimit = Math.max(minimum, Math.round(viewportWidth * pointsPerPixel));
        return {
            ...base,
            maxLinePoints: Math.min(base.maxLinePoints, pixelAwareLineLimit),
        };
    }

    class GraflumeError extends Error {
        code;
        path;
        details;
        constructor(code, message, options = {}) {
            super(message, options.cause === undefined ? undefined : { cause: options.cause });
            this.name = 'GraflumeError';
            this.code = code;
            if (options.path !== undefined)
                this.path = options.path;
            if (options.details !== undefined)
                this.details = options.details;
        }
    }

    const UNSAFE_KEYS = new Set(['__proto__', 'prototype', 'constructor']);
    function assertSafeKey(key, path = key) {
        if (UNSAFE_KEYS.has(key)) {
            throw new GraflumeError('UNSAFE_KEY', `Unsafe key "${key}" is not allowed.`, { path });
        }
    }
    function isPlainObject(value) {
        if (value === null || typeof value !== 'object')
            return false;
        const prototype = Object.getPrototypeOf(value);
        return prototype === Object.prototype || prototype === null;
    }
    function deepMerge(base, override) {
        const output = { ...base };
        for (const [key, overrideValue] of Object.entries(override)) {
            assertSafeKey(key);
            if (overrideValue === undefined)
                continue;
            const baseValue = output[key];
            if (isPlainObject(baseValue) && isPlainObject(overrideValue)) {
                output[key] = deepMerge(baseValue, overrideValue);
            }
            else if (Array.isArray(overrideValue)) {
                output[key] = [...overrideValue];
            }
            else {
                output[key] = overrideValue;
            }
        }
        return output;
    }
    function ownValue(record, key) {
        assertSafeKey(key, `data.${key}`);
        return Object.prototype.hasOwnProperty.call(record, key) ? record[key] : null;
    }
    function clamp$d(value, min, max) {
        return Math.min(max, Math.max(min, value));
    }
    function finiteNumber(value) {
        if (value instanceof Date) {
            const timestamp = value.getTime();
            return Number.isFinite(timestamp) ? timestamp : null;
        }
        if (typeof value !== 'number')
            return null;
        return Number.isFinite(value) ? value : null;
    }

    const unsafeFields$2 = new Set(['__proto__', 'prototype', 'constructor']);
    const types = new Set([
        'filter',
        'sort',
        'calculate',
        'aggregate',
        'joinaggregate',
        'bin',
        'bin2d',
        'density1d',
        'density2d',
        'stack',
        'window',
        'regression',
        'fold',
        'flatten',
        'pivot',
        'impute',
        'lookup',
        'quantile',
        'sample',
        'resample',
        'timeUnit',
    ]);
    const expressionOps = new Set([
        'literal',
        'field',
        'not',
        'negate',
        'isValid',
        'toNumber',
        'toString',
        'add',
        'subtract',
        'multiply',
        'divide',
        'modulo',
        'equal',
        'notEqual',
        'lessThan',
        'lessThanOrEqual',
        'greaterThan',
        'greaterThanOrEqual',
        'and',
        'or',
        'if',
        'coalesce',
    ]);
    function issue(issues, path, message) {
        issues.push({ path, message });
    }
    function field$4(value, path, issues) {
        if (typeof value !== 'string' || value.trim() === '' || unsafeFields$2.has(value)) {
            issue(issues, path, 'Transform field must be a non-empty safe string.');
            return false;
        }
        return true;
    }
    function fields(value, path, issues, allowEmpty = false) {
        if (!Array.isArray(value) || (!allowEmpty && value.length === 0) || value.length > 256) {
            issue(issues, path, `Transform fields must be ${allowEmpty ? 'an' : 'a non-empty'} array of at most 256 fields.`);
            return false;
        }
        value.forEach((item, index) => field$4(item, `${path}[${index}]`, issues));
        return true;
    }
    function tuple(value, length, path, issues) {
        if (!Array.isArray(value) || value.length !== length) {
            issue(issues, path, `Transform output must contain exactly ${length} field names.`);
            return;
        }
        value.forEach((item, index) => field$4(item, `${path}[${index}]`, issues));
    }
    function expression(value, path, issues, depth = 0) {
        if (depth > 32 ||
            !isPlainObject(value) ||
            typeof value.op !== 'string' ||
            !expressionOps.has(value.op)) {
            issue(issues, path, depth > 32
                ? 'Transform expression exceeds 32 levels.'
                : 'Invalid closed transform expression.');
            return;
        }
        const op = value.op;
        const allowed = op === 'literal'
            ? ['op', 'value']
            : op === 'field'
                ? ['op', 'field']
                : ['not', 'negate', 'isValid', 'toNumber', 'toString'].includes(op)
                    ? ['op', 'value']
                    : op === 'if'
                        ? ['op', 'condition', 'then', 'else']
                        : op === 'coalesce'
                            ? ['op', 'values']
                            : ['op', 'left', 'right'];
        Object.keys(value).forEach((key) => {
            if (!allowed.includes(key))
                issue(issues, `${path}.${key}`, `Unknown ${op} expression property "${key}".`);
        });
        if (op === 'literal') {
            const literal = value.value;
            if (literal !== null && !['string', 'number', 'boolean'].includes(typeof literal))
                issue(issues, `${path}.value`, 'Literal must be a JSON scalar.');
            if (typeof literal === 'number' && !Number.isFinite(literal))
                issue(issues, `${path}.value`, 'Literal number must be finite.');
        }
        else if (op === 'field')
            field$4(value.field, `${path}.field`, issues);
        else if (['not', 'negate', 'isValid', 'toNumber', 'toString'].includes(op))
            expression(value.value, `${path}.value`, issues, depth + 1);
        else if (op === 'if') {
            expression(value.condition, `${path}.condition`, issues, depth + 1);
            expression(value.then, `${path}.then`, issues, depth + 1);
            expression(value.else, `${path}.else`, issues, depth + 1);
        }
        else if (op === 'coalesce') {
            if (!Array.isArray(value.values) || value.values.length === 0 || value.values.length > 32)
                issue(issues, `${path}.values`, 'Coalesce requires 1..32 expressions.');
            else
                value.values.forEach((item, index) => expression(item, `${path}.values[${index}]`, issues, depth + 1));
        }
        else {
            expression(value.left, `${path}.left`, issues, depth + 1);
            expression(value.right, `${path}.right`, issues, depth + 1);
        }
    }
    function validateTransformExpression(value, path, issues) {
        expression(value, path, issues);
    }
    function positive$1(value, path, issues, integer = false) {
        if (typeof value !== 'number' ||
            !Number.isFinite(value) ||
            value <= 0 ||
            (integer && !Number.isInteger(value)))
            issue(issues, path, `Value must be a positive${integer ? ' integer' : ''}.`);
    }
    function groupby(value, path, issues) {
        if (value !== undefined)
            fields(value, path, issues, true);
    }
    function sort(value, path, issues) {
        if (!Array.isArray(value) || value.length === 0 || value.length > 32) {
            issue(issues, path, 'Sort must contain 1..32 field declarations.');
            return;
        }
        value.forEach((item, index) => {
            const itemPath = `${path}[${index}]`;
            if (!isPlainObject(item)) {
                issue(issues, itemPath, 'Sort field must be an object.');
                return;
            }
            Object.keys(item).forEach((key) => {
                if (!['field', 'order'].includes(key))
                    issue(issues, `${itemPath}.${key}`, `Unknown sort property "${key}".`);
            });
            field$4(item.field, `${itemPath}.field`, issues);
            if (item.order !== undefined && item.order !== 'ascending' && item.order !== 'descending')
                issue(issues, `${itemPath}.order`, 'Sort order must be ascending or descending.');
        });
    }
    const keys = {
        filter: ['type', 'expr'],
        sort: ['type', 'by'],
        calculate: ['type', 'as', 'expr'],
        aggregate: ['type', 'groupby', 'fields'],
        joinaggregate: ['type', 'groupby', 'fields'],
        bin: ['type', 'field', 'as', 'maxbins', 'step', 'extent'],
        bin2d: ['type', 'x', 'y', 'as', 'maxbins'],
        density1d: ['type', 'field', 'as', 'groupby', 'points', 'bandwidth'],
        density2d: ['type', 'x', 'y', 'as', 'bins', 'bandwidth'],
        stack: ['type', 'field', 'groupby', 'series', 'sort', 'as', 'offset', 'order'],
        window: ['type', 'fields', 'groupby', 'sort', 'frame'],
        regression: ['type', 'x', 'y', 'as', 'groupby'],
        fold: ['type', 'fields', 'as'],
        flatten: ['type', 'fields', 'as'],
        pivot: ['type', 'field', 'value', 'groupby', 'op'],
        impute: ['type', 'field', 'key', 'groupby', 'method', 'value'],
        lookup: ['type', 'field', 'from', 'key', 'values', 'as', 'default'],
        quantile: ['type', 'field', 'probs', 'as', 'groupby'],
        sample: ['type', 'size', 'seed'],
        resample: ['type', 'field', 'interval', 'groupby', 'method'],
        timeUnit: ['type', 'field', 'unit', 'as', 'utc'],
    };
    function validateTransforms(value, path, issues) {
        if (value === undefined)
            return;
        if (!Array.isArray(value) || value.length > 128) {
            issue(issues, path, 'Transforms must be an ordered array of at most 128 entries.');
            return;
        }
        value.forEach((transform, index) => {
            const itemPath = `${path}[${index}]`;
            if (!isPlainObject(transform) ||
                typeof transform.type !== 'string' ||
                !types.has(transform.type)) {
                issue(issues, itemPath, 'Transform type is not supported.');
                return;
            }
            const t = transform.type;
            Object.keys(transform).forEach((key) => {
                if (!keys[t].includes(key))
                    issue(issues, `${itemPath}.${key}`, `Unknown ${t} transform property "${key}".`);
            });
            if (t === 'filter' || t === 'calculate')
                expression(transform.expr, `${itemPath}.expr`, issues);
            if (t === 'calculate')
                field$4(transform.as, `${itemPath}.as`, issues);
            if (t === 'sort')
                sort(transform.by, `${itemPath}.by`, issues);
            if (['aggregate', 'joinaggregate'].includes(t)) {
                groupby(transform.groupby, `${itemPath}.groupby`, issues);
                if (!Array.isArray(transform.fields) ||
                    transform.fields.length === 0 ||
                    transform.fields.length > 128)
                    issue(issues, `${itemPath}.fields`, 'Aggregate fields must contain 1..128 entries.');
                else
                    transform.fields.forEach((entry, entryIndex) => {
                        const entryPath = `${itemPath}.fields[${entryIndex}]`;
                        if (!isPlainObject(entry)) {
                            issue(issues, entryPath, 'Aggregate field must be an object.');
                            return;
                        }
                        Object.keys(entry).forEach((key) => {
                            if (!['op', 'field', 'weight', 'as'].includes(key))
                                issue(issues, `${entryPath}.${key}`, `Unknown aggregate field property "${key}".`);
                        });
                        if (![
                            'count',
                            'valid',
                            'missing',
                            'sum',
                            'mean',
                            'weightedMean',
                            'min',
                            'max',
                            'median',
                            'variance',
                            'stdev',
                        ].includes(String(entry.op)))
                            issue(issues, `${entryPath}.op`, 'Aggregate operation is not supported.');
                        field$4(entry.as, `${entryPath}.as`, issues);
                        if (entry.field !== undefined)
                            field$4(entry.field, `${entryPath}.field`, issues);
                        if (entry.weight !== undefined)
                            field$4(entry.weight, `${entryPath}.weight`, issues);
                        if (entry.op === 'weightedMean' &&
                            (entry.field === undefined || entry.weight === undefined))
                            issue(issues, entryPath, 'weightedMean requires field and weight.');
                        if (!['count'].includes(String(entry.op)) && entry.field === undefined)
                            issue(issues, `${entryPath}.field`, `${String(entry.op)} requires a field.`);
                    });
            }
            for (const name of ['field', 'x', 'y', 'key', 'value'])
                if (transform[name] !== undefined && !(['impute'].includes(t) && name === 'value'))
                    field$4(transform[name], `${itemPath}.${name}`, issues);
            groupby(transform.groupby, `${itemPath}.groupby`, issues);
            if (transform.series !== undefined)
                fields(transform.series, `${itemPath}.series`, issues, true);
            if (transform.sort !== undefined)
                sort(transform.sort, `${itemPath}.sort`, issues);
            if (t === 'bin')
                tuple(transform.as, 2, `${itemPath}.as`, issues);
            if (t === 'bin2d')
                tuple(transform.as, 5, `${itemPath}.as`, issues);
            if (t === 'density1d' || t === 'regression' || t === 'fold' || t === 'quantile')
                tuple(transform.as, 2, `${itemPath}.as`, issues);
            if (t === 'density2d')
                tuple(transform.as, 3, `${itemPath}.as`, issues);
            if (t === 'stack')
                tuple(transform.as, 2, `${itemPath}.as`, issues);
            if (t === 'flatten' && transform.as !== undefined)
                fields(transform.as, `${itemPath}.as`, issues);
            if (t === 'fold' || t === 'flatten')
                fields(transform.fields, `${itemPath}.fields`, issues);
            if (t === 'lookup') {
                fields(transform.values, `${itemPath}.values`, issues);
                if (transform.as !== undefined)
                    fields(transform.as, `${itemPath}.as`, issues);
                if (!Array.isArray(transform.as) ||
                    !Array.isArray(transform.values) ||
                    transform.as.length !== transform.values.length) {
                    if (transform.as !== undefined)
                        issue(issues, `${itemPath}.as`, 'Lookup as must match values length.');
                }
                if (!Array.isArray(transform.from) && !isPlainObject(transform.from))
                    issue(issues, `${itemPath}.from`, 'Lookup from must be inline data.');
            }
            if (t === 'window') {
                if (!Array.isArray(transform.fields) || transform.fields.length === 0)
                    issue(issues, `${itemPath}.fields`, 'Window fields are required.');
                else
                    transform.fields.forEach((entry, entryIndex) => {
                        const entryPath = `${itemPath}.fields[${entryIndex}]`;
                        if (!isPlainObject(entry)) {
                            issue(issues, entryPath, 'Window field must be an object.');
                            return;
                        }
                        Object.keys(entry).forEach((key) => {
                            if (!['op', 'field', 'as', 'offset'].includes(key))
                                issue(issues, `${entryPath}.${key}`, `Unknown window field property "${key}".`);
                        });
                        const op = String(entry.op);
                        if (![
                            'rowNumber',
                            'rank',
                            'denseRank',
                            'lag',
                            'lead',
                            'sum',
                            'mean',
                            'min',
                            'max',
                            'count',
                            'cumulativeSum',
                            'movingAverage',
                        ].includes(op))
                            issue(issues, `${entryPath}.op`, 'Window operation is not supported.');
                        field$4(entry.as, `${entryPath}.as`, issues);
                        if (entry.field !== undefined)
                            field$4(entry.field, `${entryPath}.field`, issues);
                        if (!['rowNumber', 'rank', 'denseRank', 'count'].includes(op) &&
                            entry.field === undefined)
                            issue(issues, `${entryPath}.field`, `${op} requires a field.`);
                        if (entry.offset !== undefined &&
                            (typeof entry.offset !== 'number' ||
                                !Number.isInteger(entry.offset) ||
                                entry.offset < 0))
                            issue(issues, `${entryPath}.offset`, 'Window offset must be a non-negative integer.');
                    });
            }
            for (const name of ['maxbins', 'points', 'bandwidth', 'size', 'interval']) {
                const current = transform[name];
                if (current !== undefined && typeof current === 'number') {
                    positive$1(current, `${itemPath}.${name}`, issues, ['maxbins', 'points', 'size'].includes(name));
                    if (name === 'points' && current > 512)
                        issue(issues, `${itemPath}.${name}`, 'density1d points must not exceed 512.');
                }
            }
            if (t === 'quantile' && transform.probs !== undefined) {
                if (!Array.isArray(transform.probs) ||
                    transform.probs.length === 0 ||
                    transform.probs.length > 512)
                    issue(issues, `${itemPath}.probs`, 'Quantile probabilities must contain 1..512 values.');
                else
                    transform.probs.forEach((probability, probabilityIndex) => {
                        if (typeof probability !== 'number' || probability < 0 || probability > 1)
                            issue(issues, `${itemPath}.probs[${probabilityIndex}]`, 'Probability must be between 0 and 1.');
                    });
            }
            if (t === 'stack') {
                if (transform.offset !== undefined &&
                    !['zero', 'normalize', 'expand', 'center', 'silhouette', 'wiggle'].includes(String(transform.offset)))
                    issue(issues, `${itemPath}.offset`, 'Stack offset is not supported.');
                if (transform.order !== undefined &&
                    ![
                        'input',
                        'ascending',
                        'descending',
                        'sumAscending',
                        'sumDescending',
                        'insideOut',
                    ].includes(String(transform.order)))
                    issue(issues, `${itemPath}.order`, 'Stack order is not supported.');
            }
            if (t === 'pivot' &&
                transform.op !== undefined &&
                !['sum', 'mean', 'min', 'max', 'count', 'first'].includes(String(transform.op)))
                issue(issues, `${itemPath}.op`, 'Pivot operation is not supported.');
            if (t === 'impute' &&
                transform.method !== undefined &&
                !['value', 'mean', 'median', 'min', 'max'].includes(String(transform.method)))
                issue(issues, `${itemPath}.method`, 'Impute method is not supported.');
            if (t === 'resample' &&
                transform.method !== undefined &&
                !['linear', 'previous', 'next'].includes(String(transform.method)))
                issue(issues, `${itemPath}.method`, 'Resample method is not supported.');
            if (t === 'timeUnit') {
                if (![
                    'year',
                    'quarter',
                    'month',
                    'week',
                    'date',
                    'day',
                    'hours',
                    'minutes',
                    'seconds',
                ].includes(String(transform.unit)))
                    issue(issues, `${itemPath}.unit`, 'Time unit is not supported.');
                if (transform.utc !== undefined && typeof transform.utc !== 'boolean')
                    issue(issues, `${itemPath}.utc`, 'Time unit utc must be boolean.');
            }
            if (t === 'bin' &&
                transform.extent !== undefined &&
                (!Array.isArray(transform.extent) ||
                    transform.extent.length !== 2 ||
                    transform.extent.some((entry) => typeof entry !== 'number' || !Number.isFinite(entry))))
                issue(issues, `${itemPath}.extent`, 'Bin extent must contain two finite numbers.');
            for (const name of ['maxbins', 'bins', 'bandwidth', 'frame']) {
                const current = transform[name];
                if (Array.isArray(current)) {
                    if (current.length !== 2)
                        issue(issues, `${itemPath}.${name}`, `${name} must contain exactly two values.`);
                    else if (name === 'frame')
                        current.forEach((entry, entryIndex) => {
                            if (entry !== null && !Number.isInteger(entry))
                                issue(issues, `${itemPath}.${name}[${entryIndex}]`, 'Window frame values must be integers or null.');
                        });
                    else
                        current.forEach((entry, entryIndex) => {
                            if (typeof entry !== 'number' ||
                                !Number.isFinite(entry) ||
                                entry <= 0 ||
                                (['maxbins', 'bins'].includes(name) && (!Number.isInteger(entry) || entry > 128)))
                                issue(issues, `${itemPath}.${name}[${entryIndex}]`, `${name} values must be positive${['maxbins', 'bins'].includes(name) ? ' integers up to 128' : ''}.`);
                        });
                }
            }
            if (transform.seed !== undefined &&
                (typeof transform.seed !== 'number' || !Number.isFinite(transform.seed)))
                issue(issues, `${itemPath}.seed`, 'Sample seed must be finite.');
        });
    }

    function isColumnarData(input) {
        return !Array.isArray(input);
    }
    function inferColumnarLength(input) {
        const entries = Object.entries(input.columns);
        if (entries.length === 0)
            return input.length ?? 0;
        const inferred = input.length ?? entries[0]?.[1].length ?? 0;
        for (const [name, column] of entries) {
            assertSafeKey(name, `data.columns.${name}`);
            if (column.length !== inferred) {
                throw new GraflumeError('INVALID_DATA', `Column "${name}" has length ${column.length}; expected ${inferred}.`, { path: `data.columns.${name}` });
            }
        }
        return inferred;
    }
    class DataTable {
        #columns = new Map();
        #length = 0;
        static from(input) {
            return isColumnarData(input) ? DataTable.fromColumns(input) : DataTable.fromRows(input);
        }
        static fromRows(rows) {
            const table = new DataTable();
            const fieldOrder = [];
            const fields = new Set();
            for (const row of rows) {
                for (const key of Object.keys(row)) {
                    assertSafeKey(key, `data.${key}`);
                    if (!fields.has(key)) {
                        fields.add(key);
                        fieldOrder.push(key);
                    }
                }
            }
            for (const field of fieldOrder) {
                const column = rows.map((row) => ownValue(row, field));
                table.#columns.set(field, column);
            }
            table.#length = rows.length;
            return table;
        }
        static fromColumns(input) {
            const table = new DataTable();
            table.#length = inferColumnarLength(input);
            for (const [name, column] of Object.entries(input.columns)) {
                assertSafeKey(name, `data.columns.${name}`);
                table.#columns.set(name, column);
            }
            return table;
        }
        get length() {
            return this.#length;
        }
        fields() {
            return [...this.#columns.keys()];
        }
        has(field) {
            assertSafeKey(field, `data.${field}`);
            return this.#columns.has(field);
        }
        column(field) {
            assertSafeKey(field, `data.${field}`);
            const column = this.#columns.get(field);
            if (column === undefined) {
                throw new GraflumeError('INVALID_DATA', `Data field "${field}" does not exist.`, {
                    path: `data.${field}`,
                    details: { availableFields: this.fields() },
                });
            }
            return column;
        }
        value(rowIndex, field) {
            if (!Number.isInteger(rowIndex) || rowIndex < 0 || rowIndex >= this.#length) {
                throw new GraflumeError('INVALID_DATA', `Row index ${rowIndex} is out of bounds.`, {
                    path: `data[${rowIndex}]`,
                });
            }
            return this.column(field)[rowIndex];
        }
        numericValue(rowIndex, field) {
            const value = this.value(rowIndex, field);
            if (typeof value === 'string') {
                const timestamp = Date.parse(value);
                return Number.isFinite(timestamp) ? timestamp : null;
            }
            return finiteNumber(value);
        }
        extent(field, asTemporal = false) {
            const column = this.column(field);
            let min = Number.POSITIVE_INFINITY;
            let max = Number.NEGATIVE_INFINITY;
            for (let index = 0; index < this.#length; index += 1) {
                const raw = column[index];
                let value;
                if (asTemporal && typeof raw === 'string') {
                    const timestamp = Date.parse(raw);
                    value = Number.isFinite(timestamp) ? timestamp : null;
                }
                else {
                    value = finiteNumber(raw);
                }
                if (value === null)
                    continue;
                min = Math.min(min, value);
                max = Math.max(max, value);
            }
            return Number.isFinite(min) && Number.isFinite(max) ? [min, max] : null;
        }
        unique(field) {
            const column = this.column(field);
            const values = new Set();
            for (let index = 0; index < this.#length; index += 1) {
                const value = column[index];
                if (value === null || value === undefined)
                    continue;
                values.add(value instanceof Date ? value.toISOString() : String(value));
            }
            return [...values];
        }
        row(index) {
            if (!Number.isInteger(index) || index < 0 || index >= this.#length) {
                throw new GraflumeError('INVALID_DATA', `Row index ${index} is out of bounds.`);
            }
            const row = Object.create(null);
            for (const [field, column] of this.#columns)
                row[field] = column[index];
            return row;
        }
        append(rows) {
            if (rows.length === 0)
                return;
            const allFields = new Set(this.fields());
            for (const row of rows) {
                for (const field of Object.keys(row)) {
                    assertSafeKey(field, `data.${field}`);
                    allFields.add(field);
                }
            }
            for (const field of allFields) {
                const existing = this.#columns.get(field);
                const mutable = existing === undefined ? Array(this.#length).fill(null) : Array.from(existing);
                for (const row of rows)
                    mutable.push(ownValue(row, field));
                this.#columns.set(field, mutable);
            }
            this.#length += rows.length;
        }
    }

    function sorted$1(input) {
        return input
            .filter(({ value, weight }) => Number.isFinite(value) && Number.isFinite(weight) && weight >= 0)
            .map((observation, index) => ({ observation, index }))
            .sort((a, b) => a.observation.value - b.observation.value || a.index - b.index)
            .map(({ observation }) => observation);
    }
    function quantile$3(values, probability) {
        const position = Math.max(0, Math.min(1, probability)) * (values.length - 1);
        const lower = Math.floor(position);
        const upper = Math.ceil(position);
        const a = values[lower];
        const b = values[upper];
        return a + (b - a) * (position - lower);
    }
    function weightedQuantile(observations, probability) {
        const totalWeight = observations.reduce((sum, observation) => sum + observation.weight, 0);
        if (totalWeight <= 0)
            return observations[0]?.value ?? 0;
        const target = Math.max(0, Math.min(1, probability)) * totalWeight;
        let cumulative = 0;
        for (const observation of observations) {
            cumulative += observation.weight;
            if (cumulative >= target)
                return observation.value;
        }
        return observations.at(-1)?.value ?? 0;
    }
    function weightedHistogram(input, options) {
        const observations = sorted$1(input);
        if (observations.length === 0)
            return [];
        const minimum = options.extent?.[0] ?? observations[0].value;
        const maximum = options.extent?.[1] ?? observations.at(-1).value;
        const bins = Math.max(1, Math.min(512, Math.trunc(options.bins)));
        const span = maximum - minimum || 1;
        const width = span / bins;
        const accumulators = Array.from({ length: bins }, () => ({
            weight: 0,
            count: 0,
            rows: [],
        }));
        for (const observation of observations) {
            if (observation.value < minimum || observation.value > maximum)
                continue;
            const index = Math.min(bins - 1, Math.max(0, Math.floor(((observation.value - minimum) / span) * bins)));
            const accumulator = accumulators[index];
            accumulator.weight += observation.weight;
            accumulator.count += 1;
            accumulator.rows.push(observation.rowIndex);
        }
        const totalWeight = accumulators.reduce((sum, bin) => sum + bin.weight, 0);
        let cumulativeWeight = 0;
        return accumulators.map((bin, index) => {
            cumulativeWeight += bin.weight;
            const effectiveWeight = options.cumulative === true ? cumulativeWeight : bin.weight;
            const normalization = options.normalization ?? 'count';
            const value = normalization === 'probability'
                ? totalWeight === 0
                    ? 0
                    : effectiveWeight / totalWeight
                : normalization === 'density'
                    ? totalWeight === 0
                        ? 0
                        : options.cumulative === true
                            ? effectiveWeight / totalWeight
                            : effectiveWeight / (totalWeight * width)
                    : effectiveWeight;
            return {
                start: minimum + index * width,
                end: minimum + (index + 1) * width,
                value,
                weight: bin.weight,
                count: bin.count,
                proportion: totalWeight === 0 ? 0 : bin.weight / totalWeight,
                rowIndices: bin.rows,
            };
        });
    }
    function empiricalDistribution(input, complementary = false) {
        const observations = sorted$1(input);
        const totalWeight = observations.reduce((sum, observation) => sum + observation.weight, 0);
        if (observations.length === 0 || totalWeight === 0)
            return [];
        const groups = new Map();
        for (const observation of observations) {
            const group = groups.get(observation.value) ?? [];
            group.push(observation);
            groups.set(observation.value, group);
        }
        let cumulative = 0;
        return [...groups].map(([value, group]) => {
            const weight = group.reduce((sum, observation) => sum + observation.weight, 0);
            cumulative += weight;
            return {
                value,
                probability: complementary
                    ? Math.max(0, 1 - cumulative / totalWeight)
                    : cumulative / totalWeight,
                weight,
                count: group.length,
                rowIndices: group.map(({ rowIndex }) => rowIndex),
            };
        });
    }
    /** Gaussian KDE with a robust Silverman bandwidth and unit-integral density. */
    function kernelDensity1d(input, options = {}) {
        const observations = sorted$1(input).filter(({ weight }) => weight > 0);
        if (observations.length === 0)
            return { bandwidth: 1, points: [] };
        const totalWeight = observations.reduce((sum, observation) => sum + observation.weight, 0);
        const squaredWeight = observations.reduce((sum, observation) => sum + observation.weight ** 2, 0);
        const effectiveSampleSize = Math.max(1, totalWeight ** 2 / squaredWeight);
        const values = observations.map(({ value }) => value);
        const mean = observations.reduce((sum, observation) => sum + observation.value * observation.weight, 0) /
            totalWeight;
        const varianceDenominator = totalWeight - squaredWeight / totalWeight;
        const deviation = Math.sqrt(observations.reduce((sum, observation) => sum + observation.weight * (observation.value - mean) ** 2, 0) / Math.max(Number.EPSILON, varianceDenominator));
        const iqr = weightedQuantile(observations, 0.75) - weightedQuantile(observations, 0.25);
        const robustScale = Math.min(deviation || Number.POSITIVE_INFINITY, iqr > 0 ? iqr / 1.34 : Number.POSITIVE_INFINITY);
        const fallback = (values.at(-1) - values[0]) / 6 || Math.abs(mean) * 0.1 || 1;
        const automaticBandwidth = Math.max(Number.EPSILON, 0.9 * (Number.isFinite(robustScale) ? robustScale : fallback) * effectiveSampleSize ** -0.2);
        const bandwidth = options.bandwidth !== undefined && Number.isFinite(options.bandwidth) && options.bandwidth > 0
            ? options.bandwidth
            : automaticBandwidth;
        const extentStart = options.extent?.[0] ?? values[0] - bandwidth * 3;
        const extentEnd = options.extent?.[1] ?? values.at(-1) + bandwidth * 3;
        const minimum = Math.min(extentStart, extentEnd);
        const maximum = Math.max(extentStart, extentEnd);
        const count = Math.max(2, Math.min(512, Math.trunc(options.points ?? 96)));
        const normalizer = bandwidth * Math.sqrt(2 * Math.PI) * totalWeight;
        return {
            bandwidth,
            points: Array.from({ length: count }, (_, index) => {
                const value = minimum + ((maximum - minimum || 1) * index) / (count - 1);
                const density = observations.reduce((sum, observation) => {
                    const z = (value - observation.value) / bandwidth;
                    return sum + observation.weight * Math.exp(-0.5 * z * z);
                }, 0) / normalizer;
                return { value, density };
            }),
        };
    }
    function rawBoxSummary(input, whisker = 1.5) {
        const observations = sorted$1(input);
        if (observations.length === 0)
            return null;
        const values = observations.map(({ value }) => value);
        const q1 = quantile$3(values, 0.25);
        const median = quantile$3(values, 0.5);
        const q3 = quantile$3(values, 0.75);
        const iqr = q3 - q1;
        const lowerFence = q1 - Math.max(0, whisker) * iqr;
        const upperFence = q3 + Math.max(0, whisker) * iqr;
        const inliers = observations.filter(({ value }) => value >= lowerFence && value <= upperFence);
        const outliers = observations.filter(({ value }) => value < lowerFence || value > upperFence);
        return {
            minimum: values[0],
            q1,
            median,
            q3,
            maximum: values.at(-1),
            lowerWhisker: inliers[0]?.value ?? values[0],
            upperWhisker: inliers.at(-1)?.value ?? values.at(-1),
            outliers,
            rowIndices: observations.map(({ rowIndex }) => rowIndex),
        };
    }

    const unsafeFields$1 = new Set(['__proto__', 'prototype', 'constructor']);
    const maximumDerivedRows = 100_000;
    const maximumKernelEvaluations = 10_000_000;
    const maximumStackMatrixCells = 1_000_000;
    const maximumWindowEvaluations = 10_000_000;
    function enforceWorkBudget(count, limit, operation, path) {
        if (!Number.isFinite(count) || count > limit) {
            throw new GraflumeError('INVALID_DATA', `${operation} would require ${Number.isFinite(count) ? Math.ceil(count) : 'an unbounded number of'} derived operations; the deterministic limit is ${limit}.`, { path });
        }
    }
    function safeField(field) {
        if (field.trim() === '' || unsafeFields$1.has(field)) {
            throw new GraflumeError('INVALID_SPEC', `Unsafe transform field "${field}".`, {
                path: '$.transform',
            });
        }
        return field;
    }
    function cloneRecord(row) {
        const output = Object.create(null);
        for (const [field, value] of Object.entries(row)) {
            safeField(field);
            output[field] = value;
        }
        return output;
    }
    function jsonSafe(value) {
        if (value === null || typeof value === 'string' || typeof value === 'boolean')
            return value;
        if (typeof value === 'number')
            return Number.isFinite(value) ? value : null;
        if (value instanceof Date)
            return value.toISOString();
        if (ArrayBuffer.isView(value)) {
            return Array.from(value, (entry) => jsonSafe(entry));
        }
        if (Array.isArray(value))
            return value.map(jsonSafe);
        if (typeof value === 'object' && value !== null) {
            const output = Object.create(null);
            for (const [key, child] of Object.entries(value)) {
                safeField(key);
                if (child !== undefined)
                    output[key] = jsonSafe(child);
            }
            return output;
        }
        throw new GraflumeError('INVALID_SPEC', 'Transform lineage parameters must be JSON-safe.', {
            path: '$.transform',
        });
    }
    function inputRows(input) {
        const table = DataTable.from(input);
        return Array.from({ length: table.length }, (_, index) => ({
            value: cloneRecord(table.row(index)),
            sources: [index],
        }));
    }
    function scalar(value) {
        return value === undefined ||
            value === null ||
            value instanceof Date ||
            ['string', 'number', 'boolean'].includes(typeof value)
            ? value
            : String(value);
    }
    function numeric$2(value) {
        if (typeof value === 'number')
            return Number.isFinite(value) ? value : null;
        if (value instanceof Date)
            return value.getTime();
        if (typeof value === 'string' && value.trim() !== '') {
            const number = Number(value);
            if (Number.isFinite(number))
                return number;
            const date = Date.parse(value);
            return Number.isFinite(date) ? date : null;
        }
        return null;
    }
    function truthy$1(value) {
        return value !== false && value !== null && value !== undefined && value !== 0 && value !== '';
    }
    function evaluate(expr, row) {
        switch (expr.op) {
            case 'literal':
                return expr.value;
            case 'field':
                return row[safeField(expr.field)];
            case 'not':
                return !truthy$1(evaluate(expr.value, row));
            case 'negate': {
                const value = numeric$2(evaluate(expr.value, row));
                return value === null ? null : -value;
            }
            case 'isValid': {
                const value = evaluate(expr.value, row);
                return (value !== null &&
                    value !== undefined &&
                    !(typeof value === 'number' && !Number.isFinite(value)));
            }
            case 'toNumber':
                return numeric$2(evaluate(expr.value, row));
            case 'toString': {
                const value = evaluate(expr.value, row);
                return value === null || value === undefined
                    ? ''
                    : value instanceof Date
                        ? value.toISOString()
                        : String(value);
            }
            case 'if':
                return evaluate(truthy$1(evaluate(expr.condition, row)) ? expr.then : expr.else, row);
            case 'coalesce': {
                for (const item of expr.values) {
                    const value = evaluate(item, row);
                    if (value !== null && value !== undefined)
                        return value;
                }
                return null;
            }
            default: {
                const left = evaluate(expr.left, row);
                if (expr.op === 'and' && !truthy$1(left))
                    return false;
                if (expr.op === 'or' && truthy$1(left))
                    return true;
                const right = evaluate(expr.right, row);
                const a = numeric$2(left);
                const b = numeric$2(right);
                switch (expr.op) {
                    case 'add':
                        return a === null || b === null ? `${left ?? ''}${right ?? ''}` : a + b;
                    case 'subtract':
                        return a === null || b === null ? null : a - b;
                    case 'multiply':
                        return a === null || b === null ? null : a * b;
                    case 'divide':
                        return a === null || b === null || b === 0 ? null : a / b;
                    case 'modulo':
                        return a === null || b === null || b === 0 ? null : a % b;
                    case 'equal':
                        return Object.is(left, right);
                    case 'notEqual':
                        return !Object.is(left, right);
                    case 'lessThan':
                        return a !== null && b !== null ? a < b : String(left) < String(right);
                    case 'lessThanOrEqual':
                        return a !== null && b !== null ? a <= b : String(left) <= String(right);
                    case 'greaterThan':
                        return a !== null && b !== null ? a > b : String(left) > String(right);
                    case 'greaterThanOrEqual':
                        return a !== null && b !== null ? a >= b : String(left) >= String(right);
                    case 'and':
                        return truthy$1(right);
                    case 'or':
                        return truthy$1(right);
                }
            }
        }
    }
    function keyOf(row, fields) {
        return JSON.stringify(fields.map((field) => {
            const value = row[safeField(field)];
            return value instanceof Date ? { date: value.toISOString() } : (value ?? null);
        }));
    }
    function compareValue(a, b) {
        if (a === b)
            return 0;
        if (a === null || a === undefined)
            return 1;
        if (b === null || b === undefined)
            return -1;
        const an = numeric$2(a);
        const bn = numeric$2(b);
        if (an !== null && bn !== null)
            return an - bn;
        return String(a).localeCompare(String(b), 'en');
    }
    function sorted(rows, by) {
        return rows
            .map((row, index) => ({ row, index }))
            .sort((a, b) => {
            for (const sort of by) {
                const result = compareValue(a.row.value[safeField(sort.field)], b.row.value[safeField(sort.field)]);
                if (result !== 0)
                    return sort.order === 'descending' ? -result : result;
            }
            return a.index - b.index;
        })
            .map(({ row }) => row);
    }
    function groups(rows, fields = []) {
        const grouped = new Map();
        for (const row of rows) {
            const key = keyOf(row.value, fields);
            const group = grouped.get(key) ?? [];
            group.push(row);
            grouped.set(key, group);
        }
        return [...grouped.values()];
    }
    function values(group, field) {
        if (field === undefined)
            return [];
        return group.flatMap(({ value }) => {
            const number = numeric$2(value[safeField(field)]);
            return number === null ? [] : [number];
        });
    }
    function quantileValue(input, probability) {
        if (input.length === 0)
            return null;
        const ordered = [...input].sort((a, b) => a - b);
        return quantileFromSorted(ordered, probability);
    }
    function quantileFromSorted(ordered, probability) {
        if (ordered.length === 0)
            return null;
        const position = Math.max(0, Math.min(1, probability)) * (ordered.length - 1);
        const low = Math.floor(position);
        const high = Math.ceil(position);
        const a = ordered[low];
        const b = ordered[high];
        return a + (b - a) * (position - low);
    }
    function aggregateValue(group, spec) {
        const numbers = values(group, spec.field);
        switch (spec.op) {
            case 'count':
                return group.length;
            case 'valid':
                return spec.field === undefined
                    ? group.length
                    : group.length - group.filter(({ value }) => value[spec.field] == null).length;
            case 'missing':
                return spec.field === undefined
                    ? 0
                    : group.filter(({ value }) => value[spec.field] == null).length;
            case 'sum':
                return numbers.reduce((sum, value) => sum + value, 0);
            case 'mean':
                return numbers.length === 0
                    ? null
                    : numbers.reduce((sum, value) => sum + value, 0) / numbers.length;
            case 'weightedMean': {
                if (spec.field === undefined || spec.weight === undefined)
                    return null;
                let weighted = 0;
                let weights = 0;
                for (const { value } of group) {
                    const number = numeric$2(value[spec.field]);
                    const weight = numeric$2(value[spec.weight]);
                    if (number === null || weight === null)
                        continue;
                    weighted += number * weight;
                    weights += weight;
                }
                return weights === 0 ? null : weighted / weights;
            }
            case 'min':
                return numbers.length === 0
                    ? null
                    : numbers.reduce((minimum, value) => Math.min(minimum, value), Number.POSITIVE_INFINITY);
            case 'max':
                return numbers.length === 0
                    ? null
                    : numbers.reduce((maximum, value) => Math.max(maximum, value), Number.NEGATIVE_INFINITY);
            case 'median':
                return quantileValue(numbers, 0.5);
            case 'variance': {
                if (numbers.length < 2)
                    return null;
                const mean = numbers.reduce((sum, value) => sum + value, 0) / numbers.length;
                return numbers.reduce((sum, value) => sum + (value - mean) ** 2, 0) / (numbers.length - 1);
            }
            case 'stdev': {
                const variance = aggregateValue(group, { ...spec, op: 'variance' });
                return typeof variance === 'number' ? Math.sqrt(variance) : null;
            }
        }
    }
    function aggregate(rows, transform) {
        const groupby = transform.groupby ?? [];
        return groups(rows, groupby).flatMap((group) => {
            const aggregated = Object.create(null);
            for (const field of groupby)
                aggregated[field] = group[0]?.value[field];
            for (const field of transform.fields)
                aggregated[safeField(field.as)] = aggregateValue(group, field);
            const sources = [...new Set(group.flatMap((row) => row.sources))].sort((a, b) => a - b);
            if (transform.type === 'aggregate')
                return [{ value: aggregated, sources }];
            return group.map((row) => ({ value: { ...row.value, ...aggregated }, sources: row.sources }));
        });
    }
    function extent$2(rows, field) {
        const found = values(rows, field);
        if (found.length === 0)
            return null;
        let minimum = Number.POSITIVE_INFINITY;
        let maximum = Number.NEGATIVE_INFINITY;
        for (const value of found) {
            minimum = Math.min(minimum, value);
            maximum = Math.max(maximum, value);
        }
        return [minimum, maximum];
    }
    function binStep(domain, maxbins) {
        const span = Math.max(Number.EPSILON, domain[1] - domain[0]);
        const raw = span / Math.max(1, maxbins);
        const power = 10 ** Math.floor(Math.log10(raw));
        const error = raw / power;
        return (error >= 5 ? 5 : error >= 2 ? 2 : 1) * power;
    }
    function bin(rows, transform) {
        const domain = transform.extent ?? extent$2(rows, transform.field);
        if (domain === null)
            return rows.map((row) => ({
                ...row,
                value: { ...row.value, [transform.as[0]]: null, [transform.as[1]]: null },
            }));
        const step = transform.step ?? binStep(domain, transform.maxbins ?? 10);
        return rows.map((row) => {
            const value = numeric$2(row.value[transform.field]);
            const start = value === null ? null : Math.floor((value - domain[0]) / step) * step + domain[0];
            return {
                ...row,
                value: {
                    ...row.value,
                    [transform.as[0]]: start,
                    [transform.as[1]]: start === null ? null : start + step,
                },
            };
        });
    }
    function density1d(rows, transform) {
        let kernelEvaluations = 0;
        return groups(rows, transform.groupby).flatMap((group) => {
            const input = group.flatMap((row, index) => {
                const value = numeric$2(row.value[transform.field]);
                return value === null ? [] : [{ value, weight: 1, rowIndex: row.sources[0] ?? index }];
            });
            if (input.length === 0)
                return [];
            const pointCount = Math.max(2, transform.points ?? 64);
            kernelEvaluations += input.length * pointCount;
            enforceWorkBudget(kernelEvaluations, maximumKernelEvaluations, 'density1d', '$.transform[].points');
            let minimum = Number.POSITIVE_INFINITY;
            let maximum = Number.NEGATIVE_INFINITY;
            for (const { value } of input) {
                minimum = Math.min(minimum, value);
                maximum = Math.max(maximum, value);
            }
            const estimate = kernelDensity1d(input, {
                points: pointCount,
                extent: [minimum, maximum],
                ...(transform.bandwidth === undefined ? {} : { bandwidth: transform.bandwidth }),
            });
            const prefix = Object.create(null);
            for (const field of transform.groupby ?? [])
                prefix[field] = group[0]?.value[field];
            const sources = [...new Set(group.flatMap((row) => row.sources))].sort((a, b) => a - b);
            return estimate.points.map(({ value, density }) => ({
                value: { ...prefix, [transform.as[0]]: value, [transform.as[1]]: density },
                sources,
            }));
        });
    }
    function stack(rows, transform) {
        const seriesFields = transform.series ?? [];
        const buckets = groups(rows, transform.groupby);
        const seriesTotals = new Map();
        const seriesOrder = [];
        for (const row of rows) {
            const key = keyOf(row.value, seriesFields);
            if (!seriesTotals.has(key))
                seriesOrder.push(key);
            seriesTotals.set(key, (seriesTotals.get(key) ?? 0) + (numeric$2(row.value[transform.field]) ?? 0));
        }
        if (transform.order === 'insideOut' || (transform.offset ?? 'zero') === 'wiggle') {
            enforceWorkBudget(buckets.length * seriesOrder.length, maximumStackMatrixCells, 'Stack layout', '$.transform[].order');
        }
        const matrixSeriesOrder = [...seriesOrder];
        const stackMatrix = transform.order === 'insideOut' || (transform.offset ?? 'zero') === 'wiggle'
            ? (() => {
                const seriesIndex = new Map(matrixSeriesOrder.map((key, index) => [key, index]));
                return buckets.map((bucket) => {
                    const totals = Array.from({ length: matrixSeriesOrder.length }, () => 0);
                    for (const row of bucket) {
                        const index = seriesIndex.get(keyOf(row.value, seriesFields));
                        if (index === undefined)
                            continue;
                        totals[index] =
                            totals[index] + Math.max(0, numeric$2(row.value[transform.field]) ?? 0);
                    }
                    return totals;
                });
            })()
            : undefined;
        if (transform.order === 'insideOut') {
            const appearances = seriesOrder
                .map((key, index) => {
                let peakIndex = 0;
                let peakValue = Number.NEGATIVE_INFINITY;
                stackMatrix.forEach((bucket, bucketIndex) => {
                    const value = bucket[index] ?? 0;
                    if (value > peakValue) {
                        peakValue = value;
                        peakIndex = bucketIndex;
                    }
                });
                return { key, index, peakIndex, total: seriesTotals.get(key) ?? 0 };
            })
                .sort((a, b) => a.peakIndex - b.peakIndex || a.index - b.index);
            const top = [];
            const bottom = [];
            let topTotal = 0;
            let bottomTotal = 0;
            for (const series of appearances) {
                if (topTotal < bottomTotal) {
                    top.push(series.key);
                    topTotal += series.total;
                }
                else {
                    bottom.push(series.key);
                    bottomTotal += series.total;
                }
            }
            seriesOrder.splice(0, seriesOrder.length, ...bottom.reverse(), ...top);
        }
        else if (transform.order !== undefined && transform.order !== 'input') {
            const sign = transform.order === 'descending' || transform.order === 'sumDescending' ? -1 : 1;
            seriesOrder.sort((a, b) => sign * ((seriesTotals.get(a) ?? 0) - (seriesTotals.get(b) ?? 0)));
        }
        const orderedBuckets = buckets.map((bucket) => {
            const authored = transform.sort === undefined ? [...bucket] : sorted(bucket, transform.sort);
            if (seriesFields.length === 0)
                return authored;
            const seriesIndex = new Map(seriesOrder.map((key, index) => [key, index]));
            return authored
                .map((row, index) => ({ row, index }))
                .sort((a, b) => (seriesIndex.get(keyOf(a.row.value, seriesFields)) ?? 0) -
                (seriesIndex.get(keyOf(b.row.value, seriesFields)) ?? 0) || a.index - b.index)
                .map(({ row }) => row);
        });
        const wiggleBaselines = Array.from({ length: orderedBuckets.length }, () => 0);
        if ((transform.offset ?? 'zero') === 'wiggle' &&
            rows.some((row) => (numeric$2(row.value[transform.field]) ?? 0) < 0)) {
            throw new GraflumeError('INVALID_DATA', 'The wiggle stack offset requires non-negative values; use zero, normalize, or center for diverging data.', { path: '$.transform[].offset' });
        }
        if ((transform.offset ?? 'zero') === 'wiggle' && orderedBuckets.length > 1) {
            let baseline = 0;
            let minimum = 0;
            const originalSeriesIndex = new Map(matrixSeriesOrder.map((key, index) => [key, index]));
            const matrix = stackMatrix.map((bucket) => seriesOrder.map((key) => bucket[originalSeriesIndex.get(key) ?? 0] ?? 0));
            for (let bucketIndex = 1; bucketIndex < matrix.length; bucketIndex += 1) {
                const current = matrix[bucketIndex];
                const previous = matrix[bucketIndex - 1];
                const total = current.reduce((sum, value) => sum + value, 0);
                let weightedSlope = 0;
                let precedingSlope = 0;
                for (let seriesIndex = 0; seriesIndex < current.length; seriesIndex += 1) {
                    const slope = current[seriesIndex] - previous[seriesIndex];
                    weightedSlope += (slope / 2 + precedingSlope) * current[seriesIndex];
                    precedingSlope += slope;
                }
                baseline -= total === 0 ? 0 : weightedSlope / total;
                wiggleBaselines[bucketIndex] = baseline;
                minimum = Math.min(minimum, baseline);
            }
            for (let index = 0; index < wiggleBaselines.length; index += 1) {
                wiggleBaselines[index] = wiggleBaselines[index] - minimum;
            }
        }
        return orderedBuckets.flatMap((ordered, bucketIndex) => {
            const positiveTotal = ordered.reduce((sum, row) => sum + Math.max(0, numeric$2(row.value[transform.field]) ?? 0), 0);
            const negativeTotal = ordered.reduce((sum, row) => sum + Math.max(0, -(numeric$2(row.value[transform.field]) ?? 0)), 0);
            const absoluteTotal = positiveTotal + negativeTotal;
            const offset = transform.offset ?? 'zero';
            const centered = offset === 'center' || offset === 'silhouette';
            const normalized = offset === 'normalize' || offset === 'expand';
            const baseline = centered
                ? (negativeTotal - positiveTotal) / 2
                : offset === 'wiggle'
                    ? wiggleBaselines[bucketIndex]
                    : 0;
            let positiveCursor = baseline;
            let negativeCursor = baseline;
            return ordered.map((row) => {
                let amount = numeric$2(row.value[transform.field]) ?? 0;
                if (normalized)
                    amount = absoluteTotal === 0 ? 0 : amount / absoluteTotal;
                let start;
                let end;
                if (amount < 0) {
                    end = negativeCursor;
                    negativeCursor += amount;
                    start = negativeCursor;
                }
                else {
                    start = positiveCursor;
                    positiveCursor += amount;
                    end = positiveCursor;
                }
                return { ...row, value: { ...row.value, [transform.as[0]]: start, [transform.as[1]]: end } };
            });
        });
    }
    function frameRows(group, index, frame) {
        const start = frame[0] === null ? 0 : Math.max(0, index + frame[0]);
        const end = frame[1] === null ? group.length - 1 : Math.min(group.length - 1, index + frame[1]);
        return start > end ? [] : group.slice(start, end + 1);
    }
    function frameLength(length, index, frame) {
        const start = frame[0] === null ? 0 : Math.max(0, index + frame[0]);
        const end = frame[1] === null ? length - 1 : Math.min(length - 1, index + frame[1]);
        return Math.max(0, end - start + 1);
    }
    function windowValue(group, index, spec, frame) {
        if (spec.op === 'rowNumber')
            return index + 1;
        if (spec.op === 'lag' || spec.op === 'lead') {
            const direction = spec.op === 'lag' ? -1 : 1;
            return group[index + direction * (spec.offset ?? 1)]?.value[spec.field ?? ''] ?? null;
        }
        if (spec.op === 'rank' || spec.op === 'denseRank')
            return index + 1;
        const selected = spec.op === 'cumulativeSum' ? group.slice(0, index + 1) : frameRows(group, index, frame);
        if (spec.op === 'count')
            return selected.length;
        const input = values(selected, spec.field);
        if (spec.op === 'sum' || spec.op === 'cumulativeSum')
            return input.reduce((sum, value) => sum + value, 0);
        if (spec.op === 'mean' || spec.op === 'movingAverage')
            return input.length === 0 ? null : input.reduce((sum, value) => sum + value, 0) / input.length;
        if (spec.op === 'min')
            return input.length === 0
                ? null
                : input.reduce((minimum, value) => Math.min(minimum, value), Number.POSITIVE_INFINITY);
        if (spec.op === 'max')
            return input.length === 0
                ? null
                : input.reduce((maximum, value) => Math.max(maximum, value), Number.NEGATIVE_INFINITY);
        return null;
    }
    function windowTransform(rows, transform) {
        let windowEvaluations = 0;
        return groups(rows, transform.groupby).flatMap((raw) => {
            const group = transform.sort === undefined ? raw : sorted(raw, transform.sort);
            const frame = transform.frame ?? [null, 0];
            const aggregateFields = transform.fields.filter((field) => ['sum', 'mean', 'min', 'max', 'count', 'cumulativeSum', 'movingAverage'].includes(field.op));
            for (const field of aggregateFields) {
                for (let index = 0; index < group.length; index += 1) {
                    windowEvaluations +=
                        field.op === 'cumulativeSum' ? index + 1 : frameLength(group.length, index, frame);
                }
            }
            enforceWorkBudget(windowEvaluations, maximumWindowEvaluations, 'window transform', '$.transform[].frame');
            const rankKeys = transform.sort === undefined
                ? group.map((_, index) => String(index))
                : group.map((row) => keyOf(row.value, transform.sort.map(({ field }) => field)));
            const ranks = [];
            const denseRanks = [];
            let dense = 0;
            for (let index = 0; index < rankKeys.length; index += 1) {
                if (index === 0 || rankKeys[index] !== rankKeys[index - 1])
                    dense += 1;
                ranks[index] =
                    index === 0 || rankKeys[index] !== rankKeys[index - 1] ? index + 1 : ranks[index - 1];
                denseRanks[index] = dense;
            }
            return group.map((row, index) => {
                const value = { ...row.value };
                for (const field of transform.fields) {
                    if (field.op === 'rank')
                        value[field.as] = ranks[index];
                    else if (field.op === 'denseRank')
                        value[field.as] = denseRanks[index];
                    else
                        value[field.as] = windowValue(group, index, field, frame);
                }
                return { ...row, value };
            });
        });
    }
    function regression$1(rows, transform) {
        return groups(rows, transform.groupby).flatMap((group) => {
            const pairs = group.flatMap((row) => {
                const x = numeric$2(row.value[transform.x]);
                const y = numeric$2(row.value[transform.y]);
                return x === null || y === null ? [] : [{ x, y }];
            });
            if (pairs.length < 2)
                return [];
            const meanX = pairs.reduce((sum, pair) => sum + pair.x, 0) / pairs.length;
            const meanY = pairs.reduce((sum, pair) => sum + pair.y, 0) / pairs.length;
            const denominator = pairs.reduce((sum, pair) => sum + (pair.x - meanX) ** 2, 0);
            const slope = denominator === 0
                ? 0
                : pairs.reduce((sum, pair) => sum + (pair.x - meanX) * (pair.y - meanY), 0) / denominator;
            const intercept = meanY - slope * meanX;
            const prefix = Object.create(null);
            for (const field of transform.groupby ?? [])
                prefix[field] = group[0]?.value[field];
            const sources = [...new Set(group.flatMap((row) => row.sources))].sort((a, b) => a - b);
            const xExtent = pairs.reduce(([minimum, maximum], { x }) => [Math.min(minimum, x), Math.max(maximum, x)], [Number.POSITIVE_INFINITY, Number.NEGATIVE_INFINITY]);
            return xExtent.map((x) => ({
                value: { ...prefix, [transform.as[0]]: x, [transform.as[1]]: intercept + slope * x },
                sources,
            }));
        });
    }
    function pivotAggregate(rows, field, op) {
        if (op === 'first')
            return rows[0]?.value[field];
        if (op === 'count')
            return rows.length;
        const input = values(rows, field);
        if (input.length === 0)
            return null;
        if (op === 'sum')
            return input.reduce((a, b) => a + b, 0);
        if (op === 'mean')
            return input.reduce((a, b) => a + b, 0) / input.length;
        return op === 'min'
            ? input.reduce((minimum, value) => Math.min(minimum, value), Number.POSITIVE_INFINITY)
            : input.reduce((maximum, value) => Math.max(maximum, value), Number.NEGATIVE_INFINITY);
    }
    function impute(rows, transform) {
        const keyValues = [
            ...new Map(rows.map((row) => [JSON.stringify(row.value[transform.key]), row.value[transform.key]])).values(),
        ];
        return groups(rows, transform.groupby).flatMap((group) => {
            const existing = new Map(group.map((row) => [JSON.stringify(row.value[transform.key]), row]));
            const replacement = transform.method === undefined || transform.method === 'value'
                ? (transform.value ?? null)
                : aggregateValue(group, {
                    op: transform.method === 'mean'
                        ? 'mean'
                        : transform.method === 'median'
                            ? 'median'
                            : transform.method,
                    field: transform.field,
                    as: transform.field,
                });
            return keyValues.map((key) => existing.get(JSON.stringify(key)) ?? {
                value: {
                    ...Object.fromEntries((transform.groupby ?? []).map((field) => [field, group[0]?.value[field]])),
                    [transform.key]: key,
                    [transform.field]: replacement,
                },
                sources: [],
            });
        });
    }
    function seeded(seed) {
        let state = seed >>> 0;
        return () => {
            state = (state + 0x6d2b79f5) | 0;
            let value = Math.imul(state ^ (state >>> 15), 1 | state);
            value ^= value + Math.imul(value ^ (value >>> 7), 61 | value);
            return ((value ^ (value >>> 14)) >>> 0) / 4294967296;
        };
    }
    function apply(rows, transform) {
        switch (transform.type) {
            case 'filter':
                return rows.filter((row) => truthy$1(evaluate(transform.expr, row.value)));
            case 'sort':
                return sorted(rows, transform.by);
            case 'calculate':
                return rows.map((row) => ({
                    ...row,
                    value: { ...row.value, [safeField(transform.as)]: evaluate(transform.expr, row.value) },
                }));
            case 'aggregate':
            case 'joinaggregate':
                return aggregate(rows, transform);
            case 'bin':
                return bin(rows, transform);
            case 'bin2d': {
                const [x0, x1, y0, y1, count] = transform.as;
                const xb = bin(rows, {
                    field: transform.x,
                    as: [x0, x1],
                    maxbins: transform.maxbins?.[0] ?? 10,
                });
                const both = bin(xb, {
                    field: transform.y,
                    as: [y0, y1],
                    maxbins: transform.maxbins?.[1] ?? 10,
                });
                return aggregate(both, {
                    type: 'aggregate',
                    groupby: [x0, x1, y0, y1],
                    fields: [{ op: 'count', as: count }],
                });
            }
            case 'density1d':
                return density1d(rows, transform);
            case 'density2d': {
                const pairs = rows.flatMap((row) => {
                    const x = numeric$2(row.value[transform.x]);
                    const y = numeric$2(row.value[transform.y]);
                    return x === null || y === null ? [] : [{ x, y }];
                });
                if (pairs.length === 0)
                    return [];
                const [xBins, yBins] = transform.bins ?? [32, 32];
                enforceWorkBudget(pairs.length * xBins * yBins, maximumKernelEvaluations, 'density2d', '$.transform[].bins');
                let xMin = Number.POSITIVE_INFINITY;
                let xMax = Number.NEGATIVE_INFINITY;
                let yMin = Number.POSITIVE_INFINITY;
                let yMax = Number.NEGATIVE_INFINITY;
                for (const pair of pairs) {
                    xMin = Math.min(xMin, pair.x);
                    xMax = Math.max(xMax, pair.x);
                    yMin = Math.min(yMin, pair.y);
                    yMax = Math.max(yMax, pair.y);
                }
                const deviation = (input) => {
                    const mean = input.reduce((sum, value) => sum + value, 0) / input.length;
                    return Math.sqrt(input.reduce((sum, value) => sum + (value - mean) ** 2, 0) /
                        Math.max(1, input.length - 1));
                };
                const [bandwidthX, bandwidthY] = transform.bandwidth ?? [
                    Math.max(Number.EPSILON, 1.06 *
                        (deviation(pairs.map(({ x }) => x)) || (xMax - xMin) / 6 || 1) *
                        pairs.length ** -0.2),
                    Math.max(Number.EPSILON, 1.06 *
                        (deviation(pairs.map(({ y }) => y)) || (yMax - yMin) / 6 || 1) *
                        pairs.length ** -0.2),
                ];
                const factor = 1 / (2 * Math.PI * bandwidthX * bandwidthY * pairs.length);
                const sources = [...new Set(rows.flatMap((row) => row.sources))].sort((a, b) => a - b);
                return Array.from({ length: xBins * yBins }, (_, index) => {
                    const xIndex = index % xBins;
                    const yIndex = Math.floor(index / xBins);
                    const x = xMin + ((xMax - xMin || 1) * xIndex) / Math.max(1, xBins - 1);
                    const y = yMin + ((yMax - yMin || 1) * yIndex) / Math.max(1, yBins - 1);
                    const density = pairs.reduce((sum, pair) => {
                        const zx = (x - pair.x) / bandwidthX;
                        const zy = (y - pair.y) / bandwidthY;
                        return sum + Math.exp(-0.5 * (zx * zx + zy * zy));
                    }, 0) * factor;
                    return {
                        value: { [transform.as[0]]: x, [transform.as[1]]: y, [transform.as[2]]: density },
                        sources,
                    };
                });
            }
            case 'stack':
                return stack(rows, transform);
            case 'window':
                return windowTransform(rows, transform);
            case 'regression':
                return regression$1(rows, transform);
            case 'fold':
                return rows.flatMap((row) => transform.fields.map((field) => ({
                    ...row,
                    value: { ...row.value, [transform.as[0]]: field, [transform.as[1]]: row.value[field] },
                })));
            case 'flatten':
                return rows.flatMap((row) => {
                    const arrays = transform.fields.map((field) => Array.isArray(row.value[field])
                        ? row.value[field]
                        : [row.value[field]]);
                    const length = Math.max(0, ...arrays.map((array) => array.length));
                    return Array.from({ length }, (_, index) => ({
                        ...row,
                        value: {
                            ...row.value,
                            ...Object.fromEntries(transform.fields.map((field, fieldIndex) => [
                                transform.as?.[fieldIndex] ?? field,
                                scalar(arrays[fieldIndex]?.[index]),
                            ])),
                        },
                    }));
                });
            case 'pivot':
                return groups(rows, transform.groupby).map((group) => {
                    const output = Object.create(null);
                    for (const field of transform.groupby ?? [])
                        output[field] = group[0]?.value[field];
                    for (const cell of groups(group, [transform.field])) {
                        const column = String(cell[0]?.value[transform.field] ?? 'null');
                        safeField(column);
                        output[column] = pivotAggregate(cell, transform.value, transform.op ?? 'first');
                    }
                    return {
                        value: output,
                        sources: [...new Set(group.flatMap((row) => row.sources))].sort((a, b) => a - b),
                    };
                });
            case 'impute':
                return impute(rows, transform);
            case 'lookup': {
                const lookup = new Map(inputRows(transform.from).map((row) => [
                    JSON.stringify(row.value[transform.key]),
                    row.value,
                ]));
                return rows.map((row) => {
                    const match = lookup.get(JSON.stringify(row.value[transform.field]));
                    const additions = Object.fromEntries(transform.values.map((field, index) => [
                        transform.as?.[index] ?? field,
                        match?.[field] ?? transform.default ?? null,
                    ]));
                    return { ...row, value: { ...row.value, ...additions } };
                });
            }
            case 'quantile':
                return groups(rows, transform.groupby).flatMap((group) => {
                    const input = values(group, transform.field).sort((a, b) => a - b);
                    const sources = [...new Set(group.flatMap((row) => row.sources))].sort((a, b) => a - b);
                    return (transform.probs ?? [0.25, 0.5, 0.75]).map((probability) => ({
                        value: {
                            ...Object.fromEntries((transform.groupby ?? []).map((field) => [field, group[0]?.value[field]])),
                            [transform.as[0]]: probability,
                            [transform.as[1]]: quantileFromSorted(input, probability),
                        },
                        sources,
                    }));
                });
            case 'sample': {
                if (transform.size >= rows.length)
                    return [...rows];
                const random = seeded(transform.seed ?? 0);
                const reservoir = rows.slice(0, transform.size).map((row, index) => ({ row, index }));
                for (let index = transform.size; index < rows.length; index += 1) {
                    const selected = Math.floor(random() * (index + 1));
                    if (selected < transform.size)
                        reservoir[selected] = { row: rows[index], index };
                }
                return reservoir.sort((a, b) => a.index - b.index).map(({ row }) => row);
            }
            case 'resample': {
                const output = [];
                for (const group of groups(rows, transform.groupby)) {
                    const ordered = sorted(group, [{ field: transform.field }]);
                    const domain = extent$2(ordered, transform.field);
                    if (domain === null)
                        continue;
                    const expected = Math.floor((domain[1] - domain[0]) / transform.interval) + 1;
                    enforceWorkBudget(output.length + expected, maximumDerivedRows, 'resample', '$.transform[].interval');
                    let cursor = 0;
                    let before;
                    for (let x = domain[0]; x <= domain[1] + transform.interval / 1e9; x += transform.interval) {
                        while (cursor < ordered.length) {
                            const row = ordered[cursor];
                            const value = numeric$2(row.value[transform.field]);
                            if (value === null) {
                                cursor += 1;
                                continue;
                            }
                            if (value >= x)
                                break;
                            before = row;
                            cursor += 1;
                        }
                        const candidate = ordered[cursor];
                        const candidateValue = candidate === undefined ? null : numeric$2(candidate.value[transform.field]);
                        const after = candidateValue !== null && candidateValue >= x ? candidate : undefined;
                        const exact = after !== undefined && candidateValue === x ? after : undefined;
                        if (exact !== undefined) {
                            output.push(exact);
                            continue;
                        }
                        const method = transform.method ?? 'linear';
                        const base = method === 'next' ? after : before;
                        if (base === undefined)
                            continue;
                        const value = { ...base.value, [transform.field]: x };
                        if (method === 'linear' && before !== undefined && after !== undefined) {
                            const bx = numeric$2(before.value[transform.field]);
                            const ax = numeric$2(after.value[transform.field]);
                            const ratio = ax === bx ? 0 : (x - bx) / (ax - bx);
                            for (const field of new Set([
                                ...Object.keys(before.value),
                                ...Object.keys(after.value),
                            ])) {
                                if (field === transform.field)
                                    continue;
                                const b = numeric$2(before.value[field]);
                                const a = numeric$2(after.value[field]);
                                if (b !== null && a !== null)
                                    value[field] = b + (a - b) * ratio;
                            }
                        }
                        output.push({
                            value,
                            sources: [...new Set([...(before?.sources ?? []), ...(after?.sources ?? [])])].sort((a, b) => a - b),
                        });
                    }
                }
                return output;
            }
            case 'timeUnit':
                return rows.map((row) => {
                    const raw = row.value[transform.field];
                    const date = raw instanceof Date
                        ? new Date(raw)
                        : new Date(typeof raw === 'number' || typeof raw === 'string' ? raw : Number.NaN);
                    let result = null;
                    if (Number.isFinite(date.getTime())) {
                        const utc = transform.utc ?? true;
                        const value = (name) => date[`get${utc ? 'UTC' : ''}${name}`]();
                        result =
                            transform.unit === 'year'
                                ? value('FullYear')
                                : transform.unit === 'quarter'
                                    ? Math.floor(value('Month') / 3) + 1
                                    : transform.unit === 'month'
                                        ? value('Month') + 1
                                        : transform.unit === 'week'
                                            ? Math.floor((Date.UTC(value('FullYear'), value('Month'), value('Date')) -
                                                Date.UTC(value('FullYear'), 0, 1)) /
                                                604800000) + 1
                                            : transform.unit === 'date'
                                                ? value('Date')
                                                : transform.unit === 'day'
                                                    ? value('Day')
                                                    : transform.unit === 'hours'
                                                        ? value('Hours')
                                                        : transform.unit === 'minutes'
                                                            ? value('Minutes')
                                                            : value('Seconds');
                    }
                    return { ...row, value: { ...row.value, [transform.as]: result } };
                });
        }
    }
    function executeTransforms(input, transforms = [], options = {}) {
        const issues = [];
        validateTransforms(transforms, '$.transform', issues);
        if (issues.length > 0) {
            throw new GraflumeError('INVALID_SPEC', issues[0].message, {
                path: issues[0].path,
                details: { issues },
            });
        }
        const source = inputRows(input);
        let rows = source;
        const steps = [];
        transforms.forEach((transform, index) => {
            const before = rows.length;
            const aggregationCount = transform.type === 'aggregate' ||
                transform.type === 'joinaggregate' ||
                transform.type === 'stack'
                ? groups(rows, transform.groupby).length
                : undefined;
            rows = apply(rows, transform);
            const detail = transform.type !== 'stack'
                ? undefined
                : transform.offset === 'normalize' || transform.offset === 'expand'
                    ? 'Diverging normalization uses each bucket total absolute magnitude.'
                    : transform.offset === 'wiggle'
                        ? 'Wiggle uses a non-negative streamgraph baseline and rejects negative values.'
                        : undefined;
            steps.push({
                index,
                type: transform.type,
                inputRows: before,
                outputRows: rows.length,
                parameters: jsonSafe(transform),
                ...(transform.type === 'sample' ? { seed: transform.seed ?? 0 } : {}),
                ...(aggregationCount === undefined ? {} : { aggregationCount }),
                ...(detail === undefined ? {} : { detail }),
            });
        });
        const sourceId = options.sourceId ?? 'data';
        return {
            data: rows.map(({ value }) => ({ ...value })),
            lineage: {
                sourceId,
                sourceRows: source.length,
                outputRows: rows.length,
                transforms: steps,
                rowSources: rows.map(({ sources }) => sources),
                summary: `${sourceId}: ${source.length} source rows, ${steps.length} ordered transforms, ${rows.length} output rows.${steps.flatMap(({ detail }) => (detail === undefined ? [] : [` ${detail}`])).join('')}`,
            },
        };
    }
    const evaluateTransformExpression = evaluate;

    const maximumNamedSources = 128;
    const maximumDataflowNodes = 256;
    const unsafeNames = new Set(['__proto__', 'prototype', 'constructor']);
    const lineageByOutput = new WeakMap();
    function fail$5(message, path) {
        throw new GraflumeError('INVALID_SPEC', message, { path });
    }
    function safeName(value, path) {
        if (typeof value !== 'string' ||
            value.trim() === '' ||
            value.length > 128 ||
            unsafeNames.has(value)) {
            fail$5('Dataflow names must be non-empty safe strings no longer than 128 characters.', path);
        }
        return value;
    }
    function cloneRows$2(input) {
        return input.map((row) => ({ ...row }));
    }
    function remapLineage(upstream, branch, nodeId) {
        const offset = upstream.transforms.length;
        const branchSteps = branch.transforms.map((step) => ({
            ...step,
            index: step.index + offset,
        }));
        const rowSources = branch.rowSources.map((indices) => [...new Set(indices.flatMap((index) => upstream.rowSources[index] ?? []))].sort((left, right) => left - right));
        const transforms = [...upstream.transforms, ...branchSteps];
        return {
            sourceId: upstream.sourceId,
            sourceRows: upstream.sourceRows,
            outputRows: branch.outputRows,
            transforms,
            rowSources,
            summary: `${upstream.sourceId} -> ${nodeId}: ${upstream.sourceRows} source rows, ${transforms.length} ordered transforms, ${branch.outputRows} output rows.`,
        };
    }
    /** Preserve upstream source-row identity while applying a branch-local transform list. */
    function executeTransformBranch(upstream, transforms, nodeId) {
        const branch = executeTransforms(upstream.data, transforms, { sourceId: nodeId });
        const result = {
            data: cloneRows$2(branch.data),
            lineage: remapLineage(upstream.lineage, branch.lineage, nodeId),
        };
        lineageByOutput.set(result.data, result.lineage);
        return result;
    }
    /** Execute a layer-local branch without discarding named-DAG provenance. */
    function executeTransformsWithNamedLineage(input, transforms, sourceId) {
        const upstream = namedDataLineage(input);
        return upstream === undefined
            ? executeTransforms(input, transforms, { sourceId })
            : executeTransformBranch({ data: input, lineage: upstream }, transforms, sourceId);
    }
    /** Return named-DAG lineage attached to a materialized data array, when present. */
    function namedDataLineage(input) {
        return lineageByOutput.get(input);
    }
    /** Execute one closed named transform graph with dependency memoization. */
    class TransformDataflow {
        #sources;
        #nodes;
        #cache = new Map();
        #executionOrder = [];
        #cacheHits = 0;
        constructor(spec) {
            if (!isPlainObject(spec))
                fail$5('Dataflow must be an object.', '$.dataflow');
            if (Object.keys(spec).some((key) => !['sources', 'nodes'].includes(key))) {
                fail$5('Unknown dataflow property.', '$.dataflow');
            }
            if (!isPlainObject(spec.sources)) {
                fail$5('Dataflow sources must be an object.', '$.dataflow.sources');
            }
            const sourceNames = Object.keys(spec.sources);
            if (sourceNames.length === 0 || sourceNames.length > maximumNamedSources) {
                fail$5(`Dataflow requires 1 to ${maximumNamedSources} named sources.`, '$.dataflow.sources');
            }
            for (const name of sourceNames)
                safeName(name, `$.dataflow.sources.${name}`);
            const nodes = spec.nodes ?? [];
            if (!Array.isArray(nodes) || nodes.length > maximumDataflowNodes) {
                fail$5(`Dataflow supports at most ${maximumDataflowNodes} nodes.`, '$.dataflow.nodes');
            }
            const byId = new Map();
            nodes.forEach((node, index) => {
                if (!isPlainObject(node))
                    fail$5('Dataflow node must be an object.', `$.dataflow.nodes[${index}]`);
                const id = safeName(node.id, `$.dataflow.nodes[${index}].id`);
                safeName(node.source, `$.dataflow.nodes[${index}].source`);
                if (Object.keys(node).some((key) => !['id', 'source', 'transform'].includes(key))) {
                    fail$5('Unknown dataflow node property.', `$.dataflow.nodes[${index}]`);
                }
                if (!Array.isArray(node.transform)) {
                    fail$5('Dataflow node transform must be an array.', `$.dataflow.nodes[${index}].transform`);
                }
                if (Object.hasOwn(spec.sources, id) || byId.has(id)) {
                    fail$5(`Duplicate dataflow name "${id}".`, `$.dataflow.nodes[${index}].id`);
                }
                // Reuse the established transform validator and fail before any graph result is cached.
                const transformIssues = [];
                validateTransforms(node.transform, `$.dataflow.nodes[${index}].transform`, transformIssues);
                if (transformIssues.length > 0) {
                    const first = transformIssues[0];
                    throw new GraflumeError('INVALID_SPEC', first.message, {
                        path: first.path,
                        details: { issues: transformIssues },
                    });
                }
                byId.set(id, node);
            });
            this.#sources = spec.sources;
            this.#nodes = byId;
            this.#assertAcyclicAndClosed();
        }
        #assertAcyclicAndClosed() {
            const visited = new Set();
            const active = new Set();
            const visit = (name) => {
                if (Object.hasOwn(this.#sources, name) || visited.has(name))
                    return;
                const node = this.#nodes.get(name);
                if (node === undefined)
                    fail$5(`Unknown dataflow source or node "${name}".`, '$.dataflow.nodes');
                if (active.has(name))
                    fail$5(`Dataflow contains a cycle at "${name}".`, '$.dataflow.nodes');
                active.add(name);
                visit(node.source);
                active.delete(name);
                visited.add(name);
            };
            for (const name of this.#nodes.keys())
                visit(name);
        }
        resolve(name) {
            safeName(name, '$.data.source');
            const cached = this.#cache.get(name);
            if (cached !== undefined) {
                this.#cacheHits += 1;
                return cached;
            }
            let result;
            if (Object.hasOwn(this.#sources, name)) {
                result = executeTransforms(this.#sources[name], [], { sourceId: `source:${name}` });
                result = { data: cloneRows$2(result.data), lineage: result.lineage };
            }
            else {
                const node = this.#nodes.get(name);
                if (node === undefined)
                    fail$5(`Unknown dataflow source or node "${name}".`, '$.data.source');
                result = executeTransformBranch(this.resolve(node.source), node.transform, `node:${node.id}`);
            }
            this.#cache.set(name, result);
            this.#executionOrder.push(name);
            lineageByOutput.set(result.data, result.lineage);
            return result;
        }
        execute(targets = [...this.#nodes.keys()]) {
            const requested = targets.length === 0 ? Object.keys(this.#sources) : targets;
            const outputs = Object.fromEntries(requested.map((name) => [name, this.resolve(name)]));
            return { outputs, state: this.state() };
        }
        state() {
            return {
                sourceCount: Object.keys(this.#sources).length,
                nodeCount: this.#nodes.size,
                cachedOutputs: [...this.#cache.keys()],
                executionOrder: [...this.#executionOrder],
                cacheHits: this.#cacheHits,
            };
        }
        clearCache() {
            this.#cache.clear();
            this.#executionOrder.length = 0;
            this.#cacheHits = 0;
        }
    }
    function createTransformDataflow(spec) {
        return new TransformDataflow(spec);
    }
    function executeTransformDataflow(spec, targets) {
        return createTransformDataflow(spec).execute(targets);
    }
    function isNamedDataReference(value) {
        return (isPlainObject(value) &&
            Object.keys(value).length === 1 &&
            typeof value.source === 'string' &&
            value.source.trim() !== '');
    }
    /**
     * Resolve all named data references before ordinary ChartSpec normalization.
     * One graph instance is shared by every branch in the chart tree, so common
     * ancestors execute once even when several layers or views consume them.
     */
    function materializeSpecDataflow(input) {
        const containsDataflow = (spec) => {
            if (!isPlainObject(spec))
                return false;
            const chart = spec;
            return (chart.dataflow !== undefined ||
                ['layer', 'hconcat', 'vconcat', 'concat'].some((key) => chart[key]?.some(containsDataflow)) ||
                (chart.spec !== undefined && containsDataflow(chart.spec)) ||
                (chart.inset !== undefined &&
                    (containsDataflow(chart.inset.base) || containsDataflow(chart.inset.view))));
        };
        if (!containsDataflow(input))
            return input;
        const visit = (spec, inherited) => {
            const localSpec = spec.dataflow;
            const graph = localSpec === undefined ? inherited : createTransformDataflow(localSpec);
            const resolveData = (value) => {
                if (value === undefined)
                    return undefined;
                if (!isNamedDataReference(value))
                    return value;
                if (graph === undefined) {
                    fail$5(`Named data reference "${value.source}" has no enclosing dataflow.`, '$.data.source');
                }
                return graph.resolve(value.source).data;
            };
            const output = { ...spec };
            delete output.dataflow;
            delete output.source;
            if (spec.source !== undefined) {
                if (spec.data !== undefined)
                    fail$5('Use either inline data or a named source, not both.', '$.source');
                if (graph === undefined)
                    fail$5(`Named source "${spec.source}" has no enclosing dataflow.`, '$.source');
                output.data = graph.resolve(spec.source).data;
            }
            else if (spec.data !== undefined)
                output.data = resolveData(spec.data);
            if (spec.layers !== undefined) {
                output.layers = spec.layers.map((layer) => {
                    const materialized = { ...layer };
                    delete materialized.source;
                    if (layer.source !== undefined) {
                        if (layer.data !== undefined) {
                            fail$5('Use either inline layer data or a named source, not both.', '$.layers[].source');
                        }
                        if (graph === undefined) {
                            fail$5(`Named source "${layer.source}" has no enclosing dataflow.`, '$.layers[].source');
                        }
                        materialized.data = graph.resolve(layer.source).data;
                    }
                    else if (layer.data !== undefined)
                        materialized.data = resolveData(layer.data);
                    return materialized;
                });
            }
            for (const key of ['layer', 'hconcat', 'vconcat', 'concat']) {
                const children = spec[key];
                if (children !== undefined)
                    output[key] = children.map((child) => visit(child, graph));
            }
            if (spec.spec !== undefined)
                output.spec = visit(spec.spec, graph);
            if (spec.inset !== undefined) {
                output.inset = {
                    ...spec.inset,
                    base: visit(spec.inset.base, graph),
                    view: visit(spec.inset.view, graph),
                };
            }
            return output;
        };
        return visit(input);
    }

    const builtInAxisIds = Object.freeze(['x', 'x2', 'y', 'y2']);
    const unsafeAxisIds = new Set(['__proto__', 'prototype', 'constructor']);
    const safeAxisIdPattern = /^[A-Za-z][A-Za-z0-9_-]{0,63}$/u;
    function isSafeAxisId(value) {
        return typeof value === 'string' && safeAxisIdPattern.test(value) && !unsafeAxisIds.has(value);
    }
    function builtInAxisChannel(id) {
        if (id === 'x' || id === 'x2')
            return 'x';
        if (id === 'y' || id === 'y2')
            return 'y';
        return undefined;
    }
    function axisChannel(id, axes = {}) {
        return builtInAxisChannel(id) ?? (axes[id] === false ? undefined : axes[id]?.channel);
    }
    function defaultAxisPosition(id, channel) {
        if (id === 'x2')
            return 'top';
        if (id === 'y2')
            return 'right';
        return channel === 'x' ? 'bottom' : 'left';
    }
    function axisPositionChannel(position) {
        return position === 'top' || position === 'bottom' ? 'x' : 'y';
    }
    function declaredAxisIds(axes = {}) {
        const ids = [...builtInAxisIds];
        for (const id of Object.keys(axes))
            if (!ids.includes(id))
                ids.push(id);
        return Object.freeze(ids);
    }

    const indexByScene = new WeakMap();
    const coordinateEpsilon = 1e-6;
    function primary(target, channel) {
        return channel === 'x' ? target.x : target.y;
    }
    function perpendicular(target, channel) {
        return channel === 'x' ? target.y : target.x;
    }
    function registerAxisTooltipIndex(scene, registration) {
        const channel = registration.channel ?? builtInAxisChannel(registration.axis) ?? 'x';
        const targets = registration.targets
            .filter((target) => Number.isFinite(target.x) && Number.isFinite(target.y))
            .sort((left, right) => {
            const coordinate = primary(left, channel) - primary(right, channel);
            if (Math.abs(coordinate) > coordinateEpsilon)
                return coordinate;
            if (left.order !== right.order)
                return right.order - left.order;
            return left.nodeId.localeCompare(right.nodeId);
        });
        indexByScene.set(scene, { ...registration, channel, targets });
    }
    function insideActivationRegion(index, x, y) {
        const { plot, axis, channel, axisVisible, axisStripSize } = index;
        const right = plot.x + plot.width;
        const bottom = plot.y + plot.height;
        const horizontal = channel === 'x';
        const position = index.position ??
            (horizontal ? (axis === 'x2' ? 'top' : 'bottom') : axis === 'y2' ? 'right' : 'left');
        if (horizontal) {
            const strip = axisVisible ? axisStripSize : 0;
            const top = position === 'top' ? plot.y - strip : plot.y;
            const stripBottom = position === 'bottom' ? bottom + strip : bottom;
            return x >= plot.x && x <= right && y >= top && y <= stripBottom;
        }
        const strip = axisVisible ? axisStripSize : 0;
        const left = position === 'left' ? plot.x - strip : plot.x;
        const stripRight = position === 'right' ? right + strip : right;
        return x >= left && x <= stripRight && y >= plot.y && y <= bottom;
    }
    function lowerBound(targets, channel, value) {
        let low = 0;
        let high = targets.length;
        while (low < high) {
            const middle = low + Math.floor((high - low) / 2);
            const target = targets[middle];
            if (target !== undefined && primary(target, channel) < value)
                low = middle + 1;
            else
                high = middle;
        }
        return low;
    }
    function nearestCoordinate(index, pointer) {
        const insertion = lowerBound(index.targets, index.channel, pointer);
        const before = index.targets[insertion - 1];
        const after = index.targets[insertion];
        if (before === undefined && after === undefined)
            return null;
        if (before === undefined)
            return primary(after, index.channel);
        if (after === undefined)
            return primary(before, index.channel);
        const beforeCoordinate = primary(before, index.channel);
        const afterCoordinate = primary(after, index.channel);
        return pointer - beforeCoordinate <= afterCoordinate - pointer
            ? beforeCoordinate
            : afterCoordinate;
    }
    function hitTestAxisTooltip(scene, x, y) {
        const index = indexByScene.get(scene);
        if (index === undefined ||
            index.targets.length === 0 ||
            !scene.metadata.hitTestingEnabled ||
            !insideActivationRegion(index, x, y)) {
            return null;
        }
        const horizontal = index.channel === 'x';
        const pointerPrimary = horizontal ? x : y;
        const pointerPerpendicular = horizontal ? y : x;
        const coordinate = nearestCoordinate(index, pointerPrimary);
        if (coordinate === null)
            return null;
        const start = lowerBound(index.targets, index.channel, coordinate - coordinateEpsilon);
        let best = null;
        let bestDistance = Number.POSITIVE_INFINITY;
        for (let cursor = start; cursor < index.targets.length; cursor += 1) {
            const target = index.targets[cursor];
            if (target === undefined)
                continue;
            if (Math.abs(primary(target, index.channel) - coordinate) > coordinateEpsilon)
                break;
            const distance = Math.abs(perpendicular(target, index.channel) - pointerPerpendicular);
            if (distance < bestDistance - coordinateEpsilon ||
                (Math.abs(distance - bestDistance) <= coordinateEpsilon &&
                    (best === null || target.order > best.order))) {
                best = target;
                bestDistance = distance;
            }
        }
        if (best === null)
            return null;
        return {
            layerId: best.layerId,
            rowIndex: best.rowIndex,
            datum: best.datum,
            ...(best.tooltip === undefined ? {} : { tooltip: best.tooltip }),
            nodeId: best.nodeId,
            x,
            y,
            distance: Math.abs(pointerPrimary - coordinate),
        };
    }

    function nodeBase(id, options = {}) {
        return {
            id,
            zIndex: options.zIndex ?? 0,
            opacity: options.opacity ?? 1,
            visible: options.visible ?? true,
            ...(options.interactive === undefined ? {} : { interactive: options.interactive }),
            ...(options.datum === undefined ? {} : { datum: options.datum }),
        };
    }
    function group(id, children, options = {}) {
        return {
            type: 'group',
            ...nodeBase(id, options),
            children,
            ...(options.clip === undefined ? {} : { clip: options.clip }),
        };
    }

    function cartesianAxisChannel(context, axis) {
        const channel = context.channels?.[axis] ?? builtInAxisChannel(axis);
        if (channel === undefined)
            fail$4(`Axis "${axis}" has no Cartesian channel.`, `$.axes.${axis}`);
        return channel;
    }
    function fail$4(message, path = '$.interaction.selection') {
        throw new GraflumeError('INCOMPATIBLE_SCALE', message, { path });
    }
    function finite$d(value, label) {
        if (!Number.isFinite(value))
            fail$4(`${label} must be finite.`);
        return value;
    }
    function scaleFor$1(context, axis) {
        const scale = context.axes[axis];
        if (scale === undefined)
            fail$4(`Axis "${axis}" is not resolved.`, `$.axes.${axis}`);
        return scale;
    }
    function clampToRange(scale, pixel) {
        finite$d(pixel, 'Pixel coordinate');
        const range = scale.range();
        if (range.length === 0)
            fail$4(`Scale "${scale.kind}" has an empty range.`);
        const minimum = Math.min(...range);
        const maximum = Math.max(...range);
        return Math.max(minimum, Math.min(maximum, pixel));
    }
    function nearestCategorical(scale, pixel) {
        if (scale.kind !== 'band' && scale.kind !== 'point') {
            fail$4(`Scale "${scale.kind}" has no single-valued inverse; analytic geometry supports invertible continuous, band, and point axes only.`);
        }
        const candidates = scale.domain().map((value) => {
            const mapped = scale.map(value);
            return { value, distance: Math.abs(mapped - pixel) };
        });
        const nearest = candidates.reduce((best, candidate) => best === undefined || candidate.distance < best.distance ? candidate : best, undefined);
        if (nearest === undefined)
            fail$4(`Scale "${scale.kind}" has an empty domain.`);
        return nearest.value;
    }
    function domainToPixel(context, axis, value) {
        const mapped = scaleFor$1(context, axis).map(value);
        if (!Number.isFinite(mapped)) {
            fail$4(`Value ${String(value)} cannot be mapped on axis "${axis}".`, `$.axes.${axis}`);
        }
        return mapped;
    }
    function pixelToDomain(context, axis, pixel) {
        const scale = scaleFor$1(context, axis);
        const bounded = clampToRange(scale, pixel);
        if (scale.invert === undefined)
            return nearestCategorical(scale, bounded);
        const value = scale.invert(bounded);
        if (typeof value === 'number' && !Number.isFinite(value)) {
            fail$4(`Pixel ${pixel} cannot be inverted on axis "${axis}".`, `$.axes.${axis}`);
        }
        return value;
    }
    function clampPixelToPlot(context, point) {
        finite$d(point.x, 'Pixel x');
        finite$d(point.y, 'Pixel y');
        return Object.freeze({
            x: Math.max(context.plot.x, Math.min(context.plot.x + context.plot.width, point.x)),
            y: Math.max(context.plot.y, Math.min(context.plot.y + context.plot.height, point.y)),
        });
    }
    function pixelPointToDomain(context, point, axes = {}) {
        const bounded = clampPixelToPlot(context, point);
        const xAxis = axes.x ?? 'x';
        const yAxis = axes.y ?? 'y';
        return Object.freeze({
            type: 'point',
            xAxis,
            yAxis,
            x: pixelToDomain(context, xAxis, bounded.x),
            y: pixelToDomain(context, yAxis, bounded.y),
        });
    }
    function domainPointToPixel(context, point) {
        if (point.x === undefined || point.y === undefined) {
            fail$4('A domain point requires both x and y to map to pixels.');
        }
        return Object.freeze({
            x: domainToPixel(context, point.xAxis ?? 'x', point.x),
            y: domainToPixel(context, point.yAxis ?? 'y', point.y),
        });
    }
    function continuousDomain(context, axis, pixel) {
        const scale = scaleFor$1(context, axis);
        if (scale.invert === undefined) {
            fail$4(`Selection geometry on axis "${axis}" requires an invertible continuous scale; "${scale.kind}" is unsupported.`, `$.axes.${axis}`);
        }
        const value = pixelToDomain(context, axis, pixel);
        if (typeof value !== 'number') {
            fail$4(`Selection geometry on axis "${axis}" must invert to a number.`, `$.axes.${axis}`);
        }
        return value;
    }
    function extent$1(start, end) {
        return Object.freeze(start <= end ? [start, end] : [end, start]);
    }
    function categoricalExtent(scale, start, end) {
        if (scale.kind !== 'band' && scale.kind !== 'point') {
            fail$4(`Scale "${scale.kind}" cannot create a categorical brush extent.`);
        }
        const domain = scale.domain();
        if (domain.length === 0)
            fail$4(`Scale "${scale.kind}" has an empty domain.`);
        const nearestIndex = (pixel) => {
            let bestIndex = 0;
            let bestDistance = Number.POSITIVE_INFINITY;
            domain.forEach((value, index) => {
                const distance = Math.abs(scale.map(value) - pixel);
                if (distance < bestDistance) {
                    bestIndex = index;
                    bestDistance = distance;
                }
            });
            return bestIndex;
        };
        const first = nearestIndex(start);
        const last = nearestIndex(end);
        return Object.freeze({
            values: Object.freeze(domain.slice(Math.min(first, last), Math.max(first, last) + 1)),
        });
    }
    function selectionExtent(context, axis, start, end) {
        const scale = scaleFor$1(context, axis);
        return scale.invert === undefined
            ? categoricalExtent(scale, start, end)
            : extent$1(continuousDomain(context, axis, start), continuousDomain(context, axis, end));
    }
    function categoricalHalfSpan(scale) {
        if (scale.bandwidth > 0)
            return scale.bandwidth / 2;
        const positions = scale
            .domain()
            .map((value) => scale.map(value))
            .sort((left, right) => left - right);
        let minimum = Number.POSITIVE_INFINITY;
        for (let index = 1; index < positions.length; index += 1) {
            minimum = Math.min(minimum, Math.abs(positions[index] - positions[index - 1]));
        }
        return Number.isFinite(minimum) ? minimum / 2 : 3;
    }
    function extentToPixels(scale, value) {
        if (Array.isArray(value)) {
            return Object.freeze([scale.map(value[0]), scale.map(value[1])]);
        }
        const positions = value.values.map((entry) => scale.map(entry));
        if (positions.some((entry) => !Number.isFinite(entry))) {
            fail$4('Categorical brush values must exist in the resolved scale domain.');
        }
        const halfSpan = categoricalHalfSpan(scale);
        return Object.freeze([Math.min(...positions) - halfSpan, Math.max(...positions) + halfSpan]);
    }
    function pixelRectangleToSelection(context, start, end, options = {}) {
        const first = clampPixelToPlot(context, start);
        const last = clampPixelToPlot(context, end);
        const xAxis = options.xAxis ?? 'x';
        const yAxis = options.yAxis ?? 'y';
        return Object.freeze({
            type: options.type ?? 'rectangle',
            xAxis,
            yAxis,
            x: selectionExtent(context, xAxis, first.x, last.x),
            y: selectionExtent(context, yAxis, first.y, last.y),
        });
    }
    function pixelAxisToSelection(context, axis, start, end) {
        const first = clampPixelToPlot(context, start);
        const last = clampPixelToPlot(context, end);
        const horizontal = cartesianAxisChannel(context, axis) === 'x';
        return Object.freeze({
            type: 'axis',
            axis,
            extent: selectionExtent(context, axis, horizontal ? first.x : first.y, horizontal ? last.x : last.y),
        });
    }
    function pixelLassoToSelection(context, points, axes = {}) {
        if (points.length < 3)
            fail$4('A lasso gesture requires at least three points.');
        if (points.length > 512)
            fail$4('A lasso gesture exceeds the 512 point bound.');
        const xAxis = axes.x ?? 'x';
        const yAxis = axes.y ?? 'y';
        const domainPoints = points.map((point) => {
            const bounded = clampPixelToPlot(context, point);
            return Object.freeze({
                x: continuousDomain(context, xAxis, bounded.x),
                y: continuousDomain(context, yAxis, bounded.y),
            });
        });
        return Object.freeze({
            type: 'lasso',
            xAxis,
            yAxis,
            points: Object.freeze(domainPoints),
        });
    }
    function selectionToPixels(context, selection) {
        if (selection.type === 'point') {
            if (selection.x === undefined || selection.y === undefined)
                return [];
            return [domainPointToPixel(context, selection)];
        }
        if (selection.type === 'interval' || selection.type === 'rectangle') {
            const x = extentToPixels(scaleFor$1(context, selection.xAxis), selection.x);
            const y = extentToPixels(scaleFor$1(context, selection.yAxis), selection.y);
            return [
                Object.freeze({
                    x: x[0],
                    y: y[0],
                }),
                Object.freeze({
                    x: x[1],
                    y: y[1],
                }),
            ];
        }
        if (selection.type === 'axis') {
            const horizontal = cartesianAxisChannel(context, selection.axis) === 'x';
            const [start, end] = extentToPixels(scaleFor$1(context, selection.axis), selection.extent);
            return horizontal
                ? [
                    Object.freeze({ x: start, y: context.plot.y }),
                    Object.freeze({ x: end, y: context.plot.y + context.plot.height }),
                ]
                : [
                    Object.freeze({ x: context.plot.x, y: start }),
                    Object.freeze({ x: context.plot.x + context.plot.width, y: end }),
                ];
        }
        return selection.points.map((point) => Object.freeze({
            x: domainToPixel(context, selection.xAxis, point.x),
            y: domainToPixel(context, selection.yAxis, point.y),
        }));
    }

    /** Compile completed domain selections into non-interactive, plot-clipped Scene nodes. */
    function compileAnalyticSelectionOverlay(state, context, style, idPrefix = 'analytic-selection') {
        const nodes = [];
        state.selections.forEach((selection, index) => {
            const points = selectionToPixels(context, selection);
            if (selection.type === 'point') {
                const point = points[0];
                if (point === undefined)
                    return;
                nodes.push({
                    type: 'circle',
                    ...nodeBase(`${idPrefix}:${index}`, { zIndex: 760, opacity: style.opacity }),
                    cx: point.x,
                    cy: point.y,
                    radius: style.radius,
                    fill: style.fill,
                    stroke: style.stroke,
                    lineWidth: style.lineWidth,
                    dash: style.dash,
                });
                return;
            }
            if (selection.type === 'lasso') {
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${idPrefix}:${index}`, { zIndex: 760, opacity: style.opacity }),
                    points,
                    closed: true,
                    fill: style.fill,
                    stroke: style.stroke,
                    lineWidth: style.lineWidth,
                    dash: style.dash,
                    lineJoin: 'round',
                });
                return;
            }
            const first = points[0];
            const last = points[1];
            if (first === undefined || last === undefined)
                return;
            nodes.push({
                type: 'rect',
                ...nodeBase(`${idPrefix}:${index}`, { zIndex: 760, opacity: style.opacity }),
                x: Math.min(first.x, last.x),
                y: Math.min(first.y, last.y),
                width: Math.abs(last.x - first.x),
                height: Math.abs(last.y - first.y),
                fill: style.fill,
                stroke: style.stroke,
                lineWidth: style.lineWidth,
                dash: style.dash,
                cornerRadius: 0,
            });
        });
        return nodes.length === 0
            ? []
            : [group(`${idPrefix}:overlay`, nodes, { zIndex: 760, clip: context.plot })];
    }

    const analyticSelectionVersion = 1;
    const maximumAnalyticSelections = 64;
    const maximumLassoPoints = 512;
    const maximumCategoricalSelectionValues = 512;
    function invalid$2(message) {
        throw new GraflumeError('INVALID_SPEC', message, { path: '$.analyticSelection' });
    }
    function assertPlainObject$1(value, label) {
        if (typeof value !== 'object' || value === null || Array.isArray(value)) {
            invalid$2(`${label} must be an object.`);
        }
    }
    function assertClosedKeys$1(value, allowed, label) {
        const allowedKeys = new Set(allowed);
        const unknown = Object.keys(value).filter((key) => !allowedKeys.has(key));
        if (unknown.length > 0)
            invalid$2(`${label} contains unknown key "${unknown[0]}".`);
    }
    function canonicalNumber(value) {
        return Object.is(value, -0) ? 0 : value;
    }
    function canonicalPrimitive(value) {
        return typeof value === 'number' ? canonicalNumber(value) : value;
    }
    function domainValue(value, label) {
        if (typeof value === 'number') {
            if (!Number.isFinite(value))
                invalid$2(`${label} must be finite.`);
            return;
        }
        if (typeof value !== 'string' || value.length === 0 || value.length > 4096) {
            invalid$2(`${label} must be a finite number or a non-empty bounded string.`);
        }
    }
    function orderedExtent(value) {
        const start = canonicalNumber(value[0]);
        const end = canonicalNumber(value[1]);
        return Object.freeze(start <= end ? [start, end] : [end, start]);
    }
    function normalizeExtent(value, label) {
        if (Array.isArray(value)) {
            if (value.length !== 2 ||
                value.some((entry) => typeof entry !== 'number' || !Number.isFinite(entry))) {
                invalid$2(`${label} must contain exactly two finite numbers.`);
            }
            return orderedExtent(value);
        }
        assertPlainObject$1(value, label);
        assertClosedKeys$1(value, ['values'], label);
        if (!Array.isArray(value.values) ||
            value.values.length === 0 ||
            value.values.length > maximumCategoricalSelectionValues) {
            invalid$2(`${label}.values must contain between 1 and ${maximumCategoricalSelectionValues} categorical identities.`);
        }
        const values = value.values.map((entry, index) => {
            domainValue(entry, `${label}.values[${index}]`);
            return typeof entry === 'number' ? canonicalNumber(entry) : entry;
        });
        if (new Set(values.map((entry) => `${typeof entry}:${String(entry)}`)).size !== values.length) {
            invalid$2(`${label}.values must be unique.`);
        }
        return Object.freeze({ values: Object.freeze(values) });
    }
    function cloneTarget$2(target) {
        const rows = target.rowIndex === undefined
            ? undefined
            : Array.isArray(target.rowIndex)
                ? [...target.rowIndex].sort((left, right) => left - right)
                : target.rowIndex;
        const values = target.values === undefined
            ? undefined
            : [...target.values]
                .map(canonicalPrimitive)
                .sort((left, right) => JSON.stringify(left).localeCompare(JSON.stringify(right)));
        return Object.freeze({
            type: 'datum',
            ...(target.layerId === undefined ? {} : { layerId: target.layerId }),
            ...(rows === undefined ? {} : { rowIndex: Array.isArray(rows) ? Object.freeze(rows) : rows }),
            ...(target.field === undefined ? {} : { field: target.field }),
            ...(Object.prototype.hasOwnProperty.call(target, 'value')
                ? { value: canonicalPrimitive(target.value) }
                : {}),
            ...(values === undefined ? {} : { values: Object.freeze(values) }),
        });
    }
    function validateTarget(target) {
        assertPlainObject$1(target, 'Point target');
        assertClosedKeys$1(target, ['type', 'layerId', 'rowIndex', 'field', 'value', 'values'], 'Point target');
        const candidate = target;
        if (candidate.type !== 'datum')
            invalid$2('Point target.type must be "datum".');
        if (candidate.layerId !== undefined &&
            (typeof candidate.layerId !== 'string' ||
                candidate.layerId.length === 0 ||
                candidate.layerId.length > 4096)) {
            invalid$2('Point target.layerId must be a non-empty bounded string.');
        }
        if (candidate.rowIndex !== undefined) {
            const rows = Array.isArray(candidate.rowIndex) ? candidate.rowIndex : [candidate.rowIndex];
            if (rows.length === 0 ||
                rows.length > 1000 ||
                rows.some((row) => !Number.isInteger(row) || row < 0)) {
                invalid$2('Point target.rowIndex must contain bounded non-negative integers.');
            }
            if (new Set(rows).size !== rows.length)
                invalid$2('Point target.rowIndex must be unique.');
        }
        if (candidate.field !== undefined &&
            (typeof candidate.field !== 'string' ||
                candidate.field.length === 0 ||
                candidate.field.length > 4096 ||
                candidate.field === '__proto__' ||
                candidate.field === 'prototype' ||
                candidate.field === 'constructor')) {
            invalid$2('Point target.field must be a safe non-empty bounded string.');
        }
        const hasValue = Object.prototype.hasOwnProperty.call(candidate, 'value');
        const hasValues = Object.prototype.hasOwnProperty.call(candidate, 'values');
        if (candidate.field === undefined && (hasValue || hasValues)) {
            invalid$2('Point target.field is required for value matching.');
        }
        if (candidate.field !== undefined && hasValue === hasValues) {
            invalid$2('Point target field matching requires exactly one of value or values.');
        }
        if (hasValues && (!Array.isArray(candidate.values) || candidate.values.length === 0)) {
            invalid$2('Point target values must be a non-empty array.');
        }
        const values = hasValues ? candidate.values : hasValue ? [candidate.value] : [];
        if (values.length > 200)
            invalid$2('Point target values exceed the 200 item bound.');
        for (const value of values) {
            if (value !== null &&
                typeof value !== 'string' &&
                typeof value !== 'boolean' &&
                !(typeof value === 'number' && Number.isFinite(value))) {
                invalid$2('Point target values must be JSON primitives with finite numbers.');
            }
        }
        if (new Set(values.map((value) => JSON.stringify(canonicalPrimitive(value)))).size !==
            values.length) {
            invalid$2('Point target values must be unique.');
        }
        if (candidate.rowIndex === undefined && candidate.field === undefined) {
            invalid$2('Point target requires rowIndex or field/value identity.');
        }
    }
    function axisId(value, _orientation) {
        if (!isSafeAxisId(value))
            invalid$2('Selection axis must use the safe named-axis grammar.');
    }
    function normalizeSelection(selection) {
        assertPlainObject$1(selection, 'Selection');
        if (selection.type === 'point') {
            assertClosedKeys$1(selection, ['type', 'xAxis', 'yAxis', 'x', 'y', 'target'], 'Point selection');
            if (selection.x === undefined && selection.y === undefined && selection.target === undefined) {
                invalid$2('Point selection requires x, y, or target identity.');
            }
            if (selection.x !== undefined)
                domainValue(selection.x, 'Point x');
            if (selection.y !== undefined)
                domainValue(selection.y, 'Point y');
            if (selection.xAxis !== undefined)
                axisId(selection.xAxis);
            if (selection.yAxis !== undefined)
                axisId(selection.yAxis);
            if (selection.target !== undefined)
                validateTarget(selection.target);
            return Object.freeze({
                type: 'point',
                ...(selection.xAxis === undefined ? {} : { xAxis: selection.xAxis }),
                ...(selection.yAxis === undefined ? {} : { yAxis: selection.yAxis }),
                ...(selection.x === undefined
                    ? {}
                    : { x: typeof selection.x === 'number' ? canonicalNumber(selection.x) : selection.x }),
                ...(selection.y === undefined
                    ? {}
                    : { y: typeof selection.y === 'number' ? canonicalNumber(selection.y) : selection.y }),
                ...(selection.target === undefined ? {} : { target: cloneTarget$2(selection.target) }),
            });
        }
        if (selection.type === 'interval' || selection.type === 'rectangle') {
            assertClosedKeys$1(selection, ['type', 'xAxis', 'yAxis', 'x', 'y'], `${selection.type} selection`);
            axisId(selection.xAxis);
            axisId(selection.yAxis);
            return Object.freeze({
                type: selection.type,
                xAxis: selection.xAxis,
                yAxis: selection.yAxis,
                x: normalizeExtent(selection.x, 'Selection x extent'),
                y: normalizeExtent(selection.y, 'Selection y extent'),
            });
        }
        if (selection.type === 'axis') {
            assertClosedKeys$1(selection, ['type', 'axis', 'extent'], 'Axis selection');
            axisId(selection.axis);
            return Object.freeze({
                type: 'axis',
                axis: selection.axis,
                extent: normalizeExtent(selection.extent, 'Axis selection extent'),
            });
        }
        if (selection.type === 'lasso') {
            assertClosedKeys$1(selection, ['type', 'xAxis', 'yAxis', 'points'], 'Lasso selection');
            axisId(selection.xAxis);
            axisId(selection.yAxis);
            if (!Array.isArray(selection.points) || selection.points.length < 3) {
                invalid$2('Lasso selection requires at least three points.');
            }
            if (selection.points.length > maximumLassoPoints) {
                invalid$2(`Lasso selection exceeds the ${maximumLassoPoints} point bound.`);
            }
            const points = selection.points.map((point, index) => {
                assertPlainObject$1(point, `Lasso point ${index}`);
                assertClosedKeys$1(point, ['x', 'y'], `Lasso point ${index}`);
                if (typeof point.x !== 'number' ||
                    typeof point.y !== 'number' ||
                    !Number.isFinite(point.x) ||
                    !Number.isFinite(point.y)) {
                    invalid$2(`Lasso point ${index} must contain finite x and y values.`);
                }
                return Object.freeze({ x: canonicalNumber(point.x), y: canonicalNumber(point.y) });
            });
            return Object.freeze({
                type: 'lasso',
                xAxis: selection.xAxis,
                yAxis: selection.yAxis,
                points: Object.freeze(points),
            });
        }
        invalid$2(`Unsupported analytic selection type: ${String(selection.type)}.`);
    }
    function stableTarget(target) {
        if (target === undefined)
            return null;
        const rows = target.rowIndex === undefined
            ? null
            : Array.isArray(target.rowIndex)
                ? [...target.rowIndex].sort((a, b) => a - b)
                : [target.rowIndex];
        const values = target.values ?? (target.value === undefined ? [] : [target.value]);
        return [
            target.layerId ?? null,
            rows,
            target.field ?? null,
            [...values].sort((a, b) => JSON.stringify(a).localeCompare(JSON.stringify(b))),
        ];
    }
    function analyticSelectionKey(selection) {
        switch (selection.type) {
            case 'point':
                return JSON.stringify([
                    'point',
                    selection.xAxis ?? null,
                    selection.x ?? null,
                    selection.yAxis ?? null,
                    selection.y ?? null,
                    stableTarget(selection.target),
                ]);
            case 'interval':
            case 'rectangle':
                return JSON.stringify([
                    selection.type,
                    selection.xAxis,
                    selection.x,
                    selection.yAxis,
                    selection.y,
                ]);
            case 'axis':
                return JSON.stringify(['axis', selection.axis, selection.extent]);
            case 'lasso':
                return JSON.stringify(['lasso', selection.xAxis, selection.yAxis, selection.points]);
        }
    }
    function normalizeAnalyticSelectionState(input) {
        assertPlainObject$1(input, 'Analytic selection state');
        assertClosedKeys$1(input, ['version', 'combine', 'selections'], 'Analytic selection state');
        if (input.version !== analyticSelectionVersion) {
            invalid$2(`Analytic selection state.version must be ${analyticSelectionVersion}.`);
        }
        if (input.combine !== 'union' && input.combine !== 'intersection') {
            invalid$2('Analytic selection state.combine must be "union" or "intersection".');
        }
        if (!Array.isArray(input.selections) || input.selections.length > maximumAnalyticSelections) {
            invalid$2(`Analytic selection state must contain at most ${maximumAnalyticSelections} selections.`);
        }
        const selections = input.selections.map(normalizeSelection);
        const keys = selections.map(analyticSelectionKey);
        if (new Set(keys).size !== keys.length)
            invalid$2('Analytic selections must be unique.');
        return Object.freeze({
            version: analyticSelectionVersion,
            combine: input.combine,
            selections: Object.freeze(selections),
        });
    }
    function emptyAnalyticSelectionState(combine = 'union') {
        return normalizeAnalyticSelectionState({
            version: analyticSelectionVersion,
            combine,
            selections: [],
        });
    }
    function primitiveEqual(left, right) {
        return typeof left === 'number' && typeof right === 'number'
            ? Number.isFinite(left) && Object.is(left, right)
            : Object.is(left, right);
    }
    function targetMatches(target, sample) {
        if (target.layerId !== undefined && target.layerId !== sample.layerId)
            return false;
        if (target.rowIndex !== undefined) {
            const rows = Array.isArray(target.rowIndex) ? target.rowIndex : [target.rowIndex];
            if (sample.rowIndex === undefined || !rows.includes(sample.rowIndex))
                return false;
        }
        if (target.field !== undefined) {
            const values = target.values ?? (target.value === undefined ? [] : [target.value]);
            const actual = sample.datum?.[target.field];
            if (!values.some((value) => primitiveEqual(actual, value)))
                return false;
        }
        return target.rowIndex !== undefined || target.field !== undefined;
    }
    function inExtent(value, extent) {
        if (Array.isArray(extent)) {
            return (typeof value === 'number' &&
                Number.isFinite(value) &&
                value >= extent[0] &&
                value <= extent[1]);
        }
        return extent.values.some((candidate) => Object.is(candidate, value));
    }
    function pointInPolygon$1(point, polygon) {
        let inside = false;
        for (let current = 0, previous = polygon.length - 1; current < polygon.length; previous = current++) {
            const a = polygon[current];
            const b = polygon[previous];
            const crosses = a.y > point.y !== b.y > point.y;
            const edgeX = ((b.x - a.x) * (point.y - a.y)) / (b.y - a.y || Number.EPSILON) + a.x;
            if (crosses && point.x < edgeX)
                inside = !inside;
        }
        return inside;
    }
    function normalizedSelectionMatches(normalized, sample) {
        if (normalized.selections.length === 0)
            return false;
        const matches = (selection) => {
            switch (selection.type) {
                case 'point': {
                    if (selection.target !== undefined && !targetMatches(selection.target, sample))
                        return false;
                    if (selection.x !== undefined && !Object.is(sample[selection.xAxis ?? 'x'], selection.x))
                        return false;
                    if (selection.y !== undefined && !Object.is(sample[selection.yAxis ?? 'y'], selection.y))
                        return false;
                    return true;
                }
                case 'interval':
                case 'rectangle':
                    return (inExtent(sample[selection.xAxis], selection.x) &&
                        inExtent(sample[selection.yAxis], selection.y));
                case 'axis':
                    return inExtent(sample[selection.axis], selection.extent);
                case 'lasso': {
                    const x = sample[selection.xAxis];
                    const y = sample[selection.yAxis];
                    return (typeof x === 'number' &&
                        typeof y === 'number' &&
                        pointInPolygon$1({ x, y }, selection.points));
                }
            }
        };
        return normalized.combine === 'union'
            ? normalized.selections.some(matches)
            : normalized.selections.every(matches);
    }
    function analyticSelectionPredicate(state) {
        const normalized = normalizeAnalyticSelectionState(state);
        return (sample) => normalizedSelectionMatches(normalized, sample);
    }
    function analyticSelectionMatches(state, sample) {
        return normalizedSelectionMatches(normalizeAnalyticSelectionState(state), sample);
    }
    class AnalyticSelectionStore {
        #state;
        constructor(initial = emptyAnalyticSelectionState()) {
            this.#state = normalizeAnalyticSelectionState(initial);
        }
        get() {
            return this.#state;
        }
        set(state) {
            this.#state = normalizeAnalyticSelectionState(state);
            return this.#state;
        }
        clear(combine = this.#state.combine) {
            this.#state = emptyAnalyticSelectionState(combine);
            return this.#state;
        }
        apply(selection, update = 'replace') {
            const normalized = normalizeSelection(selection);
            if (update === 'replace') {
                return this.set({
                    version: analyticSelectionVersion,
                    combine: this.#state.combine,
                    selections: [normalized],
                });
            }
            const existing = this.#state.selections.filter((candidate) => analyticSelectionKey(candidate) !== analyticSelectionKey(normalized));
            return this.set({
                version: analyticSelectionVersion,
                combine: update,
                selections: [...existing, normalized],
            });
        }
    }

    function countSceneNodes(root) {
        let count = 1;
        for (const child of root.children) {
            count += child.type === 'group' ? countSceneNodes(child) : 1;
        }
        return count;
    }

    const compositionOperators = [
        'layer',
        'facet',
        'repeat',
        'hconcat',
        'vconcat',
        'concat',
        'inset',
    ];
    const maximumCompositionDepth = 4;
    const maximumCompositionViews = 64;
    const maximumCompositionLayers = 128;
    const maximumLayerCompositionChildren = 16;
    function presentCompositionOperators(input) {
        return compositionOperators.filter((key) => input[key] !== undefined);
    }
    function compositionKind(input) {
        const operators = presentCompositionOperators(input);
        return operators.length === 1 ? operators[0] : null;
    }
    function isCompositionSpec(input) {
        return presentCompositionOperators(input).length > 0;
    }
    function resolveComposition(input, kind) {
        const defaultMode = kind === 'layer' ? 'shared' : 'independent';
        return {
            scale: input?.scale ?? defaultMode,
            axis: input?.axis ?? defaultMode,
            legend: input?.legend ?? defaultMode,
            colorbar: input?.colorbar ?? defaultMode,
        };
    }

    const version = '0.1.0-alpha.0';
    const specVersion = '0.1';

    const palette$2 = [
        '#4f46e5',
        '#0f9f8a',
        '#f59e0b',
        '#e05260',
        '#7c3aed',
        '#0e7490',
        '#db2777',
        '#65a30d',
        '#475569',
        '#ea580c',
    ];
    const pointToCssPixel = 96 / 72;
    const millimeterToCssPixel = 96 / 25.4;
    const matplotlibPointToCssPixel = 100 / 72;
    const matplotlibPalette = [
        '#1F77B4',
        '#FF7F0E',
        '#2CA02C',
        '#D62728',
        '#9467BD',
        '#8C564B',
        '#E377C2',
        '#7F7F7F',
        '#BCBD22',
        '#17BECF',
    ];
    const graflumeLight = {
        name: 'graflume-light',
        mode: 'light',
        colors: {
            background: '#ffffff',
            surface: '#f8fafc',
            text: '#0f172a',
            mutedText: '#64748b',
            axis: '#cbd5e1',
            grid: '#e8eef6',
            focus: '#4f46e5',
            palette: palette$2,
            sequential: ['#eef2ff', '#c7d2fe', '#818cf8', '#4f46e5', '#312e81'],
            diverging: ['#b42318', '#f79084', '#f8fafc', '#84adff', '#3448c5'],
        },
        typography: {
            fontFamily: 'Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif',
            fontSize: 12,
            titleSize: 20,
            subtitleSize: 12,
            lineHeight: 1.45,
        },
        spacing: { xs: 4, sm: 8, md: 12, lg: 16, xl: 24 },
        axis: { lineWidth: 1, tickLength: 0, labelPadding: 9, gridLineWidth: 1 },
        mark: { lineWidth: 2.5, pointRadius: 4.5, barRadius: 5, opacity: 1 },
        motion: { duration: 280, easing: 'ease-out' },
    };
    const graflumeDark = {
        ...graflumeLight,
        name: 'graflume-dark',
        mode: 'dark',
        colors: {
            ...graflumeLight.colors,
            background: '#0b1020',
            surface: '#111827',
            text: '#f8fafc',
            mutedText: '#a7b2c5',
            axis: '#475569',
            grid: '#25314a',
            focus: '#818cf8',
            palette: [
                '#818cf8',
                '#2dd4bf',
                '#fbbf24',
                '#fb7185',
                '#a78bfa',
                '#22d3ee',
                '#f472b6',
                '#a3e635',
                '#94a3b8',
                '#fb923c',
            ],
            sequential: ['#1e293b', '#3730a3', '#6366f1', '#a5b4fc', '#eef2ff'],
            diverging: ['#fb7185', '#be123c', '#334155', '#4f46e5', '#a5b4fc'],
        },
    };
    /**
     * ggplot2 v4.0.3 theme_gray() rendered in CSS pixels at the browser reference
     * density of 96 dpi. Data-scale colours follow ggplot2's default hue and
     * two-colour continuous scales rather than a fixed categorical array.
     */
    const graflumeGgplot = {
        name: 'ggplot',
        mode: 'light',
        colors: {
            background: '#FFFFFF',
            surface: '#FFFFFF',
            panel: '#EBEBEB',
            text: '#000000',
            mutedText: '#4D4D4D',
            subtitle: '#000000',
            axisTitle: '#000000',
            axis: '#333333',
            grid: '#FFFFFF',
            minorGrid: '#FFFFFF',
            focus: '#3366FF',
            palette: [
                '#F8766D',
                '#D89000',
                '#A3A500',
                '#39B600',
                '#00BF7D',
                '#00BFC4',
                '#00B0F6',
                '#9590FF',
                '#E76BF3',
                '#FF62BC',
            ],
            paletteMode: 'ggplot2-hue',
            sequential: ['#132B43', '#56B1F7'],
            diverging: ['#832424', '#FFFFFF', '#3A3A98'],
        },
        typography: {
            fontFamily: 'sans-serif',
            fontSize: 11 * pointToCssPixel,
            fontWeight: 400,
            titleSize: 13.2 * pointToCssPixel,
            titleWeight: 400,
            subtitleSize: 11 * pointToCssPixel,
            subtitleWeight: 400,
            axisLabelSize: 8.8 * pointToCssPixel,
            axisLabelWeight: 400,
            axisTitleSize: 11 * pointToCssPixel,
            axisTitleWeight: 400,
            legendLabelSize: 8.8 * pointToCssPixel,
            legendLabelWeight: 400,
            legendTitleSize: 11 * pointToCssPixel,
            legendTitleWeight: 400,
            titlePosition: 'panel',
            lineHeight: 0.9,
        },
        spacing: {
            xs: 5.5 * pointToCssPixel,
            sm: 5.5 * pointToCssPixel,
            md: 11 * pointToCssPixel,
            lg: 5.5 * pointToCssPixel,
            xl: 22 * pointToCssPixel,
            plotMargin: 5.5 * pointToCssPixel,
        },
        axis: {
            lineWidth: 0.5 * millimeterToCssPixel,
            tickLength: 2.75 * pointToCssPixel,
            labelPadding: 2.2 * pointToCssPixel,
            gridLineWidth: 0.5 * millimeterToCssPixel,
            lineVisible: false,
            ticksVisible: true,
            gridX: true,
            gridX2: false,
            gridY: true,
            gridY2: false,
            gridOpacity: 1,
            minorGridVisible: true,
            minorGridLineWidth: 0.25 * millimeterToCssPixel,
            minorGridOpacity: 1,
            emphasizeZero: false,
            lineCap: 'butt',
            titleGap: 2.75 * pointToCssPixel,
        },
        mark: {
            lineWidth: 0.5 * millimeterToCssPixel,
            pointRadius: 0.75 * millimeterToCssPixel,
            barRadius: 0,
            opacity: 1,
            defaultColor: '#000000',
            lineColor: '#000000',
            pointFill: '#000000',
            pointStroke: '#000000',
            pointStrokeWidth: 0.5 * millimeterToCssPixel,
            barFill: '#595959',
            barWidthRatio: 0.9,
            areaFill: '#333333',
            areaStrokeVisible: false,
            lineCap: 'butt',
            lineJoin: 'round',
        },
        legend: {
            surfaceOpacity: 1,
            borderWidth: 0,
            cornerRadius: 0,
            swatchRadius: 0,
            swatchSize: 1.2 * 11 * pointToCssPixel,
            lineWidth: 0.5 * millimeterToCssPixel,
            pointRadius: 0.75 * millimeterToCssPixel,
            pointStrokeWidth: 0.5 * millimeterToCssPixel,
            lineCap: 'butt',
        },
        motion: { duration: 280, easing: 'ease-out' },
    };
    /**
     * R 4.6.1 base graphics defaults mapped to the browser reference density of
     * 96 dpi. Device-specific font discovery and rasterisation remain outside the
     * portable theme contract.
     */
    const graflumeRBase = {
        name: 'r-base',
        mode: 'light',
        colors: {
            background: '#FFFFFF',
            surface: '#FFFFFF',
            panel: '#FFFFFF',
            text: '#000000',
            mutedText: '#000000',
            subtitle: '#000000',
            axisTitle: '#000000',
            axis: '#000000',
            grid: '#D9D9D9',
            focus: '#2297E6',
            palette: [
                '#000000',
                '#DF536B',
                '#61D04F',
                '#2297E6',
                '#28E2E5',
                '#CD0BBC',
                '#F5C710',
                '#9E9E9E',
            ],
            sequential: ['#FFFFCC', '#FED976', '#FD8D3C', '#E31A1C', '#800026'],
            diverging: ['#2166AC', '#67A9CF', '#F7F7F7', '#EF8A62', '#B2182B'],
        },
        typography: {
            fontFamily: 'Helvetica, Arial, sans-serif',
            fontSize: 12 * pointToCssPixel,
            fontWeight: 400,
            titleSize: 14.4 * pointToCssPixel,
            titleWeight: 700,
            subtitleSize: 12 * pointToCssPixel,
            subtitleWeight: 400,
            axisLabelSize: 12 * pointToCssPixel,
            axisLabelWeight: 400,
            axisTitleSize: 12 * pointToCssPixel,
            axisTitleWeight: 400,
            legendLabelSize: 12 * pointToCssPixel,
            legendLabelWeight: 400,
            legendTitleSize: 12 * pointToCssPixel,
            legendTitleWeight: 400,
            titlePosition: 'panel',
            titleAlign: 'center',
            lineHeight: 1.2,
        },
        spacing: {
            xs: 4,
            sm: 8,
            md: 12,
            lg: 16,
            xl: 24,
            plotPadding: { top: 24, right: 30, bottom: 73, left: 59 },
        },
        axis: {
            lineWidth: 1,
            boxVisible: true,
            boxLineWidth: 1,
            boxExcludedMarks: ['bar', 'pie'],
            tickLength: 6,
            labelPadding: 5,
            gridLineWidth: 1,
            lineVisible: true,
            ticksVisible: true,
            gridX: false,
            gridX2: false,
            gridY: false,
            gridY2: false,
            gridOpacity: 1,
            minorGridVisible: false,
            emphasizeZero: false,
            lineCap: 'butt',
            titleGap: 8,
        },
        mark: {
            lineWidth: 1,
            pointRadius: 3.75,
            barRadius: 0,
            opacity: 1,
            defaultColor: '#000000',
            lineColor: '#000000',
            pointFill: 'transparent',
            pointStroke: '#000000',
            pointStrokeWidth: 1,
            barFill: '#BEBEBE',
            barStroke: '#000000',
            barStrokeWidth: 1,
            barWidthRatio: 0.8,
            histogramFill: '#D3D3D3',
            boxplotFill: '#D3D3D3',
            boxplotLineWidth: 1,
            boxplotRadius: 0,
            piePalette: ['#FFFFFF', '#ADD8E6', '#FFE4E1', '#E0FFFF', '#E6E6FA', '#FFF8DC'],
            pieStroke: '#000000',
            pieStrokeWidth: 1,
            areaFill: '#BEBEBE',
            areaStroke: '#000000',
            areaStrokeVisible: true,
            lineCap: 'round',
            lineJoin: 'round',
        },
        legend: {
            surfaceOpacity: 1,
            borderWidth: 1,
            cornerRadius: 0,
            swatchRadius: 0,
            swatchSize: 12 * pointToCssPixel,
            lineWidth: 1,
            pointRadius: 3.75,
            pointStrokeWidth: 1,
            lineCap: 'round',
        },
        motion: { duration: 0, easing: 'linear' },
    };
    /**
     * Matplotlib 3.11.1's default rcParams mapped to a 640 x 480 browser canvas
     * at Matplotlib's default 100 dpi. Graflume retains its own chart semantics
     * while matching the reference typography, axes, marks, colour cycle, and
     * viridis/coolwarm scale surfaces.
     */
    const graflumeMatplotlib = {
        name: 'matplotlib',
        mode: 'light',
        colors: {
            background: '#FFFFFF',
            surface: '#FFFFFF',
            panel: '#FFFFFF',
            text: '#000000',
            mutedText: '#000000',
            subtitle: '#000000',
            axisTitle: '#000000',
            axis: '#000000',
            grid: '#B0B0B0',
            minorGrid: '#B0B0B0',
            focus: '#1F77B4',
            palette: matplotlibPalette,
            continuousInterpolation: 'step',
            sequential: [
                '#440154',
                '#440256',
                '#450457',
                '#450559',
                '#46075A',
                '#46085C',
                '#460A5D',
                '#460B5E',
                '#470D60',
                '#470E61',
                '#471063',
                '#471164',
                '#471365',
                '#481467',
                '#481668',
                '#481769',
                '#48186A',
                '#481A6C',
                '#481B6D',
                '#481C6E',
                '#481D6F',
                '#481F70',
                '#482071',
                '#482173',
                '#482374',
                '#482475',
                '#482576',
                '#482677',
                '#482878',
                '#482979',
                '#472A7A',
                '#472C7A',
                '#472D7B',
                '#472E7C',
                '#472F7D',
                '#46307E',
                '#46327E',
                '#46337F',
                '#463480',
                '#453581',
                '#453781',
                '#453882',
                '#443983',
                '#443A83',
                '#443B84',
                '#433D84',
                '#433E85',
                '#423F85',
                '#424086',
                '#424186',
                '#414287',
                '#414487',
                '#404588',
                '#404688',
                '#3F4788',
                '#3F4889',
                '#3E4989',
                '#3E4A89',
                '#3E4C8A',
                '#3D4D8A',
                '#3D4E8A',
                '#3C4F8A',
                '#3C508B',
                '#3B518B',
                '#3B528B',
                '#3A538B',
                '#3A548C',
                '#39558C',
                '#39568C',
                '#38588C',
                '#38598C',
                '#375A8C',
                '#375B8D',
                '#365C8D',
                '#365D8D',
                '#355E8D',
                '#355F8D',
                '#34608D',
                '#34618D',
                '#33628D',
                '#33638D',
                '#32648E',
                '#32658E',
                '#31668E',
                '#31678E',
                '#31688E',
                '#30698E',
                '#306A8E',
                '#2F6B8E',
                '#2F6C8E',
                '#2E6D8E',
                '#2E6E8E',
                '#2E6F8E',
                '#2D708E',
                '#2D718E',
                '#2C718E',
                '#2C728E',
                '#2C738E',
                '#2B748E',
                '#2B758E',
                '#2A768E',
                '#2A778E',
                '#2A788E',
                '#29798E',
                '#297A8E',
                '#297B8E',
                '#287C8E',
                '#287D8E',
                '#277E8E',
                '#277F8E',
                '#27808E',
                '#26818E',
                '#26828E',
                '#26828E',
                '#25838E',
                '#25848E',
                '#25858E',
                '#24868E',
                '#24878E',
                '#23888E',
                '#23898E',
                '#238A8D',
                '#228B8D',
                '#228C8D',
                '#228D8D',
                '#218E8D',
                '#218F8D',
                '#21908D',
                '#21918C',
                '#20928C',
                '#20928C',
                '#20938C',
                '#1F948C',
                '#1F958B',
                '#1F968B',
                '#1F978B',
                '#1F988B',
                '#1F998A',
                '#1F9A8A',
                '#1E9B8A',
                '#1E9C89',
                '#1E9D89',
                '#1F9E89',
                '#1F9F88',
                '#1FA088',
                '#1FA188',
                '#1FA187',
                '#1FA287',
                '#20A386',
                '#20A486',
                '#21A585',
                '#21A685',
                '#22A785',
                '#22A884',
                '#23A983',
                '#24AA83',
                '#25AB82',
                '#25AC82',
                '#26AD81',
                '#27AD81',
                '#28AE80',
                '#29AF7F',
                '#2AB07F',
                '#2CB17E',
                '#2DB27D',
                '#2EB37C',
                '#2FB47C',
                '#31B57B',
                '#32B67A',
                '#34B679',
                '#35B779',
                '#37B878',
                '#38B977',
                '#3ABA76',
                '#3BBB75',
                '#3DBC74',
                '#3FBC73',
                '#40BD72',
                '#42BE71',
                '#44BF70',
                '#46C06F',
                '#48C16E',
                '#4AC16D',
                '#4CC26C',
                '#4EC36B',
                '#50C46A',
                '#52C569',
                '#54C568',
                '#56C667',
                '#58C765',
                '#5AC864',
                '#5CC863',
                '#5EC962',
                '#60CA60',
                '#63CB5F',
                '#65CB5E',
                '#67CC5C',
                '#69CD5B',
                '#6CCD5A',
                '#6ECE58',
                '#70CF57',
                '#73D056',
                '#75D054',
                '#77D153',
                '#7AD151',
                '#7CD250',
                '#7FD34E',
                '#81D34D',
                '#84D44B',
                '#86D549',
                '#89D548',
                '#8BD646',
                '#8ED645',
                '#90D743',
                '#93D741',
                '#95D840',
                '#98D83E',
                '#9BD93C',
                '#9DD93B',
                '#A0DA39',
                '#A2DA37',
                '#A5DB36',
                '#A8DB34',
                '#AADC32',
                '#ADDC30',
                '#B0DD2F',
                '#B2DD2D',
                '#B5DE2B',
                '#B8DE29',
                '#BADE28',
                '#BDDF26',
                '#C0DF25',
                '#C2DF23',
                '#C5E021',
                '#C8E020',
                '#CAE11F',
                '#CDE11D',
                '#D0E11C',
                '#D2E21B',
                '#D5E21A',
                '#D8E219',
                '#DAE319',
                '#DDE318',
                '#DFE318',
                '#E2E418',
                '#E5E419',
                '#E7E419',
                '#EAE51A',
                '#ECE51B',
                '#EFE51C',
                '#F1E51D',
                '#F4E61E',
                '#F6E620',
                '#F8E621',
                '#FBE723',
                '#FDE725',
            ],
            diverging: [
                '#3B4CC0',
                '#5977E3',
                '#7B9FF9',
                '#9EBEFF',
                '#C0D4F5',
                '#DDDCDC',
                '#F2CBB7',
                '#F7AC8E',
                '#EE8468',
                '#D65244',
                '#B40426',
            ],
        },
        typography: {
            fontFamily: '"DejaVu Sans", "Bitstream Vera Sans", "Computer Modern Sans Serif", Arial, Helvetica, sans-serif',
            fontSize: 10 * matplotlibPointToCssPixel,
            fontWeight: 400,
            titleSize: 12 * matplotlibPointToCssPixel,
            titleWeight: 400,
            subtitleSize: 10 * matplotlibPointToCssPixel,
            subtitleWeight: 400,
            axisLabelSize: 10 * matplotlibPointToCssPixel,
            axisLabelWeight: 400,
            axisTitleSize: 10 * matplotlibPointToCssPixel,
            axisTitleWeight: 400,
            legendLabelSize: 10 * matplotlibPointToCssPixel,
            legendLabelWeight: 400,
            legendTitleSize: 10 * matplotlibPointToCssPixel,
            legendTitleWeight: 400,
            titlePosition: 'panel',
            titleAlign: 'center',
            lineHeight: 1.2,
        },
        spacing: {
            xs: 4 * matplotlibPointToCssPixel,
            sm: 8 * matplotlibPointToCssPixel,
            md: 12 * matplotlibPointToCssPixel,
            lg: 16 * matplotlibPointToCssPixel,
            xl: 24 * matplotlibPointToCssPixel,
            plotPadding: { top: 19, right: 64, bottom: 53, left: 80 },
            minimumTitleBlock: 12 * matplotlibPointToCssPixel + 16 * matplotlibPointToCssPixel,
        },
        axis: {
            lineWidth: 0.8 * matplotlibPointToCssPixel,
            boxVisible: true,
            boxLineWidth: 0.8 * matplotlibPointToCssPixel,
            boxExcludedMarks: ['pie'],
            tickLength: 3.5 * matplotlibPointToCssPixel,
            labelPadding: 3.5 * matplotlibPointToCssPixel,
            gridLineWidth: 0.8 * matplotlibPointToCssPixel,
            lineVisible: true,
            ticksVisible: true,
            gridX: false,
            gridX2: false,
            gridY: false,
            gridY2: false,
            gridOpacity: 1,
            minorGridVisible: false,
            emphasizeZero: false,
            lineCap: 'butt',
            titleGap: 4 * matplotlibPointToCssPixel,
        },
        mark: {
            lineWidth: 1.5 * matplotlibPointToCssPixel,
            pointRadius: 3 * matplotlibPointToCssPixel,
            pointStrokeWidth: 1 * matplotlibPointToCssPixel,
            pointColorMode: 'series',
            barRadius: 0,
            barStroke: 'transparent',
            barStrokeWidth: 0,
            barWidthRatio: 0.8,
            histogramGap: 0,
            boxplotFill: 'transparent',
            boxplotLineWidth: 1 * matplotlibPointToCssPixel,
            boxplotRadius: 0,
            boxplotMedianStroke: '#FF7F0E',
            piePalette: matplotlibPalette,
            pieStroke: 'transparent',
            pieStrokeWidth: 0,
            pieStartAngle: 0,
            pieDirection: 'counterclockwise',
            areaStroke: '#000000',
            areaStrokeVisible: false,
            areaColorMode: 'series',
            opacity: 1,
            lineCap: 'square',
            lineJoin: 'round',
        },
        legend: {
            surfaceOpacity: 0.8,
            borderWidth: 1 * matplotlibPointToCssPixel,
            borderColor: '#CCCCCC',
            cornerRadius: 4,
            swatchRadius: 0,
            swatchSize: 10 * matplotlibPointToCssPixel,
            lineWidth: 1.5 * matplotlibPointToCssPixel,
            pointRadius: 3 * matplotlibPointToCssPixel,
            pointStrokeWidth: 1 * matplotlibPointToCssPixel,
            lineCap: 'square',
            continuousSamples: 256,
        },
        motion: { duration: 0, easing: 'linear' },
    };
    const blueSequential = ['#eff6ff', '#bfdbfe', '#60a5fa', '#2563eb', '#1e3a8a'];
    const viridisSequential = ['#440154', '#3b528b', '#21918c', '#5ec962', '#fde725'];
    const warmSequential = ['#fff7ed', '#fed7aa', '#fb923c', '#c2410c', '#7c2d12'];
    const terrainSequential = ['#e0f2fe', '#7dd3fc', '#34d399', '#a3a341', '#7c5c3b'];
    const redBlueDiverging = ['#b91c1c', '#fca5a5', '#f8fafc', '#93c5fd', '#1d4ed8'];
    const darkDiverging = ['#ff5a5f', '#7f1d1d', '#111827', '#14532d', '#00d084'];
    /**
     * Neutral profiles share the portable theme shape without aliasing an existing
     * built-in. The definition contains only visual defaults; it never enables a
     * chart family, renderer, transform, or interaction capability.
     */
    function createNeutralTheme(definition) {
        const axisColor = definition.colors.axis ?? graflumeLight.colors.axis;
        return {
            name: definition.name,
            mode: definition.mode,
            colors: { ...graflumeLight.colors, ...definition.colors },
            typography: { ...graflumeLight.typography, ...definition.typography },
            spacing: { ...graflumeLight.spacing, ...definition.spacing },
            axis: { ...graflumeLight.axis, ...definition.axis },
            mark: { ...graflumeLight.mark, ...definition.mark },
            legend: {
                surfaceOpacity: definition.mode === 'dark' ? 0.94 : 1,
                borderWidth: 1,
                borderColor: axisColor,
                cornerRadius: definition.mark?.barRadius ?? 0,
                swatchRadius: definition.mark?.barRadius ?? 0,
                swatchSize: 12,
                lineWidth: definition.mark?.lineWidth ?? graflumeLight.mark.lineWidth,
                pointRadius: definition.mark?.pointRadius ?? graflumeLight.mark.pointRadius,
                pointStrokeWidth: 1,
                lineCap: definition.axis?.lineCap ?? 'round',
                continuousSamples: 64,
                ...definition.legend,
            },
            motion: { ...graflumeLight.motion, ...definition.motion },
        };
    }
    const neutralThemeDefinitions = [
        {
            name: 'editorial',
            mode: 'light',
            colors: {
                background: '#fbf8f1',
                surface: '#fffdf8',
                panel: '#fffdf8',
                text: '#26231f',
                mutedText: '#756d63',
                axis: '#8f867b',
                grid: '#ded7cb',
                focus: '#1f5a7a',
                palette: ['#1f3b5d', '#9f3a38', '#b07d2c', '#536b4a', '#76527a', '#447c87'],
                sequential: warmSequential,
                diverging: ['#9f3a38', '#df9b8f', '#fffdf8', '#91b2c2', '#1f5a7a'],
            },
            typography: {
                fontFamily: 'Georgia, "Times New Roman", serif',
                fontSize: 13,
                titleSize: 23,
                titleWeight: 700,
                subtitleSize: 13,
                titlePosition: 'plot',
                lineHeight: 1.55,
            },
            spacing: { xs: 5, sm: 9, md: 14, lg: 19, xl: 28 },
            axis: {
                lineWidth: 1,
                tickLength: 4,
                labelPadding: 8,
                gridLineWidth: 1,
                gridX: false,
                gridY: true,
                emphasizeZero: true,
            },
            mark: { lineWidth: 2, pointRadius: 4, barRadius: 0, opacity: 0.96 },
            legend: { borderWidth: 0, cornerRadius: 0, swatchRadius: 0 },
            motion: { duration: 180, easing: 'ease-out' },
        },
        {
            name: 'scientific-classic',
            mode: 'light',
            colors: {
                background: '#ffffff',
                surface: '#ffffff',
                panel: '#ffffff',
                text: '#111111',
                mutedText: '#4b5563',
                axis: '#111111',
                grid: '#d1d5db',
                focus: '#005f73',
                palette: ['#005f73', '#bb3e03', '#0a9396', '#9b2226', '#ca6702', '#3a0ca3'],
                sequential: viridisSequential,
                diverging: redBlueDiverging,
            },
            typography: {
                fontFamily: '"STIX Two Text", "Times New Roman", serif',
                fontSize: 12,
                titleSize: 17,
                titleWeight: 600,
                subtitleSize: 12,
                titleAlign: 'center',
                lineHeight: 1.35,
            },
            spacing: { xs: 4, sm: 7, md: 11, lg: 15, xl: 22 },
            axis: {
                lineWidth: 1.2,
                boxVisible: true,
                boxLineWidth: 1.2,
                tickLength: 5,
                labelPadding: 6,
                gridLineWidth: 0.75,
                gridX: false,
                gridY: false,
                emphasizeZero: false,
                lineCap: 'square',
            },
            mark: {
                lineWidth: 1.8,
                pointRadius: 3.5,
                barRadius: 0,
                opacity: 1,
                pointFill: 'transparent',
                pointStrokeWidth: 1.2,
                lineCap: 'square',
                lineJoin: 'miter',
            },
            legend: { cornerRadius: 0, swatchRadius: 0, borderColor: '#111111' },
            motion: { duration: 0, easing: 'linear' },
        },
        {
            name: 'statistical-minimal',
            mode: 'light',
            colors: {
                background: '#ffffff',
                surface: '#ffffff',
                panel: '#ffffff',
                text: '#172033',
                mutedText: '#667085',
                axis: '#98a2b3',
                grid: '#eaecf0',
                focus: '#2563eb',
                palette: ['#2563eb', '#dc2626', '#059669', '#7c3aed', '#d97706', '#0891b2'],
                sequential: blueSequential,
                diverging: redBlueDiverging,
            },
            typography: { fontSize: 12, titleSize: 18, subtitleSize: 12, lineHeight: 1.4 },
            spacing: { xs: 4, sm: 7, md: 11, lg: 15, xl: 21 },
            axis: {
                lineWidth: 1,
                tickLength: 0,
                labelPadding: 8,
                gridLineWidth: 1,
                lineVisible: false,
                ticksVisible: false,
                gridX: false,
                gridY: true,
                emphasizeZero: true,
            },
            mark: { lineWidth: 2.2, pointRadius: 4, barRadius: 1, opacity: 0.94 },
            legend: { borderWidth: 0, cornerRadius: 2, swatchRadius: 1 },
            motion: { duration: 160, easing: 'ease-out' },
        },
        {
            name: 'dashboard-dense',
            mode: 'light',
            colors: {
                background: '#f1f5f9',
                surface: '#ffffff',
                panel: '#ffffff',
                text: '#0f172a',
                mutedText: '#64748b',
                axis: '#94a3b8',
                grid: '#e2e8f0',
                focus: '#2563eb',
                palette: ['#2563eb', '#14b8a6', '#f59e0b', '#ef4444', '#8b5cf6', '#06b6d4'],
                sequential: blueSequential,
                diverging: ['#dc2626', '#fca5a5', '#ffffff', '#86efac', '#15803d'],
            },
            typography: { fontSize: 11, titleSize: 16, subtitleSize: 11, lineHeight: 1.25 },
            spacing: { xs: 3, sm: 5, md: 8, lg: 11, xl: 16 },
            axis: {
                lineWidth: 1,
                tickLength: 3,
                labelPadding: 5,
                gridLineWidth: 1,
                gridX: true,
                gridY: true,
                emphasizeZero: true,
            },
            mark: { lineWidth: 1.8, pointRadius: 3.5, barRadius: 2, opacity: 1 },
            legend: { cornerRadius: 4, swatchRadius: 2, swatchSize: 10 },
            motion: { duration: 120, easing: 'ease-out' },
        },
        {
            name: 'finance-terminal',
            mode: 'dark',
            colors: {
                background: '#070b0e',
                surface: '#0d1419',
                panel: '#0a1014',
                text: '#e6edf3',
                mutedText: '#8b949e',
                axis: '#3d4d57',
                grid: '#1c2a31',
                focus: '#f2cc60',
                palette: ['#00d084', '#ff5a5f', '#2f81f7', '#f2cc60', '#bc8cff', '#39c5cf'],
                sequential: ['#0d281f', '#075c40', '#00a36c', '#39d98a', '#b7f7d7'],
                diverging: darkDiverging,
            },
            typography: {
                fontFamily: '"IBM Plex Mono", "SFMono-Regular", Consolas, monospace',
                fontSize: 11,
                titleSize: 16,
                titleWeight: 600,
                subtitleSize: 11,
                lineHeight: 1.3,
            },
            spacing: { xs: 3, sm: 6, md: 9, lg: 12, xl: 18 },
            axis: {
                lineWidth: 1,
                tickLength: 4,
                labelPadding: 6,
                gridLineWidth: 1,
                gridX: true,
                gridY: true,
                emphasizeZero: true,
                lineCap: 'square',
            },
            mark: { lineWidth: 1.6, pointRadius: 3, barRadius: 0, opacity: 1, lineCap: 'square' },
            legend: { borderColor: '#3d4d57', cornerRadius: 0, swatchRadius: 0 },
            motion: { duration: 0, easing: 'linear' },
        },
        {
            name: 'observability',
            mode: 'dark',
            colors: {
                background: '#11131a',
                surface: '#191c26',
                panel: '#151822',
                text: '#dce3f0',
                mutedText: '#8f9bb3',
                axis: '#47516a',
                grid: '#293047',
                focus: '#7dcfff',
                palette: ['#7aa2f7', '#9ece6a', '#e0af68', '#f7768e', '#bb9af7', '#7dcfff'],
                sequential: ['#202544', '#344b8e', '#4f7ccf', '#7dcfff', '#c7f0ff'],
                diverging: ['#f7768e', '#9d3c5b', '#191c26', '#527a45', '#9ece6a'],
            },
            typography: { fontSize: 11, titleSize: 17, subtitleSize: 11, lineHeight: 1.35 },
            spacing: { xs: 3, sm: 6, md: 10, lg: 13, xl: 19 },
            axis: {
                lineWidth: 1,
                tickLength: 3,
                labelPadding: 6,
                gridLineWidth: 1,
                lineVisible: false,
                gridX: true,
                gridY: true,
                gridOpacity: 0.8,
                emphasizeZero: true,
            },
            mark: { lineWidth: 2, pointRadius: 3.5, barRadius: 2, opacity: 0.96 },
            legend: { borderColor: '#293047', cornerRadius: 4, swatchRadius: 2 },
            motion: { duration: 140, easing: 'ease-out' },
        },
        {
            name: 'geospatial',
            mode: 'light',
            colors: {
                background: '#eaf6fb',
                surface: '#f8fcfe',
                panel: '#f1f8f4',
                text: '#17324d',
                mutedText: '#5b7183',
                axis: '#7893a6',
                grid: '#d0e5ec',
                focus: '#0077b6',
                palette: ['#0077b6', '#2a9d8f', '#e9c46a', '#f4a261', '#e76f51', '#588157'],
                sequential: terrainSequential,
                diverging: ['#9b2226', '#ee9b8f', '#f8fcfe', '#94d2bd', '#005f73'],
            },
            typography: { fontSize: 12, titleSize: 19, subtitleSize: 12, lineHeight: 1.4 },
            spacing: { xs: 4, sm: 8, md: 12, lg: 16, xl: 24 },
            axis: {
                lineWidth: 1,
                tickLength: 3,
                labelPadding: 7,
                gridLineWidth: 0.8,
                gridX: true,
                gridY: true,
                gridOpacity: 0.75,
                emphasizeZero: false,
            },
            mark: { lineWidth: 1.8, pointRadius: 4, barRadius: 2, opacity: 0.92 },
            legend: { borderColor: '#b6d2dc', cornerRadius: 4, swatchRadius: 2 },
            motion: { duration: 220, easing: 'ease-in-out' },
        },
        {
            name: 'spatial-lab',
            mode: 'dark',
            colors: {
                background: '#050816',
                surface: '#0b1026',
                panel: '#080d20',
                text: '#edf4ff',
                mutedText: '#96a6c8',
                axis: '#41527a',
                grid: '#1a2850',
                focus: '#00e5ff',
                palette: ['#00e5ff', '#7c4dff', '#ff4081', '#76ff03', '#ffd740', '#ff6e40'],
                sequential: viridisSequential,
                diverging: ['#ff4081', '#7c4dff', '#101936', '#00b8d4', '#76ff03'],
            },
            typography: {
                fontFamily: '"IBM Plex Sans", Inter, ui-sans-serif, system-ui, sans-serif',
                fontSize: 12,
                titleSize: 18,
                subtitleSize: 12,
                lineHeight: 1.35,
            },
            spacing: { xs: 4, sm: 7, md: 11, lg: 15, xl: 22 },
            axis: {
                lineWidth: 1,
                tickLength: 4,
                labelPadding: 7,
                gridLineWidth: 0.8,
                gridX: true,
                gridY: true,
                gridOpacity: 0.8,
                emphasizeZero: false,
            },
            mark: { lineWidth: 1.8, pointRadius: 4, barRadius: 1, opacity: 0.92 },
            legend: { borderColor: '#41527a', cornerRadius: 2, swatchRadius: 1 },
            motion: { duration: 0, easing: 'linear' },
        },
        {
            name: 'presentation',
            mode: 'light',
            colors: {
                background: '#ffffff',
                surface: '#ffffff',
                panel: '#f8fafc',
                text: '#0f172a',
                mutedText: '#475569',
                axis: '#94a3b8',
                grid: '#e2e8f0',
                focus: '#1d4ed8',
                palette: ['#2563eb', '#e11d48', '#059669', '#f59e0b', '#7c3aed', '#0891b2'],
                sequential: blueSequential,
                diverging: redBlueDiverging,
            },
            typography: {
                fontSize: 14,
                titleSize: 26,
                titleWeight: 700,
                subtitleSize: 15,
                axisLabelSize: 13,
                axisTitleSize: 14,
                legendLabelSize: 13,
                legendTitleSize: 14,
                lineHeight: 1.5,
            },
            spacing: { xs: 6, sm: 10, md: 16, lg: 22, xl: 32 },
            axis: {
                lineWidth: 1.2,
                tickLength: 4,
                labelPadding: 10,
                gridLineWidth: 1,
                gridX: false,
                gridY: true,
                emphasizeZero: true,
            },
            mark: { lineWidth: 3, pointRadius: 5.5, barRadius: 6, opacity: 1 },
            legend: { borderWidth: 0, cornerRadius: 6, swatchRadius: 4, swatchSize: 15 },
            motion: { duration: 320, easing: 'ease-in-out' },
        },
        {
            name: 'pictorial',
            mode: 'light',
            colors: {
                background: '#fff9ed',
                surface: '#ffffff',
                panel: '#fffdf7',
                text: '#3b2f2f',
                mutedText: '#7c6860',
                axis: '#b39b8f',
                grid: '#f0dfd2',
                focus: '#e8590c',
                palette: ['#ff6b6b', '#4dabf7', '#ffd43b', '#69db7c', '#b197fc', '#ffa94d'],
                sequential: warmSequential,
                diverging: ['#f06595', '#ffa8c5', '#fff9ed', '#74c0fc', '#1971c2'],
            },
            typography: {
                fontFamily: '"Trebuchet MS", "Avenir Next", ui-sans-serif, sans-serif',
                fontSize: 13,
                titleSize: 22,
                titleWeight: 700,
                subtitleSize: 13,
                lineHeight: 1.45,
            },
            spacing: { xs: 5, sm: 9, md: 14, lg: 19, xl: 27 },
            axis: {
                lineWidth: 1.2,
                tickLength: 0,
                labelPadding: 9,
                gridLineWidth: 1,
                lineVisible: false,
                ticksVisible: false,
                gridX: false,
                gridY: true,
                emphasizeZero: false,
            },
            mark: { lineWidth: 3, pointRadius: 6, barRadius: 8, opacity: 0.96, lineCap: 'round' },
            legend: { borderColor: '#f0dfd2', cornerRadius: 8, swatchRadius: 6, swatchSize: 15 },
            motion: { duration: 360, easing: 'ease-in-out' },
        },
        {
            name: 'dark-technical',
            mode: 'dark',
            colors: {
                background: '#07111f',
                surface: '#0b1728',
                panel: '#091525',
                text: '#dbeafe',
                mutedText: '#8ba3bf',
                axis: '#35506d',
                grid: '#19324d',
                focus: '#22d3ee',
                palette: ['#22d3ee', '#a78bfa', '#fb7185', '#4ade80', '#facc15', '#60a5fa'],
                sequential: ['#082f49', '#075985', '#0284c7', '#22d3ee', '#cffafe'],
                diverging: ['#fb7185', '#9f1239', '#0b1728', '#0e7490', '#67e8f9'],
            },
            typography: {
                fontFamily: '"JetBrains Mono", "SFMono-Regular", Consolas, monospace',
                fontSize: 11,
                titleSize: 17,
                titleWeight: 600,
                subtitleSize: 11,
                lineHeight: 1.35,
            },
            spacing: { xs: 3, sm: 6, md: 10, lg: 14, xl: 20 },
            axis: {
                lineWidth: 1,
                tickLength: 4,
                labelPadding: 6,
                gridLineWidth: 1,
                gridX: true,
                gridY: true,
                gridOpacity: 0.9,
                emphasizeZero: true,
                lineCap: 'square',
            },
            mark: { lineWidth: 2, pointRadius: 3.5, barRadius: 0, opacity: 1, lineCap: 'square' },
            legend: { borderColor: '#35506d', cornerRadius: 0, swatchRadius: 0 },
            motion: { duration: 0, easing: 'linear' },
        },
        {
            name: 'high-contrast',
            mode: 'dark',
            colors: {
                background: '#000000',
                surface: '#000000',
                panel: '#000000',
                text: '#ffffff',
                mutedText: '#f5f5f5',
                axis: '#ffffff',
                grid: '#737373',
                focus: '#ffff00',
                palette: ['#ffff00', '#00ffff', '#ff00ff', '#00ff00', '#ff7f00', '#ffffff'],
                sequential: ['#000000', '#003cff', '#00ffff', '#ffff00', '#ffffff'],
                diverging: ['#ff00ff', '#ff7f00', '#000000', '#00ffff', '#ffffff'],
            },
            typography: {
                fontSize: 14,
                fontWeight: 600,
                titleSize: 24,
                titleWeight: 700,
                subtitleSize: 14,
                subtitleWeight: 600,
                axisLabelSize: 13,
                axisLabelWeight: 600,
                axisTitleSize: 14,
                axisTitleWeight: 700,
                legendLabelSize: 13,
                legendLabelWeight: 600,
                legendTitleSize: 14,
                legendTitleWeight: 700,
                lineHeight: 1.5,
            },
            spacing: { xs: 6, sm: 10, md: 15, lg: 20, xl: 28 },
            axis: {
                lineWidth: 2,
                boxVisible: true,
                boxLineWidth: 2,
                tickLength: 7,
                labelPadding: 9,
                gridLineWidth: 1.5,
                gridX: true,
                gridY: true,
                gridOpacity: 1,
                emphasizeZero: true,
                lineCap: 'square',
            },
            mark: {
                lineWidth: 3,
                pointRadius: 6,
                barRadius: 0,
                opacity: 1,
                pointStroke: '#000000',
                pointStrokeWidth: 2,
                lineCap: 'square',
                lineJoin: 'miter',
            },
            legend: {
                surfaceOpacity: 1,
                borderWidth: 2,
                borderColor: '#ffffff',
                cornerRadius: 0,
                swatchRadius: 0,
                swatchSize: 16,
                lineWidth: 3,
                pointRadius: 6,
                pointStrokeWidth: 2,
                lineCap: 'square',
            },
            motion: { duration: 0, easing: 'linear' },
        },
    ];
    const neutralBuiltInThemes = neutralThemeDefinitions.map((definition) => createNeutralTheme(definition));
    const defaultThemeId = graflumeLight.name;
    /** Ordered source of truth for every built-in theme and generated preview. */
    const builtInThemeCatalog = [
        { id: graflumeLight.name, tokens: graflumeLight, snapshot: false },
        { id: graflumeDark.name, tokens: graflumeDark, snapshot: true },
        {
            id: graflumeGgplot.name,
            tokens: graflumeGgplot,
            snapshot: true,
            sourceBaseline: 'ggplot2 4.0.3',
        },
        {
            id: graflumeRBase.name,
            tokens: graflumeRBase,
            snapshot: true,
            sourceBaseline: 'R 4.6.1',
        },
        {
            id: graflumeMatplotlib.name,
            tokens: graflumeMatplotlib,
            snapshot: true,
            sourceBaseline: 'Matplotlib 3.11.1',
        },
        ...neutralBuiltInThemes.map((tokens) => ({ id: tokens.name, tokens, snapshot: true })),
    ];
    function builtInTheme(name) {
        return builtInThemeCatalog.find((entry) => entry.id === name)?.tokens;
    }

    /** Function-free curve names accepted by line-like portable marks. */
    const curveNames = [
        'straight',
        'step-before',
        'step-after',
        'step-mid',
        'monotone-x',
        'natural',
        'basis',
        'cardinal',
    ];
    function subdivisions(options) {
        const requested = options?.samples;
        return typeof requested === 'number' && Number.isFinite(requested)
            ? Math.max(1, Math.min(64, Math.floor(requested)))
            : 8;
    }
    function boundedTension(options) {
        const requested = options?.tension;
        return typeof requested === 'number' && Number.isFinite(requested)
            ? Math.max(0, Math.min(1, requested))
            : 0;
    }
    function straight(points) {
        return [...points];
    }
    function stepBefore(points) {
        const output = [];
        points.forEach((point, index) => {
            const previous = points[index - 1];
            if (previous !== undefined)
                output.push({ x: previous.x, y: point.y });
            output.push(point);
        });
        return output;
    }
    function stepAfter(points) {
        const output = [];
        points.forEach((point, index) => {
            const previous = points[index - 1];
            if (previous !== undefined)
                output.push({ x: point.x, y: previous.y });
            output.push(point);
        });
        return output;
    }
    function stepMid(points) {
        const first = points[0];
        if (first === undefined)
            return [];
        const output = [first];
        for (let index = 1; index < points.length; index += 1) {
            const previous = points[index - 1];
            const point = points[index];
            if (previous === undefined || point === undefined)
                continue;
            const middle = (previous.x + point.x) / 2;
            output.push({ x: middle, y: previous.y }, { x: middle, y: point.y }, point);
        }
        return output;
    }
    function cardinal(points, options) {
        if (points.length < 3)
            return [...points];
        const samples = subdivisions(options);
        const tangentScale = 1 - boundedTension(options);
        const output = [];
        for (let index = 0; index < points.length - 1; index += 1) {
            const p0 = points[Math.max(0, index - 1)] ?? points[0];
            const p1 = points[index];
            const p2 = points[index + 1];
            const p3 = points[Math.min(points.length - 1, index + 2)] ?? p2;
            const m1 = {
                x: ((p2.x - p0.x) * tangentScale) / 2,
                y: ((p2.y - p0.y) * tangentScale) / 2,
            };
            const m2 = {
                x: ((p3.x - p1.x) * tangentScale) / 2,
                y: ((p3.y - p1.y) * tangentScale) / 2,
            };
            for (let sample = 0; sample < samples; sample += 1) {
                const t = sample / samples;
                const t2 = t * t;
                const t3 = t2 * t;
                const h00 = 2 * t3 - 3 * t2 + 1;
                const h10 = t3 - 2 * t2 + t;
                const h01 = -2 * t3 + 3 * t2;
                const h11 = t3 - t2;
                output.push({
                    x: h00 * p1.x + h10 * m1.x + h01 * p2.x + h11 * m2.x,
                    y: h00 * p1.y + h10 * m1.y + h01 * p2.y + h11 * m2.y,
                });
            }
        }
        output.push(points.at(-1));
        return output;
    }
    function monotoneX(points, options) {
        if (points.length < 3)
            return [...points];
        const direction = Math.sign((points.at(-1)?.x ?? 0) - (points[0]?.x ?? 0));
        if (direction === 0 ||
            points.some((point, index) => {
                const previous = points[index - 1];
                return previous !== undefined && Math.sign(point.x - previous.x) !== direction;
            })) {
            return [...points];
        }
        const slopes = Array.from({ length: points.length - 1 }, (_, index) => {
            const left = points[index];
            const right = points[index + 1];
            return (right.y - left.y) / (right.x - left.x);
        });
        const tangents = Array.from({ length: points.length }, (_, index) => {
            const left = slopes[index - 1];
            const right = slopes[index];
            if (left === undefined)
                return right ?? 0;
            if (right === undefined)
                return left;
            if (left === 0 || right === 0 || Math.sign(left) !== Math.sign(right))
                return 0;
            return (2 * left * right) / (left + right);
        });
        const samples = subdivisions(options);
        const output = [];
        for (let index = 0; index < points.length - 1; index += 1) {
            const left = points[index];
            const right = points[index + 1];
            const span = right.x - left.x;
            for (let sample = 0; sample < samples; sample += 1) {
                const t = sample / samples;
                const t2 = t * t;
                const t3 = t2 * t;
                const h00 = 2 * t3 - 3 * t2 + 1;
                const h10 = t3 - 2 * t2 + t;
                const h01 = -2 * t3 + 3 * t2;
                const h11 = t3 - t2;
                output.push({
                    x: left.x + span * t,
                    y: h00 * left.y +
                        h10 * span * (tangents[index] ?? 0) +
                        h01 * right.y +
                        h11 * span * (tangents[index + 1] ?? 0),
                });
            }
        }
        output.push(points.at(-1));
        return output;
    }
    function naturalCoordinate(values) {
        const count = values.length;
        if (count < 3)
            return Array.from({ length: count }, () => 0);
        const second = Array.from({ length: count }, () => 0);
        const interiorCount = count - 2;
        const upper = [];
        const right = [];
        for (let interior = 0; interior < interiorCount; interior += 1) {
            const index = interior + 1;
            const source = 6 * (values[index - 1] - 2 * values[index] + values[index + 1]);
            const denominator = 4 - (interior === 0 ? 0 : upper[interior - 1]);
            upper[interior] = interior === interiorCount - 1 ? 0 : 1 / denominator;
            right[interior] = (source - (interior === 0 ? 0 : right[interior - 1])) / denominator;
        }
        for (let interior = interiorCount - 1; interior >= 0; interior -= 1) {
            second[interior + 1] =
                right[interior] -
                    (interior === interiorCount - 1 ? 0 : upper[interior] * second[interior + 2]);
        }
        return second;
    }
    function natural(points, options) {
        if (points.length < 3)
            return [...points];
        const xSecond = naturalCoordinate(points.map(({ x }) => x));
        const ySecond = naturalCoordinate(points.map(({ y }) => y));
        const samples = subdivisions(options);
        const output = [];
        for (let index = 0; index < points.length - 1; index += 1) {
            const left = points[index];
            const right = points[index + 1];
            for (let sample = 0; sample < samples; sample += 1) {
                const t = sample / samples;
                const inverse = 1 - t;
                const correctionLeft = (inverse * inverse * inverse - inverse) / 6;
                const correctionRight = (t * t * t - t) / 6;
                output.push({
                    x: inverse * left.x +
                        t * right.x +
                        correctionLeft * xSecond[index] +
                        correctionRight * xSecond[index + 1],
                    y: inverse * left.y +
                        t * right.y +
                        correctionLeft * ySecond[index] +
                        correctionRight * ySecond[index + 1],
                });
            }
        }
        output.push(points.at(-1));
        return output;
    }
    function basis(points, options) {
        if (points.length < 3)
            return [...points];
        const first = points[0];
        const last = points.at(-1);
        const controls = [first, first, ...points, last, last];
        const samples = subdivisions(options);
        const output = [];
        for (let index = 0; index < controls.length - 3; index += 1) {
            const p0 = controls[index];
            const p1 = controls[index + 1];
            const p2 = controls[index + 2];
            const p3 = controls[index + 3];
            for (let sample = 0; sample < samples; sample += 1) {
                const t = sample / samples;
                const t2 = t * t;
                const t3 = t2 * t;
                const b0 = (1 - 3 * t + 3 * t2 - t3) / 6;
                const b1 = (4 - 6 * t2 + 3 * t3) / 6;
                const b2 = (1 + 3 * t + 3 * t2 - 3 * t3) / 6;
                const b3 = t3 / 6;
                output.push({
                    x: b0 * p0.x + b1 * p1.x + b2 * p2.x + b3 * p3.x,
                    y: b0 * p0.y + b1 * p1.y + b2 * p2.y + b3 * p3.y,
                });
            }
        }
        output[0] = first;
        output.push(last);
        return output;
    }
    /** Immutable renderer-neutral curve registry used by every line-like compiler. */
    const curveRegistry = Object.freeze({
        straight,
        'step-before': stepBefore,
        'step-after': stepAfter,
        'step-mid': stepMid,
        'monotone-x': monotoneX,
        natural,
        basis,
        cardinal,
    });
    function isCurveName(value) {
        return typeof value === 'string' && curveNames.includes(value);
    }
    function resolveCurveName(value, fallback) {
        return isCurveName(value) ? value : fallback;
    }
    function interpolateCurve(points, curve, options) {
        return curveRegistry[curve](points, options);
    }

    const seriesStackModes = [
        'grouped',
        'stacked',
        '100-percent',
        'diverging',
        'streamgraph',
    ];
    const STACK_FIELD_KEYS = {
        source: '__stackSource',
        start: '__stackStart',
        end: '__stackEnd',
        absoluteTotal: '__stackAbsoluteTotal',
        positiveTotal: '__stackPositiveTotal',
        negativeTotal: '__stackNegativeTotal',
        netTotal: '__stackNetTotal',
        series: '__stackSeries',
        category: '__stackCategory',
    };
    function plainObject(value) {
        return value !== null &&
            typeof value === 'object' &&
            Object.prototype.toString.call(value) === '[object Object]'
            ? value
            : null;
    }
    function stackMode(value) {
        return typeof value === 'string' && seriesStackModes.includes(value)
            ? value
            : null;
    }
    function defaultOffset(mode) {
        if (mode === 'grouped')
            return null;
        if (mode === '100-percent')
            return 'normalize';
        if (mode === 'streamgraph')
            return 'wiggle';
        return 'zero';
    }
    function defaultOrder(mode) {
        return mode === 'streamgraph' ? 'insideOut' : 'input';
    }
    function safeToken$1(value) {
        let hash = 2_166_136_261;
        for (const character of value) {
            hash ^= character.codePointAt(0) ?? 0;
            hash = Math.imul(hash, 16_777_619);
        }
        return (hash >>> 0).toString(36);
    }
    function internalFields(layer) {
        const suffix = safeToken$1(`${layer.id}\u0000${layer.x.field}\u0000${layer.y.field}\u0000${layer.mark.fields.series ?? ''}`);
        const field = (name) => `__graflume_${name}_${suffix}`;
        return {
            source: field('stack_source'),
            start: field('stack_start'),
            end: field('stack_end'),
            absoluteTotal: field('stack_total'),
            positiveTotal: field('stack_positive_total'),
            negativeTotal: field('stack_negative_total'),
            netTotal: field('stack_net_total'),
            series: layer.mark.fields.series ?? layer.mark.fields.category ?? 'series',
            category: layer.mark.orientation === 'horizontal' && layer.mark.type === 'bar'
                ? layer.y.field
                : layer.x.field,
        };
    }
    function optionSort(value) {
        if (!Array.isArray(value))
            return undefined;
        const fields = value.flatMap((item) => {
            const object = plainObject(item);
            if (object === null || typeof object.field !== 'string')
                return [];
            const order = object.order;
            const entry = {
                field: object.field,
                ...(order === 'ascending' || order === 'descending' ? { order } : {}),
            };
            return [entry];
        });
        return fields.length === value.length ? fields : undefined;
    }
    /**
     * Resolve the public series layout without executing data. Theme river keeps its
     * historical centered layout unless the streamgraph preset opts into wiggle.
     */
    function resolveSeriesStackSpec(layer) {
        if (!['area', 'bar', 'theme-river'].includes(layer.mark.type))
            return null;
        const authored = layer.mark.options.stack;
        const object = plainObject(authored);
        const implicitThemeRiver = layer.mark.type === 'theme-river' && authored === undefined;
        const mode = implicitThemeRiver
            ? 'streamgraph'
            : (stackMode(authored) ?? stackMode(object?.mode) ?? null);
        if (mode === null)
            return null;
        const fields = internalFields(layer);
        const authoredOffset = object?.offset;
        const offset = typeof authoredOffset === 'string' &&
            ['zero', 'normalize', 'expand', 'center', 'silhouette', 'wiggle'].includes(authoredOffset)
            ? authoredOffset
            : implicitThemeRiver
                ? 'silhouette'
                : defaultOffset(mode);
        const authoredOrder = object?.order;
        const order = typeof authoredOrder === 'string' &&
            ['input', 'ascending', 'descending', 'sumAscending', 'sumDescending', 'insideOut'].includes(authoredOrder)
            ? authoredOrder
            : implicitThemeRiver
                ? 'input'
                : defaultOrder(mode);
        const sort = object === null ? undefined : optionSort(object.sort);
        return {
            mode,
            offset,
            order,
            ...(sort === undefined ? {} : { sort }),
            seriesField: fields.series,
            categoryField: fields.category,
            valueField: layer.mark.orientation === 'horizontal' && layer.mark.type === 'bar'
                ? layer.x.field
                : layer.y.field,
        };
    }
    function calculatedTotalTransforms(spec, fields) {
        const source = { op: 'field', field: spec.valueField };
        const zero = { op: 'literal', value: 0 };
        return [
            { type: 'calculate', as: fields.source, expr: source },
            {
                type: 'calculate',
                as: fields.absoluteTotal,
                expr: {
                    op: 'if',
                    condition: { op: 'lessThan', left: source, right: zero },
                    then: { op: 'negate', value: source },
                    else: source,
                },
            },
            {
                type: 'calculate',
                as: fields.positiveTotal,
                expr: {
                    op: 'if',
                    condition: { op: 'greaterThan', left: source, right: zero },
                    then: source,
                    else: zero,
                },
            },
            {
                type: 'calculate',
                as: fields.negativeTotal,
                expr: {
                    op: 'if',
                    condition: { op: 'lessThan', left: source, right: zero },
                    then: { op: 'negate', value: source },
                    else: zero,
                },
            },
            {
                type: 'joinaggregate',
                groupby: [spec.categoryField],
                fields: [
                    { op: 'sum', field: fields.source, as: fields.netTotal },
                    { op: 'sum', field: fields.absoluteTotal, as: fields.absoluteTotal },
                    { op: 'sum', field: fields.positiveTotal, as: fields.positiveTotal },
                    { op: 'sum', field: fields.negativeTotal, as: fields.negativeTotal },
                ],
            },
        ];
    }
    /** Append the shared transform and annotate only the internal layer copy. */
    function prepareSeriesStackLayer(layer) {
        const spec = resolveSeriesStackSpec(layer);
        if (spec === null)
            return null;
        const fields = internalFields(layer);
        const transforms = spec.offset === null
            ? []
            : [
                ...calculatedTotalTransforms(spec, fields),
                {
                    type: 'stack',
                    field: fields.source,
                    groupby: [spec.categoryField],
                    series: [spec.seriesField],
                    ...(spec.sort === undefined ? {} : { sort: spec.sort }),
                    as: [fields.start, fields.end],
                    offset: spec.offset,
                    order: spec.order,
                },
            ];
        return {
            spec,
            fields,
            layer: {
                ...layer,
                transform: [...layer.transform, ...transforms],
                mark: {
                    ...layer.mark,
                    fields: {
                        ...layer.mark.fields,
                        [STACK_FIELD_KEYS.source]: fields.source,
                        [STACK_FIELD_KEYS.start]: fields.start,
                        [STACK_FIELD_KEYS.end]: fields.end,
                        [STACK_FIELD_KEYS.absoluteTotal]: fields.absoluteTotal,
                        [STACK_FIELD_KEYS.positiveTotal]: fields.positiveTotal,
                        [STACK_FIELD_KEYS.negativeTotal]: fields.negativeTotal,
                        [STACK_FIELD_KEYS.netTotal]: fields.netTotal,
                        [STACK_FIELD_KEYS.series]: fields.series,
                        [STACK_FIELD_KEYS.category]: fields.category,
                    },
                },
            },
        };
    }
    function preparedSeriesStackFields(layer) {
        const read = (key) => layer.mark.fields[STACK_FIELD_KEYS[key]];
        const source = read('source');
        const start = read('start');
        const end = read('end');
        const absoluteTotal = read('absoluteTotal');
        const positiveTotal = read('positiveTotal');
        const negativeTotal = read('negativeTotal');
        const netTotal = read('netTotal');
        const series = read('series');
        const category = read('category');
        if (source === undefined ||
            start === undefined ||
            end === undefined ||
            absoluteTotal === undefined ||
            positiveTotal === undefined ||
            negativeTotal === undefined ||
            netTotal === undefined ||
            series === undefined ||
            category === undefined) {
            return null;
        }
        return {
            source,
            start,
            end,
            absoluteTotal,
            positiveTotal,
            negativeTotal,
            netTotal,
            series,
            category,
        };
    }

    function empty(length) {
        return Array.from({ length }).fill(null);
    }
    function finite$c(value) {
        return typeof value === 'number' && Number.isFinite(value);
    }
    function simpleAverage$1(values, period) {
        const output = empty(values.length);
        let sum = 0;
        let valid = 0;
        for (let index = 0; index < values.length; index += 1) {
            const value = values[index];
            if (finite$c(value)) {
                sum += value;
                valid += 1;
            }
            const removed = values[index - period];
            if (finite$c(removed)) {
                sum -= removed;
                valid -= 1;
            }
            if (index + 1 >= period && valid === period)
                output[index] = sum / period;
        }
        return output;
    }
    function exponentialAverage$2(values, period) {
        const output = empty(values.length);
        const alpha = 2 / (period + 1);
        let seed = [];
        let previous = null;
        values.forEach((value, index) => {
            if (!finite$c(value)) {
                seed = [];
                previous = null;
                return;
            }
            if (previous === null) {
                seed.push(value);
                if (seed.length < period)
                    return;
                if (seed.length > period)
                    seed.shift();
                previous = seed.reduce((sum, item) => sum + item, 0) / period;
            }
            else {
                previous = value * alpha + previous * (1 - alpha);
            }
            output[index] = previous;
        });
        return output;
    }
    function rollingSum$1(values, period) {
        const output = empty(values.length);
        let sum = 0;
        let valid = 0;
        for (let index = 0; index < values.length; index += 1) {
            const value = values[index];
            if (finite$c(value)) {
                sum += value;
                valid += 1;
            }
            const removed = values[index - period];
            if (finite$c(removed)) {
                sum -= removed;
                valid -= 1;
            }
            if (index + 1 >= period && valid === period)
                output[index] = sum;
        }
        return output;
    }
    function rollingDeviation$1(values, averages, period) {
        return values.map((_, index) => {
            const average = averages[index];
            if (!finite$c(average) || index + 1 < period)
                return null;
            let sum = 0;
            for (let offset = index + 1 - period; offset <= index; offset += 1) {
                const value = values[offset];
                if (!finite$c(value))
                    return null;
                sum += Math.abs(value - average);
            }
            return sum / period;
        });
    }
    function rollingStandardDeviation(values, averages, period) {
        return values.map((_, index) => {
            const average = averages[index];
            if (!finite$c(average) || index + 1 < period)
                return null;
            let sum = 0;
            for (let offset = index + 1 - period; offset <= index; offset += 1) {
                const value = values[offset];
                if (!finite$c(value))
                    return null;
                sum += (value - average) ** 2;
            }
            return Math.sqrt(sum / period);
        });
    }
    function rollingExtrema$1(values, period, mode) {
        return values.map((_, index) => {
            if (index + 1 < period)
                return null;
            let result = mode === 'minimum' ? Number.POSITIVE_INFINITY : Number.NEGATIVE_INFINITY;
            for (let offset = index + 1 - period; offset <= index; offset += 1) {
                const value = values[offset];
                if (!finite$c(value))
                    return null;
                result = mode === 'minimum' ? Math.min(result, value) : Math.max(result, value);
            }
            return result;
        });
    }
    function rollingRangePosition$1(high, low, close, period) {
        const highest = rollingExtrema$1(high, period, 'maximum');
        const lowest = rollingExtrema$1(low, period, 'minimum');
        return close.map((value, index) => {
            const upper = highest[index];
            const lower = lowest[index];
            if (!finite$c(value) || !finite$c(upper) || !finite$c(lower))
                return null;
            return upper === lower ? 0 : ((value - lower) / (upper - lower)) * 100;
        });
    }
    function wilderAverage$1(values, period) {
        const output = empty(values.length);
        let seed = [];
        let previous = null;
        values.forEach((value, index) => {
            if (!finite$c(value)) {
                seed = [];
                previous = null;
                return;
            }
            if (previous === null) {
                seed.push(value);
                if (seed.length < period)
                    return;
                if (seed.length > period)
                    seed.shift();
                previous = seed.reduce((sum, item) => sum + item, 0) / period;
            }
            else {
                previous = (previous * (period - 1) + value) / period;
            }
            output[index] = previous;
        });
        return output;
    }
    function trueRange$1(high, low, close) {
        return high.map((highValue, index) => {
            const lowValue = low[index];
            if (!finite$c(highValue) || !finite$c(lowValue))
                return null;
            const previousClose = close[index - 1];
            return !finite$c(previousClose)
                ? highValue - lowValue
                : Math.max(highValue - lowValue, Math.abs(highValue - previousClose), Math.abs(lowValue - previousClose));
        });
    }
    function typicalPrice(high, low, close) {
        return high.map((highValue, index) => {
            const lowValue = low[index];
            const closeValue = close[index];
            return finite$c(highValue) && finite$c(lowValue) && finite$c(closeValue)
                ? (highValue + lowValue + closeValue) / 3
                : null;
        });
    }
    function medianPrice(high, low) {
        return high.map((highValue, index) => {
            const lowValue = low[index];
            return finite$c(highValue) && finite$c(lowValue) ? (highValue + lowValue) / 2 : null;
        });
    }
    function pairDifference$1(left, right) {
        return left.map((value, index) => {
            const baseline = right[index];
            return finite$c(value) && finite$c(baseline) ? value - baseline : null;
        });
    }
    function accelerationBands$1(inputs, period, multiplier) {
        const rawUpper = inputs.high.map((high, index) => {
            const low = inputs.low[index];
            if (!finite$c(high) || !finite$c(low) || high + low === 0)
                return null;
            return high * (1 + (multiplier * (high - low)) / (high + low));
        });
        const rawLower = inputs.low.map((low, index) => {
            const high = inputs.high[index];
            if (!finite$c(high) || !finite$c(low) || high + low === 0)
                return null;
            return low * (1 - (multiplier * (high - low)) / (high + low));
        });
        return {
            outputs: {
                value: simpleAverage$1(inputs.close, period),
                lower: simpleAverage$1(rawLower, period),
                upper: simpleAverage$1(rawUpper, period),
            },
            warmUpRows: period - 1,
        };
    }
    function aroon$1(inputs, period) {
        const up = empty(inputs.high.length);
        const down = empty(inputs.high.length);
        for (let index = period; index < inputs.high.length; index += 1) {
            let highIndex = -1;
            let lowIndex = -1;
            let highest = Number.NEGATIVE_INFINITY;
            let lowest = Number.POSITIVE_INFINITY;
            for (let cursor = index - period; cursor <= index; cursor += 1) {
                const high = inputs.high[cursor];
                const low = inputs.low[cursor];
                if (!finite$c(high) || !finite$c(low)) {
                    highIndex = -1;
                    break;
                }
                if (high >= highest) {
                    highest = high;
                    highIndex = cursor;
                }
                if (low <= lowest) {
                    lowest = low;
                    lowIndex = cursor;
                }
            }
            if (highIndex < 0 || lowIndex < 0)
                continue;
            up[index] = ((period - (index - highIndex)) / period) * 100;
            down[index] = ((period - (index - lowIndex)) / period) * 100;
        }
        return { up, down };
    }
    function bollinger(values, period, deviations) {
        const value = simpleAverage$1(values, period);
        const deviation = rollingStandardDeviation(values, value, period);
        return {
            outputs: {
                value,
                lower: value.map((middle, index) => finite$c(middle) && finite$c(deviation[index]) ? middle - deviations * deviation[index] : null),
                upper: value.map((middle, index) => finite$c(middle) && finite$c(deviation[index]) ? middle + deviations * deviation[index] : null),
            },
            warmUpRows: period - 1,
        };
    }
    function accumulationDistribution(inputs) {
        const output = empty(inputs.close.length);
        let cumulative = 0;
        inputs.close.forEach((close, index) => {
            const high = inputs.high[index];
            const low = inputs.low[index];
            const volume = inputs.volume[index];
            if (!finite$c(close) || !finite$c(high) || !finite$c(low) || !finite$c(volume))
                return;
            const multiplier = high === low ? 0 : (close - low - (high - close)) / (high - low);
            cumulative += multiplier * volume;
            output[index] = cumulative;
        });
        return output;
    }
    function commodityChannelIndex$1(inputs, period) {
        const typical = typicalPrice(inputs.high, inputs.low, inputs.close);
        const average = simpleAverage$1(typical, period);
        const deviation = rollingDeviation$1(typical, average, period);
        return {
            outputs: {
                value: typical.map((item, index) => {
                    const baseline = average[index];
                    const meanDeviation = deviation[index];
                    if (!finite$c(item) || !finite$c(baseline) || !finite$c(meanDeviation))
                        return null;
                    return meanDeviation === 0 ? 0 : (item - baseline) / (0.015 * meanDeviation);
                }),
            },
            warmUpRows: period - 1,
        };
    }
    function chaikinMoneyFlow$1(inputs, period) {
        const flow = inputs.close.map((close, index) => {
            const high = inputs.high[index];
            const low = inputs.low[index];
            const volume = inputs.volume[index];
            if (!finite$c(close) || !finite$c(high) || !finite$c(low) || !finite$c(volume))
                return null;
            return (high === low ? 0 : (close - low - (high - close)) / (high - low)) * volume;
        });
        const flowSum = rollingSum$1(flow, period);
        const volumeSum = rollingSum$1(inputs.volume, period);
        return {
            outputs: {
                value: flowSum.map((value, index) => {
                    const volume = volumeSum[index];
                    return finite$c(value) && finite$c(volume) && volume !== 0 ? value / volume : null;
                }),
            },
            warmUpRows: period - 1,
        };
    }
    function chandeMomentum(values, period) {
        const gains = empty(values.length);
        const losses = empty(values.length);
        for (let index = 1; index < values.length; index += 1) {
            const value = values[index];
            const previous = values[index - 1];
            if (!finite$c(value) || !finite$c(previous))
                continue;
            gains[index] = Math.max(0, value - previous);
            losses[index] = Math.max(0, previous - value);
        }
        const gainSum = rollingSum$1(gains, period);
        const lossSum = rollingSum$1(losses, period);
        return {
            outputs: {
                value: gainSum.map((gain, index) => {
                    const loss = lossSum[index];
                    if (!finite$c(gain) || !finite$c(loss))
                        return null;
                    return gain + loss === 0 ? 0 : ((gain - loss) / (gain + loss)) * 100;
                }),
            },
            warmUpRows: period,
        };
    }
    function directionalMovement(inputs, period) {
        const ranges = trueRange$1(inputs.high, inputs.low, inputs.close);
        const plusMovement = empty(inputs.high.length);
        const minusMovement = empty(inputs.high.length);
        for (let index = 1; index < inputs.high.length; index += 1) {
            const high = inputs.high[index];
            const low = inputs.low[index];
            const previousHigh = inputs.high[index - 1];
            const previousLow = inputs.low[index - 1];
            if (![high, low, previousHigh, previousLow].every(finite$c))
                continue;
            const up = high - previousHigh;
            const down = previousLow - low;
            plusMovement[index] = up > down && up > 0 ? up : 0;
            minusMovement[index] = down > up && down > 0 ? down : 0;
        }
        const averageRange = wilderAverage$1(ranges.slice(1), period);
        const averagePlus = wilderAverage$1(plusMovement.slice(1), period);
        const averageMinus = wilderAverage$1(minusMovement.slice(1), period);
        const plus = empty(inputs.high.length);
        const minus = empty(inputs.high.length);
        const dx = empty(inputs.high.length);
        for (let index = 1; index < inputs.high.length; index += 1) {
            const range = averageRange[index - 1];
            const positive = averagePlus[index - 1];
            const negative = averageMinus[index - 1];
            if (!finite$c(range) || !finite$c(positive) || !finite$c(negative) || range === 0)
                continue;
            plus[index] = (positive / range) * 100;
            minus[index] = (negative / range) * 100;
            const sum = plus[index] + minus[index];
            dx[index] = sum === 0 ? 0 : (Math.abs(plus[index] - minus[index]) / sum) * 100;
        }
        const value = wilderAverage$1(dx, period);
        return { outputs: { value, plus, minus }, warmUpRows: 2 * period - 1 };
    }
    function ichimoku(inputs, parameters) {
        const conversionPeriod = parameters.conversionPeriod;
        const basePeriod = parameters.basePeriod;
        const spanPeriod = parameters.spanPeriod;
        const displacement = parameters.displacement;
        const midpoint = (period) => {
            const high = rollingExtrema$1(inputs.high, period, 'maximum');
            const low = rollingExtrema$1(inputs.low, period, 'minimum');
            return high.map((value, index) => finite$c(value) && finite$c(low[index]) ? (value + low[index]) / 2 : null);
        };
        const conversion = midpoint(conversionPeriod);
        const base = midpoint(basePeriod);
        const spanA = conversion.map((value, index) => finite$c(value) && finite$c(base[index]) ? (value + base[index]) / 2 : null);
        const spanB = midpoint(spanPeriod);
        const value = empty(inputs.high.length);
        const lower = empty(inputs.high.length);
        const upper = empty(inputs.high.length);
        spanA.forEach((leadingA, index) => {
            const leadingB = spanB[index];
            const target = index + displacement;
            if (target >= value.length || !finite$c(leadingA) || !finite$c(leadingB))
                return;
            value[target] = leadingA;
            lower[target] = Math.min(leadingA, leadingB);
            upper[target] = Math.max(leadingA, leadingB);
        });
        return {
            outputs: { value, lower, upper, conversion, base },
            warmUpRows: spanPeriod - 1 + displacement,
        };
    }
    function keltner(inputs, parameters) {
        const period = parameters.period;
        const atrPeriod = parameters.atrPeriod;
        const multiplier = parameters.multiplier;
        const value = exponentialAverage$2(inputs.close, period);
        const atr = wilderAverage$1(trueRange$1(inputs.high, inputs.low, inputs.close), atrPeriod);
        return {
            outputs: {
                value,
                lower: value.map((middle, index) => finite$c(middle) && finite$c(atr[index]) ? middle - multiplier * atr[index] : null),
                upper: value.map((middle, index) => finite$c(middle) && finite$c(atr[index]) ? middle + multiplier * atr[index] : null),
            },
            warmUpRows: Math.max(period, atrPeriod) - 1,
        };
    }
    function klinger(inputs, parameters) {
        const force = empty(inputs.close.length);
        let previousTrend = 0;
        let previousMovement = 0;
        let cumulativeMovement = 0;
        for (let index = 1; index < inputs.close.length; index += 1) {
            const current = [
                inputs.high[index],
                inputs.low[index],
                inputs.close[index],
                inputs.volume[index],
            ];
            const previous = [inputs.high[index - 1], inputs.low[index - 1], inputs.close[index - 1]];
            if (![...current, ...previous].every(finite$c)) {
                previousTrend = 0;
                previousMovement = 0;
                cumulativeMovement = 0;
                continue;
            }
            const currentSum = current[0] + current[1] + current[2];
            const previousSum = previous[0] + previous[1] + previous[2];
            const trend = currentSum > previousSum ? 1 : currentSum < previousSum ? -1 : previousTrend || 1;
            const movement = current[0] - current[1];
            cumulativeMovement =
                trend === previousTrend ? cumulativeMovement + movement : previousMovement + movement;
            force[index] =
                cumulativeMovement === 0
                    ? 0
                    : current[3] * Math.abs(2 * (movement / cumulativeMovement - 1)) * trend * 100;
            previousTrend = trend;
            previousMovement = movement;
        }
        const fast = exponentialAverage$2(force, parameters.fastPeriod);
        const slow = exponentialAverage$2(force, parameters.slowPeriod);
        const value = pairDifference$1(fast, slow);
        const signal = exponentialAverage$2(value, parameters.signalPeriod);
        return {
            outputs: { value, signal },
            warmUpRows: parameters.slowPeriod + parameters.signalPeriod - 1,
        };
    }
    function moneyFlow(inputs, period) {
        const typical = typicalPrice(inputs.high, inputs.low, inputs.close);
        const positive = empty(inputs.close.length);
        const negative = empty(inputs.close.length);
        for (let index = 1; index < inputs.close.length; index += 1) {
            const price = typical[index];
            const previous = typical[index - 1];
            const volume = inputs.volume[index];
            if (!finite$c(price) || !finite$c(previous) || !finite$c(volume))
                continue;
            const flow = price * volume;
            positive[index] = price > previous ? flow : 0;
            negative[index] = price < previous ? flow : 0;
        }
        const positiveSum = rollingSum$1(positive, period);
        const negativeSum = rollingSum$1(negative, period);
        return {
            outputs: {
                value: positiveSum.map((gain, index) => {
                    const loss = negativeSum[index];
                    if (!finite$c(gain) || !finite$c(loss))
                        return null;
                    if (loss === 0)
                        return gain === 0 ? 50 : 100;
                    return 100 - 100 / (1 + gain / loss);
                }),
            },
            warmUpRows: period,
        };
    }
    function onBalanceVolume$1(inputs) {
        const value = empty(inputs.close.length);
        let balance = 0;
        inputs.close.forEach((close, index) => {
            const volume = inputs.volume[index];
            if (!finite$c(close) || !finite$c(volume))
                return;
            const previous = inputs.close[index - 1];
            if (index > 0 && finite$c(previous)) {
                if (close > previous)
                    balance += volume;
                else if (close < previous)
                    balance -= volume;
            }
            value[index] = balance;
        });
        return { outputs: { value }, warmUpRows: 0 };
    }
    function pivotPoints$1(inputs) {
        const value = empty(inputs.close.length);
        const support = empty(inputs.close.length);
        const resistance = empty(inputs.close.length);
        for (let index = 1; index < inputs.close.length; index += 1) {
            const high = inputs.high[index - 1];
            const low = inputs.low[index - 1];
            const close = inputs.close[index - 1];
            if (!finite$c(high) || !finite$c(low) || !finite$c(close))
                continue;
            const pivot = (high + low + close) / 3;
            value[index] = pivot;
            support[index] = 2 * pivot - high;
            resistance[index] = 2 * pivot - low;
        }
        return { outputs: { value, support, resistance }, warmUpRows: 1 };
    }
    function parabolicSar(inputs, acceleration, maximumAcceleration) {
        const value = empty(inputs.high.length);
        if (inputs.high.length < 2)
            return { outputs: { value }, warmUpRows: 1 };
        const firstHigh = inputs.high[0];
        const firstLow = inputs.low[0];
        const secondHigh = inputs.high[1];
        const secondLow = inputs.low[1];
        if (![firstHigh, firstLow, secondHigh, secondLow].every(finite$c)) {
            return { outputs: { value }, warmUpRows: 1 };
        }
        let rising = (secondHigh + secondLow) / 2 >= (firstHigh + firstLow) / 2;
        let extreme = rising ? Math.max(firstHigh, secondHigh) : Math.min(firstLow, secondLow);
        let sar = rising ? Math.min(firstLow, secondLow) : Math.max(firstHigh, secondHigh);
        let factor = acceleration;
        value[1] = sar;
        for (let index = 2; index < inputs.high.length; index += 1) {
            const high = inputs.high[index];
            const low = inputs.low[index];
            if (!finite$c(high) || !finite$c(low))
                continue;
            sar += factor * (extreme - sar);
            if (rising) {
                sar = Math.min(sar, inputs.low[index - 1] ?? sar, inputs.low[index - 2] ?? sar);
                if (low < sar) {
                    rising = false;
                    sar = extreme;
                    extreme = low;
                    factor = acceleration;
                }
                else if (high > extreme) {
                    extreme = high;
                    factor = Math.min(maximumAcceleration, factor + acceleration);
                }
            }
            else {
                sar = Math.max(sar, inputs.high[index - 1] ?? sar, inputs.high[index - 2] ?? sar);
                if (high > sar) {
                    rising = true;
                    sar = extreme;
                    extreme = high;
                    factor = acceleration;
                }
                else if (low < extreme) {
                    extreme = low;
                    factor = Math.min(maximumAcceleration, factor + acceleration);
                }
            }
            value[index] = sar;
        }
        return { outputs: { value }, warmUpRows: 1 };
    }
    function stochastic$1(inputs, period, smoothK, smoothD) {
        const fast = rollingRangePosition$1(inputs.high, inputs.low, inputs.close, period);
        const value = smoothK === 1 ? fast : simpleAverage$1(fast, smoothK);
        const signal = smoothD === 1 ? [...value] : simpleAverage$1(value, smoothD);
        return {
            outputs: { value, signal },
            warmUpRows: period - 1 + (smoothK - 1) + (smoothD - 1),
        };
    }
    function supertrend$1(inputs, parameters) {
        const atr = wilderAverage$1(trueRange$1(inputs.high, inputs.low, inputs.close), parameters.atrPeriod);
        const value = empty(inputs.close.length);
        const direction = empty(inputs.close.length);
        let finalUpper = null;
        let finalLower = null;
        let previousTrend = null;
        for (let index = 0; index < inputs.close.length; index += 1) {
            const high = inputs.high[index];
            const low = inputs.low[index];
            const close = inputs.close[index];
            const range = atr[index];
            if (!finite$c(high) || !finite$c(low) || !finite$c(close) || !finite$c(range))
                continue;
            const middle = (high + low) / 2;
            const basicUpper = middle + parameters.multiplier * range;
            const basicLower = middle - parameters.multiplier * range;
            const previousClose = inputs.close[index - 1];
            finalUpper =
                finalUpper === null ||
                    !finite$c(previousClose) ||
                    basicUpper < finalUpper ||
                    previousClose > finalUpper
                    ? basicUpper
                    : finalUpper;
            finalLower =
                finalLower === null ||
                    !finite$c(previousClose) ||
                    basicLower > finalLower ||
                    previousClose < finalLower
                    ? basicLower
                    : finalLower;
            if (previousTrend === null)
                previousTrend = close <= finalUpper ? -1 : 1;
            else if (previousTrend < 0 && close > finalUpper)
                previousTrend = 1;
            else if (previousTrend > 0 && close < finalLower)
                previousTrend = -1;
            direction[index] = previousTrend;
            value[index] = previousTrend > 0 ? finalLower : finalUpper;
        }
        return { outputs: { value, direction }, warmUpRows: parameters.atrPeriod - 1 };
    }
    function volumeWeightedAveragePrice$1(inputs) {
        const typical = typicalPrice(inputs.high, inputs.low, inputs.close);
        const value = empty(inputs.close.length);
        let weighted = 0;
        let volumeTotal = 0;
        typical.forEach((price, index) => {
            const volume = inputs.volume[index];
            if (!finite$c(price) || !finite$c(volume))
                return;
            weighted += price * volume;
            volumeTotal += volume;
            value[index] = volumeTotal === 0 ? null : weighted / volumeTotal;
        });
        return { outputs: { value }, warmUpRows: 0 };
    }
    function zigzag$1(values, deviation) {
        const output = empty(values.length);
        const threshold = deviation / 100;
        const firstIndex = values.findIndex(finite$c);
        if (firstIndex < 0)
            return { outputs: { value: output }, warmUpRows: 1 };
        let candidateIndex = firstIndex;
        let candidate = values[firstIndex];
        let direction = 0;
        output[firstIndex] = candidate;
        for (let index = firstIndex + 1; index < values.length; index += 1) {
            const value = values[index];
            if (!finite$c(value))
                continue;
            if (direction >= 0) {
                if (value >= candidate) {
                    output[candidateIndex] = null;
                    candidate = value;
                    candidateIndex = index;
                    output[candidateIndex] = candidate;
                }
                else if (candidate !== 0 && (candidate - value) / Math.abs(candidate) >= threshold) {
                    direction = -1;
                    candidate = value;
                    candidateIndex = index;
                    output[candidateIndex] = candidate;
                }
            }
            else if (direction <= 0) {
                if (value <= candidate) {
                    output[candidateIndex] = null;
                    candidate = value;
                    candidateIndex = index;
                    output[candidateIndex] = candidate;
                }
                else if (candidate !== 0 && (value - candidate) / Math.abs(candidate) >= threshold) {
                    direction = 1;
                    candidate = value;
                    candidateIndex = index;
                    output[candidateIndex] = candidate;
                }
            }
        }
        output[candidateIndex] = candidate;
        return { outputs: { value: output }, warmUpRows: 1 };
    }
    /**
     * Calculations that historically required supplied columns. The caller owns
     * identifier/parameter/input validation and session segmentation.
     */
    function calculateOhlcvTechnicalIndicator(kind, inputs, parameters) {
        switch (kind) {
            case 'abands':
                return accelerationBands$1(inputs, parameters.period, parameters.multiplier);
            case 'ao': {
                const median = medianPrice(inputs.high, inputs.low);
                return {
                    outputs: {
                        value: pairDifference$1(simpleAverage$1(median, parameters.fastPeriod), simpleAverage$1(median, parameters.slowPeriod)),
                    },
                    warmUpRows: parameters.slowPeriod - 1,
                };
            }
            case 'aroon': {
                const output = aroon$1(inputs, parameters.period);
                return { outputs: { value: output.up, ...output }, warmUpRows: parameters.period };
            }
            case 'aroonoscillator': {
                const output = aroon$1(inputs, parameters.period);
                return {
                    outputs: { value: pairDifference$1(output.up, output.down) },
                    warmUpRows: parameters.period,
                };
            }
            case 'atr':
                return {
                    outputs: {
                        value: wilderAverage$1(trueRange$1(inputs.high, inputs.low, inputs.close), parameters.period),
                    },
                    warmUpRows: parameters.period - 1,
                };
            case 'bb':
                return bollinger(inputs.value, parameters.period, parameters.standardDeviations);
            case 'cci':
                return commodityChannelIndex$1(inputs, parameters.period);
            case 'chaikin': {
                const line = accumulationDistribution(inputs);
                return {
                    outputs: {
                        value: pairDifference$1(exponentialAverage$2(line, parameters.fastPeriod), exponentialAverage$2(line, parameters.slowPeriod)),
                    },
                    warmUpRows: parameters.slowPeriod - 1,
                };
            }
            case 'cmf':
                return chaikinMoneyFlow$1(inputs, parameters.period);
            case 'cmo':
                return chandeMomentum(inputs.value, parameters.period);
            case 'dmi':
                return directionalMovement(inputs, parameters.period);
            case 'dpo': {
                const average = simpleAverage$1(inputs.value, parameters.period);
                const shift = Math.floor(parameters.period / 2) + 1;
                return {
                    outputs: {
                        value: average.map((baseline, index) => {
                            const source = inputs.value[index - shift];
                            return finite$c(baseline) && finite$c(source) ? source - baseline : null;
                        }),
                    },
                    warmUpRows: parameters.period - 1,
                };
            }
            case 'ikh':
                return ichimoku(inputs, parameters);
            case 'keltnerchannels':
                return keltner(inputs, parameters);
            case 'klinger':
                return klinger(inputs, parameters);
            case 'mfi':
                return moneyFlow(inputs, parameters.period);
            case 'natr': {
                const atr = wilderAverage$1(trueRange$1(inputs.high, inputs.low, inputs.close), parameters.period);
                return {
                    outputs: {
                        value: atr.map((range, index) => {
                            const close = inputs.close[index];
                            return finite$c(range) && finite$c(close) && close !== 0 ? (range / close) * 100 : null;
                        }),
                    },
                    warmUpRows: parameters.period - 1,
                };
            }
            case 'obv':
                return onBalanceVolume$1(inputs);
            case 'pc': {
                const upper = rollingExtrema$1(inputs.high, parameters.period, 'maximum');
                const lower = rollingExtrema$1(inputs.low, parameters.period, 'minimum');
                return {
                    outputs: {
                        value: upper.map((high, index) => finite$c(high) && finite$c(lower[index]) ? (high + lower[index]) / 2 : null),
                        lower,
                        upper,
                    },
                    warmUpRows: parameters.period - 1,
                };
            }
            case 'pivotpoints':
                return pivotPoints$1(inputs);
            case 'priceenvelopes': {
                const value = simpleAverage$1(inputs.value, parameters.period);
                const factor = parameters.envelopePercent / 100;
                return {
                    outputs: {
                        value,
                        lower: value.map((item) => (finite$c(item) ? item * (1 - factor) : null)),
                        upper: value.map((item) => (finite$c(item) ? item * (1 + factor) : null)),
                    },
                    warmUpRows: parameters.period - 1,
                };
            }
            case 'psar':
                return parabolicSar(inputs, parameters.acceleration, parameters.maximumAcceleration);
            case 'slowstochastic':
                return stochastic$1(inputs, parameters.period, parameters.smoothK, parameters.smoothD);
            case 'stochastic':
                return stochastic$1(inputs, parameters.period, 1, parameters.signalPeriod);
            case 'supertrend':
                return supertrend$1(inputs, parameters);
            case 'vwap':
                return volumeWeightedAveragePrice$1(inputs);
            case 'williamsr': {
                const position = rollingRangePosition$1(inputs.high, inputs.low, inputs.close, parameters.period);
                return {
                    outputs: { value: position.map((item) => (finite$c(item) ? item - 100 : null)) },
                    warmUpRows: parameters.period - 1,
                };
            }
            case 'zigzag':
                return zigzag$1(inputs.value, parameters.deviation);
            default:
                throw new Error(`Unsupported OHLCV technical indicator: ${kind}`);
        }
    }

    const technicalIndicatorPresetIds = [
        'acceleration-bands',
        'awesome-oscillator',
        'absolute-price-oscillator',
        'aroon',
        'aroon-oscillator',
        'average-true-range',
        'volatility-bands',
        'commodity-channel-index',
        'chaikin-oscillator',
        'chaikin-money-flow',
        'chande-momentum-oscillator',
        'double-exponential-average',
        'disparity-index',
        'directional-movement-index',
        'detrended-price-oscillator',
        'exponential-moving-average',
        'ichimoku-cloud',
        'keltner-channels',
        'klinger-oscillator',
        'linear-regression',
        'linear-regression-angle',
        'linear-regression-intercept',
        'linear-regression-slope',
        'moving-average-convergence-divergence',
        'money-flow-index',
        'momentum',
        'normalized-average-true-range',
        'on-balance-volume',
        'price-channel',
        'pivot-points',
        'percentage-price-oscillator',
        'price-envelopes',
        'parabolic-stop-and-reverse',
        'rate-of-change',
        'relative-strength-index',
        'slow-stochastic',
        'simple-moving-average',
        'stochastic',
        'supertrend',
        'triple-exponential-average',
        'triple-exponential-oscillator',
        'volume-weighted-average-price',
        'williams-range',
        'weighted-moving-average',
        'zigzag',
    ];
    const definitions = [
        { id: 'acceleration-bands', kind: 'abands', quickApi: 'accelerationBands' },
        { id: 'awesome-oscillator', kind: 'ao', quickApi: 'awesomeOscillator' },
        { id: 'absolute-price-oscillator', kind: 'apo', quickApi: 'absolutePriceOscillator' },
        { id: 'aroon', kind: 'aroon', quickApi: 'aroon' },
        { id: 'aroon-oscillator', kind: 'aroonoscillator', quickApi: 'aroonOscillator' },
        { id: 'average-true-range', kind: 'atr', quickApi: 'averageTrueRange' },
        { id: 'volatility-bands', kind: 'bb', quickApi: 'volatilityBands' },
        { id: 'commodity-channel-index', kind: 'cci', quickApi: 'commodityChannelIndex' },
        { id: 'chaikin-oscillator', kind: 'chaikin', quickApi: 'chaikinOscillator' },
        { id: 'chaikin-money-flow', kind: 'cmf', quickApi: 'chaikinMoneyFlow' },
        { id: 'chande-momentum-oscillator', kind: 'cmo', quickApi: 'chandeMomentumOscillator' },
        {
            id: 'double-exponential-average',
            kind: 'dema',
            quickApi: 'doubleExponentialMovingAverage',
        },
        { id: 'disparity-index', kind: 'disparityindex', quickApi: 'disparityIndex' },
        { id: 'directional-movement-index', kind: 'dmi', quickApi: 'directionalMovementIndex' },
        { id: 'detrended-price-oscillator', kind: 'dpo', quickApi: 'detrendedPriceOscillator' },
        {
            id: 'exponential-moving-average',
            kind: 'ema',
            quickApi: 'exponentialMovingAverage',
        },
        { id: 'ichimoku-cloud', kind: 'ikh', quickApi: 'ichimokuCloud' },
        { id: 'keltner-channels', kind: 'keltnerchannels', quickApi: 'keltnerChannels' },
        { id: 'klinger-oscillator', kind: 'klinger', quickApi: 'klingerOscillator' },
        { id: 'linear-regression', kind: 'linearregression', quickApi: 'linearRegression' },
        {
            id: 'linear-regression-angle',
            kind: 'linearregressionangle',
            quickApi: 'linearRegressionAngle',
        },
        {
            id: 'linear-regression-intercept',
            kind: 'linearregressionintercept',
            quickApi: 'linearRegressionIntercept',
        },
        {
            id: 'linear-regression-slope',
            kind: 'linearregressionslope',
            quickApi: 'linearRegressionSlope',
        },
        {
            id: 'moving-average-convergence-divergence',
            kind: 'macd',
            quickApi: 'movingAverageConvergenceDivergence',
        },
        { id: 'money-flow-index', kind: 'mfi', quickApi: 'moneyFlowIndex' },
        { id: 'momentum', kind: 'momentum', quickApi: 'momentumIndicator' },
        { id: 'normalized-average-true-range', kind: 'natr', quickApi: 'normalizedAverageTrueRange' },
        { id: 'on-balance-volume', kind: 'obv', quickApi: 'onBalanceVolume' },
        { id: 'price-channel', kind: 'pc', quickApi: 'priceChannel' },
        { id: 'pivot-points', kind: 'pivotpoints', quickApi: 'pivotPoints' },
        { id: 'percentage-price-oscillator', kind: 'ppo', quickApi: 'percentagePriceOscillator' },
        { id: 'price-envelopes', kind: 'priceenvelopes', quickApi: 'priceEnvelopes' },
        { id: 'parabolic-stop-and-reverse', kind: 'psar', quickApi: 'parabolicStopAndReverse' },
        { id: 'rate-of-change', kind: 'roc', quickApi: 'rateOfChange' },
        { id: 'relative-strength-index', kind: 'rsi', quickApi: 'relativeStrengthIndex' },
        { id: 'slow-stochastic', kind: 'slowstochastic', quickApi: 'slowStochastic' },
        { id: 'simple-moving-average', kind: 'sma', quickApi: 'simpleMovingAverage' },
        { id: 'stochastic', kind: 'stochastic', quickApi: 'stochastic' },
        { id: 'supertrend', kind: 'supertrend', quickApi: 'supertrend' },
        {
            id: 'triple-exponential-average',
            kind: 'tema',
            quickApi: 'tripleExponentialMovingAverage',
        },
        {
            id: 'triple-exponential-oscillator',
            kind: 'trix',
            quickApi: 'tripleExponentialAverageOscillator',
        },
        {
            id: 'volume-weighted-average-price',
            kind: 'vwap',
            quickApi: 'volumeWeightedAveragePrice',
        },
        { id: 'williams-range', kind: 'williamsr', quickApi: 'williamsRange' },
        { id: 'weighted-moving-average', kind: 'wma', quickApi: 'weightedMovingAverage' },
        { id: 'zigzag', kind: 'zigzag', quickApi: 'zigzag' },
    ];
    function indicatorParameter(name, defaultValue, minimum, maximum, type = 'integer') {
        return { name, type, minimum, maximum, default: defaultValue };
    }
    const period14 = indicatorParameter('period', 14, 2, 200);
    const period20 = indicatorParameter('period', 20, 2, 200);
    const fast12 = indicatorParameter('fastPeriod', 12, 2, 200);
    const slow26 = indicatorParameter('slowPeriod', 26, 2, 200);
    const signal9 = indicatorParameter('signalPeriod', 9, 2, 200);
    const legacyComputedKinds = new Set([
        'apo',
        'dema',
        'disparityindex',
        'ema',
        'linearregression',
        'linearregressionangle',
        'linearregressionintercept',
        'linearregressionslope',
        'macd',
        'momentum',
        'ppo',
        'roc',
        'rsi',
        'sma',
        'tema',
        'trix',
        'wma',
    ]);
    const computedKinds = new Set(definitions.map(({ kind }) => kind));
    function capabilityParameters(kind) {
        switch (kind) {
            case 'abands':
                return [period20, indicatorParameter('multiplier', 4, 0, 20, 'number')];
            case 'ao':
                return [
                    indicatorParameter('fastPeriod', 5, 2, 200),
                    indicatorParameter('slowPeriod', 34, 2, 200),
                ];
            case 'apo':
            case 'ppo':
                return [fast12, slow26];
            case 'macd':
                return [fast12, slow26, signal9];
            case 'bb':
                return [period20, indicatorParameter('standardDeviations', 2, 0, 10, 'number')];
            case 'chaikin':
                return [
                    indicatorParameter('fastPeriod', 3, 2, 200),
                    indicatorParameter('slowPeriod', 10, 2, 200),
                ];
            case 'cmf':
                return [period20];
            case 'cci':
            case 'dpo':
            case 'pc':
                return [period20];
            case 'ikh':
                return [
                    indicatorParameter('conversionPeriod', 9, 2, 200),
                    indicatorParameter('basePeriod', 26, 2, 200),
                    indicatorParameter('spanPeriod', 52, 2, 400),
                    indicatorParameter('displacement', 26, 0, 200),
                ];
            case 'keltnerchannels':
                return [
                    period20,
                    indicatorParameter('atrPeriod', 10, 2, 200),
                    indicatorParameter('multiplier', 2, 0, 20, 'number'),
                ];
            case 'klinger':
                return [
                    indicatorParameter('fastPeriod', 34, 2, 200),
                    indicatorParameter('slowPeriod', 55, 2, 200),
                    indicatorParameter('signalPeriod', 13, 2, 200),
                ];
            case 'pivotpoints':
            case 'obv':
            case 'vwap':
                return [];
            case 'priceenvelopes':
                return [period20, indicatorParameter('envelopePercent', 2.5, 0, 100, 'number')];
            case 'psar':
                return [
                    indicatorParameter('acceleration', 0.02, 0.001, 1, 'number'),
                    indicatorParameter('maximumAcceleration', 0.2, 0.001, 1, 'number'),
                ];
            case 'slowstochastic':
                return [
                    period14,
                    indicatorParameter('smoothK', 3, 1, 200),
                    indicatorParameter('smoothD', 3, 1, 200),
                ];
            case 'stochastic':
                return [period14, indicatorParameter('signalPeriod', 3, 1, 200)];
            case 'supertrend':
                return [
                    indicatorParameter('atrPeriod', 10, 2, 200),
                    indicatorParameter('multiplier', 3, 0, 20, 'number'),
                ];
            case 'zigzag':
                return [indicatorParameter('deviation', 5, 0.01, 100, 'number')];
            default:
                return computedKinds.has(kind) ? [period14] : [];
        }
    }
    function capabilityInputs(kind) {
        if (['abands', 'atr', 'cci', 'dmi', 'keltnerchannels', 'natr', 'supertrend'].includes(kind)) {
            return ['high', 'low', 'close'];
        }
        if (['ao', 'aroon', 'aroonoscillator', 'ikh', 'pc', 'psar'].includes(kind)) {
            return ['high', 'low'];
        }
        if (['chaikin', 'cmf', 'klinger', 'mfi', 'vwap'].includes(kind)) {
            return ['high', 'low', 'close', 'volume'];
        }
        if (kind === 'obv')
            return ['close', 'volume'];
        if (kind === 'pivotpoints')
            return ['high', 'low', 'close'];
        if (['slowstochastic', 'stochastic', 'williamsr'].includes(kind)) {
            return ['high', 'low', 'close'];
        }
        return ['value'];
    }
    function capabilityOutputs(kind) {
        if (kind === 'macd')
            return ['value', 'signal', 'histogram'];
        if (['abands', 'bb', 'keltnerchannels', 'pc', 'priceenvelopes'].includes(kind)) {
            return ['value', 'lower', 'upper'];
        }
        if (kind === 'aroon')
            return ['value', 'up', 'down'];
        if (kind === 'dmi')
            return ['value', 'plus', 'minus'];
        if (kind === 'ikh')
            return ['value', 'lower', 'upper', 'conversion', 'base'];
        if (['klinger', 'slowstochastic', 'stochastic'].includes(kind))
            return ['value', 'signal'];
        if (kind === 'pivotpoints')
            return ['value', 'support', 'resistance'];
        if (kind === 'supertrend')
            return ['value', 'direction'];
        return ['value'];
    }
    function warmUpDescription(kind) {
        if (kind === 'dema')
            return '2 * (period - 1)';
        if (kind === 'tema' || kind === 'trix')
            return '3 * (period - 1)';
        if (['apo', 'ppo', 'ao', 'chaikin'].includes(kind))
            return 'slowPeriod - 1';
        if (kind === 'macd')
            return 'slowPeriod + signalPeriod - 2';
        if (kind === 'klinger')
            return 'slowPeriod + signalPeriod - 1';
        if (['momentum', 'roc', 'rsi', 'aroon', 'aroonoscillator', 'cmo', 'mfi'].includes(kind)) {
            return 'period';
        }
        if (kind === 'dmi')
            return '2 * period - 1';
        if (kind === 'ikh')
            return 'spanPeriod - 1 + displacement';
        if (kind === 'keltnerchannels')
            return 'max(period, atrPeriod) - 1';
        if (kind === 'slowstochastic')
            return 'period + smoothK + smoothD - 3';
        if (kind === 'stochastic')
            return 'period + signalPeriod - 2';
        if (kind === 'supertrend')
            return 'atrPeriod - 1';
        if (['obv', 'vwap'].includes(kind))
            return '0';
        if (kind === 'pivotpoints')
            return '1 row, or one complete configured session';
        if (['psar', 'zigzag'].includes(kind))
            return '1';
        return 'period - 1';
    }
    function dependencyDag(kind) {
        const inputs = capabilityInputs(kind).map((name) => ({
            id: name === 'value' ? 'source' : name,
            operation: `input:${name}`,
            inputs: [],
            parameters: [],
        }));
        const input = inputs[0] ?? {
            id: 'source',
            operation: 'input:value',
            inputs: [],
            parameters: [],
        };
        if (kind === 'dema') {
            return [
                input,
                { id: 'ema1', operation: 'ema', inputs: ['source'], parameters: ['period'] },
                { id: 'ema2', operation: 'ema', inputs: ['ema1'], parameters: ['period'] },
                { id: 'value', operation: '2*ema1-ema2', inputs: ['ema1', 'ema2'], parameters: [] },
            ];
        }
        if (kind === 'tema' || kind === 'trix') {
            return [
                input,
                { id: 'ema1', operation: 'ema', inputs: ['source'], parameters: ['period'] },
                { id: 'ema2', operation: 'ema', inputs: ['ema1'], parameters: ['period'] },
                { id: 'ema3', operation: 'ema', inputs: ['ema2'], parameters: ['period'] },
                {
                    id: 'value',
                    operation: kind === 'trix' ? 'rate-of-change' : '3*ema1-3*ema2+ema3',
                    inputs: kind === 'trix' ? ['ema3'] : ['ema1', 'ema2', 'ema3'],
                    parameters: kind === 'trix' ? ['1'] : [],
                },
            ];
        }
        if (kind === 'apo' || kind === 'ppo' || kind === 'macd') {
            return [
                input,
                { id: 'fast', operation: 'ema', inputs: ['source'], parameters: ['fastPeriod'] },
                { id: 'slow', operation: 'ema', inputs: ['source'], parameters: ['slowPeriod'] },
                {
                    id: 'value',
                    operation: kind === 'ppo' ? 'percent-difference' : 'difference',
                    inputs: ['fast', 'slow'],
                    parameters: [],
                },
                ...(kind === 'macd'
                    ? [
                        {
                            id: 'signal',
                            operation: 'ema',
                            inputs: ['value'],
                            parameters: ['signalPeriod'],
                        },
                        {
                            id: 'histogram',
                            operation: 'difference',
                            inputs: ['value', 'signal'],
                            parameters: [],
                        },
                    ]
                    : []),
            ];
        }
        const parameterNames = capabilityParameters(kind).map(({ name }) => name);
        const nodes = (...entries) => [...inputs, ...entries];
        if (kind === 'abands') {
            return nodes({
                id: 'raw-bands',
                operation: 'acceleration-envelope',
                inputs: ['high', 'low'],
                parameters: ['multiplier'],
            }, { id: 'value', operation: 'sma', inputs: ['close'], parameters: ['period'] }, { id: 'lower', operation: 'sma', inputs: ['raw-bands'], parameters: ['period'] }, { id: 'upper', operation: 'sma', inputs: ['raw-bands'], parameters: ['period'] });
        }
        if (kind === 'ao') {
            return nodes({ id: 'median', operation: 'median-price', inputs: ['high', 'low'], parameters: [] }, { id: 'fast', operation: 'sma', inputs: ['median'], parameters: ['fastPeriod'] }, { id: 'slow', operation: 'sma', inputs: ['median'], parameters: ['slowPeriod'] }, { id: 'value', operation: 'difference', inputs: ['fast', 'slow'], parameters: [] });
        }
        if (kind === 'aroon' || kind === 'aroonoscillator') {
            return nodes({
                id: 'extrema-age',
                operation: 'rolling-extrema-age',
                inputs: ['high', 'low'],
                parameters: ['period'],
            }, { id: 'up', operation: 'aroon-up', inputs: ['extrema-age'], parameters: ['period'] }, { id: 'down', operation: 'aroon-down', inputs: ['extrema-age'], parameters: ['period'] }, {
                id: 'value',
                operation: kind === 'aroon' ? 'alias-up' : 'difference',
                inputs: kind === 'aroon' ? ['up'] : ['up', 'down'],
                parameters: [],
            });
        }
        if (kind === 'atr' || kind === 'natr') {
            return nodes({
                id: 'true-range',
                operation: 'true-range',
                inputs: ['high', 'low', 'close'],
                parameters: [],
            }, { id: 'atr', operation: 'wilder-average', inputs: ['true-range'], parameters: ['period'] }, {
                id: 'value',
                operation: kind === 'natr' ? 'normalize-by-close-percent' : 'alias',
                inputs: kind === 'natr' ? ['atr', 'close'] : ['atr'],
                parameters: [],
            });
        }
        if (kind === 'bb') {
            return nodes({ id: 'value', operation: 'sma', inputs: ['source'], parameters: ['period'] }, {
                id: 'deviation',
                operation: 'population-standard-deviation',
                inputs: ['source', 'value'],
                parameters: ['period'],
            }, {
                id: 'lower',
                operation: 'subtract-scaled',
                inputs: ['value', 'deviation'],
                parameters: ['standardDeviations'],
            }, {
                id: 'upper',
                operation: 'add-scaled',
                inputs: ['value', 'deviation'],
                parameters: ['standardDeviations'],
            });
        }
        if (kind === 'cci') {
            return nodes({
                id: 'typical',
                operation: 'typical-price',
                inputs: ['high', 'low', 'close'],
                parameters: [],
            }, { id: 'average', operation: 'sma', inputs: ['typical'], parameters: ['period'] }, {
                id: 'deviation',
                operation: 'mean-absolute-deviation',
                inputs: ['typical', 'average'],
                parameters: ['period'],
            }, {
                id: 'value',
                operation: 'commodity-channel-index',
                inputs: ['typical', 'average', 'deviation'],
                parameters: [],
            });
        }
        if (kind === 'chaikin' || kind === 'cmf') {
            return nodes({
                id: 'money-flow',
                operation: 'money-flow-volume',
                inputs: ['high', 'low', 'close', 'volume'],
                parameters: [],
            }, ...(kind === 'chaikin'
                ? [
                    { id: 'adl', operation: 'cumulative-sum', inputs: ['money-flow'], parameters: [] },
                    { id: 'fast', operation: 'ema', inputs: ['adl'], parameters: ['fastPeriod'] },
                    { id: 'slow', operation: 'ema', inputs: ['adl'], parameters: ['slowPeriod'] },
                    { id: 'value', operation: 'difference', inputs: ['fast', 'slow'], parameters: [] },
                ]
                : [
                    {
                        id: 'flow-sum',
                        operation: 'rolling-sum',
                        inputs: ['money-flow'],
                        parameters: ['period'],
                    },
                    {
                        id: 'volume-sum',
                        operation: 'rolling-sum',
                        inputs: ['volume'],
                        parameters: ['period'],
                    },
                    { id: 'value', operation: 'ratio', inputs: ['flow-sum', 'volume-sum'], parameters: [] },
                ]));
        }
        if (kind === 'cmo') {
            return nodes({ id: 'changes', operation: 'signed-change', inputs: ['source'], parameters: [] }, {
                id: 'gain-loss-sums',
                operation: 'rolling-gain-loss',
                inputs: ['changes'],
                parameters: ['period'],
            }, {
                id: 'value',
                operation: 'chande-ratio-percent',
                inputs: ['gain-loss-sums'],
                parameters: [],
            });
        }
        if (kind === 'dmi') {
            return nodes({
                id: 'true-range',
                operation: 'true-range',
                inputs: ['high', 'low', 'close'],
                parameters: [],
            }, {
                id: 'movement',
                operation: 'directional-movement',
                inputs: ['high', 'low'],
                parameters: [],
            }, {
                id: 'plus-minus',
                operation: 'wilder-directional-index',
                inputs: ['true-range', 'movement'],
                parameters: ['period'],
            }, {
                id: 'plus',
                operation: 'positive-directional-index',
                inputs: ['plus-minus'],
                parameters: [],
            }, {
                id: 'minus',
                operation: 'negative-directional-index',
                inputs: ['plus-minus'],
                parameters: [],
            }, { id: 'value', operation: 'wilder-adx', inputs: ['plus', 'minus'], parameters: ['period'] });
        }
        if (kind === 'dpo') {
            return nodes({ id: 'average', operation: 'sma', inputs: ['source'], parameters: ['period'] }, {
                id: 'value',
                operation: 'detrended-shift-difference',
                inputs: ['source', 'average'],
                parameters: ['period'],
            });
        }
        if (kind === 'ikh') {
            return nodes({
                id: 'conversion',
                operation: 'midprice',
                inputs: ['high', 'low'],
                parameters: ['conversionPeriod'],
            }, { id: 'base', operation: 'midprice', inputs: ['high', 'low'], parameters: ['basePeriod'] }, { id: 'span-a', operation: 'average', inputs: ['conversion', 'base'], parameters: [] }, { id: 'span-b', operation: 'midprice', inputs: ['high', 'low'], parameters: ['spanPeriod'] }, {
                id: 'value',
                operation: 'forward-displacement',
                inputs: ['span-a'],
                parameters: ['displacement'],
            }, {
                id: 'lower',
                operation: 'minimum',
                inputs: ['span-a', 'span-b'],
                parameters: ['displacement'],
            }, {
                id: 'upper',
                operation: 'maximum',
                inputs: ['span-a', 'span-b'],
                parameters: ['displacement'],
            });
        }
        if (kind === 'keltnerchannels') {
            return nodes({ id: 'value', operation: 'ema', inputs: ['close'], parameters: ['period'] }, {
                id: 'true-range',
                operation: 'true-range',
                inputs: ['high', 'low', 'close'],
                parameters: [],
            }, { id: 'atr', operation: 'wilder-average', inputs: ['true-range'], parameters: ['atrPeriod'] }, {
                id: 'lower',
                operation: 'subtract-scaled',
                inputs: ['value', 'atr'],
                parameters: ['multiplier'],
            }, {
                id: 'upper',
                operation: 'add-scaled',
                inputs: ['value', 'atr'],
                parameters: ['multiplier'],
            });
        }
        if (kind === 'klinger') {
            return nodes({
                id: 'force',
                operation: 'klinger-volume-force',
                inputs: ['high', 'low', 'close', 'volume'],
                parameters: [],
            }, { id: 'fast', operation: 'ema', inputs: ['force'], parameters: ['fastPeriod'] }, { id: 'slow', operation: 'ema', inputs: ['force'], parameters: ['slowPeriod'] }, { id: 'value', operation: 'difference', inputs: ['fast', 'slow'], parameters: [] }, { id: 'signal', operation: 'ema', inputs: ['value'], parameters: ['signalPeriod'] });
        }
        if (kind === 'mfi') {
            return nodes({
                id: 'typical',
                operation: 'typical-price',
                inputs: ['high', 'low', 'close'],
                parameters: [],
            }, { id: 'raw-flow', operation: 'multiply', inputs: ['typical', 'volume'], parameters: [] }, {
                id: 'signed-flow',
                operation: 'price-direction-split',
                inputs: ['typical', 'raw-flow'],
                parameters: [],
            }, {
                id: 'value',
                operation: 'rolling-money-flow-index',
                inputs: ['signed-flow'],
                parameters: ['period'],
            });
        }
        if (kind === 'obv') {
            return nodes({
                id: 'value',
                operation: 'on-balance-volume',
                inputs: ['close', 'volume'],
                parameters: [],
            });
        }
        if (kind === 'pc') {
            return nodes({ id: 'upper', operation: 'rolling-maximum', inputs: ['high'], parameters: ['period'] }, { id: 'lower', operation: 'rolling-minimum', inputs: ['low'], parameters: ['period'] }, { id: 'value', operation: 'midpoint', inputs: ['upper', 'lower'], parameters: [] });
        }
        if (kind === 'pivotpoints') {
            return nodes({
                id: 'previous-session',
                operation: 'previous-session-hlc',
                inputs: ['high', 'low', 'close'],
                parameters: [],
            }, { id: 'value', operation: 'classic-pivot', inputs: ['previous-session'], parameters: [] }, {
                id: 'support',
                operation: 'classic-support-1',
                inputs: ['value', 'previous-session'],
                parameters: [],
            }, {
                id: 'resistance',
                operation: 'classic-resistance-1',
                inputs: ['value', 'previous-session'],
                parameters: [],
            });
        }
        if (kind === 'priceenvelopes') {
            return nodes({ id: 'value', operation: 'sma', inputs: ['source'], parameters: ['period'] }, {
                id: 'lower',
                operation: 'percent-envelope-lower',
                inputs: ['value'],
                parameters: ['envelopePercent'],
            }, {
                id: 'upper',
                operation: 'percent-envelope-upper',
                inputs: ['value'],
                parameters: ['envelopePercent'],
            });
        }
        if (kind === 'psar') {
            return nodes({
                id: 'value',
                operation: 'parabolic-sar-state-machine',
                inputs: ['high', 'low'],
                parameters: ['acceleration', 'maximumAcceleration'],
            });
        }
        if (kind === 'stochastic' || kind === 'slowstochastic') {
            return nodes({
                id: 'fast-k',
                operation: 'rolling-range-position',
                inputs: ['high', 'low', 'close'],
                parameters: ['period'],
            }, ...(kind === 'slowstochastic'
                ? [
                    { id: 'value', operation: 'sma', inputs: ['fast-k'], parameters: ['smoothK'] },
                    { id: 'signal', operation: 'sma', inputs: ['value'], parameters: ['smoothD'] },
                ]
                : [
                    { id: 'value', operation: 'alias', inputs: ['fast-k'], parameters: [] },
                    { id: 'signal', operation: 'sma', inputs: ['value'], parameters: ['signalPeriod'] },
                ]));
        }
        if (kind === 'supertrend') {
            return nodes({
                id: 'true-range',
                operation: 'true-range',
                inputs: ['high', 'low', 'close'],
                parameters: [],
            }, { id: 'atr', operation: 'wilder-average', inputs: ['true-range'], parameters: ['atrPeriod'] }, {
                id: 'bands',
                operation: 'recursive-final-bands',
                inputs: ['high', 'low', 'close', 'atr'],
                parameters: ['multiplier'],
            }, {
                id: 'state',
                operation: 'supertrend-state-machine',
                inputs: ['close', 'bands'],
                parameters: [],
            }, { id: 'value', operation: 'supertrend-line', inputs: ['state'], parameters: [] }, { id: 'direction', operation: 'supertrend-direction', inputs: ['state'], parameters: [] });
        }
        if (kind === 'vwap') {
            return nodes({
                id: 'typical',
                operation: 'typical-price',
                inputs: ['high', 'low', 'close'],
                parameters: [],
            }, {
                id: 'value',
                operation: 'cumulative-volume-weighted-average',
                inputs: ['typical', 'volume'],
                parameters: [],
            });
        }
        if (kind === 'williamsr') {
            return nodes({
                id: 'range',
                operation: 'rolling-high-low',
                inputs: ['high', 'low'],
                parameters: ['period'],
            }, {
                id: 'value',
                operation: 'williams-percent-range',
                inputs: ['range', 'close'],
                parameters: [],
            });
        }
        if (kind === 'zigzag') {
            return nodes({
                id: 'value',
                operation: 'confirmed-threshold-pivots',
                inputs: ['source'],
                parameters: ['deviation'],
            });
        }
        return [
            ...inputs,
            {
                id: 'value',
                operation: kind,
                inputs: ['source'],
                parameters: parameterNames,
            },
        ];
    }
    function computedProvenance(kind) {
        const descriptions = {
            abands: 'Acceleration bands smooth high/low envelopes expanded by relative intrabar range; the middle line is the close SMA.',
            ao: 'Awesome oscillator = fast SMA(median price) - slow SMA(median price).',
            sma: 'Arithmetic rolling mean over period values.',
            ema: 'EMA seeded by the first complete period mean, then alpha = 2 / (period + 1).',
            wma: 'Linearly weighted rolling mean with weights 1 through period.',
            dema: 'DEMA = 2 * EMA(source) - EMA(EMA(source)).',
            tema: 'TEMA = 3 * EMA1 - 3 * EMA2 + EMA3.',
            momentum: 'Momentum = value - value[period].',
            roc: 'Rate of change = 100 * (value - value[period]) / value[period].',
            rsi: 'Wilder RSI with an arithmetic seed and Wilder-smoothed gains and losses.',
            apo: 'Absolute price oscillator = fast EMA - slow EMA.',
            ppo: 'Percentage price oscillator = 100 * (fast EMA - slow EMA) / slow EMA.',
            macd: 'MACD = fast EMA - slow EMA; signal is an EMA of MACD; histogram is their difference.',
            disparityindex: 'Disparity index = 100 * (value / SMA(period) - 1).',
            linearregression: 'Rolling least-squares value at the final index in each period window.',
            linearregressionintercept: 'Rolling least-squares intercept over index 0 through period - 1.',
            linearregressionslope: 'Rolling least-squares slope over index 0 through period - 1.',
            linearregressionangle: 'Arctangent of the rolling least-squares slope, in degrees.',
            trix: 'One-period percentage rate of change of a triple EMA.',
            aroon: 'Aroon up/down measure the age of the most recent high and low over period + 1 observations.',
            aroonoscillator: 'Aroon oscillator = Aroon up - Aroon down.',
            atr: 'Average true range uses Wilder smoothing of max(high-low, abs(high-prevClose), abs(low-prevClose)).',
            bb: 'Volatility bands are a rolling mean plus/minus a configurable population standard deviation multiple.',
            cci: 'CCI = (typical price - its SMA) / (0.015 * mean absolute deviation).',
            chaikin: 'Chaikin oscillator = fast EMA(ADL) - slow EMA(ADL), using money-flow volume.',
            cmf: 'Chaikin money flow = rolling money-flow volume / rolling volume.',
            cmo: 'CMO = 100 * (rolling gains - rolling losses) / (rolling gains + rolling losses).',
            dmi: 'Directional movement exposes +DI, -DI, and Wilder-smoothed ADX derived from true range.',
            dpo: 'DPO subtracts the current period SMA from the source shifted back floor(period / 2) + 1 rows.',
            ikh: 'Ichimoku derives conversion/base midprices and forward-displaced span A/B cloud bounds.',
            keltnerchannels: 'Keltner channels use an EMA center and Wilder ATR multiplied above and below it.',
            klinger: 'Klinger oscillator is the fast/slow EMA difference of trend-sensitive volume force plus a signal EMA.',
            mfi: 'Money flow index applies an RSI-style ratio to signed typical-price volume flows.',
            natr: 'Normalized ATR = 100 * Wilder ATR / close.',
            obv: 'On-balance volume cumulatively adds or subtracts volume according to close direction.',
            pc: 'Price channel uses rolling high/low extrema and their midpoint.',
            pivotpoints: 'Classic pivot, support 1, and resistance 1 derive from the previous row or previous configured session HLC.',
            priceenvelopes: 'Price envelopes are a rolling mean shifted by a configurable percentage.',
            psar: 'Parabolic SAR is a bounded acceleration-factor trend state machine over high and low.',
            slowstochastic: 'Slow stochastic smooths range-position %K and then smooths its %D signal.',
            stochastic: 'Stochastic uses close position in the rolling high-low range and a signal SMA.',
            supertrend: 'Supertrend uses recursive ATR bands and close crossings to maintain trend direction.',
            vwap: 'VWAP is cumulative typical-price times volume divided by cumulative volume within calculation state.',
            williamsr: 'Williams %R = close position in the rolling high-low range minus 100.',
            zigzag: 'Zigzag retains confirmed extrema after a configurable percentage reversal threshold.',
        };
        return descriptions[kind] ?? 'Deterministic Graflume built-in calculation.';
    }
    const technicalIndicatorCapabilities = definitions.map((definition) => {
        return {
            ...definition,
            support: 'computed',
            requiredInputs: capabilityInputs(definition.kind),
            outputs: capabilityOutputs(definition.kind),
            parameters: capabilityParameters(definition.kind),
            dependencyDag: dependencyDag(definition.kind),
            warmUp: {
                policy: 'null',
                rows: warmUpDescription(definition.kind),
            },
            provenance: computedProvenance(definition.kind),
        };
    });
    const capabilityByIdentifier = new Map();
    technicalIndicatorCapabilities.forEach((capability) => {
        for (const identifier of [capability.id, capability.kind, capability.quickApi]) {
            capabilityByIdentifier.set(identifier.toLowerCase(), capability);
        }
    });
    const indicatorParameterNames = new Set(technicalIndicatorCapabilities.flatMap(({ parameters }) => parameters.map(({ name }) => name)));
    function resolveTechnicalIndicatorCapability(identifier) {
        return capabilityByIdentifier.get(identifier.trim().toLowerCase()) ?? null;
    }
    function movingAverage(values, period) {
        const output = Array.from({ length: values.length }).fill(null);
        let sum = 0;
        let valid = 0;
        for (let index = 0; index < values.length; index += 1) {
            const value = values[index];
            if (value !== null && value !== undefined) {
                sum += value;
                valid += 1;
            }
            const removed = values[index - period];
            if (removed !== null && removed !== undefined) {
                sum -= removed;
                valid -= 1;
            }
            if (index + 1 >= period && valid === period)
                output[index] = sum / period;
        }
        return output;
    }
    function exponentialAverage$1(values, period) {
        const output = Array.from({ length: values.length }).fill(null);
        const alpha = 2 / (period + 1);
        let seed = [];
        let previous = null;
        values.forEach((value, index) => {
            if (value === null) {
                seed = [];
                previous = null;
                return;
            }
            if (previous === null) {
                seed.push(value);
                if (seed.length < period)
                    return;
                if (seed.length > period)
                    seed.shift();
                previous = seed.reduce((sum, item) => sum + item, 0) / period;
            }
            else {
                previous = value * alpha + previous * (1 - alpha);
            }
            output[index] = previous;
        });
        return output;
    }
    function weightedAverage$1(values, period) {
        const denominator = (period * (period + 1)) / 2;
        return values.map((_, index) => {
            if (index + 1 < period)
                return null;
            const window = values.slice(index + 1 - period, index + 1);
            if (window.some((value) => value === null))
                return null;
            return (window.reduce((sum, value, offset) => sum + value * (offset + 1), 0) / denominator);
        });
    }
    function difference(left, right, percent = false) {
        return left.map((value, index) => {
            const baseline = right[index];
            if (value === null || baseline === null || baseline === undefined)
                return null;
            if (percent && baseline === 0)
                return null;
            return percent ? ((value - baseline) / baseline) * 100 : value - baseline;
        });
    }
    function lagDifference$1(values, period, percent) {
        return values.map((value, index) => {
            const previous = values[index - period];
            if (value === null || previous === null || previous === undefined)
                return null;
            if (percent && previous === 0)
                return null;
            return percent ? ((value - previous) / previous) * 100 : value - previous;
        });
    }
    function relativeStrength$1(values, period) {
        const output = Array.from({ length: values.length }).fill(null);
        let gains = [];
        let losses = [];
        let averageGain = null;
        let averageLoss = null;
        for (let index = 1; index < values.length; index += 1) {
            const value = values[index];
            const previous = values[index - 1];
            if (value === null || previous === null || value === undefined || previous === undefined) {
                gains = [];
                losses = [];
                averageGain = null;
                averageLoss = null;
                continue;
            }
            const change = value - previous;
            const gain = Math.max(0, change);
            const loss = Math.max(0, -change);
            if (averageGain === null || averageLoss === null) {
                gains.push(gain);
                losses.push(loss);
                if (gains.length < period)
                    continue;
                averageGain = gains.reduce((sum, item) => sum + item, 0) / period;
                averageLoss = losses.reduce((sum, item) => sum + item, 0) / period;
            }
            else {
                averageGain = (averageGain * (period - 1) + gain) / period;
                averageLoss = (averageLoss * (period - 1) + loss) / period;
            }
            output[index] = averageLoss === 0 ? 100 : 100 - 100 / (1 + averageGain / averageLoss);
        }
        return output;
    }
    function rollingRegression(values, period) {
        const value = Array.from({ length: values.length }).fill(null);
        const slope = [...value];
        const intercept = [...value];
        const angle = [...value];
        const sumX = (period * (period - 1)) / 2;
        const sumXX = (period * (period - 1) * (2 * period - 1)) / 6;
        const denominator = period * sumXX - sumX * sumX;
        for (let index = period - 1; index < values.length; index += 1) {
            const window = values.slice(index + 1 - period, index + 1);
            if (window.some((item) => item === null))
                continue;
            const sumY = window.reduce((sum, item) => sum + item, 0);
            const sumXY = window.reduce((sum, item, x) => sum + x * item, 0);
            const resolvedSlope = denominator === 0 ? 0 : (period * sumXY - sumX * sumY) / denominator;
            const resolvedIntercept = (sumY - resolvedSlope * sumX) / period;
            slope[index] = resolvedSlope;
            intercept[index] = resolvedIntercept;
            angle[index] = (Math.atan(resolvedSlope) * 180) / Math.PI;
            value[index] = resolvedIntercept + resolvedSlope * (period - 1);
        }
        return { value, slope, intercept, angle };
    }
    function warmUpRows(kind, period, slow, signal) {
        if (kind === 'dema')
            return 2 * (period - 1);
        if (kind === 'tema' || kind === 'trix')
            return 3 * (period - 1);
        if (kind === 'apo' || kind === 'ppo')
            return slow - 1;
        if (kind === 'macd')
            return slow + signal - 2;
        if (kind === 'momentum' || kind === 'roc' || kind === 'rsi')
            return period;
        return period - 1;
    }
    function calculateLegacyTechnicalIndicator(capability, values, parameters) {
        const period = parameters.period ?? 14;
        const fastPeriod = parameters.fastPeriod ?? 12;
        const slowPeriod = parameters.slowPeriod ?? 26;
        const signalPeriod = parameters.signalPeriod ?? 9;
        let outputs;
        switch (capability.kind) {
            case 'sma':
                outputs = { value: movingAverage(values, period) };
                break;
            case 'ema':
                outputs = { value: exponentialAverage$1(values, period) };
                break;
            case 'wma':
                outputs = { value: weightedAverage$1(values, period) };
                break;
            case 'dema': {
                const first = exponentialAverage$1(values, period);
                const second = exponentialAverage$1(first, period);
                outputs = {
                    value: first.map((item, index) => item === null || second[index] === null ? null : 2 * item - second[index]),
                };
                break;
            }
            case 'tema': {
                const first = exponentialAverage$1(values, period);
                const second = exponentialAverage$1(first, period);
                const third = exponentialAverage$1(second, period);
                outputs = {
                    value: first.map((item, index) => item === null || second[index] === null || third[index] === null
                        ? null
                        : 3 * item - 3 * second[index] + third[index]),
                };
                break;
            }
            case 'momentum':
                outputs = { value: lagDifference$1(values, period, false) };
                break;
            case 'roc':
                outputs = { value: lagDifference$1(values, period, true) };
                break;
            case 'rsi':
                outputs = { value: relativeStrength$1(values, period) };
                break;
            case 'apo':
            case 'ppo':
            case 'macd': {
                const fast = exponentialAverage$1(values, fastPeriod);
                const slow = exponentialAverage$1(values, slowPeriod);
                const primary = difference(fast, slow, capability.kind === 'ppo');
                if (capability.kind !== 'macd') {
                    outputs = { value: primary };
                    break;
                }
                const signal = exponentialAverage$1(primary, signalPeriod);
                outputs = { value: primary, signal, histogram: difference(primary, signal) };
                break;
            }
            case 'disparityindex': {
                const average = movingAverage(values, period);
                outputs = {
                    value: values.map((item, index) => {
                        const baseline = average[index];
                        return item === null || baseline === null || baseline === undefined || baseline === 0
                            ? null
                            : (item / baseline - 1) * 100;
                    }),
                };
                break;
            }
            case 'linearregression':
            case 'linearregressionslope':
            case 'linearregressionintercept':
            case 'linearregressionangle': {
                const regression = rollingRegression(values, period);
                const key = capability.kind === 'linearregressionslope'
                    ? 'slope'
                    : capability.kind === 'linearregressionintercept'
                        ? 'intercept'
                        : capability.kind === 'linearregressionangle'
                            ? 'angle'
                            : 'value';
                outputs = { value: regression[key] };
                break;
            }
            case 'trix': {
                const first = exponentialAverage$1(values, period);
                const second = exponentialAverage$1(first, period);
                const third = exponentialAverage$1(second, period);
                outputs = { value: lagDifference$1(third, 1, true) };
                break;
            }
            default:
                throw new GraflumeError('INVALID_SPEC', `${capability.id} has no built-in calculation.`, {
                    path: '$.mark.options.calculate',
                });
        }
        return { outputs, warmUpRows: warmUpRows(capability.kind, period, slowPeriod, signalPeriod) };
    }
    const overlayIndicatorKinds = new Set([
        'abands',
        'bb',
        'dema',
        'ema',
        'ikh',
        'keltnerchannels',
        'linearregression',
        'pc',
        'pivotpoints',
        'priceenvelopes',
        'psar',
        'sma',
        'supertrend',
        'tema',
        'vwap',
        'wma',
        'zigzag',
    ]);
    function resolveTechnicalIndicatorPresentation(identifier) {
        const capability = resolveTechnicalIndicatorCapability(identifier);
        if (capability === null)
            return null;
        const placement = overlayIndicatorKinds.has(capability.kind)
            ? 'overlay'
            : 'panel';
        return {
            placement,
            panelId: placement === 'overlay' ? 'price' : `indicator:${capability.id}`,
            synchronizedCrosshair: {
                axis: 'x',
                sharedDomain: true,
                fields: capability.outputs,
            },
        };
    }
    function jsonRecord(value) {
        return value !== null && typeof value === 'object' && !Array.isArray(value)
            ? value
            : null;
    }
    function resolveIndicatorParameters(capability, options) {
        const parameters = {};
        const accepted = new Set(capability.parameters.map(({ name }) => name));
        for (const name of indicatorParameterNames) {
            if (options[name] !== undefined && !accepted.has(name)) {
                throw new GraflumeError('INVALID_SPEC', `${name} is not a parameter of ${capability.id}.`, {
                    path: `$.mark.options.${name}`,
                });
            }
        }
        capability.parameters.forEach((parameterCapability) => {
            const supplied = options[parameterCapability.name];
            const value = supplied === undefined ? parameterCapability.default : supplied;
            if (typeof value !== 'number' ||
                !Number.isFinite(value) ||
                (parameterCapability.type === 'integer' && !Number.isInteger(value)) ||
                value < parameterCapability.minimum ||
                value > parameterCapability.maximum) {
                throw new GraflumeError('INVALID_SPEC', `${parameterCapability.name} for ${capability.id} must be a ${parameterCapability.type} between ${parameterCapability.minimum} and ${parameterCapability.maximum}.`, { path: `$.mark.options.${parameterCapability.name}` });
            }
            parameters[parameterCapability.name] = value;
        });
        const fast = parameters.fastPeriod;
        const slow = parameters.slowPeriod;
        if (fast !== undefined && slow !== undefined && fast >= slow) {
            throw new GraflumeError('INVALID_SPEC', 'fastPeriod must be smaller than slowPeriod.', {
                path: '$.mark.options.fastPeriod',
            });
        }
        const conversion = parameters.conversionPeriod;
        const base = parameters.basePeriod;
        const span = parameters.spanPeriod;
        if (conversion !== undefined &&
            base !== undefined &&
            span !== undefined &&
            (conversion > base || base > span)) {
            throw new GraflumeError('INVALID_SPEC', 'Ichimoku periods must satisfy conversionPeriod <= basePeriod <= spanPeriod.', { path: '$.mark.options.conversionPeriod' });
        }
        if (parameters.acceleration !== undefined &&
            parameters.maximumAcceleration !== undefined &&
            parameters.acceleration > parameters.maximumAcceleration) {
            throw new GraflumeError('INVALID_SPEC', 'acceleration must be no greater than maximumAcceleration.', { path: '$.mark.options.acceleration' });
        }
        return parameters;
    }
    function resolveSessionSpec(options) {
        const supplied = options.session;
        if (supplied === undefined)
            return { mode: 'none', reset: 'carry' };
        const session = jsonRecord(supplied);
        if (session === null) {
            throw new GraflumeError('INVALID_SPEC', 'session must be an object.', {
                path: '$.mark.options.session',
            });
        }
        const allowed = new Set(['mode', 'field', 'timeField', 'gapMs', 'reset']);
        const unknown = Object.keys(session).find((key) => !allowed.has(key));
        if (unknown !== undefined) {
            throw new GraflumeError('INVALID_SPEC', `Unknown indicator session property "${unknown}".`, {
                path: `$.mark.options.session.${unknown}`,
            });
        }
        const mode = session.mode ?? 'none';
        if (!['none', 'field', 'utc-day', 'gap'].includes(String(mode))) {
            throw new GraflumeError('INVALID_SPEC', 'session.mode must be none, field, utc-day, or gap.', {
                path: '$.mark.options.session.mode',
            });
        }
        const reset = session.reset ?? (mode === 'none' ? 'carry' : 'hard');
        if (reset !== 'hard' && reset !== 'carry') {
            throw new GraflumeError('INVALID_SPEC', 'session.reset must be hard or carry.', {
                path: '$.mark.options.session.reset',
            });
        }
        const field = session.field;
        const timeField = session.timeField;
        const gapMs = session.gapMs;
        if (field !== undefined && typeof field !== 'string') {
            throw new GraflumeError('INVALID_SPEC', 'session.field must be a field name.', {
                path: '$.mark.options.session.field',
            });
        }
        if (field === '__proto__' || field === 'prototype' || field === 'constructor') {
            throw new GraflumeError('INVALID_SPEC', 'session.field is unsafe.', {
                path: '$.mark.options.session.field',
            });
        }
        if (timeField !== undefined && typeof timeField !== 'string') {
            throw new GraflumeError('INVALID_SPEC', 'session.timeField must be a field name.', {
                path: '$.mark.options.session.timeField',
            });
        }
        if (timeField === '__proto__' || timeField === 'prototype' || timeField === 'constructor') {
            throw new GraflumeError('INVALID_SPEC', 'session.timeField is unsafe.', {
                path: '$.mark.options.session.timeField',
            });
        }
        if (mode === 'gap' && (typeof gapMs !== 'number' || !Number.isFinite(gapMs) || gapMs <= 0)) {
            throw new GraflumeError('INVALID_SPEC', 'session.gapMs must be a positive number in gap mode.', {
                path: '$.mark.options.session.gapMs',
            });
        }
        return {
            mode: mode,
            reset,
            ...(typeof field === 'string' ? { field } : {}),
            ...(typeof timeField === 'string' ? { timeField } : {}),
            ...(typeof gapMs === 'number' ? { gapMs } : {}),
        };
    }
    function timestamp$1(value, index) {
        const resolved = value instanceof Date
            ? value.getTime()
            : typeof value === 'number'
                ? value
                : Date.parse(value ?? '');
        if (!Number.isFinite(resolved)) {
            throw new GraflumeError('INVALID_SPEC', `Invalid session timestamp at row ${index}.`, {
                path: '$.mark.options.session.timeField',
            });
        }
        return resolved;
    }
    function sameSessionValue$1(left, right) {
        if (Object.is(left, right))
            return true;
        return JSON.stringify(left) === JSON.stringify(right);
    }
    function resolveSessionState(spec, input, length) {
        if (spec.mode === 'none' || length === 0) {
            return { mode: spec.mode, reset: spec.reset, boundaries: [] };
        }
        const boundaries = [0];
        if (spec.mode === 'field') {
            if (input.session === undefined || input.session.length !== length) {
                throw new GraflumeError('INVALID_SPEC', 'field session mode requires one session value per input row.', { path: '$.mark.options.session.field' });
            }
            for (let index = 1; index < length; index += 1) {
                if (!sameSessionValue$1(input.session[index - 1], input.session[index]))
                    boundaries.push(index);
            }
        }
        else {
            if (input.time === undefined || input.time.length !== length) {
                throw new GraflumeError('INVALID_SPEC', `${spec.mode} session mode requires one timestamp per input row.`, { path: '$.mark.options.session.timeField' });
            }
            let previous = timestamp$1(input.time[0], 0);
            for (let index = 1; index < length; index += 1) {
                const current = timestamp$1(input.time[index], index);
                if (current < previous) {
                    throw new GraflumeError('INVALID_SPEC', 'Session timestamps must be ordered from earliest to latest.', { path: '$.mark.options.session.timeField' });
                }
                const changed = spec.mode === 'utc-day'
                    ? Math.floor(current / 86_400_000) !== Math.floor(previous / 86_400_000)
                    : current - previous > spec.gapMs;
                if (changed)
                    boundaries.push(index);
                previous = current;
            }
        }
        return { mode: spec.mode, reset: spec.reset, boundaries };
    }
    function normalizeIndicatorInputs(capability, supplied) {
        const original = Array.isArray(supplied)
            ? { value: supplied }
            : supplied;
        const clean = (series) => series?.map((value) => (typeof value === 'number' && Number.isFinite(value) ? value : null));
        const cleanedValue = clean(original.value);
        const cleanedOpen = clean(original.open);
        const cleanedHigh = clean(original.high);
        const cleanedLow = clean(original.low);
        const cleanedClose = clean(original.close);
        const cleanedVolume = clean(original.volume);
        const raw = {
            ...(cleanedValue === undefined ? {} : { value: cleanedValue }),
            ...(cleanedOpen === undefined ? {} : { open: cleanedOpen }),
            ...(cleanedHigh === undefined ? {} : { high: cleanedHigh }),
            ...(cleanedLow === undefined ? {} : { low: cleanedLow }),
            ...(cleanedClose === undefined ? {} : { close: cleanedClose }),
            ...(cleanedVolume === undefined ? {} : { volume: cleanedVolume }),
            ...(original.session === undefined ? {} : { session: original.session }),
            ...(original.time === undefined ? {} : { time: original.time }),
        };
        const allSeries = [raw.value, raw.open, raw.high, raw.low, raw.close, raw.volume].filter((series) => series !== undefined);
        const length = allSeries[0]?.length ?? raw.session?.length ?? raw.time?.length ?? 0;
        allSeries.forEach((series) => {
            if (series.length !== length) {
                throw new GraflumeError('INVALID_SPEC', 'Technical indicator input series must have equal lengths.', {
                    path: '$.mark.fields',
                });
            }
        });
        capability.requiredInputs.forEach((role) => {
            if (raw[role] === undefined) {
                throw new GraflumeError('INVALID_SPEC', `${capability.id} requires the ${role} input series.`, {
                    path: `$.mark.fields.${role}`,
                });
            }
        });
        const blank = () => Array.from({ length }).fill(null);
        const value = raw.value ?? raw.close ?? blank();
        return {
            raw,
            length,
            inputs: {
                value,
                open: raw.open ?? blank(),
                high: raw.high ?? blank(),
                low: raw.low ?? blank(),
                close: raw.close ?? raw.value ?? blank(),
                volume: raw.volume ?? blank(),
            },
        };
    }
    function sliceInputs(inputs, start, end) {
        return {
            value: inputs.value.slice(start, end),
            open: inputs.open.slice(start, end),
            high: inputs.high.slice(start, end),
            low: inputs.low.slice(start, end),
            close: inputs.close.slice(start, end),
            volume: inputs.volume.slice(start, end),
        };
    }
    function calculateIndicatorCore(capability, inputs, parameters) {
        return legacyComputedKinds.has(capability.kind)
            ? calculateLegacyTechnicalIndicator(capability, inputs.value, parameters)
            : calculateOhlcvTechnicalIndicator(capability.kind, inputs, parameters);
    }
    function calculateSessionPivotPoints(inputs, boundaries) {
        const value = Array.from({ length: inputs.close.length }).fill(null);
        const support = [...value];
        const resistance = [...value];
        for (let sessionIndex = 1; sessionIndex < boundaries.length; sessionIndex += 1) {
            const previousStart = boundaries[sessionIndex - 1];
            const currentStart = boundaries[sessionIndex];
            const currentEnd = boundaries[sessionIndex + 1] ?? inputs.close.length;
            const highs = inputs.high
                .slice(previousStart, currentStart)
                .filter((item) => item !== null);
            const lows = inputs.low
                .slice(previousStart, currentStart)
                .filter((item) => item !== null);
            const previousClose = inputs.close[currentStart - 1];
            if (highs.length === 0 ||
                lows.length === 0 ||
                previousClose === null ||
                previousClose === undefined)
                continue;
            const high = Math.max(...highs);
            const low = Math.min(...lows);
            const pivot = (high + low + previousClose) / 3;
            for (let index = currentStart; index < currentEnd; index += 1) {
                value[index] = pivot;
                support[index] = 2 * pivot - high;
                resistance[index] = 2 * pivot - low;
            }
        }
        return { value, support, resistance };
    }
    function calculateTechnicalIndicator(identifier, supplied, options = {}) {
        const capability = resolveTechnicalIndicatorCapability(identifier);
        if (capability === null) {
            throw new GraflumeError('INVALID_SPEC', `Unknown technical indicator "${identifier}".`, {
                path: '$.mark.options.kind',
            });
        }
        const parameters = resolveIndicatorParameters(capability, options);
        const { inputs, raw, length } = normalizeIndicatorInputs(capability, supplied);
        const sessionSpec = resolveSessionSpec(options);
        const session = resolveSessionState(sessionSpec, raw, length);
        let calculated = calculateIndicatorCore(capability, inputs, parameters);
        if (capability.kind === 'pivotpoints' && session.boundaries.length > 0) {
            calculated = {
                outputs: calculateSessionPivotPoints(inputs, session.boundaries),
                warmUpRows: session.boundaries[1] ?? length,
            };
        }
        else if (session.reset === 'hard' && session.boundaries.length > 0) {
            const output = Object.fromEntries(capability.outputs.map((role) => [role, Array.from({ length }).fill(null)]));
            session.boundaries.forEach((start, boundaryIndex) => {
                const end = session.boundaries[boundaryIndex + 1] ?? length;
                const segment = calculateIndicatorCore(capability, sliceInputs(inputs, start, end), parameters);
                Object.entries(segment.outputs).forEach(([role, values]) => {
                    const target = output[role] ?? (output[role] = Array.from({ length }).fill(null));
                    values.forEach((value, index) => {
                        target[start + index] = value;
                    });
                });
            });
            calculated = { outputs: output, warmUpRows: calculated.warmUpRows };
        }
        return {
            capability,
            outputs: calculated.outputs,
            warmUpRows: calculated.warmUpRows,
            parameters,
            provenance: capability.provenance,
            session,
            presentation: resolveTechnicalIndicatorPresentation(identifier),
        };
    }
    function indicatorSourceField(layer) {
        let hash = 0;
        for (const character of `${layer.id}\u0000${layer.y.field}`) {
            hash = (Math.imul(hash, 31) + (character.codePointAt(0) ?? 0)) >>> 0;
        }
        return `__graflume_indicator_source_${hash.toString(36)}`;
    }
    function numericFieldSeries(data, field) {
        return data.map((row) => {
            const value = row[field];
            return typeof value === 'number' && Number.isFinite(value) ? value : null;
        });
    }
    function indicatorInputFields(layer, capability) {
        const fields = {
            value: layer.y.field,
            close: layer.mark.fields.close ?? layer.y.field,
        };
        for (const role of ['open', 'high', 'low', 'volume']) {
            const field = layer.mark.fields[role];
            if (field !== undefined)
                fields[role] = field;
        }
        capability.requiredInputs.forEach((role) => {
            if (fields[role] === undefined) {
                throw new GraflumeError('INVALID_SPEC', `${capability.id} requires an encoding or mark.fields mapping for ${role}.`, { path: `$.mark.fields.${role}` });
            }
        });
        return fields;
    }
    function sessionFieldValues(data, field) {
        return data.map((row) => {
            const value = row[field];
            if (value instanceof Date)
                return value.toISOString();
            if (value === undefined)
                return null;
            return value;
        });
    }
    function sessionTimeValues(data, field) {
        return data.map((row) => {
            const value = row[field];
            return typeof value === 'number' || typeof value === 'string' || value instanceof Date
                ? value
                : null;
        });
    }
    /** Materialize a supported calculation before scale-domain resolution and rendering. */
    function prepareTechnicalIndicator(layer, transformed) {
        if (layer.mark.type !== 'indicator' || layer.mark.options.calculate !== true) {
            return { layer, result: transformed, calculation: null };
        }
        const kind = typeof layer.mark.options.kind === 'string' && layer.mark.options.kind.trim() !== ''
            ? layer.mark.options.kind
            : 'sma';
        const capability = resolveTechnicalIndicatorCapability(kind);
        if (capability === null) {
            throw new GraflumeError('INVALID_SPEC', `Unknown technical indicator "${kind}".`, {
                path: '$.mark.options.kind',
            });
        }
        const sourceField = indicatorSourceField(layer);
        const inputFields = indicatorInputFields(layer, capability);
        const inputs = {};
        Object.entries(inputFields).forEach(([role, field]) => {
            inputs[role] = numericFieldSeries(transformed.data, field);
        });
        const sessionSpec = resolveSessionSpec(layer.mark.options);
        if (sessionSpec.mode === 'field') {
            if (sessionSpec.field === undefined) {
                throw new GraflumeError('INVALID_SPEC', 'field session mode requires session.field.', {
                    path: '$.mark.options.session.field',
                });
            }
            inputs.session = sessionFieldValues(transformed.data, sessionSpec.field);
        }
        if (sessionSpec.mode === 'utc-day' || sessionSpec.mode === 'gap') {
            const timeField = sessionSpec.timeField ?? layer.x.field;
            inputs.time = sessionTimeValues(transformed.data, timeField);
        }
        const calculation = calculateTechnicalIndicator(kind, inputs, layer.mark.options);
        const data = transformed.data.map((row, index) => {
            const output = {
                ...row,
                [sourceField]: row[layer.y.field],
                [layer.y.field]: calculation.outputs.value?.[index] ?? null,
            };
            Object.entries(calculation.outputs).forEach(([role, series]) => {
                const field = role === 'value' ? layer.y.field : (layer.mark.fields[role] ?? role);
                output[field] = series[index] ?? null;
            });
            const fields = layer.mark.options.fields;
            if (Array.isArray(fields)) {
                fields.forEach((field) => {
                    if (typeof field !== 'string' || calculation.outputs[field] === undefined)
                        return;
                    output[field] = calculation.outputs[field]?.[index] ?? null;
                });
            }
            for (const [role, field] of Object.entries(layer.mark.fields)) {
                const series = calculation.outputs[role === 'middle' ? 'value' : role];
                if (series !== undefined)
                    output[field] = series[index] ?? null;
            }
            return output;
        });
        const step = {
            index: transformed.lineage.transforms.length,
            type: 'calculate',
            inputRows: transformed.data.length,
            outputRows: data.length,
            parameters: {
                operation: 'technical-indicator',
                id: calculation.capability.id,
                kind: calculation.capability.kind,
                support: calculation.capability.support,
                warmUpPolicy: 'null',
                warmUpRows: calculation.warmUpRows,
                parameters: calculation.parameters,
                requiredInputs: calculation.capability.requiredInputs.map((role) => ({
                    role,
                    field: inputFields[role],
                })),
                outputFields: calculation.capability.outputs.map((role) => ({
                    role,
                    field: role === 'value' ? layer.y.field : (layer.mark.fields[role] ?? role),
                })),
                session: {
                    mode: calculation.session.mode,
                    reset: calculation.session.reset,
                    boundaries: [...calculation.session.boundaries],
                },
                presentation: {
                    placement: calculation.presentation.placement,
                    panelId: calculation.presentation.panelId,
                    synchronizedCrosshair: {
                        axis: calculation.presentation.synchronizedCrosshair.axis,
                        sharedDomain: calculation.presentation.synchronizedCrosshair.sharedDomain,
                        fields: calculation.presentation.synchronizedCrosshair.fields.map((role) => role === 'value' ? layer.y.field : (layer.mark.fields[role] ?? role)),
                    },
                },
                dependencyDag: calculation.capability.dependencyDag.map((node) => ({
                    id: node.id,
                    operation: node.operation,
                    inputs: [...node.inputs],
                    parameters: [...node.parameters],
                })),
                provenance: calculation.provenance,
            },
            detail: `${calculation.capability.id} uses a parameterized dependency DAG, ${calculation.session.reset} session state, null warm-up rows, and ${calculation.presentation.placement} presentation.`,
        };
        const lineage = {
            ...transformed.lineage,
            transforms: [...transformed.lineage.transforms, step],
            summary: `${transformed.lineage.summary} ${step.detail}`,
        };
        return {
            layer: {
                ...layer,
                mark: {
                    ...layer.mark,
                    fields: {
                        ...layer.mark.fields,
                        __indicatorSource: sourceField,
                    },
                },
            },
            result: { data, lineage },
            calculation,
        };
    }

    const UNSAFE_FIELDS = new Set(['__proto__', 'prototype', 'constructor']);
    const TOOLTIP_FORMATS = new Set(['auto', 'number', 'integer', 'percent', 'date', 'datetime']);
    const TOOLTIP_KEYS = new Set(['trigger', 'axis', 'title', 'fields']);
    const TOOLTIP_FIELD_KEYS = new Set([
        'field',
        'label',
        'format',
        'fractionDigits',
        'prefix',
        'suffix',
    ]);
    const INTERACTION_KEYS = new Set([
        'hover',
        'click',
        'tooltip',
        'navigation',
        'domainNavigation',
        'playback',
        'controls',
        'selection',
    ]);
    const NAVIGATION_KEYS = new Set(['minZoom', 'maxZoom', 'wheel', 'drag', 'pinch', 'keyboard']);
    const NAVIGATION_WHEEL_MODES = new Set(['off', 'modifier', 'always']);
    const DOMAIN_NAVIGATION_KEYS = new Set(['axes', 'maxZoom', 'wheel', 'drag', 'keyboard']);
    const PLAYBACK_KEYS = new Set([
        'field',
        'key',
        'layerId',
        'mode',
        'interval',
        'rate',
        'loop',
        'direction',
        'range',
        'namedFrames',
        'windowSize',
        'autoplay',
        'transition',
        'filter',
    ]);
    const PLAYBACK_MODES = new Set(['frame', 'cumulative', 'window']);
    const PLAYBACK_DIRECTIONS = new Set(['forward', 'reverse']);
    const PLAYBACK_RANGE_KEYS = new Set(['start', 'end']);
    const PLAYBACK_NAMED_FRAME_KEYS = new Set(['name', 'value']);
    const PLAYBACK_TRANSITION_KEYS = new Set(['duration', 'easing']);
    const PLAYBACK_TRANSITION_EASINGS = new Set(['linear', 'ease-in-out']);
    const CONTROLS_KEYS = new Set([
        'zoom',
        'reset',
        'fullscreen',
        'export',
        'annotations',
        'playback',
        'labels',
    ]);
    const CONTROL_LABEL_KEYS = new Set([
        'controls',
        'zoomIn',
        'zoomOut',
        'reset',
        'enterFullscreen',
        'exitFullscreen',
        'exportPng',
        'showAnnotations',
        'hideAnnotations',
        'previousFrame',
        'play',
        'pause',
        'nextFrame',
        'seek',
        'speed',
        'loop',
    ]);
    const SELECTION_KEYS = new Set([
        'kind',
        'combine',
        'mode',
        'toggle',
        'key',
        'clearOnBackground',
        'clearOnEscape',
        'ariaLabel',
        'highlight',
        'axis',
        'xAxis',
        'yAxis',
        'maxSelections',
        'maxLassoPoints',
        'minPixelSpan',
        'keyboard',
        'keyboardStep',
        'filter',
        'linked',
    ]);
    const SELECTION_MODES = new Set(['single', 'multiple']);
    const SELECTION_KINDS = new Set(['point', 'interval', 'rectangle', 'axis', 'lasso']);
    const SELECTION_COMBINE = new Set(['union', 'intersection']);
    const ACCESSIBILITY_KEYS = new Set([
        'label',
        'description',
        'table',
        'maxRows',
        'navigation',
        'explorer',
        'linkedFocus',
        'summary',
        'live',
    ]);
    const ACCESSIBILITY_LIVE_KEYS = new Set(['enabled', 'throttleMs']);
    const ACCESSIBILITY_EXPLORER_KEYS = new Set(['windowRows', 'overscanRows', 'rowHeight']);
    const ACCESSIBILITY_LINKED_FOCUS_KEYS = new Set(['group', 'key']);
    const ACCESSIBILITY_TABLE_MODES = new Set(['hidden', 'visible']);
    const LEGEND_KEYS = new Set([
        'visible',
        'mode',
        'position',
        'orientation',
        'title',
        'field',
        'layerId',
        'items',
        'maxItems',
        'interactive',
        'labels',
    ]);
    const LEGEND_ITEM_KEYS = new Set(['id', 'label', 'color', 'layerId', 'value', 'symbol']);
    const LEGEND_ITEM_SYMBOLS = new Set(['auto', 'line', 'point', 'rect']);
    const LEGEND_LABEL_KEYS = new Set(['show', 'hide']);
    const LEGEND_MODES = new Set(['auto', 'layers', 'categories', 'continuous']);
    const LEGEND_POSITIONS = new Set([
        'top',
        'right',
        'bottom',
        'left',
        'inside-top-left',
        'inside-top-right',
        'inside-bottom-left',
        'inside-bottom-right',
    ]);
    const LEGEND_ORIENTATIONS = new Set(['auto', 'horizontal', 'vertical']);
    const DECORATION_TARGET_KEYS = new Set([
        'type',
        'layerId',
        'rowIndex',
        'field',
        'value',
        'values',
        'x',
        'y',
        'width',
        'height',
    ]);
    const AXIS_RANGE_KEYS = new Set(['axis', 'from', 'to']);
    const HIGHLIGHT_KEYS = new Set([
        'id',
        'target',
        'fill',
        'stroke',
        'opacity',
        'lineWidth',
        'dash',
        'padding',
        'radius',
    ]);
    const ANNOTATION_KEYS = new Set([
        'id',
        'primitive',
        'target',
        'text',
        'detail',
        'placement',
        'offsetX',
        'offsetY',
        'connector',
        'style',
    ]);
    const ANNOTATION_PRIMITIVES = new Set(['callout', 'label', 'point', 'rule', 'band']);
    const ANNOTATION_PLACEMENTS = new Set(['auto', 'top', 'right', 'bottom', 'left']);
    const CONNECTOR_KEYS = new Set(['visible', 'color', 'width', 'dash']);
    const ANNOTATION_STYLE_KEYS = new Set([
        'background',
        'border',
        'color',
        'opacity',
        'fontSize',
        'maxWidth',
        'padding',
        'align',
    ]);
    const ANNOTATION_TEXT_ALIGNS = new Set(['start', 'center', 'end']);
    const MARK_LABEL_KEYS = new Set([
        'visible',
        'field',
        'key',
        'layerIds',
        'placement',
        'offset',
        'collision',
        'connector',
        'maxLabels',
        'positions',
        'style',
        'authoring',
    ]);
    const MARK_LABEL_PLACEMENTS = new Set(['auto', 'top', 'right', 'bottom', 'left', 'center']);
    const MARK_LABEL_COLLISIONS = new Set(['avoid', 'hide', 'none']);
    const MARK_LABEL_POSITION_KEYS = new Set(['target', 'offsetX', 'offsetY', 'hidden']);
    const MARK_LABEL_STYLE_KEYS = new Set([
        'color',
        'background',
        'border',
        'opacity',
        'fontSize',
        'fontWeight',
        'maxWidth',
        'padding',
        'radius',
    ]);
    const MARK_LABEL_AUTHORING_KEYS = new Set(['pointer', 'keyboard', 'step', 'historyLimit', 'snap']);
    const MARK_LABEL_SNAP_KEYS = new Set(['grid', 'marks', 'plot', 'distance']);
    const ENCODING_KEYS = new Set([
        'field',
        'value',
        'type',
        'title',
        'scale',
        'axis',
        'axisId',
        'condition',
    ]);
    const ENCODING_CONDITION_KEYS = new Set(['test', 'field', 'value']);
    const ENCODING_CHANNELS = new Set([
        'x',
        'x2',
        'y',
        'y2',
        'color',
        'fill',
        'stroke',
        'size',
        'radius',
        'shape',
        'symbol',
        'icon',
        'opacity',
        'strokeWidth',
        'strokeDash',
        'text',
        'angle',
        'theta',
        'longitude',
        'latitude',
        'open',
        'high',
        'low',
        'close',
        'volume',
        'order',
        'detail',
        'tooltip',
    ]);
    const FIELD_TYPES = new Set(['quantitative', 'temporal', 'ordinal', 'nominal']);
    const SCALE_KEYS = new Set([
        'type',
        'domain',
        'range',
        'zero',
        'nice',
        'clamp',
        'reverse',
        'outOfBounds',
        'base',
        'exponent',
        'constant',
        'paddingInner',
        'paddingOuter',
    ]);
    const SCALE_TYPES = new Set([
        'linear',
        'log',
        'symlog',
        'asinh',
        'pow',
        'sqrt',
        'time',
        'utc',
        'band',
        'point',
        'ordinal',
        'quantile',
        'quantize',
        'threshold',
        'sequential',
        'diverging',
        'cyclic',
        'probability',
        'logit',
        'probit',
    ]);
    const SCALE_OUT_OF_BOUNDS = new Set(['extrapolate', 'clamp', 'error', 'unknown']);
    const POINT_SHAPES = new Set(['circle', 'square', 'diamond', 'triangle', 'cross']);
    const RAW_ONLY_ENCODING_CHANNELS = new Set([
        'shape',
        'symbol',
        'icon',
        'strokeDash',
        'text',
        'angle',
        'theta',
        'longitude',
        'latitude',
        'open',
        'high',
        'low',
        'close',
        'volume',
        'order',
        'detail',
        'tooltip',
    ]);
    const FIELD_ONLY_SEMANTIC_CHANNELS = new Set([
        'angle',
        'theta',
        'longitude',
        'latitude',
        'open',
        'high',
        'low',
        'close',
        'volume',
    ]);
    const GEOGRAPHIC_POSITION_MARKS = new Set([
        'map',
        'geo-flow',
        'geo-line',
        'geo-heatmap',
        'tiled-map',
        'tilemap',
    ]);
    const TRADING_CHANNEL_MARKS = new Set(['candlestick', 'financial']);
    const AXIS_KEYS = new Set([
        'channel',
        'title',
        'visible',
        'position',
        'offset',
        'line',
        'grid',
        'ticks',
        'labels',
        'tickCount',
        'format',
        'labelAngle',
    ]);
    const AXIS_STROKE_KEYS = new Set(['visible', 'color', 'width', 'opacity', 'dash']);
    const AXIS_TICK_KEYS = new Set([...AXIS_STROKE_KEYS, 'count', 'spacing', 'size', 'values']);
    const AXIS_LABEL_KEYS = new Set([
        'visible',
        'orientation',
        'angle',
        'align',
        'padding',
        'maxLength',
        'color',
        'font',
    ]);
    const AXIS_TITLE_KEYS = new Set(['text', 'visible', 'align', 'angle', 'padding', 'color', 'font']);
    const AXIS_FONT_KEYS = new Set(['family', 'size', 'weight', 'style']);
    const AXIS_FORMAT_KEYS = new Set([
        'type',
        'fractionDigits',
        'notation',
        'useGrouping',
        'currency',
        'currencyDisplay',
        'dateStyle',
        'timeStyle',
        'timeZone',
        'prefix',
        'suffix',
    ]);
    const AXIS_FORMAT_TYPES = new Set([
        'auto',
        'number',
        'integer',
        'percent',
        'compact',
        'scientific',
        'currency',
        'date',
        'time',
        'datetime',
    ]);
    const AXIS_POSITIONS = new Set(['top', 'bottom', 'left', 'right']);
    const AXIS_LABEL_ORIENTATIONS = new Set(['auto', 'horizontal', 'vertical-up', 'vertical-down']);
    const AXIS_TEXT_ALIGNS = new Set(['auto', 'start', 'center', 'end']);
    const AXIS_TITLE_ALIGNS = new Set(['start', 'center', 'end']);
    const AXIS_FONT_WEIGHTS = new Set(['normal', 'medium', 'semibold', 'bold']);
    const AXIS_FONT_STYLES = new Set(['normal', 'italic']);
    const AXIS_NOTATIONS = new Set(['standard', 'compact', 'scientific', 'engineering']);
    const AXIS_CURRENCY_DISPLAYS = new Set(['symbol', 'narrowSymbol', 'code', 'name']);
    const AXIS_DATE_STYLES = new Set(['short', 'medium', 'long', 'full']);
    const AXIS_TIME_STYLES = new Set(['short', 'medium', 'long']);
    const THEME_CONTINUOUS_INTERPOLATIONS = new Set(['step', 'rgb', 'lab']);
    const THEME_SERIES_COLOR_MODES = new Set(['theme', 'series']);
    const THEME_PIE_DIRECTIONS = new Set(['clockwise', 'counterclockwise']);
    const LAYER_KEYS = new Set([
        'id',
        'name',
        'data',
        'source',
        'transform',
        'mark',
        'x',
        'y',
        'encoding',
        'clip',
        'visible',
        'zIndex',
    ]);
    const COMPOSITION_RESOLVE_KEYS = new Set(['scale', 'axis', 'legend', 'colorbar']);
    const FACET_KEYS = new Set(['row', 'column', 'wrap', 'columns']);
    const FACET_FIELD_KEYS = new Set(['field', 'title', 'sort']);
    const FACET_SORTS = new Set(['input', 'ascending', 'descending']);
    const REPEAT_KEYS = new Set(['items', 'columns']);
    const REPEAT_ITEM_KEYS = new Set(['id', 'label', 'x', 'y']);
    const INSET_KEYS = new Set(['base', 'view', 'x', 'y', 'width', 'height', 'label']);
    const LAYER_COMPOSITION_CHILD_KEYS = new Set([
        '$schema',
        'specVersion',
        'data',
        'source',
        'dataflow',
        'transform',
        'mark',
        'x',
        'y',
        'encoding',
        'layers',
        'markLabels',
    ]);
    const COMPOSITION_KEYS = new Set([
        '$schema',
        'specVersion',
        'data',
        'source',
        'dataflow',
        'transform',
        'mark',
        'x',
        'y',
        'encoding',
        'layers',
        'layer',
        'facet',
        'repeat',
        'hconcat',
        'vconcat',
        'concat',
        'inset',
        'spec',
        'columns',
        'spacing',
        'resolve',
        'width',
        'height',
        'padding',
        'title',
        'description',
        'renderer',
        'performance',
        'theme',
        'locale',
        'axes',
        'legend',
        'highlights',
        'annotations',
        'interaction',
        'accessibility',
        'streaming',
    ]);
    const CURVE_NAMES = new Set(curveNames);
    const MISSING_VALUE_POLICIES = new Set(['gap', 'zero', 'connect']);
    const CURVE_MARKS = new Set(['line', 'area', 'smooth', 'stepped-area', 'theme-river']);
    const SERIES_STACK_MARKS = new Set(['area', 'bar', 'theme-river']);
    const SERIES_STACK_OFFSETS = new Set([
        'zero',
        'normalize',
        'expand',
        'center',
        'silhouette',
        'wiggle',
    ]);
    const SERIES_STACK_ORDERS = new Set([
        'input',
        'ascending',
        'descending',
        'sumAscending',
        'sumDescending',
        'insideOut',
    ]);
    const STREAMING_KEYS = new Set([
        'key',
        'mode',
        'maxBatchRows',
        'retention',
        'eventTime',
        'queue',
        'replay',
        'runtime',
        'worker',
    ]);
    const STREAMING_RETENTION_KEYS = new Set(['maxRows', 'time']);
    const STREAMING_TIME_KEYS = new Set(['field', 'durationMs']);
    const STREAMING_EVENT_TIME_KEYS = new Set(['field', 'allowedLatenessMs', 'lateData']);
    const STREAMING_QUEUE_KEYS = new Set(['maxBatches', 'maxRows', 'overflow']);
    const STREAMING_REPLAY_KEYS = new Set(['maxBatches', 'maxRows']);
    const STREAMING_RUNTIME_KEYS = new Set([
        'schedule',
        'maxBatchesPerFrame',
        'overflow',
        'paused',
        'followLive',
        'history',
    ]);
    const STREAMING_HISTORY_KEYS = new Set(['maxBatches', 'pageRows']);
    const STREAMING_WORKER_KEYS = new Set([
        'moduleURL',
        'name',
        'maxQueueBatches',
        'maxQueueRows',
        'maxInputRows',
        'maxBinaryBytes',
        'maxTransforms',
        'overflow',
        'engine',
    ]);
    const STREAMING_WORKER_ENGINE_KEYS = new Set(['type', 'adapter']);
    const STREAMING_OVERFLOW_POLICIES = new Set(['reject', 'drop-oldest', 'coalesce']);
    function validateUnknownKeys(value, allowed, path, kind, issues) {
        for (const key of Object.keys(value)) {
            if (!allowed.has(key)) {
                issues.push({ path: `${path}.${key}`, message: `Unknown ${kind} property "${key}".` });
            }
        }
    }
    function validateFiniteNumber(value, path, label, issues, options = {}) {
        if (typeof value !== 'number' ||
            !Number.isFinite(value) ||
            (options.integer === true && !Number.isInteger(value)) ||
            (options.min !== undefined && value < options.min) ||
            (options.max !== undefined && value > options.max)) {
            const integer = options.integer === true ? ' integer' : '';
            const range = options.min === undefined && options.max === undefined
                ? ''
                : ` from ${options.min ?? '-infinity'} to ${options.max ?? 'infinity'}`;
            issues.push({ path, message: `${label} must be a finite${integer} number${range}.` });
        }
    }
    function validateOptionalBoolean(value, path, label, issues) {
        if (value !== undefined && typeof value !== 'boolean') {
            issues.push({ path, message: `${label} must be a boolean.` });
        }
    }
    function validateOptionalString(value, path, label, issues, allowEmpty = true) {
        if (value !== undefined && (typeof value !== 'string' || (!allowEmpty && value.trim() === ''))) {
            issues.push({ path, message: `${label} must be a${allowEmpty ? '' : ' non-empty'} string.` });
        }
    }
    function validateTheme(value, path, issues) {
        if (value === undefined)
            return;
        if (typeof value === 'string') {
            if (value.trim() === '')
                issues.push({ path, message: 'Theme name must be non-empty.' });
            return;
        }
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Theme must be a name or an object.' });
            return;
        }
        validateOptionalString(value.extends, `${path}.extends`, 'Theme base name', issues, false);
        if (value.colors !== undefined) {
            if (!isPlainObject(value.colors)) {
                issues.push({ path: `${path}.colors`, message: 'Theme colors must be an object.' });
            }
            else if (value.colors.continuousInterpolation !== undefined &&
                (typeof value.colors.continuousInterpolation !== 'string' ||
                    !THEME_CONTINUOUS_INTERPOLATIONS.has(value.colors.continuousInterpolation))) {
                issues.push({
                    path: `${path}.colors.continuousInterpolation`,
                    message: 'Theme continuous interpolation is not supported.',
                });
            }
        }
        if (value.spacing !== undefined) {
            if (!isPlainObject(value.spacing)) {
                issues.push({ path: `${path}.spacing`, message: 'Theme spacing must be an object.' });
            }
            else if (value.spacing.minimumTitleBlock !== undefined) {
                validateFiniteNumber(value.spacing.minimumTitleBlock, `${path}.spacing.minimumTitleBlock`, 'Theme minimum title block', issues, { min: 0, max: 2_000 });
            }
        }
        if (value.mark !== undefined) {
            if (!isPlainObject(value.mark)) {
                issues.push({ path: `${path}.mark`, message: 'Theme mark must be an object.' });
            }
            else {
                for (const key of ['pointColorMode', 'areaColorMode']) {
                    if (value.mark[key] !== undefined &&
                        (typeof value.mark[key] !== 'string' || !THEME_SERIES_COLOR_MODES.has(value.mark[key]))) {
                        issues.push({
                            path: `${path}.mark.${key}`,
                            message: 'Theme series color mode is not supported.',
                        });
                    }
                }
                if (value.mark.pieDirection !== undefined &&
                    (typeof value.mark.pieDirection !== 'string' ||
                        !THEME_PIE_DIRECTIONS.has(value.mark.pieDirection))) {
                    issues.push({
                        path: `${path}.mark.pieDirection`,
                        message: 'Theme pie direction is not supported.',
                    });
                }
                if (value.mark.pieStartAngle !== undefined) {
                    validateFiniteNumber(value.mark.pieStartAngle, `${path}.mark.pieStartAngle`, 'Theme pie start angle', issues, { min: -1e6, max: 1_000_000 });
                }
                if (value.mark.histogramGap !== undefined) {
                    validateFiniteNumber(value.mark.histogramGap, `${path}.mark.histogramGap`, 'Theme histogram gap', issues, { min: 0, max: 2_000 });
                }
                validateOptionalString(value.mark.boxplotMedianStroke, `${path}.mark.boxplotMedianStroke`, 'Theme boxplot median stroke', issues, false);
            }
        }
        if (value.legend !== undefined) {
            if (!isPlainObject(value.legend)) {
                issues.push({ path: `${path}.legend`, message: 'Theme legend must be an object.' });
            }
            else {
                validateOptionalString(value.legend.borderColor, `${path}.legend.borderColor`, 'Theme legend border color', issues, false);
                if (value.legend.continuousSamples !== undefined) {
                    validateFiniteNumber(value.legend.continuousSamples, `${path}.legend.continuousSamples`, 'Theme continuous legend samples', issues, { min: 1, max: 256, integer: true });
                }
            }
        }
    }
    function validateScale(value, path, issues) {
        if (value === undefined)
            return;
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Scale must be an object.' });
            return;
        }
        validateUnknownKeys(value, SCALE_KEYS, path, 'scale', issues);
        const type = typeof value.type === 'string' ? value.type : undefined;
        if (value.type !== undefined &&
            (typeof value.type !== 'string' || !SCALE_TYPES.has(value.type))) {
            issues.push({ path: `${path}.type`, message: 'Scale type is not supported.' });
        }
        if (value.domain !== undefined) {
            if (!Array.isArray(value.domain) || value.domain.length < 1) {
                issues.push({
                    path: `${path}.domain`,
                    message: 'Scale domain must contain at least 1 value.',
                });
            }
            else {
                value.domain.forEach((entry, index) => {
                    if ((typeof entry !== 'number' && typeof entry !== 'string') ||
                        (typeof entry === 'number' && !Number.isFinite(entry))) {
                        issues.push({
                            path: `${path}.domain[${index}]`,
                            message: 'Scale domain values must be finite numbers or strings.',
                        });
                    }
                });
            }
        }
        if (value.range !== undefined) {
            if (!Array.isArray(value.range) || value.range.length === 0) {
                issues.push({
                    path: `${path}.range`,
                    message: 'Scale range must contain at least one value.',
                });
            }
            else {
                value.range.forEach((entry, index) => {
                    if ((typeof entry !== 'number' && typeof entry !== 'string') ||
                        (typeof entry === 'number' && !Number.isFinite(entry))) {
                        issues.push({
                            path: `${path}.range[${index}]`,
                            message: 'Scale range values must be finite numbers or strings.',
                        });
                    }
                });
            }
        }
        for (const key of ['zero', 'nice', 'clamp', 'reverse']) {
            validateOptionalBoolean(value[key], `${path}.${key}`, `Scale ${key}`, issues);
        }
        if (value.outOfBounds !== undefined &&
            (typeof value.outOfBounds !== 'string' || !SCALE_OUT_OF_BOUNDS.has(value.outOfBounds))) {
            issues.push({
                path: `${path}.outOfBounds`,
                message: 'Scale outOfBounds policy is not supported.',
            });
        }
        if (value.clamp !== undefined && value.outOfBounds !== undefined) {
            issues.push({
                path: `${path}.clamp`,
                message: 'Scale clamp is a compatibility alias; do not combine it with outOfBounds.',
            });
        }
        for (const key of ['base', 'exponent', 'constant']) {
            if (value[key] !== undefined) {
                validateFiniteNumber(value[key], `${path}.${key}`, `Scale ${key}`, issues, {
                    min: Number.MIN_VALUE,
                });
            }
        }
        if (value.base === 1) {
            issues.push({ path: `${path}.base`, message: 'Log scale base must not equal 1.' });
        }
        if (value.paddingInner !== undefined) {
            validateFiniteNumber(value.paddingInner, `${path}.paddingInner`, 'Scale paddingInner', issues, {
                min: 0,
                max: 1,
            });
        }
        if (value.paddingOuter !== undefined) {
            validateFiniteNumber(value.paddingOuter, `${path}.paddingOuter`, 'Scale paddingOuter', issues, {
                min: 0,
            });
        }
        if (Array.isArray(value.range) && type !== undefined) {
            const numericRangeTypes = new Set([
                'linear',
                'log',
                'symlog',
                'asinh',
                'pow',
                'sqrt',
                'time',
                'utc',
                'band',
                'point',
                'quantile',
                'quantize',
                'threshold',
                'probability',
                'logit',
                'probit',
            ]);
            if (numericRangeTypes.has(type) && value.range.some((entry) => typeof entry !== 'number')) {
                issues.push({
                    path: `${path}.range`,
                    message: `${type} position scale range must contain finite numbers.`,
                });
            }
            if ([
                'linear',
                'log',
                'symlog',
                'asinh',
                'pow',
                'sqrt',
                'time',
                'utc',
                'band',
                'point',
                'probability',
                'logit',
                'probit',
            ].includes(type) &&
                value.range.length !== 2) {
                issues.push({
                    path: `${path}.range`,
                    message: `${type} position scale range must contain exactly 2 values.`,
                });
            }
            if ((type === 'sequential' || type === 'diverging' || type === 'cyclic') &&
                value.range.some((entry) => typeof entry !== 'string')) {
                issues.push({
                    path: `${path}.range`,
                    message: `${type} color scale range must contain strings.`,
                });
            }
            const minimumColorStops = type === 'diverging' ? 3 : type === 'sequential' || type === 'cyclic' ? 2 : 0;
            if (minimumColorStops > 0 && value.range.length < minimumColorStops) {
                issues.push({
                    path: `${path}.range`,
                    message: `${type} color scale requires at least ${minimumColorStops} colors.`,
                });
            }
        }
        if (value.base !== undefined && type !== 'log') {
            issues.push({ path: `${path}.base`, message: 'Scale base is only valid for log scales.' });
        }
        if (value.exponent !== undefined && type !== 'pow') {
            issues.push({
                path: `${path}.exponent`,
                message: 'Scale exponent is only valid for pow scales.',
            });
        }
        if (value.constant !== undefined && type !== 'symlog' && type !== 'asinh') {
            issues.push({
                path: `${path}.constant`,
                message: 'Scale constant is only valid for symlog or asinh scales.',
            });
        }
        if ((value.paddingInner !== undefined || value.paddingOuter !== undefined) &&
            type !== 'band' &&
            type !== 'point') {
            issues.push({ path, message: 'Scale padding is only valid for band or point scales.' });
        }
        if (value.nice !== undefined && type !== undefined && !['linear', 'time', 'utc'].includes(type)) {
            issues.push({
                path: `${path}.nice`,
                message: 'Scale nice is currently implemented only for linear, time, and utc scales.',
            });
        }
        if (value.zero === true && (type === 'log' || type === 'logit' || type === 'probit')) {
            issues.push({ path: `${path}.zero`, message: `${type} scale cannot include zero.` });
        }
        if ((type === 'band' || type === 'point' || type === 'ordinal') &&
            (value.outOfBounds === 'clamp' || value.outOfBounds === 'extrapolate' || value.clamp === true)) {
            issues.push({
                path: `${path}.outOfBounds`,
                message: `${type} scale supports only error or unknown for unseen categories.`,
            });
        }
        if (type === 'quantize' && value.outOfBounds === 'extrapolate') {
            issues.push({
                path: `${path}.outOfBounds`,
                message: 'Quantize scales cannot extrapolate discrete range values.',
            });
        }
        if ((type === 'quantile' || type === 'threshold') &&
            (value.outOfBounds !== undefined || value.clamp !== undefined)) {
            issues.push({
                path: `${path}.outOfBounds`,
                message: `${type} scale bin semantics do not accept outOfBounds.`,
            });
        }
        if (type === 'cyclic' && (value.outOfBounds !== undefined || value.clamp !== undefined)) {
            issues.push({
                path: `${path}.outOfBounds`,
                message: 'Cyclic scales always wrap and do not accept outOfBounds.',
            });
        }
        if (Array.isArray(value.domain)) {
            const numbers = value.domain.filter((entry) => typeof entry === 'number');
            if ((type === 'band' || type === 'point') && numbers.length > 0) {
                issues.push({
                    path: `${path}.domain`,
                    message: `${type} scale domain must contain strings.`,
                });
            }
            if (type !== undefined &&
                [
                    'linear',
                    'log',
                    'symlog',
                    'asinh',
                    'pow',
                    'sqrt',
                    'quantile',
                    'quantize',
                    'threshold',
                    'sequential',
                    'diverging',
                    'cyclic',
                    'probability',
                    'logit',
                    'probit',
                ].includes(type) &&
                numbers.length !== value.domain.length) {
                issues.push({ path: `${path}.domain`, message: `${type} scale domain must be numeric.` });
            }
            if (type === 'quantile' && value.domain.length < 2) {
                issues.push({
                    path: `${path}.domain`,
                    message: 'Quantile scale domain must contain at least 2 numeric samples.',
                });
            }
            if ((type === 'time' || type === 'utc') &&
                value.domain.some((entry) => typeof entry !== 'number' &&
                    (typeof entry !== 'string' || !Number.isFinite(Date.parse(entry))))) {
                issues.push({
                    path: `${path}.domain`,
                    message: `${type} scale domain values must be finite epochs or parseable date strings.`,
                });
            }
            if (type === 'log' && numbers.some((entry) => entry <= 0)) {
                issues.push({
                    path: `${path}.domain`,
                    message: 'Log scale domain values must be greater than 0.',
                });
            }
            if (type === 'probability' && numbers.some((entry) => entry < 0 || entry > 1)) {
                issues.push({
                    path: `${path}.domain`,
                    message: 'Probability scale domain values must be between 0 and 1.',
                });
            }
            if ((type === 'logit' || type === 'probit') &&
                numbers.some((entry) => entry <= 0 || entry >= 1)) {
                issues.push({
                    path: `${path}.domain`,
                    message: `${type} scale domain values must be strictly between 0 and 1.`,
                });
            }
            const requiredLength = type === 'diverging'
                ? 3
                : [
                    'linear',
                    'log',
                    'symlog',
                    'asinh',
                    'pow',
                    'sqrt',
                    'time',
                    'utc',
                    'quantize',
                    'sequential',
                    'cyclic',
                    'probability',
                    'logit',
                    'probit',
                ].includes(type ?? '')
                    ? 2
                    : undefined;
            if (requiredLength !== undefined && value.domain.length !== requiredLength) {
                issues.push({
                    path: `${path}.domain`,
                    message: `${type} scale domain must contain exactly ${requiredLength} values.`,
                });
            }
            if (type !== undefined &&
                [
                    'linear',
                    'log',
                    'symlog',
                    'asinh',
                    'pow',
                    'sqrt',
                    'quantize',
                    'sequential',
                    'cyclic',
                    'probability',
                    'logit',
                    'probit',
                ].includes(type) &&
                numbers.length === 2 &&
                numbers[0] === numbers[1]) {
                issues.push({
                    path: `${path}.domain`,
                    message: `${type} scale domain endpoints must differ.`,
                });
            }
            if ((type === 'time' || type === 'utc') &&
                value.domain.length === 2 &&
                value.domain
                    .map((entry) => (typeof entry === 'number' ? entry : Date.parse(String(entry))))
                    .every(Number.isFinite) &&
                (typeof value.domain[0] === 'number'
                    ? value.domain[0]
                    : Date.parse(String(value.domain[0]))) ===
                    (typeof value.domain[1] === 'number'
                        ? value.domain[1]
                        : Date.parse(String(value.domain[1])))) {
                issues.push({
                    path: `${path}.domain`,
                    message: `${type} scale domain endpoints must differ.`,
                });
            }
            if (type === 'diverging' &&
                numbers.length === 3 &&
                !(numbers[0] < numbers[1] && numbers[1] < numbers[2])) {
                issues.push({
                    path: `${path}.domain`,
                    message: 'Diverging scale domain must be strictly ascending.',
                });
            }
            if (type === 'threshold' &&
                numbers.length === value.domain.length &&
                numbers.some((entry, index) => index > 0 && entry <= numbers[index - 1])) {
                issues.push({
                    path: `${path}.domain`,
                    message: 'Threshold scale domain must be strictly ascending.',
                });
            }
        }
        if (type === 'threshold' &&
            Array.isArray(value.domain) &&
            Array.isArray(value.range) &&
            value.range.length !== value.domain.length + 1) {
            issues.push({
                path: `${path}.range`,
                message: 'Threshold scale range length must equal domain length + 1.',
            });
        }
    }
    function validateAxisFont(value, path, issues) {
        if (value === undefined)
            return;
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Axis font must be an object.' });
            return;
        }
        validateUnknownKeys(value, AXIS_FONT_KEYS, path, 'axis font', issues);
        validateOptionalString(value.family, `${path}.family`, 'Axis font family', issues, false);
        if (value.size !== undefined) {
            validateFiniteNumber(value.size, `${path}.size`, 'Axis font size', issues, {
                min: 1,
                max: 256,
            });
        }
        if (value.weight !== undefined &&
            !((typeof value.weight === 'number' &&
                Number.isInteger(value.weight) &&
                value.weight >= 100 &&
                value.weight <= 900) ||
                (typeof value.weight === 'string' && AXIS_FONT_WEIGHTS.has(value.weight)))) {
            issues.push({
                path: `${path}.weight`,
                message: 'Axis font weight must be 100..900 or a supported named weight.',
            });
        }
        if (value.style !== undefined &&
            (typeof value.style !== 'string' || !AXIS_FONT_STYLES.has(value.style))) {
            issues.push({ path: `${path}.style`, message: 'Axis font style is not supported.' });
        }
    }
    function validateAxisStroke(value, path, issues, allowedKeys = AXIS_STROKE_KEYS) {
        if (typeof value === 'boolean')
            return false;
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Axis stroke must be a boolean or an object.' });
            return false;
        }
        validateUnknownKeys(value, allowedKeys, path, 'axis stroke', issues);
        validateOptionalBoolean(value.visible, `${path}.visible`, 'Axis stroke visibility', issues);
        validateOptionalString(value.color, `${path}.color`, 'Axis stroke color', issues, false);
        if (value.width !== undefined) {
            validateFiniteNumber(value.width, `${path}.width`, 'Axis stroke width', issues, {
                min: 0,
                max: 32,
            });
        }
        if (value.opacity !== undefined) {
            validateFiniteNumber(value.opacity, `${path}.opacity`, 'Axis stroke opacity', issues, {
                min: 0,
                max: 1,
            });
        }
        if (value.dash !== undefined) {
            if (!Array.isArray(value.dash) || value.dash.length > 16) {
                issues.push({
                    path: `${path}.dash`,
                    message: 'Axis stroke dash must be an array of at most 16 numbers.',
                });
            }
            else {
                value.dash.forEach((entry, index) => validateFiniteNumber(entry, `${path}.dash[${index}]`, 'Axis stroke dash value', issues, {
                    min: 0,
                    max: 256,
                }));
            }
        }
        return true;
    }
    function validateAxisTicks(value, path, issues) {
        if (typeof value === 'boolean')
            return;
        if (!validateAxisStroke(value, path, issues, AXIS_TICK_KEYS))
            return;
        if (value.count !== undefined) {
            validateFiniteNumber(value.count, `${path}.count`, 'Axis tick count', issues, {
                integer: true,
                min: 1,
                max: 200,
            });
        }
        for (const key of ['spacing', 'size']) {
            if (value[key] !== undefined) {
                validateFiniteNumber(value[key], `${path}.${key}`, `Axis tick ${key}`, issues, {
                    min: 0,
                    max: 256,
                });
            }
        }
        if (value.values !== undefined) {
            if (!Array.isArray(value.values) || value.values.length === 0 || value.values.length > 200) {
                issues.push({
                    path: `${path}.values`,
                    message: 'Axis tick values must contain between 1 and 200 entries.',
                });
            }
            else {
                value.values.forEach((entry, index) => {
                    if ((typeof entry !== 'number' && typeof entry !== 'string') ||
                        (typeof entry === 'number' && !Number.isFinite(entry))) {
                        issues.push({
                            path: `${path}.values[${index}]`,
                            message: 'Axis tick values must be finite numbers or strings.',
                        });
                    }
                });
            }
        }
    }
    function validateAxisLabels(value, path, issues) {
        if (typeof value === 'boolean')
            return;
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Axis labels must be a boolean or an object.' });
            return;
        }
        validateUnknownKeys(value, AXIS_LABEL_KEYS, path, 'axis label', issues);
        validateOptionalBoolean(value.visible, `${path}.visible`, 'Axis label visibility', issues);
        if (value.orientation !== undefined &&
            (typeof value.orientation !== 'string' || !AXIS_LABEL_ORIENTATIONS.has(value.orientation))) {
            issues.push({
                path: `${path}.orientation`,
                message: 'Axis label orientation is not supported.',
            });
        }
        if (value.angle !== undefined) {
            validateFiniteNumber(value.angle, `${path}.angle`, 'Axis label angle', issues, {
                min: -360,
                max: 360,
            });
        }
        if (value.align !== undefined &&
            (typeof value.align !== 'string' || !AXIS_TEXT_ALIGNS.has(value.align))) {
            issues.push({ path: `${path}.align`, message: 'Axis label alignment is not supported.' });
        }
        if (value.padding !== undefined) {
            validateFiniteNumber(value.padding, `${path}.padding`, 'Axis label padding', issues, {
                min: 0,
                max: 256,
            });
        }
        if (value.maxLength !== undefined) {
            validateFiniteNumber(value.maxLength, `${path}.maxLength`, 'Axis label maxLength', issues, {
                integer: true,
                min: 1,
                max: 1000,
            });
        }
        validateOptionalString(value.color, `${path}.color`, 'Axis label color', issues, false);
        validateAxisFont(value.font, `${path}.font`, issues);
    }
    function validateAxisTitle(value, path, issues) {
        if (value === undefined)
            return;
        if (typeof value === 'string' || value === false)
            return;
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Axis title must be a string, false, or an object.' });
            return;
        }
        validateUnknownKeys(value, AXIS_TITLE_KEYS, path, 'axis title', issues);
        validateOptionalString(value.text, `${path}.text`, 'Axis title text', issues);
        validateOptionalBoolean(value.visible, `${path}.visible`, 'Axis title visibility', issues);
        if (value.align !== undefined &&
            (typeof value.align !== 'string' || !AXIS_TITLE_ALIGNS.has(value.align))) {
            issues.push({ path: `${path}.align`, message: 'Axis title alignment is not supported.' });
        }
        if (value.angle !== undefined) {
            validateFiniteNumber(value.angle, `${path}.angle`, 'Axis title angle', issues, {
                min: -360,
                max: 360,
            });
        }
        if (value.padding !== undefined) {
            validateFiniteNumber(value.padding, `${path}.padding`, 'Axis title padding', issues, {
                min: 0,
                max: 256,
            });
        }
        validateOptionalString(value.color, `${path}.color`, 'Axis title color', issues, false);
        validateAxisFont(value.font, `${path}.font`, issues);
    }
    function validateAxisFormat(value, path, issues) {
        if (typeof value === 'string') {
            if (!AXIS_FORMAT_TYPES.has(value)) {
                issues.push({ path, message: 'Axis format is not supported.' });
            }
            return;
        }
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Axis format must be a supported name or an object.' });
            return;
        }
        validateUnknownKeys(value, AXIS_FORMAT_KEYS, path, 'axis format', issues);
        if (value.type !== undefined &&
            (typeof value.type !== 'string' || !AXIS_FORMAT_TYPES.has(value.type))) {
            issues.push({ path: `${path}.type`, message: 'Axis format type is not supported.' });
        }
        if (value.fractionDigits !== undefined) {
            validateFiniteNumber(value.fractionDigits, `${path}.fractionDigits`, 'Axis format fractionDigits', issues, { integer: true, min: 0, max: 20 });
        }
        if (value.notation !== undefined &&
            (typeof value.notation !== 'string' || !AXIS_NOTATIONS.has(value.notation))) {
            issues.push({ path: `${path}.notation`, message: 'Axis number notation is not supported.' });
        }
        validateOptionalBoolean(value.useGrouping, `${path}.useGrouping`, 'Axis format useGrouping', issues);
        if (value.currency !== undefined) {
            if (typeof value.currency !== 'string' || !/^[A-Z]{3}$/.test(value.currency)) {
                issues.push({
                    path: `${path}.currency`,
                    message: 'Axis currency must be an uppercase three-letter code.',
                });
            }
            if (value.type !== undefined && value.type !== 'currency') {
                issues.push({
                    path: `${path}.currency`,
                    message: 'Axis currency is only valid for the currency format.',
                });
            }
        }
        if (value.currencyDisplay !== undefined &&
            (typeof value.currencyDisplay !== 'string' ||
                !AXIS_CURRENCY_DISPLAYS.has(value.currencyDisplay))) {
            issues.push({
                path: `${path}.currencyDisplay`,
                message: 'Axis currency display is not supported.',
            });
        }
        if (value.dateStyle !== undefined &&
            (typeof value.dateStyle !== 'string' || !AXIS_DATE_STYLES.has(value.dateStyle))) {
            issues.push({ path: `${path}.dateStyle`, message: 'Axis date style is not supported.' });
        }
        if (value.timeStyle !== undefined &&
            (typeof value.timeStyle !== 'string' || !AXIS_TIME_STYLES.has(value.timeStyle))) {
            issues.push({ path: `${path}.timeStyle`, message: 'Axis time style is not supported.' });
        }
        validateOptionalString(value.timeZone, `${path}.timeZone`, 'Axis timeZone', issues, false);
        validateOptionalString(value.prefix, `${path}.prefix`, 'Axis format prefix', issues);
        validateOptionalString(value.suffix, `${path}.suffix`, 'Axis format suffix', issues);
    }
    function validateAxis(value, path, axisId, expectedChannel, issues) {
        if (value === false)
            return;
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Axis must be an object or false.' });
            return;
        }
        validateUnknownKeys(value, AXIS_KEYS, path, 'axis', issues);
        if (value.channel !== undefined &&
            (typeof value.channel !== 'string' || !['x', 'y'].includes(value.channel))) {
            issues.push({ path: `${path}.channel`, message: 'Axis channel must be x or y.' });
        }
        const channel = expectedChannel ??
            builtInAxisChannel(axisId) ??
            (value.channel === 'x' || value.channel === 'y' ? value.channel : undefined);
        if (expectedChannel !== undefined &&
            value.channel !== undefined &&
            value.channel !== expectedChannel) {
            issues.push({
                path: `${path}.channel`,
                message: `Axis channel must be "${expectedChannel}" in this context.`,
            });
        }
        validateAxisTitle(value.title, `${path}.title`, issues);
        validateOptionalBoolean(value.visible, `${path}.visible`, 'Axis visibility', issues);
        if (value.position !== undefined) {
            if (typeof value.position !== 'string' || !AXIS_POSITIONS.has(value.position)) {
                issues.push({ path: `${path}.position`, message: 'Axis position is not supported.' });
            }
            else if (channel !== undefined &&
                axisPositionChannel(value.position) !== channel) {
                issues.push({
                    path: `${path}.position`,
                    message: `${axisId}-axis position is incompatible with its channel.`,
                });
            }
        }
        if (value.offset !== undefined) {
            validateFiniteNumber(value.offset, `${path}.offset`, 'Axis offset', issues, {
                min: 0,
                max: 256,
            });
        }
        for (const key of ['line', 'grid']) {
            if (value[key] !== undefined)
                validateAxisStroke(value[key], `${path}.${key}`, issues);
        }
        if (value.ticks !== undefined)
            validateAxisTicks(value.ticks, `${path}.ticks`, issues);
        if (value.labels !== undefined)
            validateAxisLabels(value.labels, `${path}.labels`, issues);
        if (value.tickCount !== undefined) {
            validateFiniteNumber(value.tickCount, `${path}.tickCount`, 'Axis tickCount', issues, {
                integer: true,
                min: 1,
                max: 200,
            });
        }
        if (value.format !== undefined)
            validateAxisFormat(value.format, `${path}.format`, issues);
        if (value.labelAngle !== undefined) {
            validateFiniteNumber(value.labelAngle, `${path}.labelAngle`, 'Axis labelAngle', issues, {
                min: -360,
                max: 360,
            });
        }
    }
    function validateAxes(value, path, issues) {
        if (value === undefined)
            return;
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Axes must be an object.' });
            return;
        }
        for (const [axisId, axis] of Object.entries(value)) {
            if (!isSafeAxisId(axisId)) {
                issues.push({
                    path: `${path}.${axisId}`,
                    message: 'Axis id must start with an ASCII letter and contain at most 64 letters, digits, underscores, or hyphens.',
                });
                continue;
            }
            const builtInChannel = builtInAxisChannel(axisId);
            if (builtInChannel === undefined) {
                if (!isPlainObject(axis)) {
                    issues.push({
                        path: `${path}.${axisId}`,
                        message: 'Named axis must be an object with explicit channel and position.',
                    });
                    continue;
                }
                if (axis.channel !== 'x' && axis.channel !== 'y') {
                    issues.push({
                        path: `${path}.${axisId}.channel`,
                        message: 'Named axis requires an explicit x or y channel.',
                    });
                }
                if (axis.position === undefined) {
                    issues.push({
                        path: `${path}.${axisId}.position`,
                        message: 'Named axis requires an explicit top, bottom, left, or right position.',
                    });
                }
            }
            validateAxis(axis, `${path}.${axisId}`, axisId, builtInChannel, issues);
        }
    }
    function validateEncodingCondition(value, path, channel, issues) {
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Encoding condition must be an object.' });
            return;
        }
        validateUnknownKeys(value, ENCODING_CONDITION_KEYS, path, 'encoding condition', issues);
        validateTransformExpression(value.test, `${path}.test`, issues);
        const hasField = value.field !== undefined;
        const hasValue = value.value !== undefined;
        if (hasField === hasValue) {
            issues.push({ path, message: 'Encoding condition requires exactly one of field or value.' });
        }
        if (hasField) {
            if (typeof value.field !== 'string' ||
                value.field.trim() === '' ||
                UNSAFE_FIELDS.has(value.field)) {
                issues.push({
                    path: `${path}.field`,
                    message: 'Conditional field must be a non-empty safe string.',
                });
            }
        }
        if (hasValue)
            validateChannelValue(value.value, `${path}.value`, channel, issues);
    }
    function validateChannelValue(value, path, channel, issues) {
        if (channel === 'strokeDash') {
            if (!Array.isArray(value) ||
                value.some((entry) => typeof entry !== 'number' || !Number.isFinite(entry) || entry < 0)) {
                issues.push({
                    path,
                    message: 'strokeDash value must be an array of non-negative finite numbers.',
                });
            }
            return;
        }
        if (Array.isArray(value) ||
            value === undefined ||
            (value !== null && !['string', 'number', 'boolean'].includes(typeof value))) {
            issues.push({ path, message: 'Encoding value must be a JSON scalar.' });
            return;
        }
        if (typeof value === 'number' && !Number.isFinite(value)) {
            issues.push({ path, message: 'Encoding number must be finite.' });
        }
        if (['size', 'radius', 'strokeWidth'].includes(channel) &&
            (typeof value !== 'number' || value < 0)) {
            issues.push({ path, message: `${channel} value must be a non-negative number.` });
        }
        if (channel === 'opacity' && (typeof value !== 'number' || value < 0 || value > 1)) {
            issues.push({ path, message: 'Opacity value must be a number between 0 and 1.' });
        }
        if ((channel === 'shape' || channel === 'symbol') &&
            (typeof value !== 'string' || !POINT_SHAPES.has(value))) {
            issues.push({
                path,
                message: `${channel} value must be circle, square, diamond, triangle, or cross.`,
            });
        }
        if (channel === 'icon' &&
            (typeof value !== 'string' ||
                value.trim() === '' ||
                value.length > 32 ||
                /[\u0000-\u001f\u007f]/u.test(value))) {
            issues.push({
                path,
                message: 'Icon value must be a safe non-empty string up to 32 characters.',
            });
        }
    }
    function validateEncoding(value, path, channel, issues) {
        if (typeof value === 'string') {
            if (value.trim() === '')
                issues.push({ path, message: 'Field name must not be empty.' });
            if (UNSAFE_FIELDS.has(value))
                issues.push({ path, message: `Unsafe field "${value}" is forbidden.` });
            return;
        }
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Encoding must be a field name or a closed encoding object.' });
            return;
        }
        validateUnknownKeys(value, ENCODING_KEYS, path, 'encoding', issues);
        const hasField = value.field !== undefined;
        const hasValue = value.value !== undefined;
        if (hasField === hasValue) {
            issues.push({ path, message: 'Encoding requires exactly one of field or value.' });
        }
        if (FIELD_ONLY_SEMANTIC_CHANNELS.has(channel) && !hasField) {
            issues.push({ path: `${path}.field`, message: `${channel} requires a field encoding.` });
        }
        if ((channel === 'x' || channel === 'y') && !hasField) {
            issues.push({ path: `${path}.field`, message: `${channel} encoding requires a field.` });
        }
        if (hasField) {
            if (typeof value.field !== 'string' || value.field.trim() === '')
                issues.push({ path: `${path}.field`, message: 'Field must not be empty.' });
            if (typeof value.field === 'string' && UNSAFE_FIELDS.has(value.field)) {
                issues.push({
                    path: `${path}.field`,
                    message: `Unsafe field "${value.field}" is forbidden.`,
                });
            }
        }
        if (hasValue)
            validateChannelValue(value.value, `${path}.value`, channel, issues);
        if (value.type !== undefined &&
            (typeof value.type !== 'string' || !FIELD_TYPES.has(value.type))) {
            issues.push({ path: `${path}.type`, message: 'Encoding type is not supported.' });
        }
        validateOptionalString(value.title, `${path}.title`, 'Encoding title', issues);
        validateScale(value.scale, `${path}.scale`, issues);
        if (value.scale !== undefined && RAW_ONLY_ENCODING_CHANNELS.has(channel)) {
            issues.push({
                path: `${path}.scale`,
                message: `${channel} uses its raw or closed built-in channel registry and does not accept a scale.`,
            });
        }
        const conditions = value.condition === undefined
            ? []
            : Array.isArray(value.condition)
                ? value.condition
                : [value.condition];
        if (hasValue &&
            value.scale !== undefined &&
            !conditions.some((condition) => isPlainObject(condition) && condition.field !== undefined)) {
            issues.push({
                path: `${path}.scale`,
                message: 'A literal value bypasses scales; remove scale or add a conditional field.',
            });
        }
        if (isPlainObject(value.scale) &&
            value.scale.range !== undefined &&
            (channel === 'x' || channel === 'x2' || channel === 'y' || channel === 'y2')) {
            issues.push({
                path: `${path}.scale.range`,
                message: 'Cartesian position ranges are owned by chart layout; use the standalone scale registry for explicit ranges.',
            });
        }
        if (isPlainObject(value.scale) &&
            value.scale.type === 'threshold' &&
            value.scale.domain === undefined &&
            !['x', 'x2', 'y', 'y2'].includes(channel)) {
            issues.push({
                path: `${path}.scale.domain`,
                message: 'Threshold channel scales require explicit thresholds in domain.',
            });
        }
        if (isPlainObject(value.scale) &&
            value.scale.type === undefined &&
            Array.isArray(value.scale.domain) &&
            (value.type === 'quantitative' || value.type === 'temporal') &&
            value.scale.domain.length !== 2) {
            issues.push({
                path: `${path}.scale.domain`,
                message: `An inferred ${value.type} scale domain must contain exactly 2 values.`,
            });
        }
        if (isPlainObject(value.scale) &&
            (value.type === 'nominal' || value.type === 'ordinal') &&
            typeof value.scale.type === 'string' &&
            !(channel === 'x' || channel === 'y'
                ? ['band', 'point'].includes(value.scale.type)
                : value.scale.type === 'ordinal')) {
            issues.push({
                path: `${path}.scale.type`,
                message: channel === 'x' || channel === 'y'
                    ? `${value.type} Cartesian encodings require band or point scales.`
                    : `${value.type} visual encodings require an ordinal scale.`,
            });
        }
        if (isPlainObject(value.scale) &&
            (value.type === 'quantitative' || value.type === 'temporal') &&
            typeof value.scale.type === 'string' &&
            ['band', 'point', 'ordinal'].includes(value.scale.type)) {
            issues.push({
                path: `${path}.scale.type`,
                message: `${value.type} encodings cannot use a categorical scale.`,
            });
        }
        if (isPlainObject(value.scale) &&
            (channel === 'x' || channel === 'y') &&
            typeof value.scale.type === 'string' &&
            ![
                'linear',
                'log',
                'symlog',
                'asinh',
                'pow',
                'sqrt',
                'time',
                'utc',
                'band',
                'point',
                'probability',
                'logit',
                'probit',
            ].includes(value.scale.type)) {
            issues.push({
                path: `${path}.scale.type`,
                message: `${value.scale.type} is not implemented as a Cartesian axis scale.`,
            });
        }
        if (isPlainObject(value.scale) &&
            ['color', 'fill', 'stroke'].includes(channel) &&
            (value.scale.type === 'band' || value.scale.type === 'point')) {
            issues.push({
                path: `${path}.scale.type`,
                message: `${value.scale.type} is a Cartesian categorical scale; color channels use ordinal.`,
            });
        }
        if (isPlainObject(value.scale) &&
            ['color', 'fill', 'stroke'].includes(channel) &&
            value.scale.outOfBounds === 'extrapolate' &&
            value.scale.type !== 'cyclic') {
            issues.push({
                path: `${path}.scale.outOfBounds`,
                message: 'Color scales do not extrapolate colors; use clamp, error, or unknown.',
            });
        }
        if (isPlainObject(value.scale) && Array.isArray(value.scale.range)) {
            if (['color', 'fill', 'stroke'].includes(channel) &&
                value.scale.range.some((entry) => typeof entry !== 'string')) {
                issues.push({
                    path: `${path}.scale.range`,
                    message: 'Color scale range must contain strings.',
                });
            }
            if (['size', 'radius', 'opacity', 'strokeWidth'].includes(channel) &&
                value.scale.range.some((entry) => typeof entry !== 'number')) {
                issues.push({
                    path: `${path}.scale.range`,
                    message: `${channel} scale range must contain numbers.`,
                });
            }
            if (['color', 'fill', 'stroke'].includes(channel) && typeof value.scale.type !== 'string') {
                issues.push({
                    path: `${path}.scale.type`,
                    message: 'An explicit color range requires an explicit color scale type.',
                });
            }
            if (['color', 'fill', 'stroke'].includes(channel) &&
                typeof value.scale.type === 'string' &&
                !['ordinal', 'sequential', 'diverging', 'cyclic'].includes(value.scale.type)) {
                issues.push({
                    path: `${path}.scale.range`,
                    message: 'Transformed numeric color scales use the active theme palette; explicit color ranges require sequential, diverging, cyclic, or ordinal.',
                });
            }
            if (['size', 'radius', 'opacity', 'strokeWidth'].includes(channel) &&
                (value.scale.type === undefined ||
                    (typeof value.scale.type === 'string' &&
                        !['ordinal', 'quantile', 'quantize', 'threshold'].includes(value.scale.type))) &&
                (value.type === 'quantitative' || value.type === 'temporal') &&
                value.scale.range.length !== 2) {
                issues.push({
                    path: `${path}.scale.range`,
                    message: `${channel} continuous scale range must contain exactly 2 numbers.`,
                });
            }
        }
        if (isPlainObject(value.scale) && (channel === 'x2' || channel === 'y2')) {
            issues.push({
                path: `${path}.scale`,
                message: `${channel} shares the primary position scale and cannot declare a separate scale.`,
            });
        }
        if (isPlainObject(value.scale) &&
            typeof value.scale.type === 'string' &&
            ['sequential', 'diverging', 'cyclic'].includes(value.scale.type) &&
            !['color', 'fill', 'stroke'].includes(channel)) {
            issues.push({
                path: `${path}.scale.type`,
                message: `${value.scale.type} is a color-only scale.`,
            });
        }
        const axisId = typeof value.axisId === 'string' && isSafeAxisId(value.axisId)
            ? value.axisId
            : channel === 'x' || channel === 'y'
                ? channel
                : undefined;
        if (value.axisId !== undefined &&
            ((channel !== 'x' && channel !== 'y') ||
                typeof value.axisId !== 'string' ||
                !isSafeAxisId(value.axisId))) {
            issues.push({
                path: `${path}.axisId`,
                message: channel === 'x' || channel === 'y'
                    ? `${channel}-encoding axisId must use the safe named-axis grammar.`
                    : `axisId is not valid for the ${channel} channel.`,
            });
        }
        if (value.axis !== undefined) {
            if ((channel === 'x' || channel === 'y') && axisId !== undefined) {
                validateAxis(value.axis, `${path}.axis`, axisId, channel, issues);
            }
            else {
                issues.push({
                    path: `${path}.axis`,
                    message: `Axis is not valid for the ${channel} channel.`,
                });
            }
        }
        if (value.condition !== undefined) {
            if (channel === 'x' || channel === 'y' || channel === 'x2' || channel === 'y2') {
                issues.push({
                    path: `${path}.condition`,
                    message: 'Conditional Cartesian positions are not implemented; precompute the position field with calculate instead.',
                });
            }
            if (conditions.length === 0 || conditions.length > 32) {
                issues.push({
                    path: `${path}.condition`,
                    message: 'Encoding condition requires 1..32 entries.',
                });
            }
            else {
                conditions.forEach((condition, index) => validateEncodingCondition(condition, `${path}.condition[${index}]`, channel, issues));
            }
        }
    }
    function markType$1(value) {
        if (typeof value === 'string')
            return value;
        return isPlainObject(value) && typeof value.type === 'string' ? value.type : undefined;
    }
    function validateMarkEncodingCompatibility(mark, encoding, path, issues) {
        if (!isPlainObject(encoding))
            return;
        const type = markType$1(mark);
        const markObject = isPlainObject(mark) ? mark : null;
        const markOptions = markObject !== null && isPlainObject(markObject.options) ? markObject.options : null;
        const seriesLayout = type === 'theme-river' || markOptions?.stack !== undefined;
        const shared = new Set(['x', 'y']);
        const allowed = new Set(shared);
        if (type === 'point' || type === 'circle') {
            for (const channel of [
                'color',
                'fill',
                'stroke',
                'size',
                'radius',
                'shape',
                'symbol',
                'icon',
                'opacity',
                'strokeWidth',
                'strokeDash',
                'text',
                'order',
                'tooltip',
            ])
                allowed.add(channel);
        }
        else if (type === 'bar') {
            for (const channel of [
                'x2',
                'y2',
                'color',
                'fill',
                'stroke',
                'opacity',
                'strokeWidth',
                'strokeDash',
                'order',
                'tooltip',
            ])
                allowed.add(channel);
        }
        else if (type === 'line') {
            for (const channel of [
                'color',
                'stroke',
                'opacity',
                'strokeWidth',
                'strokeDash',
                'order',
                'detail',
            ])
                allowed.add(channel);
            if (isPlainObject(mark) && mark.point === true) {
                for (const channel of ['fill', 'size', 'radius', 'tooltip'])
                    allowed.add(channel);
            }
        }
        else if (type === 'area' || type === 'stepped-area' || type === 'theme-river') {
            for (const channel of [
                'y2',
                'color',
                'fill',
                'stroke',
                'opacity',
                'strokeWidth',
                'strokeDash',
                'order',
                'detail',
            ])
                allowed.add(channel);
            if (isPlainObject(mark) && mark.point === true) {
                for (const channel of ['size', 'radius', 'tooltip'])
                    allowed.add(channel);
            }
        }
        else if (type === 'heatmap') {
            for (const channel of [
                'x2',
                'y2',
                'color',
                'fill',
                'stroke',
                'opacity',
                'strokeWidth',
                'strokeDash',
                'text',
                'order',
                'tooltip',
            ])
                allowed.add(channel);
        }
        else if (type !== undefined && GEOGRAPHIC_POSITION_MARKS.has(type)) {
            allowed.add('longitude');
            allowed.add('latitude');
        }
        else if (type !== undefined && TRADING_CHANNEL_MARKS.has(type)) {
            for (const channel of ['open', 'high', 'low', 'close', 'volume'])
                allowed.add(channel);
        }
        else if (type === 'polar') {
            for (const channel of ['angle', 'theta', 'radius'])
                allowed.add(channel);
        }
        else if (type === 'pie' || type === 'variable-pie') {
            for (const channel of ['angle', 'theta'])
                allowed.add(channel);
        }
        else if (type === 'indicator') {
            for (const channel of ['open', 'high', 'low', 'close', 'volume'])
                allowed.add(channel);
        }
        else if (type === 'renko' || type === 'point-figure') {
            allowed.add('close');
        }
        else if (type === 'volume-profile') {
            allowed.add('volume');
        }
        const aliases = [
            ...(type !== undefined && GEOGRAPHIC_POSITION_MARKS.has(type)
                ? [
                    ['x', 'longitude'],
                    ['y', 'latitude'],
                ]
                : []),
            ...(type === 'polar'
                ? [
                    ['x', 'theta'],
                    ['x', 'angle'],
                    ['y', 'radius'],
                ]
                : []),
            ...(type === 'pie' || type === 'variable-pie'
                ? [
                    ['y', 'theta'],
                    ['y', 'angle'],
                ]
                : []),
            ...(type !== undefined &&
                (TRADING_CHANNEL_MARKS.has(type) || ['renko', 'point-figure', 'indicator'].includes(type))
                ? [['y', 'close']]
                : []),
        ];
        for (const [position, semantic] of aliases) {
            if (encoding[position] !== undefined && encoding[semantic] !== undefined) {
                issues.push({
                    path: `${path}.${semantic}`,
                    message: `Use either ${position} or its ${semantic} semantic alias, not both.`,
                });
            }
        }
        if (encoding.theta !== undefined && encoding.angle !== undefined) {
            issues.push({ path: `${path}.angle`, message: 'Use either theta or angle, not both.' });
        }
        if (type === 'point' || type === 'circle') {
            const glyphs = ['shape', 'symbol', 'icon'].filter((channel) => encoding[channel] !== undefined);
            if (glyphs.length > 1) {
                issues.push({
                    path,
                    message: `Use one point glyph channel; received ${glyphs.join(', ')}.`,
                });
            }
        }
        for (const channel of Object.keys(encoding)) {
            if (seriesLayout &&
                ((type === 'bar' && (channel === 'x2' || channel === 'y2')) ||
                    ((type === 'area' || type === 'theme-river') && channel === 'y2'))) {
                issues.push({
                    path: `${path}.${channel}`,
                    message: `The ${channel} range channel conflicts with series-stack boundaries; remove the range channel or the stack layout.`,
                });
                continue;
            }
            if (!allowed.has(channel)) {
                issues.push({
                    path: `${path}.${channel}`,
                    message: `The ${channel} channel is not implemented for mark "${type ?? 'unknown'}".`,
                });
            }
        }
    }
    function validateEncodingMap(value, path, issues, mark) {
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Encoding map must be an object.' });
            return;
        }
        validateUnknownKeys(value, ENCODING_CHANNELS, path, 'encoding channel', issues);
        const type = markType$1(mark);
        const derivedX = type !== undefined && GEOGRAPHIC_POSITION_MARKS.has(type)
            ? value.longitude
            : type === 'polar'
                ? (value.theta ?? value.angle)
                : undefined;
        const derivedY = type !== undefined && GEOGRAPHIC_POSITION_MARKS.has(type)
            ? value.latitude
            : type === 'polar'
                ? value.radius
                : type === 'pie' || type === 'variable-pie'
                    ? (value.theta ?? value.angle)
                    : type !== undefined &&
                        (TRADING_CHANNEL_MARKS.has(type) ||
                            ['renko', 'point-figure', 'indicator'].includes(type))
                        ? value.close
                        : undefined;
        if (value.x === undefined && derivedX === undefined)
            issues.push({ path: `${path}.x`, message: 'Encoding map requires x.' });
        if (value.y === undefined && derivedY === undefined)
            issues.push({ path: `${path}.y`, message: 'Encoding map requires y.' });
        for (const channel of ENCODING_CHANNELS) {
            if (value[channel] !== undefined) {
                validateEncoding(value[channel], `${path}.${channel}`, channel, issues);
            }
        }
    }
    function validateLayerClip(value, path, issues) {
        if (value === undefined || typeof value === 'boolean')
            return;
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Layer clip must be a boolean or clip object.' });
            return;
        }
        if (value.type === 'plot') {
            validateUnknownKeys(value, new Set(['type', 'x', 'y', 'width', 'height']), path, 'plot clip', issues);
            for (const key of ['x', 'y', 'width', 'height']) {
                if (value[key] === undefined) {
                    issues.push({ path: `${path}.${key}`, message: `Plot clip requires ${key}.` });
                }
                else {
                    validateFiniteNumber(value[key], `${path}.${key}`, `Plot clip ${key}`, issues, {
                        min: 0,
                        max: 1,
                    });
                }
            }
            if (typeof value.x === 'number' &&
                typeof value.width === 'number' &&
                value.x + value.width > 1) {
                issues.push({ path, message: 'Plot clip x + width must not exceed 1.' });
            }
            if (typeof value.y === 'number' &&
                typeof value.height === 'number' &&
                value.y + value.height > 1) {
                issues.push({ path, message: 'Plot clip y + height must not exceed 1.' });
            }
            return;
        }
        if (value.type === 'domain') {
            validateUnknownKeys(value, new Set(['type', 'x', 'y']), path, 'domain clip', issues);
            if (value.x === undefined && value.y === undefined) {
                issues.push({ path, message: 'Domain clip requires x, y, or both ranges.' });
            }
            for (const channel of ['x', 'y']) {
                const range = value[channel];
                if (range === undefined)
                    continue;
                if (!isPlainObject(range)) {
                    issues.push({
                        path: `${path}.${channel}`,
                        message: 'Domain clip range must be an object.',
                    });
                    continue;
                }
                validateUnknownKeys(range, new Set(['axis', 'from', 'to']), `${path}.${channel}`, 'domain clip range', issues);
                if (range.axis !== undefined && !isSafeAxisId(range.axis)) {
                    issues.push({
                        path: `${path}.${channel}.axis`,
                        message: 'Domain clip axis must use the safe named-axis grammar.',
                    });
                }
                for (const key of ['from', 'to']) {
                    if ((typeof range[key] !== 'number' || !Number.isFinite(range[key])) &&
                        typeof range[key] !== 'string') {
                        issues.push({
                            path: `${path}.${channel}.${key}`,
                            message: `Domain clip ${key} must be a finite number or string.`,
                        });
                    }
                }
            }
            return;
        }
        issues.push({ path: `${path}.type`, message: 'Layer clip type must be plot or domain.' });
    }
    function validateSeriesStack(markType, markFields, value, path, issues) {
        if (value === undefined)
            return;
        if (!SERIES_STACK_MARKS.has(markType)) {
            issues.push({
                path,
                message: 'Series stack layout is supported only by area, bar, and theme-river.',
            });
            return;
        }
        const object = isPlainObject(value) ? value : null;
        const mode = typeof value === 'string' ? value : (object?.mode ?? 'stacked');
        if (typeof mode !== 'string' || !seriesStackModes.includes(mode)) {
            issues.push({
                path: object === null ? path : `${path}.mode`,
                message: `Series stack mode must be one of: ${seriesStackModes.join(', ')}.`,
            });
        }
        if (object === null) {
            if (typeof value !== 'string') {
                issues.push({ path, message: 'Series stack must be a supported mode string or an object.' });
            }
        }
        else {
            validateUnknownKeys(object, new Set(['mode', 'offset', 'order', 'sort']), path, 'series stack', issues);
            if (object.offset !== undefined &&
                (typeof object.offset !== 'string' || !SERIES_STACK_OFFSETS.has(object.offset))) {
                issues.push({ path: `${path}.offset`, message: 'Series stack offset is not supported.' });
            }
            if (object.order !== undefined &&
                (typeof object.order !== 'string' || !SERIES_STACK_ORDERS.has(object.order))) {
                issues.push({ path: `${path}.order`, message: 'Series stack order is not supported.' });
            }
            if (object.sort !== undefined) {
                if (!Array.isArray(object.sort) || object.sort.length === 0 || object.sort.length > 16) {
                    issues.push({ path: `${path}.sort`, message: 'Series stack sort requires 1..16 fields.' });
                }
                else {
                    object.sort.forEach((entry, index) => {
                        const entryPath = `${path}.sort[${index}]`;
                        if (!isPlainObject(entry)) {
                            issues.push({ path: entryPath, message: 'Series stack sort entry must be an object.' });
                            return;
                        }
                        validateUnknownKeys(entry, new Set(['field', 'order']), entryPath, 'series stack sort', issues);
                        if (typeof entry.field !== 'string' ||
                            entry.field.trim() === '' ||
                            UNSAFE_FIELDS.has(entry.field)) {
                            issues.push({
                                path: `${entryPath}.field`,
                                message: 'Series stack sort field must be safe and non-empty.',
                            });
                        }
                        if (entry.order !== undefined &&
                            entry.order !== 'ascending' &&
                            entry.order !== 'descending') {
                            issues.push({
                                path: `${entryPath}.order`,
                                message: 'Sort order must be ascending or descending.',
                            });
                        }
                    });
                }
            }
            if (mode === 'grouped' && object.offset !== undefined) {
                issues.push({ path: `${path}.offset`, message: 'Grouped series do not use a stack offset.' });
            }
            if (mode === '100-percent' &&
                object.offset !== undefined &&
                !['normalize', 'expand'].includes(String(object.offset))) {
                issues.push({
                    path: `${path}.offset`,
                    message: '100-percent series require normalize or expand offset.',
                });
            }
            if (mode === 'diverging' && object.offset === 'wiggle') {
                issues.push({
                    path: `${path}.offset`,
                    message: 'Diverging series cannot use the non-negative wiggle offset.',
                });
            }
            if (mode === 'streamgraph' &&
                object.offset !== undefined &&
                !['wiggle', 'center', 'silhouette'].includes(String(object.offset))) {
                issues.push({
                    path: `${path}.offset`,
                    message: 'Streamgraph offset must be wiggle, center, or silhouette.',
                });
            }
        }
        const hasSeries = isPlainObject(markFields) &&
            (typeof markFields.series === 'string' ||
                (markType === 'theme-river' && typeof markFields.category === 'string'));
        if (!hasSeries) {
            issues.push({
                path: `${path.replace(/\.options\.stack$/, '.fields')}.series`,
                message: 'Series layout requires mark.fields.series.',
            });
        }
    }
    function validateIndicatorCalculation(options, path, issues) {
        if (options.calculate !== undefined && typeof options.calculate !== 'boolean') {
            issues.push({ path: `${path}.calculate`, message: 'Indicator calculate must be boolean.' });
        }
        if (options.kind !== undefined &&
            (typeof options.kind !== 'string' || options.kind.trim() === '')) {
            issues.push({ path: `${path}.kind`, message: 'Indicator kind must be a non-empty string.' });
        }
        const kind = typeof options.kind === 'string' ? options.kind : 'sma';
        const capability = resolveTechnicalIndicatorCapability(kind);
        if (options.calculate === true && capability === null) {
            issues.push({ path: `${path}.kind`, message: `Unknown calculated indicator "${kind}".` });
        }
        if (capability !== null && options.calculate === true) {
            const allParameters = new Set(technicalIndicatorCapabilities.flatMap(({ parameters }) => parameters.map(({ name }) => name)));
            for (const name of allParameters) {
                if (options[name] === undefined)
                    continue;
                const parameter = capability.parameters.find((candidate) => candidate.name === name);
                if (parameter === undefined) {
                    issues.push({
                        path: `${path}.${name}`,
                        message: `${name} is not a parameter of ${capability.id}.`,
                    });
                    continue;
                }
                validateFiniteNumber(options[name], `${path}.${name}`, `Indicator ${name}`, issues, {
                    integer: parameter.type === 'integer',
                    min: parameter.minimum,
                    max: parameter.maximum,
                });
            }
            const numeric = (name) => {
                if (typeof options[name] === 'number')
                    return options[name];
                return capability.parameters.find((parameter) => parameter.name === name)?.default;
            };
            if (numeric('fastPeriod') !== undefined &&
                numeric('slowPeriod') !== undefined &&
                numeric('fastPeriod') >= numeric('slowPeriod')) {
                issues.push({
                    path: `${path}.fastPeriod`,
                    message: 'Indicator fastPeriod must be smaller than slowPeriod.',
                });
            }
            if (numeric('conversionPeriod') !== undefined &&
                numeric('basePeriod') !== undefined &&
                numeric('spanPeriod') !== undefined &&
                (numeric('conversionPeriod') > numeric('basePeriod') ||
                    numeric('basePeriod') > numeric('spanPeriod'))) {
                issues.push({
                    path: `${path}.conversionPeriod`,
                    message: 'Ichimoku periods must satisfy conversionPeriod <= basePeriod <= spanPeriod.',
                });
            }
            if (numeric('acceleration') !== undefined &&
                numeric('maximumAcceleration') !== undefined &&
                numeric('acceleration') > numeric('maximumAcceleration')) {
                issues.push({
                    path: `${path}.acceleration`,
                    message: 'Indicator acceleration must be no greater than maximumAcceleration.',
                });
            }
        }
        if (options.session !== undefined) {
            if (!isPlainObject(options.session)) {
                issues.push({ path: `${path}.session`, message: 'Indicator session must be an object.' });
            }
            else {
                validateUnknownKeys(options.session, new Set(['mode', 'field', 'timeField', 'gapMs', 'reset']), `${path}.session`, 'indicator session', issues);
                const mode = options.session.mode ?? 'none';
                if (!['none', 'field', 'utc-day', 'gap'].includes(String(mode))) {
                    issues.push({
                        path: `${path}.session.mode`,
                        message: 'Indicator session mode must be none, field, utc-day, or gap.',
                    });
                }
                if (options.session.reset !== undefined &&
                    !['hard', 'carry'].includes(String(options.session.reset))) {
                    issues.push({
                        path: `${path}.session.reset`,
                        message: 'Indicator session reset must be hard or carry.',
                    });
                }
                if (mode === 'field') {
                    validateOptionalString(options.session.field, `${path}.session.field`, 'Indicator session field', issues, false);
                    if (options.session.field === undefined) {
                        issues.push({
                            path: `${path}.session.field`,
                            message: 'Indicator field session mode requires field.',
                        });
                    }
                    if (typeof options.session.field === 'string' && UNSAFE_FIELDS.has(options.session.field)) {
                        issues.push({
                            path: `${path}.session.field`,
                            message: 'Indicator session field is unsafe.',
                        });
                    }
                }
                if (options.session.timeField !== undefined) {
                    validateOptionalString(options.session.timeField, `${path}.session.timeField`, 'Indicator session time field', issues, false);
                    if (typeof options.session.timeField === 'string' &&
                        UNSAFE_FIELDS.has(options.session.timeField)) {
                        issues.push({
                            path: `${path}.session.timeField`,
                            message: 'Indicator session time field is unsafe.',
                        });
                    }
                }
                if (mode === 'gap') {
                    validateFiniteNumber(options.session.gapMs, `${path}.session.gapMs`, 'Indicator session gap', issues, { min: Number.MIN_VALUE });
                }
            }
        }
    }
    function validateMark(value, path, issues) {
        if (typeof value === 'string') {
            if (value.trim() === '')
                issues.push({ path, message: 'Mark type must not be empty.' });
            return;
        }
        if (!isPlainObject(value) || typeof value.type !== 'string' || value.type.trim() === '') {
            issues.push({ path, message: 'Mark must be a type string or an object with a type.' });
            return;
        }
        if (value.fields !== undefined) {
            if (!isPlainObject(value.fields)) {
                issues.push({ path: `${path}.fields`, message: 'Mark fields must be an object.' });
            }
            else {
                for (const [name, field] of Object.entries(value.fields)) {
                    if (UNSAFE_FIELDS.has(name)) {
                        issues.push({
                            path: `${path}.fields.${name}`,
                            message: `Unsafe key "${name}" is forbidden.`,
                        });
                    }
                    if (typeof field !== 'string' || field.trim() === '') {
                        issues.push({
                            path: `${path}.fields.${name}`,
                            message: 'Named mark fields must be non-empty strings.',
                        });
                    }
                    else if (UNSAFE_FIELDS.has(field)) {
                        issues.push({
                            path: `${path}.fields.${name}`,
                            message: `Unsafe field "${field}" is forbidden.`,
                        });
                    }
                }
            }
        }
        if (value.options !== undefined) {
            if (!isPlainObject(value.options)) {
                issues.push({ path: `${path}.options`, message: 'Mark options must be a JSON object.' });
            }
            else {
                validateSeriesStack(value.type, value.fields, value.options.stack, `${path}.options.stack`, issues);
                if (value.type === 'indicator') {
                    validateIndicatorCalculation(value.options, `${path}.options`, issues);
                }
            }
            if (isPlainObject(value.options) && CURVE_MARKS.has(value.type)) {
                if (value.options.curve !== undefined &&
                    (typeof value.options.curve !== 'string' || !CURVE_NAMES.has(value.options.curve))) {
                    issues.push({
                        path: `${path}.options.curve`,
                        message: `Curve must be one of: ${curveNames.join(', ')}.`,
                    });
                }
                if (value.options.missing !== undefined &&
                    (typeof value.options.missing !== 'string' ||
                        !MISSING_VALUE_POLICIES.has(value.options.missing))) {
                    issues.push({
                        path: `${path}.options.missing`,
                        message: 'Missing-value policy must be "gap", "zero", or "connect".',
                    });
                }
                if (value.options.tension !== undefined) {
                    validateFiniteNumber(value.options.tension, `${path}.options.tension`, 'Cardinal curve tension', issues, { min: 0, max: 1 });
                }
                if (value.options.curveSamples !== undefined) {
                    validateFiniteNumber(value.options.curveSamples, `${path}.options.curveSamples`, 'Curve samples', issues, { integer: true, min: 1, max: 64 });
                }
            }
        }
    }
    function validateTooltipField(value, path, issues) {
        if (typeof value === 'string') {
            if (value.trim() === '')
                issues.push({ path, message: 'Tooltip field must not be empty.' });
            if (UNSAFE_FIELDS.has(value)) {
                issues.push({ path, message: `Unsafe field "${value}" is forbidden.` });
            }
            return;
        }
        if (!isPlainObject(value) || typeof value.field !== 'string' || value.field.trim() === '') {
            issues.push({ path, message: 'Tooltip field must be a field name or an object with a field.' });
            return;
        }
        for (const key of Object.keys(value)) {
            if (!TOOLTIP_FIELD_KEYS.has(key)) {
                issues.push({ path: `${path}.${key}`, message: `Unknown tooltip field property "${key}".` });
            }
        }
        if (UNSAFE_FIELDS.has(value.field)) {
            issues.push({ path: `${path}.field`, message: `Unsafe field "${value.field}" is forbidden.` });
        }
        if (value.label !== undefined && typeof value.label !== 'string') {
            issues.push({ path: `${path}.label`, message: 'Tooltip label must be a string.' });
        }
        if (value.format !== undefined &&
            (typeof value.format !== 'string' || !TOOLTIP_FORMATS.has(value.format))) {
            issues.push({ path: `${path}.format`, message: 'Tooltip format is not supported.' });
        }
        if (value.fractionDigits !== undefined &&
            (typeof value.fractionDigits !== 'number' ||
                !Number.isInteger(value.fractionDigits) ||
                value.fractionDigits < 0 ||
                value.fractionDigits > 6)) {
            issues.push({
                path: `${path}.fractionDigits`,
                message: 'Tooltip fractionDigits must be an integer from 0 to 6.',
            });
        }
        for (const key of ['prefix', 'suffix']) {
            if (value[key] !== undefined && typeof value[key] !== 'string') {
                issues.push({ path: `${path}.${key}`, message: `Tooltip ${key} must be a string.` });
            }
        }
    }
    function validateInteraction(value, path, issues) {
        if (value === undefined)
            return;
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Interaction must be an object.' });
            return;
        }
        validateUnknownKeys(value, INTERACTION_KEYS, path, 'interaction', issues);
        for (const key of ['hover', 'click']) {
            if (value[key] !== undefined && typeof value[key] !== 'boolean') {
                issues.push({ path: `${path}.${key}`, message: `Interaction ${key} must be a boolean.` });
            }
        }
        const tooltip = value.tooltip;
        if (tooltip !== undefined && typeof tooltip !== 'boolean') {
            if (!isPlainObject(tooltip)) {
                issues.push({ path: `${path}.tooltip`, message: 'Tooltip must be a boolean or an object.' });
            }
            else {
                for (const key of Object.keys(tooltip)) {
                    if (!TOOLTIP_KEYS.has(key)) {
                        issues.push({
                            path: `${path}.tooltip.${key}`,
                            message: `Unknown tooltip property "${key}".`,
                        });
                    }
                }
                if (tooltip.trigger !== undefined &&
                    (typeof tooltip.trigger !== 'string' || !['mark', 'axis'].includes(tooltip.trigger))) {
                    issues.push({
                        path: `${path}.tooltip.trigger`,
                        message: 'Tooltip trigger must be "mark" or "axis".',
                    });
                }
                if (tooltip.axis !== undefined && !isSafeAxisId(tooltip.axis)) {
                    issues.push({
                        path: `${path}.tooltip.axis`,
                        message: 'Tooltip axis must use the safe named-axis grammar.',
                    });
                }
                const trigger = tooltip.trigger ?? 'mark';
                if (trigger === 'axis' && tooltip.axis === undefined) {
                    issues.push({
                        path: `${path}.tooltip.axis`,
                        message: 'Tooltip axis is required when trigger is "axis".',
                    });
                }
                if (trigger !== 'axis' && tooltip.axis !== undefined) {
                    issues.push({
                        path: `${path}.tooltip.axis`,
                        message: 'Tooltip axis is only valid when trigger is "axis".',
                    });
                }
                if (tooltip.title !== undefined && typeof tooltip.title !== 'string') {
                    issues.push({ path: `${path}.tooltip.title`, message: 'Tooltip title must be a string.' });
                }
                if (tooltip.fields !== undefined) {
                    if (!Array.isArray(tooltip.fields) ||
                        tooltip.fields.length === 0 ||
                        tooltip.fields.length > 12) {
                        issues.push({
                            path: `${path}.tooltip.fields`,
                            message: 'Tooltip fields must contain between 1 and 12 entries.',
                        });
                    }
                    else {
                        tooltip.fields.forEach((field, index) => validateTooltipField(field, `${path}.tooltip.fields[${index}]`, issues));
                    }
                }
            }
        }
        validateNavigation(value.navigation, `${path}.navigation`, issues);
        validateDomainNavigation(value.domainNavigation, `${path}.domainNavigation`, issues);
        validatePlayback(value.playback, `${path}.playback`, issues);
        validateControls(value.controls, `${path}.controls`, issues);
        validateSelection(value.selection, `${path}.selection`, issues);
        const inspectionEnabled = value.navigation !== undefined && value.navigation !== false;
        const domainEnabled = value.domainNavigation !== undefined && value.domainNavigation !== false;
        if (inspectionEnabled && domainEnabled) {
            issues.push({
                path: `${path}.domainNavigation`,
                message: 'Domain navigation cannot be combined with inspection navigation; choose one coordinate model.',
            });
        }
        const selection = isPlainObject(value.selection) ? value.selection : undefined;
        const kind = selection?.kind ?? 'point';
        const inspectionGesture = value.navigation === true ||
            (isPlainObject(value.navigation) &&
                (value.navigation.drag !== false || value.navigation.pinch !== false));
        const domainDrag = value.domainNavigation === true ||
            (isPlainObject(value.domainNavigation) && value.domainNavigation.drag !== false);
        if (kind !== 'point' && (inspectionGesture || domainDrag)) {
            issues.push({
                path: `${path}.selection.kind`,
                message: 'Interval, rectangle, axis, and lasso pointer gestures require navigation.drag=false, navigation.pinch=false, and domainNavigation.drag=false to avoid an ambiguous gesture.',
            });
        }
    }
    function validateAccessibility(value, path, issues) {
        if (value === undefined)
            return;
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Accessibility must be an object.' });
            return;
        }
        validateUnknownKeys(value, ACCESSIBILITY_KEYS, path, 'accessibility', issues);
        validateOptionalString(value.label, `${path}.label`, 'Accessibility label', issues, false);
        validateOptionalString(value.description, `${path}.description`, 'Accessibility description', issues, false);
        validateOptionalString(value.summary, `${path}.summary`, 'Accessibility summary', issues, false);
        validateOptionalBoolean(value.navigation, `${path}.navigation`, 'Accessibility navigation', issues);
        if (value.explorer !== undefined && typeof value.explorer !== 'boolean') {
            if (!isPlainObject(value.explorer)) {
                issues.push({
                    path: `${path}.explorer`,
                    message: 'Accessibility explorer must be a boolean or object.',
                });
            }
            else {
                validateUnknownKeys(value.explorer, ACCESSIBILITY_EXPLORER_KEYS, `${path}.explorer`, 'accessibility explorer', issues);
                if (value.explorer.windowRows !== undefined) {
                    validateFiniteNumber(value.explorer.windowRows, `${path}.explorer.windowRows`, 'Explorer windowRows', issues, { integer: true, min: 1, max: 5_000 });
                }
                if (value.explorer.overscanRows !== undefined) {
                    validateFiniteNumber(value.explorer.overscanRows, `${path}.explorer.overscanRows`, 'Explorer overscanRows', issues, { integer: true, min: 0, max: 100 });
                }
                if (value.explorer.rowHeight !== undefined) {
                    validateFiniteNumber(value.explorer.rowHeight, `${path}.explorer.rowHeight`, 'Explorer rowHeight', issues, { integer: true, min: 1, max: 256 });
                }
            }
        }
        if (value.linkedFocus !== undefined) {
            if (!isPlainObject(value.linkedFocus)) {
                issues.push({
                    path: `${path}.linkedFocus`,
                    message: 'Accessibility linkedFocus must be an object.',
                });
            }
            else {
                validateUnknownKeys(value.linkedFocus, ACCESSIBILITY_LINKED_FOCUS_KEYS, `${path}.linkedFocus`, 'accessibility linkedFocus', issues);
                if (value.linkedFocus.group === undefined) {
                    issues.push({
                        path: `${path}.linkedFocus.group`,
                        message: 'Linked focus group is required.',
                    });
                }
                else {
                    validateOptionalString(value.linkedFocus.group, `${path}.linkedFocus.group`, 'Linked focus group', issues, false);
                    if (typeof value.linkedFocus.group === 'string' &&
                        !/^[a-zA-Z0-9][a-zA-Z0-9_.:-]{0,95}$/.test(value.linkedFocus.group)) {
                        issues.push({
                            path: `${path}.linkedFocus.group`,
                            message: 'Linked focus group must contain 1 to 96 portable identity characters.',
                        });
                    }
                }
                if (value.linkedFocus.key === undefined) {
                    issues.push({
                        path: `${path}.linkedFocus.key`,
                        message: 'Linked focus key is required.',
                    });
                }
                else {
                    validateOptionalString(value.linkedFocus.key, `${path}.linkedFocus.key`, 'Linked focus key', issues, false);
                    if (typeof value.linkedFocus.key === 'string' &&
                        (value.linkedFocus.key.length > 128 || UNSAFE_FIELDS.has(value.linkedFocus.key))) {
                        issues.push({
                            path: `${path}.linkedFocus.key`,
                            message: 'Linked focus key must be safe and no longer than 128 characters.',
                        });
                    }
                }
            }
        }
        if (value.table !== undefined &&
            typeof value.table !== 'boolean' &&
            (typeof value.table !== 'string' || !ACCESSIBILITY_TABLE_MODES.has(value.table))) {
            issues.push({
                path: `${path}.table`,
                message: 'Accessibility table must be a boolean, "hidden", or "visible".',
            });
        }
        if (value.maxRows !== undefined) {
            validateFiniteNumber(value.maxRows, `${path}.maxRows`, 'Accessibility maxRows', issues, {
                integer: true,
                min: 1,
                max: 5_000,
            });
        }
        if (value.live !== undefined && typeof value.live !== 'boolean') {
            if (!isPlainObject(value.live)) {
                issues.push({
                    path: `${path}.live`,
                    message: 'Accessibility live must be a boolean or object.',
                });
            }
            else {
                validateUnknownKeys(value.live, ACCESSIBILITY_LIVE_KEYS, `${path}.live`, 'accessibility live', issues);
                validateOptionalBoolean(value.live.enabled, `${path}.live.enabled`, 'Live announcements', issues);
                if (value.live.throttleMs !== undefined) {
                    validateFiniteNumber(value.live.throttleMs, `${path}.live.throttleMs`, 'Live announcement throttle', issues, { integer: true, min: 0, max: 10_000 });
                }
            }
        }
    }
    function validateHighlightStyle(value, path, issues) {
        for (const key of ['fill', 'stroke']) {
            validateOptionalString(value[key], `${path}.${key}`, `Highlight ${key}`, issues, false);
        }
        if (value.opacity !== undefined)
            validateFiniteNumber(value.opacity, `${path}.opacity`, 'Highlight opacity', issues, {
                min: 0,
                max: 1,
            });
        if (value.lineWidth !== undefined)
            validateFiniteNumber(value.lineWidth, `${path}.lineWidth`, 'Highlight lineWidth', issues, {
                min: 0,
                max: 64,
            });
        if (value.padding !== undefined)
            validateFiniteNumber(value.padding, `${path}.padding`, 'Highlight padding', issues, {
                min: 0,
                max: 256,
            });
        if (value.radius !== undefined)
            validateFiniteNumber(value.radius, `${path}.radius`, 'Highlight radius', issues, {
                min: 0,
                max: 256,
            });
        if (value.dash !== undefined) {
            if (!Array.isArray(value.dash) || value.dash.length > 16) {
                issues.push({
                    path: `${path}.dash`,
                    message: 'Highlight dash must contain at most 16 values.',
                });
            }
            else {
                value.dash.forEach((entry, index) => validateFiniteNumber(entry, `${path}.dash[${index}]`, 'Highlight dash value', issues, {
                    min: 0,
                    max: 256,
                }));
            }
        }
    }
    function validateSelection(value, path, issues) {
        if (value === undefined || typeof value === 'boolean')
            return;
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Selection must be a boolean or an object.' });
            return;
        }
        validateUnknownKeys(value, SELECTION_KEYS, path, 'selection', issues);
        if (value.kind !== undefined &&
            (typeof value.kind !== 'string' || !SELECTION_KINDS.has(value.kind))) {
            issues.push({
                path: `${path}.kind`,
                message: 'Selection kind must be "point", "interval", "rectangle", "axis", or "lasso".',
            });
        }
        if (value.combine !== undefined &&
            (typeof value.combine !== 'string' || !SELECTION_COMBINE.has(value.combine))) {
            issues.push({
                path: `${path}.combine`,
                message: 'Selection combine must be "union" or "intersection".',
            });
        }
        const kind = typeof value.kind === 'string' && SELECTION_KINDS.has(value.kind) ? value.kind : 'point';
        if (value.mode !== undefined &&
            (typeof value.mode !== 'string' || !SELECTION_MODES.has(value.mode))) {
            issues.push({
                path: `${path}.mode`,
                message: 'Selection mode must be "single" or "multiple".',
            });
        }
        validateOptionalBoolean(value.toggle, `${path}.toggle`, 'Selection toggle', issues);
        validateOptionalString(value.key, `${path}.key`, 'Selection key', issues, false);
        if (typeof value.key === 'string' && UNSAFE_FIELDS.has(value.key)) {
            issues.push({ path: `${path}.key`, message: `Unsafe field "${value.key}" is forbidden.` });
        }
        validateOptionalBoolean(value.clearOnBackground, `${path}.clearOnBackground`, 'Selection clearOnBackground', issues);
        validateOptionalBoolean(value.clearOnEscape, `${path}.clearOnEscape`, 'Selection clearOnEscape', issues);
        validateOptionalString(value.ariaLabel, `${path}.ariaLabel`, 'Selection ariaLabel', issues, false);
        if (value.axis !== undefined && !isSafeAxisId(value.axis)) {
            issues.push({
                path: `${path}.axis`,
                message: 'Selection axis must use the safe named-axis grammar.',
            });
        }
        if (kind === 'axis' && value.axis === undefined) {
            issues.push({ path: `${path}.axis`, message: 'Axis selection requires an explicit axis.' });
        }
        if (kind !== 'axis' && value.axis !== undefined) {
            issues.push({ path: `${path}.axis`, message: 'Selection axis is only valid for kind "axis".' });
        }
        if (value.xAxis !== undefined && !isSafeAxisId(value.xAxis)) {
            issues.push({
                path: `${path}.xAxis`,
                message: 'Selection xAxis must use the safe named-axis grammar.',
            });
        }
        if (value.yAxis !== undefined && !isSafeAxisId(value.yAxis)) {
            issues.push({
                path: `${path}.yAxis`,
                message: 'Selection yAxis must use the safe named-axis grammar.',
            });
        }
        if (kind === 'axis' && (value.xAxis !== undefined || value.yAxis !== undefined)) {
            issues.push({
                path,
                message: 'Axis selection uses axis; xAxis and yAxis are not accepted for this kind.',
            });
        }
        if (kind !== 'point' && (value.toggle !== undefined || value.key !== undefined)) {
            issues.push({
                path,
                message: 'Selection toggle and key are supported only by point/datum selection.',
            });
        }
        if (value.maxSelections !== undefined)
            validateFiniteNumber(value.maxSelections, `${path}.maxSelections`, 'Selection maxSelections', issues, { integer: true, min: 1, max: 64 });
        if (value.maxLassoPoints !== undefined)
            validateFiniteNumber(value.maxLassoPoints, `${path}.maxLassoPoints`, 'Selection maxLassoPoints', issues, { integer: true, min: 3, max: 512 });
        if (kind !== 'lasso' && value.maxLassoPoints !== undefined) {
            issues.push({
                path: `${path}.maxLassoPoints`,
                message: 'Selection maxLassoPoints is only valid for kind "lasso".',
            });
        }
        if (value.minPixelSpan !== undefined)
            validateFiniteNumber(value.minPixelSpan, `${path}.minPixelSpan`, 'Selection minPixelSpan', issues, { min: 0, max: 64 });
        validateOptionalBoolean(value.keyboard, `${path}.keyboard`, 'Selection keyboard', issues);
        if (value.keyboardStep !== undefined)
            validateFiniteNumber(value.keyboardStep, `${path}.keyboardStep`, 'Selection keyboardStep', issues, { min: 1, max: 64 });
        validateOptionalBoolean(value.filter, `${path}.filter`, 'Selection filter', issues);
        validateOptionalBoolean(value.linked, `${path}.linked`, 'Selection linked', issues);
        if (kind === 'point' && value.filter === true && value.key === undefined) {
            issues.push({
                path: `${path}.key`,
                message: 'Selection-driven point filtering requires a stable selection key.',
            });
        }
        if (kind === 'point' && value.linked === true && value.key === undefined) {
            issues.push({
                path: `${path}.key`,
                message: 'Linked point selection requires a stable selection key.',
            });
        }
        if (value.highlight !== undefined) {
            if (!isPlainObject(value.highlight)) {
                issues.push({ path: `${path}.highlight`, message: 'Selection highlight must be an object.' });
            }
            else {
                validateUnknownKeys(value.highlight, new Set(['fill', 'stroke', 'opacity', 'lineWidth', 'dash', 'padding', 'radius']), `${path}.highlight`, 'selection highlight', issues);
                validateHighlightStyle(value.highlight, `${path}.highlight`, issues);
            }
        }
    }
    function validateNavigation(value, path, issues) {
        if (value === undefined || typeof value === 'boolean')
            return;
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Navigation must be a boolean or an object.' });
            return;
        }
        validateUnknownKeys(value, NAVIGATION_KEYS, path, 'navigation', issues);
        if (value.minZoom !== undefined)
            validateFiniteNumber(value.minZoom, `${path}.minZoom`, 'Navigation minZoom', issues, {
                min: 1,
                max: 6,
            });
        if (value.maxZoom !== undefined)
            validateFiniteNumber(value.maxZoom, `${path}.maxZoom`, 'Navigation maxZoom', issues, {
                min: 1,
                max: 6,
            });
        if (typeof value.minZoom === 'number' &&
            typeof value.maxZoom === 'number' &&
            value.minZoom > value.maxZoom) {
            issues.push({ path: `${path}.maxZoom`, message: 'Navigation maxZoom must be >= minZoom.' });
        }
        if (value.wheel !== undefined &&
            (typeof value.wheel !== 'string' || !NAVIGATION_WHEEL_MODES.has(value.wheel))) {
            issues.push({
                path: `${path}.wheel`,
                message: 'Navigation wheel must be "off", "modifier", or "always".',
            });
        }
        validateOptionalBoolean(value.drag, `${path}.drag`, 'Navigation drag', issues);
        validateOptionalBoolean(value.pinch, `${path}.pinch`, 'Navigation pinch', issues);
        validateOptionalBoolean(value.keyboard, `${path}.keyboard`, 'Navigation keyboard', issues);
    }
    function validateDomainNavigation(value, path, issues) {
        if (value === undefined || typeof value === 'boolean')
            return;
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Domain navigation must be a boolean or an object.' });
            return;
        }
        validateUnknownKeys(value, DOMAIN_NAVIGATION_KEYS, path, 'domain navigation', issues);
        if (value.axes !== undefined) {
            if (!Array.isArray(value.axes) || value.axes.length === 0 || value.axes.length > 64) {
                issues.push({
                    path: `${path}.axes`,
                    message: 'Domain navigation axes must contain 1 to 64 axis IDs.',
                });
            }
            else {
                const axes = value.axes.filter((axis) => typeof axis === 'string');
                value.axes.forEach((axis, index) => {
                    if (!isSafeAxisId(axis)) {
                        issues.push({
                            path: `${path}.axes[${index}]`,
                            message: 'Domain navigation axis must use the safe named-axis grammar.',
                        });
                    }
                });
                if (new Set(axes).size !== axes.length) {
                    issues.push({ path: `${path}.axes`, message: 'Domain navigation axes must be unique.' });
                }
            }
        }
        if (value.maxZoom !== undefined)
            validateFiniteNumber(value.maxZoom, `${path}.maxZoom`, 'Domain navigation maxZoom', issues, {
                min: 1,
                max: 64,
            });
        if (value.wheel !== undefined &&
            (typeof value.wheel !== 'string' || !NAVIGATION_WHEEL_MODES.has(value.wheel))) {
            issues.push({
                path: `${path}.wheel`,
                message: 'Domain navigation wheel must be "off", "modifier", or "always".',
            });
        }
        validateOptionalBoolean(value.drag, `${path}.drag`, 'Domain navigation drag', issues);
        validateOptionalBoolean(value.keyboard, `${path}.keyboard`, 'Domain navigation keyboard', issues);
    }
    function validatePlayback(value, path, issues) {
        if (value === undefined || value === false)
            return;
        if (value === true) {
            issues.push({ path: `${path}.field`, message: 'Playback field is required.' });
            return;
        }
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Playback must be false or an object.' });
            return;
        }
        validateUnknownKeys(value, PLAYBACK_KEYS, path, 'playback', issues);
        validateOptionalString(value.field, `${path}.field`, 'Playback field', issues, false);
        if (value.field === undefined) {
            issues.push({ path: `${path}.field`, message: 'Playback field is required.' });
        }
        else if (typeof value.field === 'string' && UNSAFE_FIELDS.has(value.field)) {
            issues.push({ path: `${path}.field`, message: `Unsafe field "${value.field}" is forbidden.` });
        }
        validateOptionalString(value.key, `${path}.key`, 'Playback key', issues, false);
        if (typeof value.key === 'string' && UNSAFE_FIELDS.has(value.key)) {
            issues.push({ path: `${path}.key`, message: `Unsafe field "${value.key}" is forbidden.` });
        }
        validateOptionalString(value.layerId, `${path}.layerId`, 'Playback layerId', issues, false);
        if (value.mode !== undefined &&
            (typeof value.mode !== 'string' || !PLAYBACK_MODES.has(value.mode))) {
            issues.push({ path: `${path}.mode`, message: 'Playback mode is not supported.' });
        }
        if (value.interval !== undefined)
            validateFiniteNumber(value.interval, `${path}.interval`, 'Playback interval', issues, {
                min: 100,
                max: 60_000,
            });
        if (value.rate !== undefined)
            validateFiniteNumber(value.rate, `${path}.rate`, 'Playback rate', issues, {
                min: 0.1,
                max: 16,
            });
        if (value.windowSize !== undefined)
            validateFiniteNumber(value.windowSize, `${path}.windowSize`, 'Playback windowSize', issues, {
                min: 1,
                max: 10_000,
                integer: true,
            });
        validateOptionalBoolean(value.loop, `${path}.loop`, 'Playback loop', issues);
        if (value.direction !== undefined &&
            (typeof value.direction !== 'string' || !PLAYBACK_DIRECTIONS.has(value.direction))) {
            issues.push({
                path: `${path}.direction`,
                message: 'Playback direction must be "forward" or "reverse".',
            });
        }
        const namedFrameNames = new Set();
        if (value.namedFrames !== undefined) {
            if (!Array.isArray(value.namedFrames) || value.namedFrames.length > 10_000) {
                issues.push({
                    path: `${path}.namedFrames`,
                    message: 'Playback namedFrames must be an array with at most 10000 entries.',
                });
            }
            else {
                value.namedFrames.forEach((frame, index) => {
                    const framePath = `${path}.namedFrames[${index}]`;
                    if (!isPlainObject(frame)) {
                        issues.push({ path: framePath, message: 'Playback named frame must be an object.' });
                        return;
                    }
                    validateUnknownKeys(frame, PLAYBACK_NAMED_FRAME_KEYS, framePath, 'playback named frame', issues);
                    validateOptionalString(frame.name, `${framePath}.name`, 'Playback frame name', issues, false);
                    if (frame.name === undefined) {
                        issues.push({ path: `${framePath}.name`, message: 'Playback frame name is required.' });
                    }
                    else if (typeof frame.name === 'string') {
                        if (namedFrameNames.has(frame.name)) {
                            issues.push({
                                path: `${framePath}.name`,
                                message: 'Playback frame names must be unique.',
                            });
                        }
                        namedFrameNames.add(frame.name);
                    }
                    if (frame.value === undefined) {
                        issues.push({ path: `${framePath}.value`, message: 'Playback frame value is required.' });
                    }
                    else if (typeof frame.value !== 'string' &&
                        typeof frame.value !== 'boolean' &&
                        (typeof frame.value !== 'number' || !Number.isFinite(frame.value))) {
                        issues.push({
                            path: `${framePath}.value`,
                            message: 'Playback frame value must be a finite number, string, or boolean.',
                        });
                    }
                });
            }
        }
        if (value.range !== undefined) {
            if (!isPlainObject(value.range)) {
                issues.push({ path: `${path}.range`, message: 'Playback range must be an object.' });
            }
            else {
                validateUnknownKeys(value.range, PLAYBACK_RANGE_KEYS, `${path}.range`, 'playback range', issues);
                for (const bound of ['start', 'end']) {
                    const reference = value.range[bound];
                    if (reference === undefined)
                        continue;
                    if (typeof reference === 'number') {
                        validateFiniteNumber(reference, `${path}.range.${bound}`, `Playback range ${bound}`, issues, { min: 0, integer: true });
                    }
                    else if (typeof reference !== 'string' || reference.length === 0) {
                        issues.push({
                            path: `${path}.range.${bound}`,
                            message: 'Playback range bound must be a zero-based index or non-empty frame name.',
                        });
                    }
                    else if (!namedFrameNames.has(reference)) {
                        issues.push({
                            path: `${path}.range.${bound}`,
                            message: `Playback range references undeclared named frame "${reference}".`,
                        });
                    }
                }
                if (typeof value.range.start === 'number' &&
                    Number.isInteger(value.range.start) &&
                    typeof value.range.end === 'number' &&
                    Number.isInteger(value.range.end) &&
                    value.range.start > value.range.end) {
                    issues.push({
                        path: `${path}.range`,
                        message: 'Playback range start must not be greater than its end.',
                    });
                }
            }
        }
        validateOptionalBoolean(value.autoplay, `${path}.autoplay`, 'Playback autoplay', issues);
        if (value.transition !== undefined && value.transition !== false) {
            if (!isPlainObject(value.transition)) {
                issues.push({
                    path: `${path}.transition`,
                    message: 'Playback transition must be false or an object.',
                });
            }
            else {
                validateUnknownKeys(value.transition, PLAYBACK_TRANSITION_KEYS, `${path}.transition`, 'playback transition', issues);
                if (value.transition.duration !== undefined) {
                    validateFiniteNumber(value.transition.duration, `${path}.transition.duration`, 'Playback transition duration', issues, { min: 50, max: 60_000 });
                }
                if (value.transition.easing !== undefined &&
                    (typeof value.transition.easing !== 'string' ||
                        !PLAYBACK_TRANSITION_EASINGS.has(value.transition.easing))) {
                    issues.push({
                        path: `${path}.transition.easing`,
                        message: 'Playback transition easing must be "linear" or "ease-in-out".',
                    });
                }
            }
        }
        validateOptionalBoolean(value.filter, `${path}.filter`, 'Playback filter', issues);
    }
    function validateControls(value, path, issues) {
        if (value === undefined || typeof value === 'boolean')
            return;
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Controls must be a boolean or an object.' });
            return;
        }
        validateUnknownKeys(value, CONTROLS_KEYS, path, 'controls', issues);
        for (const key of ['zoom', 'reset', 'fullscreen', 'export', 'annotations', 'playback']) {
            validateOptionalBoolean(value[key], `${path}.${key}`, `Controls ${key}`, issues);
        }
        if (value.labels !== undefined) {
            if (!isPlainObject(value.labels)) {
                issues.push({ path: `${path}.labels`, message: 'Control labels must be an object.' });
            }
            else {
                validateUnknownKeys(value.labels, CONTROL_LABEL_KEYS, `${path}.labels`, 'control label', issues);
                for (const [key, label] of Object.entries(value.labels)) {
                    validateOptionalString(label, `${path}.labels.${key}`, 'Control label', issues, false);
                }
            }
        }
    }
    function validateLayer(layer, path, hasParentData, issues) {
        if (!isPlainObject(layer)) {
            issues.push({ path, message: 'Layer must be an object.' });
            return;
        }
        validateUnknownKeys(layer, LAYER_KEYS, path, 'layer', issues);
        validateOptionalString(layer.name, `${path}.name`, 'Layer name', issues, false);
        validateTransforms(layer.transform, `${path}.transform`, issues);
        if (layer.source !== undefined) {
            validateOptionalString(layer.source, `${path}.source`, 'Named data source', issues, false);
            issues.push({
                path: `${path}.source`,
                message: 'Named layer sources require an enclosing dataflow.',
            });
        }
        validateMark(layer.mark, `${path}.mark`, issues);
        validateLayerClip(layer.clip, `${path}.clip`, issues);
        if (layer.encoding !== undefined && (layer.x !== undefined || layer.y !== undefined)) {
            issues.push({
                path,
                message: 'Use either the legacy x/y facade or the canonical encoding map, not both.',
            });
        }
        if (layer.encoding !== undefined) {
            validateEncodingMap(layer.encoding, `${path}.encoding`, issues, layer.mark);
            validateMarkEncodingCompatibility(layer.mark, layer.encoding, `${path}.encoding`, issues);
        }
        else {
            validateEncoding(layer.x, `${path}.x`, 'x', issues);
            validateEncoding(layer.y, `${path}.y`, 'y', issues);
        }
        if (!hasParentData && layer.data === undefined && layer.source === undefined) {
            issues.push({
                path: `${path}.data`,
                message: 'Layer data is required when chart-level data is absent.',
            });
        }
    }
    function validateLegend(value, path, issues) {
        if (value === undefined || typeof value === 'boolean')
            return;
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Legend must be a boolean or an object.' });
            return;
        }
        validateUnknownKeys(value, LEGEND_KEYS, path, 'legend', issues);
        validateOptionalBoolean(value.visible, `${path}.visible`, 'Legend visibility', issues);
        validateOptionalBoolean(value.interactive, `${path}.interactive`, 'Legend interactive', issues);
        if (value.mode !== undefined &&
            (typeof value.mode !== 'string' || !LEGEND_MODES.has(value.mode))) {
            issues.push({ path: `${path}.mode`, message: 'Legend mode is not supported.' });
        }
        if (value.position !== undefined &&
            (typeof value.position !== 'string' || !LEGEND_POSITIONS.has(value.position))) {
            issues.push({ path: `${path}.position`, message: 'Legend position is not supported.' });
        }
        if (value.orientation !== undefined &&
            (typeof value.orientation !== 'string' || !LEGEND_ORIENTATIONS.has(value.orientation))) {
            issues.push({ path: `${path}.orientation`, message: 'Legend orientation is not supported.' });
        }
        for (const key of ['title', 'field', 'layerId']) {
            validateOptionalString(value[key], `${path}.${key}`, `Legend ${key}`, issues, false);
        }
        if (typeof value.field === 'string' && UNSAFE_FIELDS.has(value.field)) {
            issues.push({ path: `${path}.field`, message: `Unsafe field "${value.field}" is forbidden.` });
        }
        if (value.maxItems !== undefined)
            validateFiniteNumber(value.maxItems, `${path}.maxItems`, 'Legend maxItems', issues, {
                integer: true,
                min: 1,
                max: 200,
            });
        if (value.items !== undefined) {
            if (!Array.isArray(value.items) || value.items.length === 0 || value.items.length > 200) {
                issues.push({
                    path: `${path}.items`,
                    message: 'Legend items must contain between 1 and 200 entries.',
                });
            }
            else {
                value.items.forEach((item, index) => {
                    const itemPath = `${path}.items[${index}]`;
                    if (!isPlainObject(item)) {
                        issues.push({ path: itemPath, message: 'Legend item must be an object.' });
                        return;
                    }
                    validateUnknownKeys(item, LEGEND_ITEM_KEYS, itemPath, 'legend item', issues);
                    validateOptionalString(item.id, `${itemPath}.id`, 'Legend item id', issues, false);
                    validateOptionalString(item.label, `${itemPath}.label`, 'Legend item label', issues, false);
                    if (item.label === undefined) {
                        issues.push({ path: `${itemPath}.label`, message: 'Legend item label is required.' });
                    }
                    validateOptionalString(item.color, `${itemPath}.color`, 'Legend item color', issues, false);
                    validateOptionalString(item.layerId, `${itemPath}.layerId`, 'Legend item layerId', issues, false);
                    if (item.symbol !== undefined &&
                        (typeof item.symbol !== 'string' || !LEGEND_ITEM_SYMBOLS.has(item.symbol))) {
                        issues.push({
                            path: `${itemPath}.symbol`,
                            message: 'Legend item symbol is not supported.',
                        });
                    }
                    if (item.value !== undefined &&
                        item.value !== null &&
                        (!['string', 'number', 'boolean'].includes(typeof item.value) ||
                            (typeof item.value === 'number' && !Number.isFinite(item.value)))) {
                        issues.push({
                            path: `${itemPath}.value`,
                            message: 'Legend item value must be JSON scalar.',
                        });
                    }
                });
            }
        }
        if (value.labels !== undefined) {
            if (!isPlainObject(value.labels)) {
                issues.push({ path: `${path}.labels`, message: 'Legend labels must be an object.' });
            }
            else {
                validateUnknownKeys(value.labels, LEGEND_LABEL_KEYS, `${path}.labels`, 'legend label', issues);
                for (const [key, label] of Object.entries(value.labels)) {
                    validateOptionalString(label, `${path}.labels.${key}`, 'Legend label', issues, false);
                }
            }
        }
    }
    function validateAxisRange(value, path, channel, issues) {
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Axis range target must be an object.' });
            return;
        }
        validateUnknownKeys(value, AXIS_RANGE_KEYS, path, 'axis range target', issues);
        if (value.from === undefined ||
            !['number', 'string'].includes(typeof value.from) ||
            (typeof value.from === 'number' && !Number.isFinite(value.from))) {
            issues.push({ path: `${path}.from`, message: 'Axis range from must be a number or string.' });
        }
        if (value.to === undefined ||
            !['number', 'string'].includes(typeof value.to) ||
            (typeof value.to === 'number' && !Number.isFinite(value.to))) {
            issues.push({ path: `${path}.to`, message: 'Axis range to must be a number or string.' });
        }
        if (value.axis !== undefined && !isSafeAxisId(value.axis)) {
            issues.push({ path: `${path}.axis`, message: `Range ${channel} axis is not compatible.` });
        }
    }
    function validateDecorationTarget(value, path, issues) {
        if (!isPlainObject(value) || typeof value.type !== 'string') {
            issues.push({ path, message: 'Decoration target must be an object with a type.' });
            return;
        }
        validateUnknownKeys(value, DECORATION_TARGET_KEYS, path, 'decoration target', issues);
        if (value.type === 'datum') {
            validateUnknownKeys(value, new Set(['type', 'layerId', 'rowIndex', 'field', 'value', 'values']), path, 'datum target', issues);
            validateOptionalString(value.layerId, `${path}.layerId`, 'Datum target layerId', issues, false);
            if (value.rowIndex !== undefined) {
                const rows = Array.isArray(value.rowIndex) ? value.rowIndex : [value.rowIndex];
                if (rows.length === 0 || rows.length > 1000) {
                    issues.push({
                        path: `${path}.rowIndex`,
                        message: 'Datum rowIndex must select 1..1000 rows.',
                    });
                }
                rows.forEach((row, index) => validateFiniteNumber(row, Array.isArray(value.rowIndex) ? `${path}.rowIndex[${index}]` : `${path}.rowIndex`, 'Datum rowIndex', issues, { integer: true, min: 0 }));
                if (new Set(rows).size !== rows.length) {
                    issues.push({ path: `${path}.rowIndex`, message: 'Datum rowIndex values must be unique.' });
                }
            }
            validateOptionalString(value.field, `${path}.field`, 'Datum target field', issues, false);
            if (typeof value.field === 'string' && UNSAFE_FIELDS.has(value.field)) {
                issues.push({
                    path: `${path}.field`,
                    message: `Unsafe field "${value.field}" is forbidden.`,
                });
            }
            const hasValue = Object.prototype.hasOwnProperty.call(value, 'value');
            const hasValues = Object.prototype.hasOwnProperty.call(value, 'values');
            if (hasValue) {
                const valid = value.value === null ||
                    typeof value.value === 'string' ||
                    typeof value.value === 'boolean' ||
                    (typeof value.value === 'number' && Number.isFinite(value.value));
                if (!valid)
                    issues.push({
                        path: `${path}.value`,
                        message: 'Datum target value must be a JSON scalar.',
                    });
            }
            if (hasValues) {
                if (!Array.isArray(value.values) || value.values.length === 0 || value.values.length > 200) {
                    issues.push({
                        path: `${path}.values`,
                        message: 'Datum target values must contain 1..200 JSON scalars.',
                    });
                }
                else {
                    value.values.forEach((entry, index) => {
                        const valid = entry === null ||
                            typeof entry === 'string' ||
                            typeof entry === 'boolean' ||
                            (typeof entry === 'number' && Number.isFinite(entry));
                        if (!valid)
                            issues.push({
                                path: `${path}.values[${index}]`,
                                message: 'Datum target values must be JSON scalars.',
                            });
                    });
                    if (new Set(value.values.map((entry) => JSON.stringify(entry))).size !== value.values.length)
                        issues.push({ path: `${path}.values`, message: 'Datum target values must be unique.' });
                }
            }
            if (value.field === undefined && (value.value !== undefined || value.values !== undefined)) {
                issues.push({ path: `${path}.field`, message: 'Datum target field is required for values.' });
            }
            if (value.field !== undefined && hasValue === hasValues) {
                issues.push({
                    path,
                    message: 'Datum target field matching requires exactly one of value or values.',
                });
            }
            if (value.rowIndex === undefined && value.field === undefined) {
                issues.push({ path, message: 'Datum target requires rowIndex or field matching.' });
            }
            return;
        }
        if (value.type === 'layer') {
            validateUnknownKeys(value, new Set(['type', 'layerId']), path, 'layer target', issues);
            validateOptionalString(value.layerId, `${path}.layerId`, 'Layer target layerId', issues, false);
            if (value.layerId === undefined)
                issues.push({ path: `${path}.layerId`, message: 'Layer target layerId is required.' });
            return;
        }
        if (value.type === 'range') {
            validateUnknownKeys(value, new Set(['type', 'x', 'y']), path, 'range target', issues);
            if (value.x === undefined && value.y === undefined) {
                issues.push({ path, message: 'Range target requires x or y.' });
            }
            if (value.x !== undefined)
                validateAxisRange(value.x, `${path}.x`, 'x', issues);
            if (value.y !== undefined)
                validateAxisRange(value.y, `${path}.y`, 'y', issues);
            return;
        }
        if (value.type === 'plot') {
            validateUnknownKeys(value, new Set(['type', 'x', 'y', 'width', 'height']), path, 'plot target', issues);
            for (const key of ['x', 'y', 'width', 'height']) {
                if ((key === 'x' || key === 'y') && value[key] === undefined) {
                    issues.push({ path: `${path}.${key}`, message: `Plot target ${key} is required.` });
                }
                else if (value[key] !== undefined) {
                    validateFiniteNumber(value[key], `${path}.${key}`, `Plot target ${key}`, issues, {
                        min: 0,
                        max: 1,
                    });
                }
            }
            return;
        }
        issues.push({ path: `${path}.type`, message: 'Decoration target type is not supported.' });
    }
    function validateHighlights(value, path, issues) {
        if (value === undefined)
            return;
        if (!Array.isArray(value) || value.length > 256) {
            issues.push({ path, message: 'Highlights must be an array of at most 256 entries.' });
            return;
        }
        value.forEach((highlight, index) => {
            const itemPath = `${path}[${index}]`;
            if (!isPlainObject(highlight)) {
                issues.push({ path: itemPath, message: 'Highlight must be an object.' });
                return;
            }
            validateUnknownKeys(highlight, HIGHLIGHT_KEYS, itemPath, 'highlight', issues);
            validateOptionalString(highlight.id, `${itemPath}.id`, 'Highlight id', issues, false);
            validateDecorationTarget(highlight.target, `${itemPath}.target`, issues);
            validateHighlightStyle(highlight, itemPath, issues);
        });
    }
    function validateAnnotations(value, path, issues) {
        if (value === undefined)
            return;
        if (!Array.isArray(value) || value.length > 256) {
            issues.push({ path, message: 'Annotations must be an array of at most 256 entries.' });
            return;
        }
        value.forEach((annotation, index) => {
            const itemPath = `${path}[${index}]`;
            if (!isPlainObject(annotation)) {
                issues.push({ path: itemPath, message: 'Annotation must be an object.' });
                return;
            }
            validateUnknownKeys(annotation, ANNOTATION_KEYS, itemPath, 'annotation', issues);
            validateOptionalString(annotation.id, `${itemPath}.id`, 'Annotation id', issues, false);
            if (annotation.primitive !== undefined &&
                (typeof annotation.primitive !== 'string' || !ANNOTATION_PRIMITIVES.has(annotation.primitive))) {
                issues.push({
                    path: `${itemPath}.primitive`,
                    message: 'Annotation primitive is not supported.',
                });
            }
            validateOptionalString(annotation.text, `${itemPath}.text`, 'Annotation text', issues, false);
            if (annotation.text === undefined)
                issues.push({ path: `${itemPath}.text`, message: 'Annotation text is required.' });
            validateOptionalString(annotation.detail, `${itemPath}.detail`, 'Annotation detail', issues);
            validateDecorationTarget(annotation.target, `${itemPath}.target`, issues);
            if (isPlainObject(annotation.target) && typeof annotation.target.type === 'string') {
                const targetType = annotation.target.type;
                const primitive = annotation.primitive ?? 'callout';
                const supported = primitive === 'point'
                    ? targetType === 'datum' || targetType === 'plot'
                    : primitive === 'band'
                        ? targetType === 'layer' || targetType === 'range' || targetType === 'plot'
                        : targetType === 'datum' ||
                            targetType === 'layer' ||
                            targetType === 'range' ||
                            targetType === 'plot';
                if (!supported) {
                    issues.push({
                        path: `${itemPath}.target`,
                        message: `Annotation primitive "${String(primitive)}" does not support a "${targetType}" target.`,
                    });
                }
            }
            if (annotation.placement !== undefined &&
                (typeof annotation.placement !== 'string' || !ANNOTATION_PLACEMENTS.has(annotation.placement))) {
                issues.push({
                    path: `${itemPath}.placement`,
                    message: 'Annotation placement is not supported.',
                });
            }
            for (const key of ['offsetX', 'offsetY']) {
                if (annotation[key] !== undefined)
                    validateFiniteNumber(annotation[key], `${itemPath}.${key}`, `Annotation ${key}`, issues, {
                        min: -1e4,
                        max: 10_000,
                    });
            }
            if (annotation.connector !== undefined && typeof annotation.connector !== 'boolean') {
                if (!isPlainObject(annotation.connector)) {
                    issues.push({ path: `${itemPath}.connector`, message: 'Annotation connector is invalid.' });
                }
                else {
                    validateUnknownKeys(annotation.connector, CONNECTOR_KEYS, `${itemPath}.connector`, 'annotation connector', issues);
                    validateOptionalBoolean(annotation.connector.visible, `${itemPath}.connector.visible`, 'Annotation connector visibility', issues);
                    validateOptionalString(annotation.connector.color, `${itemPath}.connector.color`, 'Annotation connector color', issues, false);
                    validateHighlightStyle({
                        lineWidth: annotation.connector.width,
                        dash: annotation.connector.dash,
                    }, `${itemPath}.connector`, issues);
                }
            }
            if (annotation.style !== undefined) {
                if (!isPlainObject(annotation.style)) {
                    issues.push({ path: `${itemPath}.style`, message: 'Annotation style must be an object.' });
                }
                else {
                    validateUnknownKeys(annotation.style, ANNOTATION_STYLE_KEYS, `${itemPath}.style`, 'annotation style', issues);
                    for (const key of ['background', 'border', 'color']) {
                        validateOptionalString(annotation.style[key], `${itemPath}.style.${key}`, `Annotation style ${key}`, issues, false);
                    }
                    if (annotation.style.opacity !== undefined)
                        validateFiniteNumber(annotation.style.opacity, `${itemPath}.style.opacity`, 'Annotation style opacity', issues, { min: 0, max: 1 });
                    for (const key of ['fontSize', 'maxWidth', 'padding']) {
                        if (annotation.style[key] !== undefined)
                            validateFiniteNumber(annotation.style[key], `${itemPath}.style.${key}`, `Annotation style ${key}`, issues, { min: 1, max: 2000 });
                    }
                    if (annotation.style.align !== undefined &&
                        (typeof annotation.style.align !== 'string' ||
                            !ANNOTATION_TEXT_ALIGNS.has(annotation.style.align))) {
                        issues.push({
                            path: `${itemPath}.style.align`,
                            message: 'Annotation text alignment is not supported.',
                        });
                    }
                }
            }
        });
    }
    function validateMarkLabels(value, path, issues) {
        if (value === undefined || typeof value === 'boolean')
            return;
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Mark labels must be a boolean or an object.' });
            return;
        }
        validateUnknownKeys(value, MARK_LABEL_KEYS, path, 'mark label', issues);
        validateOptionalBoolean(value.visible, `${path}.visible`, 'Mark label visibility', issues);
        for (const field of ['field', 'key']) {
            validateOptionalString(value[field], `${path}.${field}`, `Mark label ${field}`, issues, false);
            if (typeof value[field] === 'string' && UNSAFE_FIELDS.has(value[field])) {
                issues.push({
                    path: `${path}.${field}`,
                    message: `Unsafe field "${value[field]}" is forbidden.`,
                });
            }
        }
        if (value.layerIds !== undefined) {
            if (!Array.isArray(value.layerIds) || value.layerIds.length > 64) {
                issues.push({
                    path: `${path}.layerIds`,
                    message: 'Mark label layerIds must contain at most 64 entries.',
                });
            }
            else {
                value.layerIds.forEach((layerId, index) => validateOptionalString(layerId, `${path}.layerIds[${index}]`, 'Mark label layerId', issues, false));
                if (new Set(value.layerIds).size !== value.layerIds.length) {
                    issues.push({ path: `${path}.layerIds`, message: 'Mark label layerIds must be unique.' });
                }
            }
        }
        if (value.placement !== undefined &&
            (typeof value.placement !== 'string' || !MARK_LABEL_PLACEMENTS.has(value.placement))) {
            issues.push({ path: `${path}.placement`, message: 'Mark label placement is not supported.' });
        }
        if (value.collision !== undefined &&
            (typeof value.collision !== 'string' || !MARK_LABEL_COLLISIONS.has(value.collision))) {
            issues.push({
                path: `${path}.collision`,
                message: 'Mark label collision policy is not supported.',
            });
        }
        if (value.offset !== undefined) {
            validateFiniteNumber(value.offset, `${path}.offset`, 'Mark label offset', issues, {
                min: 0,
                max: 256,
            });
        }
        if (value.maxLabels !== undefined) {
            validateFiniteNumber(value.maxLabels, `${path}.maxLabels`, 'Mark label limit', issues, {
                min: 1,
                max: 1000,
                integer: true,
            });
        }
        if (value.connector !== undefined && typeof value.connector !== 'boolean') {
            if (!isPlainObject(value.connector)) {
                issues.push({ path: `${path}.connector`, message: 'Mark label connector is invalid.' });
            }
            else {
                validateUnknownKeys(value.connector, CONNECTOR_KEYS, `${path}.connector`, 'mark label connector', issues);
                validateOptionalBoolean(value.connector.visible, `${path}.connector.visible`, 'Mark label connector visibility', issues);
                validateOptionalString(value.connector.color, `${path}.connector.color`, 'Mark label connector color', issues, false);
                validateHighlightStyle({ lineWidth: value.connector.width, dash: value.connector.dash }, `${path}.connector`, issues);
            }
        }
        if (value.positions !== undefined) {
            if (!Array.isArray(value.positions) || value.positions.length > 1000) {
                issues.push({
                    path: `${path}.positions`,
                    message: 'Mark label positions must be an array of at most 1000 entries.',
                });
            }
            else {
                const targets = new Set();
                value.positions.forEach((position, index) => {
                    const itemPath = `${path}.positions[${index}]`;
                    if (!isPlainObject(position)) {
                        issues.push({ path: itemPath, message: 'Mark label position must be an object.' });
                        return;
                    }
                    validateUnknownKeys(position, MARK_LABEL_POSITION_KEYS, itemPath, 'mark label position', issues);
                    validateDecorationTarget(position.target, `${itemPath}.target`, issues);
                    if (isPlainObject(position.target) && position.target.type !== 'datum') {
                        issues.push({
                            path: `${itemPath}.target.type`,
                            message: 'Mark label positions require datum targets.',
                        });
                    }
                    for (const offset of ['offsetX', 'offsetY']) {
                        if (position[offset] !== undefined) {
                            validateFiniteNumber(position[offset], `${itemPath}.${offset}`, `Mark label ${offset}`, issues, {
                                min: -1e4,
                                max: 10_000,
                            });
                        }
                    }
                    validateOptionalBoolean(position.hidden, `${itemPath}.hidden`, 'Mark label hidden state', issues);
                    if (isPlainObject(position.target)) {
                        const key = JSON.stringify(position.target);
                        if (targets.has(key)) {
                            issues.push({
                                path: `${itemPath}.target`,
                                message: 'Mark label position targets must be unique.',
                            });
                        }
                        targets.add(key);
                    }
                });
            }
        }
        if (value.style !== undefined) {
            if (!isPlainObject(value.style)) {
                issues.push({ path: `${path}.style`, message: 'Mark label style must be an object.' });
            }
            else {
                validateUnknownKeys(value.style, MARK_LABEL_STYLE_KEYS, `${path}.style`, 'mark label style', issues);
                for (const key of ['color', 'background', 'border']) {
                    validateOptionalString(value.style[key], `${path}.style.${key}`, `Mark label style ${key}`, issues, false);
                }
                if (value.style.opacity !== undefined) {
                    validateFiniteNumber(value.style.opacity, `${path}.style.opacity`, 'Mark label opacity', issues, { min: 0, max: 1 });
                }
                for (const [key, minimum, maximum] of [
                    ['fontSize', 1, 256],
                    ['fontWeight', 1, 1000],
                    ['maxWidth', 8, 2000],
                    ['padding', 0, 256],
                    ['radius', 0, 256],
                ]) {
                    if (value.style[key] !== undefined) {
                        validateFiniteNumber(value.style[key], `${path}.style.${key}`, `Mark label style ${key}`, issues, {
                            min: minimum,
                            max: maximum,
                        });
                    }
                }
            }
        }
        if (value.authoring !== undefined && typeof value.authoring !== 'boolean') {
            if (!isPlainObject(value.authoring)) {
                issues.push({
                    path: `${path}.authoring`,
                    message: 'Mark label authoring must be a boolean or an object.',
                });
            }
            else {
                validateUnknownKeys(value.authoring, MARK_LABEL_AUTHORING_KEYS, `${path}.authoring`, 'mark label authoring', issues);
                for (const key of ['pointer', 'keyboard']) {
                    validateOptionalBoolean(value.authoring[key], `${path}.authoring.${key}`, `Mark label authoring ${key}`, issues);
                }
                if (value.authoring.step !== undefined) {
                    validateFiniteNumber(value.authoring.step, `${path}.authoring.step`, 'Mark label keyboard step', issues, {
                        min: 0.1,
                        max: 256,
                    });
                }
                if (value.authoring.historyLimit !== undefined) {
                    validateFiniteNumber(value.authoring.historyLimit, `${path}.authoring.historyLimit`, 'Mark label history limit', issues, { min: 1, max: 500, integer: true });
                }
                const snap = value.authoring.snap;
                if (snap !== undefined && snap !== false) {
                    if (!isPlainObject(snap)) {
                        issues.push({
                            path: `${path}.authoring.snap`,
                            message: 'Mark label snapping must be false or an object.',
                        });
                    }
                    else {
                        validateUnknownKeys(snap, MARK_LABEL_SNAP_KEYS, `${path}.authoring.snap`, 'mark label snapping', issues);
                        if (snap.grid !== undefined && snap.grid !== false) {
                            validateFiniteNumber(snap.grid, `${path}.authoring.snap.grid`, 'Mark label snap grid', issues, {
                                min: 1,
                                max: 256,
                            });
                        }
                        for (const key of ['marks', 'plot']) {
                            validateOptionalBoolean(snap[key], `${path}.authoring.snap.${key}`, `Mark label snap ${key}`, issues);
                        }
                        if (snap.distance !== undefined) {
                            validateFiniteNumber(snap.distance, `${path}.authoring.snap.distance`, 'Mark label snap distance', issues, {
                                min: 0,
                                max: 128,
                            });
                        }
                    }
                }
            }
        }
    }
    function validateLayerReferences(input, issues) {
        const sourceLayers = Array.isArray(input.layers)
            ? input.layers
            : input.mark !== undefined ||
                input.x !== undefined ||
                input.y !== undefined ||
                input.encoding !== undefined
                ? [input]
                : [];
        const layerIds = new Set();
        sourceLayers.forEach((layer, index) => {
            if (!isPlainObject(layer))
                return;
            const layerId = layer.id === undefined ? `layer-${index}` : layer.id;
            if (typeof layerId !== 'string' || layerId.trim() === '')
                return;
            if (layerIds.has(layerId)) {
                issues.push({
                    path: Array.isArray(input.layers) ? `$.layers[${index}].id` : '$.id',
                    message: `Layer id "${layerId}" must be unique.`,
                });
                return;
            }
            layerIds.add(layerId);
        });
        const check = (value, path) => {
            if (!isPlainObject(value) || typeof value.layerId !== 'string')
                return;
            if (!layerIds.has(value.layerId)) {
                issues.push({
                    path: `${path}.layerId`,
                    message: `Layer id "${value.layerId}" does not exist.`,
                });
            }
        };
        const checkUniqueIds = (value, path, label, defaultPrefix) => {
            if (!Array.isArray(value))
                return;
            const ids = new Set();
            value.forEach((entry, index) => {
                if (!isPlainObject(entry))
                    return;
                const id = typeof entry.id === 'string' && entry.id.trim() !== ''
                    ? entry.id
                    : `${defaultPrefix}-${index}`;
                if (ids.has(id)) {
                    issues.push({
                        path: `${path}[${index}].id`,
                        message: `${label} id "${id}" must be unique after defaults are resolved.`,
                    });
                    return;
                }
                ids.add(id);
            });
        };
        if (isPlainObject(input.legend)) {
            const legend = input.legend;
            check(legend, '$.legend');
            if (Array.isArray(legend.items)) {
                legend.items.forEach((item, index) => check(item, `$.legend.items[${index}]`));
            }
            checkUniqueIds(legend.items, '$.legend.items', 'Legend item', 'item');
            if (Array.isArray(legend.items)) {
                const semanticOwners = new Set();
                legend.items.forEach((item, index) => {
                    if (!isPlainObject(item) || typeof item.layerId !== 'string')
                        return;
                    const owner = legend.mode === 'categories' && Object.prototype.hasOwnProperty.call(item, 'value')
                        ? JSON.stringify(['category', item.layerId, item.value])
                        : legend.mode === 'layers'
                            ? JSON.stringify(['layer', item.layerId])
                            : null;
                    if (owner === null)
                        return;
                    if (semanticOwners.has(owner)) {
                        issues.push({
                            path: `$.legend.items[${index}]`,
                            message: 'Interactive legend items must not control the same semantic owner.',
                        });
                    }
                    else
                        semanticOwners.add(owner);
                });
            }
        }
        if (Array.isArray(input.highlights)) {
            input.highlights.forEach((highlight, index) => {
                if (isPlainObject(highlight))
                    check(highlight.target, `$.highlights[${index}].target`);
            });
        }
        if (Array.isArray(input.annotations)) {
            input.annotations.forEach((annotation, index) => {
                if (isPlainObject(annotation))
                    check(annotation.target, `$.annotations[${index}].target`);
            });
        }
        if (isPlainObject(input.markLabels)) {
            if (Array.isArray(input.markLabels.layerIds)) {
                input.markLabels.layerIds.forEach((layerId, index) => check({ layerId }, `$.markLabels.layerIds[${index}]`));
            }
            if (Array.isArray(input.markLabels.positions)) {
                input.markLabels.positions.forEach((position, index) => {
                    if (isPlainObject(position))
                        check(position.target, `$.markLabels.positions[${index}].target`);
                });
            }
        }
        checkUniqueIds(input.highlights, '$.highlights', 'Highlight', 'highlight');
        checkUniqueIds(input.annotations, '$.annotations', 'Annotation', 'annotation');
    }
    function validateStreaming(value, path, issues) {
        if (value === undefined)
            return;
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Streaming must be an object.' });
            return;
        }
        validateUnknownKeys(value, STREAMING_KEYS, path, 'streaming', issues);
        if (value.key === undefined) {
            issues.push({ path: `${path}.key`, message: 'Streaming stable key is required.' });
        }
        else {
            validateOptionalString(value.key, `${path}.key`, 'Streaming stable key', issues, false);
        }
        if (typeof value.key === 'string' && UNSAFE_FIELDS.has(value.key)) {
            issues.push({ path: `${path}.key`, message: 'Streaming stable key is unsafe.' });
        }
        if (value.mode !== undefined &&
            !['append', 'upsert', 'replaceLast'].includes(String(value.mode))) {
            issues.push({ path: `${path}.mode`, message: 'Streaming mode is not supported.' });
        }
        if (value.maxBatchRows !== undefined) {
            validateFiniteNumber(value.maxBatchRows, `${path}.maxBatchRows`, 'Streaming batch limit', issues, {
                min: 1,
                max: 1_000_000,
                integer: true,
            });
        }
        if (value.retention !== undefined) {
            if (!isPlainObject(value.retention)) {
                issues.push({ path: `${path}.retention`, message: 'Streaming retention must be an object.' });
            }
            else {
                validateUnknownKeys(value.retention, STREAMING_RETENTION_KEYS, `${path}.retention`, 'streaming retention', issues);
                if (value.retention.maxRows !== undefined) {
                    validateFiniteNumber(value.retention.maxRows, `${path}.retention.maxRows`, 'Streaming retention row limit', issues, { min: 1, max: 1_000_000, integer: true });
                }
                if (value.retention.time !== undefined) {
                    if (!isPlainObject(value.retention.time)) {
                        issues.push({
                            path: `${path}.retention.time`,
                            message: 'Time retention must be an object.',
                        });
                    }
                    else {
                        validateUnknownKeys(value.retention.time, STREAMING_TIME_KEYS, `${path}.retention.time`, 'time retention', issues);
                        if (value.retention.time.field === undefined) {
                            issues.push({
                                path: `${path}.retention.time.field`,
                                message: 'Time retention field is required.',
                            });
                        }
                        else {
                            validateOptionalString(value.retention.time.field, `${path}.retention.time.field`, 'Time retention field', issues, false);
                        }
                        if (typeof value.retention.time.field === 'string' &&
                            UNSAFE_FIELDS.has(value.retention.time.field)) {
                            issues.push({
                                path: `${path}.retention.time.field`,
                                message: 'Time retention field is unsafe.',
                            });
                        }
                        validateFiniteNumber(value.retention.time.durationMs, `${path}.retention.time.durationMs`, 'Time retention duration', issues, { min: 0 });
                        if (value.eventTime === undefined) {
                            issues.push({
                                path: `${path}.retention.time`,
                                message: 'Time retention requires an eventTime watermark contract.',
                            });
                        }
                    }
                }
            }
        }
        if (value.eventTime !== undefined) {
            if (!isPlainObject(value.eventTime)) {
                issues.push({ path: `${path}.eventTime`, message: 'Event time must be an object.' });
            }
            else {
                validateUnknownKeys(value.eventTime, STREAMING_EVENT_TIME_KEYS, `${path}.eventTime`, 'event-time', issues);
                if (value.eventTime.field === undefined) {
                    issues.push({ path: `${path}.eventTime.field`, message: 'Event-time field is required.' });
                }
                else {
                    validateOptionalString(value.eventTime.field, `${path}.eventTime.field`, 'Event-time field', issues, false);
                }
                if (typeof value.eventTime.field === 'string' && UNSAFE_FIELDS.has(value.eventTime.field)) {
                    issues.push({ path: `${path}.eventTime.field`, message: 'Event-time field is unsafe.' });
                }
                if (value.eventTime.allowedLatenessMs !== undefined) {
                    validateFiniteNumber(value.eventTime.allowedLatenessMs, `${path}.eventTime.allowedLatenessMs`, 'Allowed lateness', issues, { min: 0 });
                }
                if (value.eventTime.lateData !== undefined &&
                    !['reject', 'drop', 'accept'].includes(String(value.eventTime.lateData))) {
                    issues.push({
                        path: `${path}.eventTime.lateData`,
                        message: 'Late-data policy is unsupported.',
                    });
                }
            }
        }
        for (const [name, keys, maximumBatches] of [
            ['queue', STREAMING_QUEUE_KEYS, 1_024],
            ['replay', STREAMING_REPLAY_KEYS, 4_096],
        ]) {
            const object = value[name];
            if (object === undefined)
                continue;
            if (!isPlainObject(object)) {
                issues.push({ path: `${path}.${name}`, message: `Streaming ${name} must be an object.` });
                continue;
            }
            validateUnknownKeys(object, keys, `${path}.${name}`, `streaming ${name}`, issues);
            for (const field of ['maxBatches', 'maxRows']) {
                if (object[field] !== undefined) {
                    validateFiniteNumber(object[field], `${path}.${name}.${field}`, `Streaming ${name} ${field}`, issues, {
                        min: 1,
                        max: field === 'maxBatches' ? maximumBatches : 1_000_000,
                        integer: true,
                    });
                }
            }
            if (name === 'queue' &&
                object.overflow !== undefined &&
                !STREAMING_OVERFLOW_POLICIES.has(String(object.overflow))) {
                issues.push({
                    path: `${path}.queue.overflow`,
                    message: 'Streaming queue overflow policy is unsupported.',
                });
            }
        }
        if (value.runtime !== undefined) {
            if (!isPlainObject(value.runtime)) {
                issues.push({ path: `${path}.runtime`, message: 'Streaming runtime must be an object.' });
            }
            else {
                validateUnknownKeys(value.runtime, STREAMING_RUNTIME_KEYS, `${path}.runtime`, 'streaming runtime', issues);
                if (value.runtime.schedule !== undefined &&
                    !['animation-frame', 'microtask'].includes(String(value.runtime.schedule))) {
                    issues.push({
                        path: `${path}.runtime.schedule`,
                        message: 'Streaming runtime schedule is unsupported.',
                    });
                }
                if (value.runtime.maxBatchesPerFrame !== undefined) {
                    validateFiniteNumber(value.runtime.maxBatchesPerFrame, `${path}.runtime.maxBatchesPerFrame`, 'Streaming frame batch limit', issues, { integer: true, min: 1, max: 1_024 });
                }
                if (value.runtime.overflow !== undefined &&
                    !STREAMING_OVERFLOW_POLICIES.has(String(value.runtime.overflow))) {
                    issues.push({
                        path: `${path}.runtime.overflow`,
                        message: 'Streaming runtime overflow policy is unsupported.',
                    });
                }
                validateOptionalBoolean(value.runtime.paused, `${path}.runtime.paused`, 'Streaming paused state', issues);
                validateOptionalBoolean(value.runtime.followLive, `${path}.runtime.followLive`, 'Streaming follow-live state', issues);
                if (value.runtime.history !== undefined) {
                    if (!isPlainObject(value.runtime.history)) {
                        issues.push({
                            path: `${path}.runtime.history`,
                            message: 'Streaming runtime history must be an object.',
                        });
                    }
                    else {
                        validateUnknownKeys(value.runtime.history, STREAMING_HISTORY_KEYS, `${path}.runtime.history`, 'streaming history', issues);
                        if (value.runtime.history.maxBatches !== undefined) {
                            validateFiniteNumber(value.runtime.history.maxBatches, `${path}.runtime.history.maxBatches`, 'Streaming history batch limit', issues, { integer: true, min: 1, max: 4_096 });
                        }
                        if (value.runtime.history.pageRows !== undefined) {
                            validateFiniteNumber(value.runtime.history.pageRows, `${path}.runtime.history.pageRows`, 'Streaming history page size', issues, { integer: true, min: 1, max: 100_000 });
                        }
                    }
                }
            }
        }
        if (value.worker !== undefined) {
            if (!isPlainObject(value.worker)) {
                issues.push({ path: `${path}.worker`, message: 'Streaming Worker must be an object.' });
            }
            else {
                validateUnknownKeys(value.worker, STREAMING_WORKER_KEYS, `${path}.worker`, 'streaming Worker', issues);
                if (typeof value.worker.moduleURL !== 'string' ||
                    value.worker.moduleURL.trim() === '' ||
                    value.worker.moduleURL.length > 2_048) {
                    issues.push({
                        path: `${path}.worker.moduleURL`,
                        message: 'Streaming Worker moduleURL must be a non-empty string of at most 2048 characters.',
                    });
                }
                if (value.worker.name !== undefined &&
                    (typeof value.worker.name !== 'string' ||
                        !/^[a-zA-Z0-9][a-zA-Z0-9_.:-]{0,95}$/.test(value.worker.name))) {
                    issues.push({
                        path: `${path}.worker.name`,
                        message: 'Streaming Worker name must contain 1 to 96 portable identity characters.',
                    });
                }
                for (const [field, maximum] of [
                    ['maxQueueBatches', 1_024],
                    ['maxQueueRows', 1_000_000],
                    ['maxInputRows', 1_000_000],
                    ['maxBinaryBytes', 256 * 1024 * 1024],
                    ['maxTransforms', 128],
                ]) {
                    if (value.worker[field] !== undefined) {
                        validateFiniteNumber(value.worker[field], `${path}.worker.${field}`, `Streaming Worker ${field}`, issues, { integer: true, min: 1, max: maximum });
                    }
                }
                if (value.worker.overflow !== undefined &&
                    !STREAMING_OVERFLOW_POLICIES.has(String(value.worker.overflow))) {
                    issues.push({
                        path: `${path}.worker.overflow`,
                        message: 'Streaming Worker overflow policy is unsupported.',
                    });
                }
                if (value.worker.engine !== undefined) {
                    if (!isPlainObject(value.worker.engine)) {
                        issues.push({
                            path: `${path}.worker.engine`,
                            message: 'Streaming Worker engine must be an object.',
                        });
                    }
                    else {
                        validateUnknownKeys(value.worker.engine, STREAMING_WORKER_ENGINE_KEYS, `${path}.worker.engine`, 'streaming Worker engine', issues);
                        if (!['javascript', 'wasm'].includes(String(value.worker.engine.type))) {
                            issues.push({
                                path: `${path}.worker.engine.type`,
                                message: 'Streaming Worker engine type must be "javascript" or "wasm".',
                            });
                        }
                        if (value.worker.engine.type === 'wasm') {
                            if (typeof value.worker.engine.adapter !== 'string' ||
                                !/^[a-zA-Z0-9][a-zA-Z0-9_.:-]{0,95}$/.test(value.worker.engine.adapter)) {
                                issues.push({
                                    path: `${path}.worker.engine.adapter`,
                                    message: 'WASM engine requires a portable adapter identifier.',
                                });
                            }
                        }
                        else if (value.worker.engine.adapter !== undefined) {
                            issues.push({
                                path: `${path}.worker.engine.adapter`,
                                message: 'JavaScript engine does not accept an adapter.',
                            });
                        }
                    }
                }
            }
        }
    }
    function findFunctions(value, path, issues, seen) {
        if (typeof value === 'function') {
            issues.push({ path, message: 'Functions are not allowed in the portable chart spec.' });
            return;
        }
        if (value === null || typeof value !== 'object' || value instanceof Date)
            return;
        if (ArrayBuffer.isView(value))
            return;
        if (seen.has(value))
            return;
        seen.add(value);
        if (Array.isArray(value)) {
            value.forEach((item, index) => findFunctions(item, `${path}[${index}]`, issues, seen));
            return;
        }
        for (const [key, child] of Object.entries(value)) {
            if (UNSAFE_FIELDS.has(key)) {
                issues.push({ path: `${path}.${key}`, message: `Unsafe key "${key}" is forbidden.` });
                continue;
            }
            findFunctions(child, `${path}.${key}`, issues, seen);
        }
    }
    function validateAxisBindings(input, issues) {
        const axes = isPlainObject(input.axes)
            ? input.axes
            : {};
        const declared = (id) => builtInAxisChannel(id) !== undefined || Object.prototype.hasOwnProperty.call(axes, id);
        const reference = (id, channel, path) => {
            if (!isSafeAxisId(id))
                return;
            if (!declared(id)) {
                issues.push({
                    path,
                    message: `Named axis "${id}" must be declared in $.axes with channel and position.`,
                });
                return;
            }
            const resolvedChannel = axisChannel(id, axes);
            if (channel !== undefined && resolvedChannel !== undefined && resolvedChannel !== channel) {
                issues.push({
                    path,
                    message: `Axis "${id}" belongs to the ${resolvedChannel} channel, not ${channel}.`,
                });
            }
        };
        const encoding = (value, channel, path) => {
            const id = isPlainObject(value) && value.axisId !== undefined ? value.axisId : channel;
            reference(id, channel, `${path}.axisId`);
        };
        const layer = (value, path) => {
            if (!isPlainObject(value))
                return;
            if (isPlainObject(value.encoding)) {
                encoding(value.encoding.x, 'x', `${path}.encoding.x`);
                encoding(value.encoding.y, 'y', `${path}.encoding.y`);
            }
            else {
                encoding(value.x, 'x', `${path}.x`);
                encoding(value.y, 'y', `${path}.y`);
            }
            if (isPlainObject(value.clip) && value.clip.type === 'domain') {
                for (const channel of ['x', 'y']) {
                    const range = value.clip[channel];
                    if (isPlainObject(range) && range.axis !== undefined) {
                        reference(range.axis, channel, `${path}.clip.${channel}.axis`);
                    }
                }
            }
        };
        if (Array.isArray(input.layers))
            input.layers.forEach((value, index) => layer(value, `$.layers[${index}]`));
        if (input.mark !== undefined ||
            input.x !== undefined ||
            input.y !== undefined ||
            input.encoding !== undefined) {
            layer(input, '$');
        }
        const interaction = isPlainObject(input.interaction) ? input.interaction : undefined;
        const tooltip = isPlainObject(interaction?.tooltip) ? interaction.tooltip : undefined;
        if (tooltip?.axis !== undefined)
            reference(tooltip.axis, undefined, '$.interaction.tooltip.axis');
        const navigation = isPlainObject(interaction?.domainNavigation)
            ? interaction.domainNavigation
            : undefined;
        if (Array.isArray(navigation?.axes)) {
            navigation.axes.forEach((id, index) => reference(id, undefined, `$.interaction.domainNavigation.axes[${index}]`));
        }
        const selection = isPlainObject(interaction?.selection) ? interaction.selection : undefined;
        if (selection?.axis !== undefined)
            reference(selection.axis, undefined, '$.interaction.selection.axis');
        if (selection?.xAxis !== undefined)
            reference(selection.xAxis, 'x', '$.interaction.selection.xAxis');
        if (selection?.yAxis !== undefined)
            reference(selection.yAxis, 'y', '$.interaction.selection.yAxis');
        for (const collection of ['highlights', 'annotations']) {
            const values = input[collection];
            if (!Array.isArray(values))
                continue;
            values.forEach((entry, index) => {
                if (!isPlainObject(entry) || !isPlainObject(entry.target) || entry.target.type !== 'range')
                    return;
                for (const channel of ['x', 'y']) {
                    const range = entry.target[channel];
                    if (isPlainObject(range) && range.axis !== undefined) {
                        reference(range.axis, channel, `$.${collection}[${index}].target.${channel}.axis`);
                    }
                }
            });
        }
    }
    function validateUnitSpec(input) {
        const issues = [];
        if (!isPlainObject(input)) {
            return [{ path: '$', message: 'Chart spec must be an object.' }];
        }
        if (input.specVersion !== undefined && input.specVersion !== '0.1') {
            issues.push({ path: '$.specVersion', message: 'Only specVersion "0.1" is supported.' });
        }
        const layers = input.layers;
        const hasShorthand = input.mark !== undefined ||
            input.x !== undefined ||
            input.y !== undefined ||
            input.encoding !== undefined;
        if (layers === undefined && !hasShorthand) {
            issues.push({ path: '$', message: 'Provide layers or the mark/x/y shorthand.' });
        }
        if (layers !== undefined) {
            if (!Array.isArray(layers) || layers.length === 0) {
                issues.push({ path: '$.layers', message: 'Layers must be a non-empty array.' });
            }
            else {
                layers.forEach((layer, index) => validateLayer(layer, `$.layers[${index}]`, input.data !== undefined, issues));
            }
        }
        if (hasShorthand) {
            validateMark(input.mark, '$.mark', issues);
            if (input.encoding !== undefined && (input.x !== undefined || input.y !== undefined)) {
                issues.push({
                    path: '$',
                    message: 'Use either the legacy x/y facade or the canonical encoding map, not both.',
                });
            }
            if (input.encoding !== undefined) {
                validateEncodingMap(input.encoding, '$.encoding', issues, input.mark);
                validateMarkEncodingCompatibility(input.mark, input.encoding, '$.encoding', issues);
            }
            else {
                validateEncoding(input.x, '$.x', 'x', issues);
                validateEncoding(input.y, '$.y', 'y', issues);
            }
            if (input.source !== undefined) {
                validateOptionalString(input.source, '$.source', 'Named data source', issues, false);
                issues.push({
                    path: '$.source',
                    message: 'Named chart sources require an enclosing dataflow.',
                });
            }
            if (input.data === undefined && input.source === undefined) {
                issues.push({
                    path: '$.data',
                    message: 'Chart-level data is required for shorthand charts.',
                });
            }
        }
        validateAxes(input.axes, '$.axes', issues);
        validateAxisBindings(input, issues);
        validateTheme(input.theme, '$.theme', issues);
        validateLegend(input.legend, '$.legend', issues);
        validateHighlights(input.highlights, '$.highlights', issues);
        validateAnnotations(input.annotations, '$.annotations', issues);
        validateMarkLabels(input.markLabels, '$.markLabels', issues);
        validateInteraction(input.interaction, '$.interaction', issues);
        validateAccessibility(input.accessibility, '$.accessibility', issues);
        validateStreaming(input.streaming, '$.streaming', issues);
        validateTransforms(input.transform, '$.transform', issues);
        validateLayerReferences(input, issues);
        findFunctions(input, '$', issues, new WeakSet());
        return issues;
    }
    function prefixedIssue(issue, path) {
        return {
            path: issue.path === '$' ? path : `${path}${issue.path.slice(1)}`,
            message: issue.message,
        };
    }
    function validateFacetField(value, path, issues) {
        if (typeof value === 'string') {
            validateOptionalString(value, path, 'Facet field', issues, false);
            if (UNSAFE_FIELDS.has(value))
                issues.push({ path, message: `Unsafe facet field "${value}".` });
            return;
        }
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Facet field must be a field name or an object.' });
            return;
        }
        validateUnknownKeys(value, FACET_FIELD_KEYS, path, 'facet field', issues);
        validateOptionalString(value.field, `${path}.field`, 'Facet field', issues, false);
        if (typeof value.field === 'string' && UNSAFE_FIELDS.has(value.field)) {
            issues.push({ path: `${path}.field`, message: `Unsafe facet field "${value.field}".` });
        }
        validateOptionalString(value.title, `${path}.title`, 'Facet title', issues);
        if (value.sort !== undefined &&
            (typeof value.sort !== 'string' || !FACET_SORTS.has(value.sort))) {
            issues.push({ path: `${path}.sort`, message: 'Facet sort is not supported.' });
        }
    }
    function validateCompositionResolve(value, path, kind, issues) {
        if (value !== undefined && !isPlainObject(value)) {
            issues.push({ path, message: 'Composition resolve must be an object.' });
            return;
        }
        if (isPlainObject(value)) {
            validateUnknownKeys(value, COMPOSITION_RESOLVE_KEYS, path, 'composition resolve', issues);
            for (const key of COMPOSITION_RESOLVE_KEYS) {
                const mode = value[key];
                if (mode !== undefined && mode !== 'shared' && mode !== 'independent') {
                    issues.push({
                        path: `${path}.${key}`,
                        message: 'Resolve mode must be shared or independent.',
                    });
                }
            }
        }
        const resolved = resolveComposition(isPlainObject(value) ? value : undefined, kind);
        if (kind === 'layer' && Object.values(resolved).some((mode) => mode !== 'shared')) {
            issues.push({
                path,
                message: 'Layer composition requires shared scale, axis, legend, and colorbar resolve.',
            });
        }
        if (kind === 'inset' && Object.values(resolved).some((mode) => mode !== 'independent')) {
            issues.push({
                path,
                message: 'Inset composition requires independent scale, axis, legend, and colorbar resolve.',
            });
        }
        if (resolved.axis === 'shared' && resolved.scale !== 'shared') {
            issues.push({
                path: `${path}.axis`,
                message: 'A shared axis requires a shared position scale.',
            });
        }
    }
    function validateCompositionInteraction(value, path, kind, issues) {
        validateInteraction(value, path, issues);
        if (!isPlainObject(value))
            return;
        if (value.playback !== undefined && value.playback !== false) {
            issues.push({
                path: `${path}.playback`,
                message: 'Composition playback is not supported until frame state can be resolved per view.',
            });
        }
        if (kind !== 'layer' && isPlainObject(value.tooltip) && value.tooltip.trigger === 'axis') {
            issues.push({
                path: `${path}.tooltip`,
                message: 'Axis-nearest tooltip is not supported across independent composition views.',
            });
        }
        if (kind !== 'layer' && isPlainObject(value.controls)) {
            if (value.controls.playback === true) {
                issues.push({
                    path: `${path}.controls.playback`,
                    message: 'Composition playback controls are not supported.',
                });
            }
            if (value.controls.annotations === true) {
                issues.push({
                    path: `${path}.controls.annotations`,
                    message: 'Composition-level runtime annotations are not supported.',
                });
            }
        }
    }
    function validateCompositionChild(value, path, depth, inheritedData, issues, state) {
        if (!isPlainObject(value)) {
            issues.push({ path, message: 'Composition child must be a chart spec object.' });
            return;
        }
        if (value.width !== undefined || value.height !== undefined) {
            issues.push({
                path,
                message: 'Composition cell dimensions are owned by the parent; child width/height are unsupported.',
            });
        }
        if (value.interaction !== undefined) {
            issues.push({
                path: `${path}.interaction`,
                message: 'Declare interaction once on the composition container.',
            });
        }
        if (value.streaming !== undefined) {
            issues.push({
                path: `${path}.streaming`,
                message: 'Streaming composition children are unsupported until updates target a view id.',
            });
        }
        if (isPlainObject(value.legend) && value.legend.interactive === true) {
            issues.push({
                path: `${path}.legend.interactive`,
                message: 'Independent composition legends are visual-only and cannot be interactive.',
            });
        }
        validateCompositionNode(value, path, depth, inheritedData, issues, state);
    }
    function validateCompositionArray(value, path, depth, inheritedData, limit, issues, state) {
        if (!Array.isArray(value) || value.length === 0) {
            issues.push({ path, message: 'Composition children must be a non-empty array.' });
            return;
        }
        if (value.length > limit) {
            issues.push({
                path,
                message: `Composition child count exceeds the deterministic limit ${limit}.`,
            });
        }
        value.forEach((child, index) => validateCompositionChild(child, `${path}[${index}]`, depth + 1, inheritedData, issues, state));
    }
    function validateCompositionNode(input, path, depth, inheritedData, issues, state) {
        if (state.ancestors.has(input)) {
            issues.push({ path, message: 'Composition specs must not contain cycles.' });
            return;
        }
        const operators = presentCompositionOperators(input);
        if (operators.length === 0) {
            state.views += 1;
            if (state.views > maximumCompositionViews) {
                issues.push({ path, message: `Composition view count exceeds ${maximumCompositionViews}.` });
            }
            const unit = inheritedData && input.data === undefined ? { ...input, data: [] } : input;
            issues.push(...validateUnitSpec(unit).map((issue) => prefixedIssue(issue, path)));
            if (input.renderer !== undefined && input.renderer !== 'auto' && input.renderer !== 'canvas') {
                issues.push({
                    path: `${path}.renderer`,
                    message: 'Composition currently accepts only auto or canvas child renderers.',
                });
            }
            return;
        }
        if (operators.length !== 1) {
            issues.push({
                path,
                message: `Use exactly one composition operator; received ${operators.join(', ')}.`,
            });
            return;
        }
        if (depth >= maximumCompositionDepth) {
            issues.push({ path, message: `Composition nesting exceeds depth ${maximumCompositionDepth}.` });
            return;
        }
        const kind = operators[0];
        state.ancestors.add(input);
        validateUnknownKeys(input, COMPOSITION_KEYS, path, 'composition', issues);
        const ownsData = input.data !== undefined || inheritedData;
        const unitFields = ['mark', 'x', 'y', 'encoding', 'layers'].filter((key) => input[key] !== undefined);
        if (unitFields.length > 0) {
            issues.push({
                path,
                message: `Composition cannot be mixed with unit fields: ${unitFields.join(', ')}.`,
            });
        }
        if (input.specVersion !== undefined && input.specVersion !== '0.1') {
            issues.push({ path: `${path}.specVersion`, message: 'Only specVersion "0.1" is supported.' });
        }
        if (input.renderer !== undefined && input.renderer !== 'auto' && input.renderer !== 'canvas') {
            issues.push({
                path: `${path}.renderer`,
                message: 'Composition currently supports only the Canvas renderer.',
            });
        }
        if (input.streaming !== undefined) {
            issues.push({
                path: `${path}.streaming`,
                message: 'Streaming composition is not supported until updates are routed to explicit views.',
            });
        }
        if (input.spacing !== undefined)
            validateFiniteNumber(input.spacing, `${path}.spacing`, 'Composition spacing', issues, {
                min: 0,
                max: 64,
            });
        validateTheme(input.theme, `${path}.theme`, issues);
        validateTransforms(input.transform, `${path}.transform`, issues);
        validateAccessibility(input.accessibility, `${path}.accessibility`, issues);
        validateCompositionInteraction(input.interaction, `${path}.interaction`, kind, issues);
        validateCompositionResolve(input.resolve, `${path}.resolve`, kind, issues);
        if (kind !== 'layer') {
            if (input.axes !== undefined &&
                resolveComposition(input.resolve, kind).axis !== 'shared') {
                issues.push({ path: `${path}.axes`, message: 'Container axes require shared axis resolve.' });
            }
            else {
                validateAxes(input.axes, `${path}.axes`, issues);
            }
            const guideResolve = resolveComposition(input.resolve, kind);
            if (input.legend !== undefined &&
                guideResolve.legend !== 'shared' &&
                guideResolve.colorbar !== 'shared') {
                issues.push({
                    path: `${path}.legend`,
                    message: 'A container legend requires shared legend or colorbar resolve.',
                });
            }
            else {
                validateLegend(input.legend, `${path}.legend`, issues);
            }
            if (Array.isArray(input.highlights) && input.highlights.length > 0)
                issues.push({
                    path: `${path}.highlights`,
                    message: 'Highlights must be declared in a child view.',
                });
            if (Array.isArray(input.annotations) && input.annotations.length > 0)
                issues.push({
                    path: `${path}.annotations`,
                    message: 'Annotations must be declared in a child view.',
                });
        }
        else {
            validateAxes(input.axes, `${path}.axes`, issues);
            validateLegend(input.legend, `${path}.legend`, issues);
            validateHighlights(input.highlights, `${path}.highlights`, issues);
            validateAnnotations(input.annotations, `${path}.annotations`, issues);
        }
        switch (kind) {
            case 'layer':
                if (input.spec !== undefined)
                    issues.push({
                        path: `${path}.spec`,
                        message: 'Layer composition does not use a template spec.',
                    });
                validateCompositionArray(input.layer, `${path}.layer`, depth, ownsData, maximumLayerCompositionChildren, issues, state);
                if (Array.isArray(input.layer)) {
                    input.layer.forEach((child, index) => {
                        if (!isPlainObject(child))
                            return;
                        validateUnknownKeys(child, LAYER_COMPOSITION_CHILD_KEYS, `${path}.layer[${index}]`, 'layer composition child', issues);
                        if (presentCompositionOperators(child).length > 0) {
                            issues.push({
                                path: `${path}.layer[${index}]`,
                                message: 'Layer composition children must be unit charts so they can share one scale/axis/legend pipeline.',
                            });
                        }
                        if (child.layers !== undefined &&
                            (child.mark !== undefined ||
                                child.x !== undefined ||
                                child.y !== undefined ||
                                child.encoding !== undefined)) {
                            issues.push({
                                path: `${path}.layer[${index}]`,
                                message: 'Layer composition children must use either flat layers or unit shorthand, not both.',
                            });
                        }
                    });
                }
                break;
            case 'facet': {
                if (!isPlainObject(input.facet)) {
                    issues.push({ path: `${path}.facet`, message: 'Facet must be an object.' });
                }
                else {
                    const facet = input.facet;
                    validateUnknownKeys(facet, FACET_KEYS, `${path}.facet`, 'facet', issues);
                    const fields = ['row', 'column', 'wrap'].filter((key) => facet[key] !== undefined);
                    if (fields.length === 0)
                        issues.push({ path: `${path}.facet`, message: 'Facet requires row, column, or wrap.' });
                    if (facet.wrap !== undefined && fields.length > 1)
                        issues.push({
                            path: `${path}.facet.wrap`,
                            message: 'Wrap facet cannot be mixed with row/column.',
                        });
                    for (const field of fields)
                        validateFacetField(facet[field], `${path}.facet.${field}`, issues);
                    if (facet.columns !== undefined)
                        validateFiniteNumber(facet.columns, `${path}.facet.columns`, 'Facet columns', issues, {
                            min: 1,
                            max: 16,
                            integer: true,
                        });
                }
                if (input.data === undefined)
                    issues.push({
                        path: `${path}.data`,
                        message: 'Facet composition requires chart-level data.',
                    });
                if (!isPlainObject(input.spec))
                    issues.push({
                        path: `${path}.spec`,
                        message: 'Facet composition requires a template spec.',
                    });
                else {
                    if (input.spec.data !== undefined)
                        issues.push({
                            path: `${path}.spec.data`,
                            message: 'Facet template data comes from the partition.',
                        });
                    validateCompositionChild(input.spec, `${path}.spec`, depth + 1, true, issues, state);
                }
                break;
            }
            case 'repeat': {
                if (!isPlainObject(input.repeat)) {
                    issues.push({ path: `${path}.repeat`, message: 'Repeat must be an object.' });
                }
                else {
                    validateUnknownKeys(input.repeat, REPEAT_KEYS, `${path}.repeat`, 'repeat', issues);
                    if (!Array.isArray(input.repeat.items) || input.repeat.items.length === 0) {
                        issues.push({
                            path: `${path}.repeat.items`,
                            message: 'Repeat items must be a non-empty array.',
                        });
                    }
                    else {
                        if (input.repeat.items.length > maximumCompositionViews)
                            issues.push({
                                path: `${path}.repeat.items`,
                                message: `Repeat item count exceeds ${maximumCompositionViews}.`,
                            });
                        const ids = new Set();
                        input.repeat.items.forEach((item, index) => {
                            const itemPath = `${path}.repeat.items[${index}]`;
                            if (!isPlainObject(item)) {
                                issues.push({ path: itemPath, message: 'Repeat item must be an object.' });
                                return;
                            }
                            validateUnknownKeys(item, REPEAT_ITEM_KEYS, itemPath, 'repeat item', issues);
                            validateOptionalString(item.id, `${itemPath}.id`, 'Repeat item id', issues, false);
                            validateOptionalString(item.label, `${itemPath}.label`, 'Repeat item label', issues);
                            validateOptionalString(item.x, `${itemPath}.x`, 'Repeat x field', issues, false);
                            validateOptionalString(item.y, `${itemPath}.y`, 'Repeat y field', issues, false);
                            for (const field of ['x', 'y']) {
                                if (typeof item[field] === 'string' && UNSAFE_FIELDS.has(item[field])) {
                                    issues.push({
                                        path: `${itemPath}.${field}`,
                                        message: `Unsafe repeat ${field} field "${item[field]}".`,
                                    });
                                }
                            }
                            if (item.x === undefined && item.y === undefined)
                                issues.push({
                                    path: itemPath,
                                    message: 'Repeat item requires x or y field substitution.',
                                });
                            if (typeof item.id === 'string') {
                                if (ids.has(item.id))
                                    issues.push({ path: `${itemPath}.id`, message: 'Repeat item ids must be unique.' });
                                ids.add(item.id);
                            }
                        });
                    }
                    if (input.repeat.columns !== undefined)
                        validateFiniteNumber(input.repeat.columns, `${path}.repeat.columns`, 'Repeat columns', issues, { min: 1, max: 16, integer: true });
                }
                if (!isPlainObject(input.spec))
                    issues.push({
                        path: `${path}.spec`,
                        message: 'Repeat composition requires a template spec.',
                    });
                else {
                    if (presentCompositionOperators(input.spec).length > 0)
                        issues.push({
                            path: `${path}.spec`,
                            message: 'Repeat templates must be unit charts for explicit x/y field substitution.',
                        });
                    validateCompositionChild(input.spec, `${path}.spec`, depth + 1, ownsData, issues, state);
                }
                break;
            }
            case 'hconcat':
            case 'vconcat':
            case 'concat': {
                const children = input[kind];
                validateCompositionArray(children, `${path}.${kind}`, depth, ownsData, maximumCompositionViews, issues, state);
                if (kind === 'concat' && input.columns !== undefined)
                    validateFiniteNumber(input.columns, `${path}.columns`, 'Concat columns', issues, {
                        min: 1,
                        max: 16,
                        integer: true,
                    });
                if (kind !== 'concat' && input.columns !== undefined)
                    issues.push({
                        path: `${path}.columns`,
                        message: 'Columns is only valid for wrapped concat.',
                    });
                if (input.spec !== undefined)
                    issues.push({ path: `${path}.spec`, message: `${kind} does not use a template spec.` });
                break;
            }
            case 'inset':
                if (!isPlainObject(input.inset)) {
                    issues.push({ path: `${path}.inset`, message: 'Inset must be an object.' });
                }
                else {
                    validateUnknownKeys(input.inset, INSET_KEYS, `${path}.inset`, 'inset', issues);
                    for (const key of ['x', 'y', 'width', 'height']) {
                        validateFiniteNumber(input.inset[key], `${path}.inset.${key}`, `Inset ${key}`, issues, {
                            min: key === 'width' || key === 'height' ? Number.EPSILON : 0,
                            max: 1,
                        });
                    }
                    if (typeof input.inset.x === 'number' &&
                        typeof input.inset.width === 'number' &&
                        input.inset.x + input.inset.width > 1)
                        issues.push({
                            path: `${path}.inset.width`,
                            message: 'Inset exceeds the horizontal plot boundary.',
                        });
                    if (typeof input.inset.y === 'number' &&
                        typeof input.inset.height === 'number' &&
                        input.inset.y + input.inset.height > 1)
                        issues.push({
                            path: `${path}.inset.height`,
                            message: 'Inset exceeds the vertical plot boundary.',
                        });
                    validateOptionalString(input.inset.label, `${path}.inset.label`, 'Inset label', issues);
                    validateCompositionChild(input.inset.base, `${path}.inset.base`, depth + 1, ownsData, issues, state);
                    validateCompositionChild(input.inset.view, `${path}.inset.view`, depth + 1, ownsData, issues, state);
                }
                if (input.spec !== undefined)
                    issues.push({ path: `${path}.spec`, message: 'Inset does not use a template spec.' });
                break;
        }
        state.ancestors.delete(input);
    }
    function validateSpec(input) {
        let materialized = input;
        if (isPlainObject(input)) {
            try {
                materialized = materializeSpecDataflow(input);
            }
            catch (error) {
                if (error instanceof GraflumeError) {
                    return [{ path: error.path ?? '$.dataflow', message: error.message }];
                }
                throw error;
            }
        }
        if (!isPlainObject(materialized))
            return validateUnitSpec(materialized);
        if (presentCompositionOperators(materialized).length === 0)
            return validateUnitSpec(materialized);
        const issues = [];
        validateCompositionNode(materialized, '$', 0, false, issues, {
            ancestors: new WeakSet(),
            views: 0,
        });
        findFunctions(materialized, '$', issues, new WeakSet());
        return issues;
    }
    function assertValidSpec(input) {
        const issues = validateSpec(input);
        if (issues.length === 0)
            return;
        const first = issues[0];
        throw new GraflumeError('INVALID_SPEC', first?.message ?? 'Invalid chart spec.', {
            path: first?.path ?? '$',
            details: { issues },
        });
    }

    const defaultControlLabels = {
        controls: 'Chart controls',
        zoomIn: 'Zoom in',
        zoomOut: 'Zoom out',
        reset: 'Reset view',
        enterFullscreen: 'Enter fullscreen',
        exitFullscreen: 'Exit fullscreen',
        exportPng: 'Download PNG',
        showAnnotations: 'Show annotations',
        hideAnnotations: 'Hide annotations',
        previousFrame: 'Previous frame',
        play: 'Play',
        pause: 'Pause',
        nextFrame: 'Next frame',
        seek: 'Playback position',
        speed: 'Playback speed',
        loop: 'Loop playback',
    };
    const defaultLegendLabels = {
        show: 'Show',
        hide: 'Hide',
    };
    function normalizePadding(input, theme) {
        if (typeof input === 'number') {
            return { top: input, right: input, bottom: input, left: input };
        }
        const themedMargin = theme?.spacing.plotMargin;
        const themedPadding = theme?.spacing.plotPadding;
        return {
            top: input?.top ?? themedPadding?.top ?? themedMargin ?? 24,
            right: input?.right ?? themedPadding?.right ?? themedMargin ?? 24,
            bottom: input?.bottom ?? themedPadding?.bottom ?? themedMargin ?? 44,
            left: input?.left ?? themedPadding?.left ?? themedMargin ?? 56,
        };
    }
    function normalizeTitle(input, theme) {
        if (input === undefined)
            return undefined;
        const align = theme?.typography.titleAlign ?? 'left';
        if (typeof input === 'string')
            return { text: input, align };
        return { ...input, align: input.align ?? align };
    }
    function humanizeField$1(field) {
        return field
            .replace(/([a-z0-9])([A-Z])/g, '$1 $2')
            .replace(/[-_]+/g, ' ')
            .replace(/^\w/, (letter) => letter.toUpperCase());
    }
    function normalizeTooltipField(input) {
        const field = typeof input === 'string' ? input : input.field;
        return {
            field,
            label: typeof input === 'string' ? humanizeField$1(field) : (input.label ?? humanizeField$1(field)),
            format: typeof input === 'string' ? 'auto' : (input.format ?? 'auto'),
            ...(typeof input === 'string' || input.fractionDigits === undefined
                ? {}
                : { fractionDigits: input.fractionDigits }),
            prefix: typeof input === 'string' ? '' : (input.prefix ?? ''),
            suffix: typeof input === 'string' ? '' : (input.suffix ?? ''),
        };
    }
    function normalizeInteraction(input) {
        const hover = input?.hover ?? true;
        const tooltipInput = input?.tooltip;
        const tooltip = !hover || tooltipInput === undefined || tooltipInput === false
            ? false
            : {
                trigger: typeof tooltipInput === 'object' ? (tooltipInput.trigger ?? 'mark') : 'mark',
                ...(typeof tooltipInput === 'object' && tooltipInput.axis !== undefined
                    ? { axis: tooltipInput.axis }
                    : {}),
                ...(typeof tooltipInput === 'object' && tooltipInput.title !== undefined
                    ? { title: tooltipInput.title }
                    : {}),
                fields: typeof tooltipInput === 'object'
                    ? (tooltipInput.fields ?? []).map(normalizeTooltipField)
                    : [],
            };
        const navigationInput = input?.navigation;
        const navigation = navigationInput === undefined || navigationInput === false
            ? false
            : {
                minZoom: typeof navigationInput === 'object' ? (navigationInput.minZoom ?? 1) : 1,
                maxZoom: typeof navigationInput === 'object' ? (navigationInput.maxZoom ?? 6) : 6,
                wheel: typeof navigationInput === 'object'
                    ? (navigationInput.wheel ?? 'modifier')
                    : 'modifier',
                drag: typeof navigationInput === 'object' ? (navigationInput.drag ?? true) : true,
                pinch: typeof navigationInput === 'object' ? (navigationInput.pinch ?? true) : true,
                keyboard: typeof navigationInput === 'object' ? (navigationInput.keyboard ?? true) : true,
            };
        const domainNavigationInput = input?.domainNavigation;
        const domainNavigation = domainNavigationInput === undefined || domainNavigationInput === false
            ? false
            : {
                axes: typeof domainNavigationInput === 'object'
                    ? [...(domainNavigationInput.axes ?? ['x', 'y'])]
                    : ['x', 'y'],
                maxZoom: typeof domainNavigationInput === 'object' ? (domainNavigationInput.maxZoom ?? 64) : 64,
                wheel: typeof domainNavigationInput === 'object'
                    ? (domainNavigationInput.wheel ?? 'modifier')
                    : 'modifier',
                drag: typeof domainNavigationInput === 'object' ? (domainNavigationInput.drag ?? true) : true,
                keyboard: typeof domainNavigationInput === 'object'
                    ? (domainNavigationInput.keyboard ?? true)
                    : true,
            };
        const playbackInput = input?.playback;
        const playback = playbackInput === undefined || playbackInput === false
            ? false
            : typeof playbackInput === 'object'
                ? {
                    field: playbackInput.field,
                    ...(playbackInput.key === undefined ? {} : { key: playbackInput.key }),
                    ...(playbackInput.layerId === undefined ? {} : { layerId: playbackInput.layerId }),
                    mode: playbackInput.mode ?? 'frame',
                    interval: playbackInput.interval ?? 1_000,
                    rate: playbackInput.rate ?? 1,
                    loop: playbackInput.loop ?? false,
                    direction: playbackInput.direction ?? 'forward',
                    range: playbackInput.range === undefined
                        ? false
                        : {
                            ...(playbackInput.range.start === undefined
                                ? {}
                                : { start: playbackInput.range.start }),
                            ...(playbackInput.range.end === undefined
                                ? {}
                                : { end: playbackInput.range.end }),
                        },
                    namedFrames: (playbackInput.namedFrames ?? []).map((frame) => ({ ...frame })),
                    windowSize: playbackInput.windowSize ?? 1,
                    autoplay: playbackInput.autoplay ?? false,
                    transition: playbackInput.transition === undefined || playbackInput.transition === false
                        ? false
                        : {
                            duration: playbackInput.transition.duration ?? 400,
                            easing: playbackInput.transition.easing ?? 'ease-in-out',
                        },
                    filter: playbackInput.filter ?? false,
                }
                : false;
        const controlsInput = input?.controls;
        const controls = controlsInput === undefined || controlsInput === false
            ? false
            : {
                zoom: typeof controlsInput === 'object' ? (controlsInput.zoom ?? false) : true,
                reset: typeof controlsInput === 'object' ? (controlsInput.reset ?? false) : true,
                fullscreen: typeof controlsInput === 'object' ? (controlsInput.fullscreen ?? false) : true,
                export: typeof controlsInput === 'object' ? (controlsInput.export ?? false) : true,
                annotations: typeof controlsInput === 'object' ? (controlsInput.annotations ?? false) : true,
                playback: typeof controlsInput === 'object' ? (controlsInput.playback ?? false) : true,
                labels: {
                    ...defaultControlLabels,
                    ...(typeof controlsInput === 'object' ? controlsInput.labels : undefined),
                },
            };
        const selectionInput = input?.selection;
        const selection = selectionInput === undefined || selectionInput === false
            ? false
            : {
                kind: typeof selectionInput === 'object'
                    ? (selectionInput.kind ?? 'point')
                    : 'point',
                combine: typeof selectionInput === 'object'
                    ? (selectionInput.combine ?? 'union')
                    : 'union',
                mode: typeof selectionInput === 'object' ? (selectionInput.mode ?? 'single') : 'single',
                toggle: typeof selectionInput === 'object' ? (selectionInput.toggle ?? true) : true,
                ...(typeof selectionInput === 'object' && selectionInput.key !== undefined
                    ? { key: selectionInput.key }
                    : {}),
                clearOnBackground: typeof selectionInput === 'object' ? (selectionInput.clearOnBackground ?? true) : true,
                clearOnEscape: typeof selectionInput === 'object' ? (selectionInput.clearOnEscape ?? true) : true,
                ariaLabel: typeof selectionInput === 'object'
                    ? (selectionInput.ariaLabel ?? 'Chart selection')
                    : 'Chart selection',
                ...(typeof selectionInput === 'object' && selectionInput.axis !== undefined
                    ? { axis: selectionInput.axis }
                    : {}),
                xAxis: typeof selectionInput === 'object' ? (selectionInput.xAxis ?? 'x') : 'x',
                yAxis: typeof selectionInput === 'object' ? (selectionInput.yAxis ?? 'y') : 'y',
                maxSelections: typeof selectionInput === 'object' ? (selectionInput.maxSelections ?? 64) : 64,
                maxLassoPoints: typeof selectionInput === 'object' ? (selectionInput.maxLassoPoints ?? 512) : 512,
                minPixelSpan: typeof selectionInput === 'object' ? (selectionInput.minPixelSpan ?? 3) : 3,
                keyboard: typeof selectionInput === 'object' ? (selectionInput.keyboard ?? true) : true,
                keyboardStep: typeof selectionInput === 'object' ? (selectionInput.keyboardStep ?? 8) : 8,
                filter: typeof selectionInput === 'object' ? (selectionInput.filter ?? false) : false,
                linked: typeof selectionInput === 'object' ? (selectionInput.linked ?? false) : false,
                highlight: {
                    fill: typeof selectionInput === 'object'
                        ? (selectionInput.highlight?.fill ?? 'rgba(79,70,229,0.12)')
                        : 'rgba(79,70,229,0.12)',
                    stroke: typeof selectionInput === 'object'
                        ? (selectionInput.highlight?.stroke ?? '#4f46e5')
                        : '#4f46e5',
                    opacity: typeof selectionInput === 'object' ? (selectionInput.highlight?.opacity ?? 1) : 1,
                    lineWidth: typeof selectionInput === 'object'
                        ? (selectionInput.highlight?.lineWidth ?? 2.5)
                        : 2.5,
                    dash: typeof selectionInput === 'object' ? [...(selectionInput.highlight?.dash ?? [])] : [],
                    padding: typeof selectionInput === 'object' ? (selectionInput.highlight?.padding ?? 5) : 5,
                    radius: typeof selectionInput === 'object' ? (selectionInput.highlight?.radius ?? 7) : 7,
                },
            };
        return {
            hover,
            click: input?.click ?? true,
            tooltip,
            navigation,
            domainNavigation,
            playback,
            controls,
            selection,
        };
    }
    function normalizeLegendItem(item, index) {
        return {
            id: item.id ?? `item-${index}`,
            label: item.label,
            ...(item.color === undefined ? {} : { color: item.color }),
            ...(item.layerId === undefined ? {} : { layerId: item.layerId }),
            ...(item.value === undefined ? {} : { value: item.value }),
            symbol: item.symbol ?? 'auto',
        };
    }
    function normalizeLegend(input) {
        if (input === undefined || input === false)
            return false;
        const legend = typeof input === 'object' ? input : {};
        const position = legend.position ?? 'right';
        return {
            visible: legend.visible ?? true,
            mode: legend.mode ?? 'auto',
            position,
            orientation: legend.orientation === undefined || legend.orientation === 'auto'
                ? position === 'top' || position === 'bottom'
                    ? 'horizontal'
                    : 'vertical'
                : legend.orientation,
            ...(legend.title === undefined ? {} : { title: legend.title }),
            ...(legend.field === undefined ? {} : { field: legend.field }),
            ...(legend.layerId === undefined ? {} : { layerId: legend.layerId }),
            items: (legend.items ?? []).map(normalizeLegendItem),
            maxItems: legend.maxItems ?? 24,
            interactive: legend.interactive ?? false,
            labels: { ...defaultLegendLabels, ...legend.labels },
        };
    }
    function cloneDecorationTarget$1(target) {
        switch (target.type) {
            case 'datum':
                return {
                    ...target,
                    ...(Array.isArray(target.rowIndex) ? { rowIndex: [...target.rowIndex] } : {}),
                    ...(target.values === undefined ? {} : { values: [...target.values] }),
                };
            case 'range':
                return {
                    type: 'range',
                    ...(target.x === undefined ? {} : { x: { ...target.x } }),
                    ...(target.y === undefined ? {} : { y: { ...target.y } }),
                };
            case 'layer':
                return { ...target };
            case 'plot':
                return { ...target };
        }
    }
    function cloneAnnotation$2(annotation) {
        return {
            ...annotation,
            target: cloneDecorationTarget$1(annotation.target),
            ...(typeof annotation.connector !== 'object'
                ? {}
                : { connector: { ...annotation.connector, dash: [...(annotation.connector.dash ?? [])] } }),
            ...(annotation.style === undefined ? {} : { style: { ...annotation.style } }),
        };
    }
    function cloneMarkLabelPosition$1(position) {
        return {
            ...position,
            target: cloneDecorationTarget$1(position.target),
        };
    }
    function normalizeMarkLabels(input) {
        if (input === undefined || input === false)
            return false;
        const labels = input === true ? {} : input;
        const connector = labels.connector === undefined || labels.connector === false
            ? false
            : labels.connector === true
                ? {}
                : {
                    ...labels.connector,
                    ...(labels.connector.dash === undefined ? {} : { dash: [...labels.connector.dash] }),
                };
        const authoringInput = labels.authoring;
        const authoring = authoringInput === undefined || authoringInput === false
            ? false
            : (() => {
                const resolved = typeof authoringInput === 'object' ? authoringInput : {};
                const snapInput = resolved.snap;
                const snapResolved = typeof snapInput === 'object' ? snapInput : {};
                return {
                    pointer: resolved.pointer ?? true,
                    keyboard: resolved.keyboard ?? true,
                    step: resolved.step ?? 1,
                    historyLimit: resolved.historyLimit ?? 50,
                    snap: snapInput === false
                        ? false
                        : {
                            grid: snapResolved.grid ?? false,
                            marks: snapResolved.marks ?? true,
                            plot: snapResolved.plot ?? true,
                            distance: snapResolved.distance ?? 6,
                        },
                };
            })();
        return {
            visible: labels.visible ?? true,
            ...(labels.field === undefined ? {} : { field: labels.field }),
            ...(labels.key === undefined ? {} : { key: labels.key }),
            layerIds: [...(labels.layerIds ?? [])],
            placement: labels.placement ?? 'auto',
            offset: labels.offset ?? 6,
            collision: labels.collision ?? 'avoid',
            connector,
            maxLabels: labels.maxLabels ?? 128,
            positions: (labels.positions ?? []).map(cloneMarkLabelPosition$1),
            style: { ...labels.style },
            authoring,
        };
    }
    function axisGridDefault(channel, position, theme) {
        const themed = channel === 'x' && position === 'bottom'
            ? theme?.axis.gridX
            : channel === 'x'
                ? theme?.axis.gridX2
                : position === 'left'
                    ? theme?.axis.gridY
                    : theme?.axis.gridY2;
        return themed ?? (channel === 'y' && position === 'left');
    }
    function normalizeAxisFont(input) {
        return {
            ...(input?.family === undefined ? {} : { family: input.family }),
            ...(input?.size === undefined ? {} : { size: input.size }),
            ...(input?.weight === undefined ? {} : { weight: input.weight }),
            style: input?.style ?? 'normal',
        };
    }
    function normalizeAxisStroke(input, defaultVisible, defaultOpacity = 1) {
        const stroke = typeof input === 'object' ? input : undefined;
        return {
            visible: typeof input === 'boolean' ? input : (stroke?.visible ?? defaultVisible),
            ...(stroke?.color === undefined ? {} : { color: stroke.color }),
            ...(stroke?.width === undefined ? {} : { width: stroke.width }),
            opacity: stroke?.opacity ?? defaultOpacity,
            dash: [...(stroke?.dash ?? [])],
        };
    }
    function normalizeAxisTicks(input, legacyCount, defaultVisible = true) {
        const ticks = typeof input === 'object' ? input : undefined;
        const count = ticks?.count ?? legacyCount;
        return {
            ...normalizeAxisStroke(input, defaultVisible),
            ...(count === undefined ? {} : { count }),
            spacing: ticks?.spacing ?? 0,
            ...(ticks?.size === undefined ? {} : { size: ticks.size }),
            ...(ticks?.values === undefined ? {} : { values: [...ticks.values] }),
        };
    }
    function normalizeAxisLabels(input, legacyAngle) {
        const labels = typeof input === 'object' ? input : undefined;
        const angle = labels?.angle ?? legacyAngle;
        return {
            visible: typeof input === 'boolean' ? input : (labels?.visible ?? true),
            orientation: labels?.orientation ?? 'auto',
            ...(angle === undefined ? {} : { angle }),
            align: labels?.align ?? 'auto',
            ...(labels?.padding === undefined ? {} : { padding: labels.padding }),
            ...(labels?.maxLength === undefined ? {} : { maxLength: labels.maxLength }),
            ...(labels?.color === undefined ? {} : { color: labels.color }),
            font: normalizeAxisFont(labels?.font),
        };
    }
    function normalizeAxisTitle(input, defaultPadding, themeGap) {
        const title = typeof input === 'object' ? input : undefined;
        return {
            ...(typeof input === 'string'
                ? { text: input }
                : title?.text === undefined
                    ? {}
                    : { text: title.text }),
            visible: input === false ? false : (title?.visible ?? true),
            align: title?.align ?? 'center',
            ...(title?.angle === undefined ? {} : { angle: title.angle }),
            padding: title?.padding ?? defaultPadding,
            ...(title?.padding === undefined && themeGap !== undefined ? { themeGap } : {}),
            ...(title?.color === undefined ? {} : { color: title.color }),
            font: normalizeAxisFont(title?.font),
        };
    }
    function normalizeAxisFormat(input) {
        const format = typeof input === 'string' ? { type: input } : (input ?? {});
        const type = format.type ?? 'auto';
        return {
            type,
            ...(format.fractionDigits === undefined ? {} : { fractionDigits: format.fractionDigits }),
            notation: format.notation ??
                (type === 'compact' ? 'compact' : type === 'scientific' ? 'scientific' : 'standard'),
            useGrouping: format.useGrouping ?? true,
            ...(format.currency === undefined && type !== 'currency'
                ? {}
                : { currency: format.currency ?? 'USD' }),
            currencyDisplay: format.currencyDisplay ?? 'symbol',
            dateStyle: format.dateStyle ?? 'medium',
            timeStyle: format.timeStyle ?? 'short',
            timeZone: format.timeZone ?? 'UTC',
            prefix: format.prefix ?? '',
            suffix: format.suffix ?? '',
        };
    }
    function mergeBooleanObject(base, override) {
        if (override === undefined)
            return base;
        if (typeof override === 'boolean')
            return override;
        const baseObject = typeof base === 'object' ? base : base === undefined ? {} : { visible: base };
        return { ...baseObject, ...override };
    }
    function mergeFont(base, override) {
        if (override === undefined)
            return base;
        return { ...base, ...override };
    }
    function mergeLabels(base, override) {
        const merged = mergeBooleanObject(base, override);
        if (typeof merged !== 'object' || typeof override !== 'object')
            return merged;
        const baseFont = typeof base === 'object' ? base.font : undefined;
        const font = mergeFont(baseFont, override.font);
        return { ...merged, ...(font === undefined ? {} : { font }) };
    }
    function mergeTitle(base, override) {
        if (override === undefined)
            return base;
        if (typeof override !== 'object')
            return override;
        const baseObject = typeof base === 'object'
            ? base
            : typeof base === 'string'
                ? { text: base }
                : base === false
                    ? { visible: false }
                    : {};
        const font = mergeFont(baseObject.font, override.font);
        return { ...baseObject, ...override, ...(font === undefined ? {} : { font }) };
    }
    function mergeFormat(base, override) {
        if (override === undefined)
            return base;
        if (typeof override === 'string')
            return override;
        const baseObject = typeof base === 'string' ? { type: base } : base;
        return { ...baseObject, ...override };
    }
    function mergeAxis(base, override) {
        if (override === undefined)
            return base;
        if (override === false)
            return false;
        if (base === false || base === undefined)
            return override;
        const line = mergeBooleanObject(base.line, override.line);
        const grid = mergeBooleanObject(base.grid, override.grid);
        const ticks = mergeBooleanObject(base.ticks, override.ticks);
        const labels = mergeLabels(base.labels, override.labels);
        const title = mergeTitle(base.title, override.title);
        const format = mergeFormat(base.format, override.format);
        return {
            ...base,
            ...override,
            ...(line === undefined ? {} : { line }),
            ...(grid === undefined ? {} : { grid }),
            ...(ticks === undefined ? {} : { ticks }),
            ...(labels === undefined ? {} : { labels }),
            ...(title === undefined ? {} : { title }),
            ...(format === undefined ? {} : { format }),
        };
    }
    function normalizeAxis(input, id, channel, theme) {
        if (input === false)
            return false;
        const position = input?.position ?? defaultAxisPosition(id, channel);
        const titlePadding = channel === 'x' ? 32 : 46;
        return {
            channel,
            visible: input?.visible ?? true,
            position,
            offset: input?.offset ?? 0,
            line: normalizeAxisStroke(input?.line, theme?.axis.lineVisible ?? true),
            grid: normalizeAxisStroke(input?.grid, axisGridDefault(channel, position, theme), theme?.axis.gridOpacity ?? 0.82),
            ticks: normalizeAxisTicks(input?.ticks, input?.tickCount, theme?.axis.ticksVisible ?? true),
            labels: normalizeAxisLabels(input?.labels, input?.labelAngle),
            title: normalizeAxisTitle(input?.title, titlePadding, theme?.axis.titleGap),
            format: normalizeAxisFormat(input?.format),
        };
    }
    function normalizeEncoding(input, channel, chartAxes, theme) {
        const encoding = typeof input === 'string' ? { field: input } : input;
        const axisId = encoding.axisId ?? channel;
        const axis = mergeAxis(chartAxes[axisId], encoding.axis);
        return {
            field: encoding.field,
            ...(encoding.type === undefined ? {} : { type: encoding.type }),
            title: encoding.title ?? encoding.field,
            scale: { ...encoding.scale },
            axisId,
            axis: normalizeAxis(axis, axisId, channel, theme),
        };
    }
    function normalizeChannelEncoding(input, channel) {
        const encoding = typeof input === 'string' ? { field: input } : input;
        const conditions = encoding.condition === undefined
            ? []
            : Array.isArray(encoding.condition)
                ? encoding.condition
                : [encoding.condition];
        return {
            ...(encoding.field === undefined ? {} : { field: encoding.field }),
            ...(encoding.value === undefined ? {} : { value: encoding.value }),
            ...(encoding.type === undefined ? {} : { type: encoding.type }),
            ...(encoding.title === undefined ? {} : { title: encoding.title }),
            scale: { ...encoding.scale },
            ...(encoding.axisId === undefined ? {} : { axisId: encoding.axisId }),
            condition: [...conditions],
        };
    }
    function normalizeEncodingMap(legacyX, legacyY, input, chartAxes, theme) {
        const x = input?.x ?? legacyX;
        const y = input?.y ?? legacyY;
        if (x === undefined || y === undefined) {
            throw new Error('Spec validation should guarantee x and y encodings.');
        }
        const normalized = {};
        if (input !== undefined) {
            for (const [channel, encoding] of Object.entries(input)) {
                if (encoding === undefined || channel === 'x' || channel === 'y')
                    continue;
                normalized[channel] = normalizeChannelEncoding(encoding);
            }
        }
        return {
            x: normalizeEncoding(x, 'x', chartAxes, theme),
            y: normalizeEncoding(y, 'y', chartAxes, theme),
            ...normalized,
        };
    }
    function normalizeMark(input) {
        const mark = typeof input === 'string' ? { type: input } : input;
        return {
            type: mark.type,
            ...(mark.stroke === undefined ? {} : { stroke: mark.stroke }),
            ...(mark.fill === undefined ? {} : { fill: mark.fill }),
            opacity: mark.opacity ?? 1,
            ...(mark.lineWidth === undefined ? {} : { lineWidth: mark.lineWidth }),
            ...(mark.radius === undefined ? {} : { radius: mark.radius }),
            ...(mark.cornerRadius === undefined ? {} : { cornerRadius: mark.cornerRadius }),
            point: mark.point ?? false,
            position: mark.position ?? 'overlay',
            orientation: mark.orientation ?? 'vertical',
            fields: { ...mark.fields },
            options: { ...mark.options },
        };
    }
    function resolveSpecializedEncoding(markType, input) {
        if (input === undefined)
            return undefined;
        const specialized = { ...input };
        if (['map', 'geo-flow', 'geo-line', 'geo-heatmap', 'tiled-map', 'tilemap'].includes(markType)) {
            if (specialized.x === undefined && specialized.longitude !== undefined) {
                specialized.x = specialized.longitude;
            }
            if (specialized.y === undefined && specialized.latitude !== undefined) {
                specialized.y = specialized.latitude;
            }
        }
        if (markType === 'polar') {
            const angle = specialized.theta ?? specialized.angle;
            if (specialized.x === undefined && angle !== undefined)
                specialized.x = angle;
            if (specialized.y === undefined && specialized.radius !== undefined) {
                specialized.y = specialized.radius;
            }
        }
        if (markType === 'pie' || markType === 'variable-pie') {
            const angle = specialized.theta ?? specialized.angle;
            if (specialized.y === undefined && angle !== undefined)
                specialized.y = angle;
        }
        if (['candlestick', 'financial', 'renko', 'point-figure', 'indicator'].includes(markType)) {
            if (specialized.y === undefined && specialized.close !== undefined) {
                specialized.y = specialized.close;
            }
        }
        return specialized;
    }
    function normalizeLayer(layer, index, parentData, chartAxes, theme, parentTransforms) {
        const data = layer.data ?? parentData;
        if (data === undefined) {
            throw new Error('Spec validation should guarantee layer data.');
        }
        const markType = typeof layer.mark === 'string' ? layer.mark : layer.mark.type;
        const specialized = resolveSpecializedEncoding(markType, layer.encoding);
        const encoding = normalizeEncodingMap(layer.x, layer.y, specialized, chartAxes, theme);
        const normalizedMark = normalizeMark(layer.mark);
        const semanticFields = Object.fromEntries(['longitude', 'latitude', 'open', 'high', 'low', 'close', 'volume', 'angle', 'theta'].flatMap((channel) => {
            const field = encoding[channel]?.field;
            return field === undefined ? [] : [[channel, field]];
        }));
        return {
            id: layer.id ?? `layer-${index}`,
            name: layer.name ?? layer.id ?? `Series ${index + 1}`,
            data,
            transform: [...parentTransforms, ...(layer.transform ?? [])],
            mark: {
                ...normalizedMark,
                fields: { ...normalizedMark.fields, ...semanticFields },
            },
            encoding,
            x: encoding.x,
            y: encoding.y,
            clip: layer.clip === undefined || layer.clip === true
                ? true
                : layer.clip === false
                    ? false
                    : layer.clip.type === 'plot'
                        ? { ...layer.clip }
                        : {
                            type: 'domain',
                            ...(layer.clip.x === undefined ? {} : { x: { ...layer.clip.x } }),
                            ...(layer.clip.y === undefined ? {} : { y: { ...layer.clip.y } }),
                        },
            visible: layer.visible ?? true,
            zIndex: layer.zIndex ?? index,
        };
    }
    function normalizeSpec$1(input, resolvedTheme) {
        input = materializeSpecDataflow(input);
        assertValidSpec(input);
        // Keep the one-argument public normalizer registry-driven so every built-in
        // theme receives its structural defaults without constructing a runtime.
        const theme = resolvedTheme ?? (typeof input.theme === 'string' ? builtInTheme(input.theme) : undefined);
        const chartAxes = input.axes ?? {};
        const axes = Object.freeze(Object.fromEntries(declaredAxisIds(chartAxes).map((id) => {
            const channel = axisChannel(id, chartAxes) ?? builtInAxisChannel(id);
            if (channel === undefined) {
                throw new Error(`Spec validation should guarantee a channel for named axis "${id}".`);
            }
            return [id, normalizeAxis(chartAxes[id], id, channel, theme)];
        })));
        const shorthandEncoding = resolveSpecializedEncoding(typeof input.mark === 'string' ? input.mark : (input.mark?.type ?? ''), input.encoding);
        const shorthandLayer = input.mark === undefined ||
            ((shorthandEncoding?.x === undefined || shorthandEncoding.y === undefined) &&
                (input.x === undefined || input.y === undefined))
            ? undefined
            : {
                ...(input.data === undefined ? {} : { data: input.data }),
                mark: input.mark,
                ...(shorthandEncoding === undefined
                    ? { x: input.x, y: input.y }
                    : { encoding: shorthandEncoding }),
            };
        const sourceLayers = input.layers ?? (shorthandLayer === undefined ? [] : [shorthandLayer]);
        const layers = sourceLayers.map((layer, index) => normalizeLayer(layer, index, input.data, chartAxes, theme, input.transform ?? []));
        const title = normalizeTitle(input.title, theme);
        const normalized = {
            specVersion,
            layers,
            width: input.width ?? 'container',
            height: input.height ?? 400,
            padding: normalizePadding(input.padding, theme),
            renderer: input.renderer ?? 'auto',
            performance: input.performance ?? 'auto',
            theme: input.theme ?? defaultThemeId,
            axes,
            legend: normalizeLegend(input.legend),
            highlights: (input.highlights ?? []).map((highlight) => ({
                ...highlight,
                target: cloneDecorationTarget$1(highlight.target),
                ...(highlight.dash === undefined ? {} : { dash: [...highlight.dash] }),
            })),
            annotations: (input.annotations ?? []).map(cloneAnnotation$2),
            markLabels: normalizeMarkLabels(input.markLabels),
            interaction: normalizeInteraction(input.interaction),
            accessibility: {
                ...(input.accessibility?.label === undefined ? {} : { label: input.accessibility.label }),
                ...(input.accessibility?.description === undefined
                    ? {}
                    : { description: input.accessibility.description }),
                table: input.accessibility?.table === true
                    ? 'hidden'
                    : input.accessibility?.table === false || input.accessibility?.table === undefined
                        ? false
                        : input.accessibility.table,
                maxRows: input.accessibility?.maxRows ?? 500,
                navigation: input.accessibility?.navigation ?? false,
                explorer: input.accessibility?.explorer === false ||
                    (input.accessibility?.explorer === undefined &&
                        input.accessibility?.navigation !== true &&
                        input.accessibility?.linkedFocus === undefined &&
                        input.accessibility?.table !== 'visible')
                    ? false
                    : {
                        windowRows: typeof input.accessibility?.explorer === 'object'
                            ? (input.accessibility.explorer.windowRows ?? 24)
                            : 24,
                        overscanRows: typeof input.accessibility?.explorer === 'object'
                            ? (input.accessibility.explorer.overscanRows ?? 6)
                            : 6,
                        rowHeight: typeof input.accessibility?.explorer === 'object'
                            ? (input.accessibility.explorer.rowHeight ?? 32)
                            : 32,
                    },
                linkedFocus: input.accessibility?.linkedFocus === undefined
                    ? false
                    : { ...input.accessibility.linkedFocus },
                ...(input.accessibility?.summary === undefined
                    ? {}
                    : { summary: input.accessibility.summary }),
                live: input.accessibility?.live === false ||
                    (typeof input.accessibility?.live === 'object' &&
                        input.accessibility.live.enabled === false)
                    ? false
                    : {
                        throttleMs: typeof input.accessibility?.live === 'object'
                            ? (input.accessibility.live.throttleMs ?? 150)
                            : 150,
                    },
            },
            ...(title === undefined ? {} : { title }),
            ...(input.description === undefined ? {} : { description: input.description }),
            ...(input.locale === undefined ? {} : { locale: input.locale }),
        };
        return normalized;
    }

    function normalizedHex(color) {
        const value = color.trim().replace(/^#/, '');
        if (/^[0-9a-f]{3}$/i.test(value)) {
            return value
                .split('')
                .map((channel) => `${channel}${channel}`)
                .join('');
        }
        return /^[0-9a-f]{6}$/i.test(value) ? value : null;
    }
    function hexColor(channels, uppercase = false) {
        const value = `#${channels
        .map((channelValue) => Math.round(Math.max(0, Math.min(255, channelValue)))
        .toString(16)
        .padStart(2, '0'))
        .join('')}`;
        return uppercase ? value.toUpperCase() : value;
    }
    function linearToSrgb(value) {
        return value <= 0.0031308 ? 12.92 * value : 1.055 * value ** (1 / 2.4) - 0.055;
    }
    function srgbToLinear(value) {
        return value <= 0.04045 ? value / 12.92 : ((value + 0.055) / 1.055) ** 2.4;
    }
    /** Convert the polar CIELUV scale used by grDevices::hcl() into clipped sRGB. */
    function hclColor(hue, chroma, luminance) {
        const radians = (hue * Math.PI) / 180;
        const u = Math.cos(radians) * chroma;
        const v = Math.sin(radians) * chroma;
        const referenceU = 0.19783000664283;
        const referenceV = 0.46831999493879;
        const y = ((luminance + 16) / 116) ** 3 ;
        const uPrime = u / (13 * luminance) + referenceU;
        const vPrime = v / (13 * luminance) + referenceV;
        const x = (9 * y * uPrime) / (4 * vPrime);
        const z = (y * (12 - 3 * uPrime - 20 * vPrime)) / (4 * vPrime);
        const red = linearToSrgb(3.2404542 * x - 1.5371385 * y - 0.4985314 * z);
        const green = linearToSrgb(-0.969266 * x + 1.8760108 * y + 0.041556 * z);
        const blue = linearToSrgb(0.0556434 * x - 0.2040259 * y + 1.0572252 * z);
        return hexColor([red * 255, green * 255, blue * 255], true);
    }
    /** Resolve a category colour while preserving fixed-palette behaviour for existing themes. */
    function categoricalColor(theme, index, count) {
        const paletteSize = Number.isFinite(count) ? Math.max(1, Math.floor(count)) : 1;
        const paletteIndex = Number.isFinite(index) ? Math.floor(index) : 0;
        const normalizedIndex = ((paletteIndex % paletteSize) + paletteSize) % paletteSize;
        if (theme.colors.paletteMode === 'ggplot2-hue') {
            return hclColor(15 + (360 * normalizedIndex) / paletteSize, 100, 65);
        }
        const palette = theme.colors.palette;
        if (palette.length === 0)
            return theme.colors.focus;
        const fixedIndex = ((paletteIndex % palette.length) + palette.length) % palette.length;
        return palette[fixedIndex] ?? theme.colors.focus;
    }
    function labFunction(value) {
        const delta = 6 / 29;
        return value > delta ** 3 ? Math.cbrt(value) : value / (3 * delta ** 2) + 4 / 29;
    }
    function inverseLabFunction(value) {
        const delta = 6 / 29;
        return value > delta ? value ** 3 : 3 * delta ** 2 * (value - 4 / 29);
    }
    function hexToLab(color) {
        const hex = normalizedHex(color);
        if (hex === null)
            return null;
        const red = srgbToLinear(channel$1(hex, 0) / 255);
        const green = srgbToLinear(channel$1(hex, 1) / 255);
        const blue = srgbToLinear(channel$1(hex, 2) / 255);
        const x = (0.4124564 * red + 0.3575761 * green + 0.1804375 * blue) / 0.95047;
        const y = 0.2126729 * red + 0.7151522 * green + 0.072175 * blue;
        const z = (0.0193339 * red + 0.119192 * green + 0.9503041 * blue) / 1.08883;
        const fx = labFunction(x);
        const fy = labFunction(y);
        const fz = labFunction(z);
        return { l: 116 * fy - 16, a: 500 * (fx - fy), b: 200 * (fy - fz) };
    }
    function labToHex(color) {
        const fy = (color.l + 16) / 116;
        const fx = fy + color.a / 500;
        const fz = fy - color.b / 200;
        const x = 0.95047 * inverseLabFunction(fx);
        const y = inverseLabFunction(fy);
        const z = 1.08883 * inverseLabFunction(fz);
        const red = linearToSrgb(3.2404542 * x - 1.5371385 * y - 0.4985314 * z);
        const green = linearToSrgb(-0.969266 * x + 1.8760108 * y + 0.041556 * z);
        const blue = linearToSrgb(0.0556434 * x - 0.2040259 * y + 1.0572252 * z);
        return hexColor([red * 255, green * 255, blue * 255], true);
    }
    function mixLabColor(start, end, ratio) {
        const startLab = hexToLab(start);
        const endLab = hexToLab(end);
        if (startLab === null || endLab === null)
            return mixColor$1(start, end, ratio);
        const bounded = Number.isFinite(ratio) ? Math.max(0, Math.min(1, ratio)) : 0;
        return labToHex({
            l: startLab.l + (endLab.l - startLab.l) * bounded,
            a: startLab.a + (endLab.a - startLab.a) * bounded,
            b: startLab.b + (endLab.b - startLab.b) * bounded,
        });
    }
    /** Resolve a continuous colour; an explicit interpolation token wins over palette defaults. */
    function continuousColor(theme, ratio) {
        const palette = theme.colors.sequential;
        if (palette.length === 0)
            return theme.colors.focus;
        if (palette.length === 1)
            return palette[0] ?? theme.colors.focus;
        const bounded = Number.isFinite(ratio) ? Math.max(0, Math.min(1, ratio)) : 0;
        if (theme.colors.paletteMode === 'ggplot2-hue' &&
            theme.colors.continuousInterpolation === undefined) {
            return mixLabColor(palette[0] ?? theme.colors.focus, palette[palette.length - 1] ?? theme.colors.focus, bounded);
        }
        if (theme.colors.continuousInterpolation === 'rgb' ||
            theme.colors.continuousInterpolation === 'lab') {
            const scaled = bounded * (palette.length - 1);
            const startIndex = Math.min(palette.length - 2, Math.floor(scaled));
            const start = palette[startIndex] ?? theme.colors.focus;
            const end = palette[startIndex + 1] ?? start;
            const localRatio = scaled - startIndex;
            return theme.colors.continuousInterpolation === 'lab'
                ? mixLabColor(start, end, localRatio)
                : mixColor$1(start, end, localRatio);
        }
        if (theme.colors.continuousInterpolation === 'step') {
            const index = Math.min(palette.length - 1, Math.floor(bounded * palette.length));
            return palette[index] ?? theme.colors.focus;
        }
        return palette[Math.round(bounded * (palette.length - 1))] ?? theme.colors.focus;
    }
    function channel$1(color, index) {
        return Number.parseInt(color.slice(index * 2, index * 2 + 2), 16);
    }
    function mixColor$1(start, end, ratio) {
        const startHex = normalizedHex(start);
        const endHex = normalizedHex(end);
        if (startHex === null || endHex === null)
            return ratio < 0.5 ? start : end;
        const bounded = Math.max(0, Math.min(1, ratio));
        const channels = [0, 1, 2].map((index) => Math.round(channel$1(startHex, index) + (channel$1(endHex, index) - channel$1(startHex, index)) * bounded));
        return `#${channels.map((value) => value.toString(16).padStart(2, '0')).join('')}`;
    }
    function colorWithOpacity(color, opacity) {
        const hex = normalizedHex(color);
        if (hex === null)
            return color;
        const alpha = Math.round(Math.max(0, Math.min(1, opacity)) * 255)
            .toString(16)
            .padStart(2, '0');
        return `#${hex}${alpha}`;
    }
    function readableTextColor(color, light, dark) {
        const hex = normalizedHex(color);
        if (hex === null)
            return light;
        const linear = [0, 1, 2].map((index) => {
            const value = channel$1(hex, index) / 255;
            return value <= 0.03928 ? value / 12.92 : ((value + 0.055) / 1.055) ** 2.4;
        });
        const luminance = 0.2126 * (linear[0] ?? 0) + 0.7152 * (linear[1] ?? 0) + 0.0722 * (linear[2] ?? 0);
        return luminance > 0.42 ? dark : light;
    }

    const provenanceLimit = 1_024;
    const tableColumnLimit = 24;
    function boundedText$1(value, limit = 240) {
        const text = value instanceof Date ? value.toISOString() : String(value ?? '');
        const characters = Array.from(text);
        return characters.length <= limit
            ? text
            : `${characters.slice(0, Math.max(1, limit - 1)).join('')}\u2026`;
    }
    function formatValue$1(value, locale) {
        if (value === null || value === undefined)
            return '\u2014';
        if (value instanceof Date)
            return value.toISOString();
        if (typeof value === 'number' && Number.isFinite(value)) {
            try {
                return new Intl.NumberFormat(locale, { maximumFractionDigits: 6 }).format(value);
            }
            catch {
                return String(value);
            }
        }
        return boundedText$1(value, 80);
    }
    function unionBounds(left, right) {
        const x = Math.min(left.x, right.x);
        const y = Math.min(left.y, right.y);
        const rightEdge = Math.max(left.x + left.width, right.x + right.width);
        const bottomEdge = Math.max(left.y + left.height, right.y + right.height);
        return { x, y, width: rightEdge - x, height: bottomEdge - y };
    }
    function intersectBounds(bounds, clip) {
        const x = Math.max(bounds.x, clip.x);
        const y = Math.max(bounds.y, clip.y);
        const right = Math.min(bounds.x + bounds.width, clip.x + clip.width);
        const bottom = Math.min(bounds.y + bounds.height, clip.y + clip.height);
        return { x, y, width: Math.max(0, right - x), height: Math.max(0, bottom - y) };
    }
    function nodeBounds(node) {
        switch (node.type) {
            case 'circle':
                return {
                    x: node.cx - node.radius - node.lineWidth / 2,
                    y: node.cy - node.radius - node.lineWidth / 2,
                    width: node.radius * 2 + node.lineWidth,
                    height: node.radius * 2 + node.lineWidth,
                };
            case 'rect': {
                const inset = node.lineWidth / 2;
                return {
                    x: Math.min(node.x, node.x + node.width) - inset,
                    y: Math.min(node.y, node.y + node.height) - inset,
                    width: Math.abs(node.width) + inset * 2,
                    height: Math.abs(node.height) + inset * 2,
                };
            }
            case 'line': {
                const inset = node.lineWidth / 2;
                return {
                    x: Math.min(node.x1, node.x2) - inset,
                    y: Math.min(node.y1, node.y2) - inset,
                    width: Math.abs(node.x2 - node.x1) + inset * 2,
                    height: Math.abs(node.y2 - node.y1) + inset * 2,
                };
            }
            case 'path': {
                const points = [node.points, ...(node.subpaths ?? [])].flat();
                if (points.length === 0)
                    return { x: 0, y: 0, width: 0, height: 0 };
                const inset = node.lineWidth / 2;
                const xs = points.map(({ x }) => x);
                const ys = points.map(({ y }) => y);
                const x = Math.min(...xs);
                const y = Math.min(...ys);
                return {
                    x: x - inset,
                    y: y - inset,
                    width: Math.max(...xs) - x + inset * 2,
                    height: Math.max(...ys) - y + inset * 2,
                };
            }
            case 'text': {
                const width = Math.max(node.fontSize * 0.6, Array.from(node.text).length * node.fontSize * 0.6);
                const height = Math.max(1, node.fontSize * 1.2);
                const x = node.align === 'center'
                    ? node.x - width / 2
                    : node.align === 'right' || node.align === 'end'
                        ? node.x - width
                        : node.x;
                const y = node.baseline === 'middle'
                    ? node.y - height / 2
                    : node.baseline === 'bottom' || node.baseline === 'ideographic'
                        ? node.y - height
                        : node.baseline === 'alphabetic'
                            ? node.y - height * 0.8
                            : node.y;
                // Rotation is represented by its safe axis-aligned envelope.
                const radians = (Math.abs(node.rotation) * Math.PI) / 180;
                const rotatedWidth = Math.abs(width * Math.cos(radians)) + Math.abs(height * Math.sin(radians));
                const rotatedHeight = Math.abs(width * Math.sin(radians)) + Math.abs(height * Math.cos(radians));
                return {
                    x: x - Math.max(0, rotatedWidth - width) / 2,
                    y: y - Math.max(0, rotatedHeight - height) / 2,
                    width: rotatedWidth,
                    height: rotatedHeight,
                };
            }
        }
    }
    function collectNodeDatumBounds(root) {
        const output = [];
        const visit = (node, parentVisible, parentOpacity, clips) => {
            const visible = parentVisible && node.visible && parentOpacity * node.opacity > 0;
            if (node.type === 'group') {
                const nextClips = node.clip === undefined ? clips : [...clips, node.clip];
                for (const child of node.children)
                    visit(child, visible, parentOpacity * node.opacity, nextClips);
                return;
            }
            if (node.datum === undefined)
                return;
            const bounds = clips.reduce(intersectBounds, nodeBounds(node));
            output.push({
                nodeId: node.id,
                layerId: node.datum.layerId,
                rowIndex: node.datum.rowIndex,
                datum: node.datum.datum,
                ...(node.datum.tooltip === undefined ? {} : { tooltip: node.datum.tooltip }),
                bounds,
                visible: visible && bounds.width >= 0 && bounds.height >= 0,
            });
        };
        visit(root, true, 1, []);
        return output;
    }
    function valuesDiffer(left, right) {
        const keys = new Set([...Object.keys(left), ...Object.keys(right)]);
        return [...keys].some((key) => !Object.is(left[key], right[key]));
    }
    function publicDatum(row) {
        return Object.fromEntries(Object.entries(row).filter(([field]) => !field.startsWith('__graflume_')));
    }
    function channels(layer, row) {
        return {
            x: { field: layer.layer.x.field, value: row[layer.layer.x.field] },
            y: { field: layer.layer.y.field, value: row[layer.layer.y.field] },
        };
    }
    function semanticLabel(layer, row, locale) {
        const x = channels(layer, row).x;
        const y = channels(layer, row).y;
        return boundedText$1(`${layer.layer.name}. ${layer.layer.x.title}: ${formatValue$1(x.value, locale)}. ${layer.layer.y.title}: ${formatValue$1(y.value, locale)}.`);
    }
    function lineageFor(layer, rowIndex, aggregate, tooltip) {
        const explicit = tooltip?.sourceRowIndices;
        const explicitRows = Array.isArray(explicit)
            ? explicit.filter((value) => typeof value === 'number' && Number.isInteger(value) && value >= 0)
            : undefined;
        const sourceRows = explicitRows !== undefined
            ? explicitRows
            : aggregate
                ? layer.lineage.rowSources.flat()
                : (layer.lineage.rowSources[rowIndex] ?? []);
        const unique = [...new Set(sourceRows)].sort((left, right) => left - right);
        const authoredCount = tooltip?.sourceRowCount;
        const sourceCount = typeof authoredCount === 'number' && Number.isInteger(authoredCount) && authoredCount >= 0
            ? authoredCount
            : unique.length;
        return {
            sourceId: layer.lineage.sourceId,
            sourceRowIndices: unique.slice(0, provenanceLimit),
            truncated: sourceCount > unique.length || unique.length > provenanceLimit,
        };
    }
    function mappedObservationBounds(layer, row) {
        const xValue = row[layer.layer.x.field];
        const yValue = row[layer.layer.y.field];
        const scalar = (value) => typeof value === 'string' || typeof value === 'number' || value instanceof Date ? value : null;
        const xInput = scalar(xValue);
        const yInput = scalar(yValue);
        if (xInput === null || yInput === null) {
            return { x: 0, y: 0, width: 0, height: 0 };
        }
        const x = layer.xScale.map(xInput);
        const y = layer.yScale.map(yInput);
        if (!Number.isFinite(x) || !Number.isFinite(y))
            return { x: 0, y: 0, width: 0, height: 0 };
        return { x: x - 4, y: y - 4, width: 8, height: 8 };
    }
    /** Build the semantic sidecar without depending on any renderer implementation. */
    function buildSemanticIndex(root, layers, maxRows, locale) {
        const nodeData = collectNodeDatumBounds(root);
        const candidates = layers.map((layer) => {
            const layerNodes = nodeData.filter(({ layerId }) => layerId === layer.layer.id);
            const rowBounds = new Map();
            for (const item of layerNodes) {
                const current = rowBounds.get(item.rowIndex);
                rowBounds.set(item.rowIndex, {
                    bounds: current === undefined ? item.bounds : unionBounds(current.bounds, item.bounds),
                    visible: (current?.visible ?? false) || item.visible,
                });
            }
            const records = [];
            const retainedRows = Math.min(layer.table.length, maxRows);
            for (let rowIndex = 0; rowIndex < retainedRows; rowIndex += 1) {
                const row = layer.table.row(rowIndex);
                const datum = publicDatum(row);
                const measured = rowBounds.get(rowIndex);
                records.push({
                    id: `${layer.layer.id}:observation:${rowIndex}`,
                    viewId: 'plot',
                    layerId: layer.layer.id,
                    rowIndex,
                    role: layer.layer.mark.type,
                    channels: channels(layer, row),
                    datum,
                    lineage: lineageFor(layer, rowIndex, false),
                    bounds: measured?.bounds ?? mappedObservationBounds(layer, row),
                    visible: layer.layer.visible && (measured?.visible ?? true),
                    label: semanticLabel(layer, row, locale),
                });
            }
            for (const item of layerNodes) {
                if (records.length >= maxRows)
                    break;
                if (item.tooltip === undefined || !valuesDiffer(item.datum, item.tooltip))
                    continue;
                const row = item.tooltip;
                records.push({
                    id: `${item.nodeId}:derived`,
                    viewId: 'plot',
                    layerId: layer.layer.id,
                    rowIndex: item.rowIndex,
                    role: `${layer.layer.mark.type}-aggregate`,
                    channels: channels(layer, row),
                    datum: row,
                    lineage: lineageFor(layer, item.rowIndex, true, item.tooltip),
                    bounds: item.bounds,
                    visible: layer.layer.visible && item.visible,
                    label: semanticLabel(layer, row, locale),
                });
            }
            return records;
        });
        const bounded = [];
        for (let index = 0; bounded.length < maxRows; index += 1) {
            let appended = false;
            for (const layer of candidates) {
                const record = layer[index];
                if (record === undefined)
                    continue;
                bounded.push(record);
                appended = true;
                if (bounded.length >= maxRows)
                    break;
            }
            if (!appended)
                break;
        }
        return bounded;
    }
    /** Convert semantic marks into bounded, renderer-independent native-table rows. */
    function toAccessibleRows(semanticIndex, maxRows = semanticIndex.length) {
        return semanticIndex.slice(0, Math.max(0, maxRows)).map((mark) => ({
            id: mark.id,
            layerId: mark.layerId,
            role: mark.role,
            label: mark.label,
            values: Object.fromEntries(Object.entries(mark.datum).slice(0, tableColumnLimit)),
            visible: mark.visible,
        }));
    }

    const unsafeFields = new Set(['__proto__', 'prototype', 'constructor']);
    function fieldName$1(value, path) {
        if (value.trim() === '' || unsafeFields.has(value)) {
            throw new GraflumeError('INVALID_SPEC', `${path} must be a safe non-empty field name.`, {
                path,
            });
        }
        return value;
    }
    function rowsFrom$1(input) {
        const table = DataTable.from(input);
        return Array.from({ length: table.length }, (_, index) => table.row(index));
    }
    function finite$b(value) {
        if (typeof value === 'number')
            return Number.isFinite(value) ? value : null;
        if (value instanceof Date)
            return Number.isFinite(value.getTime()) ? value.getTime() : null;
        if (typeof value === 'string' && value.trim() !== '') {
            const numeric = Number(value);
            if (Number.isFinite(numeric))
                return numeric;
        }
        return null;
    }
    function timestamp(value, path) {
        const resolved = value instanceof Date
            ? value.getTime()
            : typeof value === 'number'
                ? value
                : typeof value === 'string'
                    ? Date.parse(value)
                    : Number.NaN;
        if (!Number.isFinite(resolved)) {
            throw new GraflumeError('INVALID_DATA', `${path} must be a finite timestamp.`, { path });
        }
        return resolved;
    }
    function positiveInteger(value, path, maximum = 100_000) {
        if (!Number.isInteger(value) || value < 1 || value > maximum) {
            throw new GraflumeError('INVALID_SPEC', `${path} must be an integer from 1 to ${maximum}.`, {
                path,
            });
        }
        return value;
    }
    function quantile$2(sorted, probability) {
        if (sorted.length === 0)
            return null;
        const position = Math.max(0, Math.min(1, probability)) * (sorted.length - 1);
        const lower = Math.floor(position);
        const upper = Math.ceil(position);
        return sorted[lower] + (sorted[upper] - sorted[lower]) * (position - lower);
    }
    /** Exact weighted-count contract used by bars, histograms, and grouped summaries. */
    function weightedCount(input, weightField) {
        const rows = rowsFrom$1(input);
        if (weightField === undefined)
            return rows.length;
        fieldName$1(weightField, '$.weightField');
        return rows.reduce((sum, row, index) => {
            const weight = finite$b(row[weightField]);
            if (weight === null || weight < 0) {
                throw new GraflumeError('INVALID_DATA', `Weight at row ${index} must be a finite non-negative number.`, { path: `$.data[${index}].${weightField}` });
            }
            return sum + weight;
        }, 0);
    }
    function normalizedAreaValue(value, zero, negative) {
        if (!Number.isFinite(value))
            return null;
        if (value < 0) {
            if (negative === 'reject') {
                throw new GraflumeError('INVALID_DATA', 'Negative bubble sizes require an explicit policy.');
            }
            if (negative === 'hide')
                return null;
            return Math.abs(value);
        }
        if (value === 0 && zero === 'hide')
            return null;
        return value;
    }
    /** Maps a quantitative value so visual circle area, rather than radius, is proportional. */
    function areaRadius(value, domain, options = {}) {
        const minimum = options.minRadius ?? 3;
        const maximum = options.maxRadius ?? 24;
        if (![minimum, maximum, domain[0], domain[1]].every(Number.isFinite) || minimum < 0) {
            throw new GraflumeError('INVALID_SPEC', 'Bubble radius and domain values must be finite.');
        }
        if (maximum < minimum || domain[1] < domain[0]) {
            throw new GraflumeError('INVALID_SPEC', 'Bubble ranges and domains must be ascending.');
        }
        const normalized = normalizedAreaValue(value, options.zero ?? 'minimum', options.negative ?? 'reject');
        if (normalized === null)
            return null;
        if (normalized === 0 && (options.zero ?? 'minimum') === 'zero')
            return 0;
        const lower = Math.max(0, domain[0]);
        const upper = Math.max(lower, domain[1]);
        const ratio = upper === lower ? 0.5 : Math.max(0, Math.min(1, (normalized - lower) / (upper - lower)));
        const minimumArea = Math.PI * minimum * minimum;
        const maximumArea = Math.PI * maximum * maximum;
        return Math.sqrt((minimumArea + ratio * (maximumArea - minimumArea)) / Math.PI);
    }
    /** Generates an explicit, truthful size guide from the same absolute-area scale. */
    function areaSizeGuide(domain, options = {}) {
        const count = positiveInteger(options.count ?? 3, '$.count', 12);
        return Array.from({ length: count }, (_, index) => {
            const value = count === 1 ? domain[1] : domain[0] + ((domain[1] - domain[0]) * index) / (count - 1);
            const radius = areaRadius(value, domain, options) ?? 0;
            return { value, radius, area: Math.PI * radius * radius };
        });
    }
    // Stable CLDR-derived fallback rules for engines without Intl.Locale week information.
    const saturdayFirstRegions = new Set([
        'AF',
        'BH',
        'DJ',
        'DZ',
        'EG',
        'IQ',
        'IR',
        'JO',
        'KW',
        'LY',
        'OM',
        'QA',
        'SD',
        'SY',
    ]);
    const sundayFirstRegions = new Set([
        'AG',
        'AS',
        'BD',
        'BR',
        'BS',
        'BT',
        'BW',
        'BZ',
        'CA',
        'CN',
        'CO',
        'DM',
        'DO',
        'ET',
        'GT',
        'GU',
        'HK',
        'HN',
        'ID',
        'IL',
        'IN',
        'JM',
        'JP',
        'KE',
        'KH',
        'KR',
        'LA',
        'MH',
        'MM',
        'MO',
        'MT',
        'MX',
        'MZ',
        'NI',
        'NP',
        'PA',
        'PE',
        'PH',
        'PK',
        'PR',
        'PT',
        'PY',
        'SA',
        'SG',
        'SV',
        'TH',
        'TT',
        'TW',
        'UM',
        'US',
        'VE',
        'VI',
        'WS',
        'YE',
        'ZA',
        'ZW',
    ]);
    function explicitCalendarWeekStart(value) {
        if (value === undefined)
            return undefined;
        if (!Number.isInteger(value) || value < 0 || value > 6) {
            throw new GraflumeError('INVALID_SPEC', '$.weekStart must be an integer from 0 to 6.', {
                path: '$.weekStart',
            });
        }
        return value;
    }
    function canonicalCalendarLocale(locale) {
        try {
            return Intl.getCanonicalLocales(locale)[0] ?? locale;
        }
        catch {
            throw new GraflumeError('INVALID_SPEC', `Unsupported calendar locale "${locale}".`, {
                path: '$.locale',
            });
        }
    }
    function localeRegion(locale, runtimeLocale) {
        const runtimeRegion = runtimeLocale?.region ?? runtimeLocale?.maximize?.().region;
        if (runtimeRegion !== undefined)
            return runtimeRegion.toUpperCase();
        return locale
            .split('-')
            .slice(1)
            .find((part) => /^(?:[A-Z]{2}|\d{3})$/u.test(part.toUpperCase()))
            ?.toUpperCase();
    }
    /** Resolves the locale's first weekday, while always honoring an explicit override. */
    function calendarWeekStart(options = {}) {
        const explicit = explicitCalendarWeekStart(options.weekStart);
        if (explicit !== undefined)
            return explicit;
        if (options.locale === undefined)
            return 1;
        const canonical = canonicalCalendarLocale(options.locale);
        const LocaleConstructor = Intl
            .Locale;
        let runtimeLocale;
        if (LocaleConstructor !== undefined) {
            try {
                runtimeLocale = new LocaleConstructor(canonical);
            }
            catch {
                throw new GraflumeError('INVALID_SPEC', `Unsupported calendar locale "${options.locale}".`, {
                    path: '$.locale',
                });
            }
            const info = runtimeLocale.getWeekInfo?.() ?? runtimeLocale.weekInfo;
            const firstDay = info?.firstDay;
            if (firstDay !== undefined && Number.isInteger(firstDay) && firstDay >= 1 && firstDay <= 7) {
                return firstDay % 7;
            }
        }
        const region = localeRegion(canonical, runtimeLocale);
        if (region === 'MV')
            return 5;
        if (region !== undefined && saturdayFirstRegions.has(region))
            return 6;
        if (region !== undefined && sundayFirstRegions.has(region))
            return 0;
        return 1;
    }
    function civilDate(epoch, timeZone) {
        let parts;
        try {
            parts = new Intl.DateTimeFormat('en-US-u-ca-gregory', {
                timeZone,
                year: 'numeric',
                month: '2-digit',
                day: '2-digit',
                weekday: 'short',
            }).formatToParts(epoch);
        }
        catch {
            throw new GraflumeError('INVALID_SPEC', `Unsupported calendar time zone "${timeZone}".`, {
                path: '$.timeZone',
            });
        }
        const value = (type) => parts.find((part) => part.type === type)?.value ?? '';
        const weekdays = new Map([
            ['Sun', 0],
            ['Mon', 1],
            ['Tue', 2],
            ['Wed', 3],
            ['Thu', 4],
            ['Fri', 5],
            ['Sat', 6],
        ]);
        return {
            year: Number(value('year')),
            month: Number(value('month')),
            day: Number(value('day')),
            weekday: weekdays.get(value('weekday')) ?? 0,
        };
    }
    /** Produces localized short weekday headers in the same order used by calendar cells. */
    function calendarWeekdayLabels(options = {}) {
        const weekStart = calendarWeekStart(options);
        const timeZone = options.timeZone ?? 'UTC';
        // Validate the zone independently so locale failures retain their own diagnostic path.
        civilDate(Date.UTC(2024, 0, 7, 12), timeZone);
        let formatter;
        try {
            formatter = new Intl.DateTimeFormat(options.locale ?? 'en', {
                weekday: 'short',
                timeZone,
            });
        }
        catch {
            throw new GraflumeError('INVALID_SPEC', `Unsupported calendar locale "${options.locale ?? 'en'}".`, { path: '$.locale' });
        }
        const labels = new Map();
        const anchor = Date.UTC(2024, 0, 7, 12);
        for (let offset = 0; offset < 14 && labels.size < 7; offset += 1) {
            const epoch = anchor + offset * 86_400_000;
            labels.set(civilDate(epoch, timeZone).weekday, formatter.format(epoch));
        }
        return Array.from({ length: 7 }, (_, row) => {
            const weekday = (weekStart + row) % 7;
            return { weekday, label: labels.get(weekday) ?? String(weekday) };
        });
    }
    function dateKey(date) {
        return `${String(date.year).padStart(4, '0')}-${String(date.month).padStart(2, '0')}-${String(date.day).padStart(2, '0')}`;
    }
    function utcNoon(date) {
        return Date.UTC(date.year, date.month - 1, date.day, 12);
    }
    /** Produces locale-week-aware cells and explicit missing/zero/leap/month-boundary semantics. */
    function calendarCells(input, options) {
        const dateField = fieldName$1(options.dateField, '$.dateField');
        const valueField = options.valueField === undefined ? undefined : fieldName$1(options.valueField, '$.valueField');
        const weekStart = calendarWeekStart(options);
        const timeZone = options.timeZone ?? 'UTC';
        const rows = rowsFrom$1(input);
        const grouped = new Map();
        rows.forEach((row, rowIndex) => {
            const date = civilDate(timestamp(row[dateField], `$.data[${rowIndex}].${dateField}`), timeZone);
            const key = dateKey(date);
            const bucket = grouped.get(key) ?? { date, values: [], sourceRows: [] };
            const value = valueField === undefined ? 1 : finite$b(row[valueField]);
            if (value !== null)
                bucket.values.push(value);
            bucket.sourceRows.push(rowIndex);
            grouped.set(key, bucket);
        });
        const observed = [...grouped.values()].sort((a, b) => utcNoon(a.date) - utcNoon(b.date));
        if (observed.length === 0)
            return [];
        const startCivil = options.start === undefined
            ? observed[0].date
            : civilDate(timestamp(options.start, '$.start'), timeZone);
        const endCivil = options.end === undefined
            ? observed.at(-1).date
            : civilDate(timestamp(options.end, '$.end'), timeZone);
        let start = utcNoon(startCivil);
        let end = utcNoon(endCivil);
        if (end < start) {
            throw new GraflumeError('INVALID_SPEC', '$.end must be at or after $.start.');
        }
        const mode = options.mode ?? 'year';
        if (mode === 'year') {
            start = Date.UTC(startCivil.year, 0, 1, 12);
            end = Date.UTC(endCivil.year, 11, 31, 12);
        }
        else if (mode === 'month') {
            start = Date.UTC(startCivil.year, startCivil.month - 1, 1, 12);
            end = Date.UTC(endCivil.year, endCivil.month, 0, 12);
        }
        else if (mode === 'week') {
            start -= ((startCivil.weekday - weekStart + 7) % 7) * 86_400_000;
            end += ((weekStart + 6 - endCivil.weekday + 7) % 7) * 86_400_000;
        }
        const maximumCells = 3_662;
        const count = Math.floor((end - start) / 86_400_000) + 1;
        if (count > maximumCells) {
            throw new GraflumeError('INVALID_DATA', `Calendar range has ${count} days; the deterministic limit is ${maximumCells}.`);
        }
        const first = civilDate(start, 'UTC');
        const firstWeekday = first.weekday;
        const missing = options.missing ?? 'gap';
        return Array.from({ length: count }, (_, index) => {
            const date = civilDate(start + index * 86_400_000, 'UTC');
            const key = dateKey(date);
            const bucket = grouped.get(key);
            const value = bucket === undefined
                ? missing === 'zero'
                    ? 0
                    : null
                : bucket.values.length === 0
                    ? null
                    : bucket.values.reduce((sum, item) => sum + item, 0);
            return {
                key,
                date: key,
                year: date.year,
                month: date.month,
                day: date.day,
                weekday: date.weekday,
                weekIndex: Math.floor((((firstWeekday - weekStart + 7) % 7) + index) / 7),
                monthBoundary: date.day === 1,
                leapDay: date.month === 2 && date.day === 29,
                value,
                sourceRows: bucket?.sourceRows ?? [],
            };
        });
    }
    function localTradingTime(epoch, timeZone) {
        let parts;
        try {
            parts = new Intl.DateTimeFormat('en-US', {
                timeZone,
                year: 'numeric',
                month: '2-digit',
                day: '2-digit',
                hour: '2-digit',
                minute: '2-digit',
                second: '2-digit',
                hourCycle: 'h23',
            }).formatToParts(epoch);
        }
        catch {
            throw new GraflumeError('INVALID_SPEC', `Unsupported trading time zone "${timeZone}".`);
        }
        const hour = Number(parts.find((part) => part.type === 'hour')?.value ?? 0);
        const minute = Number(parts.find((part) => part.type === 'minute')?.value ?? 0);
        const year = Number(parts.find((part) => part.type === 'year')?.value ?? 0);
        const month = Number(parts.find((part) => part.type === 'month')?.value ?? 0);
        const day = Number(parts.find((part) => part.type === 'day')?.value ?? 0);
        const second = Number(parts.find((part) => part.type === 'second')?.value ?? 0);
        const millisecond = ((epoch % 1_000) + 1_000) % 1_000;
        const dayEpoch = Date.UTC(year, month - 1, day);
        const civilEpoch = Date.UTC(year, month - 1, day, hour, minute, second, millisecond);
        return {
            minute: hour * 60 + minute,
            civilEpoch,
            dayEpoch,
            offset: civilEpoch - epoch,
        };
    }
    function civilDateKey(dayEpoch) {
        return new Date(dayEpoch).toISOString().slice(0, 10);
    }
    function dateSet(values, path) {
        const output = new Set();
        (values ?? []).forEach((value, index) => {
            if (!/^\d{4}-\d{2}-\d{2}$/u.test(value) || Number.isNaN(Date.parse(`${value}T00:00:00Z`)))
                throw new GraflumeError('INVALID_SPEC', `${path}[${index}] must be YYYY-MM-DD.`);
            output.add(value);
        });
        return output;
    }
    /** Aggregates ticks or precomputed OHLC rows into deterministic, gapless trading buckets. */
    function aggregateOhlc(input, options) {
        const timeField = fieldName$1(options.timeField, '$.timeField');
        const priceField = options.priceField;
        const openField = options.openField ?? 'open';
        const highField = options.highField ?? 'high';
        const lowField = options.lowField ?? 'low';
        const closeField = options.closeField ?? priceField ?? 'close';
        const volumeField = options.volumeField;
        [priceField, openField, highField, lowField, closeField, volumeField]
            .filter((field) => field !== undefined)
            .forEach((field) => fieldName$1(field, '$.fields'));
        const intervalMs = positiveInteger(options.intervalMs, '$.intervalMs', 31_536_000_000);
        const timeZone = options.timeZone ?? 'UTC';
        const session = options.session ?? { startMinute: 9 * 60 + 30, endMinute: 16 * 60 };
        if (!Number.isInteger(session.startMinute) ||
            !Number.isInteger(session.endMinute) ||
            session.startMinute < 0 ||
            session.endMinute > 1_440 ||
            session.endMinute === session.startMinute) {
            throw new GraflumeError('INVALID_SPEC', 'Trading session minutes must form a non-empty same-day or overnight range.');
        }
        const tradingDays = options.tradingDays ?? [1, 2, 3, 4, 5];
        if (tradingDays.length === 0 ||
            new Set(tradingDays).size !== tradingDays.length ||
            tradingDays.some((day) => !Number.isInteger(day) || day < 0 || day > 6))
            throw new GraflumeError('INVALID_SPEC', '$.tradingDays must contain unique weekdays 0..6.');
        const openWeekdays = new Set(tradingDays);
        const excludedDates = dateSet(options.excludedDates, '$.excludedDates');
        const includedDates = dateSet(options.includedDates, '$.includedDates');
        const overnight = session.endMinute < session.startMinute;
        const policy = options.extendedHours ?? 'exclude';
        const points = rowsFrom$1(input)
            .map((row, rowIndex) => {
            const time = timestamp(row[timeField], `$.data[${rowIndex}].${timeField}`);
            const price = priceField === undefined ? null : finite$b(row[priceField]);
            const open = price ?? finite$b(row[openField]);
            const high = price ?? finite$b(row[highField]);
            const low = price ?? finite$b(row[lowField]);
            const close = price ?? finite$b(row[closeField]);
            const volume = volumeField === undefined ? 0 : (finite$b(row[volumeField]) ?? 0);
            if (open === null || high === null || low === null || close === null)
                return null;
            const local = localTradingTime(time, timeZone);
            const regular = overnight
                ? local.minute >= session.startMinute || local.minute < session.endMinute
                : local.minute >= session.startMinute && local.minute < session.endMinute;
            const tradingDayEpoch = overnight && local.minute < session.endMinute
                ? local.dayEpoch - 86_400_000
                : local.dayEpoch;
            const tradingDate = civilDateKey(tradingDayEpoch);
            const calendarOpen = !excludedDates.has(tradingDate) &&
                (includedDates.has(tradingDate) || openWeekdays.has(new Date(tradingDayEpoch).getUTCDay()));
            if (!calendarOpen)
                return null;
            const anchor = tradingDayEpoch + session.startMinute * 60_000;
            const bucketLocal = intervalMs >= 86_400_000
                ? Math.floor(anchor / intervalMs) * intervalMs
                : anchor + Math.floor((local.civilEpoch - anchor) / intervalMs) * intervalMs;
            const bucket = bucketLocal - local.offset;
            return {
                time,
                bucket,
                open,
                high,
                low,
                close,
                volume,
                extended: !regular,
                rowIndex,
            };
        })
            .filter((point) => point !== null)
            .filter((point) => policy !== 'exclude' || !point.extended)
            .sort((a, b) => a.time - b.time || a.rowIndex - b.rowIndex);
        const buckets = new Map();
        for (const point of points) {
            const key = `${point.bucket}:${policy === 'separate' ? point.extended : false}`;
            const group = buckets.get(key) ?? [];
            group.push(point);
            buckets.set(key, group);
        }
        return [...buckets.entries()]
            .map(([key, group]) => ({ bucket: Number(key.split(':')[0]), group }))
            .sort((a, b) => a.bucket - b.bucket || Number(a.group[0].extended) - Number(b.group[0].extended))
            .map(({ bucket, group }, tradingIndex) => ({
            time: bucket,
            tradingIndex,
            open: group[0].open,
            high: Math.max(...group.map((point) => point.high)),
            low: Math.min(...group.map((point) => point.low)),
            close: group.at(-1).close,
            volume: group.reduce((sum, point) => sum + point.volume, 0),
            extended: group[0].extended,
            sourceRows: group.map((point) => point.rowIndex),
        }));
    }
    function navigatorWindow(length, start, end) {
        if (!Number.isInteger(length) || length < 0) {
            throw new GraflumeError('INVALID_SPEC', 'Navigator length must be a non-negative integer.');
        }
        if (![start, end].every(Number.isFinite)) {
            throw new GraflumeError('INVALID_SPEC', 'Navigator bounds must be finite.');
        }
        const lower = Math.max(0, Math.min(length, Math.floor(Math.min(start, end))));
        const upper = Math.max(lower, Math.min(length, Math.ceil(Math.max(start, end))));
        return [lower, upper];
    }
    /** Aligns comparison series by stable key and inserts exact zero crossings when requested. */
    function differenceSeries(input, options) {
        const keyField = fieldName$1(options.keyField, '$.keyField');
        const policy = options.policy ?? 'absolute';
        const source = rowsFrom$1(input);
        const canonicalKey = (rawKey, rowIndex) => options.keyType === 'temporal' ? timestamp(rawKey, `$.data[${rowIndex}].${keyField}`) : rawKey;
        let aligned;
        const longForm = options.seriesField !== undefined ||
            options.valueField !== undefined ||
            options.baselineSeries !== undefined ||
            options.comparisonSeries !== undefined;
        if (longForm) {
            const seriesField = fieldName$1(options.seriesField ?? '', '$.seriesField');
            const valueField = fieldName$1(options.valueField ?? '', '$.valueField');
            const baselineSeries = (options.baselineSeries ?? '').trim();
            const comparisonSeries = (options.comparisonSeries ?? '').trim();
            if (baselineSeries === '' || comparisonSeries === '')
                throw new GraflumeError('INVALID_SPEC', '$.baselineSeries and $.comparisonSeries must be non-empty.');
            if (baselineSeries === comparisonSeries)
                throw new GraflumeError('INVALID_SPEC', 'Difference long-form series roles must be distinct.');
            const groups = new Map();
            source.forEach((row, rowIndex) => {
                const rawKey = row[keyField];
                const value = finite$b(row[valueField]);
                const series = row[seriesField];
                if (rawKey === undefined || value === null || typeof series !== 'string')
                    return;
                if (series !== baselineSeries && series !== comparisonSeries)
                    return;
                const key = canonicalKey(rawKey, rowIndex);
                const signature = JSON.stringify(key);
                const group = groups.get(signature) ?? { key };
                const role = series === baselineSeries ? 'baseline' : 'comparison';
                if (group[role] !== undefined)
                    throw new GraflumeError('INVALID_DATA', `Duplicate ${role} series value for key "${String(key)}".`);
                group[role] = { value, row: rowIndex };
                groups.set(signature, group);
            });
            const unmatched = options.unmatched ?? 'error';
            aligned = [...groups.values()].flatMap((group, sequenceIndex) => {
                if (group.baseline === undefined || group.comparison === undefined) {
                    if (unmatched === 'error')
                        throw new GraflumeError('INVALID_DATA', `Unmatched difference series key "${String(group.key)}".`);
                    if (unmatched === 'drop')
                        return [];
                }
                return [
                    {
                        key: group.key,
                        baseline: group.baseline?.value ?? 0,
                        comparison: group.comparison?.value ?? 0,
                        sourceRows: [group.baseline?.row, group.comparison?.row].filter((row) => row !== undefined),
                        sequenceIndex,
                    },
                ];
            });
        }
        else {
            const baselineField = fieldName$1(options.baselineField ?? '', '$.baselineField');
            const comparisonField = fieldName$1(options.comparisonField ?? '', '$.comparisonField');
            aligned = source.flatMap((row, rowIndex) => {
                const baseline = finite$b(row[baselineField]);
                const comparison = finite$b(row[comparisonField]);
                const rawKey = row[keyField];
                if (baseline === null || comparison === null || rawKey === undefined)
                    return [];
                return [
                    {
                        key: canonicalKey(rawKey, rowIndex),
                        baseline,
                        comparison,
                        sourceRows: [rowIndex],
                        sequenceIndex: rowIndex,
                    },
                ];
            });
        }
        let previousSequenceIndex;
        const points = aligned.flatMap(({ key, baseline, comparison, sourceRows, sequenceIndex }) => {
            const absolute = comparison - baseline;
            let difference;
            if (policy === 'absolute')
                difference = absolute;
            else if (baseline === 0 && comparison === 0)
                difference = 0;
            else if (baseline === 0) {
                const zeroBaseline = options.zeroBaseline ?? 'error';
                if (zeroBaseline === 'error')
                    throw new GraflumeError('INVALID_DATA', `Difference ${policy} policy cannot divide by a zero baseline at key "${String(key)}".`);
                if (zeroBaseline === 'skip')
                    return [];
                difference = absolute;
            }
            else
                difference =
                    policy === 'relative'
                        ? absolute / Math.abs(baseline)
                        : (absolute / Math.abs(baseline)) * 100;
            const continuousFromPrevious = previousSequenceIndex !== undefined && sequenceIndex === previousSequenceIndex + 1;
            previousSequenceIndex = sequenceIndex;
            return [{ key, baseline, comparison, difference, sourceRows, continuousFromPrevious }];
        });
        if (options.interpolateCrossings !== true)
            return points;
        const output = [];
        points.forEach((point, index) => {
            const previous = points[index - 1];
            if (previous !== undefined &&
                point.continuousFromPrevious &&
                previous.difference * point.difference < 0) {
                const ratio = Math.abs(previous.difference) /
                    (Math.abs(previous.difference) + Math.abs(point.difference));
                const numericPrevious = finite$b(previous.key);
                const numericCurrent = finite$b(point.key);
                if (numericPrevious !== null && numericCurrent !== null) {
                    const key = numericPrevious + (numericCurrent - numericPrevious) * ratio;
                    const baseline = previous.baseline + (point.baseline - previous.baseline) * ratio;
                    output.push({
                        key,
                        baseline,
                        comparison: baseline,
                        difference: 0,
                        sourceRows: [...previous.sourceRows, ...point.sourceRows],
                        continuousFromPrevious: true,
                        crossing: true,
                    });
                }
            }
            output.push(point);
        });
        return output;
    }
    /** Computes one set of bin edges for every series so comparisons stay aligned. */
    function sharedHistogramBins(series, binCount = 10) {
        const count = positiveInteger(binCount, '$.binCount', 1_000);
        const values = series.flatMap((items) => items.map(({ value }) => value).filter(Number.isFinite));
        if (values.length === 0)
            return [];
        const observedMinimum = Math.min(...values);
        const observedMaximum = Math.max(...values);
        const constantPadding = Math.max(1, Math.abs(observedMinimum) * 0.01);
        const minimum = observedMinimum === observedMaximum ? observedMinimum - constantPadding / 2 : observedMinimum;
        const maximum = observedMinimum === observedMaximum ? observedMaximum + constantPadding / 2 : observedMaximum;
        const step = (maximum - minimum) / count;
        const bins = Array.from({ length: count }, (_, index) => ({
            start: minimum + index * step,
            end: index === count - 1 ? maximum : minimum + (index + 1) * step,
            counts: Array.from({ length: series.length }, () => 0),
            weights: Array.from({ length: series.length }, () => 0),
        }));
        series.forEach((items, seriesIndex) => {
            items.forEach(({ value, weight = 1 }, rowIndex) => {
                if (!Number.isFinite(value) || !Number.isFinite(weight) || weight < 0) {
                    throw new GraflumeError('INVALID_DATA', `Invalid histogram observation ${rowIndex}.`);
                }
                const index = Math.min(count - 1, Math.max(0, Math.floor((value - minimum) / step)));
                bins[index].counts[seriesIndex] = bins[index].counts[seriesIndex] + 1;
                bins[index].weights[seriesIndex] = bins[index].weights[seriesIndex] + weight;
            });
        });
        return bins;
    }
    /** Weighted quartiles plus a bounded McGill-style median notch. */
    function weightedBoxSummary(observations) {
        const ordered = observations
            .map((item, index) => ({ ...item, weight: item.weight ?? 1, rowIndex: item.rowIndex ?? index }))
            .filter(({ value, weight }) => Number.isFinite(value) && Number.isFinite(weight) && weight > 0)
            .sort((a, b) => a.value - b.value || a.rowIndex - b.rowIndex);
        if (ordered.length === 0)
            return null;
        const total = ordered.reduce((sum, item) => sum + item.weight, 0);
        const weightedQuantile = (probability) => {
            const target = probability * total;
            let cumulative = 0;
            for (const item of ordered) {
                cumulative += item.weight;
                if (cumulative >= target)
                    return item.value;
            }
            return ordered.at(-1).value;
        };
        const q1 = weightedQuantile(0.25);
        const median = weightedQuantile(0.5);
        const q3 = weightedQuantile(0.75);
        const squareWeights = ordered.reduce((sum, item) => sum + item.weight * item.weight, 0);
        const effectiveSampleSize = squareWeights === 0 ? 0 : (total * total) / squareWeights;
        const half = effectiveSampleSize <= 0 ? 0 : (1.57 * (q3 - q1)) / Math.sqrt(effectiveSampleSize);
        return {
            minimum: ordered[0].value,
            q1,
            median,
            q3,
            maximum: ordered.at(-1).value,
            notch: [Math.max(q1, median - half), Math.min(q3, median + half)],
            effectiveSampleSize,
            sourceRows: ordered.map(({ rowIndex }) => rowIndex),
        };
    }
    /** Deterministic centered strip offsets; `spread=0` is a conventional rug. */
    function rugStrip(values, options = {}) {
        const spread = options.spread ?? 0;
        if (!Number.isFinite(spread) || spread < 0) {
            throw new GraflumeError('INVALID_SPEC', '$.spread must be finite and non-negative.');
        }
        let state = (options.seed ?? 0) >>> 0;
        const random = () => {
            state = (state + 0x6d2b79f5) | 0;
            let result = Math.imul(state ^ (state >>> 15), 1 | state);
            result ^= result + Math.imul(result ^ (result >>> 7), 61 | result);
            return ((result ^ (result >>> 14)) >>> 0) / 4_294_967_296;
        };
        return values.flatMap((value, rowIndex) => Number.isFinite(value)
            ? [{ value, offset: spread === 0 ? 0 : (random() - 0.5) * spread, rowIndex }]
            : []);
    }
    function normalCritical(confidence) {
        const probability = 0.5 + confidence / 2;
        const a = [
            -39.69683028665376, 220.9460984245205, -275.9285104469687, 138.357751867269, -30.66479806614716,
            2.506628277459239,
        ];
        const b = [
            -54.47609879822406, 161.5858368580409, -155.6989798598866, 66.80131188771972,
            -13.28068155288572,
        ];
        const c = [
            -0.007784894002430293, -0.3223964580411365, -2.400758277161838, -2.549732539343734,
            4.374664141464968, 2.938163982698783,
        ];
        const d = [0.007784695709041462, 0.3224671290700398, 2.445134137142996, 3.754408661907416];
        if (probability < 0.02425) {
            const q = Math.sqrt(-2 * Math.log(probability));
            return ((((((c[0] * q + c[1]) * q + c[2]) * q + c[3]) * q + c[4]) * q + c[5]) /
                ((((d[0] * q + d[1]) * q + d[2]) * q + d[3]) * q + 1));
        }
        if (probability > 1 - 0.02425) {
            const q = Math.sqrt(-2 * Math.log(1 - probability));
            return -((((((c[0] * q + c[1]) * q + c[2]) * q + c[3]) * q + c[4]) * q + c[5]) /
                ((((d[0] * q + d[1]) * q + d[2]) * q + d[3]) * q + 1));
        }
        const q = probability - 0.5;
        const r = q * q;
        return (((((((a[0] * r + a[1]) * r + a[2]) * r + a[3]) * r + a[4]) * r + a[5]) * q) /
            (((((b[0] * r + b[1]) * r + b[2]) * r + b[3]) * r + b[4]) * r + 1));
    }
    /** Raw-data interval estimator with explicit kind, orientation, and provenance. */
    function estimateInterval(values, options = {}) {
        const indexed = values
            .map((value, rowIndex) => ({ value, rowIndex }))
            .filter(({ value }) => Number.isFinite(value))
            .sort((a, b) => a.value - b.value || a.rowIndex - b.rowIndex);
        if (indexed.length === 0)
            return null;
        const ordered = indexed.map(({ value }) => value);
        const mean = ordered.reduce((sum, value) => sum + value, 0) / ordered.length;
        const median = quantile$2(ordered, 0.5);
        const estimate = options.estimator === 'median' ? median : mean;
        const variance = ordered.length < 2
            ? 0
            : ordered.reduce((sum, value) => sum + (value - mean) ** 2, 0) / (ordered.length - 1);
        const deviation = Math.sqrt(variance);
        const error = deviation / Math.sqrt(ordered.length);
        const confidence = options.confidence ?? 0.95;
        if (!Number.isFinite(confidence) || confidence <= 0 || confidence >= 1) {
            throw new GraflumeError('INVALID_SPEC', '$.confidence must be between 0 and 1.');
        }
        const kind = options.kind ?? 'CI';
        const critical = normalCritical(confidence);
        let low;
        let high;
        if (kind === 'SE')
            [low, high] = [estimate - error, estimate + error];
        else if (kind === 'SD')
            [low, high] = [estimate - deviation, estimate + deviation];
        else if (kind === 'PI') {
            const half = critical * deviation * Math.sqrt(1 + 1 / ordered.length);
            [low, high] = [estimate - half, estimate + half];
        }
        else if (kind === 'IQR')
            [low, high] = [quantile$2(ordered, 0.25), quantile$2(ordered, 0.75)];
        else if (kind === 'HDI') {
            const windowLength = Math.max(1, Math.ceil(confidence * ordered.length));
            let start = 0;
            for (let index = 1; index + windowLength - 1 < ordered.length; index += 1) {
                if (ordered[index + windowLength - 1] - ordered[index] <
                    ordered[start + windowLength - 1] - ordered[start]) {
                    start = index;
                }
            }
            [low, high] = [
                ordered[start],
                ordered[Math.min(ordered.length - 1, start + windowLength - 1)],
            ];
        }
        else {
            const half = critical * error;
            [low, high] = [estimate - half, estimate + half];
        }
        return {
            estimate,
            low,
            high,
            kind,
            orientation: options.orientation ?? 'vertical',
            confidence,
            sourceRows: indexed.map(({ rowIndex }) => rowIndex),
            summary: `${kind} ${Math.round(confidence * 100)}% from ${indexed.length} source rows`,
        };
    }
    /** Makes duplicate and implicit sorting behavior explicit for line-like series. */
    function prepareOrderedSeries(input, options) {
        const keyField = fieldName$1(options.keyField, '$.keyField');
        const valueField = fieldName$1(options.valueField, '$.valueField');
        const duplicatePolicy = options.duplicates ?? 'error';
        const source = rowsFrom$1(input).flatMap((row, rowIndex) => {
            const rawKey = row[keyField];
            const value = finite$b(row[valueField]);
            if (rawKey === undefined || value === null)
                return [];
            const key = options.keyType === 'temporal'
                ? timestamp(rawKey, `$.data[${rowIndex}].${keyField}`)
                : rawKey;
            return [{ key, value, rowIndex }];
        });
        const groups = new Map();
        for (const point of source) {
            const key = JSON.stringify(point.key);
            const group = groups.get(key) ?? [];
            group.push(point);
            groups.set(key, group);
        }
        let points = [...groups.values()].map((group) => {
            if (group.length > 1 && duplicatePolicy === 'error') {
                throw new GraflumeError('INVALID_DATA', `Duplicate series key "${String(group[0].key)}".`);
            }
            const value = duplicatePolicy === 'first'
                ? group[0].value
                : duplicatePolicy === 'last'
                    ? group.at(-1).value
                    : duplicatePolicy === 'sum' || duplicatePolicy === 'mean'
                        ? group.reduce((sum, item) => sum + item.value, 0) /
                            (duplicatePolicy === 'mean' ? group.length : 1)
                        : group[0].value;
            return { key: group[0].key, value, sourceRows: group.map(({ rowIndex }) => rowIndex) };
        });
        const compare = (left, right) => {
            const a = finite$b(left.key);
            const b = finite$b(right.key);
            return a !== null && b !== null
                ? a - b
                : String(left.key).localeCompare(String(right.key), 'en');
        };
        const ascending = points.every((point, index) => index === 0 || compare(points[index - 1], point) <= 0);
        if (options.sort === 'error' && !ascending) {
            throw new GraflumeError('INVALID_DATA', 'Series keys are not ascending.');
        }
        if (options.sort === 'ascending')
            points = points.slice().sort(compare);
        if (options.sort === 'descending')
            points = points.slice().sort((a, b) => -compare(a, b));
        return points;
    }
    /** Resolves explicit relative, absolute, subtotal, and total waterfall semantics. */
    function waterfallSteps(input) {
        let cursor = 0;
        let subtotalBase = 0;
        return input.map((item, index) => {
            if (!Number.isFinite(item.value)) {
                throw new GraflumeError('INVALID_DATA', `Waterfall value at ${index} must be finite.`);
            }
            const kind = item.kind ?? 'relative';
            let start;
            let end;
            if (kind === 'relative') {
                start = cursor;
                end = cursor + item.value;
                cursor = end;
            }
            else if (kind === 'absolute') {
                start = cursor;
                end = item.value;
                cursor = end;
                subtotalBase = end;
            }
            else if (kind === 'subtotal') {
                start = subtotalBase;
                end = cursor;
                subtotalBase = cursor;
            }
            else {
                start = 0;
                end = item.value;
                cursor = end;
                subtotalBase = end;
            }
            return { ...item, kind, start, end, change: end - start, index };
        });
    }

    function strideSampleIndices(length, target) {
        if (length <= target || target <= 0)
            return Array.from({ length }, (_, index) => index);
        const step = length / target;
        const indices = [];
        for (let cursor = 0; cursor < target; cursor += 1) {
            indices.push(Math.min(length - 1, Math.floor(cursor * step)));
        }
        if (indices.at(-1) !== length - 1)
            indices.push(length - 1);
        return indices;
    }
    /**
     * Returns at most `target` evenly spaced indices, including both endpoints when
     * the budget allows. Unlike the compatibility sampler above, this helper never
     * emits an extra endpoint beyond the requested budget.
     */
    function exactStrideSampleIndices(length, target) {
        const safeLength = Number.isFinite(length) ? Math.max(0, Math.trunc(length)) : 0;
        const safeTarget = Number.isFinite(target) ? Math.max(0, Math.trunc(target)) : 0;
        const count = Math.min(safeLength, safeTarget);
        if (count === 0)
            return [];
        if (count === safeLength)
            return Array.from({ length: safeLength }, (_, index) => index);
        if (count === 1)
            return [Math.floor((safeLength - 1) / 2)];
        return Array.from({ length: count }, (_, index) => Math.round((index * (safeLength - 1)) / (count - 1)));
    }
    function minMaxSampleIndices(values, target) {
        const length = values.length;
        if (length <= target || target < 4)
            return strideSampleIndices(length, Math.max(1, target));
        const bucketCount = Math.max(1, Math.floor((target - 2) / 2));
        const bucketSize = (length - 2) / bucketCount;
        const selected = new Set([0, length - 1]);
        for (let bucket = 0; bucket < bucketCount; bucket += 1) {
            const start = Math.max(1, Math.floor(1 + bucket * bucketSize));
            const end = Math.min(length - 1, Math.ceil(1 + (bucket + 1) * bucketSize));
            let minIndex = -1;
            let maxIndex = -1;
            let minValue = Number.POSITIVE_INFINITY;
            let maxValue = Number.NEGATIVE_INFINITY;
            for (let index = start; index < end; index += 1) {
                const value = values[index];
                if (value === null || value === undefined || !Number.isFinite(value))
                    continue;
                if (value < minValue) {
                    minValue = value;
                    minIndex = index;
                }
                if (value > maxValue) {
                    maxValue = value;
                    maxIndex = index;
                }
            }
            if (minIndex >= 0)
                selected.add(minIndex);
            if (maxIndex >= 0)
                selected.add(maxIndex);
        }
        return [...selected].sort((left, right) => left - right);
    }

    function finite$a(value, path) {
        if (typeof value !== 'number' || !Number.isFinite(value)) {
            throw new GraflumeError('INVALID_DATA', `${path} must be a finite number.`, { path });
        }
        return value;
    }
    function clamp$c(value, low, high) {
        return Math.max(low, Math.min(high, value));
    }
    function text$1(value, path) {
        const normalized = value.trim();
        if (normalized === '')
            throw new GraflumeError('INVALID_DATA', `${path} must be non-empty.`, { path });
        return normalized;
    }
    function identity$1(value) {
        return `${typeof value}:${String(value)}`;
    }
    function orderedDomain(observed, authored, path) {
        const unique = [];
        const seen = new Set();
        observed.forEach((value) => {
            const key = identity$1(value);
            if (!seen.has(key)) {
                seen.add(key);
                unique.push(value);
            }
        });
        if (authored === undefined)
            return unique;
        const authoredKeys = authored.map(identity$1);
        if (new Set(authoredKeys).size !== authored.length)
            throw new GraflumeError('INVALID_SPEC', `${path} contains duplicates.`);
        const missing = unique.filter((value) => !authoredKeys.includes(identity$1(value)));
        if (missing.length > 0)
            throw new GraflumeError('INVALID_SPEC', `${path} omits observed values.`);
        return [...authored];
    }
    function heatmapColorPosition(value, domain, mode, midpoint) {
        const [minimum, maximum] = domain;
        if (maximum === minimum)
            return 0.5;
        if (mode === 'log') {
            if (minimum <= 0 || value <= 0)
                throw new GraflumeError('INVALID_DATA', 'Log heatmap colors require positive values.');
            return (Math.log(value) - Math.log(minimum)) / (Math.log(maximum) - Math.log(minimum));
        }
        if (mode === 'symlog') {
            const transform = (current) => Math.sign(current) * Math.log1p(Math.abs(current));
            return (transform(value) - transform(minimum)) / (transform(maximum) - transform(minimum));
        }
        if (mode === 'diverging') {
            if (value <= midpoint)
                return (0.5 * (value - minimum)) / Math.max(Number.EPSILON, midpoint - minimum);
            return 0.5 + (0.5 * (value - midpoint)) / Math.max(Number.EPSILON, maximum - midpoint);
        }
        return (value - minimum) / (maximum - minimum);
    }
    function matrixHeatmapRows(input) {
        const height = input.values.length;
        const width = height === 0 ? (input.columns?.length ?? 0) : input.values[0].length;
        input.values.forEach((row, index) => {
            if (row.length !== width)
                throw new GraflumeError('INVALID_DATA', 'Heatmap matrix rows must have equal length.', {
                    path: `$.values[${index}]`,
                });
        });
        const rows = input.rows ?? Array.from({ length: height }, (_, index) => index);
        const columns = input.columns ?? Array.from({ length: width }, (_, index) => index);
        if (rows.length !== height)
            throw new GraflumeError('INVALID_DATA', 'Heatmap matrix row labels must match its height.', {
                path: '$.rows',
            });
        if (columns.length !== width)
            throw new GraflumeError('INVALID_DATA', 'Heatmap matrix column labels must match its width.', {
                path: '$.columns',
            });
        if (new Set(rows.map(identity$1)).size !== rows.length)
            throw new GraflumeError('INVALID_DATA', 'Heatmap matrix row labels must be unique.', {
                path: '$.rows',
            });
        if (new Set(columns.map(identity$1)).size !== columns.length)
            throw new GraflumeError('INVALID_DATA', 'Heatmap matrix column labels must be unique.', {
                path: '$.columns',
            });
        if (input.xExtents !== undefined && input.xExtents.length !== width)
            throw new GraflumeError('INVALID_DATA', 'Heatmap xExtents must match matrix width.', {
                path: '$.xExtents',
            });
        if (input.yExtents !== undefined && input.yExtents.length !== height)
            throw new GraflumeError('INVALID_DATA', 'Heatmap yExtents must match matrix height.', {
                path: '$.yExtents',
            });
        return input.values.flatMap((values, rowIndex) => values.map((rawValue, columnIndex) => {
            const value = rawValue === null ? null : finite$a(rawValue, `$.values[${rowIndex}][${columnIndex}]`);
            const xExtent = input.xExtents?.[columnIndex];
            const yExtent = input.yExtents?.[rowIndex];
            return {
                row: rows[rowIndex],
                column: columns[columnIndex],
                value,
                ...(xExtent === undefined ? {} : { x0: xExtent[0], x1: xExtent[1] }),
                ...(yExtent === undefined ? {} : { y0: yExtent[0], y1: yExtent[1] }),
            };
        }));
    }
    function inferredHeatmapExtents(data, axis) {
        const output = new Map();
        data.forEach((datum, index) => {
            const start = axis === 'x' ? datum.x0 : datum.y0;
            const end = axis === 'x' ? datum.x1 : datum.y1;
            if (start === undefined && end === undefined)
                return;
            if (start === undefined || end === undefined)
                throw new GraflumeError('INVALID_DATA', `Heatmap ${axis} extents require both ${axis}0 and ${axis}1.`, { path: `$.data[${index}]` });
            const low = finite$a(start, `$.data[${index}].${axis}0`);
            const high = finite$a(end, `$.data[${index}].${axis}1`);
            if (high <= low)
                throw new GraflumeError('INVALID_DATA', 'Heatmap cell extents must be ascending.', {
                    path: `$.data[${index}]`,
                });
            const key = identity$1(axis === 'x' ? datum.column : datum.row);
            const previous = output.get(key);
            if (previous !== undefined && (previous[0] !== low || previous[1] !== high))
                throw new GraflumeError('INVALID_DATA', `Heatmap ${axis} extent must be consistent for each ${axis === 'x' ? 'column' : 'row'}.`, { path: `$.data[${index}]` });
            output.set(key, [low, high]);
        });
        return output;
    }
    /** Pivots long-form heatmap rows into a matrix with irregular extents, explicit NA pattern and color contracts. */
    function buildHeatmapMatrix(input, options = {}) {
        const data = Array.isArray(input)
            ? input
            : matrixHeatmapRows(input);
        const rows = orderedDomain(data.map(({ row }) => row), options.rowOrder, '$.rowOrder');
        const columns = orderedDomain(data.map(({ column }) => column), options.columnOrder, '$.columnOrder');
        const rowIndex = new Map(rows.map((value, index) => [identity$1(value), index]));
        const columnIndex = new Map(columns.map((value, index) => [identity$1(value), index]));
        const matrix = rows.map(() => columns.map(() => null));
        const byCell = new Map();
        data.forEach((datum, index) => {
            const key = `${identity$1(datum.row)}\u0000${identity$1(datum.column)}`;
            if (byCell.has(key))
                throw new GraflumeError('INVALID_DATA', `Duplicate heatmap cell at row ${index}.`);
            if (datum.value !== null)
                finite$a(datum.value, `$.data[${index}].value`);
            byCell.set(key, datum);
            matrix[rowIndex.get(identity$1(datum.row))][columnIndex.get(identity$1(datum.column))] =
                datum.value;
        });
        const numeric = data.flatMap(({ value }) => (value === null ? [] : [value]));
        const domain = numeric.length === 0 ? [0, 1] : [Math.min(...numeric), Math.max(...numeric)];
        const mode = options.color ?? 'sequential';
        const midpoint = finite$a(options.midpoint ?? 0, '$.midpoint');
        const sorted = [...numeric].sort((a, b) => a - b);
        const columnExtents = inferredHeatmapExtents(data, 'x');
        const rowExtents = inferredHeatmapExtents(data, 'y');
        const quantilePosition = (value) => {
            let upper = sorted.findIndex((current) => current > value);
            if (upper < 0)
                upper = sorted.length;
            return sorted.length <= 1 ? 0.5 : clamp$c((upper - 1) / (sorted.length - 1), 0, 1);
        };
        const cells = rows.flatMap((row, r) => columns.map((column, c) => {
            const datum = byCell.get(`${identity$1(row)}\u0000${identity$1(column)}`);
            const value = datum?.value ?? null;
            const inferredX = columnExtents.get(identity$1(column));
            const inferredY = rowExtents.get(identity$1(row));
            const x0 = datum?.x0 === undefined ? (inferredX?.[0] ?? c) : finite$a(datum.x0, `$.data[${r}].x0`);
            const x1 = datum?.x1 === undefined ? (inferredX?.[1] ?? c + 1) : finite$a(datum.x1, `$.data[${r}].x1`);
            const y0 = datum?.y0 === undefined ? (inferredY?.[0] ?? r) : finite$a(datum.y0, `$.data[${r}].y0`);
            const y1 = datum?.y1 === undefined ? (inferredY?.[1] ?? r + 1) : finite$a(datum.y1, `$.data[${r}].y1`);
            if (x1 <= x0 || y1 <= y0)
                throw new GraflumeError('INVALID_DATA', 'Heatmap cell extents must be ascending.');
            return {
                row,
                column,
                rowIndex: r,
                columnIndex: c,
                value,
                x0,
                x1,
                y0,
                y1,
                colorPosition: value === null
                    ? null
                    : clamp$c(mode === 'quantile'
                        ? quantilePosition(value)
                        : heatmapColorPosition(value, domain, mode, midpoint), 0, 1),
                missingPattern: value === null ? (options.missing?.pattern ?? 'cross') : null,
            };
        }));
        return { rows, columns, values: matrix, cells, domain };
    }
    /** Selects row, column or cell heatmap regions for linked brushes. */
    function brushHeatmap(matrix, brush) {
        const rowKeys = new Set(brush.rows?.map(identity$1));
        const columnKeys = new Set(brush.columns?.map(identity$1));
        const low = brush.value === undefined ? -Infinity : Math.min(brush.value[0], brush.value[1]);
        const high = brush.value === undefined ? Infinity : Math.max(brush.value[0], brush.value[1]);
        return matrix.cells.filter((cell) => (rowKeys.size === 0 || rowKeys.has(identity$1(cell.row))) &&
            (columnKeys.size === 0 || columnKeys.has(identity$1(cell.column))) &&
            cell.value !== null &&
            cell.value >= low &&
            cell.value <= high);
    }
    function rasterDimensions(image) {
        const width = Math.floor(finite$a(image.width, '$.width'));
        const height = Math.floor(finite$a(image.height, '$.height'));
        const channels = Math.floor(finite$a(image.channels, '$.channels'));
        if (width < 1 || height < 1 || channels < 1 || channels > 4)
            throw new GraflumeError('INVALID_DATA', 'Raster dimensions and channel count are invalid.');
        if (image.values.length !== width * height * channels)
            throw new GraflumeError('INVALID_DATA', 'Raster values length does not match dimensions.');
        image.values.forEach((value, index) => finite$a(value, `$.values[${index}]`));
        return { width, height, channels };
    }
    function cubicWeight(value) {
        const absolute = Math.abs(value);
        if (absolute <= 1)
            return (1.5 * absolute - 2.5) * absolute * absolute + 1;
        if (absolute < 2)
            return ((-0.5 * absolute + 2.5) * absolute - 4) * absolute + 2;
        return 0;
    }
    /** Samples raster pixels in data coordinates with upper/lower origin and nearest/bilinear/bicubic filters. */
    function sampleRaster(image, x, y, resampling = 'nearest') {
        const { width, height, channels } = rasterDimensions(image);
        const extent = image.extent ?? [0, width, 0, height];
        extent.forEach((value, index) => finite$a(value, `$.extent[${index}]`));
        if (extent[1] === extent[0] || extent[3] === extent[2])
            throw new GraflumeError('INVALID_DATA', 'Raster extent must have non-zero width and height.');
        // Extents describe the outer pixel edges. Mapping the center of each output cell to
        // `index + 0.5` therefore lands exactly on an input pixel center at matching resolution.
        const px = ((finite$a(x, '$.x') - extent[0]) / (extent[1] - extent[0])) * width - 0.5;
        const normalizedY = ((finite$a(y, '$.y') - extent[2]) / (extent[3] - extent[2])) * height - 0.5;
        const py = (image.origin ?? 'upper') === 'upper' ? height - 1 - normalizedY : normalizedY;
        const valueAt = (column, row, channel) => image.values[(clamp$c(row, 0, height - 1) * width + clamp$c(column, 0, width - 1)) * channels + channel];
        if (resampling === 'nearest')
            return Array.from({ length: channels }, (_, channel) => valueAt(Math.round(px), Math.round(py), channel));
        if (resampling === 'bilinear') {
            const x0 = Math.floor(px);
            const y0 = Math.floor(py);
            const fx = px - x0;
            const fy = py - y0;
            return Array.from({ length: channels }, (_, channel) => valueAt(x0, y0, channel) * (1 - fx) * (1 - fy) +
                valueAt(x0 + 1, y0, channel) * fx * (1 - fy) +
                valueAt(x0, y0 + 1, channel) * (1 - fx) * fy +
                valueAt(x0 + 1, y0 + 1, channel) * fx * fy);
        }
        return Array.from({ length: channels }, (_, channel) => {
            let total = 0;
            let weight = 0;
            for (let row = Math.floor(py) - 1; row <= Math.floor(py) + 2; row += 1)
                for (let column = Math.floor(px) - 1; column <= Math.floor(px) + 2; column += 1) {
                    const current = cubicWeight(px - column) * cubicWeight(py - row);
                    total += valueAt(column, row, channel) * current;
                    weight += current;
                }
            return weight === 0 ? 0 : total / weight;
        });
    }
    function parseHex(color) {
        const match = /^#([0-9a-f]{6})$/i.exec(color);
        if (match === null)
            throw new GraflumeError('INVALID_SPEC', `Unsupported colormap color "${color}".`);
        return [
            Number.parseInt(match[1].slice(0, 2), 16),
            Number.parseInt(match[1].slice(2, 4), 16),
            Number.parseInt(match[1].slice(4, 6), 16),
        ];
    }
    /** Applies scalar window/level, colormap, alpha and RGB/RGBA channel compositing. */
    function mapRasterColor(channels, options = {}) {
        if (channels.length < 1 || channels.length > 4)
            throw new GraflumeError('INVALID_DATA', 'Raster samples need one to four channels.');
        channels.forEach((value, index) => finite$a(value, `$.channels[${index}]`));
        const alpha = clamp$c(finite$a(options.alpha ?? 1, '$.alpha'), 0, 1);
        if (channels.length >= 3)
            return [
                clamp$c(channels[0], 0, 255),
                clamp$c(channels[1], 0, 255),
                clamp$c(channels[2], 0, 255),
                alpha * (channels.length === 4 ? clamp$c(channels[3], 0, 255) / 255 : 1),
            ];
        const window = options.window ?? [0, 255];
        const windowLow = finite$a(window[0], '$.window[0]');
        const windowHigh = finite$a(window[1], '$.window[1]');
        if (windowHigh <= windowLow)
            throw new GraflumeError('INVALID_SPEC', 'Raster window must be finite and ascending.', {
                path: '$.window',
            });
        const position = clamp$c((channels[0] - windowLow) / (windowHigh - windowLow), 0, 1);
        const authoredColors = options.colormap ?? ['#000000', '#ffffff'];
        if (authoredColors.length === 0)
            throw new GraflumeError('INVALID_SPEC', 'Raster colormap must contain at least one color.', {
                path: '$.colormap',
            });
        const colors = authoredColors.map(parseHex);
        if (colors.length === 1)
            return [...colors[0], alpha];
        const scaled = position * (colors.length - 1);
        const index = Math.min(colors.length - 2, Math.floor(scaled));
        const ratio = scaled - index;
        const first = colors[index];
        const second = colors[index + 1] ?? first;
        return [
            first[0] + (second[0] - first[0]) * ratio,
            first[1] + (second[1] - first[1]) * ratio,
            first[2] + (second[2] - first[2]) * ratio,
            alpha,
        ];
    }
    /** Validates or normalizes constant-sum components and resolves barycentric coordinates, ticks and dual tooltips. */
    function projectTernary(data, options = {}) {
        const target = finite$a(options.sum ?? 1, '$.sum');
        if (target <= 0)
            throw new GraflumeError('INVALID_SPEC', '$.sum must be positive.');
        const tolerance = Math.max(0, finite$a(options.tolerance ?? target * 1e-9, '$.tolerance'));
        const format = options.format ?? ((value) => String(Number(value.toPrecision(6))));
        const points = data.map((datum, index) => {
            const raw = [
                finite$a(datum.a, `$.data[${index}].a`),
                finite$a(datum.b, `$.data[${index}].b`),
                finite$a(datum.c, `$.data[${index}].c`),
            ];
            if (raw.some((value) => value < 0))
                throw new GraflumeError('INVALID_DATA', 'Ternary components must be non-negative.');
            const sum = raw[0] + raw[1] + raw[2];
            if (sum <= 0)
                throw new GraflumeError('INVALID_DATA', 'Ternary component sum must be positive.');
            if ((options.policy ?? 'reject') === 'reject' && Math.abs(sum - target) > tolerance)
                throw new GraflumeError('INVALID_DATA', `Ternary row ${index} does not sum to ${target}.`);
            const normalized = [raw[0] / sum, raw[1] / sum, raw[2] / sum];
            return {
                id: datum.id?.trim() || `ternary-${index}`,
                raw,
                normalized,
                x: normalized[1] + normalized[2] * 0.5,
                y: 1 - (normalized[2] * Math.sqrt(3)) / 2,
                tooltip: `raw (${raw.map(format).join(', ')}) · normalized (${normalized.map(format).join(', ')})`,
            };
        });
        const tickCount = clamp$c(Math.floor(options.ticks ?? 5), 2, 20);
        const tickFormat = options.tickFormat ?? format;
        const ticks = Array.from({ length: tickCount + 1 }, (_, index) => ({
            value: (target * index) / tickCount,
            label: tickFormat((target * index) / tickCount),
        }));
        return { sum: target, points, axes: { a: ticks, b: ticks, c: ticks } };
    }
    function complexDivide(a, b) {
        const denominator = b.real * b.real + b.imaginary * b.imaginary;
        if (denominator === 0)
            throw new GraflumeError('INVALID_DATA', 'Complex division by zero.');
        return {
            real: (a.real * b.real + a.imaginary * b.imaginary) / denominator,
            imaginary: (a.imaginary * b.real - a.real * b.imaginary) / denominator,
        };
    }
    /** Converts S/reflection, Z and Y inputs to the Smith reflection plane with reference impedance and specialist labels. */
    function projectSmith(data, options = {}) {
        const reference = finite$a(options.referenceImpedance ?? 50, '$.referenceImpedance');
        if (reference <= 0)
            throw new GraflumeError('INVALID_SPEC', '$.referenceImpedance must be positive.');
        const mode = options.mode ?? 'reflection';
        const points = data.map((datum, index) => {
            const input = {
                real: finite$a(datum.real, `$.data[${index}].real`),
                imaginary: finite$a(datum.imaginary, `$.data[${index}].imaginary`),
            };
            let normalized;
            let openCircuit = false;
            let reflection;
            if (mode === 'reflection' || mode === 's') {
                reflection = input;
                const onePlus = { real: 1 + input.real, imaginary: input.imaginary };
                const oneMinus = { real: 1 - input.real, imaginary: -input.imaginary };
                if (oneMinus.real === 0 && oneMinus.imaginary === 0) {
                    normalized = { real: 0, imaginary: 0 };
                    openCircuit = true;
                }
                else
                    normalized = complexDivide(onePlus, oneMinus);
            }
            else if (mode === 'z') {
                normalized = { real: input.real / reference, imaginary: input.imaginary / reference };
                reflection = complexDivide({ real: normalized.real - 1, imaginary: normalized.imaginary }, { real: normalized.real + 1, imaginary: normalized.imaginary });
            }
            else {
                const normalizedAdmittance = {
                    real: input.real * reference,
                    imaginary: input.imaginary * reference,
                };
                if (normalizedAdmittance.real === 0 && normalizedAdmittance.imaginary === 0) {
                    normalized = { real: 0, imaginary: 0 };
                    reflection = { real: 1, imaginary: 0 };
                    openCircuit = true;
                }
                else {
                    normalized = complexDivide({ real: 1, imaginary: 0 }, normalizedAdmittance);
                    reflection = complexDivide({ real: normalized.real - 1, imaginary: normalized.imaginary }, { real: normalized.real + 1, imaginary: normalized.imaginary });
                }
            }
            return {
                id: datum.id?.trim() || `smith-${index}`,
                input,
                normalized: openCircuit ? null : normalized,
                openCircuit,
                reflection,
                x: reflection.real,
                y: reflection.imaginary,
                magnitude: Math.hypot(reflection.real, reflection.imaginary),
                phase: Math.atan2(reflection.imaginary, reflection.real),
                tooltip: `${mode.toUpperCase()} ${input.real}${input.imaginary < 0 ? '' : '+'}${input.imaginary}j · z ${openCircuit ? '∞' : `${normalized.real}${normalized.imaginary < 0 ? '' : '+'}${normalized.imaginary}j`} · Γ ${reflection.real}${reflection.imaginary < 0 ? '' : '+'}${reflection.imaginary}j`,
            };
        });
        const values = [0, 0.2, 0.5, 1, 2, 5];
        return {
            mode,
            grid: options.grid ?? 'impedance',
            referenceImpedance: reference,
            points,
            labels: {
                resistance: values.map((value) => ({ value, label: `r=${value}` })),
                reactance: values.slice(1).flatMap((value) => [
                    { value, label: `+j${value}` },
                    { value: -value, label: `-j${value}` },
                ]),
                conductance: values.map((value) => ({ value, label: `g=${value}` })),
                susceptance: values.slice(1).flatMap((value) => [
                    { value, label: `+jb${value}` },
                    { value: -value, label: `-jb${value}` },
                ]),
            },
        };
    }
    function bracket(values, target, path) {
        if (values.length < 2 ||
            values.some((value, index) => !Number.isFinite(value) || (index > 0 && value <= values[index - 1])))
            throw new GraflumeError('INVALID_DATA', `${path} must be strictly ascending.`);
        if (target < values[0] || target > values.at(-1))
            throw new GraflumeError('INVALID_DATA', `${path} value is outside the logical grid.`);
        let upper = values.findIndex((value) => value >= target);
        if (upper <= 0)
            upper = 1;
        const lower = upper - 1;
        return [lower, upper, (target - values[lower]) / (values[upper] - values[lower])];
    }
    /** Projects irregular logical a/b carpet coordinates, honors masks and retains dual logical/projected tooltips. */
    function projectCarpet(grid, points) {
        if (grid.x.length !== grid.b.length ||
            grid.y.length !== grid.b.length ||
            grid.x.some((row) => row.length !== grid.a.length) ||
            grid.y.some((row) => row.length !== grid.a.length))
            throw new GraflumeError('INVALID_DATA', 'Carpet x/y grid dimensions must be b by a.');
        if (grid.mask !== undefined &&
            (grid.mask.length !== grid.b.length ||
                grid.mask.some((row) => row.length !== grid.a.length || row.some((value) => typeof value !== 'boolean'))))
            throw new GraflumeError('INVALID_DATA', 'Carpet mask dimensions must be b by a booleans.');
        const projected = points.map((point, index) => {
            const a = finite$a(point.a, `$.points[${index}].a`);
            const b = finite$a(point.b, `$.points[${index}].b`);
            const [a0, a1, fa] = bracket(grid.a, a, '$.grid.a');
            const [b0, b1, fb] = bracket(grid.b, b, '$.grid.b');
            const masked = [
                grid.mask?.[b0]?.[a0],
                grid.mask?.[b0]?.[a1],
                grid.mask?.[b1]?.[a0],
                grid.mask?.[b1]?.[a1],
            ].some((value) => value === false);
            const interpolate = (values) => {
                const top = finite$a(values[b0][a0], '$.grid') * (1 - fa) + finite$a(values[b0][a1], '$.grid') * fa;
                const bottom = finite$a(values[b1][a0], '$.grid') * (1 - fa) + finite$a(values[b1][a1], '$.grid') * fa;
                return top * (1 - fb) + bottom * fb;
            };
            const x = interpolate(grid.x);
            const y = interpolate(grid.y);
            return {
                id: point.id?.trim() || `carpet-${index}`,
                logical: { a, b },
                projected: { x, y },
                masked,
                tooltip: `a=${a}, b=${b} · x=${x}, y=${y}`,
            };
        });
        return {
            points: projected,
            ticks: {
                a: grid.a.map((value) => ({ value, label: String(value) })),
                b: grid.b.map((value) => ({ value, label: String(value) })),
            },
        };
    }
    /** Expands waffle/isotype counts into grouped units with partial-unit and fill-direction semantics. */
    function layoutItems(groups, options = {}) {
        const unit = finite$a(options.unit ?? 1, '$.unit');
        if (unit <= 0)
            throw new GraflumeError('INVALID_SPEC', '$.unit must be positive.');
        const columns = clamp$c(Math.floor(options.columns ?? 10), 1, 1_000);
        const expanded = [];
        groups.forEach((group, groupIndex) => {
            const id = text$1(group.id, `$.groups[${groupIndex}].id`);
            const value = finite$a(group.value, `$.groups[${groupIndex}].value`);
            if (value < 0)
                throw new GraflumeError('INVALID_DATA', 'Item values must be non-negative.');
            const exact = value / unit;
            const count = options.partial === 'floor'
                ? Math.floor(exact)
                : options.partial === 'ceil'
                    ? Math.ceil(exact)
                    : options.partial === 'round'
                        ? Math.round(exact)
                        : Math.ceil(exact);
            for (let index = 0; index < count; index += 1) {
                const fraction = options.partial === 'fraction' || options.partial === undefined
                    ? clamp$c(exact - index, 0, 1)
                    : 1;
                if (fraction <= 0)
                    continue;
                const global = expanded.length;
                expanded.push({
                    index: global,
                    group: id,
                    amount: Math.min(unit, Math.max(0, value - index * unit)),
                    fraction,
                    accessibleLabel: `${id}: ${Math.min(unit, Math.max(0, value - index * unit))} of ${value}`,
                });
            }
        });
        const direction = options.direction ?? 'row';
        const rows = Math.max(1, Math.ceil(expanded.length / columns));
        const output = expanded.map((item, global) => {
            let row;
            let column;
            if (direction === 'column' || direction === 'column-reverse') {
                row = global % rows;
                column = Math.floor(global / rows);
                if (direction === 'column-reverse')
                    row = rows - 1 - row;
            }
            else {
                row = Math.floor(global / columns);
                column = global % columns;
                if (direction === 'row-reverse')
                    column = columns - 1 - column;
            }
            return { ...item, row, column };
        });
        return {
            mode: options.mode ?? 'waffle',
            unit,
            columns,
            units: output,
            summary: groups.map(({ id, value }) => `${id}: ${value}`).join('; '),
            total: groups.reduce((sum, { value }) => sum + value, 0),
        };
    }
    /** Creates an explicit upper/lower/diagonal scatter-matrix composition plan. */
    function scatterMatrixPlan(rows, options = {}) {
        const variables = options.variables === undefined
            ? [
                ...new Set(rows.flatMap((row) => Object.entries(row).flatMap(([field, value]) => typeof value === 'number' && Number.isFinite(value) ? [field] : []))),
            ]
            : options.variables.map((field, index) => text$1(field, `$.variables[${index}]`));
        if (variables.length < 2)
            throw new GraflumeError('INVALID_SPEC', 'Scatter matrix needs at least two numeric variables.');
        const cells = variables.flatMap((y, row) => variables.map((x, column) => ({
            row,
            column,
            x,
            y,
            kind: row === column
                ? (options.diagonal ?? 'kde')
                : row < column
                    ? (options.upper ?? 'scatter')
                    : (options.lower ?? 'scatter'),
            linkedSelectionKey: 'scatter-matrix',
        })));
        return { variables, cells };
    }

    class BandScale {
        kind = 'band';
        descriptor;
        #domain;
        #range;
        #positions = new Map();
        #outOfBounds;
        step;
        bandwidth;
        constructor(options) {
            this.#domain = Object.freeze([...options.domain]);
            this.#range = Object.freeze(options.reverse === true ? [options.range[1], options.range[0]] : [...options.range]);
            this.#outOfBounds = options.outOfBounds ?? 'unknown';
            const paddingInner = clamp$d(options.paddingInner ?? 0.1, 0, 1);
            const paddingOuter = Math.max(0, options.paddingOuter ?? 0.05);
            const [start, end] = this.#range;
            const direction = end >= start ? 1 : -1;
            const span = Math.abs(end - start);
            const denominator = Math.max(1, this.#domain.length - paddingInner + paddingOuter * 2);
            const step = span / denominator;
            this.step = step;
            this.bandwidth = step * (1 - paddingInner);
            this.#domain.forEach((value, index) => {
                const position = start + direction * step * (paddingOuter + index);
                this.#positions.set(value, position);
            });
            this.descriptor = Object.freeze({
                type: this.kind,
                domain: this.#domain,
                range: this.#range,
                reverse: options.reverse ?? false,
                rangeDirection: end < start ? 'descending' : 'ascending',
                outOfBounds: this.#outOfBounds,
            });
        }
        domain() {
            return this.#domain;
        }
        range() {
            return this.#range;
        }
        map(input) {
            const value = input instanceof Date ? input.toISOString() : String(input);
            const position = this.#positions.get(value);
            if (position !== undefined)
                return position + this.bandwidth / 2;
            if (this.#outOfBounds === 'error') {
                throw new GraflumeError('INVALID_DATA', `Unknown band value: ${value}`, {
                    path: '$.data',
                });
            }
            return Number.NaN;
        }
        start(input) {
            return this.map(input) - this.bandwidth / 2;
        }
        ticks(count) {
            const domain = this.#range[1] < this.#range[0] ? [...this.#domain].reverse() : this.#domain;
            const step = Math.max(1, Math.ceil(domain.length / Math.max(1, count)));
            const ticks = [];
            for (let index = 0; index < domain.length; index += step) {
                const value = domain[index];
                if (value === undefined)
                    continue;
                ticks.push({ value, label: value, position: this.map(value) });
            }
            const last = domain.at(-1);
            if (last !== undefined && ticks.at(-1)?.value !== last) {
                ticks.push({ value: last, label: last, position: this.map(last) });
            }
            return ticks;
        }
    }

    /** Resolves canonical/default labels and unknown compatibility values to histogram mode. */
    function resolveDistributionMode(value) {
        if (value === 'box' || value === 'boxplot')
            return 'boxplot';
        if (value === 'violin')
            return 'violin';
        if (value === 'curve' || value === 'bell-curve')
            return 'curve';
        if (value === 'kde' || value === 'density')
            return 'kde';
        if (value === 'ecdf')
            return 'ecdf';
        if (value === 'ccdf' || value === 'survival')
            return 'ccdf';
        if (value === 'rug')
            return 'rug';
        if (value === 'strip')
            return 'strip';
        if (value === 'histogram-2d' || value === 'bivariate')
            return 'histogram-2d';
        if (value === 'histogram-2d-contour' || value === 'bivariate-contour') {
            return 'histogram-2d-contour';
        }
        return 'histogram';
    }

    function tickStep(start, stop, count) {
        const span = Math.abs(stop - start);
        if (span === 0 || count <= 0)
            return 0;
        const raw = span / count;
        const power = Math.floor(Math.log10(raw));
        const magnitude = 10 ** power;
        const error = raw / magnitude;
        const factor = error >= Math.sqrt(50) ? 10 : error >= Math.sqrt(10) ? 5 : error >= Math.sqrt(2) ? 2 : 1;
        return factor * magnitude;
    }
    function niceDomain(domain, count = 5) {
        let [start, stop] = domain;
        if (start === stop) {
            const delta = start === 0 ? 1 : Math.abs(start) * 0.05;
            return [start - delta, stop + delta];
        }
        const step = tickStep(start, stop, count);
        if (step === 0)
            return domain;
        const reverse = stop < start;
        if (reverse)
            [start, stop] = [stop, start];
        const niceStart = Math.floor(start / step) * step;
        const niceStop = Math.ceil(stop / step) * step;
        return reverse ? [niceStop, niceStart] : [niceStart, niceStop];
    }
    class LinearScale {
        kind;
        bandwidth = 0;
        descriptor;
        #domain;
        #range;
        #outOfBounds;
        constructor(options) {
            this.kind = options.kind ?? 'linear';
            this.#domain = Object.freeze([
                ...(options.nice === false ? options.domain : niceDomain(options.domain)),
            ]);
            this.#range = Object.freeze(options.reverse === true ? [options.range[1], options.range[0]] : [...options.range]);
            this.#outOfBounds = options.outOfBounds ?? (options.clamp === true ? 'clamp' : 'extrapolate');
            this.descriptor = Object.freeze({
                type: this.kind,
                domain: this.#domain,
                range: this.#range,
                reverse: options.reverse ?? false,
                rangeDirection: this.#range[1] < this.#range[0] ? 'descending' : 'ascending',
                outOfBounds: this.#outOfBounds,
            });
        }
        domain() {
            return this.#domain;
        }
        range() {
            return this.#range;
        }
        #ratio(value, start, end) {
            const denominator = end - start;
            let ratio = denominator === 0 ? 0.5 : (value - start) / denominator;
            if (ratio < 0 || ratio > 1) {
                if (this.#outOfBounds === 'unknown')
                    return Number.NaN;
                if (this.#outOfBounds === 'error') {
                    throw new GraflumeError('INVALID_DATA', 'Scale input is outside the domain.', {
                        path: '$.data',
                    });
                }
                if (this.#outOfBounds === 'clamp')
                    ratio = clamp$d(ratio, 0, 1);
            }
            return ratio;
        }
        map(input) {
            const value = input instanceof Date
                ? input.getTime()
                : typeof input === 'string'
                    ? Date.parse(input)
                    : input;
            if (!Number.isFinite(value))
                return Number.NaN;
            const [domainStart, domainEnd] = this.#domain;
            const [rangeStart, rangeEnd] = this.#range;
            const ratio = this.#ratio(value, domainStart, domainEnd);
            return rangeStart + ratio * (rangeEnd - rangeStart);
        }
        invert = (position) => {
            const [domainStart, domainEnd] = this.#domain;
            const [rangeStart, rangeEnd] = this.#range;
            const ratio = this.#ratio(position, rangeStart, rangeEnd);
            return domainStart + ratio * (domainEnd - domainStart);
        };
        ticks(count, locale) {
            const [start, stop] = this.#domain;
            const step = tickStep(start, stop, Math.max(1, count));
            if (step === 0) {
                const position = this.map(start);
                return [{ value: start, label: this.#format(start, locale), position }];
            }
            const first = Math.ceil(Math.min(start, stop) / step) * step;
            const last = Math.floor(Math.max(start, stop) / step) * step;
            const values = [];
            for (let value = first; value <= last + step / 2; value += step)
                values.push(value);
            if (stop < start)
                values.reverse();
            return values.map((value) => ({
                value,
                label: this.#format(value, locale),
                position: this.map(value),
            }));
        }
        #format(value, locale) {
            if (this.kind === 'time' || this.kind === 'utc') {
                return new Intl.DateTimeFormat(locale, {
                    year: 'numeric',
                    month: 'short',
                    day: 'numeric',
                    ...(this.kind === 'utc' ? { timeZone: 'UTC' } : {}),
                }).format(new Date(value));
            }
            return new Intl.NumberFormat(locale, { maximumFractionDigits: 6 }).format(value);
        }
    }

    function ordinalKey(value) {
        return `${typeof value}:${String(value)}`;
    }
    function frozen(values) {
        return Object.freeze([...values]);
    }
    function fail$3(message, path = '$.scale') {
        throw new GraflumeError('INCOMPATIBLE_SCALE', message, { path });
    }
    function numericDomain$1(values, length = 2) {
        if (values.length !== length ||
            values.some((value) => typeof value !== 'number' || !Number.isFinite(value))) {
            fail$3(`Scale domain must contain exactly ${length} finite numbers.`);
        }
        return values;
    }
    function numericRange(values, minimum = 2) {
        if (values.length < minimum ||
            values.some((value) => typeof value !== 'number' || !Number.isFinite(value))) {
            fail$3(`Position scale range must contain at least ${minimum} finite numbers.`, '$.scale.range');
        }
        return values;
    }
    function numericRangePair(values) {
        const range = numericRange(values, 2);
        if (range.length !== 2) {
            fail$3('Continuous, band, and point position ranges must contain exactly 2 numbers.', '$.scale.range');
        }
        return range;
    }
    function resolvedPolicy(spec, fallback) {
        return spec.outOfBounds ?? (spec.clamp === true ? 'clamp' : fallback);
    }
    function normalizeRatio(value, domain, policy, path) {
        const denominator = domain[1] - domain[0];
        let ratio = denominator === 0 ? 0.5 : (value - domain[0]) / denominator;
        if (ratio >= 0 && ratio <= 1)
            return ratio;
        if (policy === 'clamp')
            return clamp$d(ratio, 0, 1);
        if (policy === 'unknown')
            return Number.NaN;
        if (policy === 'error') {
            throw new GraflumeError('INVALID_DATA', `Scale input ${value} is outside the domain.`, {
                path,
            });
        }
        return ratio;
    }
    function interpolateRange(range, ratio) {
        if (!Number.isFinite(ratio))
            return Number.NaN;
        const scaled = ratio * (range.length - 1);
        const index = Math.max(0, Math.min(range.length - 2, Math.floor(scaled)));
        const start = range[index] ?? Number.NaN;
        const end = range[index + 1] ?? start;
        return start + (end - start) * (scaled - index);
    }
    function inverseNormal(probability) {
        // Acklam's rational approximation; absolute error is below 1.2e-9 over (0, 1).
        const a = [
            -39.6968302866538, 220.946098424521, -275.928510446969, 138.357751867269, -30.6647980661472,
            2.50662827745924,
        ];
        const b = [
            -54.4760987982241, 161.585836858041, -155.698979859887, 66.8013118877197, -13.2806815528857,
        ];
        const c = [
            -0.00778489400243029, -0.322396458041136, -2.40075827716184, -2.54973253934373,
            4.37466414146497, 2.93816398269878,
        ];
        const d = [0.00778469570904146, 0.32246712907004, 2.445134137143, 3.75440866190742];
        const low = 0.02425;
        const high = 1 - low;
        if (probability < low) {
            const q = Math.sqrt(-2 * Math.log(probability));
            return ((((((c[0] * q + c[1]) * q + c[2]) * q + c[3]) * q + c[4]) * q + c[5]) /
                ((((d[0] * q + d[1]) * q + d[2]) * q + d[3]) * q + 1));
        }
        if (probability > high) {
            const q = Math.sqrt(-2 * Math.log(1 - probability));
            return (-(((((c[0] * q + c[1]) * q + c[2]) * q + c[3]) * q + c[4]) * q + c[5]) /
                ((((d[0] * q + d[1]) * q + d[2]) * q + d[3]) * q + 1));
        }
        const q = probability - 0.5;
        const r = q * q;
        return (((((((a[0] * r + a[1]) * r + a[2]) * r + a[3]) * r + a[4]) * r + a[5]) * q) /
            (((((b[0] * r + b[1]) * r + b[2]) * r + b[3]) * r + b[4]) * r + 1));
    }
    function normalCdf(value) {
        // Abramowitz-Stegun 7.1.26, sufficient for stable interactive inversion.
        const sign = value < 0 ? -1 : 1;
        const x = Math.abs(value) / Math.SQRT2;
        const t = 1 / (1 + 0.3275911 * x);
        const erf = 1 -
            ((((1.061405429 * t - 1.453152027) * t + 1.421413741) * t - 0.284496736) * t + 0.254829592) *
                t *
                Math.exp(-x * x);
        return 0.5 * (1 + sign * erf);
    }
    function transformFor(type, spec) {
        const identity = (value) => value;
        if (type === 'log') {
            const base = spec.base ?? 10;
            if (!(base > 0) || base === 1)
                fail$3('Log scale base must be positive and not equal to 1.', '$.scale.base');
            return {
                forward: (value) => Math.log(value) / Math.log(base),
                inverse: (value) => base ** value,
                validate: ([start, end]) => {
                    if (!(start > 0 && end > 0))
                        fail$3('Log scale domain values must be greater than 0.', '$.scale.domain');
                },
            };
        }
        if (type === 'symlog') {
            const constant = spec.constant ?? 1;
            if (!(constant > 0))
                fail$3('Symlog constant must be greater than 0.', '$.scale.constant');
            return {
                forward: (value) => Math.sign(value) * Math.log1p(Math.abs(value) / constant),
                inverse: (value) => Math.sign(value) * Math.expm1(Math.abs(value)) * constant,
                validate: () => undefined,
            };
        }
        if (type === 'asinh') {
            const constant = spec.constant ?? 1;
            if (!(constant > 0))
                fail$3('Asinh constant must be greater than 0.', '$.scale.constant');
            return {
                forward: (value) => Math.asinh(value / constant),
                inverse: (value) => Math.sinh(value) * constant,
                validate: () => undefined,
            };
        }
        if (type === 'pow' || type === 'sqrt') {
            const exponent = type === 'sqrt' ? 0.5 : (spec.exponent ?? 1);
            if (!(exponent > 0))
                fail$3('Power scale exponent must be greater than 0.', '$.scale.exponent');
            return {
                forward: (value) => Math.sign(value) * Math.abs(value) ** exponent,
                inverse: (value) => Math.sign(value) * Math.abs(value) ** (1 / exponent),
                validate: () => undefined,
            };
        }
        if (type === 'logit') {
            return {
                forward: (value) => Math.log(value / (1 - value)),
                inverse: (value) => 1 / (1 + Math.exp(-value)),
                validate: ([start, end]) => {
                    if (!(start > 0 && start < 1 && end > 0 && end < 1))
                        fail$3('Logit scale domain values must be strictly between 0 and 1.', '$.scale.domain');
                },
            };
        }
        if (type === 'probit') {
            return {
                forward: inverseNormal,
                inverse: normalCdf,
                validate: ([start, end]) => {
                    if (!(start > 0 && start < 1 && end > 0 && end < 1))
                        fail$3('Probit scale domain values must be strictly between 0 and 1.', '$.scale.domain');
                },
            };
        }
        return {
            forward: identity,
            inverse: identity,
            validate: ([start, end]) => {
                if (type === 'probability' && !(start >= 0 && start <= 1 && end >= 0 && end <= 1)) {
                    fail$3('Probability scale domain values must be between 0 and 1.', '$.scale.domain');
                }
            },
        };
    }
    class ContinuousScale {
        bandwidth = 0;
        descriptor;
        #domain;
        #range;
        #policy;
        #transform;
        constructor(type, spec, domain, range) {
            this.kind = type;
            this.#domain = frozen(domain);
            this.#range = frozen(spec.reverse === true ? [...range].reverse() : range);
            this.#policy = resolvedPolicy(spec, 'extrapolate');
            this.#transform = transformFor(type, spec);
            this.#transform.validate(this.#domain);
            if (this.#domain[0] === this.#domain[1])
                fail$3('Continuous scale domain endpoints must differ.', '$.scale.domain');
            this.descriptor = Object.freeze({
                type,
                domain: this.#domain,
                range: this.#range,
                reverse: spec.reverse === true,
                rangeDirection: this.#range.at(-1) < this.#range[0] ? 'descending' : 'ascending',
                outOfBounds: this.#policy,
            });
        }
        kind;
        domain() {
            return this.#domain;
        }
        range() {
            return this.#range;
        }
        map(input) {
            const value = input instanceof Date
                ? input.getTime()
                : typeof input === 'string'
                    ? Date.parse(input)
                    : input;
            if (!Number.isFinite(value))
                return Number.NaN;
            const outsideMathematicalDomain = (this.kind === 'log' && value <= 0) ||
                (this.kind === 'probability' && !(value >= 0 && value <= 1)) ||
                ((this.kind === 'logit' || this.kind === 'probit') && !(value > 0 && value < 1));
            let supportedValue = value;
            if (outsideMathematicalDomain) {
                if (this.#policy === 'unknown')
                    return Number.NaN;
                if (this.#policy === 'clamp') {
                    const minimum = Math.min(this.#domain[0], this.#domain[1]);
                    const maximum = Math.max(this.#domain[0], this.#domain[1]);
                    supportedValue = clamp$d(value, minimum, maximum);
                }
                else {
                    throw new GraflumeError('INVALID_DATA', `Value ${value} is outside the mathematical domain of ${this.kind}; it cannot be extrapolated.`, { path: '$.data' });
                }
            }
            const transformedDomain = [
                this.#transform.forward(this.#domain[0]),
                this.#transform.forward(this.#domain[1]),
            ];
            const ratio = normalizeRatio(this.#transform.forward(supportedValue), transformedDomain, this.#policy, '$.data');
            return interpolateRange(this.#range, ratio);
        }
        invert = (position) => {
            const rangePair = [this.#range[0], this.#range.at(-1)];
            const ratio = normalizeRatio(position, rangePair, this.#policy, '$.position');
            if (!Number.isFinite(ratio))
                return Number.NaN;
            const start = this.#transform.forward(this.#domain[0]);
            const end = this.#transform.forward(this.#domain[1]);
            return this.#transform.inverse(start + (end - start) * ratio);
        };
        ticks(count, locale) {
            const size = Math.max(1, Math.floor(count));
            const start = this.#transform.forward(this.#domain[0]);
            const end = this.#transform.forward(this.#domain[1]);
            return Array.from({ length: size + 1 }, (_, index) => {
                const value = this.#transform.inverse(start + ((end - start) * index) / size);
                const label = this.kind === 'time' || this.kind === 'utc'
                    ? new Intl.DateTimeFormat(locale, {
                        year: 'numeric',
                        month: 'short',
                        day: 'numeric',
                        timeZone: this.kind === 'utc' ? 'UTC' : undefined,
                    }).format(new Date(value))
                    : new Intl.NumberFormat(locale, { maximumFractionDigits: 6 }).format(value);
                return { value, label, position: this.map(value) };
            });
        }
    }
    class PointScale {
        kind = 'point';
        bandwidth = 0;
        descriptor;
        #domain;
        #range;
        #positions = new Map();
        #policy;
        constructor(spec, domain, range) {
            this.#domain = frozen(domain);
            this.#range = frozen(spec.reverse === true ? [range[1], range[0]] : range);
            this.#policy = resolvedPolicy(spec, 'unknown');
            if (this.#policy === 'clamp' || this.#policy === 'extrapolate') {
                fail$3('Point scales support only error or unknown for unseen categories.', '$.scale.outOfBounds');
            }
            const padding = Math.max(0, spec.paddingOuter ?? 0.5);
            const denominator = Math.max(1, this.#domain.length - 1 + padding * 2);
            this.#domain.forEach((value, index) => this.#positions.set(value, this.#range[0] + ((this.#range[1] - this.#range[0]) * (index + padding)) / denominator));
            this.descriptor = Object.freeze({
                type: this.kind,
                domain: this.#domain,
                range: this.#range,
                reverse: spec.reverse === true,
                rangeDirection: this.#range[1] < this.#range[0] ? 'descending' : 'ascending',
                outOfBounds: this.#policy,
            });
        }
        domain() {
            return this.#domain;
        }
        range() {
            return this.#range;
        }
        map(input) {
            const value = input instanceof Date ? input.toISOString() : String(input);
            const mapped = this.#positions.get(value);
            if (mapped !== undefined)
                return mapped;
            if (this.#policy === 'error')
                throw new GraflumeError('INVALID_DATA', `Unknown point scale value: ${value}`, {
                    path: '$.data',
                });
            return Number.NaN;
        }
        ticks(count) {
            const domain = this.#range[1] < this.#range[0] ? [...this.#domain].reverse() : this.#domain;
            const step = Math.max(1, Math.ceil(domain.length / Math.max(1, count)));
            return domain
                .filter((_value, index) => index % step === 0)
                .map((value) => ({ value, label: value, position: this.map(value) }));
        }
    }
    function quantile$1(sorted, probability) {
        const index = (sorted.length - 1) * probability;
        const lower = Math.floor(index);
        const upper = Math.ceil(index);
        const start = sorted[lower] ?? sorted[0] ?? Number.NaN;
        const end = sorted[upper] ?? start;
        return start + (end - start) * (index - lower);
    }
    class DiscretePositionScale {
        kind;
        bandwidth = 0;
        descriptor;
        #domain;
        #range;
        #policy;
        #thresholds;
        #ordinalIndices = new Map();
        constructor(kind, spec, domain, range) {
            this.kind = kind;
            this.#domain = frozen(domain);
            this.#range = frozen(spec.reverse === true ? [...range].reverse() : range);
            this.#policy = resolvedPolicy(spec, kind === 'quantize' ? 'clamp' : 'unknown');
            if (kind === 'ordinal' && (this.#policy === 'clamp' || this.#policy === 'extrapolate')) {
                fail$3('Ordinal scales support only error or unknown for unseen categories.', '$.scale.outOfBounds');
            }
            if (kind === 'quantize' && this.#policy === 'extrapolate') {
                fail$3('Quantize scales cannot extrapolate discrete range values.', '$.scale.outOfBounds');
            }
            if ((kind === 'quantile' || kind === 'threshold') &&
                (spec.outOfBounds !== undefined || spec.clamp !== undefined)) {
                fail$3(`${kind} scale bin semantics do not accept outOfBounds.`, '$.scale.outOfBounds');
            }
            if (kind === 'ordinal' && this.#range.length < 1)
                fail$3('Ordinal scale requires at least one range value.', '$.scale.range');
            if (kind === 'ordinal') {
                this.#domain.forEach((value, index) => {
                    const id = ordinalKey(value);
                    if (!this.#ordinalIndices.has(id))
                        this.#ordinalIndices.set(id, index);
                });
            }
            if (kind !== 'ordinal' && this.#domain.some((value) => typeof value !== 'number'))
                fail$3(`${kind} scale domain must be numeric.`, '$.scale.domain');
            const authoredNumbers = this.#domain;
            const numbers = [...authoredNumbers].sort((a, b) => a - b);
            if (kind === 'quantile') {
                if (numbers.length < 2) {
                    fail$3('Quantile scale domain must contain at least 2 numeric samples.', '$.scale.domain');
                }
                this.#thresholds = frozen(Array.from({ length: Math.max(0, this.#range.length - 1) }, (_, index) => quantile$1(numbers, (index + 1) / this.#range.length)));
            }
            else if (kind === 'quantize') {
                if (numbers.length !== 2 || numbers[0] === numbers[1])
                    fail$3('Quantize scale requires two distinct numeric domain endpoints.', '$.scale.domain');
                this.#thresholds = frozen(Array.from({ length: Math.max(0, this.#range.length - 1) }, (_, index) => numbers[0] + ((numbers[1] - numbers[0]) * (index + 1)) / this.#range.length));
            }
            else if (kind === 'threshold') {
                if (authoredNumbers.some((value, index) => index > 0 && value <= authoredNumbers[index - 1])) {
                    fail$3('Threshold scale domain must be strictly ascending.', '$.scale.domain');
                }
                if (this.#range.length !== numbers.length + 1)
                    fail$3('Threshold scale range length must equal domain length + 1.', '$.scale.range');
                this.#thresholds = frozen(authoredNumbers);
            }
            else
                this.#thresholds = [];
            this.descriptor = Object.freeze({
                type: kind,
                domain: this.#domain,
                range: this.#range,
                reverse: spec.reverse === true,
                rangeDirection: this.#range.at(-1) < this.#range[0] ? 'descending' : 'ascending',
                outOfBounds: this.#policy,
            });
        }
        domain() {
            return this.#domain;
        }
        range() {
            return this.#range;
        }
        map(input) {
            const value = input instanceof Date ? input.getTime() : input;
            if (this.kind === 'ordinal') {
                const index = this.#ordinalIndices.get(ordinalKey(value));
                if (index !== undefined)
                    return this.#range[index % this.#range.length];
                if (this.#policy === 'error')
                    throw new GraflumeError('INVALID_DATA', `Unknown ordinal scale value: ${String(value)}`, {
                        path: '$.data',
                    });
                return Number.NaN;
            }
            const number = typeof value === 'number' ? value : Number(value);
            if (!Number.isFinite(number))
                return Number.NaN;
            if (this.kind === 'quantize') {
                const endpoints = this.#domain;
                const minimum = Math.min(endpoints[0], endpoints[1]);
                const maximum = Math.max(endpoints[0], endpoints[1]);
                if (number < minimum || number > maximum) {
                    if (this.#policy === 'unknown')
                        return Number.NaN;
                    if (this.#policy === 'error') {
                        throw new GraflumeError('INVALID_DATA', `Quantize input ${number} is outside the domain.`, { path: '$.data' });
                    }
                }
            }
            let index = 0;
            while (index < this.#thresholds.length && number >= this.#thresholds[index])
                index += 1;
            return this.#range[Math.min(index, this.#range.length - 1)] ?? Number.NaN;
        }
        ticks(_count, locale) {
            if (this.kind === 'ordinal')
                return this.#domain.map((value) => ({
                    value,
                    label: String(value),
                    position: this.map(value),
                }));
            return this.#thresholds.map((value) => ({
                value,
                label: new Intl.NumberFormat(locale, { maximumFractionDigits: 6 }).format(value),
                position: this.map(value),
            }));
        }
    }
    function createPositionScale(spec, options) {
        if (spec.type !== undefined && options.type !== undefined && spec.type !== options.type) {
            fail$3(`Scale type ${spec.type} conflicts with the requested position scale type ${options.type}.`, '$.scale.type');
        }
        const requestedType = spec.type ?? options.type ?? 'linear';
        if (requestedType === 'sequential' ||
            requestedType === 'diverging' ||
            requestedType === 'cyclic') {
            fail$3(`${requestedType} is a color scale and cannot produce positions.`, '$.scale.type');
        }
        const type = requestedType;
        const domain = spec.domain ?? options.domain;
        const range = spec.range === undefined ? options.range : numericRange(spec.range);
        if (type === 'band') {
            if (domain.some((value) => typeof value !== 'string'))
                fail$3('Band scale domain must contain strings.', '$.scale.domain');
            const pair = numericRangePair(range);
            const policy = resolvedPolicy(spec, 'unknown');
            if (policy === 'clamp' || policy === 'extrapolate') {
                fail$3('Band scales support only error or unknown for unseen categories.', '$.scale.outOfBounds');
            }
            return new BandScale({
                domain: domain,
                range: [pair[0], pair.at(-1)],
                ...(spec.paddingInner === undefined ? {} : { paddingInner: spec.paddingInner }),
                ...(spec.paddingOuter === undefined ? {} : { paddingOuter: spec.paddingOuter }),
                outOfBounds: policy,
                ...(spec.reverse === undefined ? {} : { reverse: spec.reverse }),
            });
        }
        if (type === 'point') {
            if (domain.some((value) => typeof value !== 'string'))
                fail$3('Point scale domain must contain strings.', '$.scale.domain');
            const pair = numericRangePair(range);
            return new PointScale(spec, domain, [pair[0], pair.at(-1)]);
        }
        if (type === 'ordinal' || type === 'quantile' || type === 'quantize' || type === 'threshold')
            return new DiscretePositionScale(type, spec, domain, numericRange(range, 1));
        const resolvedContinuousDomain = type === 'time' || type === 'utc'
            ? domain.map((value) => (typeof value === 'number' ? value : Date.parse(value)))
            : domain;
        const pair = numericDomain$1(resolvedContinuousDomain);
        if (type === 'linear' || type === 'time' || type === 'utc') {
            const numeric = numericRangePair(range);
            return new LinearScale({
                domain: pair,
                range: [numeric[0], numeric.at(-1)],
                kind: type,
                ...(spec.nice === undefined ? {} : { nice: spec.nice }),
                outOfBounds: resolvedPolicy(spec, 'extrapolate'),
                ...(spec.reverse === undefined ? {} : { reverse: spec.reverse }),
            });
        }
        return new ContinuousScale(type, spec, pair, numericRangePair(range));
    }
    function interpolateColors(range, ratio) {
        const bounded = clamp$d(ratio, 0, 1);
        if (range.length === 1)
            return range[0];
        const scaled = bounded * (range.length - 1);
        const index = Math.min(range.length - 2, Math.floor(scaled));
        return mixColor$1(range[index], range[index + 1], scaled - index);
    }
    function interpolateCyclicColors(range, ratio) {
        const wrapped = ((ratio % 1) + 1) % 1;
        const scaled = wrapped * range.length;
        const index = Math.floor(scaled) % range.length;
        const next = (index + 1) % range.length;
        return mixColor$1(range[index], range[next], scaled - Math.floor(scaled));
    }
    class RegisteredColorScale {
        kind;
        descriptor;
        #domain;
        #range;
        #policy;
        #ordinalIndices = new Map();
        constructor(kind, spec, domain, range) {
            this.kind = kind;
            this.#domain = frozen(domain);
            this.#range = frozen(spec.reverse === true ? [...range].reverse() : range);
            this.#policy = resolvedPolicy(spec, kind === 'ordinal' ? 'unknown' : 'clamp');
            if (kind === 'cyclic' && (spec.outOfBounds !== undefined || spec.clamp !== undefined)) {
                fail$3('Cyclic scales always wrap and do not accept outOfBounds.', '$.scale.outOfBounds');
            }
            if (this.#policy === 'extrapolate' && kind !== 'cyclic') {
                fail$3('Color scales do not extrapolate colors; use clamp, error, or unknown.', '$.scale.outOfBounds');
            }
            if (kind === 'ordinal' && this.#policy === 'clamp') {
                fail$3('Ordinal color scales support only error or unknown for unseen categories.', '$.scale.outOfBounds');
            }
            if (kind === 'ordinal') {
                this.#domain.forEach((value, index) => {
                    const id = ordinalKey(value);
                    if (!this.#ordinalIndices.has(id))
                        this.#ordinalIndices.set(id, index);
                });
            }
            if (this.#range.length === 0)
                fail$3('Color scale requires at least one color in range.', '$.scale.range');
            if (kind === 'sequential' && this.#range.length < 2)
                fail$3('Sequential color scale requires at least 2 colors.', '$.scale.range');
            if (kind === 'diverging' && this.#range.length < 3)
                fail$3('Diverging color scale requires at least 3 colors.', '$.scale.range');
            if (kind === 'cyclic' && this.#range.length < 2)
                fail$3('Cyclic color scale requires at least 2 colors.', '$.scale.range');
            if (kind === 'diverging')
                numericDomain$1(domain, 3);
            else if (kind !== 'ordinal')
                numericDomain$1(domain);
            if (kind === 'diverging') {
                const [start, middle, end] = domain;
                if (!(start < middle && middle < end)) {
                    fail$3('Diverging color scale domain must be strictly ascending.', '$.scale.domain');
                }
            }
            else if (kind !== 'ordinal') {
                const [start, end] = domain;
                if (start === end)
                    fail$3(`${kind} color scale domain endpoints must differ.`, '$.scale.domain');
            }
            this.descriptor = Object.freeze({
                type: kind,
                domain: this.#domain,
                range: this.#range,
                reverse: spec.reverse === true,
                outOfBounds: kind === 'cyclic' ? 'wrap' : this.#policy,
            });
        }
        domain() {
            return this.#domain;
        }
        range() {
            return this.#range;
        }
        map(input) {
            const value = input instanceof Date ? input.getTime() : input;
            if (this.kind === 'ordinal') {
                const index = this.#ordinalIndices.get(ordinalKey(value));
                if (index !== undefined)
                    return this.#range[index % this.#range.length];
                if (this.#policy === 'error')
                    throw new GraflumeError('INVALID_DATA', `Unknown ordinal color value: ${String(value)}`, {
                        path: '$.data',
                    });
                return specUnknownColor(this.#policy);
            }
            const number = typeof value === 'number' ? value : Number(value);
            if (!Number.isFinite(number))
                return specUnknownColor(this.#policy);
            if (this.kind === 'cyclic') {
                const domain = this.#domain;
                const span = domain[1] - domain[0];
                const ratio = span === 0 ? 0 : ((((number - domain[0]) / span) % 1) + 1) % 1;
                return interpolateCyclicColors(this.#range, ratio);
            }
            if (this.kind === 'diverging') {
                const [start, middle, end] = this.#domain;
                const ratio = number <= middle
                    ? normalizeRatio(number, [start, middle], this.#policy, '$.data') / 2
                    : 0.5 + normalizeRatio(number, [middle, end], this.#policy, '$.data') / 2;
                return Number.isFinite(ratio)
                    ? interpolateColors(this.#range, ratio)
                    : specUnknownColor(this.#policy);
            }
            const ratio = normalizeRatio(number, this.#domain, this.#policy, '$.data');
            return Number.isFinite(ratio)
                ? interpolateColors(this.#range, ratio)
                : specUnknownColor(this.#policy);
        }
    }
    function specUnknownColor(policy) {
        if (policy === 'error')
            throw new GraflumeError('INVALID_DATA', 'Color scale input is outside the domain.', {
                path: '$.data',
            });
        return 'transparent';
    }
    function createColorScale(spec, fallback) {
        if (spec.type !== undefined &&
            spec.type !== 'ordinal' &&
            spec.type !== 'sequential' &&
            spec.type !== 'diverging' &&
            spec.type !== 'cyclic') {
            fail$3(`${spec.type} is not a color registry scale.`, '$.scale.type');
        }
        const type = spec.type === 'sequential' || spec.type === 'diverging' || spec.type === 'cyclic'
            ? spec.type
            : 'ordinal';
        const domain = spec.domain ?? fallback.domain;
        const rawRange = spec.range ?? fallback.range;
        if (rawRange.some((value) => typeof value !== 'string'))
            fail$3('Color scale range must contain strings.', '$.scale.range');
        return new RegisteredColorScale(type, spec, domain, rawRange);
    }
    const positionScaleTypes = Object.freeze([
        'linear',
        'log',
        'symlog',
        'asinh',
        'pow',
        'sqrt',
        'time',
        'utc',
        'band',
        'point',
        'ordinal',
        'quantile',
        'quantize',
        'threshold',
        'probability',
        'logit',
        'probit',
    ]);
    const colorScaleTypes = Object.freeze([
        'ordinal',
        'sequential',
        'diverging',
        'cyclic',
    ]);

    const shapeRange = ['circle', 'square', 'diamond', 'triangle', 'cross'];
    const dashRange = [[], [7, 4], [2, 3], [10, 3, 2, 3], [1, 3]];
    function truthy(value) {
        return value !== false && value !== null && value !== undefined && value !== 0 && value !== '';
    }
    function key(value) {
        if (value instanceof Date)
            return value.toISOString();
        return Array.isArray(value) ? JSON.stringify(value) : `${typeof value}:${String(value)}`;
    }
    function numeric$1(value) {
        if (value instanceof Date)
            return value.getTime();
        if (typeof value === 'number' && Number.isFinite(value))
            return value;
        if (typeof value === 'string' && value.trim() !== '') {
            const parsed = Number(value);
            if (Number.isFinite(parsed))
                return parsed;
            const date = Date.parse(value);
            return Number.isFinite(date) ? date : null;
        }
        return null;
    }
    function channelSpec(context, channel) {
        if (channel === 'x' || channel === 'y') {
            return { ...context.layer.encoding[channel], condition: [] };
        }
        return context.layer.encoding[channel];
    }
    function resolvedFrom(spec, row) {
        for (const condition of spec.condition) {
            if (!truthy(evaluateTransformExpression(condition.test, row)))
                continue;
            if (condition.field !== undefined)
                return { value: row[condition.field], literal: false };
            return { value: condition.value, literal: true };
        }
        if (spec.field !== undefined)
            return { value: row[spec.field], literal: false };
        return { value: spec.value, literal: true };
    }
    function rawFrom(spec, row) {
        return resolvedFrom(spec, row).value;
    }
    function fieldValues(context, spec) {
        const values = [];
        for (let rowIndex = 0; rowIndex < context.table.length; rowIndex += 1) {
            const resolved = resolvedFrom(spec, context.table.row(rowIndex));
            if (!resolved.literal && resolved.value !== null && resolved.value !== undefined) {
                values.push(resolved.value);
            }
        }
        return values;
    }
    function uniqueValues$1(values) {
        const seen = new Set();
        const result = [];
        for (const value of values) {
            if ((typeof value === 'object' && !(value instanceof Date)) || typeof value === 'boolean')
                continue;
            const normalized = value instanceof Date ? value.getTime() : value;
            if (normalized === null || normalized === undefined)
                continue;
            const id = key(normalized);
            if (seen.has(id))
                continue;
            seen.add(id);
            result.push(normalized);
        }
        return result;
    }
    function numericExtent(values, includeZero = false) {
        let minimum = includeZero ? 0 : Number.POSITIVE_INFINITY;
        let maximum = includeZero ? 0 : Number.NEGATIVE_INFINITY;
        for (const value of values) {
            const number = numeric$1(value);
            if (number === null)
                continue;
            minimum = Math.min(minimum, number);
            maximum = Math.max(maximum, number);
        }
        if (!Number.isFinite(minimum) || !Number.isFinite(maximum))
            return [0, 1];
        if (minimum === maximum) {
            const delta = minimum === 0 ? 1 : Math.abs(minimum) * 0.05;
            minimum -= delta;
            maximum += delta;
        }
        return [minimum, maximum];
    }
    function numericSamples(values) {
        return values.map(numeric$1).filter((value) => value !== null);
    }
    function scaleType(spec) {
        return spec.scale.type;
    }
    function isCategorical(spec, values) {
        if (spec.type === 'nominal' || spec.type === 'ordinal')
            return true;
        if (spec.type === 'quantitative' || spec.type === 'temporal')
            return false;
        return values.some((value) => typeof value === 'string' &&
            !Number.isFinite(Number(value)) &&
            !Number.isFinite(Date.parse(value)));
    }
    function defaultNumericRange(channel) {
        switch (channel) {
            case 'size':
                return [16, 400];
            case 'radius':
                return [2, 18];
            case 'opacity':
                return [0.15, 1];
            case 'strokeWidth':
                return [0.5, 8];
            default:
                return [0, 1];
        }
    }
    class EncodingResolver {
        context;
        #channels = new Map();
        constructor(context) {
            this.context = context;
        }
        #prepare(channel) {
            if (this.#channels.has(channel))
                return this.#channels.get(channel) ?? undefined;
            const spec = channelSpec(this.context, channel);
            if (spec === undefined) {
                this.#channels.set(channel, null);
                return undefined;
            }
            const values = fieldValues(this.context, spec);
            const categories = uniqueValues$1(values);
            const categoryIndices = new Map(categories.map((value, index) => [key(value), index]));
            const type = scaleType(spec);
            const usesField = spec.field !== undefined || spec.condition.some((condition) => condition.field !== undefined);
            let positionScale;
            let colorScale;
            if (channel === 'x2')
                positionScale = this.context.xScale;
            else if (channel === 'y2')
                positionScale = this.context.yScale;
            else if (usesField && (channel === 'color' || channel === 'fill' || channel === 'stroke')) {
                const categorical = type === undefined ? isCategorical(spec, values) : type === 'ordinal';
                if (categorical || type === 'ordinal') {
                    colorScale = createColorScale({ ...spec.scale, type: 'ordinal' }, {
                        domain: categories,
                        range: categories.map((_value, index) => categoricalColor(this.context.theme, index, categories.length)),
                    });
                }
                else if (type === 'sequential' || type === 'diverging' || type === 'cyclic') {
                    const domain = type === 'diverging'
                        ? (() => {
                            const [minimum, maximum] = numericExtent(values, spec.scale.zero === true);
                            return [minimum, (minimum + maximum) / 2, maximum];
                        })()
                        : numericExtent(values, spec.scale.zero === true);
                    colorScale = createColorScale(spec.scale, {
                        domain,
                        range: type === 'diverging'
                            ? this.context.theme.colors.diverging
                            : this.context.theme.colors.sequential,
                    });
                }
                else {
                    const numericDomain = type === 'quantile'
                        ? numericSamples(values)
                        : type === 'threshold' && spec.scale.domain !== undefined
                            ? spec.scale.domain
                            : numericExtent(values, spec.scale.zero === true);
                    const thresholdCount = type === 'threshold' && spec.scale.domain !== undefined
                        ? spec.scale.domain.length + 1
                        : 2;
                    const thresholdRange = Array.from({ length: thresholdCount }, (_value, index) => index / Math.max(1, thresholdCount - 1));
                    const numericColorScale = spec.scale.outOfBounds === undefined && spec.scale.clamp === undefined
                        ? { ...spec.scale, nice: false, outOfBounds: 'clamp' }
                        : { ...spec.scale, nice: false };
                    positionScale = createPositionScale(numericColorScale, {
                        domain: numericDomain,
                        range: thresholdRange,
                        type: (type ?? 'linear'),
                    });
                }
            }
            else if (usesField &&
                (channel === 'size' ||
                    channel === 'radius' ||
                    channel === 'opacity' ||
                    channel === 'strokeWidth')) {
                const categorical = isCategorical(spec, values) || type === 'ordinal';
                const output = defaultNumericRange(channel);
                const categoryCount = Math.max(1, categories.length);
                const range = categorical
                    ? Array.from({ length: categoryCount }, (_value, index) => output[0] + ((output[1] - output[0]) * index) / Math.max(1, categoryCount - 1))
                    : output;
                positionScale = createPositionScale({ ...spec.scale, nice: false }, {
                    domain: type === 'quantile'
                        ? numericSamples(values)
                        : categorical
                            ? categories
                            : numericExtent(values, spec.scale.zero === true),
                    range,
                    type: (categorical ? 'ordinal' : (type ?? 'linear')),
                });
            }
            const prepared = {
                spec,
                values,
                categories,
                categoryIndices,
                ...(positionScale === undefined ? {} : { positionScale }),
                ...(colorScale === undefined ? {} : { colorScale }),
            };
            this.#channels.set(channel, prepared);
            return prepared;
        }
        has(channel) {
            return channelSpec(this.context, channel) !== undefined;
        }
        raw(channel, rowIndex) {
            const prepared = this.#prepare(channel);
            return prepared === undefined
                ? undefined
                : rawFrom(prepared.spec, this.context.table.row(rowIndex));
        }
        number(channel, rowIndex, fallback) {
            const prepared = this.#prepare(channel);
            if (prepared === undefined)
                return fallback;
            const resolved = resolvedFrom(prepared.spec, this.context.table.row(rowIndex));
            const value = resolved.value;
            if (resolved.literal)
                return numeric$1(value) ?? fallback;
            if (prepared.positionScale !== undefined) {
                const input = value instanceof Date || typeof value === 'number' || typeof value === 'string'
                    ? value
                    : null;
                if (input === null)
                    return fallback;
                const mapped = prepared.positionScale.map(input);
                return Number.isFinite(mapped) ? mapped : fallback;
            }
            return numeric$1(value) ?? fallback;
        }
        position(channel, rowIndex) {
            const prepared = this.#prepare(channel);
            if (prepared?.positionScale === undefined)
                return null;
            const value = rawFrom(prepared.spec, this.context.table.row(rowIndex));
            const input = value instanceof Date || typeof value === 'number' || typeof value === 'string'
                ? value
                : null;
            if (input === null)
                return null;
            const mapped = prepared.positionScale.map(input);
            return Number.isFinite(mapped) ? mapped : null;
        }
        color(channel, rowIndex, fallback) {
            const prepared = this.#prepare(channel);
            if (prepared === undefined)
                return fallback;
            const resolved = resolvedFrom(prepared.spec, this.context.table.row(rowIndex));
            const value = resolved.value;
            if (resolved.literal)
                return typeof value === 'string' ? value : fallback;
            if (prepared.colorScale !== undefined &&
                (value instanceof Date || typeof value === 'number' || typeof value === 'string')) {
                return prepared.colorScale.map(value);
            }
            if (prepared.positionScale !== undefined &&
                (value instanceof Date || typeof value === 'number' || typeof value === 'string')) {
                const ratio = prepared.positionScale.map(value);
                return Number.isFinite(ratio)
                    ? continuousColor(this.context.theme, ratio)
                    : prepared.spec.scale.outOfBounds === 'unknown'
                        ? 'transparent'
                        : fallback;
            }
            return typeof value === 'string' ? value : fallback;
        }
        dash(rowIndex, fallback = []) {
            const prepared = this.#prepare('strokeDash');
            if (prepared === undefined)
                return fallback;
            const value = rawFrom(prepared.spec, this.context.table.row(rowIndex));
            if (Array.isArray(value))
                return value;
            const index = prepared.categoryIndices.get(key(value));
            return index === undefined ? fallback : (dashRange[index % dashRange.length] ?? fallback);
        }
        shape(rowIndex, fallback = 'circle') {
            const channel = this.has('shape') ? 'shape' : 'symbol';
            const prepared = this.#prepare(channel);
            if (prepared === undefined)
                return fallback;
            const resolved = resolvedFrom(prepared.spec, this.context.table.row(rowIndex));
            const value = resolved.value;
            if (resolved.literal && typeof value === 'string')
                return value;
            const index = prepared.categoryIndices.get(key(value));
            return index === undefined ? fallback : (shapeRange[index % shapeRange.length] ?? fallback);
        }
        icon(rowIndex) {
            const value = this.raw('icon', rowIndex);
            if (typeof value !== 'string')
                return undefined;
            const normalized = value.trim();
            if (normalized === '' || /[\u0000-\u001f\u007f]/u.test(normalized))
                return undefined;
            return Array.from(normalized).slice(0, 8).join('');
        }
        text(rowIndex, fallback) {
            const value = this.raw('text', rowIndex);
            if (value === null || value === undefined || Array.isArray(value))
                return fallback;
            return String(value);
        }
        tooltip(rowIndex) {
            const value = this.raw('tooltip', rowIndex);
            if (value === undefined || Array.isArray(value) || value instanceof Date)
                return undefined;
            return { encoded: value };
        }
        groupKey(rowIndex) {
            return ['detail', 'color', 'fill', 'stroke', 'strokeDash', 'strokeWidth', 'opacity']
                .filter((channel) => this.has(channel))
                .map((channel) => key(this.raw(channel, rowIndex)))
                .join('|');
        }
        orderedIndices(indices) {
            if (!this.has('order'))
                return indices;
            return [...indices].sort((left, right) => {
                const a = this.raw('order', left);
                const b = this.raw('order', right);
                const an = numeric$1(a);
                const bn = numeric$1(b);
                if (an !== null && bn !== null)
                    return an - bn || left - right;
                return String(a ?? '').localeCompare(String(b ?? '')) || left - right;
            });
        }
    }
    function createEncodingResolver(context) {
        return new EncodingResolver(context);
    }

    function scaleInput(value) {
        if (value === null ||
            value === undefined ||
            typeof value === 'boolean' ||
            (typeof value === 'object' && !(value instanceof Date)))
            return null;
        return value;
    }
    function numericDataValue(value, temporal = false) {
        if (value instanceof Date) {
            const timestamp = value.getTime();
            return Number.isFinite(timestamp) ? timestamp : null;
        }
        if (typeof value === 'number')
            return Number.isFinite(value) ? value : null;
        if (temporal && typeof value === 'string') {
            const timestamp = Date.parse(value);
            return Number.isFinite(timestamp) ? timestamp : null;
        }
        return null;
    }
    function themedPointFill(theme, seriesColor, fallback) {
        return theme.mark.pointColorMode === 'series' ? seriesColor : (theme.mark.pointFill ?? fallback);
    }
    function themedPointStroke(theme, seriesColor, fallback) {
        return theme.mark.pointColorMode === 'series'
            ? seriesColor
            : (theme.mark.pointStroke ?? fallback);
    }
    function themedAreaFill(theme, seriesColor, fallback) {
        return theme.mark.areaColorMode === 'series' ? seriesColor : (theme.mark.areaFill ?? fallback);
    }
    function themedAreaStroke(theme, seriesColor, fallback) {
        return theme.mark.areaColorMode === 'series' ? seriesColor : (theme.mark.areaStroke ?? fallback);
    }
    /**
     * Use a theme's declared continuous interpolation while retaining the exact
     * legacy sequential-stop behaviour of Graflume's light and dark themes.
     */
    function mappedContinuousColor(theme, ratio, legacyMode = 'stepped') {
        const bounded = Math.max(0, Math.min(1, ratio));
        if (theme.colors.paletteMode === 'ggplot2-hue' ||
            theme.colors.continuousInterpolation !== undefined) {
            return continuousColor(theme, bounded);
        }
        const palette = theme.colors.sequential;
        if (palette.length === 0)
            return theme.colors.focus;
        if (legacyMode === 'endpoints') {
            return mixColor$1(palette[0] ?? theme.colors.focus, palette[palette.length - 1] ?? theme.colors.focus, bounded);
        }
        return palette[Math.round(bounded * (palette.length - 1))] ?? theme.colors.focus;
    }

    function boundedSampleIndices(values, budget) {
        const target = Math.max(1, Math.floor(budget));
        return target < 4
            ? exactStrideSampleIndices(values.length, target)
            : minMaxSampleIndices(values, target);
    }
    function allocateSegmentBudgets(lengths, maximumPoints) {
        let remainingPoints = Math.max(0, Math.floor(maximumPoints));
        let remainingWeight = lengths.reduce((total, length) => total + length, 0);
        return lengths.map((length, index) => {
            const remainingSegments = lengths.length - index - 1;
            const maximum = Math.max(1, remainingPoints - remainingSegments);
            const proportional = Math.max(1, Math.floor((remainingPoints * length) / Math.max(1, remainingWeight)));
            const budget = Math.min(length, maximum, proportional);
            remainingPoints -= budget;
            remainingWeight -= length;
            return budget;
        });
    }
    function resolveMissingValuePolicy(value, fallback) {
        return value === 'gap' || value === 'zero' || value === 'connect' ? value : fallback;
    }
    function curveOptionsForMark(options) {
        const tension = options.tension;
        const samples = options.curveSamples;
        return {
            ...(typeof tension === 'number' && Number.isFinite(tension) ? { tension } : {}),
            ...(typeof samples === 'number' && Number.isFinite(samples) ? { samples } : {}),
        };
    }
    function curveNameForMark(options, fallback) {
        return resolveCurveName(options.curve, fallback);
    }
    function collectCurveSegments(context, defaultMissing) {
        const { table, layer, xScale, yScale, performance } = context;
        const encoding = createEncodingResolver(context);
        const missing = resolveMissingValuePolicy(layer.mark.options.missing, defaultMissing);
        const rawSegments = [];
        const allIndices = encoding.orderedIndices(Array.from({ length: table.length }, (_value, index) => index));
        const grouped = new Map();
        for (const rowIndex of allIndices) {
            const group = encoding.groupKey(rowIndex);
            const indices = grouped.get(group) ?? [];
            indices.push(rowIndex);
            grouped.set(group, indices);
        }
        for (const indices of grouped.values()) {
            let current = [];
            const flush = () => {
                if (current.length > 0)
                    rawSegments.push(current);
                current = [];
            };
            for (const rowIndex of indices) {
                const xInput = scaleInput(table.value(rowIndex, layer.x.field));
                const yInput = scaleInput(table.value(rowIndex, layer.y.field));
                if (xInput === null) {
                    if (missing !== 'connect')
                        flush();
                    continue;
                }
                const x = xScale.map(xInput);
                let sourceValue = numericDataValue(table.value(rowIndex, layer.y.field), layer.y.type === 'temporal');
                let yValue;
                if (yInput === null) {
                    if (missing === 'zero') {
                        yValue = 0;
                        sourceValue = 0;
                    }
                    else {
                        if (missing === 'gap')
                            flush();
                        continue;
                    }
                }
                else
                    yValue = yInput;
                const y = yScale.map(yValue);
                if (!Number.isFinite(x) || !Number.isFinite(y)) {
                    if (missing !== 'connect')
                        flush();
                    continue;
                }
                current.push({ point: { x, y }, rowIndex, value: sourceValue ?? y });
            }
            flush();
        }
        const maximumPoints = Math.max(1, Math.floor(performance.maxLinePoints));
        const retainedSegments = exactStrideSampleIndices(rawSegments.length, maximumPoints).flatMap((index) => (rawSegments[index] === undefined ? [] : [rawSegments[index]]));
        const budgets = allocateSegmentBudgets(retainedSegments.map(({ length }) => length), maximumPoints);
        return retainedSegments.map((segment, segmentIndex) => {
            const indices = boundedSampleIndices(segment.map(({ value }) => value), budgets[segmentIndex] ?? 1);
            return indices
                .map((index) => segment[index])
                .filter((entry) => entry !== undefined);
        });
    }
    function interpolateSegments(segments, curve, options, maxPoints = Number.POSITIVE_INFINITY) {
        const interpolated = segments.map((source) => ({
            source,
            points: interpolateCurve(source.map(({ point }) => point), curve, options),
        }));
        if (!Number.isFinite(maxPoints))
            return interpolated;
        const maximumPoints = Math.max(1, Math.floor(maxPoints));
        const retained = exactStrideSampleIndices(interpolated.length, maximumPoints).flatMap((index) => interpolated[index] === undefined ? [] : [interpolated[index]]);
        const budgets = allocateSegmentBudgets(retained.map(({ points }) => points.length), maximumPoints);
        return retained.map(({ source, points }, segmentIndex) => {
            const budget = budgets[segmentIndex] ?? 1;
            if (points.length <= budget)
                return { source, points };
            const indices = boundedSampleIndices(points.map(({ y }) => y), budget);
            return {
                source,
                points: indices
                    .map((index) => points[index])
                    .filter((point) => point !== undefined),
            };
        });
    }

    function identityKey(value) {
        if (value instanceof Date)
            return `date:${value.toISOString()}`;
        return `${typeof value}:${String(value)}`;
    }
    function finiteRowValue(context, rowIndex, field) {
        return numericDataValue(context.table.value(rowIndex, field));
    }
    function cleanRow(row) {
        return Object.fromEntries(Object.entries(row).filter(([field]) => !field.startsWith('__graflume_')));
    }
    function tooltipRow(context, entry, encoding) {
        const encoded = encoding.tooltip(entry.rowIndex);
        return {
            ...cleanRow(context.table.row(entry.rowIndex)),
            stackSeries: entry.series,
            stackCategory: entry.category,
            stackValue: entry.source,
            stackStart: entry.start,
            stackEnd: entry.end,
            stackTotal: entry.absoluteTotal,
            stackPositiveTotal: entry.positiveTotal,
            stackNegativeTotal: entry.negativeTotal,
            stackNetTotal: entry.netTotal,
            stackPercent: entry.absoluteTotal === 0 ? 0 : entry.source / entry.absoluteTotal,
            ...(encoded ?? {}),
        };
    }
    function datum(context, entry, encoding) {
        const row = context.table.row(entry.rowIndex);
        return {
            layerId: context.layer.id,
            rowIndex: entry.rowIndex,
            datum: cleanRow(row),
            tooltip: tooltipRow(context, entry, encoding),
        };
    }
    function seriesEntries(context, fields) {
        const stack = resolveSeriesStackSpec(context.layer);
        if (stack === null)
            return [];
        const groupedTotals = new Map();
        if (stack.offset === null) {
            for (let rowIndex = 0; rowIndex < context.table.length; rowIndex += 1) {
                const category = scaleInput(context.table.value(rowIndex, fields.category));
                const source = finiteRowValue(context, rowIndex, stack.valueField);
                if (category === null || source === null)
                    continue;
                const key = identityKey(category);
                const total = groupedTotals.get(key) ?? { absolute: 0, positive: 0, negative: 0, net: 0 };
                total.absolute += Math.abs(source);
                total.positive += Math.max(0, source);
                total.negative += Math.max(0, -source);
                total.net += source;
                groupedTotals.set(key, total);
            }
        }
        const names = [];
        const indexes = new Map();
        const combinations = new Set();
        const entries = [];
        for (let rowIndex = 0; rowIndex < context.table.length; rowIndex += 1) {
            const category = scaleInput(context.table.value(rowIndex, fields.category));
            const rawSeries = context.table.value(rowIndex, fields.series);
            const series = rawSeries === null || rawSeries === undefined
                ? ''
                : rawSeries instanceof Date
                    ? rawSeries.toISOString()
                    : String(rawSeries);
            const seriesKey = rawSeries === null || rawSeries === undefined
                ? ''
                : rawSeries instanceof Date
                    ? `date:${rawSeries.toISOString()}`
                    : `${typeof rawSeries}:${String(rawSeries)}`;
            const source = finiteRowValue(context, rowIndex, stack.offset === null ? stack.valueField : fields.source);
            if (category === null || series === '' || source === null)
                continue;
            const categoryKey = identityKey(category);
            const combination = `${categoryKey}\u0000${seriesKey}`;
            if (combinations.has(combination)) {
                throw new GraflumeError('INVALID_DATA', 'Series layouts require one row per category-series pair; aggregate duplicate pairs explicitly.', { path: '$.data' });
            }
            combinations.add(combination);
            let seriesIndex = indexes.get(seriesKey);
            if (seriesIndex === undefined) {
                seriesIndex = names.length;
                names.push(seriesKey);
                indexes.set(seriesKey, seriesIndex);
            }
            const grouped = groupedTotals.get(categoryKey);
            const start = stack.offset === null ? 0 : (finiteRowValue(context, rowIndex, fields.start) ?? 0);
            const end = stack.offset === null ? source : (finiteRowValue(context, rowIndex, fields.end) ?? start);
            entries.push({
                rowIndex,
                series,
                seriesKey,
                seriesIndex,
                category,
                categoryKey,
                source,
                start,
                end,
                absoluteTotal: grouped?.absolute ?? finiteRowValue(context, rowIndex, fields.absoluteTotal) ?? 0,
                positiveTotal: grouped?.positive ?? finiteRowValue(context, rowIndex, fields.positiveTotal) ?? 0,
                negativeTotal: grouped?.negative ?? finiteRowValue(context, rowIndex, fields.negativeTotal) ?? 0,
                netTotal: grouped?.net ?? finiteRowValue(context, rowIndex, fields.netTotal) ?? 0,
            });
        }
        return entries;
    }
    function seriesColor(context, index, count) {
        return context.layer.mark.fill ?? categoricalColor(context.theme, index, count);
    }
    function compileSeriesBarMark(context) {
        const fields = preparedSeriesStackFields(context.layer);
        if (fields === null)
            return null;
        const entries = seriesEntries(context, fields);
        if (entries.length === 0)
            return [];
        const { layer, xScale, yScale, plot, theme, performance, barGroup } = context;
        const encoding = createEncodingResolver(context);
        const seriesCount = Math.max(1, new Set(entries.map(({ seriesKey }) => seriesKey)).size);
        const grouped = resolveSeriesStackSpec(layer)?.offset === null;
        const externalCount = layer.mark.position === 'group' ? Math.max(1, barGroup.count) : 1;
        const externalIndex = layer.mark.position === 'group' ? barGroup.index : 0;
        const subgroupCount = grouped ? seriesCount * externalCount : externalCount;
        const categoryCount = Math.max(1, new Set(entries.map(({ categoryKey }) => categoryKey)).size);
        const themedWidthRatio = theme.mark.barWidthRatio;
        const indices = new Set(strideSampleIndices(entries.length, performance.maxBarMarks));
        const nodes = [];
        const entryByRow = new Map(entries.map((entry) => [entry.rowIndex, entry]));
        const visibleEntries = encoding
            .orderedIndices(entries.flatMap((entry, index) => (indices.has(index) ? [entry.rowIndex] : [])))
            .flatMap((rowIndex) => {
            const entry = entryByRow.get(rowIndex);
            return entry === undefined ? [] : [entry];
        });
        visibleEntries.forEach((entry, drawIndex) => {
            const subgroupIndex = grouped ? externalIndex * seriesCount + entry.seriesIndex : externalIndex;
            const baseColor = seriesColor(context, entry.seriesIndex, seriesCount);
            const encodedColor = encoding.color('color', entry.rowIndex, baseColor);
            const fill = encoding.color('fill', entry.rowIndex, encoding.has('color') ? encodedColor : baseColor);
            const fallbackStroke = layer.mark.stroke ?? theme.mark.barStroke;
            const stroke = encoding.color('stroke', entry.rowIndex, fallbackStroke ?? encodedColor);
            const hasStroke = encoding.has('stroke') || fallbackStroke !== undefined;
            const lineWidth = encoding.number('strokeWidth', entry.rowIndex, layer.mark.lineWidth ?? (hasStroke ? (theme.mark.barStrokeWidth ?? theme.mark.lineWidth) : 0));
            const dash = encoding.dash(entry.rowIndex);
            if (layer.mark.orientation === 'horizontal') {
                const center = yScale.map(entry.category);
                const start = xScale.map(entry.start);
                const end = xScale.map(entry.end);
                if (![center, start, end].every(Number.isFinite))
                    return;
                const slot = yScale instanceof BandScale
                    ? (themedWidthRatio === undefined ? yScale.bandwidth : yScale.step) / subgroupCount
                    : ((plot.height / categoryCount) * (themedWidthRatio === undefined ? 0.8 : 1)) /
                        subgroupCount;
                const size = Math.max(1, slot * (themedWidthRatio ?? 0.74));
                const offset = (subgroupIndex - (subgroupCount - 1) / 2) * slot;
                nodes.push({
                    type: 'rect',
                    ...nodeBase(`${layer.id}:series-bar:${entry.rowIndex}`, {
                        zIndex: layer.zIndex +
                            (encoding.has('order')
                                ? drawIndex / Math.max(100, visibleEntries.length * 10)
                                : entry.seriesIndex / 100),
                        opacity: encoding.number('opacity', entry.rowIndex, layer.mark.opacity),
                        interactive: performance.enableHitTesting,
                        datum: datum(context, entry, encoding),
                    }),
                    x: Math.min(start, end),
                    y: center + offset - size / 2,
                    width: Math.max(0.5, Math.abs(end - start)),
                    height: size,
                    fill,
                    ...(hasStroke ? { stroke } : {}),
                    lineWidth,
                    ...(dash.length === 0 ? {} : { dash }),
                    cornerRadius: layer.mark.cornerRadius ?? theme.mark.barRadius,
                });
                return;
            }
            const center = xScale.map(entry.category);
            const start = yScale.map(entry.start);
            const end = yScale.map(entry.end);
            if (![center, start, end].every(Number.isFinite))
                return;
            const slot = xScale instanceof BandScale
                ? (themedWidthRatio === undefined ? xScale.bandwidth : xScale.step) / subgroupCount
                : ((plot.width / categoryCount) * (themedWidthRatio === undefined ? 0.8 : 1)) /
                    subgroupCount;
            const size = Math.max(1, slot * (themedWidthRatio ?? 0.74));
            const offset = (subgroupIndex - (subgroupCount - 1) / 2) * slot;
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:series-bar:${entry.rowIndex}`, {
                    zIndex: layer.zIndex +
                        (encoding.has('order')
                            ? drawIndex / Math.max(100, visibleEntries.length * 10)
                            : entry.seriesIndex / 100),
                    opacity: encoding.number('opacity', entry.rowIndex, layer.mark.opacity),
                    interactive: performance.enableHitTesting,
                    datum: datum(context, entry, encoding),
                }),
                x: center + offset - size / 2,
                y: Math.min(start, end),
                width: size,
                height: Math.max(0.5, Math.abs(end - start)),
                fill,
                ...(hasStroke ? { stroke } : {}),
                lineWidth,
                ...(dash.length === 0 ? {} : { dash }),
                cornerRadius: layer.mark.cornerRadius ?? theme.mark.barRadius,
            });
        });
        return nodes;
    }
    function sampledEntries(entries, budget) {
        if (entries.length <= budget)
            return entries;
        const target = Math.max(1, Math.floor(budget));
        const indices = target < 4
            ? exactStrideSampleIndices(entries.length, target)
            : minMaxSampleIndices(entries.map(({ end }) => end), target);
        return indices.flatMap((index) => (entries[index] === undefined ? [] : [entries[index]]));
    }
    function compileSeriesAreaMark(context) {
        const fields = preparedSeriesStackFields(context.layer);
        if (fields === null)
            return null;
        const entries = seriesEntries(context, fields);
        if (entries.length === 0)
            return [];
        const { layer, xScale, yScale, theme, performance } = context;
        const encoding = createEncodingResolver(context);
        const series = [...new Set(entries.map(({ seriesKey }) => seriesKey))];
        const groups = new Map();
        for (const entry of entries) {
            const key = `${entry.seriesKey}\u0000${encoding.groupKey(entry.rowIndex)}`;
            const group = groups.get(key) ?? {
                series: entry.series,
                seriesIndex: entry.seriesIndex,
                entries: [],
            };
            group.entries.push(entry);
            groups.set(key, group);
        }
        const allRenderGroups = [...groups.values()];
        const maximumGroups = Math.max(1, Math.floor(performance.maxLinePoints / 2));
        const renderGroups = exactStrideSampleIndices(allRenderGroups.length, maximumGroups).flatMap((index) => (allRenderGroups[index] === undefined ? [] : [allRenderGroups[index]]));
        const curve = curveNameForMark(layer.mark.options, 'straight');
        const curveOptions = curveOptionsForMark(layer.mark.options);
        const nodes = [];
        renderGroups.forEach((group, groupIndex) => {
            const entryByRow = new Map(group.entries.map((entry) => [entry.rowIndex, entry]));
            const ordered = encoding.has('order')
                ? encoding
                    .orderedIndices(group.entries.map(({ rowIndex }) => rowIndex))
                    .flatMap((rowIndex) => {
                    const entry = entryByRow.get(rowIndex);
                    return entry === undefined ? [] : [entry];
                })
                : [...group.entries].sort((left, right) => xScale.map(left.category) - xScale.map(right.category));
            const pathBudget = Math.max(2, Math.floor(performance.maxLinePoints / Math.max(1, renderGroups.length)));
            const boundaryBudget = Math.max(1, Math.floor(pathBudget / 2));
            const source = sampledEntries(ordered, boundaryBudget);
            if (source.length === 0)
                return;
            const upperSource = source.map((entry) => ({
                x: xScale.map(entry.category),
                y: yScale.map(entry.end),
            }));
            const lowerSource = source.map((entry) => ({
                x: xScale.map(entry.category),
                y: yScale.map(entry.start),
            }));
            if ([...upperSource, ...lowerSource].some(({ x, y }) => !Number.isFinite(x) || !Number.isFinite(y))) {
                return;
            }
            const interpolatedUpper = interpolateCurve(upperSource, curve, curveOptions);
            const interpolatedLower = interpolateCurve(lowerSource, curve, curveOptions);
            const commonLength = Math.min(interpolatedUpper.length, interpolatedLower.length);
            const boundaryIndices = commonLength <= boundaryBudget
                ? exactStrideSampleIndices(commonLength, commonLength)
                : boundaryBudget < 4
                    ? exactStrideSampleIndices(commonLength, boundaryBudget)
                    : minMaxSampleIndices(Array.from({ length: commonLength }, (_value, index) => Math.abs(interpolatedUpper[index].y - interpolatedLower[index].y)), boundaryBudget);
            const upper = boundaryIndices.map((index) => interpolatedUpper[index]);
            const lower = boundaryIndices.map((index) => interpolatedLower[index]);
            const representative = source[0].rowIndex;
            const baseColor = seriesColor(context, group.seriesIndex, series.length);
            const color = encoding.color('color', representative, baseColor);
            const themeRiver = layer.mark.type === 'theme-river';
            const fillColor = themeRiver
                ? encoding.color('fill', representative, encoding.has('color')
                    ? color
                    : colorWithOpacity(color, theme.mode === 'dark' ? 0.72 : 0.62))
                : encoding.color('fill', representative, encoding.has('color')
                    ? color
                    : (layer.mark.fill ??
                        themedAreaFill(theme, color, colorWithOpacity(color, theme.mode === 'dark' ? 0.28 : 0.2))));
            const strokeColor = encoding.color('stroke', representative, encoding.has('color')
                ? color
                : (layer.mark.stroke ??
                    (themeRiver
                        ? color
                        : themedAreaStroke(theme, color, theme.mark.lineColor ?? theme.mark.defaultColor ?? color))));
            const lineWidth = encoding.number('strokeWidth', representative, layer.mark.lineWidth ?? (themeRiver ? 1.2 : theme.mark.lineWidth));
            const dash = encoding.dash(representative);
            const stem = themeRiver ? 'river' : 'series-area';
            const fill = {
                type: 'path',
                ...nodeBase(`${layer.id}:${stem}:${groupIndex}`, {
                    zIndex: layer.zIndex + groupIndex / Math.max(100, renderGroups.length * 10),
                    opacity: encoding.number('opacity', representative, layer.mark.opacity),
                }),
                points: [...upper, ...[...lower].reverse()],
                closed: true,
                fill: fillColor,
                stroke: strokeColor,
                lineWidth,
                lineJoin: theme.mark.lineJoin ?? 'round',
                ...(dash.length === 0 ? {} : { dash }),
            };
            nodes.push(fill);
            source.forEach((entry, index) => {
                if (!performance.enableHitTesting)
                    return;
                const upperPoint = upperSource[index];
                const lowerPoint = lowerSource[index];
                if (upperPoint === undefined || lowerPoint === undefined)
                    return;
                const pointColor = encoding.color('color', entry.rowIndex, baseColor);
                const encodedSize = encoding.number('size', entry.rowIndex, Number.NaN);
                const radius = layer.mark.point
                    ? encoding.number('radius', entry.rowIndex, Number.isFinite(encodedSize)
                        ? Math.sqrt(Math.max(0, encodedSize) / Math.PI)
                        : (layer.mark.radius ?? theme.mark.pointRadius))
                    : Math.max(6, Math.min(16, Math.abs(upperPoint.y - lowerPoint.y) / 2));
                const pointFill = encoding.color('fill', entry.rowIndex, encoding.has('color')
                    ? pointColor
                    : (layer.mark.fill ?? themedPointFill(theme, pointColor, theme.colors.background)));
                const pointStroke = encoding.color('stroke', entry.rowIndex, encoding.has('color')
                    ? pointColor
                    : (layer.mark.stroke ??
                        themedPointStroke(theme, pointColor, themedAreaStroke(theme, pointColor, theme.mark.lineColor ?? pointColor))));
                const pointDash = encoding.dash(entry.rowIndex);
                const target = {
                    type: 'circle',
                    ...nodeBase(`${layer.id}:${stem}-target:${entry.rowIndex}`, {
                        zIndex: layer.zIndex + groupIndex / Math.max(100, renderGroups.length * 10) + 0.01,
                        opacity: layer.mark.point
                            ? encoding.number('opacity', entry.rowIndex, layer.mark.opacity)
                            : 0.001,
                        interactive: true,
                        datum: datum(context, entry, encoding),
                    }),
                    cx: upperPoint.x,
                    cy: (upperPoint.y + lowerPoint.y) / 2,
                    radius,
                    fill: layer.mark.point ? pointFill : pointColor,
                    ...(layer.mark.point ? { stroke: pointStroke } : {}),
                    lineWidth: layer.mark.point
                        ? encoding.number('strokeWidth', entry.rowIndex, layer.mark.lineWidth === undefined
                            ? (theme.mark.pointStrokeWidth ?? Math.max(1.5, theme.mark.lineWidth * 0.68))
                            : Math.max(1.5, layer.mark.lineWidth * 0.68))
                        : 0,
                    ...(layer.mark.point && pointDash.length > 0 ? { dash: pointDash } : {}),
                };
                nodes.push(target);
            });
        });
        return nodes;
    }

    const TAU$3 = Math.PI * 2;
    function clamp$b(value, min, max) {
        return Math.max(min, Math.min(max, value));
    }
    function finiteOption(value, fallback) {
        return typeof value === 'number' && Number.isFinite(value) ? value : fallback;
    }
    function stringValue$4(value) {
        if (value === null || value === undefined)
            return null;
        if (value instanceof Date)
            return value.toISOString();
        return String(value);
    }
    function optionStrings$3(value) {
        if (!Array.isArray(value))
            return [];
        return value.filter((item) => typeof item === 'string' && item.trim() !== '');
    }
    function pointOnCircle$2(cx, cy, radius, angle) {
        return { x: cx + Math.cos(angle) * radius, y: cy + Math.sin(angle) * radius };
    }
    function sampledArc$2(cx, cy, radius, startAngle, endAngle, segments = 24) {
        const count = Math.max(2, segments);
        return Array.from({ length: count + 1 }, (_, index) => {
            const ratio = index / count;
            return pointOnCircle$2(cx, cy, radius, startAngle + (endAngle - startAngle) * ratio);
        });
    }
    function annularSector$2(cx, cy, innerRadius, outerRadius, startAngle, endAngle) {
        const span = Math.abs(endAngle - startAngle);
        const segments = Math.max(4, Math.ceil((span / TAU$3) * 48));
        return [
            ...sampledArc$2(cx, cy, outerRadius, startAngle, endAngle, segments),
            ...sampledArc$2(cx, cy, innerRadius, endAngle, startAngle, segments),
        ];
    }
    function quadraticPoints$1(start, control, end, segments = 20) {
        return Array.from({ length: segments + 1 }, (_, index) => {
            const t = index / segments;
            const inverse = 1 - t;
            return {
                x: inverse * inverse * start.x + 2 * inverse * t * control.x + t * t * end.x,
                y: inverse * inverse * start.y + 2 * inverse * t * control.y + t * t * end.y,
            };
        });
    }
    function themeColor(context, index, count) {
        return categoricalColor(context.theme, index, count);
    }
    function textNode$9(id, x, y, text, context, options = {}) {
        return {
            type: 'text',
            ...nodeBase(id, { zIndex: options.zIndex ?? context.layer.zIndex + 2 }),
            x,
            y,
            text,
            fill: options.fill ?? context.theme.colors.text,
            fontFamily: context.theme.typography.fontFamily,
            fontSize: options.size ?? context.theme.typography.fontSize,
            fontWeight: options.weight ?? 500,
            align: options.align ?? 'center',
            baseline: options.baseline ?? 'middle',
            rotation: options.rotation ?? 0,
        };
    }
    function datumBase$3(context, id, rowIndex, zIndex = 0, tooltip) {
        return nodeBase(id, {
            zIndex: context.layer.zIndex + zIndex,
            opacity: context.layer.mark.opacity,
            interactive: context.performance.enableHitTesting,
            datum: {
                layerId: context.layer.id,
                rowIndex,
                datum: context.table.row(rowIndex),
                ...(tooltip === undefined ? {} : { tooltip }),
            },
        });
    }
    const compileRadarMark = (context) => {
        const { layer, table, plot, theme, performance } = context;
        const seriesField = layer.mark.fields.series;
        const maxOption = finiteOption(layer.mark.options.max, Number.NaN);
        let maximum = Number.isFinite(maxOption) && maxOption > 0 ? maxOption : 0;
        const categoryNames = [];
        const categorySet = new Set();
        const seriesNames = [];
        const seriesRows = new Map();
        // Build the category/series matrix in one pass. The previous implementation
        // rescanned the full table once per series, which became quadratic when both
        // fields were high-cardinality.
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const category = stringValue$4(table.value(rowIndex, layer.x.field));
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            const seriesName = seriesField === undefined ? 'Series' : stringValue$4(table.value(rowIndex, seriesField));
            if (category === null || value === null || seriesName === null)
                continue;
            maximum = Math.max(maximum, value);
            if (!categorySet.has(category)) {
                categorySet.add(category);
                categoryNames.push(category);
            }
            let rows = seriesRows.get(seriesName);
            if (rows === undefined) {
                rows = new Map();
                seriesRows.set(seriesName, rows);
                seriesNames.push(seriesName);
            }
            rows.set(category, { value, rowIndex });
        }
        if (categoryNames.length < 3 || seriesNames.length === 0)
            return [];
        if (maximum <= 0)
            maximum = 1;
        const cx = plot.x + plot.width / 2;
        const cy = plot.y + plot.height / 2;
        const radius = Math.max(16, Math.min(plot.width, plot.height) * 0.34);
        const nodes = [];
        const rings = clamp$b(Math.floor(finiteOption(layer.mark.options.rings, 5)), 1, 8);
        const linePointBudget = Math.max(1, Math.floor(performance.maxLinePoints));
        const balancedSeriesLimit = Math.max(1, Math.floor(Math.sqrt(linePointBudget)));
        const plannedSeriesCount = Math.min(seriesNames.length, balancedSeriesLimit);
        const categoryLimit = Math.floor(linePointBudget / (rings + plannedSeriesCount));
        if (categoryLimit < 3)
            return [];
        const categories = categoryNames.slice(0, Math.min(categoryNames.length, categoryLimit));
        const seriesLimit = Math.max(0, Math.floor(linePointBudget / categories.length) - rings);
        const retainedSeries = seriesNames.slice(0, seriesLimit);
        if (retainedSeries.length === 0)
            return [];
        for (let ring = 1; ring <= rings; ring += 1) {
            nodes.push({
                type: 'path',
                ...nodeBase(`${layer.id}:radar-grid:${ring}`, { zIndex: layer.zIndex }),
                points: categories.map((_, index) => pointOnCircle$2(cx, cy, (radius * ring) / rings, -Math.PI / 2 + (index * TAU$3) / categories.length)),
                closed: true,
                stroke: colorWithOpacity(theme.colors.grid, ring === rings ? 0.9 : 0.7),
                lineWidth: ring === rings ? 1.2 : 1,
                lineJoin: 'round',
            });
        }
        categories.forEach((category, index) => {
            const angle = -Math.PI / 2 + (index * TAU$3) / categories.length;
            const edge = pointOnCircle$2(cx, cy, radius, angle);
            const label = pointOnCircle$2(cx, cy, radius + 18, angle);
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:radar-axis:${index}`, { zIndex: layer.zIndex }),
                x1: cx,
                y1: cy,
                x2: edge.x,
                y2: edge.y,
                stroke: theme.colors.grid,
                lineWidth: 1,
                lineCap: 'round',
            });
            nodes.push(textNode$9(`${layer.id}:radar-label:${index}`, label.x, label.y, category, context, {
                fill: theme.colors.mutedText,
                size: Math.max(9, theme.typography.fontSize - 1),
                weight: 600,
            }));
        });
        let renderedPointMarks = 0;
        retainedSeries.forEach((seriesName, seriesIndex) => {
            const rows = seriesRows.get(seriesName);
            if (rows === undefined)
                return;
            const color = themeColor(context, seriesIndex, retainedSeries.length);
            const points = categories.map((category, index) => {
                const ratio = clamp$b((rows.get(category)?.value ?? 0) / maximum, 0, 1);
                return pointOnCircle$2(cx, cy, radius * ratio, -Math.PI / 2 + (index * TAU$3) / categories.length);
            });
            nodes.push({
                type: 'path',
                ...nodeBase(`${layer.id}:radar-series:${seriesIndex}`, {
                    zIndex: layer.zIndex + 1,
                    opacity: layer.mark.opacity,
                }),
                points,
                closed: true,
                fill: colorWithOpacity(layer.mark.fill ?? color, 0.2),
                stroke: layer.mark.stroke ?? color,
                lineWidth: layer.mark.lineWidth ?? 2.25,
                lineJoin: 'round',
            });
            points.forEach((point, index) => {
                if (renderedPointMarks >= performance.maxPointMarks)
                    return;
                const row = rows.get(categories[index] ?? '');
                if (row === undefined)
                    return;
                nodes.push({
                    type: 'circle',
                    ...datumBase$3(context, `${layer.id}:radar-point:${seriesIndex}:${index}`, row.rowIndex, 2),
                    cx: point.x,
                    cy: point.y,
                    radius: layer.mark.radius ?? 3.6,
                    fill: theme.colors.background,
                    stroke: layer.mark.stroke ?? color,
                    lineWidth: 2,
                });
                renderedPointMarks += 1;
            });
        });
        return nodes;
    };
    function hierarchyItems(context) {
        const { layer, table } = context;
        const idField = layer.mark.fields.id ?? layer.x.field;
        const parentField = layer.mark.fields.parent ?? layer.y.field;
        const labelField = layer.mark.fields.label ?? idField;
        const valueField = layer.mark.fields.value;
        const items = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const id = stringValue$4(table.value(rowIndex, idField));
            if (id === null || id === '')
                continue;
            const parentRaw = stringValue$4(table.value(rowIndex, parentField));
            const label = stringValue$4(table.value(rowIndex, labelField)) ?? id;
            const value = valueField === undefined
                ? (numericDataValue(table.value(rowIndex, layer.y.field)) ?? 1)
                : (numericDataValue(table.value(rowIndex, valueField)) ?? 1);
            items.push({
                id,
                parent: parentRaw === null || parentRaw === '' || parentRaw === id ? null : parentRaw,
                label,
                rowIndex,
                value: Math.max(0, value),
            });
        }
        return items;
    }
    function hierarchyDepths(items) {
        const byId = new Map(items.map((item) => [item.id, item]));
        const depths = new Map();
        const visiting = new Set();
        const depthOf = (id) => {
            const known = depths.get(id);
            if (known !== undefined)
                return known;
            if (visiting.has(id))
                return 0;
            visiting.add(id);
            const item = byId.get(id);
            const depth = item?.parent === null || item?.parent === undefined ? 0 : depthOf(item.parent) + 1;
            visiting.delete(id);
            depths.set(id, depth);
            return depth;
        };
        items.forEach((item) => depthOf(item.id));
        return depths;
    }
    const compileTreeMark = (context) => {
        const { layer, plot, theme } = context;
        const items = hierarchyItems(context);
        if (items.length === 0)
            return [];
        const depths = hierarchyDepths(items);
        const maxDepth = Math.max(0, ...depths.values());
        const levels = Array.from({ length: maxDepth + 1 }, () => []);
        items.forEach((item) => levels[depths.get(item.id) ?? 0]?.push(item));
        const orientation = layer.mark.options.orientation === 'horizontal' ? 'horizontal' : 'vertical';
        const positions = new Map();
        levels.forEach((level, depth) => {
            level.forEach((item, index) => {
                const across = (index + 1) / (level.length + 1);
                const down = maxDepth === 0 ? 0.5 : depth / maxDepth;
                positions.set(item.id, orientation === 'horizontal'
                    ? {
                        x: plot.x + plot.width * (0.08 + down * 0.84),
                        y: plot.y + plot.height * across,
                    }
                    : {
                        x: plot.x + plot.width * across,
                        y: plot.y + plot.height * (0.08 + down * 0.84),
                    });
            });
        });
        const nodes = [];
        items.forEach((item) => {
            const child = positions.get(item.id);
            const parent = item.parent === null ? undefined : positions.get(item.parent);
            if (child === undefined || parent === undefined)
                return;
            const elbow = orientation === 'horizontal'
                ? { x: (parent.x + child.x) / 2, y: parent.y }
                : { x: parent.x, y: (parent.y + child.y) / 2 };
            const elbow2 = orientation === 'horizontal' ? { x: elbow.x, y: child.y } : { x: child.x, y: elbow.y };
            nodes.push({
                type: 'path',
                ...nodeBase(`${layer.id}:tree-edge:${item.id}`, { zIndex: layer.zIndex }),
                points: [parent, elbow, elbow2, child],
                closed: false,
                stroke: colorWithOpacity(theme.colors.axis, 0.85),
                lineWidth: 1.5,
                lineCap: 'round',
                lineJoin: 'round',
            });
        });
        const cardWidth = clamp$b(plot.width / Math.max(3, Math.max(...levels.map((level) => level.length))), 66, 112);
        const cardHeight = 30;
        items.forEach((item, index) => {
            const position = positions.get(item.id);
            if (position === undefined)
                return;
            const color = themeColor(context, depths.get(item.id) ?? index, maxDepth + 1);
            nodes.push({
                type: 'rect',
                ...datumBase$3(context, `${layer.id}:tree-node:${item.id}`, item.rowIndex, 1),
                x: position.x - cardWidth / 2,
                y: position.y - cardHeight / 2,
                width: cardWidth,
                height: cardHeight,
                fill: layer.mark.fill ?? theme.colors.surface,
                stroke: layer.mark.stroke ?? color,
                lineWidth: layer.mark.lineWidth ?? 1.5,
                cornerRadius: layer.mark.cornerRadius ?? 8,
            });
            nodes.push(textNode$9(`${layer.id}:tree-label:${item.id}`, position.x, position.y, item.label, context, {
                size: Math.max(9, theme.typography.fontSize - 1),
                weight: 650,
                zIndex: layer.zIndex + 2,
            }));
        });
        return nodes;
    };
    function graphData(context) {
        const { layer, table } = context;
        const idField = layer.mark.fields.id;
        const sourceField = layer.mark.fields.source ?? layer.x.field;
        const targetField = layer.mark.fields.target ?? layer.y.field;
        const labelField = layer.mark.fields.label ?? idField;
        const valueField = layer.mark.fields.value;
        const nodesById = new Map();
        const edges = [];
        const ensureNode = (id, rowIndex, label = id) => {
            const existing = nodesById.get(id);
            if (existing !== undefined)
                return existing;
            const node = { id, label, rowIndex, degree: 0 };
            nodesById.set(id, node);
            return node;
        };
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            if (idField !== undefined && table.has(idField)) {
                const id = stringValue$4(table.value(rowIndex, idField));
                if (id !== null && id !== '') {
                    ensureNode(id, rowIndex, labelField !== undefined && table.has(labelField)
                        ? (stringValue$4(table.value(rowIndex, labelField)) ?? id)
                        : id);
                }
            }
            if (!table.has(sourceField) || !table.has(targetField))
                continue;
            const source = stringValue$4(table.value(rowIndex, sourceField));
            const target = stringValue$4(table.value(rowIndex, targetField));
            if (source === null || target === null || source === '' || target === '')
                continue;
            const sourceNode = ensureNode(source, rowIndex);
            const targetNode = ensureNode(target, rowIndex);
            sourceNode.degree += 1;
            targetNode.degree += 1;
            const value = valueField !== undefined && table.has(valueField)
                ? (numericDataValue(table.value(rowIndex, valueField)) ?? 1)
                : (numericDataValue(table.value(rowIndex, layer.y.field)) ?? 1);
            edges.push({ source, target, value: Math.max(0, value), rowIndex });
        }
        return { nodes: [...nodesById.values()], edges };
    }
    const compileGraphMark = (context) => {
        const { layer, plot, theme } = context;
        const { nodes: graphNodes, edges } = graphData(context);
        if (graphNodes.length === 0)
            return [];
        const cx = plot.x + plot.width / 2;
        const cy = plot.y + plot.height / 2;
        const radius = Math.max(20, Math.min(plot.width, plot.height) * 0.34);
        const positions = new Map();
        graphNodes.forEach((node, index) => {
            positions.set(node.id, pointOnCircle$2(cx, cy, radius, -Math.PI / 2 + (index * TAU$3) / graphNodes.length));
        });
        const maxEdge = Math.max(1, ...edges.map((edge) => edge.value));
        const totals = new Map(graphNodes.map((node) => [node.id, 0]));
        edges.forEach((edge) => {
            totals.set(edge.source, (totals.get(edge.source) ?? 0) + edge.value);
            totals.set(edge.target, (totals.get(edge.target) ?? 0) + edge.value);
        });
        const nodes = [];
        edges.forEach((edge, index) => {
            const source = positions.get(edge.source);
            const target = positions.get(edge.target);
            if (source === undefined || target === undefined)
                return;
            nodes.push({
                type: 'line',
                ...datumBase$3(context, `${layer.id}:graph-edge:${index}`, edge.rowIndex, 0, {
                    kind: 'edge',
                    source: edge.source,
                    target: edge.target,
                    value: edge.value,
                }),
                x1: source.x,
                y1: source.y,
                x2: target.x,
                y2: target.y,
                stroke: colorWithOpacity(theme.colors.axis, 0.55),
                lineWidth: 1 + (edge.value / maxEdge) * 4,
                lineCap: 'round',
            });
        });
        const maxDegree = Math.max(1, ...graphNodes.map((node) => node.degree));
        graphNodes.forEach((node, index) => {
            const position = positions.get(node.id);
            if (position === undefined)
                return;
            const nodeRadius = (layer.mark.radius ?? 8) + (node.degree / maxDegree) * 6;
            const color = themeColor(context, index, graphNodes.length);
            nodes.push({
                type: 'circle',
                ...datumBase$3(context, `${layer.id}:graph-node:${node.id}`, node.rowIndex, 1, {
                    kind: 'node',
                    node: node.label,
                    degree: node.degree,
                    total: totals.get(node.id) ?? 0,
                }),
                cx: position.x,
                cy: position.y,
                radius: nodeRadius,
                fill: layer.mark.fill ?? color,
                stroke: layer.mark.stroke ?? theme.colors.background,
                lineWidth: layer.mark.lineWidth ?? 2,
            });
            const label = pointOnCircle$2(cx, cy, radius + 20, -Math.PI / 2 + (index * TAU$3) / graphNodes.length);
            nodes.push(textNode$9(`${layer.id}:graph-label:${node.id}`, label.x, label.y, node.label, context, {
                fill: theme.colors.mutedText,
                size: Math.max(9, theme.typography.fontSize - 1),
                weight: 600,
            }));
        });
        return nodes;
    };
    const compileChordMark = (context) => {
        const { layer, plot, theme } = context;
        const { nodes: graphNodes, edges } = graphData(context);
        if (graphNodes.length < 2)
            return [];
        const cx = plot.x + plot.width / 2;
        const cy = plot.y + plot.height / 2;
        const radius = Math.max(22, Math.min(plot.width, plot.height) * 0.34);
        const totals = new Map(graphNodes.map((node) => [node.id, 0]));
        edges.forEach((edge) => {
            totals.set(edge.source, (totals.get(edge.source) ?? 0) + edge.value);
            totals.set(edge.target, (totals.get(edge.target) ?? 0) + edge.value);
        });
        const grandTotal = Math.max(1, [...totals.values()].reduce((sum, value) => sum + value, 0));
        const gap = Math.min(0.06, TAU$3 / graphNodes.length / 4);
        let cursor = -Math.PI / 2;
        const spans = new Map();
        graphNodes.forEach((node) => {
            const rawSpan = (Math.max(1, totals.get(node.id) ?? 1) / grandTotal) * (TAU$3 - gap * graphNodes.length);
            const start = cursor;
            const end = start + rawSpan;
            spans.set(node.id, { start, end, mid: (start + end) / 2 });
            cursor = end + gap;
        });
        const maxEdge = Math.max(1, ...edges.map((edge) => edge.value));
        const nodes = [];
        edges.forEach((edge, index) => {
            const sourceSpan = spans.get(edge.source);
            const targetSpan = spans.get(edge.target);
            if (sourceSpan === undefined || targetSpan === undefined)
                return;
            const source = pointOnCircle$2(cx, cy, radius - 8, sourceSpan.mid);
            const target = pointOnCircle$2(cx, cy, radius - 8, targetSpan.mid);
            const control = { x: cx, y: cy };
            nodes.push({
                type: 'path',
                ...datumBase$3(context, `${layer.id}:chord-ribbon:${index}`, edge.rowIndex, 0, {
                    kind: 'flow',
                    source: edge.source,
                    target: edge.target,
                    value: edge.value,
                }),
                points: quadraticPoints$1(source, control, target, 24),
                closed: false,
                stroke: colorWithOpacity(themeColor(context, index, edges.length), 0.42),
                lineWidth: 2 + (edge.value / maxEdge) * 9,
                lineCap: 'round',
                lineJoin: 'round',
            });
        });
        graphNodes.forEach((node, index) => {
            const span = spans.get(node.id);
            if (span === undefined)
                return;
            const color = themeColor(context, index, graphNodes.length);
            nodes.push({
                type: 'path',
                ...datumBase$3(context, `${layer.id}:chord-segment:${node.id}`, node.rowIndex, 1, {
                    kind: 'segment',
                    node: node.label,
                    total: totals.get(node.id) ?? 0,
                }),
                points: sampledArc$2(cx, cy, radius, span.start, span.end, 24),
                closed: false,
                stroke: layer.mark.stroke ?? color,
                lineWidth: layer.mark.lineWidth ?? 13,
                lineCap: 'round',
                lineJoin: 'round',
            });
            const label = pointOnCircle$2(cx, cy, radius + 20, span.mid);
            nodes.push(textNode$9(`${layer.id}:chord-label:${node.id}`, label.x, label.y, node.label, context, {
                fill: theme.colors.mutedText,
                size: Math.max(9, theme.typography.fontSize - 1),
                weight: 650,
            }));
        });
        return nodes;
    };
    const compileFunnelMark = (context) => {
        const { layer, table, plot, theme, performance } = context;
        const items = [];
        let maxValue = 1;
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const label = stringValue$4(table.value(rowIndex, layer.x.field));
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            if (label === null || value === null || value < 0)
                continue;
            items.push({ label, value, rowIndex });
            maxValue = Math.max(maxValue, value);
        }
        if (layer.mark.options.sort !== false)
            items.sort((left, right) => right.value - left.value);
        if (items.length === 0)
            return [];
        const itemBudget = Math.max(1, Math.floor(performance.maxBarMarks / 2));
        const retainedItems = exactStrideSampleIndices(items.length, itemBudget).map((index) => items[index]);
        const stageHeight = plot.height / retainedItems.length;
        const nodes = [];
        if (layer.mark.options.mode === 'area') {
            retainedItems.forEach((item, index) => {
                if (item === undefined)
                    return;
                // Scaling both dimensions by sqrt(value) keeps the visible stage area
                // proportional while retaining a stable slot for labels and hit testing.
                const scale = Math.sqrt(clamp$b(item.value / maxValue, 0.015, 1));
                const width = plot.width * scale;
                const height = Math.max(8, (stageHeight - 4) * scale);
                const cx = plot.x + plot.width / 2;
                const cy = plot.y + stageHeight * (index + 0.5);
                const topWidth = width;
                const bottomWidth = width * 0.82;
                const fill = layer.mark.fill ?? themeColor(context, index, retainedItems.length);
                nodes.push({
                    type: 'path',
                    ...datumBase$3(context, `${layer.id}:funnel-area:${index}`, item.rowIndex, 0, {
                        stage: item.label,
                        value: item.value,
                        share: item.value / maxValue,
                    }),
                    points: [
                        { x: cx - topWidth / 2, y: cy - height / 2 },
                        { x: cx + topWidth / 2, y: cy - height / 2 },
                        { x: cx + bottomWidth / 2, y: cy + height / 2 },
                        { x: cx - bottomWidth / 2, y: cy + height / 2 },
                    ],
                    closed: true,
                    fill,
                    stroke: layer.mark.stroke ?? theme.colors.background,
                    lineWidth: layer.mark.lineWidth ?? 2,
                    lineJoin: 'round',
                });
                if (width > 68 && height > 18) {
                    nodes.push(textNode$9(`${layer.id}:funnel-area-label:${index}`, cx, cy, `${item.label}  ${item.value}`, context, {
                        fill: readableTextColor(fill, '#ffffff', '#0f172a'),
                        size: Math.max(9, Math.min(theme.typography.fontSize, height * 0.42)),
                        weight: 700,
                    }));
                }
            });
            return nodes;
        }
        retainedItems.forEach((item, index) => {
            if (item === undefined)
                return;
            const next = retainedItems[index + 1];
            const topWidth = plot.width * clamp$b(item.value / maxValue, 0.08, 1);
            const bottomWidth = plot.width * clamp$b((next?.value ?? item.value * 0.78) / maxValue, 0.06, 1);
            const y1 = plot.y + index * stageHeight + 2;
            const y2 = plot.y + (index + 1) * stageHeight - 2;
            const cx = plot.x + plot.width / 2;
            const fill = layer.mark.fill ?? themeColor(context, index, retainedItems.length);
            nodes.push({
                type: 'path',
                ...datumBase$3(context, `${layer.id}:funnel:${index}`, item.rowIndex),
                points: [
                    { x: cx - topWidth / 2, y: y1 },
                    { x: cx + topWidth / 2, y: y1 },
                    { x: cx + bottomWidth / 2, y: y2 },
                    { x: cx - bottomWidth / 2, y: y2 },
                ],
                closed: true,
                fill,
                stroke: layer.mark.stroke ?? theme.colors.background,
                lineWidth: layer.mark.lineWidth ?? 2,
                lineJoin: 'round',
            });
            nodes.push(textNode$9(`${layer.id}:funnel-label:${index}`, cx, (y1 + y2) / 2, `${item.label}  ${item.value}`, context, {
                fill: readableTextColor(fill, '#ffffff', '#0f172a'),
                size: Math.max(10, theme.typography.fontSize),
                weight: 700,
            }));
        });
        return nodes;
    };
    function parallelDimensions(context, maximumDimensions = Number.POSITIVE_INFINITY) {
        const { layer, table } = context;
        const configured = optionStrings$3(layer.mark.options.dimensions);
        const candidates = configured.length > 0
            ? configured
            : [layer.x.field, layer.y.field, ...Object.values(layer.mark.fields)];
        const fields = candidates.filter((field, index, all) => table.has(field) && all.indexOf(field) === index);
        return fields.slice(0, maximumDimensions).map((field) => {
            const extent = table.extent(field);
            const values = table.unique(field);
            return extent === null
                ? { field, values, min: 0, max: Math.max(1, values.length - 1), numeric: false }
                : { field, values, min: extent[0], max: extent[1], numeric: true };
        });
    }
    function compileParallelCategories(context, dimensions) {
        const { layer, table, plot, theme, performance } = context;
        const nodeBudget = Math.max(1, Math.floor(performance.maxBarMarks));
        const linePointBudget = Math.max(1, Math.floor(performance.maxLinePoints));
        // One retained combination can emit one ribbon, up to one block and one
        // label per dimension, plus one title per dimension. Reserve that worst-case
        // footprint up front so wide categorical tables cannot bypass the profile.
        const dimensionLimit = Math.min(dimensions.length, Math.floor((nodeBudget - 1) / 3), Math.floor(linePointBudget / 2));
        if (dimensionLimit < 2)
            return [];
        const retainedDimensions = dimensions.slice(0, dimensionLimit);
        const xFor = (index) => plot.x + (index / Math.max(1, retainedDimensions.length - 1)) * plot.width;
        const combinations = new Map();
        const combinationBudget = Math.min(Math.floor((nodeBudget - retainedDimensions.length) / (retainedDimensions.length * 2 + 1)), Math.floor(linePointBudget / (retainedDimensions.length * 2)));
        if (combinationBudget < 1)
            return [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const values = retainedDimensions.map((dimension) => stringValue$4(table.value(rowIndex, dimension.field)));
            if (values.some((value) => value === null))
                continue;
            const categories = values;
            const key = JSON.stringify(categories);
            const existing = combinations.get(key);
            if (existing !== undefined)
                existing.count += 1;
            else if (combinations.size < combinationBudget) {
                combinations.set(key, { values: categories, count: 1, rowIndex });
            }
        }
        const retainedCombinations = [...combinations.values()];
        const retainedTotal = retainedCombinations.reduce((sum, combination) => sum + combination.count, 0);
        if (retainedTotal <= 0)
            return [];
        const categoryCounts = retainedDimensions.map(() => new Map());
        const categoryOrders = retainedDimensions.map(() => []);
        retainedCombinations.forEach((combination) => {
            combination.values.forEach((value, dimensionIndex) => {
                const counts = categoryCounts[dimensionIndex];
                const order = categoryOrders[dimensionIndex];
                if (counts !== undefined) {
                    if (!counts.has(value))
                        order?.push(value);
                    counts.set(value, (counts.get(value) ?? 0) + combination.count);
                }
            });
        });
        const countScale = plot.height / retainedTotal;
        const categoryLayouts = retainedDimensions.map((_dimension, dimensionIndex) => {
            const counts = categoryCounts[dimensionIndex] ?? new Map();
            let offset = 0;
            const layouts = new Map();
            categoryOrders[dimensionIndex]?.forEach((value) => {
                const count = counts.get(value) ?? 0;
                if (count <= 0)
                    return;
                const height = count * countScale;
                const start = plot.y + offset * countScale;
                layouts.set(value, { start, center: start + height / 2, height });
                offset += count;
            });
            return layouts;
        });
        const nodes = [];
        const categoryOffsets = retainedDimensions.map(() => new Map());
        retainedCombinations.forEach((combination, combinationIndex) => {
            const bands = combination.values.map((value, dimensionIndex) => {
                const layout = categoryLayouts[dimensionIndex]?.get(value);
                const offsets = categoryOffsets[dimensionIndex];
                const offset = offsets?.get(value) ?? 0;
                if (offsets !== undefined)
                    offsets.set(value, offset + combination.count);
                const top = (layout?.start ?? plot.y) + offset * countScale;
                return {
                    x: xFor(dimensionIndex),
                    top,
                    bottom: top + combination.count * countScale,
                };
            });
            const polygon = [
                ...bands.map(({ x, top }) => ({ x, y: top })),
                ...[...bands].reverse().map(({ x, bottom }) => ({ x, y: bottom })),
            ];
            const combinationColor = themeColor(context, combinationIndex, retainedCombinations.length);
            const fill = layer.mark.fill ?? colorWithOpacity(combinationColor, 0.34);
            nodes.push({
                type: 'path',
                ...datumBase$3(context, `${layer.id}:parallel-ribbon:${combinationIndex}`, combination.rowIndex, 0, {
                    path: combination.values.join(' → '),
                    count: combination.count,
                }),
                points: polygon,
                closed: true,
                fill,
                stroke: layer.mark.stroke ?? colorWithOpacity(combinationColor, 0.72),
                lineWidth: layer.mark.lineWidth ?? 1,
                lineJoin: 'round',
            });
        });
        retainedDimensions.forEach((dimension, dimensionIndex) => {
            const x = xFor(dimensionIndex);
            const layouts = categoryLayouts[dimensionIndex];
            categoryOrders[dimensionIndex]?.forEach((value, categoryIndex) => {
                const item = layouts?.get(value);
                if (item === undefined)
                    return;
                const blockGap = Math.min(2, item.height * 0.08);
                const rectHeight = Math.max(Number.EPSILON, item.height - blockGap);
                const rectY = item.start + blockGap / 2;
                nodes.push({
                    type: 'rect',
                    ...nodeBase(`${layer.id}:parallel-category:${dimensionIndex}:${categoryIndex}`, {
                        zIndex: layer.zIndex + 2,
                    }),
                    x: x - 6,
                    y: rectY,
                    width: 12,
                    height: rectHeight,
                    fill: theme.colors.surface,
                    stroke: theme.colors.axis,
                    lineWidth: 1,
                    cornerRadius: 2,
                });
                if (item.height >= 10) {
                    nodes.push(textNode$9(`${layer.id}:parallel-category-label:${dimensionIndex}:${categoryIndex}`, x + (dimensionIndex === retainedDimensions.length - 1 ? -10 : 10), item.center, value, context, {
                        align: dimensionIndex === retainedDimensions.length - 1 ? 'right' : 'left',
                        fill: theme.colors.mutedText,
                        size: Math.max(8, theme.typography.fontSize - 2),
                    }));
                }
            });
            nodes.push(textNode$9(`${layer.id}:parallel-category-title:${dimensionIndex}`, x, plot.y - 10, dimension.field, context, { fill: theme.colors.text, size: Math.max(9, theme.typography.fontSize - 1), weight: 700 }));
        });
        return nodes;
    }
    const compileParallelMark = (context) => {
        const { layer, table, plot, theme, performance } = context;
        const mode = typeof layer.mark.options.mode === 'string' ? layer.mark.options.mode : 'coordinates';
        const categorical = mode === 'categories' || mode === 'categorical' || mode === 'ribbons';
        const dimensionScanLimit = categorical
            ? Math.min(Math.floor((performance.maxBarMarks - 1) / 3), Math.floor(performance.maxLinePoints / 2))
            : Number.POSITIVE_INFINITY;
        const dimensions = parallelDimensions(context, dimensionScanLimit);
        if (dimensions.length < 2)
            return [];
        if (categorical) {
            return compileParallelCategories(context, dimensions);
        }
        const nodes = [];
        const xFor = (index) => plot.x +
            (dimensions.length === 1 ? plot.width / 2 : (index / (dimensions.length - 1)) * plot.width);
        dimensions.forEach((dimension, index) => {
            const x = xFor(index);
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:parallel-axis:${dimension.field}`, { zIndex: layer.zIndex }),
                x1: x,
                y1: plot.y,
                x2: x,
                y2: plot.y + plot.height,
                stroke: theme.colors.axis,
                lineWidth: 1.2,
                lineCap: 'round',
            });
            nodes.push(textNode$9(`${layer.id}:parallel-label:${dimension.field}`, x, plot.y + 10, dimension.field, context, {
                fill: theme.colors.mutedText,
                size: Math.max(9, theme.typography.fontSize - 1),
                weight: 650,
                baseline: 'top',
            }));
        });
        const colorField = layer.mark.fields.color ?? layer.mark.fields.group;
        const colorKeys = colorField === undefined ? [] : table.unique(colorField);
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const points = [];
            dimensions.forEach((dimension, dimensionIndex) => {
                const raw = table.value(rowIndex, dimension.field);
                let ratio;
                if (dimension.numeric) {
                    const value = numericDataValue(raw);
                    if (value === null)
                        return;
                    ratio =
                        dimension.max === dimension.min
                            ? 0.5
                            : (value - dimension.min) / (dimension.max - dimension.min);
                }
                else {
                    const value = stringValue$4(raw);
                    const index = value === null ? -1 : dimension.values.indexOf(value);
                    if (index < 0)
                        return;
                    ratio = dimension.values.length <= 1 ? 0.5 : index / (dimension.values.length - 1);
                }
                points.push({ x: xFor(dimensionIndex), y: plot.y + plot.height * (1 - clamp$b(ratio, 0, 1)) });
            });
            if (points.length !== dimensions.length)
                continue;
            const key = colorField === undefined ? null : stringValue$4(table.value(rowIndex, colorField));
            const colorIndex = key === null ? rowIndex : Math.max(0, colorKeys.indexOf(key));
            nodes.push({
                type: 'path',
                ...datumBase$3(context, `${layer.id}:parallel-row:${rowIndex}`, rowIndex, 1),
                points,
                closed: false,
                stroke: layer.mark.stroke ??
                    themeColor(context, colorIndex, colorKeys.length > 0 ? colorKeys.length : table.length),
                lineWidth: layer.mark.lineWidth ?? 1.8,
                lineCap: 'round',
                lineJoin: 'round',
            });
        }
        return nodes;
    };
    const compileBoxplotMark = (context) => {
        const { layer, table, xScale, yScale, theme } = context;
        const minField = layer.mark.fields.min ?? layer.mark.fields.low ?? 'min';
        const q1Field = layer.mark.fields.q1 ?? 'q1';
        const medianField = layer.mark.fields.median ?? layer.y.field;
        const q3Field = layer.mark.fields.q3 ?? 'q3';
        const maxField = layer.mark.fields.max ?? layer.mark.fields.high ?? 'max';
        const summaryFields = [minField, q1Field, medianField, q3Field, maxField];
        if (!summaryFields.every((field) => table.has(field))) {
            const valueField = layer.mark.fields.value ?? layer.y.field;
            const grouped = new Map();
            for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
                const xValue = table.value(rowIndex, layer.x.field);
                const category = stringValue$4(xValue);
                const value = numericDataValue(table.value(rowIndex, valueField));
                if (category === null || value === null)
                    continue;
                const group = grouped.get(category) ?? { xValue, observations: [] };
                group.observations.push({ value, weight: 1, rowIndex });
                grouped.set(category, group);
            }
            const boxWidth = Math.max(8, xScale instanceof BandScale
                ? xScale.bandwidth * 0.55
                : context.plot.width / Math.max(3, grouped.size * 2));
            const nodes = [];
            [...grouped].forEach(([category, group], groupIndex) => {
                const summary = rawBoxSummary(group.observations, finiteOption(layer.mark.options.whisker, 1.5));
                const input = scaleInput(group.xValue);
                if (summary === null || input === null)
                    return;
                const x = xScale.map(input);
                const yLow = yScale.map(summary.lowerWhisker);
                const yQ1 = yScale.map(summary.q1);
                const yMedian = yScale.map(summary.median);
                const yQ3 = yScale.map(summary.q3);
                const yHigh = yScale.map(summary.upperWhisker);
                const rowIndex = summary.rowIndices[0] ?? 0;
                const fill = layer.mark.fill ??
                    theme.mark.boxplotFill ??
                    theme.mark.areaFill ??
                    colorWithOpacity(context.color, 0.22);
                const stroke = layer.mark.stroke ?? theme.mark.areaStroke ?? theme.mark.lineColor ?? context.color;
                const tooltip = {
                    kind: 'raw-box-summary',
                    category,
                    valueField,
                    minimum: summary.minimum,
                    q1: summary.q1,
                    median: summary.median,
                    q3: summary.q3,
                    maximum: summary.maximum,
                    lowerWhisker: summary.lowerWhisker,
                    upperWhisker: summary.upperWhisker,
                    outlierCount: summary.outliers.length,
                    sourceRowCount: summary.rowIndices.length,
                    sourceRowIndices: summary.rowIndices.slice(0, 256),
                };
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:boxplot-whisker:${groupIndex}`, { zIndex: layer.zIndex }),
                    x1: x,
                    y1: yLow,
                    x2: x,
                    y2: yHigh,
                    stroke,
                    lineWidth: layer.mark.lineWidth ?? theme.mark.boxplotLineWidth ?? 1.5,
                    lineCap: theme.mark.lineCap ?? 'round',
                });
                [yLow, yHigh].forEach((y, capIndex) => nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:boxplot-cap:${groupIndex}:${capIndex}`, {
                        zIndex: layer.zIndex + 0.5,
                    }),
                    x1: x - boxWidth * 0.3,
                    y1: y,
                    x2: x + boxWidth * 0.3,
                    y2: y,
                    stroke,
                    lineWidth: layer.mark.lineWidth ?? theme.mark.boxplotLineWidth ?? 1.5,
                    lineCap: theme.mark.lineCap ?? 'round',
                }));
                nodes.push({
                    type: 'rect',
                    ...datumBase$3(context, `${layer.id}:boxplot-box:${groupIndex}`, rowIndex, 1, tooltip),
                    x: x - boxWidth / 2,
                    y: Math.min(yQ1, yQ3),
                    width: boxWidth,
                    height: Math.max(1, Math.abs(yQ3 - yQ1)),
                    fill,
                    stroke,
                    lineWidth: layer.mark.lineWidth ?? theme.mark.boxplotLineWidth ?? 1.8,
                    cornerRadius: layer.mark.cornerRadius ?? theme.mark.boxplotRadius ?? 3,
                });
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:boxplot-median:${groupIndex}`, { zIndex: layer.zIndex + 2 }),
                    x1: x - boxWidth / 2,
                    y1: yMedian,
                    x2: x + boxWidth / 2,
                    y2: yMedian,
                    stroke: layer.mark.stroke ??
                        theme.mark.boxplotMedianStroke ??
                        mixColor$1(stroke, theme.colors.text, 0.22),
                    lineWidth: layer.mark.lineWidth ?? theme.mark.boxplotLineWidth ?? 2.2,
                    lineCap: theme.mark.lineCap ?? 'round',
                });
                summary.outliers
                    .slice(0, context.performance.maxPointMarks)
                    .forEach((outlier, outlierIndex) => nodes.push({
                    type: 'circle',
                    ...datumBase$3(context, `${layer.id}:boxplot-outlier:${groupIndex}:${outlierIndex}`, outlier.rowIndex, 3, {
                        kind: 'boxplot-outlier',
                        category,
                        value: outlier.value,
                        valueField,
                    }),
                    cx: x,
                    cy: yScale.map(outlier.value),
                    radius: layer.mark.radius ?? 3,
                    fill: theme.colors.background,
                    stroke,
                    lineWidth: 1,
                }));
            });
            return nodes;
        }
        const nodes = [];
        const boxWidth = Math.max(8, xScale instanceof BandScale
            ? xScale.bandwidth * 0.55
            : context.plot.width / Math.max(3, table.length * 2));
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const xValue = scaleInput(table.value(rowIndex, layer.x.field));
            const values = [minField, q1Field, medianField, q3Field, maxField].map((field) => table.has(field) ? numericDataValue(table.value(rowIndex, field)) : null);
            if (xValue === null || values.some((value) => value === null))
                continue;
            const [min, q1, median, q3, max] = values;
            const x = xScale.map(xValue);
            const yMin = yScale.map(min);
            const yQ1 = yScale.map(q1);
            const yMedian = yScale.map(median);
            const yQ3 = yScale.map(q3);
            const yMax = yScale.map(max);
            if (![x, yMin, yQ1, yMedian, yQ3, yMax].every(Number.isFinite))
                continue;
            const fill = layer.mark.fill ??
                theme.mark.boxplotFill ??
                theme.mark.areaFill ??
                colorWithOpacity(context.color, 0.22);
            const stroke = layer.mark.stroke ?? theme.mark.areaStroke ?? theme.mark.lineColor ?? context.color;
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:boxplot-whisker:${rowIndex}`, { zIndex: layer.zIndex }),
                x1: x,
                y1: yMin,
                x2: x,
                y2: yMax,
                stroke,
                lineWidth: layer.mark.lineWidth ?? theme.mark.boxplotLineWidth ?? 1.5,
                lineCap: theme.mark.lineCap ?? 'round',
            });
            [yMin, yMax].forEach((y, capIndex) => {
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:boxplot-cap:${rowIndex}:${capIndex}`, {
                        zIndex: layer.zIndex + 0.5,
                    }),
                    x1: x - boxWidth * 0.3,
                    y1: y,
                    x2: x + boxWidth * 0.3,
                    y2: y,
                    stroke,
                    lineWidth: layer.mark.lineWidth ?? theme.mark.boxplotLineWidth ?? 1.5,
                    lineCap: theme.mark.lineCap ?? 'round',
                });
            });
            nodes.push({
                type: 'rect',
                ...datumBase$3(context, `${layer.id}:boxplot-box:${rowIndex}`, rowIndex, 1),
                x: x - boxWidth / 2,
                y: Math.min(yQ1, yQ3),
                width: boxWidth,
                height: Math.max(1, Math.abs(yQ3 - yQ1)),
                fill,
                stroke,
                lineWidth: layer.mark.lineWidth ?? theme.mark.boxplotLineWidth ?? 1.8,
                cornerRadius: layer.mark.cornerRadius ?? theme.mark.boxplotRadius ?? 3,
            });
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:boxplot-median:${rowIndex}`, { zIndex: layer.zIndex + 2 }),
                x1: x - boxWidth / 2,
                y1: yMedian,
                x2: x + boxWidth / 2,
                y2: yMedian,
                stroke: layer.mark.stroke ??
                    theme.mark.boxplotMedianStroke ??
                    mixColor$1(stroke, theme.colors.text, 0.22),
                lineWidth: layer.mark.lineWidth ?? theme.mark.boxplotLineWidth ?? 2.2,
                lineCap: theme.mark.lineCap ?? 'round',
            });
        }
        return nodes;
    };
    const compileEffectScatterMark = (context) => {
        const { layer, table, xScale, yScale, theme } = context;
        const sizeField = layer.mark.fields.size;
        let maxSize = 1;
        if (sizeField !== undefined && table.has(sizeField)) {
            const extent = table.extent(sizeField);
            maxSize = Math.max(1, extent?.[1] ?? 1);
        }
        const nodes = [];
        const ringCount = clamp$b(Math.floor(finiteOption(layer.mark.options.rings, 2)), 1, 4);
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const xValue = scaleInput(table.value(rowIndex, layer.x.field));
            const yValue = scaleInput(table.value(rowIndex, layer.y.field));
            if (xValue === null || yValue === null)
                continue;
            const x = xScale.map(xValue);
            const y = yScale.map(yValue);
            if (!Number.isFinite(x) || !Number.isFinite(y))
                continue;
            const size = sizeField === undefined || !table.has(sizeField)
                ? 1
                : (numericDataValue(table.value(rowIndex, sizeField)) ?? 1) / maxSize;
            const radius = (layer.mark.radius ?? 5.5) * (0.7 + Math.sqrt(Math.max(0, size)) * 0.8);
            const color = layer.mark.fill ??
                themedPointFill(theme, context.color, theme.mark.defaultColor ?? context.color);
            for (let ring = ringCount; ring >= 1; ring -= 1) {
                nodes.push({
                    type: 'circle',
                    ...nodeBase(`${layer.id}:effect-ring:${rowIndex}:${ring}`, {
                        zIndex: layer.zIndex,
                        opacity: 0.08 + (ringCount - ring) * 0.05,
                    }),
                    cx: x,
                    cy: y,
                    radius: radius * (1.45 + ring * 0.48),
                    fill: colorWithOpacity(color, 0.16),
                    stroke: colorWithOpacity(color, 0.38),
                    lineWidth: 1,
                });
            }
            nodes.push({
                type: 'circle',
                ...datumBase$3(context, `${layer.id}:effect-point:${rowIndex}`, rowIndex, 2),
                cx: x,
                cy: y,
                radius,
                fill: color,
                stroke: layer.mark.stroke ?? themedPointStroke(theme, color, theme.colors.background),
                lineWidth: layer.mark.lineWidth ?? 2,
            });
        }
        return nodes;
    };
    const compileLinesMark = (context) => {
        const { layer, table, xScale, yScale, theme } = context;
        const x2Field = layer.mark.fields.x2 ?? 'x2';
        const y2Field = layer.mark.fields.y2 ?? 'y2';
        const valueField = layer.mark.fields.value;
        const curvature = clamp$b(finiteOption(layer.mark.options.curvature, 0.18), -1, 1);
        const nodes = [];
        let maxValue = 1;
        if (valueField !== undefined && table.has(valueField))
            maxValue = Math.max(1, table.extent(valueField)?.[1] ?? 1);
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            if (!table.has(x2Field) || !table.has(y2Field))
                continue;
            const startX = scaleInput(table.value(rowIndex, layer.x.field));
            const startY = scaleInput(table.value(rowIndex, layer.y.field));
            const endX = scaleInput(table.value(rowIndex, x2Field));
            const endY = scaleInput(table.value(rowIndex, y2Field));
            if (startX === null || startY === null || endX === null || endY === null)
                continue;
            const start = { x: xScale.map(startX), y: yScale.map(startY) };
            const end = { x: xScale.map(endX), y: yScale.map(endY) };
            if (![start.x, start.y, end.x, end.y].every(Number.isFinite))
                continue;
            const dx = end.x - start.x;
            const dy = end.y - start.y;
            const length = Math.hypot(dx, dy) || 1;
            const control = {
                x: (start.x + end.x) / 2 - (dy / length) * length * curvature,
                y: (start.y + end.y) / 2 + (dx / length) * length * curvature,
            };
            const points = Math.abs(curvature) < 0.001 ? [start, end] : quadraticPoints$1(start, control, end, 24);
            const value = valueField === undefined || !table.has(valueField)
                ? 1
                : (numericDataValue(table.value(rowIndex, valueField)) ?? 1);
            const stroke = layer.mark.stroke ?? theme.mark.lineColor ?? theme.mark.defaultColor ?? context.color;
            nodes.push({
                type: 'path',
                ...datumBase$3(context, `${layer.id}:lines-path:${rowIndex}`, rowIndex),
                points,
                closed: false,
                stroke,
                lineWidth: (layer.mark.lineWidth ?? 1.8) + (value / maxValue) * 3,
                lineCap: theme.mark.lineCap ?? 'round',
                lineJoin: theme.mark.lineJoin ?? 'round',
            });
            const previous = points.at(-2) ?? start;
            const angle = Math.atan2(end.y - previous.y, end.x - previous.x);
            const arrowSize = 5 + (value / maxValue) * 3;
            nodes.push({
                type: 'path',
                ...nodeBase(`${layer.id}:lines-arrow:${rowIndex}`, { zIndex: layer.zIndex + 1 }),
                points: [
                    end,
                    pointOnCircle$2(end.x, end.y, arrowSize, angle + Math.PI * 0.82),
                    pointOnCircle$2(end.x, end.y, arrowSize, angle - Math.PI * 0.82),
                ],
                closed: true,
                fill: stroke,
                lineWidth: 0,
            });
        }
        return nodes;
    };
    function heatmapCellSpan(positions, fallback, gap) {
        const sorted = [...new Set(positions.filter(Number.isFinite))].sort((left, right) => left - right);
        let minimum = Number.POSITIVE_INFINITY;
        for (let index = 1; index < sorted.length; index += 1) {
            minimum = Math.min(minimum, (sorted[index] ?? 0) - (sorted[index - 1] ?? 0));
        }
        const span = Number.isFinite(minimum) ? minimum : fallback;
        return Math.max(1, span - Math.min(gap, Math.max(0, span - 1)));
    }
    const compileHeatmapMark = (context) => {
        const { layer, table, plot, xScale, yScale, theme } = context;
        const encoding = createEncodingResolver(context);
        const valueField = layer.mark.fields.value ?? 'value';
        if (!table.has(valueField))
            return [];
        const values = [];
        const xPositions = [];
        const yPositions = [];
        const indices = encoding.orderedIndices(Array.from({ length: table.length }, (_value, index) => index));
        for (const rowIndex of indices) {
            const value = numericDataValue(table.value(rowIndex, valueField));
            const xValue = scaleInput(table.value(rowIndex, layer.x.field));
            const yValue = scaleInput(table.value(rowIndex, layer.y.field));
            if (value === null || xValue === null || yValue === null)
                continue;
            values.push(value);
            xPositions.push(xScale.map(xValue));
            yPositions.push(yScale.map(yValue));
        }
        if (values.length === 0)
            return [];
        const min = Math.min(...values);
        const max = Math.max(...values);
        const cellGap = clamp$b(finiteOption(layer.mark.options.cellGap, 1), 0, 24);
        const xCellPositions = xScale instanceof BandScale ? xScale.domain().map((value) => xScale.map(value)) : xPositions;
        const yCellPositions = yScale instanceof BandScale ? yScale.domain().map((value) => yScale.map(value)) : yPositions;
        const cellWidth = heatmapCellSpan(xCellPositions, xScale instanceof BandScale ? plot.width / Math.max(1, xScale.domain().length) : 18, cellGap);
        const cellHeight = heatmapCellSpan(yCellPositions, yScale instanceof BandScale ? plot.height / Math.max(1, yScale.domain().length) : 18, cellGap);
        const nodes = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const value = numericDataValue(table.value(rowIndex, valueField));
            const xValue = scaleInput(table.value(rowIndex, layer.x.field));
            const yValue = scaleInput(table.value(rowIndex, layer.y.field));
            if (value === null || xValue === null || yValue === null)
                continue;
            const x = xScale.map(xValue);
            const y = yScale.map(yValue);
            if (!Number.isFinite(x) || !Number.isFinite(y))
                continue;
            const ratio = max === min ? 0.5 : clamp$b((value - min) / (max - min), 0, 1);
            const seriesColor = encoding.color('color', rowIndex, mappedContinuousColor(theme, ratio));
            const color = encoding.color('fill', rowIndex, encoding.has('color') ? seriesColor : (layer.mark.fill ?? seriesColor));
            const x2 = encoding.position('x2', rowIndex);
            const y2 = encoding.position('y2', rowIndex);
            const cellX = x2 === null ? x - cellWidth / 2 : Math.min(x, x2);
            const cellY = y2 === null ? y - cellHeight / 2 : Math.min(y, y2);
            const width = x2 === null ? cellWidth : Math.max(1, Math.abs(x2 - x));
            const height = y2 === null ? cellHeight : Math.max(1, Math.abs(y2 - y));
            const stroke = encoding.color('stroke', rowIndex, layer.mark.stroke ?? theme.colors.background);
            const tooltip = encoding.tooltip(rowIndex);
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:heatmap:${rowIndex}`, {
                    zIndex: layer.zIndex,
                    opacity: encoding.number('opacity', rowIndex, layer.mark.opacity),
                    interactive: context.performance.enableHitTesting,
                    datum: {
                        layerId: layer.id,
                        rowIndex,
                        datum: table.row(rowIndex),
                        ...(tooltip === undefined ? {} : { tooltip }),
                    },
                }),
                x: cellX,
                y: cellY,
                width,
                height,
                fill: color,
                stroke,
                lineWidth: encoding.number('strokeWidth', rowIndex, layer.mark.lineWidth ?? 1),
                ...(encoding.dash(rowIndex).length === 0 ? {} : { dash: encoding.dash(rowIndex) }),
                cornerRadius: layer.mark.cornerRadius ?? 1,
            });
            if (width >= 34 && height >= 24 && layer.mark.options.labels !== false) {
                nodes.push(textNode$9(`${layer.id}:heatmap-label:${rowIndex}`, cellX + width / 2, cellY + height / 2, encoding.text(rowIndex, String(value)) ?? String(value), context, {
                    fill: readableTextColor(color, '#ffffff', '#0f172a'),
                    size: Math.max(9, theme.typography.fontSize - 1),
                    weight: 650,
                }));
            }
        }
        return nodes;
    };
    const compilePictorialBarMark = (context) => {
        const { layer, table, xScale, yScale, plot, theme } = context;
        const maxSymbols = clamp$b(Math.floor(finiteOption(layer.mark.options.maxSymbols, 12)), 2, 40);
        let maxAbs = 0;
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            maxAbs = Math.max(maxAbs, Math.abs(numericDataValue(table.value(rowIndex, layer.y.field)) ?? 0));
        }
        const unit = Math.max(1e-9, finiteOption(layer.mark.options.unit, maxAbs / maxSymbols || 1));
        const symbol = typeof layer.mark.options.symbol === 'string' ? layer.mark.options.symbol : 'circle';
        const nodes = [];
        const zero = yScale.map(0);
        const band = xScale instanceof BandScale ? xScale.bandwidth : plot.width / Math.max(2, table.length * 1.5);
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const category = scaleInput(table.value(rowIndex, layer.x.field));
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            if (category === null || value === null)
                continue;
            const x = xScale.map(category);
            const end = yScale.map(value);
            if (![x, end, zero].every(Number.isFinite))
                continue;
            const count = Math.min(maxSymbols, Math.max(1, Math.ceil(Math.abs(value) / unit)));
            const step = Math.abs(end - zero) / count;
            const requestedSize = finiteOption(layer.mark.options.symbolSize, Number.NaN);
            const size = Number.isFinite(requestedSize)
                ? clamp$b(Math.abs(requestedSize), 3, Math.max(3, band * 0.9))
                : Math.max(3, Math.min(band * 0.62, step * 0.72, 18));
            const color = layer.mark.fill ?? themeColor(context, rowIndex, table.length);
            for (let index = 0; index < count; index += 1) {
                const y = zero + Math.sign(end - zero || -1) * step * (index + 0.5);
                const id = `${layer.id}:pictorial:${rowIndex}:${index}`;
                if (symbol === 'square') {
                    nodes.push({
                        type: 'rect',
                        ...datumBase$3(context, id, rowIndex),
                        x: x - size / 2,
                        y: y - size / 2,
                        width: size,
                        height: size,
                        fill: color,
                        stroke: layer.mark.stroke ?? theme.colors.background,
                        lineWidth: layer.mark.lineWidth ?? 1,
                        cornerRadius: layer.mark.cornerRadius ?? 2,
                    });
                }
                else if (symbol === 'diamond') {
                    nodes.push({
                        type: 'path',
                        ...datumBase$3(context, id, rowIndex),
                        points: [
                            { x, y: y - size / 2 },
                            { x: x + size / 2, y },
                            { x, y: y + size / 2 },
                            { x: x - size / 2, y },
                        ],
                        closed: true,
                        fill: color,
                        stroke: layer.mark.stroke ?? theme.colors.background,
                        lineWidth: layer.mark.lineWidth ?? 1,
                        lineJoin: 'round',
                    });
                }
                else {
                    nodes.push({
                        type: 'circle',
                        ...datumBase$3(context, id, rowIndex),
                        cx: x,
                        cy: y,
                        radius: size / 2,
                        fill: color,
                        stroke: layer.mark.stroke ?? theme.colors.background,
                        lineWidth: layer.mark.lineWidth ?? 1,
                    });
                }
            }
        }
        return nodes;
    };
    const compileThemeRiverMark = (context) => {
        return compileSeriesAreaMark(context) ?? [];
    };
    function sunburstTree(items) {
        const nodes = new Map();
        items.forEach((item) => nodes.set(item.id, { ...item, children: [], total: item.value }));
        const roots = [];
        nodes.forEach((node) => {
            const parent = node.parent === null ? undefined : nodes.get(node.parent);
            if (parent === undefined)
                roots.push(node);
            else
                parent.children.push(node);
        });
        const totalOf = (node, visiting = new Set()) => {
            if (visiting.has(node.id))
                return Math.max(1, node.value);
            visiting.add(node.id);
            const childTotal = node.children.reduce((sum, child) => sum + totalOf(child, visiting), 0);
            visiting.delete(node.id);
            node.total = node.value > 0 ? node.value : Math.max(1, childTotal);
            if (childTotal > node.total)
                node.total = childTotal;
            return node.total;
        };
        roots.forEach((root) => totalOf(root));
        return roots;
    }
    const compileSunburstMark = (context) => {
        const { layer, plot, theme } = context;
        const items = hierarchyItems(context);
        const roots = sunburstTree(items);
        if (roots.length === 0)
            return [];
        const depths = hierarchyDepths(items);
        const maxDepth = Math.max(0, ...depths.values());
        const cx = plot.x + plot.width / 2;
        const cy = plot.y + plot.height / 2;
        const outerRadius = Math.max(20, Math.min(plot.width, plot.height) * 0.42);
        const innerHole = outerRadius * clamp$b(finiteOption(layer.mark.options.innerRadius, 0.12), 0, 0.7);
        const ringWidth = (outerRadius - innerHole) / Math.max(1, maxDepth + 1);
        const nodes = [];
        let colorIndex = 0;
        const drawNode = (node, start, end, depth) => {
            const inner = innerHole + depth * ringWidth + 1;
            const outer = innerHole + (depth + 1) * ringWidth - 1;
            const color = themeColor(context, colorIndex, items.length);
            colorIndex += 1;
            nodes.push({
                type: 'path',
                ...datumBase$3(context, `${layer.id}:sunburst:${node.id}`, node.rowIndex, depth / 100),
                points: annularSector$2(cx, cy, inner, outer, start, end),
                closed: true,
                fill: layer.mark.fill ?? mixColor$1(color, theme.colors.background, depth * 0.08),
                stroke: layer.mark.stroke ?? theme.colors.background,
                lineWidth: layer.mark.lineWidth ?? 1.5,
                lineJoin: 'round',
            });
            if (end - start > 0.18 && outer - inner > 18) {
                const angle = (start + end) / 2;
                const labelPoint = pointOnCircle$2(cx, cy, (inner + outer) / 2, angle);
                nodes.push(textNode$9(`${layer.id}:sunburst-label:${node.id}`, labelPoint.x, labelPoint.y, node.label, context, {
                    fill: readableTextColor(layer.mark.fill ?? color, '#ffffff', '#0f172a'),
                    size: Math.max(8, theme.typography.fontSize - 2),
                    weight: 650,
                    rotation: (angle * 180) / Math.PI + 90,
                }));
            }
            if (node.children.length === 0)
                return;
            const total = Math.max(1, node.children.reduce((sum, child) => sum + child.total, 0));
            let cursor = start;
            node.children.forEach((child) => {
                const childEnd = cursor + ((end - start) * child.total) / total;
                drawNode(child, cursor, childEnd, depth + 1);
                cursor = childEnd;
            });
        };
        const rootTotal = Math.max(1, roots.reduce((sum, root) => sum + root.total, 0));
        let cursor = -Math.PI / 2;
        roots.forEach((root) => {
            const end = cursor + (TAU$3 * root.total) / rootTotal;
            drawNode(root, cursor, end, 0);
            cursor = end;
        });
        return nodes;
    };
    const compileCustomMark = (context) => {
        const { layer, table, xScale, yScale, theme } = context;
        const defaultPrimitive = typeof layer.mark.options.primitive === 'string' ? layer.mark.options.primitive : 'circle';
        const primitiveField = layer.mark.fields.primitive ?? layer.mark.fields.shape;
        const x2Field = layer.mark.fields.x2;
        const y2Field = layer.mark.fields.y2;
        const labelField = layer.mark.fields.label;
        const sizeField = layer.mark.fields.size;
        const widthField = layer.mark.fields.width;
        const heightField = layer.mark.fields.height;
        const radiusField = layer.mark.fields.radius;
        const nodes = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const xValue = scaleInput(table.value(rowIndex, layer.x.field));
            const yValue = scaleInput(table.value(rowIndex, layer.y.field));
            if (xValue === null || yValue === null)
                continue;
            const x = xScale.map(xValue);
            const y = yScale.map(yValue);
            if (!Number.isFinite(x) || !Number.isFinite(y))
                continue;
            const primitive = primitiveField !== undefined && table.has(primitiveField)
                ? (stringValue$4(table.value(rowIndex, primitiveField)) ?? defaultPrimitive)
                : defaultPrimitive;
            const size = sizeField !== undefined && table.has(sizeField)
                ? Math.abs(numericDataValue(table.value(rowIndex, sizeField)) ?? 12)
                : Math.abs(finiteOption(layer.mark.options.size, 12));
            const fill = layer.mark.fill ?? themeColor(context, rowIndex, table.length);
            if (primitive === 'line' &&
                x2Field !== undefined &&
                y2Field !== undefined &&
                table.has(x2Field) &&
                table.has(y2Field)) {
                const x2Value = scaleInput(table.value(rowIndex, x2Field));
                const y2Value = scaleInput(table.value(rowIndex, y2Field));
                if (x2Value === null || y2Value === null)
                    continue;
                const x2 = xScale.map(x2Value);
                const y2 = yScale.map(y2Value);
                if (!Number.isFinite(x2) || !Number.isFinite(y2))
                    continue;
                nodes.push({
                    type: 'line',
                    ...datumBase$3(context, `${layer.id}:custom-line:${rowIndex}`, rowIndex),
                    x1: x,
                    y1: y,
                    x2,
                    y2,
                    stroke: layer.mark.stroke ?? fill,
                    lineWidth: layer.mark.lineWidth ?? 2,
                    lineCap: 'round',
                });
            }
            else if (primitive === 'rect' || primitive === 'round-rect' || primitive === 'square') {
                const width = widthField !== undefined && table.has(widthField)
                    ? (numericDataValue(table.value(rowIndex, widthField)) ?? size)
                    : primitive === 'square'
                        ? size
                        : finiteOption(layer.mark.options.width, size);
                const height = heightField !== undefined && table.has(heightField)
                    ? (numericDataValue(table.value(rowIndex, heightField)) ?? size)
                    : primitive === 'square'
                        ? size
                        : finiteOption(layer.mark.options.height, size);
                nodes.push({
                    type: 'rect',
                    ...datumBase$3(context, `${layer.id}:custom-rect:${rowIndex}`, rowIndex),
                    x: x - Math.abs(width) / 2,
                    y: y - Math.abs(height) / 2,
                    width: Math.abs(width),
                    height: Math.abs(height),
                    fill,
                    stroke: layer.mark.stroke ?? theme.colors.background,
                    lineWidth: layer.mark.lineWidth ?? 1,
                    cornerRadius: primitive === 'round-rect'
                        ? (layer.mark.cornerRadius ?? 6)
                        : (layer.mark.cornerRadius ?? 2),
                });
            }
            else if (primitive === 'diamond') {
                const radius = Math.max(1, size / 2);
                nodes.push({
                    type: 'path',
                    ...datumBase$3(context, `${layer.id}:custom-diamond:${rowIndex}`, rowIndex),
                    points: [
                        { x, y: y - radius },
                        { x: x + radius, y },
                        { x, y: y + radius },
                        { x: x - radius, y },
                    ],
                    closed: true,
                    fill,
                    stroke: layer.mark.stroke ?? theme.colors.background,
                    lineWidth: layer.mark.lineWidth ?? 1.5,
                    lineJoin: 'round',
                });
            }
            else if (primitive === 'text') {
                const label = labelField !== undefined && table.has(labelField)
                    ? (stringValue$4(table.value(rowIndex, labelField)) ?? '')
                    : String(layer.mark.options.label ?? '');
                nodes.push(textNode$9(`${layer.id}:custom-text:${rowIndex}`, x, y, label, context, {
                    fill: layer.mark.stroke ?? fill,
                    size: finiteOption(layer.mark.options.fontSize, Math.max(theme.typography.fontSize, size)),
                    weight: 650,
                }));
            }
            else {
                const radius = radiusField !== undefined && table.has(radiusField)
                    ? (numericDataValue(table.value(rowIndex, radiusField)) ?? layer.mark.radius ?? size / 2)
                    : (layer.mark.radius ?? finiteOption(layer.mark.options.radius, size / 2));
                nodes.push({
                    type: 'circle',
                    ...datumBase$3(context, `${layer.id}:custom-circle:${rowIndex}`, rowIndex),
                    cx: x,
                    cy: y,
                    radius: Math.max(0, Math.abs(radius)),
                    fill,
                    stroke: layer.mark.stroke ?? theme.colors.background,
                    lineWidth: layer.mark.lineWidth ?? 1.5,
                });
            }
            if (primitive !== 'text' &&
                layer.mark.options.labels !== false &&
                labelField !== undefined &&
                table.has(labelField)) {
                const label = stringValue$4(table.value(rowIndex, labelField));
                if (label !== null && label !== '') {
                    nodes.push(textNode$9(`${layer.id}:custom-label:${rowIndex}`, x, clamp$b(y - size / 2 - 8, context.plot.y + 8, context.plot.y + context.plot.height - 8), label, context, {
                        fill: theme.colors.mutedText,
                        size: Math.max(9, theme.typography.fontSize - 1),
                        weight: 600,
                        baseline: 'middle',
                    }));
                }
            }
        }
        return nodes;
    };

    function quantile(values, probability) {
        const position = Math.max(0, Math.min(1, probability)) * (values.length - 1);
        const lower = Math.floor(position);
        const upper = Math.ceil(position);
        const a = values[lower];
        const b = values[upper];
        return a + (b - a) * (position - lower);
    }
    /** Resolves sorted unique explicit, linear, or type-7 quantile thresholds. */
    function contourThresholds(grid, options = {}) {
        const values = grid
            .flatMap((row) => row)
            .filter((value) => value !== null && Number.isFinite(value))
            .sort((left, right) => left - right);
        if (values.length === 0)
            return [];
        const minimum = values[0];
        const maximum = values.at(-1);
        const explicit = options.thresholds
            ?.filter((value) => Number.isFinite(value) && value >= minimum && value <= maximum)
            .sort((left, right) => left - right);
        if (explicit !== undefined)
            return [...new Set(explicit)];
        if (minimum === maximum)
            return [minimum];
        const count = Math.max(1, Math.min(32, Math.trunc(options.levels ?? 5)));
        return Array.from({ length: count }, (_, index) => {
            const probability = (index + 1) / (count + 1);
            return options.method === 'quantile'
                ? quantile(values, probability)
                : minimum + probability * (maximum - minimum);
        }).filter((value, index, thresholds) => index === 0 || value !== thresholds[index - 1]);
    }
    function interpolate(start, end, startValue, endValue, level) {
        const denominator = endValue - startValue;
        const ratio = denominator === 0 ? 0.5 : Math.max(0, Math.min(1, (level - startValue) / denominator));
        return { x: start.x + (end.x - start.x) * ratio, y: start.y + (end.y - start.y) * ratio };
    }
    function pointKey$1(point) {
        return `${Math.round(point.x * 1e9)}:${Math.round(point.y * 1e9)}`;
    }
    function saddleHighConnected(values, level, policy, code) {
        if (policy === 'high')
            return true;
        if (policy === 'low')
            return false;
        // Nielson-Hamann asymptotic decider for the bilinear cell. The sign of
        // Q=a*c-b*d selects which diagonally-opposed sign regions connect. Equality
        // consistently connects the high corners.
        const [a, b, c, d] = values.map((value) => value - level);
        const q = a * c - b * d;
        return code === 5 ? q >= 0 : q <= 0;
    }
    function cellSegments(values, points, level, levelIndex, sourceRows, saddle) {
        const high = values.map((value) => value >= level);
        const code = (high[0] ? 1 : 0) | (high[1] ? 2 : 0) | (high[2] ? 4 : 0) | (high[3] ? 8 : 0);
        if (code === 0 || code === 15)
            return [];
        const edges = [
            [0, 1],
            [1, 2],
            [2, 3],
            [3, 0],
        ];
        const crossing = new Map();
        edges.forEach(([start, end], edge) => {
            if (high[start] === high[end])
                return;
            crossing.set(edge, interpolate(points[start], points[end], values[start], values[end], level));
        });
        const pair = (a, b) => {
            const start = crossing.get(a);
            const end = crossing.get(b);
            return start === undefined || end === undefined
                ? null
                : { level, levelIndex, points: [start, end], sourceRows };
        };
        if (crossing.size === 2) {
            const indices = [...crossing.keys()];
            const segment = pair(indices[0], indices[1]);
            return segment === null ? [] : [segment];
        }
        const centerHigh = saddleHighConnected(values, level, saddle, code);
        // Corner order is top-left, top-right, bottom-right, bottom-left. Pairing is
        // chosen around low corners when the centre is high, and around high corners
        // when it is low, preserving topology through ambiguous saddles.
        const pairs = code === 5
            ? centerHigh
                ? [
                    [0, 1],
                    [2, 3],
                ]
                : [
                    [0, 3],
                    [1, 2],
                ]
            : centerHigh
                ? [
                    [0, 3],
                    [1, 2],
                ]
                : [
                    [0, 1],
                    [2, 3],
                ];
        return pairs.flatMap(([a, b]) => {
            const segment = pair(a, b);
            return segment === null ? [] : [segment];
        });
    }
    function stitchSegments(segments) {
        const byLevel = new Map();
        for (const segment of segments) {
            const group = byLevel.get(segment.levelIndex) ?? [];
            group.push(segment);
            byLevel.set(segment.levelIndex, group);
        }
        const output = [];
        for (const [, levelSegments] of [...byLevel].sort(([a], [b]) => a - b)) {
            const adjacency = new Map();
            levelSegments.forEach((segment, index) => {
                segment.points.forEach((point, endpoint) => {
                    const key = pointKey$1(point);
                    const entries = adjacency.get(key) ?? [];
                    entries.push({ index, endpoint: endpoint });
                    adjacency.set(key, entries);
                });
            });
            const visited = new Set();
            for (let seed = 0; seed < levelSegments.length; seed += 1) {
                if (visited.has(seed))
                    continue;
                const segment = levelSegments[seed];
                const firstDegree = adjacency.get(pointKey$1(segment.points[0]))?.length ?? 0;
                const secondDegree = adjacency.get(pointKey$1(segment.points[1]))?.length ?? 0;
                let current = firstDegree === 1 || secondDegree !== 1 ? segment.points[0] : segment.points[1];
                const startKey = pointKey$1(current);
                const points = [current];
                const rows = new Set();
                while (true) {
                    const candidate = (adjacency.get(pointKey$1(current)) ?? []).find(({ index }) => !visited.has(index));
                    if (candidate === undefined)
                        break;
                    visited.add(candidate.index);
                    const nextSegment = levelSegments[candidate.index];
                    nextSegment.sourceRows.forEach((row) => rows.add(row));
                    current = nextSegment.points[candidate.endpoint === 0 ? 1 : 0];
                    points.push(current);
                    if (pointKey$1(current) === startKey)
                        break;
                }
                if (points.length >= 2) {
                    output.push({
                        level: segment.level,
                        levelIndex: segment.levelIndex,
                        points,
                        closed: points.length > 2 && pointKey$1(points[0]) === pointKey$1(points.at(-1)),
                        sourceRows: [...rows].sort((a, b) => a - b),
                    });
                }
            }
        }
        return output;
    }
    /** Extracts deterministic marching-squares isoline topology from a scalar grid. */
    function extractIsolines(values, points, levels, sourceRows, options = {}) {
        const maximumSegments = Math.max(0, Math.trunc(options.maximumSegments ?? Number.MAX_SAFE_INTEGER));
        if (maximumSegments === 0)
            return [];
        const segments = [];
        outer: for (let row = 0; row < values.length - 1; row += 1) {
            const width = Math.min(values[row]?.length ?? 0, values[row + 1]?.length ?? 0);
            for (let column = 0; column < width - 1; column += 1) {
                const cellValues = [
                    values[row]?.[column],
                    values[row]?.[column + 1],
                    values[row + 1]?.[column + 1],
                    values[row + 1]?.[column],
                ];
                const cellPoints = [
                    points[row]?.[column],
                    points[row]?.[column + 1],
                    points[row + 1]?.[column + 1],
                    points[row + 1]?.[column],
                ];
                if (cellValues.some((value) => value === null || value === undefined || !Number.isFinite(value)) ||
                    cellPoints.some((point) => point === null || point === undefined))
                    continue;
                const rows = [
                    sourceRows?.[row]?.[column],
                    sourceRows?.[row]?.[column + 1],
                    sourceRows?.[row + 1]?.[column + 1],
                    sourceRows?.[row + 1]?.[column],
                ].flatMap((value) => value === null || value === undefined ? [] : typeof value === 'number' ? [value] : value);
                for (let levelIndex = 0; levelIndex < levels.length; levelIndex += 1) {
                    const level = levels[levelIndex];
                    if (level === undefined)
                        continue;
                    for (const segment of cellSegments(cellValues, cellPoints, level, levelIndex, rows, options.saddle ?? 'asymptotic')) {
                        segments.push(segment);
                        if (segments.length >= maximumSegments)
                            break outer;
                    }
                }
            }
        }
        return stitchSegments(segments);
    }

    function compileAreaSeries(context, defaults) {
        const { table, layer, yScale, color, theme, performance } = context;
        const encoding = createEncodingResolver(context);
        const curve = curveNameForMark(layer.mark.options, defaults.curve);
        const curveOptions = curveOptionsForMark(layer.mark.options);
        const segments = interpolateSegments(collectCurveSegments(context, defaults.missing), curve, curveOptions, performance.maxLinePoints);
        if (segments.length === 0)
            return [];
        const mappedZero = yScale.map(0);
        const baseline = Number.isFinite(mappedZero) ? mappedZero : yScale.map(yScale.domain()[0] ?? 0);
        const nodes = [];
        segments.forEach((segment, segmentIndex) => {
            const first = segment.points[0];
            const last = segment.points.at(-1);
            if (first === undefined || last === undefined)
                return;
            const suffix = segments.length === 1 ? '' : `:${segmentIndex}`;
            const representative = segment.source[0]?.rowIndex ?? 0;
            const seriesColor = encoding.color('color', representative, color);
            const strokeColor = encoding.color('stroke', representative, encoding.has('color')
                ? seriesColor
                : (layer.mark.stroke ??
                    themedAreaStroke(theme, seriesColor, theme.mark.lineColor ?? theme.mark.defaultColor ?? seriesColor)));
            const lowerSource = segment.source.map(({ point, rowIndex }) => ({
                x: point.x,
                y: encoding.position('y2', rowIndex) ?? baseline,
            }));
            const interpolatedLower = encoding.has('y2')
                ? interpolateCurve(lowerSource, curve, curveOptions)
                : [
                    { x: last.x, y: baseline },
                    { x: first.x, y: baseline },
                ];
            const lower = interpolatedLower.length <= segment.points.length
                ? interpolatedLower
                : minMaxSampleIndices(interpolatedLower.map(({ y }) => y), segment.points.length)
                    .map((index) => interpolatedLower[index])
                    .filter((point) => point !== undefined);
            const points = [...segment.points, ...[...lower].reverse()];
            const opacity = encoding.number('opacity', representative, layer.mark.opacity);
            const fillColor = encoding.color('fill', representative, encoding.has('color')
                ? seriesColor
                : (layer.mark.fill ??
                    themedAreaFill(theme, seriesColor, colorWithOpacity(seriesColor, theme.mode === 'dark' ? 0.28 : 0.2))));
            const lineWidth = encoding.number('strokeWidth', representative, layer.mark.lineWidth ?? theme.mark.lineWidth);
            const dash = encoding.dash(representative);
            const fill = {
                type: 'path',
                ...nodeBase(`${layer.id}:${defaults.idStem}-fill${suffix}`, {
                    zIndex: layer.zIndex,
                    opacity,
                }),
                points,
                closed: true,
                fill: fillColor,
                lineWidth: 0,
            };
            nodes.push(fill);
            if (layer.mark.stroke !== undefined || theme.mark.areaStrokeVisible !== false) {
                const stroke = {
                    type: 'path',
                    ...nodeBase(`${layer.id}:${defaults.idStem}-line${suffix}`, {
                        zIndex: layer.zIndex + 0.1,
                        opacity,
                    }),
                    points: segment.points,
                    closed: false,
                    stroke: strokeColor,
                    lineWidth,
                    lineCap: theme.mark.lineCap ?? 'round',
                    lineJoin: theme.mark.lineJoin ?? 'round',
                    ...(dash.length === 0 ? {} : { dash }),
                };
                nodes.push(stroke);
            }
        });
        if (layer.mark.point) {
            const sourcePoints = segments.flatMap(({ source }) => source);
            const pointRows = new Set(strideSampleIndices(sourcePoints.length, performance.maxPointMarks)
                .map((index) => sourcePoints[index]?.rowIndex)
                .filter((rowIndex) => rowIndex !== undefined));
            for (const { source } of segments) {
                for (const { point, rowIndex } of source) {
                    if (!pointRows.has(rowIndex))
                        continue;
                    const pointColor = encoding.color('color', rowIndex, color);
                    const encodedSize = encoding.number('size', rowIndex, Number.NaN);
                    const radius = encoding.number('radius', rowIndex, Number.isFinite(encodedSize)
                        ? Math.sqrt(Math.max(0, encodedSize) / Math.PI)
                        : (layer.mark.radius ?? theme.mark.pointRadius));
                    const tooltip = encoding.tooltip(rowIndex);
                    const circle = {
                        type: 'circle',
                        ...nodeBase(`${layer.id}:${defaults.idStem}-point:${rowIndex}`, {
                            zIndex: layer.zIndex + 0.2,
                            opacity: encoding.number('opacity', rowIndex, layer.mark.opacity),
                            interactive: performance.enableHitTesting,
                            datum: {
                                layerId: layer.id,
                                rowIndex,
                                datum: table.row(rowIndex),
                                ...(tooltip === undefined ? {} : { tooltip }),
                            },
                        }),
                        cx: point.x,
                        cy: point.y,
                        radius,
                        fill: encoding.color('fill', rowIndex, encoding.has('color')
                            ? pointColor
                            : (layer.mark.fill ?? themedPointFill(theme, pointColor, theme.colors.background))),
                        stroke: encoding.color('stroke', rowIndex, encoding.has('color')
                            ? pointColor
                            : (layer.mark.stroke ??
                                themedPointStroke(theme, pointColor, themedAreaStroke(theme, pointColor, theme.mark.lineColor ?? pointColor)))),
                        lineWidth: encoding.number('strokeWidth', rowIndex, layer.mark.lineWidth === undefined
                            ? (theme.mark.pointStrokeWidth ?? Math.max(1.5, theme.mark.lineWidth * 0.68))
                            : Math.max(1.5, layer.mark.lineWidth * 0.68)),
                        ...(encoding.dash(rowIndex).length === 0 ? {} : { dash: encoding.dash(rowIndex) }),
                    };
                    nodes.push(circle);
                }
            }
        }
        return nodes;
    }
    const compileAreaMark = (context) => {
        const series = compileSeriesAreaMark(context);
        return (series ?? compileAreaSeries(context, { curve: 'straight', missing: 'connect', idStem: 'area' }));
    };

    const compileBarMark = (context) => {
        const series = compileSeriesBarMark(context);
        if (series !== null)
            return series;
        const { table, layer, xScale, yScale, color, theme, barGroup, performance, plot } = context;
        const encoding = createEncodingResolver(context);
        const themedWidthRatio = theme.mark.barWidthRatio;
        if (layer.mark.orientation === 'horizontal') {
            const defaultBaseline = xScale.map(0);
            const slotHeight = yScale instanceof BandScale
                ? (themedWidthRatio === undefined ? yScale.bandwidth : yScale.step) /
                    Math.max(1, barGroup.count)
                : Math.max(1, ((plot.height / Math.max(1, table.length)) *
                    (themedWidthRatio === undefined ? 0.8 : 1)) /
                    Math.max(1, barGroup.count));
            const barHeight = Math.max(1, slotHeight * (themedWidthRatio ?? 0.74));
            const nodes = [];
            const indices = encoding.orderedIndices(strideSampleIndices(table.length, performance.maxBarMarks));
            for (const rowIndex of indices) {
                const xInput = scaleInput(table.value(rowIndex, layer.x.field));
                const yInput = scaleInput(table.value(rowIndex, layer.y.field));
                if (xInput === null || yInput === null)
                    continue;
                const xValue = xScale.map(xInput);
                const yCenter = yScale.map(yInput);
                const baseline = encoding.position('x2', rowIndex) ??
                    (Number.isFinite(defaultBaseline) ? defaultBaseline : xScale.map(xScale.domain()[0] ?? 0));
                if (!Number.isFinite(xValue) || !Number.isFinite(yCenter) || !Number.isFinite(baseline))
                    continue;
                const groupOffset = layer.mark.position === 'group'
                    ? (barGroup.index - (barGroup.count - 1) / 2) * slotHeight
                    : 0;
                const encodedY2 = encoding.position('y2', rowIndex);
                const seriesColor = encoding.color('color', rowIndex, color);
                const fill = encoding.color('fill', rowIndex, encoding.has('color')
                    ? seriesColor
                    : (layer.mark.fill ?? theme.mark.barFill ?? seriesColor));
                const fallbackStroke = layer.mark.stroke ?? theme.mark.barStroke;
                const stroke = encoding.color('stroke', rowIndex, fallbackStroke ?? seriesColor);
                const hasStroke = encoding.has('stroke') || fallbackStroke !== undefined;
                const lineWidth = encoding.number('strokeWidth', rowIndex, layer.mark.lineWidth ??
                    (hasStroke ? (theme.mark.barStrokeWidth ?? theme.mark.lineWidth) : 0));
                const resolvedY = encodedY2 === null ? yCenter + groupOffset - barHeight / 2 : Math.min(yCenter, encodedY2);
                const resolvedHeight = encodedY2 === null ? barHeight : Math.max(0.5, Math.abs(encodedY2 - yCenter));
                const tooltip = encoding.tooltip(rowIndex);
                nodes.push({
                    type: 'rect',
                    ...nodeBase(`${layer.id}:bar:${rowIndex}`, {
                        zIndex: layer.zIndex,
                        opacity: encoding.number('opacity', rowIndex, layer.mark.opacity),
                        interactive: performance.enableHitTesting,
                        datum: {
                            layerId: layer.id,
                            rowIndex,
                            datum: table.row(rowIndex),
                            ...(tooltip === undefined ? {} : { tooltip }),
                        },
                    }),
                    x: Math.min(xValue, baseline),
                    y: resolvedY,
                    width: Math.max(0.5, Math.abs(baseline - xValue)),
                    height: resolvedHeight,
                    fill,
                    ...(hasStroke ? { stroke } : {}),
                    lineWidth,
                    ...(encoding.dash(rowIndex).length === 0 ? {} : { dash: encoding.dash(rowIndex) }),
                    cornerRadius: layer.mark.cornerRadius ?? theme.mark.barRadius,
                });
            }
            return nodes;
        }
        const defaultBaseline = yScale.map(0);
        const nodes = [];
        const slotWidth = xScale instanceof BandScale
            ? (themedWidthRatio === undefined ? xScale.bandwidth : xScale.step) /
                Math.max(1, barGroup.count)
            : Math.max(1, ((plot.width / Math.max(1, table.length)) * (themedWidthRatio === undefined ? 0.8 : 1)) /
                Math.max(1, barGroup.count));
        const barWidth = Math.max(1, slotWidth * (themedWidthRatio ?? 0.74));
        const indices = encoding.orderedIndices(strideSampleIndices(table.length, performance.maxBarMarks));
        for (const rowIndex of indices) {
            const xInput = scaleInput(table.value(rowIndex, layer.x.field));
            const yInput = scaleInput(table.value(rowIndex, layer.y.field));
            if (xInput === null || yInput === null)
                continue;
            const xCenter = xScale.map(xInput);
            const yValue = yScale.map(yInput);
            const baseline = encoding.position('y2', rowIndex) ??
                (Number.isFinite(defaultBaseline) ? defaultBaseline : yScale.map(yScale.domain()[0] ?? 0));
            if (!Number.isFinite(xCenter) || !Number.isFinite(yValue) || !Number.isFinite(baseline))
                continue;
            const groupOffset = layer.mark.position === 'group' ? (barGroup.index - (barGroup.count - 1) / 2) * slotWidth : 0;
            const encodedX2 = encoding.position('x2', rowIndex);
            const x = encodedX2 === null ? xCenter + groupOffset - barWidth / 2 : Math.min(xCenter, encodedX2);
            const width = encodedX2 === null ? barWidth : Math.max(0.5, Math.abs(encodedX2 - xCenter));
            const y = Math.min(yValue, baseline);
            const height = Math.max(0.5, Math.abs(baseline - yValue));
            const seriesColor = encoding.color('color', rowIndex, color);
            const fill = encoding.color('fill', rowIndex, encoding.has('color') ? seriesColor : (layer.mark.fill ?? theme.mark.barFill ?? seriesColor));
            const fallbackStroke = layer.mark.stroke ?? theme.mark.barStroke;
            const stroke = encoding.color('stroke', rowIndex, fallbackStroke ?? seriesColor);
            const hasStroke = encoding.has('stroke') || fallbackStroke !== undefined;
            const lineWidth = encoding.number('strokeWidth', rowIndex, layer.mark.lineWidth ?? (hasStroke ? (theme.mark.barStrokeWidth ?? theme.mark.lineWidth) : 0));
            const tooltip = encoding.tooltip(rowIndex);
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:bar:${rowIndex}`, {
                    zIndex: layer.zIndex,
                    opacity: encoding.number('opacity', rowIndex, layer.mark.opacity),
                    interactive: performance.enableHitTesting,
                    datum: {
                        layerId: layer.id,
                        rowIndex,
                        datum: table.row(rowIndex),
                        ...(tooltip === undefined ? {} : { tooltip }),
                    },
                }),
                x,
                y,
                width,
                height,
                fill,
                ...(hasStroke ? { stroke } : {}),
                lineWidth,
                ...(encoding.dash(rowIndex).length === 0 ? {} : { dash: encoding.dash(rowIndex) }),
                cornerRadius: layer.mark.cornerRadius ?? theme.mark.barRadius,
            });
        }
        return nodes;
    };

    const compileLineMark = (context) => {
        const { table, layer, color, theme, performance } = context;
        const encoding = createEncodingResolver(context);
        const segments = interpolateSegments(collectCurveSegments(context, 'gap'), curveNameForMark(layer.mark.options, 'straight'), curveOptionsForMark(layer.mark.options), performance.maxLinePoints);
        const nodes = [];
        const sourcePoints = segments.flatMap(({ source }) => source);
        const pointRows = new Set(strideSampleIndices(sourcePoints.length, performance.maxPointMarks)
            .map((sampleIndex) => sourcePoints[sampleIndex]?.rowIndex)
            .filter((rowIndex) => rowIndex !== undefined));
        segments.forEach((segment, segmentIndex) => {
            const representative = segment.source[0]?.rowIndex ?? 0;
            const seriesColor = encoding.color('color', representative, color);
            const stroke = encoding.color('stroke', representative, encoding.has('color')
                ? seriesColor
                : (layer.mark.stroke ?? theme.mark.lineColor ?? theme.mark.defaultColor ?? seriesColor));
            const lineWidth = encoding.number('strokeWidth', representative, layer.mark.lineWidth ?? theme.mark.lineWidth);
            const dash = encoding.dash(representative);
            const path = {
                type: 'path',
                ...nodeBase(`${layer.id}:line:${segmentIndex}`, {
                    zIndex: layer.zIndex,
                    opacity: encoding.number('opacity', representative, layer.mark.opacity),
                }),
                points: segment.points,
                closed: false,
                stroke,
                lineWidth,
                lineCap: theme.mark.lineCap ?? 'round',
                lineJoin: theme.mark.lineJoin ?? 'round',
                ...(dash.length === 0 ? {} : { dash }),
            };
            nodes.push(path);
            if (layer.mark.point) {
                segment.source.forEach(({ point, rowIndex }) => {
                    if (rowIndex === undefined || !pointRows.has(rowIndex))
                        return;
                    const pointColor = encoding.color('color', rowIndex, seriesColor);
                    const pointStroke = encoding.color('stroke', rowIndex, layer.mark.stroke ?? themedPointStroke(theme, pointColor, stroke));
                    const encodedSize = encoding.number('size', rowIndex, Number.NaN);
                    const radius = encoding.number('radius', rowIndex, Number.isFinite(encodedSize)
                        ? Math.sqrt(Math.max(0, encodedSize) / Math.PI)
                        : (layer.mark.radius ?? theme.mark.pointRadius));
                    const tooltip = encoding.tooltip(rowIndex);
                    const circle = {
                        type: 'circle',
                        ...nodeBase(`${layer.id}:point:${rowIndex}`, {
                            zIndex: layer.zIndex + 0.1,
                            opacity: encoding.number('opacity', rowIndex, layer.mark.opacity),
                            interactive: performance.enableHitTesting,
                            datum: {
                                layerId: layer.id,
                                rowIndex,
                                datum: table.row(rowIndex),
                                ...(tooltip === undefined ? {} : { tooltip }),
                            },
                        }),
                        cx: point.x,
                        cy: point.y,
                        radius,
                        fill: encoding.color('fill', rowIndex, layer.mark.fill ?? themedPointFill(theme, pointColor, theme.colors.background)),
                        stroke: pointStroke,
                        lineWidth: encoding.number('strokeWidth', rowIndex, layer.mark.lineWidth === undefined
                            ? (theme.mark.pointStrokeWidth ?? Math.max(1.5, lineWidth * 0.68))
                            : Math.max(1.5, lineWidth * 0.68)),
                        ...(encoding.dash(rowIndex).length === 0 ? {} : { dash: encoding.dash(rowIndex) }),
                    };
                    nodes.push(circle);
                });
            }
        });
        return nodes;
    };

    const compilePointMark = (context) => {
        const { table, layer, xScale, yScale, color, theme, performance } = context;
        const encoding = createEncodingResolver(context);
        const indices = encoding.orderedIndices(strideSampleIndices(table.length, performance.maxPointMarks));
        const nodes = [];
        for (const rowIndex of indices) {
            const xInput = scaleInput(table.value(rowIndex, layer.x.field));
            const yInput = scaleInput(table.value(rowIndex, layer.y.field));
            if (xInput === null || yInput === null)
                continue;
            const cx = xScale.map(xInput);
            const cy = yScale.map(yInput);
            if (!Number.isFinite(cx) || !Number.isFinite(cy))
                continue;
            const seriesColor = encoding.color('color', rowIndex, color);
            const fill = encoding.color('fill', rowIndex, encoding.has('color')
                ? seriesColor
                : (layer.mark.fill ??
                    themedPointFill(theme, seriesColor, theme.mark.defaultColor ?? seriesColor)));
            const stroke = encoding.color('stroke', rowIndex, encoding.has('color')
                ? seriesColor
                : (layer.mark.stroke ?? themedPointStroke(theme, seriesColor, theme.colors.background)));
            const encodedSize = encoding.number('size', rowIndex, Number.NaN);
            const radius = encoding.number('radius', rowIndex, Number.isFinite(encodedSize)
                ? Math.sqrt(Math.max(0, encodedSize) / Math.PI)
                : (layer.mark.radius ?? theme.mark.pointRadius));
            const opacity = encoding.number('opacity', rowIndex, layer.mark.opacity);
            const lineWidth = encoding.number('strokeWidth', rowIndex, layer.mark.lineWidth ?? theme.mark.pointStrokeWidth ?? 1.75);
            const dash = encoding.dash(rowIndex);
            const base = nodeBase(`${layer.id}:point:${rowIndex}`, {
                zIndex: layer.zIndex,
                opacity,
                interactive: performance.enableHitTesting,
                datum: {
                    layerId: layer.id,
                    rowIndex,
                    datum: table.row(rowIndex),
                    ...(encoding.tooltip(rowIndex) === undefined
                        ? {}
                        : { tooltip: encoding.tooltip(rowIndex) }),
                },
            });
            const icon = encoding.icon(rowIndex);
            const shape = encoding.shape(rowIndex);
            if (icon !== undefined) {
                const node = {
                    type: 'text',
                    ...base,
                    x: cx,
                    y: cy,
                    text: icon,
                    fill,
                    fontFamily: theme.typography.fontFamily,
                    fontSize: Math.max(8, radius * 2.2),
                    fontWeight: theme.typography.fontWeight ?? 400,
                    align: 'center',
                    baseline: 'middle',
                    rotation: 0,
                };
                nodes.push(node);
            }
            else if (shape === 'circle') {
                const node = {
                    type: 'circle',
                    ...base,
                    cx,
                    cy,
                    radius,
                    fill,
                    stroke,
                    lineWidth,
                    ...(dash.length === 0 ? {} : { dash }),
                };
                nodes.push(node);
            }
            else if (shape === 'square') {
                const node = {
                    type: 'rect',
                    ...base,
                    x: cx - radius,
                    y: cy - radius,
                    width: radius * 2,
                    height: radius * 2,
                    fill,
                    stroke,
                    lineWidth,
                    cornerRadius: 0,
                    ...(dash.length === 0 ? {} : { dash }),
                };
                nodes.push(node);
            }
            else {
                const points = shape === 'triangle'
                    ? [
                        { x: cx, y: cy - radius },
                        { x: cx + radius * 0.866, y: cy + radius * 0.5 },
                        { x: cx - radius * 0.866, y: cy + radius * 0.5 },
                    ]
                    : shape === 'cross'
                        ? [
                            { x: cx - radius, y: cy - radius / 3 },
                            { x: cx - radius / 3, y: cy - radius / 3 },
                            { x: cx - radius / 3, y: cy - radius },
                            { x: cx + radius / 3, y: cy - radius },
                            { x: cx + radius / 3, y: cy - radius / 3 },
                            { x: cx + radius, y: cy - radius / 3 },
                            { x: cx + radius, y: cy + radius / 3 },
                            { x: cx + radius / 3, y: cy + radius / 3 },
                            { x: cx + radius / 3, y: cy + radius },
                            { x: cx - radius / 3, y: cy + radius },
                            { x: cx - radius / 3, y: cy + radius / 3 },
                            { x: cx - radius, y: cy + radius / 3 },
                        ]
                        : [
                            { x: cx, y: cy - radius },
                            { x: cx + radius, y: cy },
                            { x: cx, y: cy + radius },
                            { x: cx - radius, y: cy },
                        ];
                const node = {
                    type: 'path',
                    ...base,
                    points,
                    closed: true,
                    fill,
                    stroke,
                    lineWidth,
                    ...(dash.length === 0 ? {} : { dash }),
                };
                nodes.push(node);
            }
            const label = encoding.text(rowIndex);
            if (label !== undefined) {
                const text = {
                    type: 'text',
                    ...nodeBase(`${layer.id}:point-label:${rowIndex}`, { zIndex: layer.zIndex + 0.1, opacity }),
                    x: cx,
                    y: cy - radius - 4,
                    text: label,
                    fill: encoding.color('color', rowIndex, theme.colors.text),
                    fontFamily: theme.typography.fontFamily,
                    fontSize: theme.typography.fontSize,
                    fontWeight: theme.typography.fontWeight ?? 400,
                    align: 'center',
                    baseline: 'bottom',
                    rotation: 0,
                };
                nodes.push(text);
            }
        }
        return nodes;
    };

    function optionNumber$7(options, name, fallback) {
        const value = options[name];
        return typeof value === 'number' && Number.isFinite(value) ? value : fallback;
    }
    function optionString$7(options, name) {
        const value = options[name];
        return typeof value === 'string' ? value : undefined;
    }
    function textNode$8(id, x, y, text, context, options = {}) {
        return {
            type: 'text',
            ...nodeBase(id, { zIndex: context.layer.zIndex + 2 }),
            x,
            y,
            text,
            fill: context.theme.colors.text,
            fontFamily: context.theme.typography.fontFamily,
            fontSize: options.size ?? context.theme.typography.fontSize,
            fontWeight: options.weight ?? 500,
            align: options.align ?? 'center',
            baseline: options.baseline ?? 'middle',
            rotation: 0,
        };
    }
    const compileSteppedAreaMark = (context) => compileAreaSeries(context, {
        curve: 'step-after',
        missing: 'connect',
        idStem: 'stepped-area',
    });
    const compileBubbleMark = (context) => {
        const { table, layer, xScale, yScale, theme, color, performance } = context;
        const sizeField = layer.mark.fields.size;
        const colorField = layer.mark.fields.color;
        const timeField = layer.mark.fields.time;
        const frame = layer.mark.options.frame;
        let minimum = Number.POSITIVE_INFINITY;
        let maximum = Number.NEGATIVE_INFINITY;
        if (sizeField !== undefined && table.has(sizeField)) {
            const extent = table.extent(sizeField);
            if (extent !== null)
                [minimum, maximum] = extent;
        }
        if (!Number.isFinite(minimum) || !Number.isFinite(maximum)) {
            minimum = 1;
            maximum = 1;
        }
        const categoryColors = new Map();
        const categoryCount = colorField === undefined
            ? 0
            : new Set(Array.from({ length: table.length }, (_, rowIndex) => {
                if (timeField !== undefined &&
                    frame !== undefined &&
                    String(table.value(rowIndex, timeField)) !== String(frame)) {
                    return null;
                }
                return String(table.value(rowIndex, colorField) ?? '');
            }).filter((value) => value !== null)).size;
        const nodes = [];
        const minimumRadius = optionNumber$7(layer.mark.options, 'minRadius', layer.mark.radius ?? 5);
        const maximumRadius = optionNumber$7(layer.mark.options, 'maxRadius', 24);
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            if (timeField !== undefined &&
                frame !== undefined &&
                String(table.value(rowIndex, timeField)) !== String(frame)) {
                continue;
            }
            const xInput = scaleInput(table.value(rowIndex, layer.x.field));
            const yInput = scaleInput(table.value(rowIndex, layer.y.field));
            if (xInput === null || yInput === null)
                continue;
            const cx = xScale.map(xInput);
            const cy = yScale.map(yInput);
            if (!Number.isFinite(cx) || !Number.isFinite(cy))
                continue;
            const size = sizeField === undefined ? maximum : numericDataValue(table.value(rowIndex, sizeField));
            const ratio = size === null || maximum === minimum
                ? 0.5
                : Math.max(0, Math.min(1, (size - minimum) / (maximum - minimum)));
            let fill = layer.mark.fill ?? themedPointFill(theme, color, theme.mark.defaultColor ?? color);
            if (layer.mark.fill === undefined && colorField !== undefined) {
                const category = String(table.value(rowIndex, colorField) ?? '');
                let categoryColor = categoryColors.get(category);
                if (categoryColor === undefined) {
                    categoryColor = categoricalColor(theme, categoryColors.size, categoryCount);
                    categoryColors.set(category, categoryColor);
                }
                fill = categoryColor;
            }
            nodes.push({
                type: 'circle',
                ...nodeBase(`${layer.id}:bubble:${rowIndex}`, {
                    zIndex: layer.zIndex,
                    opacity: layer.mark.opacity,
                    interactive: performance.enableHitTesting,
                    datum: { layerId: layer.id, rowIndex, datum: table.row(rowIndex) },
                }),
                cx,
                cy,
                radius: minimumRadius + Math.sqrt(ratio) * (maximumRadius - minimumRadius),
                fill,
                stroke: layer.mark.stroke ?? themedPointStroke(theme, fill, theme.colors.background),
                lineWidth: layer.mark.lineWidth ?? theme.mark.pointStrokeWidth ?? 2,
            });
        }
        return nodes;
    };
    const compileCandlestickMark = (context) => {
        const { table, layer, xScale, yScale, plot, theme, performance } = context;
        const openField = layer.mark.fields.open ?? 'open';
        const highField = layer.mark.fields.high ?? 'high';
        const lowField = layer.mark.fields.low ?? 'low';
        const closeField = layer.mark.fields.close ?? layer.y.field;
        const width = Math.max(3, xScale instanceof BandScale
            ? xScale.bandwidth * 0.58
            : (plot.width / Math.max(1, table.length)) * 0.56);
        const nodes = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const xInput = scaleInput(table.value(rowIndex, layer.x.field));
            const open = numericDataValue(table.value(rowIndex, openField));
            const high = numericDataValue(table.value(rowIndex, highField));
            const low = numericDataValue(table.value(rowIndex, lowField));
            const close = numericDataValue(table.value(rowIndex, closeField));
            if (xInput === null || open === null || high === null || low === null || close === null)
                continue;
            const x = xScale.map(xInput);
            const yOpen = yScale.map(open);
            const yHigh = yScale.map(high);
            const yLow = yScale.map(low);
            const yClose = yScale.map(close);
            if (![x, yOpen, yHigh, yLow, yClose].every(Number.isFinite))
                continue;
            const rising = close >= open;
            const fill = layer.mark.fill ??
                (rising
                    ? (optionString$7(layer.mark.options, 'risingColor') ?? categoricalColor(theme, 1, 4))
                    : (optionString$7(layer.mark.options, 'fallingColor') ?? categoricalColor(theme, 3, 4)));
            const datum = { layerId: layer.id, rowIndex, datum: table.row(rowIndex) };
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:wick:${rowIndex}`, {
                    zIndex: layer.zIndex,
                    opacity: layer.mark.opacity,
                }),
                x1: x,
                y1: yHigh,
                x2: x,
                y2: yLow,
                stroke: layer.mark.stroke ?? mixColor$1(fill, theme.colors.text, 0.28),
                lineWidth: layer.mark.lineWidth ?? 1.5,
                lineCap: theme.mark.lineCap ?? 'round',
            });
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:body:${rowIndex}`, {
                    zIndex: layer.zIndex + 0.1,
                    opacity: layer.mark.opacity,
                    interactive: performance.enableHitTesting,
                    datum,
                }),
                x: x - width / 2,
                y: Math.min(yOpen, yClose),
                width,
                height: Math.max(1.5, Math.abs(yOpen - yClose)),
                fill,
                stroke: layer.mark.stroke ?? fill,
                lineWidth: layer.mark.lineWidth ?? 1,
                cornerRadius: layer.mark.cornerRadius ?? 1,
            });
        }
        return nodes;
    };
    const compileHistogramMark = (context) => {
        const { table, layer, xScale, yScale, theme, color, performance } = context;
        const binCount = Math.max(1, Math.min(100, Math.floor(optionNumber$7(layer.mark.options, 'bins', 10))));
        const extent = table.extent(layer.x.field, layer.x.type === 'temporal');
        if (extent === null)
            return [];
        const weightField = layer.mark.fields.weight ?? optionString$7(layer.mark.options, 'weightField');
        const observations = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const value = numericDataValue(table.value(rowIndex, layer.x.field), layer.x.type === 'temporal');
            if (value === null)
                continue;
            const weight = weightField === undefined ? 1 : numericDataValue(table.value(rowIndex, weightField));
            if (weight === null || weight < 0)
                continue;
            observations.push({ value, weight, rowIndex });
        }
        const normalizationOption = optionString$7(layer.mark.options, 'normalization');
        const normalization = normalizationOption === 'probability' ||
            normalizationOption === 'normalized' ||
            layer.mark.options.normalized === true
            ? 'probability'
            : normalizationOption === 'density'
                ? 'density'
                : 'count';
        const bins = weightedHistogram(observations, {
            bins: binCount,
            extent,
            normalization,
            cumulative: layer.mark.options.cumulative === true,
        });
        const baseline = yScale.map(0);
        const nodes = [];
        const histogramGap = Math.max(0, theme.mark.histogramGap ?? 2);
        bins.forEach((bin, index) => {
            const x1 = xScale.map(bin.start);
            const x2 = xScale.map(bin.end);
            const y = yScale.map(bin.value);
            const rowIndex = bin.rowIndices[0];
            if (![x1, x2, y, baseline].every(Number.isFinite))
                return;
            const stroke = layer.mark.stroke ??
                theme.mark.barStroke ??
                (theme.mark.barFill === undefined ? theme.colors.background : undefined);
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:bin:${index}`, {
                    zIndex: layer.zIndex,
                    opacity: layer.mark.opacity,
                    interactive: performance.enableHitTesting && rowIndex !== undefined,
                    ...(rowIndex === undefined
                        ? {}
                        : {
                            datum: {
                                layerId: layer.id,
                                rowIndex,
                                datum: table.row(rowIndex),
                                tooltip: {
                                    kind: 'histogram-bin',
                                    binStart: bin.start,
                                    binEnd: bin.end,
                                    count: bin.count,
                                    weight: bin.weight,
                                    value: bin.value,
                                    proportion: bin.proportion,
                                    normalization,
                                    cumulative: layer.mark.options.cumulative === true,
                                    weightField: weightField ?? null,
                                    sourceRowCount: bin.rowIndices.length,
                                    sourceRowIndices: bin.rowIndices.slice(0, 256),
                                },
                            },
                        }),
                }),
                x: Math.min(x1, x2) + histogramGap,
                y: Math.min(y, baseline),
                width: Math.max(1, Math.abs(x2 - x1) - histogramGap * 2),
                height: Math.max(0.5, Math.abs(baseline - y)),
                fill: layer.mark.fill ?? theme.mark.histogramFill ?? theme.mark.barFill ?? color,
                ...(stroke === undefined ? {} : { stroke }),
                lineWidth: layer.mark.lineWidth ?? (stroke === undefined ? 0 : (theme.mark.barStrokeWidth ?? 1)),
                cornerRadius: layer.mark.cornerRadius ?? theme.mark.barRadius,
            });
        });
        return nodes;
    };
    const compileIntervalMark = (context) => {
        const { table, layer, xScale, yScale, color, theme, performance } = context;
        const lowField = layer.mark.fields.low ?? 'low';
        const highField = layer.mark.fields.high ?? 'high';
        const nodes = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const xInput = scaleInput(table.value(rowIndex, layer.x.field));
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            const low = numericDataValue(table.value(rowIndex, lowField));
            const high = numericDataValue(table.value(rowIndex, highField));
            if (xInput === null || value === null || low === null || high === null)
                continue;
            const x = xScale.map(xInput);
            const y = yScale.map(value);
            const yLow = yScale.map(low);
            const yHigh = yScale.map(high);
            if (![x, y, yLow, yHigh].every(Number.isFinite))
                continue;
            const stroke = layer.mark.stroke ?? theme.mark.lineColor ?? theme.mark.defaultColor ?? color;
            const cap = Math.max(4, (xScale instanceof BandScale ? xScale.bandwidth : 14) * 0.25);
            const base = `${layer.id}:interval:${rowIndex}`;
            const lineWidth = layer.mark.lineWidth ?? 2;
            const lines = [
                {
                    type: 'line',
                    ...nodeBase(`${base}:range`, { zIndex: layer.zIndex }),
                    x1: x,
                    y1: yHigh,
                    x2: x,
                    y2: yLow,
                    stroke,
                    lineWidth,
                    lineCap: theme.mark.lineCap ?? 'round',
                },
                {
                    type: 'line',
                    ...nodeBase(`${base}:high`, { zIndex: layer.zIndex }),
                    x1: x - cap,
                    y1: yHigh,
                    x2: x + cap,
                    y2: yHigh,
                    stroke,
                    lineWidth,
                    lineCap: theme.mark.lineCap ?? 'round',
                },
                {
                    type: 'line',
                    ...nodeBase(`${base}:low`, { zIndex: layer.zIndex }),
                    x1: x - cap,
                    y1: yLow,
                    x2: x + cap,
                    y2: yLow,
                    stroke,
                    lineWidth,
                    lineCap: theme.mark.lineCap ?? 'round',
                },
            ];
            nodes.push(...lines);
            nodes.push({
                type: 'circle',
                ...nodeBase(`${base}:value`, {
                    zIndex: layer.zIndex + 0.1,
                    interactive: performance.enableHitTesting,
                    datum: { layerId: layer.id, rowIndex, datum: table.row(rowIndex) },
                }),
                cx: x,
                cy: y,
                radius: layer.mark.radius ?? theme.mark.pointRadius + 1,
                fill: layer.mark.fill ?? themedPointFill(theme, stroke, theme.colors.background),
                stroke: layer.mark.stroke ?? themedPointStroke(theme, stroke, stroke),
                lineWidth: layer.mark.lineWidth ?? theme.mark.pointStrokeWidth ?? lineWidth,
            });
        }
        return nodes;
    };
    const compileTrendlineMark = (context) => {
        const { table, layer, xScale, yScale, color, theme } = context;
        const points = compilePointMark(context);
        const pairs = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const x = numericDataValue(table.value(rowIndex, layer.x.field), layer.x.type === 'temporal');
            const y = numericDataValue(table.value(rowIndex, layer.y.field), layer.y.type === 'temporal');
            if (x !== null && y !== null)
                pairs.push({ x, y });
        }
        if (pairs.length < 2)
            return points;
        const meanX = pairs.reduce((sum, point) => sum + point.x, 0) / pairs.length;
        const meanY = pairs.reduce((sum, point) => sum + point.y, 0) / pairs.length;
        const numerator = pairs.reduce((sum, point) => sum + (point.x - meanX) * (point.y - meanY), 0);
        const denominator = pairs.reduce((sum, point) => sum + (point.x - meanX) ** 2, 0) || 1;
        const slope = numerator / denominator;
        const intercept = meanY - slope * meanX;
        const minimum = Math.min(...pairs.map((point) => point.x));
        const maximum = Math.max(...pairs.map((point) => point.x));
        const line = {
            type: 'path',
            ...nodeBase(`${layer.id}:trendline`, {
                zIndex: layer.zIndex + 0.2,
                opacity: layer.mark.opacity,
            }),
            points: [
                { x: xScale.map(minimum), y: yScale.map(intercept + slope * minimum) },
                { x: xScale.map(maximum), y: yScale.map(intercept + slope * maximum) },
            ],
            closed: false,
            stroke: layer.mark.stroke ?? theme.mark.lineColor ?? theme.mark.defaultColor ?? color,
            lineWidth: layer.mark.lineWidth ?? theme.mark.lineWidth + 0.5,
            dash: [7, 4],
            lineCap: theme.mark.lineCap ?? 'round',
            lineJoin: theme.mark.lineJoin ?? 'round',
        };
        return [...points, line];
    };
    const compileWaterfallMark = (context) => {
        const { table, layer, xScale, yScale, theme, performance, plot } = context;
        const width = Math.max(3, xScale instanceof BandScale
            ? xScale.bandwidth * 0.62
            : (plot.width / Math.max(1, table.length)) * 0.6);
        const nodes = [];
        let total = 0;
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const xInput = scaleInput(table.value(rowIndex, layer.x.field));
            const delta = numericDataValue(table.value(rowIndex, layer.y.field));
            if (xInput === null || delta === null)
                continue;
            const previous = total;
            total += delta;
            const x = xScale.map(xInput);
            const y1 = yScale.map(previous);
            const y2 = yScale.map(total);
            if (![x, y1, y2].every(Number.isFinite))
                continue;
            const fill = layer.mark.fill ??
                (delta >= 0
                    ? (optionString$7(layer.mark.options, 'positiveColor') ?? categoricalColor(theme, 1, 4))
                    : (optionString$7(layer.mark.options, 'negativeColor') ?? categoricalColor(theme, 3, 4)));
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:waterfall:${rowIndex}`, {
                    zIndex: layer.zIndex,
                    opacity: layer.mark.opacity,
                    interactive: performance.enableHitTesting,
                    datum: { layerId: layer.id, rowIndex, datum: table.row(rowIndex) },
                }),
                x: x - width / 2,
                y: Math.min(y1, y2),
                width,
                height: Math.max(1, Math.abs(y2 - y1)),
                fill,
                lineWidth: 0,
                cornerRadius: layer.mark.cornerRadius ?? theme.mark.barRadius,
            });
            const nextInput = rowIndex + 1 < table.length ? scaleInput(table.value(rowIndex + 1, layer.x.field)) : null;
            if (nextInput !== null) {
                const nextX = xScale.map(nextInput);
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:connector:${rowIndex}`, { zIndex: layer.zIndex - 0.1 }),
                    x1: x + width / 2,
                    y1: y2,
                    x2: nextX - width / 2,
                    y2,
                    stroke: theme.colors.axis,
                    lineWidth: 1,
                    dash: [3, 3],
                    lineCap: theme.mark.lineCap ?? 'round',
                });
            }
        }
        return nodes;
    };
    const compileDiffMark = (context) => {
        const { table, layer, xScale, yScale, theme, performance, plot } = context;
        const oldField = layer.mark.fields.old ?? 'old';
        const newField = layer.mark.fields.new ?? layer.y.field;
        const width = Math.max(4, xScale instanceof BandScale
            ? xScale.bandwidth * 0.64
            : (plot.width / Math.max(1, table.length)) * 0.62);
        const baseline = yScale.map(0);
        const nodes = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const xInput = scaleInput(table.value(rowIndex, layer.x.field));
            const oldValue = numericDataValue(table.value(rowIndex, oldField));
            const newValue = numericDataValue(table.value(rowIndex, newField));
            if (xInput === null || oldValue === null || newValue === null)
                continue;
            const x = xScale.map(xInput);
            const oldY = yScale.map(oldValue);
            const newY = yScale.map(newValue);
            if (![x, oldY, newY, baseline].every(Number.isFinite))
                continue;
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:old:${rowIndex}`, { zIndex: layer.zIndex, opacity: 0.28 }),
                x: x - width / 2,
                y: Math.min(oldY, baseline),
                width,
                height: Math.max(1, Math.abs(baseline - oldY)),
                fill: optionString$7(layer.mark.options, 'oldColor') ?? theme.colors.mutedText,
                lineWidth: 0,
                cornerRadius: layer.mark.cornerRadius ?? theme.mark.barRadius,
            });
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:new:${rowIndex}`, {
                    zIndex: layer.zIndex + 0.1,
                    opacity: layer.mark.opacity,
                    interactive: performance.enableHitTesting,
                    datum: { layerId: layer.id, rowIndex, datum: table.row(rowIndex) },
                }),
                x: x - width * 0.32,
                y: Math.min(newY, baseline),
                width: width * 0.64,
                height: Math.max(1, Math.abs(baseline - newY)),
                fill: layer.mark.fill ?? theme.colors.focus,
                lineWidth: 0,
                cornerRadius: layer.mark.cornerRadius ?? theme.mark.barRadius,
            });
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:delta:${rowIndex}`, { zIndex: layer.zIndex + 0.2 }),
                x1: x,
                y1: oldY,
                x2: x,
                y2: newY,
                stroke: theme.colors.text,
                lineWidth: 1.5,
                lineCap: theme.mark.lineCap ?? 'round',
            });
        }
        return nodes;
    };
    const compileAnnotationMark = (context) => {
        const nodes = [...compileLineMark(context)];
        const { table, layer, xScale, plot, theme } = context;
        const titleField = layer.mark.fields.annotation ?? 'annotation';
        const textField = layer.mark.fields.annotationText;
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            if (!table.has(titleField))
                break;
            const annotation = table.value(rowIndex, titleField);
            const xInput = scaleInput(table.value(rowIndex, layer.x.field));
            if (annotation === null || annotation === undefined || annotation === '' || xInput === null)
                continue;
            const x = xScale.map(xInput);
            if (!Number.isFinite(x))
                continue;
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:annotation-line:${rowIndex}`, { zIndex: layer.zIndex + 0.5 }),
                x1: x,
                y1: plot.y,
                x2: x,
                y2: plot.y + plot.height,
                stroke: layer.mark.stroke ?? theme.colors.focus,
                lineWidth: 1.25,
                dash: [4, 3],
            });
            const suffix = textField !== undefined && table.has(textField)
                ? table.value(rowIndex, textField)
                : undefined;
            const label = suffix === undefined || suffix === null
                ? String(annotation)
                : `${String(annotation)} — ${String(suffix)}`;
            const width = Math.min(190, Math.max(54, label.length * 6.1 + 16));
            const labelX = Math.min(plot.x + plot.width - width - 4, x + 6);
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:annotation-pill:${rowIndex}`, {
                    zIndex: layer.zIndex + 0.6,
                    opacity: 0.96,
                }),
                x: labelX,
                y: plot.y + 4,
                width,
                height: 22,
                fill: theme.colors.surface,
                stroke: colorWithOpacity(layer.mark.stroke ?? theme.colors.focus, 0.46),
                lineWidth: 1,
                cornerRadius: 6,
            });
            nodes.push(textNode$8(`${layer.id}:annotation-label:${rowIndex}`, labelX + 8, plot.y + 15, label, context, { align: 'left', baseline: 'middle', size: 10, weight: 650 }));
        }
        return nodes;
    };
    const compileVegaMark = (context) => {
        const mark = optionString$7(context.layer.mark.options, 'mark') ?? 'line';
        if (mark === 'bar')
            return compileBarMark(context);
        if (mark === 'area')
            return compileAreaMark(context);
        if (mark === 'point' || mark === 'circle')
            return compilePointMark(context);
        return compileLineMark(context);
    };

    const NORMAL_RANGE_SIGMAS = 3.5;
    const SQRT_TWO_PI = Math.sqrt(Math.PI * 2);
    function normalDensity(value, summary) {
        const standardized = (value - summary.mean) / summary.standardDeviation;
        return Math.exp(-0.5 * standardized * standardized) / (summary.standardDeviation * SQRT_TWO_PI);
    }
    function summarizeNormalDistribution(values) {
        const accepted = [];
        let sum = 0;
        let observedMinimum = Number.POSITIVE_INFINITY;
        let observedMaximum = Number.NEGATIVE_INFINITY;
        for (const value of values) {
            if (!Number.isFinite(value))
                continue;
            accepted.push(value);
            sum += value;
            observedMinimum = Math.min(observedMinimum, value);
            observedMaximum = Math.max(observedMaximum, value);
        }
        if (accepted.length < 2)
            return null;
        const mean = sum / accepted.length;
        const variance = accepted.reduce((total, value) => total + (value - mean) ** 2, 0) /
            Math.max(1, accepted.length - 1);
        const standardDeviation = Math.sqrt(variance) || 1;
        return {
            mean,
            standardDeviation,
            observedMinimum,
            observedMaximum,
            domainMinimum: mean - standardDeviation * NORMAL_RANGE_SIGMAS,
            domainMaximum: mean + standardDeviation * NORMAL_RANGE_SIGMAS,
            maximumDensity: 1 / (standardDeviation * SQRT_TWO_PI),
        };
    }

    /* This file is generated by scripts/generate-natural-earth-world.mjs. */
    /* Natural Earth vector tag v5.1.2, Admin-0 Countries 1:110m, public domain. */
    const serializedCountries = [
        '[["FJI","FJ","FJI","242","Fiji",178,-17.8,["FJI","FJ","242","Fiji"],[[[[180,-16.1],[180,-16.6],[179.4,-16.8],[178.7,-17]',
        ',[178.6,-16.6],[179.1,-16.4],[179.4,-16.4]]],[[[178.1,-17.5],[178.4,-17.3],[178.7,-17.6],[178.6,-18.2],[177.9,-18.3],[17',
        '7.4,-18.2],[177.3,-17.7],[177.7,-17.4]]],[[[-179.8,-16],[-179.9,-16.5],[-180,-16.6],[-180,-16.1]]]]],["TZA","TZ","TZA","',
        '834","Tanzania",35,-6.1,["TZA","TZ","834","Tanzania","United Republic of Tanzania"],[[[[33.9,-0.9],[34.1,-1.1],[37.7,-3.',
        '1],[37.8,-3.7],[39.2,-4.7],[38.7,-5.9],[38.8,-6.5],[39.4,-6.8],[39.5,-7.1],[39.2,-7.7],[39.3,-8],[39.2,-8.5],[39.5,-9.1]',
        ',[39.9,-10.1],[40.3,-10.3],[39.5,-10.9],[38.4,-11.3],[37.8,-11.3],[37.5,-11.6],[36.8,-11.6],[36.5,-11.7],[35.3,-11.4],[3',
        '4.6,-11.5],[34.3,-10.2],[33.9,-9.7],[33.7,-9.4],[32.8,-9.2],[32.2,-8.9],[31.6,-8.8],[31.2,-8.6],[30.7,-8.3],[30.2,-7.1],',
        '[29.6,-6.5],[29.4,-5.9],[29.5,-5.4],[29.3,-4.5],[29.8,-4.5],[30.1,-4.1],[30.5,-3.6],[30.8,-3.4],[30.7,-3],[30.5,-2.8],[3',
        '0.5,-2.4],[30.8,-2.3],[30.8,-1.7],[30.4,-1.1],[30.8,-1],[31.9,-1]]]]],["SAH","EH","ESH","732","Western Sahara",-12.6,24,',
        '["SAH","EH","ESH","732","WS","W. Sahara","Western Sahara","B28"],[[[[-8.7,27.7],[-8.7,27.6],[-8.7,27.4],[-8.7,25.9],[-12',
        ',25.9],[-11.9,23.4],[-12.9,23.3],[-13.1,22.8],[-12.9,21.3],[-16.8,21.3],[-17.1,21],[-17,21.4],[-14.8,21.5],[-14.6,21.9],',
        '[-14.2,22.3],[-13.9,23.7],[-12.5,24.8],[-12,26],[-11.7,26.1],[-11.4,26.9],[-10.6,27],[-10.2,26.9],[-9.7,26.9],[-9.4,27.1',
        '],[-8.8,27.1],[-8.8,27.7]]]]],["CAN","CA","CAN","124","Canada",-101.9,60.3,["CAN","CA","124","Canada"],[[[[-122.8,49],[-',
        '123,49],[-124.9,50],[-125.6,50.4],[-127.4,50.8],[-128,51.7],[-127.9,52.3],[-129.1,52.8],[-129.3,53.6],[-130.5,54.3],[-13',
        '0.5,54.8],[-130,55.3],[-130,55.9],[-131.7,56.6],[-132.7,57.7],[-133.4,58.4],[-134.3,58.9],[-134.9,59.3],[-135.5,59.8],[-',
        '136.5,59.5],[-137.5,58.9],[-138.3,59.6],[-139,60],[-140,60.3],[-141,60.3],[-141,66],[-141,69.7],[-139.1,69.5],[-137.5,69',
        '],[-136.5,68.9],[-135.6,69.3],[-134.4,69.6],[-132.9,69.5],[-131.4,69.9],[-129.8,70.2],[-129.1,69.8],[-128.4,70],[-128.1,',
        '70.5],[-127.4,70.4],[-125.8,69.5],[-124.4,70.2],[-124.3,69.4],[-123.1,69.6],[-122.7,69.9],[-121.5,69.8],[-119.9,69.4],[-',
        '117.6,69],[-116.2,68.8],[-115.2,68.9],[-113.9,68.4],[-115.3,67.9],[-113.5,67.7],[-110.8,67.8],[-109.9,68],[-108.9,67.4],',
        '[-107.8,67.9],[-108.8,68.3],[-108.2,68.7],[-106.9,68.7],[-106.1,68.8],[-105.3,68.6],[-104.3,68],[-103.2,68.1],[-101.5,67',
        '.6],[-99.9,67.8],[-98.4,67.8],[-98.6,68.4],[-97.7,68.6],[-96.1,68.2],[-96.1,67.3],[-95.5,68.1],[-94.7,68.1],[-94.2,69.1]',
        ',[-95.3,69.7],[-96.5,70.1],[-96.4,71.2],[-95.2,71.9],[-93.9,71.8],[-92.9,71.3],[-91.5,70.2],[-92.4,69.7],[-90.5,69.5],[-',
        '90.6,68.5],[-89.2,69.3],[-88,68.6],[-88.3,67.9],[-87.4,67.2],[-86.3,67.9],[-85.6,68.8],[-85.5,69.9],[-84.1,69.8],[-82.6,',
        '69.7],[-81.3,69.2],[-81.2,68.7],[-82,68.1],[-81.3,67.6],[-81.4,67.1],[-83.3,66.4],[-84.7,66.3],[-85.8,66.6],[-86.1,66.1]',
        ',[-87,65.2],[-87.3,64.8],[-88.5,64.1],[-89.9,64],[-90.7,63.6],[-90.8,63],[-91.9,62.8],[-93.2,62],[-94.2,60.9],[-94.6,60.',
        '1],[-94.7,58.9],[-93.2,58.8],[-92.8,57.8],[-92.3,57.1],[-90.9,57.3],[-89,56.9],[-88,56.5],[-87.3,56],[-86.1,55.7],[-85,5',
        '5.3],[-83.4,55.2],[-82.3,55.1],[-82.4,54.3],[-82.1,53.3],[-81.4,52.2],[-79.9,51.2],[-79.1,51.5],[-78.6,52.6],[-79.1,54.1',
        '],[-79.8,54.7],[-78.2,55.1],[-77.1,55.8],[-76.5,56.5],[-76.6,57.2],[-77.3,58.1],[-78.5,58.8],[-77.3,59.9],[-77.8,60.8],[',
        '-78.1,62.3],[-77.4,62.6],[-75.7,62.3],[-74.7,62.2],[-73.8,62.4],[-72.9,62.1],[-71.7,61.5],[-71.4,61.1],[-69.6,61.1],[-69',
        '.6,60.2],[-69.3,59],[-68.4,58.8],[-67.6,58.2],[-66.2,58.8],[-65.2,59.9],[-64.6,60.3],[-63.8,59.4],[-62.5,58.2],[-61.4,57',
        '],[-61.8,56.3],[-60.5,55.8],[-59.6,55.2],[-58,54.9],[-57.3,54.6],[-56.9,53.8],[-56.2,53.6],[-55.8,53.3],[-55.7,52.1],[-5',
        '6.4,51.8],[-57.1,51.4],[-58.8,51.1],[-60,50.2],[-61.7,50.1],[-63.9,50.3],[-65.4,50.3],[-66.4,50.2],[-67.2,49.5],[-68.5,4',
        '9.1],[-70,47.7],[-71.1,46.8],[-70.3,47],[-68.6,48.3],[-66.6,49.1],[-65.1,49.2],[-64.2,48.7],[-65.1,48.1],[-64.8,47],[-64',
        '.5,46.2],[-63.2,45.7],[-61.5,45.9],[-60.5,47],[-60.4,46.3],[-59.8,45.9],[-61,45.3],[-63.3,44.7],[-64.2,44.3],[-65.4,43.5',
        '],[-66.1,43.6],[-66.2,44.5],[-64.4,45.3],[-66,45.3],[-67.1,45.1],[-67.8,45.7],[-67.8,47.1],[-68.2,47.4],[-68.9,47.2],[-6',
        '9.2,47.4],[-70,46.7],[-70.3,45.9],[-70.7,45.5],[-71.1,45.3],[-71.4,45.3],[-71.5,45],[-73.3,45],[-74.9,45],[-75.3,44.8],[',
        '-76.4,44.1],[-76.5,44],[-76.8,43.6],[-77.7,43.6],[-78.7,43.6],[-79.2,43.5],[-79,43.3],[-78.9,43],[-78.9,42.9],[-80.2,42.',
        '4],[-81.3,42.2],[-82.4,41.7],[-82.7,41.7],[-83,41.8],[-83.1,42],[-83.1,42.1],[-82.9,42.4],[-82.4,43],[-82.1,43.6],[-82.3',
        ',44.4],[-82.6,45.3],[-83.6,45.8],[-83.5,46],[-83.6,46.1],[-83.9,46.1],[-84.1,46.3],[-84.1,46.5],[-84.3,46.4],[-84.6,46.4',
        '],[-84.5,46.5],[-84.8,46.6],[-84.9,46.9],[-85.7,47.2],[-86.5,47.6],[-87.4,47.9],[-88.4,48.3],[-89.3,48],[-89.6,48],[-90.',
        '8,48.3],[-91.6,48.1],[-92.6,48.5],[-93.6,48.6],[-94.3,48.7],[-94.6,48.8],[-94.8,49.4],[-95.2,49.4],[-95.2,49],[-97.2,49]',
        ',[-100.6,49],[-104,49],[-107,49],[-110,49],[-113,49],[-116,49],[-117,49],[-120,49]]],[[[-84,62.5],[-83.3,62.9],[-81.9,62',
        '.9],[-81.9,62.7],[-83.1,62.2],[-83.8,62.2]]],[[[-79.8,72.8],[-80.9,73.3],[-80.8,73.7],[-80.4,73.8],[-78.1,73.7],[-76.3,7',
        '3.1],[-76.3,72.8],[-77.3,72.9],[-78.4,72.9],[-79.5,72.7]]],[[[-80.3,62.1],[-79.9,62.4],[-79.5,62.4],[-79.3,62.2],[-79.7,',
        '61.6],[-80.1,61.7],[-80.4,62]]],[[[-93.6,75],[-94.2,74.6],[-95.6,74.7],[-96.8,74.9],[-96.3,75.4],[-94.9,75.6],[-94,75.3]',
        ']],[[[-93.8,77.5],[-94.3,77.5],[-96.2,77.6],[-96.4,77.8],[-94.4,77.8],[-93.7,77.6]]],[[[-96.8,78.8],[-95.6,78.4],[-95.8,',
        '78.1],[-97.3,77.9],[-98.1,78.1],[-98.6,78.5],[-98.6,78.9],[-97.3,78.8]]],[[[-88.2,74.4],[-89.8,74.5],[-92.4,74.8],[-92.8',
        ',75.4],[-92.9,75.9],[-93.9,76.3],[-96,76.4],[-97.1,76.8],[-96.7,77.2],[-94.7,77.1],[-93.6,76.8],[-91.6,76.8],[-90.7,76.4',
        '],[-91,76.1],[-89.8,75.8],[-89.2,75.6],[-87.8,75.6],[-86.4,75.5],[-84.8,75.7],[-82.8,75.8],[-81.1,75.7],[-80.1,75.3],[-7',
        '9.8,74.9],[-80.5,74.7],[-81.9,74.4],[-83.2,74.6],[-86.1,74.4]]],[[[-111.3,78.2],[-109.9,78],[-110.2,77.7],[-112.1,77.4],',
        '[-113.5,77.7],[-112.7,78.1]]],[[[-111,78.8],[-109.7,78.6],[-110.9,78.4],[-112.5,78.4],[-112.5,78.6],[-111.5,78.8]]],[[[-',
        '55.6,51.3],[-56.1,50.7],[-56.8,49.8],[-56.1,50.2],[-55.5,49.9],[-55.8,49.6],[-54.9,49.3],[-54.5,49.6],[-53.5,49.2],[-53.',
        '8,48.5],[-53.1,48.7],[-53,48.2],[-52.6,47.5],[-53.1,46.7],[-53.5,46.6],[-54.2,46.8],[-54,47.6],[-54.2,47.8],[-55.4,46.9]',
        ',[-56,46.9],[-55.3,47.4],[-56.3,47.6],[-57.3,47.6],[-59.3,47.6],[-59.4,47.9],[-58.8,48.3],[-59.2,48.5],[-58.4,49.1],[-57',
        '.4,50.7],[-56.7,51.3],[-55.9,51.6],[-55.4,51.6]]],[[[-83.9,65.1],[-82.8,64.8],[-81.6,64.5],[-81.6,64],[-80.8,64.1],[-80.',
        '1,63.7],[-81,63.4],[-82.5,63.7],[-83.1,64.1],[-84.1,63.6],[-85.5,63.1],[-85.9,63.6],[-87.2,63.5],[-86.4,64],[-86.2,64.8]',
        ',[-85.9,65.7],[-85.2,65.7],[-85,65.2],[-84.5,65.4]]],[[[-78.8,72.4],[-77.8,72.7],[-75.6,72.2],[-74.2,71.8],[-74.1,71.3],',
        '[-72.2,71.6],[-71.2,70.9],[-68.8,70.5],[-67.9,70.1],[-67,69.2],[-68.8,68.7],[-66.4,68.1],[-64.9,67.8],[-63.4,66.9],[-61.',
        '9,66.9],[-62.2,66.2],[-63.9,65],[-65.1,65.4],[-66.7,66.4],[-68,66.3],[-68.1,65.7],[-67.1,65.1],[-65.7,64.6],[-65.3,64.4]',
        ',[-64.7,63.4],[-65,62.7],[-66.3,62.9],[-68.8,63.7],[-67.4,62.9],[-66.3,62.3],[-66.2,61.9],[-68.9,62.3],[-71,62.9],[-72.2',
        ',63.4],[-71.9,63.7],[-73.4,64.2],[-74.8,64.7],[-74.8,64.4],[-77.7,64.2],[-78.6,64.6],[-77.9,65.3],[-76,65.3],[-74,65.5],',
        '[-74.3,65.8],[-73.9,66.3],[-72.7,67.3],[-72.9,67.7],[-73.3,68.1],[-74.8,68.6],[-76.9,68.9],[-76.2,69.1],[-77.3,69.8],[-7',
        '8.2,69.8],[-79,70.2],[-79.5,69.9],[-81.3,69.7],[-84.9,70],[-87.1,70.3],[-88.7,70.4],[-89.5,70.8],[-88.5,71.2],[-89.9,71.',
        '2],[-90.2,72.2],[-89.4,73.1],[-88.4,73.5],[-85.8,73.8],[-86.6,73.2],[-85.8,72.5],[-84.9,73.3],[-82.3,73.8],[-80.6,72.7],',
        '[-80.7,72.1]]],[[[-94.5,74.1],[-92.4,74.1],[-90.5,73.9],[-92,73],[-93.2,72.8],[-94.3,72],[-95.4,72.1],[-96,72.9],[-96,73',
        '.4],[-95.5,73.9]]],[[[-122.9,76.1],[-121.2,76.9],[-119.1,77.5],[-117.6,77.5],[-116.2,77.6],[-116.3,76.9],[-117.1,76.5],[',
        '-118,76.5],[-119.9,76.1],[-121.5,75.9]]],[[[-132.7,54],[-131.7,54.1],[-132,53],[-131.2,52.2],[-131.6,52.2],[-132.2,52.6]',
        ',[-132.5,53.1],[-133.1,53.4],[-133.2,53.9],[-133.2,54.2]]],[[[-105.5,79.3],[-103.5,79.2],[-100.8,78.8],[-100.1,78.3],[-9',
        '9.7,77.9],[-101.3,78],[-102.9,78.3],[-105.2,78.4],[-104.2,78.7],[-105.4,78.9]]],[[[-123.5,48.5],[-124,48.4],[-125.7,48.8',
        '],[-126,49.2],[-126.9,49.5],[-127,49.8],[-128.1,50],[-128.4,50.5],[-128.4,50.8],[-127.3,50.6],[-126.7,50.4],[-125.8,50.3',
        '],[-125.4,50],[-124.9,49.5],[-123.9,49.1]]],[[[-121.5,74.4],[-120.1,74.2],[-117.6,74.2],[-116.6,73.9],[-115.5,73.5],[-11',
        '6.8,73.2],[-119.2,72.5],[-120.5,71.8],[-120.5,71.4],[-123.1,70.9],[-123.6,71.3],[-125.9,71.9],[-125.5,72.3],[-124.8,73],',
        '[-123.9,73.7],[-124.9,74.3]]],[[[-107.8,75.8],[-106.9,76],[-105.9,76],[-105.7,75.5],[-106.3,75],[-109.7,74.9],[-112.2,74',
        '.4],[-113.7,74.4],[-113.9,74.7],[-111.8,75.2],[-116.3,75],[-117.7,75.2],[-116.3,76.2],[-115.4,76.5],[-112.6,76.1],[-110.',
        '8,75.5],[-109.1,75.5],[-110.5,76.4],[-109.6,76.8],[-108.5,76.7],[-108.2,76.2]]],[[[-106.5,73.1],[-105.4,72.7],[-104.8,71',
        '.7],[-104.5,71],[-102.8,70.5],[-101,70],[-101.1,69.6],[-102.7,69.5],[-102.1,69.1],[-102.4,68.8],[-104.2,68.9],[-106,69.2',
        '],[-107.1,69.1],[-109,68.8],[-111.5,68.6],[-113.3,68.5],[-113.9,69],[-115.2,69.3],[-116.1,69.2],[-117.3,70],[-116.7,70.1',
        '],[-115.1,70.2],[-113.7,70.2],[-112.4,70.4],[-114.3,70.6],[-116.5,70.5],[-117.9,70.5],[-118.4,70.9],[-116.1,71.3],[-117.',
        '7,71.3],[-119.4,71.6],[-118.6,72.3],[-117.9,72.7],[-115.2,73.3],[-114.2,73.1],[-114.7,72.7],[-112.4,73],[-111.1,72.5],[-',
        '109.9,73],[-109,72.6],[-108.2,71.7],[-107.7,72.1],[-108.4,73.1],[-107.5,73.2]]],[[[-100.4,72.7],[-101.5,73.4],[-100.4,73',
        '.8],[-99.2,73.6],[-97.4,73.8],[-97.1,73.5],[-98.1,73],[-96.5,72.6],[-96.7,71.7],[-98.4,71.3],[-99.3,71.4],[-100,71.7],[-',
        '102.5,72.5],[-102.5,72.8]]],[[[-106.6,73.6],[-105.3,73.6],[-104.5,73.4],[-105.4,72.8],[-106.9,73.5]]],[[[-98.5,76.7],[-9',
        '7.7,76.3],[-97.7,75.7],[-98.2,75],[-99.8,74.9],[-100.9,75.1],[-100.9,75.6],[-102.5,75.6],[-102.6,76.3],[-101.5,76.3],[-1',
        '00,76.6],[-98.6,76.6]]],[[[-96,80.6],[-95.3,80.9],[-94.3,81],[-94.7,81.2],[-92.4,81.3],[-91.1,80.7],[-89.4,80.5],[-87.8,',
        '80.3],[-87,79.7],[-85.8,79.3],[-87.2,79],[-89,78.3],[-90.8,78.2],[-92.9,78.3],[-94,78.8],[-93.9,79.1],[-93.1,79.4],[-95,',
        '79.4],[-96.1,79.7],[-96.7,80.2]]],[[[-91.6,81.9],[-90.1,82.1],[-88.9,82.1],[-87,82.3],[-85.5,82.7],[-84.3,82.6],[-83.2,8',
        '2.3],[-82.4,82.9],[-81.1,83],[-79.3,83.1],[-76.2,83.2],[-75.7,83.1],[-72.8,83.2],[-70.7,83.2],[-68.5,83.1],[-65.8,83],[-',
        '63.7,82.9],[-61.8,82.6],[-61.9,82.4],[-64.3,81.9],[-66.8,81.7],[-67.7,81.5],[-65.5,81.5],[-67.8,80.9],[-69.5,80.6],[-71.',
        '2,79.8],[-73.2,79.6],[-73.9,79.4],[-76.9,79.3],[-75.5,79.2],[-76.2,79],[-75.4,78.5],[-76.3,78.2],[-77.9,77.9],[-78.4,77.',
        '5],[-79.8,77.2],[-79.6,77],[-77.9,77],[-77.9,76.8],[-80.6,76.2],[-83.2,76.5],[-86.1,76.3],[-87.6,76.4],[-89.5,76.5],[-89',
        '.6,77],[-87.8,77.2],[-88.3,77.9],[-87.6,78],[-85,77.5],[-86.3,78.2],[-88,78.4],[-87.2,78.8],[-85.4,79],[-85.1,79.3],[-86',
        '.5,79.7],[-86.9,80.3],[-84.2,80.2],[-83.4,80.1],[-81.8,80.5],[-84.1,80.6],[-87.6,80.5],[-89.4,80.9],[-90.2,81.3],[-91.4,',
        '81.6]]],[[[-75.2,67.4],[-75.9,67.1],[-77,67.1],[-77.2,67.6],[-76.8,68.1],[-75.9,68.3],[-75.1,68],[-75.1,67.6]]],[[[-96.3',
        ',69.5],[-95.6,69.1],[-96.3,68.8],[-97.6,69.1],[-98.4,69],[-99.8,69.4],[-98.9,69.7],[-98.2,70.1],[-97.2,69.9],[-96.6,69.7',
        ']]],[[[-64.5,49.9],[-64.2,50],[-62.9,49.7],[-61.8,49.3],[-61.8,49.1],[-62.3,49.1],[-63.6,49.4]]],[[[-64,47],[-63.7,46.6]',
        ',[-62.9,46.4],[-62,46.4],[-62.5,46],[-62.9,46],[-64.1,46.4],[-64.4,46.7]]]]],["USA","US","USA","840","United States of A',
        'merica",-97.5,39.5,["USA","US","840","US1","United States of America","United States"],[[[[-122.8,49],[-120,49],[-117,49',
        '],[-116,49],[-113,49],[-110,49],[-107,49],[-104,49],[-100.6,49],[-97.2,49],[-95.2,49],[-95.2,49.4],[-94.8,49.4],[-94.6,4',
        '8.8],[-94.3,48.7],[-93.6,48.6],[-92.6,48.5],[-91.6,48.1],[-90.8,48.3],[-89.6,48],[-89.3,48],[-88.4,48.3],[-87.4,47.9],[-',
        '86.5,47.6],[-85.7,47.2],[-84.9,46.9],[-84.8,46.6],[-84.5,46.5],[-84.6,46.4],[-84.3,46.4],[-84.1,46.5],[-84.1,46.3],[-83.',
        '9,46.1],[-83.6,46.1],[-83.5,46],[-83.6,45.8],[-82.6,45.3],[-82.3,44.4],[-82.1,43.6],[-82.4,43],[-82.9,42.4],[-83.1,42.1]',
        ',[-83.1,42],[-83,41.8],[-82.7,41.7],[-82.4,41.7],[-81.3,42.2],[-80.2,42.4],[-78.9,42.9],[-78.9,43],[-79,43.3],[-79.2,43.',
        '5],[-78.7,43.6],[-77.7,43.6],[-76.8,43.6],[-76.5,44],[-76.4,44.1],[-75.3,44.8],[-74.9,45],[-73.3,45],[-71.5,45],[-71.4,4',
        '5.3],[-71.1,45.3],[-70.7,45.5],[-70.3,45.9],[-70,46.7],[-69.2,47.4],[-68.9,47.2],[-68.2,47.4],[-67.8,47.1],[-67.8,45.7],',
        '[-67.1,45.1],[-67,44.8],[-68,44.3],[-69.1,44],[-70.1,43.7],[-70.6,43.1],[-70.8,42.9],[-70.8,42.3],[-70.5,41.8],[-70.1,41',
        '.8],[-70.2,42.1],[-69.9,41.9],[-70,41.6],[-70.6,41.5],[-71.1,41.5],[-71.9,41.3],[-72.3,41.3],[-72.9,41.2],[-73.7,40.9],[',
        '-72.2,41.1],[-71.9,40.9],[-73.3,40.6],[-74,40.6],[-74,40.8],[-74.3,40.5],[-74,40.4],[-74.2,39.7],[-74.9,38.9],[-75,39.2]',
        ',[-75.2,39.2],[-75.5,39.5],[-75.3,39],[-75.1,38.8],[-75.1,38.4],[-75.4,38],[-75.9,37.2],[-76,37.3],[-75.7,37.9],[-76.2,3',
        '8.3],[-76.3,39.2],[-76.5,38.7],[-76.3,38.1],[-77,38.2],[-76.3,37.9],[-76.3,37],[-76,36.9],[-75.9,36.6],[-75.7,35.6],[-76',
        '.4,34.8],[-77.4,34.5],[-78.1,33.9],[-78.6,33.9],[-79.1,33.5],[-79.2,33.2],[-80.3,32.5],[-80.9,32],[-81.3,31.4],[-81.5,30',
        '.7],[-81.3,30],[-81,29.2],[-80.5,28.5],[-80.5,28],[-80.1,26.9],[-80.1,26.2],[-80.1,25.8],[-80.4,25.2],[-80.7,25.1],[-81.',
        '2,25.2],[-81.3,25.6],[-81.7,25.9],[-82.2,26.7],[-82.7,27.5],[-82.9,27.9],[-82.6,28.6],[-82.9,29.1],[-83.7,29.9],[-84.1,3',
        '0.1],[-85.1,29.6],[-85.3,29.7],[-85.8,30.2],[-86.4,30.4],[-87.5,30.3],[-88.4,30.4],[-89.2,30.3],[-89.6,30.2],[-89.4,29.9',
        '],[-89.4,29.5],[-89.2,29.3],[-89.4,29.2],[-89.8,29.3],[-90.2,29.1],[-90.9,29.1],[-91.6,29.7],[-92.5,29.6],[-93.2,29.8],[',
        '-93.8,29.7],[-94.7,29.5],[-95.6,28.7],[-96.6,28.3],[-97.1,27.8],[-97.4,27.4],[-97.4,26.7],[-97.3,26.2],[-97.1,25.9],[-97',
        '.5,25.8],[-98.2,26.1],[-99,26.4],[-99.3,26.8],[-99.5,27.5],[-100.1,28.1],[-100.5,28.7],[-101,29.4],[-101.7,29.8],[-102.5',
        ',29.8],[-103.1,29],[-103.9,29.3],[-104.5,29.6],[-104.7,30.1],[-105,30.6],[-105.6,31.1],[-106.1,31.4],[-106.5,31.8],[-108',
        '.2,31.8],[-108.2,31.3],[-109,31.3],[-111,31.3],[-113.3,32],[-114.8,32.5],[-114.7,32.7],[-116,32.6],[-117.1,32.5],[-117.3',
        ',33],[-117.9,33.6],[-118.4,33.7],[-118.5,34],[-119.1,34.1],[-119.4,34.3],[-120.4,34.4],[-120.6,34.6],[-120.7,35.2],[-121',
        '.7,36.2],[-122.5,37.6],[-122.5,37.8],[-123,38.1],[-123.7,39],[-123.9,39.8],[-124.4,40.3],[-124.2,41.1],[-124.2,42],[-124',
        '.5,42.8],[-124.1,43.7],[-124,44.6],[-123.9,45.5],[-124.1,46.9],[-124.4,47.7],[-124.7,48.2],[-124.6,48.4],[-123.1,48],[-1',
        '22.6,47.1],[-122.3,47.4],[-122.5,48.2]]],[[[-155.4,20.1],[-155.2,20],[-155.1,19.9],[-154.8,19.5],[-155.2,19.2],[-155.5,1',
        '9.1],[-155.7,18.9],[-155.9,19.1],[-155.9,19.3],[-156.1,19.7],[-156,19.8],[-155.9,20],[-155.9,20.2],[-155.9,20.3],[-155.8',
        ',20.2]]],[[[-156,20.8],[-156.1,20.6],[-156.4,20.6],[-156.6,20.8],[-156.7,20.9],[-156.6,21],[-156.3,20.9]]],[[[-156.8,21.',
        '2],[-156.8,21.1],[-157.3,21.1],[-157.3,21.2]]],[[[-158,21.7],[-157.9,21.7],[-157.7,21.3],[-157.8,21.3],[-158.1,21.3],[-1',
        '58.3,21.5],[-158.3,21.6]]],[[[-159.4,22.2],[-159.3,22],[-159.5,21.9],[-159.8,22.1],[-159.7,22.1],[-159.6,22.2]]],[[[-166',
        '.5,60.4],[-165.7,60.3],[-165.6,59.9],[-166.2,59.8],[-166.8,59.9],[-167.5,60.2]]],[[[-153.2,58],[-152.6,57.9],[-152.1,57.',
        '6],[-153,57.1],[-154,56.7],[-154.5,57],[-154.7,57.5],[-153.8,57.8]]],[[[-141,69.7],[-141,66],[-141,60.3],[-140,60.3],[-1',
        '39,60],[-138.3,59.6],[-137.5,58.9],[-136.5,59.5],[-135.5,59.8],[-134.9,59.3],[-134.3,58.9],[-133.4,58.4],[-132.7,57.7],[',
        '-131.7,56.6],[-130,55.9],[-130,55.3],[-130.5,54.8],[-131.1,55.2],[-132,55.5],[-132.3,56.4],[-133.5,57.2],[-134.1,58.1],[',
        '-135,58.2],[-136.6,58.2],[-137.8,58.5],[-139.9,59.5],[-140.8,59.7],[-142.6,60.1],[-144,60],[-145.9,60.5],[-147.1,60.9],[',
        '-148.2,60.7],[-148,60],[-148.6,59.9],[-149.7,59.7],[-150.6,59.4],[-151.7,59.2],[-151.9,59.7],[-151.4,60.7],[-150.3,61],[',
        '-150.6,61.3],[-151.9,60.7],[-152.6,60.1],[-154,59.4],[-153.3,58.9],[-154.2,58.1],[-155.3,57.7],[-156.3,57.4],[-156.6,57]',
        ',[-158.1,56.5],[-158.4,56],[-159.6,55.6],[-160.3,55.6],[-161.2,55.4],[-162.2,55],[-163.1,54.7],[-164.8,54.4],[-164.9,54.',
        '6],[-163.8,55],[-162.9,55.3],[-161.8,55.9],[-160.6,56],[-160.1,56.4],[-158.7,57],[-158.5,57.2],[-157.7,57.6],[-157.6,58.',
        '3],[-157,58.9],[-158.2,58.6],[-158.5,58.8],[-159.1,58.4],[-159.7,58.9],[-160,58.6],[-160.4,59.1],[-161.4,58.7],[-162,58.',
        '7],[-162.1,59.3],[-161.9,59.6],[-162.5,60],[-163.8,59.8],[-164.7,60.3],[-165.3,60.5],[-165.4,61.1],[-166.1,61.5],[-165.7',
        ',62.1],[-164.9,62.6],[-164.6,63.1],[-163.8,63.2],[-163.1,63.1],[-162.3,63.5],[-161.5,63.5],[-160.8,63.8],[-161,64.2],[-1',
        '61.5,64.4],[-160.8,64.8],[-161.4,64.8],[-162.5,64.6],[-162.8,64.3],[-163.5,64.6],[-165,64.4],[-166.4,64.7],[-166.8,65.1]',
        ',[-168.1,65.7],[-166.7,66.1],[-164.5,66.6],[-163.7,66.6],[-163.8,66.1],[-161.7,66.1],[-162.5,66.7],[-163.7,67.1],[-164.4',
        ',67.6],[-165.4,68],[-166.8,68.4],[-166.2,68.9],[-164.4,68.9],[-163.2,69.4],[-162.9,69.9],[-161.9,70.3],[-160.9,70.4],[-1',
        '59,70.9],[-158.1,70.8],[-156.6,71.4],[-155.1,71.1],[-154.3,70.7],[-153.9,70.9],[-152.2,70.8],[-152.3,70.6],[-150.7,70.4]',
        ',[-149.7,70.5],[-147.6,70.2],[-145.7,70.1],[-144.9,70],[-143.6,70.2],[-142.1,69.9]]],[[[-171.7,63.8],[-171.1,63.6],[-170',
        '.5,63.7],[-169.7,63.4],[-168.7,63.3],[-168.8,63.2],[-169.5,63],[-170.3,63.2],[-170.7,63.4],[-171.6,63.3],[-171.8,63.4]]]',
        ']],["KAZ","KZ","KAZ","398","Kazakhstan",68.7,49.1,["KAZ","KZ","398","KA1","Kazakhstan"],[[[[87.4,49.2],[86.6,48.5],[85.8',
        ',48.5],[85.7,47.5],[85.2,47],[83.2,47.3],[82.5,45.5],[81.9,45.3],[80,44.9],[80.9,43.2],[80.2,42.9],[80.3,42.3],[79.6,42.',
        '5],[79.1,42.9],[77.7,43],[76,43],[75.6,42.9],[74.2,43.3],[73.6,43.1],[73.5,42.5],[71.8,42.8],[71.2,42.7],[71,42.3],[70.4',
        ',42.1],[69.1,41.4],[68.6,40.7],[68.3,40.7],[68,41.1],[66.7,41.2],[66.5,42],[66,42],[66.1,43],[64.9,43.7],[63.2,43.7],[62',
        ',43.5],[61.1,44.4],[60.2,44.8],[58.7,45.5],[58.5,45.6],[55.9,45],[56,41.3],[55.5,41.3],[54.8,42],[54.1,42.3],[52.9,42.1]',
        ',[52.5,41.8],[52.4,42],[52.7,42.4],[52.5,42.8],[51.3,43.1],[50.9,44],[50.3,44.3],[50.3,44.6],[51.3,44.5],[51.3,45.2],[52',
        '.2,45.4],[53,45.3],[53.2,46.2],[53,46.9],[52,46.8],[51.2,47],[50,46.6],[49.1,46.4],[48.6,46.6],[48.7,47.1],[48.1,47.7],[',
        '47.3,47.7],[46.5,48.4],[47,49.2],[46.8,49.4],[47.5,50.5],[48.6,49.9],[48.7,50.6],[50.8,51.7],[52.3,51.7],[54.5,51],[55.7',
        ',50.6],[56.8,51],[58.4,51.1],[59.6,50.5],[59.9,50.8],[61.3,50.8],[61.6,51.3],[60,52],[60.9,52.4],[60.7,52.7],[61.7,53],[',
        '61,53.7],[61.4,54],[65.2,54.4],[65.7,54.6],[68.2,55],[69.1,55.4],[70.9,55.2],[71.2,54.1],[72.2,54.4],[73.5,54],[73.4,53.',
        '5],[74.4,53.5],[76.9,54.5],[76.5,54.2],[77.8,53.4],[80,50.9],[80.6,51.4],[81.9,50.8],[83.4,51.1],[83.9,50.9],[84.4,50.3]',
        ',[85.1,50.1],[85.5,49.7],[86.8,49.8]]]]],["UZB","UZ","UZB","860","Uzbekistan",64,41.7,["UZB","UZ","860","Uzbekistan"],[[',
        '[[56,41.3],[55.9,45],[58.5,45.6],[58.7,45.5],[60.2,44.8],[61.1,44.4],[62,43.5],[63.2,43.7],[64.9,43.7],[66.1,43],[66,42]',
        ',[66.5,42],[66.7,41.2],[68,41.1],[68.3,40.7],[68.6,40.7],[69.1,41.4],[70.4,42.1],[71,42.3],[71.3,42.2],[70.4,41.5],[71.2',
        ',41.1],[71.9,41.4],[73.1,40.9],[71.8,40.1],[71,40.2],[70.6,40.2],[70.5,40.5],[70.7,41],[69.3,40.7],[69,40.1],[68.5,39.5]',
        ',[67.7,39.6],[67.4,39.1],[68.2,38.9],[68.4,38.2],[67.8,37.1],[67.1,37.4],[66.5,37.4],[66.5,38],[65.2,38.4],[64.2,38.9],[',
        '63.5,39.4],[62.4,40.1],[61.9,41.1],[61.5,41.3],[60.5,41.2],[60.1,41.4],[60,42.2],[58.6,42.8],[57.8,42.2],[56.9,41.8],[57',
        '.1,41.3]]]]],["PNG","PG","PNG","598","Papua New Guinea",143.9,-5.7,["PNG","PG","598","Papua New Guinea","PN1"],[[[[141,-',
        '2.6],[142.7,-3.3],[144.6,-3.9],[145.3,-4.4],[145.8,-4.9],[146,-5.5],[147.6,-6.1],[147.9,-6.6],[147,-6.7],[147.2,-7.4],[1',
        '48.1,-8],[148.7,-9.1],[149.3,-9.1],[149.3,-9.5],[150,-9.7],[149.7,-9.9],[150.8,-10.3],[150.7,-10.6],[150,-10.7],[149.8,-',
        '10.4],[148.9,-10.3],[147.9,-10.1],[147.1,-9.5],[146.6,-8.9],[146,-8.1],[144.7,-7.6],[143.9,-7.9],[143.3,-8.2],[143.4,-9]',
        ',[142.6,-9.3],[142.1,-9.2],[141,-9.1],[141,-5.9]]],[[[152.6,-3.7],[153,-4],[153.1,-4.5],[152.8,-4.8],[152.6,-4.2],[152.4',
        ',-3.8],[152,-3.5],[151.4,-3],[150.7,-2.7],[150.9,-2.5],[151.5,-2.8],[151.8,-3],[152.2,-3.2]]],[[[151.3,-5.8],[150.8,-6.1',
        '],[150.2,-6.3],[149.7,-6.3],[148.9,-6],[148.3,-5.7],[148.4,-5.4],[149.3,-5.6],[149.8,-5.5],[150,-5],[150.1,-5],[150.2,-5',
        '.5],[150.8,-5.5],[151.1,-5.1],[151.6,-4.8],[151.5,-4.2],[152.1,-4.1],[152.3,-4.3],[152.3,-4.9],[152,-5.5],[151.5,-5.6]]]',
        ',[[[154.8,-5.3],[155.1,-5.6],[155.5,-6.2],[156,-6.5],[155.9,-6.8],[155.6,-6.9],[155.2,-6.5],[154.7,-5.9],[154.5,-5.1],[1',
        '54.7,-5]]]]],["IDN","ID","IDN","360","Indonesia",101.9,-1,["IDN","ID","360","INDO","Indonesia"],[[[[141,-2.6],[141,-5.9]',
        ',[141,-9.1],[140.1,-8.3],[139.1,-8.1],[138.9,-8.4],[137.6,-8.4],[138,-7.6],[138.7,-7.3],[138.4,-6.2],[137.9,-5.4],[136,-',
        '4.5],[135.2,-4.5],[133.7,-3.5],[133.4,-4],[133,-4.1],[132.8,-3.7],[132.8,-3.3],[132,-2.8],[133.1,-2.5],[133.8,-2.5],[133',
        '.7,-2.2],[132.2,-2.2],[131.8,-1.6],[130.9,-1.4],[130.5,-0.9],[131.9,-0.7],[132.4,-0.4],[134,-0.8],[134.1,-1.2],[134.4,-2',
        '.8],[135.5,-3.4],[136.3,-2.3],[137.4,-1.7],[138.3,-1.7],[139.2,-2.1],[139.9,-2.4]]],[[[125,-8.9],[125.1,-9.1],[125.1,-9.',
        '4],[124.4,-10.1],[123.6,-10.4],[123.5,-10.2],[123.6,-9.9],[124,-9.3]]],[[[134.2,-6.9],[134.1,-6.1],[134.3,-5.8],[134.5,-',
        '5.4],[134.7,-5.7],[134.7,-6.2]]],[[[117.9,4.1],[117.3,3.2],[118,2.3],[117.9,1.8],[119,0.9],[117.8,0.8],[117.5,0.1],[117.',
        '5,-0.8],[116.6,-1.5],[116.5,-2.5],[116.1,-4],[116,-3.7],[114.9,-4.1],[114.5,-3.5],[113.8,-3.4],[113.3,-3.1],[112.1,-3.5]',
        ',[111.7,-3],[111,-3],[110.2,-2.9],[110.1,-1.6],[109.6,-1.3],[109.1,-0.5],[109,0.4],[109.1,1.3],[109.7,2],[109.8,1.3],[11',
        '0.5,0.8],[111.2,1],[111.8,0.9],[112.4,1.4],[112.9,1.5],[113.8,1.2],[114.6,1.4],[115.1,2.8],[115.5,3.2],[115.9,4.3],[117,',
        '4.3]]],[[[129.4,-2.8],[130.5,-3.1],[130.8,-3.9],[130,-3.4],[129.2,-3.4],[128.6,-3.4],[127.9,-3.4],[128.1,-2.8]]],[[[126.',
        '9,-3.8],[126.2,-3.6],[126,-3.2],[127,-3.1],[127.2,-3.5]]],[[[127.9,2.2],[128,1.6],[128.6,1.5],[128.7,1.1],[128.6,0.3],[1',
        '28.1,0.4],[128,-0.3],[128.4,-0.8],[128.1,-0.9],[127.7,-0.3],[127.4,1],[127.6,1.8]]],[[[122.9,0.9],[124.1,0.9],[125.1,1.6',
        '],[125.2,1.4],[124.4,0.4],[123.7,0.2],[122.7,0.4],[121.1,0.4],[120.2,0.2],[120,-0.5],[120.9,-1.4],[121.5,-1],[123.3,-0.6',
        '],[123.3,-1.1],[122.8,-0.9],[122.4,-1.5],[121.5,-1.9],[122.5,-3.2],[122.3,-3.5],[123.2,-4.7],[123.2,-5.3],[122.6,-5.6],[',
        '122.2,-5.3],[122.7,-4.5],[121.7,-4.9],[121.5,-4.6],[121.6,-4.2],[120.9,-3.6],[121,-2.6],[120.3,-2.9],[120.4,-4.1],[120.4',
        ',-5.5],[119.8,-5.7],[119.4,-5.4],[119.7,-4.5],[119.5,-3.5],[119.1,-3.5],[118.8,-2.8],[119.2,-2.1],[119.3,-1.4],[119.8,0.',
        '2],[120,0.6],[120.9,1.3],[121.7,1]]],[[[120.3,-10.3],[119,-9.6],[119.9,-9.4],[120.4,-9.7],[120.8,-10],[120.7,-10.2]]],[[',
        '[121.3,-8.5],[122,-8.5],[122.9,-8.1],[122.8,-8.6],[121.3,-8.9],[119.9,-8.8],[119.9,-8.4],[120.7,-8.2]]],[[[118.3,-8.4],[',
        '118.9,-8.3],[119.1,-8.7],[118,-8.9],[117.3,-9],[116.7,-9],[117.1,-8.5],[117.6,-8.4],[117.9,-8.1]]],[[[108.5,-6.4],[108.6',
        ',-6.8],[110.5,-6.9],[110.8,-6.5],[112.6,-6.9],[113,-7.6],[114.5,-7.8],[115.7,-8.4],[114.6,-8.8],[113.5,-8.3],[112.6,-8.4',
        '],[111.5,-8.3],[110.6,-8.1],[109.4,-7.7],[108.7,-7.6],[108.3,-7.8],[106.5,-7.4],[106.3,-6.9],[105.4,-6.9],[106.1,-5.9],[',
        '107.3,-6],[108.1,-6.3]]],[[[104.4,-1.1],[104.5,-1.8],[104.9,-2.3],[105.6,-2.4],[106.1,-3.1],[105.9,-4.3],[105.8,-5.9],[1',
        '04.7,-5.9],[103.9,-5],[102.6,-4.2],[102.2,-3.6],[101.4,-2.8],[100.9,-2.1],[100.1,-0.7],[99.3,0.2],[99,1],[98.6,1.8],[97.',
        '7,2.5],[97.2,3.3],[96.4,3.9],[95.4,5],[95.3,5.5],[95.9,5.4],[97.5,5.2],[98.4,4.3],[99.1,3.6],[99.7,3.2],[100.6,2.1],[101',
        '.7,2.1],[102.5,1.4],[103.1,0.6],[103.8,0.1],[103.4,-0.7],[104,-1.1]]]]],["ARG","AR","ARG","032","Argentina",-64.2,-33.5,',
        '["ARG","AR","032","Argentina"],[[[[-68.6,-52.6],[-68.2,-53.1],[-67.7,-53.8],[-66.4,-54.4],[-65,-54.7],[-65.5,-55.2],[-66',
        '.4,-55.2],[-67,-54.9],[-67.6,-54.9],[-68.6,-54.9]]],[[[-57.6,-30.2],[-57.9,-31],[-58.1,-32],[-58.1,-33],[-58.3,-33.3],[-',
        '58.4,-33.9],[-58.5,-34.4],[-57.2,-35.3],[-57.4,-36],[-56.7,-36.4],[-56.8,-36.9],[-57.7,-38.2],[-59.2,-38.7],[-61.2,-38.9',
        '],[-62.3,-38.8],[-62.1,-39.4],[-62.3,-40.2],[-62.1,-40.7],[-62.7,-41],[-63.8,-41.2],[-64.7,-40.8],[-65.1,-41.1],[-65,-42',
        '.1],[-64.3,-42.4],[-63.8,-42],[-63.5,-42.6],[-64.4,-42.9],[-65.2,-43.5],[-65.3,-44.5],[-65.6,-45],[-66.5,-45],[-67.3,-45',
        '.6],[-67.6,-46.3],[-66.6,-47],[-65.6,-47.2],[-66,-48.1],[-67.2,-48.7],[-67.8,-49.9],[-68.7,-50.3],[-69.1,-50.7],[-68.8,-',
        '51.8],[-68.1,-52.3],[-68.6,-52.3],[-69.5,-52.1],[-71.9,-52],[-72.3,-51.4],[-72.3,-50.7],[-73,-50.7],[-73.3,-50.4],[-73.4',
        ',-49.3],[-72.6,-48.9],[-72.3,-48.2],[-72.4,-47.7],[-71.9,-46.9],[-71.6,-45.6],[-71.7,-45],[-71.2,-44.8],[-71.3,-44.4],[-',
        '71.8,-44.2],[-71.5,-43.8],[-71.9,-43.4],[-72.1,-42.3],[-71.7,-42.1],[-71.9,-40.8],[-71.7,-39.8],[-71.4,-38.9],[-70.8,-38',
        '.6],[-71.1,-37.6],[-71.1,-36.7],[-70.4,-36],[-70.4,-35.2],[-69.8,-34.2],[-69.8,-33.3],[-70.1,-33.1],[-70.5,-31.4],[-69.9',
        ',-30.3],[-70,-29.4],[-69.7,-28.5],[-69,-27.5],[-68.3,-26.9],[-68.6,-26.5],[-68.4,-26.2],[-68.4,-24.5],[-67.3,-24],[-67,-',
        '23],[-67.1,-22.7],[-66.3,-21.8],[-65,-22.1],[-64.4,-22.8],[-64,-22],[-62.8,-22],[-62.7,-22.2],[-60.8,-23.9],[-60,-24],[-',
        '58.8,-24.8],[-57.8,-25.2],[-57.6,-25.6],[-58.6,-27.1],[-57.6,-27.4],[-56.5,-27.5],[-55.7,-27.4],[-54.8,-26.6],[-54.6,-25',
        '.7],[-54.1,-25.5],[-53.6,-26.1],[-53.6,-26.9],[-54.5,-27.5],[-55.2,-27.9],[-56.3,-28.9]]]]],["CHL","CL","CHL","152","Chi',
        'le",-72.3,-38.2,["CHL","CL","152","Chile"],[[[[-68.6,-52.6],[-68.6,-54.9],[-67.6,-54.9],[-67,-54.9],[-67.3,-55.3],[-68.1',
        ',-55.6],[-68.6,-55.6],[-69.2,-55.5],[-70,-55.2],[-71,-55.1],[-72.3,-54.5],[-73.3,-54],[-74.7,-52.8],[-73.8,-53],[-72.4,-',
        '53.7],[-71.1,-54.1],[-70.6,-53.6],[-70.3,-52.9],[-69.3,-52.5]]],[[[-69.6,-17.6],[-69.1,-18.3],[-69,-19],[-68.4,-19.4],[-',
        '68.8,-20.4],[-68.2,-21.5],[-67.8,-22.9],[-67.1,-22.7],[-67,-23],[-67.3,-24],[-68.4,-24.5],[-68.4,-26.2],[-68.6,-26.5],[-',
        '68.3,-26.9],[-69,-27.5],[-69.7,-28.5],[-70,-29.4],[-69.9,-30.3],[-70.5,-31.4],[-70.1,-33.1],[-69.8,-33.3],[-69.8,-34.2],',
        '[-70.4,-35.2],[-70.4,-36],[-71.1,-36.7],[-71.1,-37.6],[-70.8,-38.6],[-71.4,-38.9],[-71.7,-39.8],[-71.9,-40.8],[-71.7,-42',
        '.1],[-72.1,-42.3],[-71.9,-43.4],[-71.5,-43.8],[-71.8,-44.2],[-71.3,-44.4],[-71.2,-44.8],[-71.7,-45],[-71.6,-45.6],[-71.9',
        ',-46.9],[-72.4,-47.7],[-72.3,-48.2],[-72.6,-48.9],[-73.4,-49.3],[-73.3,-50.4],[-73,-50.7],[-72.3,-50.7],[-72.3,-51.4],[-',
        '71.9,-52],[-69.5,-52.1],[-68.6,-52.3],[-69.5,-52.3],[-69.9,-52.5],[-70.8,-52.9],[-71,-53.8],[-71.4,-53.9],[-72.6,-53.5],',
        '[-73.7,-52.8],[-74.9,-52.3],[-75.3,-51.6],[-75,-51],[-75.5,-50.4],[-75.6,-48.7],[-75.2,-47.7],[-74.1,-46.9],[-75.6,-46.6',
        '],[-74.7,-45.8],[-74.4,-44.1],[-73.2,-44.5],[-72.7,-42.4],[-73.4,-42.1],[-73.7,-43.4],[-74.3,-43.2],[-74,-41.8],[-73.7,-',
        '39.9],[-73.2,-39.3],[-73.5,-38.3],[-73.6,-37.2],[-73.2,-37.1],[-72.6,-35.5],[-71.9,-33.9],[-71.4,-32.4],[-71.7,-30.9],[-',
        '71.4,-30.1],[-71.5,-28.9],[-70.9,-27.6],[-70.7,-25.7],[-70.4,-23.6],[-70.1,-21.4],[-70.2,-19.8],[-70.4,-18.3],[-69.9,-18',
        '.1]]]]],["COD","CD","COD","180","Democratic Republic of the Congo",23.5,-1.9,["COD","CD","180","DRC","ZR","ZAR","Dem. Re',
        'p. Congo","Democratic Republic of the Congo"],[[[[29.3,-4.5],[29.5,-5.4],[29.4,-5.9],[29.6,-6.5],[30.2,-7.1],[30.7,-8.3]',
        ',[30.3,-8.2],[29,-8.4],[28.7,-8.5],[28.4,-9.2],[28.7,-9.6],[28.5,-10.8],[28.4,-11.8],[28.6,-12],[29.3,-12.4],[29.6,-12.2',
        '],[29.7,-13.3],[28.9,-13.2],[28.5,-12.7],[28.2,-12.3],[27.4,-12.1],[27.2,-11.6],[26.6,-11.9],[25.8,-11.8],[25.4,-11.3],[',
        '24.8,-11.2],[24.3,-11.3],[24.3,-11],[23.9,-10.9],[23.5,-10.9],[22.8,-11],[22.4,-11],[22.2,-11.1],[22.2,-9.9],[21.9,-9.5]',
        ',[21.8,-8.9],[21.9,-8.3],[21.7,-7.9],[21.7,-7.3],[20.5,-7.3],[20.6,-6.9],[20.1,-6.9],[20,-7.1],[19.4,-7.2],[19.2,-7.7],[',
        '19,-8],[18.5,-7.8],[18.1,-8],[17.5,-8.1],[17.1,-7.5],[16.9,-7.2],[16.6,-6.6],[16.3,-5.9],[13.4,-5.9],[13,-6],[12.7,-6],[',
        '12.3,-6.1],[12.2,-5.8],[12.4,-5.7],[12.5,-5.2],[12.6,-5],[13,-4.8],[13.3,-4.9],[13.6,-4.5],[14.1,-4.5],[14.2,-4.8],[14.6',
        ',-5],[15.2,-4.3],[15.8,-3.9],[16,-3.5],[16,-2.7],[16.4,-1.7],[16.9,-1.2],[17.5,-0.7],[17.6,-0.4],[17.7,-0.1],[17.8,0.3],',
        '[17.8,0.9],[17.9,1.7],[18.1,2.4],[18.4,2.9],[18.5,3.5],[18.5,4.2],[18.9,4.7],[19.5,5],[20.3,4.7],[20.9,4.3],[21.7,4.2],[',
        '22.4,4],[22.7,4.6],[22.8,4.7],[23.3,4.6],[24.4,5.1],[24.8,4.9],[25.1,4.9],[25.3,5.2],[25.7,5.3],[26.4,5.2],[27,5.1],[27.',
        '4,5.2],[28,4.4],[28.4,4.3],[28.7,4.5],[29.2,4.4],[29.7,4.6],[30,4.2],[30.8,3.5],[30.8,2.3],[31.2,2.2],[30.9,1.8],[30.5,1',
        '.6],[30.1,1.1],[29.9,0.6],[29.8,-0.2],[29.6,-0.6],[29.6,-1.3],[29.3,-1.6],[29.3,-2.2],[29.1,-2.3],[29,-2.8],[29.3,-3.3]]',
        ']]],["SOM","SO","SOM","706","Somalia",45.2,3.6,["SOM","SO","706","Somalia"],[[[[41.6,-1.7],[41,-0.9],[41,2.8],[41.9,3.9]',
        ',[42.1,4.2],[42.8,4.3],[43.7,5],[45,5],[47.8,8],[48.5,8.8],[48.9,9.5],[48.9,10],[48.9,11],[48.9,11.4],[49.3,11.4],[49.7,',
        '11.6],[50.3,11.7],[50.7,12],[51.1,12],[51.1,11.7],[51,11.2],[51,10.6],[50.8,10.3],[50.6,9.2],[50.1,8.1],[49.5,6.8],[48.6',
        ',5.3],[47.7,4.2],[46.6,2.9],[45.6,2],[44.1,1.1],[43.1,0.3],[42,-0.9],[41.8,-1.4]]]]],["KEN","KE","KEN","404","Kenya",37.',
        '9,0.5,["KEN","KE","404","Kenya"],[[[[39.2,-4.7],[37.8,-3.7],[37.7,-3.1],[34.1,-1.1],[33.9,-0.9],[33.9,0.1],[34.2,0.5],[3',
        '4.7,1.2],[35,1.9],[34.6,3.1],[34.5,3.6],[34,4.2],[34.6,4.8],[35.3,5.5],[35.8,5.3],[35.8,4.8],[36.2,4.4],[36.9,4.4],[38.1',
        ',3.6],[38.4,3.6],[38.7,3.6],[38.9,3.5],[39.6,3.4],[39.9,3.8],[40.8,4.3],[41.2,3.9],[41.9,3.9],[41,2.8],[41,-0.9],[41.6,-',
        '1.7],[40.9,-2.1],[40.6,-2.5],[40.3,-2.6],[40.1,-3.3],[39.8,-3.7],[39.6,-4.3]]]]],["SDN","SD","SDN","729","Sudan",29.3,16',
        '.3,["SDN","SD","729","Sudan"],[[[[24.6,8.2],[23.8,8.7],[23.5,9],[23.4,9.3],[23.6,9.7],[23.6,10.1],[23,10.7],[22.9,11.1],',
        '[22.9,11.4],[22.5,11.7],[22.5,12.3],[22.3,12.6],[21.9,12.6],[22,13],[22.3,13.4],[22.2,13.8],[22.5,14.1],[22.3,14.3],[22.',
        '6,14.9],[23,15.7],[23.9,15.6],[23.8,19.6],[23.9,20],[25,20],[25,22],[29,22],[32.9,22],[36.9,22],[37.2,21],[37,20.8],[37.',
        '1,19.8],[37.5,18.6],[37.9,18.4],[38.4,18],[37.9,17.4],[37.2,17.3],[36.9,17],[36.8,16.3],[36.3,14.8],[36.4,14.4],[36.3,13',
        '.6],[35.9,12.6],[35.3,12.1],[34.8,11.3],[34.7,10.9],[34.3,10.6],[34,9.6],[34,8.7],[34,9.5],[33.8,9.5],[33.8,10],[33.7,10',
        '.3],[33.2,10.7],[33.1,11.4],[33.2,12.2],[32.7,12.2],[32.7,12],[32.1,12],[32.3,11.7],[32.4,11.1],[31.9,10.5],[31.4,9.8],[',
        '30.8,9.7],[30,10.3],[29.6,10.1],[29.5,9.8],[29,9.6],[29,9.4],[28,9.4],[27.8,9.6],[27.1,9.6],[26.8,9.5],[26.5,9.6],[26,10',
        '.1],[25.8,10.4],[25.1,10.3],[24.8,9.8],[24.5,8.9],[24.2,8.7],[23.9,8.6]]]]],["TCD","TD","TCD","148","Chad",18.6,15.1,["T',
        'CD","TD","148","Chad"],[[[[23.8,19.6],[23.9,15.6],[23,15.7],[22.6,14.9],[22.3,14.3],[22.5,14.1],[22.2,13.8],[22.3,13.4],',
        '[22,13],[21.9,12.6],[22.3,12.6],[22.5,12.3],[22.5,11.7],[22.9,11.4],[22.9,11.1],[22.2,11],[21.7,10.6],[21,9.5],[20.1,9],',
        '[19.1,9.1],[18.8,9],[18.9,8.6],[18.4,8.3],[18,7.9],[16.7,7.5],[16.5,7.7],[16.3,7.8],[16.1,7.5],[15.3,7.4],[15.4,7.7],[15',
        '.1,8.4],[15,8.8],[14.5,9],[14,9.5],[14.2,10],[14.6,9.9],[14.9,10],[15.5,10],[14.9,10.9],[15,11.6],[14.9,12.2],[14.5,12.9',
        '],[14.6,13.3],[14,13.4],[14,14],[13.5,14.4],[14,15.7],[15.2,16.6],[15.3,17.9],[15.7,20],[15.9,20.4],[15.5,20.7],[15.5,21',
        '],[15.1,21.3],[14.9,22.9],[15.9,23.4],[19.8,21.5]]]]],["HTI","HT","HTI","332","Haiti",-72.2,19.3,["HTI","HT","332","Hait',
        'i"],[[[[-71.7,19.7],[-71.6,19.2],[-71.7,18.8],[-71.9,18.6],[-71.7,18.3],[-71.7,18],[-72.4,18.2],[-72.8,18.1],[-73.5,18.2',
        '],[-73.9,18],[-74.5,18.3],[-74.4,18.7],[-73.4,18.5],[-72.7,18.4],[-72.3,18.7],[-72.8,19.1],[-72.8,19.5],[-73.4,19.6],[-7',
        '3.2,19.9],[-72.6,19.9]]]]],["DOM","DO","DOM","214","Dominican Republic",-70.7,19.1,["DOM","DO","214","Dominican Rep.","D',
        'ominican Republic"],[[[[-71.7,18],[-71.7,18.3],[-71.9,18.6],[-71.7,18.8],[-71.6,19.2],[-71.7,19.7],[-71.6,19.9],[-70.8,1',
        '9.9],[-70.2,19.6],[-70,19.6],[-69.8,19.3],[-69.2,19.3],[-69.3,19],[-68.8,19],[-68.3,18.6],[-68.7,18.2],[-69.2,18.4],[-69',
        '.6,18.4],[-70,18.4],[-70.1,18.2],[-70.5,18.2],[-70.7,18.4],[-71,18.3],[-71.4,17.6],[-71.7,17.8]]]]],["RUS","RU","RUS","6',
        '43","Russia",44.7,58.2,["RUS","RU","643","Russia","Russian Federation"],[[[[178.7,71.1],[180,71.5],[180,70.8],[178.9,70.',
        '8]]],[[[49.1,46.4],[48.6,45.8],[47.7,45.6],[46.7,44.6],[47.6,43.7],[47.5,43],[48.6,41.8],[48,41.4],[47.8,41.2],[47.4,41.',
        '2],[46.7,41.8],[46.4,41.9],[45.8,42.1],[45.5,42.5],[44.5,42.7],[43.9,42.6],[43.8,42.7],[42.4,43.2],[40.9,43.4],[40.1,43.',
        '6],[40,43.4],[38.7,44.3],[37.5,44.7],[36.7,45.2],[37.4,45.4],[38.2,46.2],[37.7,46.6],[39.1,47],[39.1,47.3],[38.2,47.1],[',
        '38.3,47.5],[38.8,47.8],[39.7,47.9],[39.9,48.2],[39.7,48.8],[40.1,49.3],[40.1,49.6],[38.6,49.9],[38,49.9],[37.4,50.4],[36',
        '.6,50.2],[35.4,50.6],[35.4,50.8],[35,51.2],[34.2,51.3],[34.1,51.6],[34.4,51.8],[33.8,52.3],[32.7,52.2],[32.4,52.3],[32.2',
        ',52.1],[31.8,52.1],[31.5,52.7],[31.3,53.1],[31.5,53.2],[32.3,53.1],[32.7,53.4],[32.4,53.6],[31.7,53.8],[31.8,54],[31.4,5',
        '4.2],[30.8,54.8],[31,55.1],[30.9,55.6],[29.9,55.8],[29.4,55.7],[29.2,55.9],[28.2,56.2],[27.9,56.8],[27.8,57.2],[27.3,57.',
        '5],[27.7,57.8],[27.4,58.7],[28.1,59.3],[28,59.5],[29.1,60],[28.1,60.5],[30.2,61.8],[31.1,62.4],[31.5,62.9],[30,63.6],[30',
        '.4,64.2],[29.5,64.9],[30.2,65.8],[29.1,66.9],[30,67.7],[28.4,68.4],[28.6,69.1],[29.4,69.2],[31.1,69.6],[32.1,69.9],[33.8',
        ',69.3],[36.5,69.1],[40.3,67.9],[41.1,67.5],[41.1,66.8],[40,66.3],[38.4,66],[33.9,66.8],[33.2,66.6],[34.8,65.9],[34.9,65.',
        '4],[34.9,64.4],[36.2,64.1],[37,63.8],[37.1,64.3],[36.5,64.8],[37.2,65.1],[39.6,64.5],[40.4,64.8],[39.8,65.5],[42.1,66.5]',
        ',[43,66.4],[43.9,66.1],[44.5,66.8],[43.7,67.4],[44.2,68],[43.5,68.6],[46.3,68.3],[46.8,67.7],[45.6,67.6],[45.6,67],[46.3',
        ',66.7],[47.9,66.9],[48.1,67.5],[50.2,68],[53.7,68.9],[54.5,68.8],[53.5,68.2],[54.7,68.1],[55.4,68.4],[57.3,68.5],[58.8,6',
        '8.9],[59.9,68.3],[61.1,68.9],[60,69.5],[60.6,69.9],[63.5,69.5],[64.9,69.2],[68.5,68.1],[69.2,68.6],[68.2,69.1],[68.1,69.',
        '4],[66.9,69.5],[67.3,69.9],[66.7,70.7],[66.7,71],[68.5,71.9],[69.2,72.8],[69.9,73],[72.6,72.8],[72.8,72.2],[71.8,71.4],[',
        '72.5,71.1],[72.8,70.4],[72.6,69],[73.7,68.4],[73.2,67.7],[71.3,66.3],[72.4,66.2],[72.8,66.5],[73.9,66.8],[74.2,67.3],[75',
        '.1,67.8],[74.5,68.3],[74.9,69],[73.8,69.1],[73.6,69.6],[74.4,70.6],[73.1,71.4],[74.9,72.1],[74.7,72.8],[75.2,72.9],[75.7',
        ',72.3],[75.3,71.3],[76.4,71.2],[75.9,71.9],[77.6,72.3],[79.7,72.3],[81.5,71.8],[80.6,72.6],[80.5,73.6],[82.3,73.9],[84.7',
        ',73.8],[86.8,73.9],[86,74.5],[87.2,75.1],[88.3,75.1],[90.3,75.6],[92.9,75.8],[93.2,76],[95.9,76.1],[96.7,75.9],[98.9,76.',
        '4],[100.8,76.4],[101,76.9],[102,77.3],[104.4,77.7],[106.1,77.4],[104.7,77.1],[107,77],[107.2,76.5],[108.2,76.7],[111.1,7',
        '6.7],[113.3,76.2],[114.1,75.8],[113.9,75.3],[112.8,75],[110.2,74.5],[109.4,74.2],[110.6,74],[112.1,73.8],[113,74],[113.5',
        ',73.3],[114,73.6],[115.6,73.8],[118.8,73.6],[119,73.1],[123.2,73],[123.3,73.7],[125.4,73.6],[127,73.6],[128.6,73],[129.1',
        ',72.4],[128.5,72],[129.7,71.2],[131.3,70.8],[132.3,71.8],[133.9,71.4],[135.6,71.7],[137.5,71.3],[138.2,71.6],[139.9,71.5',
        '],[139.1,72.4],[140.5,72.8],[149.5,72.2],[150.4,71.6],[153,70.8],[157,71],[159,70.9],[159.8,70.5],[159.7,69.7],[160.9,69',
        '.4],[162.3,69.6],[164.1,69.7],[165.9,69.5],[167.8,69.6],[169.6,68.7],[170.8,69],[170,69.7],[170.5,70.1],[173.6,69.8],[17',
        '5.7,69.9],[178.6,69.4],[180,69],[180,65],[178.7,64.5],[177.4,64.6],[178.3,64.1],[178.9,63.3],[179.4,63],[179.5,62.6],[17',
        '9.2,62.3],[177.4,62.5],[174.6,61.8],[173.7,61.7],[172.2,61],[170.7,60.3],[170.3,59.9],[168.9,60.6],[166.3,59.8],[165.8,6',
        '0.2],[164.9,59.7],[163.5,59.9],[163.2,59.2],[162,58.2],[162.1,57.8],[163.2,57.6],[163.1,56.2],[162.1,56.1],[161.7,55.3],',
        '[162.1,54.9],[160.4,54.3],[160,53.2],[158.5,53],[158.2,51.9],[156.8,51],[156.4,51.7],[156,53.2],[155.4,55.4],[155.9,56.8',
        '],[156.8,57.4],[156.8,57.8],[158.4,58.1],[160.2,59.3],[161.9,60.3],[163.7,61.1],[164.5,62.6],[163.3,62.5],[162.7,61.6],[',
        '160.1,60.5],[159.3,61.8],[156.7,61.4],[154.2,59.8],[155,59.1],[152.8,58.9],[151.3,58.8],[151.3,59.5],[149.8,59.7],[148.5',
        ',59.2],[145.5,59.3],[142.2,59],[139,57.1],[135.1,54.7],[136.7,54.6],[137.2,54],[138.2,53.8],[138.8,54.3],[139.9,54.2],[1',
        '41.3,53.1],[141.4,52.2],[140.6,51.2],[140.5,50],[140.1,48.4],[138.6,47],[138.2,46.3],[136.9,45.1],[135.5,44],[134.9,43.4',
        '],[133.5,42.8],[132.9,42.8],[132.3,43.3],[130.9,42.6],[130.8,42.2],[130.6,42.4],[130.6,42.9],[131.1,42.9],[131.3,44.1],[',
        '131,45],[131.9,45.3],[133.1,45.1],[133.8,46.1],[134.1,47.2],[134.5,47.6],[135,48.5],[133.4,48.2],[132.5,47.8],[131,47.8]',
        ',[130.6,48.7],[129.4,49.4],[127.7,49.8],[127.3,50.7],[126.9,51.4],[126.6,51.8],[125.9,52.8],[125.1,53.2],[123.6,53.5],[1',
        '22.2,53.4],[121,53.3],[120.2,52.8],[120.7,52.5],[120.7,52],[120.2,51.6],[119.3,50.6],[119.3,50.1],[117.9,49.5],[116.7,49',
        '.9],[115.5,49.8],[115,50.1],[114.4,50.2],[112.9,49.5],[111.6,49.4],[110.7,49.1],[109.4,49.3],[108.5,49.3],[107.9,49.8],[',
        '106.9,50.3],[105.9,50.4],[104.6,50.3],[103.7,50.1],[102.3,50.5],[102.1,51.3],[100.9,51.5],[100,51.6],[98.9,52],[97.8,51]',
        ',[98.2,50.4],[97.3,49.7],[95.8,50],[94.8,50],[94.1,50.5],[93.1,50.5],[92.2,50.8],[90.7,50.3],[88.8,49.5],[87.8,49.3],[87',
        '.4,49.2],[86.8,49.8],[85.5,49.7],[85.1,50.1],[84.4,50.3],[83.9,50.9],[83.4,51.1],[81.9,50.8],[80.6,51.4],[80,50.9],[77.8',
        ',53.4],[76.5,54.2],[76.9,54.5],[74.4,53.5],[73.4,53.5],[73.5,54],[72.2,54.4],[71.2,54.1],[70.9,55.2],[69.1,55.4],[68.2,5',
        '5],[65.7,54.6],[65.2,54.4],[61.4,54],[61,53.7],[61.7,53],[60.7,52.7],[60.9,52.4],[60,52],[61.6,51.3],[61.3,50.8],[59.9,5',
        '0.8],[59.6,50.5],[58.4,51.1],[56.8,51],[55.7,50.6],[54.5,51],[52.3,51.7],[50.8,51.7],[48.7,50.6],[48.6,49.9],[47.5,50.5]',
        ',[46.8,49.4],[47,49.2],[46.5,48.4],[47.3,47.7],[48.1,47.7],[48.7,47.1],[48.6,46.6]]],[[[93.8,81],[95.9,81.3],[97.9,80.7]',
        ',[100.2,79.8],[99.9,78.9],[97.8,78.8],[95,79],[93.3,79.4],[92.5,80.1],[91.2,80.3]]],[[[102.8,79.3],[105.4,78.7],[105.1,7',
        '8.3],[99.4,77.9],[101.3,79.2],[102.1,79.3]]],[[[138.8,76.1],[141.5,76.1],[145.1,75.6],[144.3,74.8],[140.6,74.8],[139,74.',
        '6],[137,75.3],[137.5,75.9]]],[[[148.2,75.3],[150.7,75.1],[149.6,74.7],[148,74.8],[146.1,75.2],[146.4,75.5]]],[[[139.9,73',
        '.4],[140.8,73.8],[142.1,73.9],[143.5,73.5],[143.6,73.2],[142.1,73.2],[140,73.3]]],[[[44.8,80.6],[46.8,80.8],[48.3,80.8],',
        '[48.5,80.5],[49.1,80.8],[50,80.9],[51.5,80.7],[51.1,80.5],[49.8,80.4],[48.9,80.3],[48.8,80.2],[47.6,80],[46.5,80.2],[47.',
        '1,80.6]]],[[[22.7,54.3],[20.9,54.3],[19.7,54.4],[19.9,54.9],[21.3,55.2],[22.3,55],[22.8,54.9],[22.7,54.6]]],[[[53.5,73.7',
        '],[55.9,74.6],[55.6,75.1],[57.9,75.6],[61.2,76.3],[64.5,76.4],[66.2,76.8],[68.2,76.9],[68.9,76.5],[68.2,76.2],[64.6,75.7',
        '],[61.6,75.3],[58.5,74.3],[57,73.3],[55.4,72.4],[55.6,71.5],[57.5,70.7],[56.9,70.6],[53.7,70.8],[53.4,71.2],[51.6,71.5],',
        '[51.5,72],[52.5,72.2],[52.4,72.8],[54.4,73.6]]],[[[142.9,53.7],[143.3,52.7],[143.2,51.8],[143.6,50.7],[144.7,49],[143.2,',
        '49.3],[142.6,47.9],[143.5,46.8],[143.5,46.1],[142.7,46.7],[142.1,46],[141.9,46.8],[142,47.8],[141.9,48.9],[142.1,49.6],[',
        '142.2,51],[141.6,51.9],[141.7,53.3],[142.6,53.8],[142.2,54.2],[142.7,54.4]]],[[[-174.9,67.2],[-175,66.6],[-174.3,66.3],[',
        '-174.6,67.1],[-171.9,66.9],[-169.9,66],[-170.9,65.5],[-172.5,65.4],[-172.6,64.5],[-173,64.3],[-173.9,64.3],[-174.7,64.6]',
        ',[-176,64.9],[-176.2,65.4],[-177.2,65.5],[-178.4,65.4],[-178.9,65.7],[-178.7,66.1],[-179.9,65.9],[-179.4,65.4],[-180,65]',
        ',[-180,69],[-177.5,68.2]]],[[[-178.7,70.9],[-180,70.8],[-180,71.5],[-179.9,71.6],[-179,71.6],[-177.6,71.3],[-177.7,71.1]',
        ']],[[[33.4,46],[33.7,46.2],[34.4,46],[34.7,46],[34.9,45.8],[35,45.7],[35.5,45.4],[36.5,45.5],[36.3,45.1],[35.2,44.9],[33',
        '.9,44.4],[33.3,44.6],[33.5,45],[32.5,45.3],[32.6,45.5],[33.6,45.9]]]]],["BHS","BS","BHS","044","The Bahamas",-77.1,26.4,',
        '["BHS","BS","044","Bahamas","The Bahamas"],[[[[-79,26.8],[-78.5,26.9],[-77.8,26.8],[-77.8,26.6],[-78.9,26.4]]],[[[-77.8,',
        '27],[-77,26.6],[-77.2,25.9],[-77.4,26],[-77.3,26.5],[-77.8,26.9]]],[[[-78.2,25.2],[-77.9,25.2],[-77.5,24.3],[-77.5,23.8]',
        ',[-77.8,23.7],[-78,24.3],[-78.4,24.6]]]]],["FLK","FK","FLK","238","Falkland Islands",-58.7,-51.6,["FLK","FK","238","GB1"',
        ',"Falkland Is.","Falkland Islands","Falkland Islands / Malvinas","United Kingdom","B12"],[[[[-61.2,-51.8],[-60,-51.2],[-',
        '59.1,-51.5],[-58.5,-51.1],[-57.7,-51.5],[-58,-51.9],[-59.4,-52.2],[-59.8,-51.8],[-60.7,-52.3]]]]],["NOR","","","","Norwa',
        'y",9.7,61.4,["NOR","N","Norway"],[[[[15.1,79.7],[15.5,80],[17,80.1],[18.3,79.7],[21.5,79],[19,78.6],[18.5,77.8],[17.6,77',
        '.6],[17.1,76.8],[15.9,76.8],[13.8,77.4],[14.7,77.7],[13.2,78],[11.2,78.9],[10.4,79.7],[13.2,80],[13.7,79.7]]],[[[31.1,69',
        '.6],[29.4,69.2],[28.6,69.1],[29,69.8],[27.7,70.2],[26.2,69.8],[25.7,69.1],[24.7,68.6],[23.7,68.9],[22.4,68.8],[21.2,69.4',
        '],[20.6,69.1],[20,69.1],[19.9,68.4],[18,68.6],[17.7,68],[16.8,68],[16.1,67.3],[15.1,66.2],[13.6,64.8],[13.9,64.4],[13.6,',
        '64],[12.6,64.1],[11.9,63.1],[12,61.8],[12.6,61.3],[12.3,60.1],[11.5,59.4],[11,58.9],[10.4,59.5],[8.4,58.3],[7,58.1],[5.7',
        ',58.6],[5.3,59.7],[5,62],[5.9,62.6],[8.6,63.5],[10.5,64.5],[12.4,65.9],[14.8,67.8],[16.4,68.6],[19.2,69.8],[21.4,70.3],[',
        '23,70.2],[24.5,71],[26.4,71],[28.2,71.2],[31.3,70.5],[30,70.2]]],[[[27.4,80.1],[25.9,79.5],[23,79.4],[20.1,79.6],[19.9,7',
        '9.8],[18.5,79.9],[17.4,80.3],[20.5,80.6],[21.9,80.4],[22.9,80.7],[25.4,80.4]]],[[[24.7,77.9],[22.5,77.4],[20.7,77.7],[21',
        '.4,77.9],[20.8,78.3],[22.9,78.5],[23.3,78.1]]]]],["GRL","GL","GRL","304","Greenland",-39.3,74.3,["GRL","GL","304","DN1",',
        '"Greenland","Denmark"],[[[[-46.8,82.6],[-43.4,83.2],[-39.9,83.2],[-38.6,83.5],[-35.1,83.6],[-27.1,83.5],[-20.8,82.7],[-2',
        '2.7,82.3],[-26.5,82.3],[-31.9,82.2],[-31.4,82],[-27.9,82.1],[-24.8,81.8],[-22.9,82.1],[-22.1,81.7],[-23.2,81.2],[-20.6,8',
        '1.5],[-15.8,81.9],[-12.8,81.7],[-12.2,81.3],[-16.3,80.6],[-16.8,80.4],[-20,80.2],[-17.7,80.1],[-18.9,79.4],[-19.7,78.8],',
        '[-19.7,77.6],[-18.5,77],[-20,76.9],[-21.7,76.6],[-19.8,76.1],[-19.6,75.2],[-20.7,75.2],[-19.4,74.3],[-21.6,74.2],[-20.4,',
        '73.8],[-20.8,73.5],[-22.2,73.3],[-23.6,73.3],[-22.3,72.6],[-22.3,72.2],[-24.3,72.6],[-24.8,72.3],[-23.4,72.1],[-22.1,71.',
        '5],[-21.8,70.7],[-23.5,70.5],[-24.3,70.9],[-25.5,71.4],[-25.2,70.8],[-26.4,70.2],[-23.7,70.2],[-22.3,70.1],[-25,69.3],[-',
        '27.7,68.5],[-30.7,68.1],[-31.8,68.1],[-32.8,67.7],[-34.2,66.7],[-36.4,66],[-37,65.9],[-38.4,65.7],[-39.8,65.5],[-40.7,64',
        '.8],[-40.7,64.1],[-41.2,63.5],[-42.8,62.7],[-42.4,61.9],[-42.9,61.1],[-43.4,60.1],[-44.8,60],[-46.3,60.9],[-48.3,60.9],[',
        '-49.2,61.4],[-49.9,62.4],[-51.6,63.6],[-52.1,64.3],[-52.3,65.2],[-53.7,66.1],[-53.3,66.8],[-54,67.2],[-53,68.4],[-51.5,6',
        '8.7],[-51.1,69.1],[-50.9,69.9],[-52,69.6],[-52.6,69.4],[-53.5,69.3],[-54.7,69.6],[-54.8,70.3],[-54.4,70.8],[-53.4,70.8],',
        '[-51.4,70.6],[-53.1,71.2],[-54,71.5],[-55,71.4],[-55.8,71.7],[-54.7,72.6],[-55.3,73],[-56.1,73.6],[-57.3,74.7],[-58.6,75',
        '.1],[-58.6,75.5],[-61.3,76.1],[-63.4,76.2],[-66.1,76.1],[-68.5,76.1],[-69.7,76.4],[-71.4,77],[-68.8,77.3],[-66.8,77.4],[',
        '-71,77.6],[-73.3,78],[-73.2,78.4],[-69.4,78.9],[-65.7,79.4],[-65.3,79.8],[-68,80.1],[-67.2,80.5],[-63.7,81.2],[-62.2,81.',
        '3],[-62.7,81.8],[-60.3,82],[-57.2,82.2],[-54.1,82.2],[-53,81.9],[-50.4,82.4],[-48,82.1],[-46.6,82],[-44.5,81.7],[-46.9,8',
        '2.2]]]]],["ATF","TF","ATF","260","French Southern and Antarctic Lands",69.1,-49.3,["ATF","TF","260","FR1","Fr. S. Antarc',
        'tic Lands","French Southern and Antarctic Lands","France","Fr. S. and Antarctic Lands"],[[[[68.9,-48.6],[69.6,-48.9],[70',
        '.5,-49.1],[70.6,-49.3],[70.3,-49.7],[68.7,-49.8],[68.7,-49.2],[68.9,-48.8]]]]],["TLS","TL","TLS","626","East Timor",125.',
        '9,-8.8,["TLS","TL","626","TP","TMP","Timor-Leste","East Timor"],[[[[125,-8.9],[125.1,-8.7],[125.9,-8.4],[126.6,-8.4],[12',
        '7,-8.3],[127.3,-8.4],[127,-8.7],[125.9,-9.1],[125.1,-9.4],[125.1,-9.1]]]]],["ZAF","ZA","ZAF","710","South Africa",23.7,-',
        '29.7,["ZAF","ZA","710","South Africa"],[[[[16.3,-28.6],[16.8,-28.1],[17.2,-28.4],[17.4,-28.8],[17.8,-28.9],[18.5,-29],[1',
        '9,-29],[19.9,-28.5],[19.9,-24.8],[20.2,-24.9],[20.8,-25.9],[20.7,-26.5],[20.9,-26.8],[21.6,-26.7],[22.1,-26.3],[22.6,-26',
        '],[22.8,-25.5],[23.3,-25.3],[23.7,-25.4],[24.2,-25.7],[25,-25.7],[25.7,-25.5],[25.8,-25.2],[25.9,-24.7],[26.5,-24.6],[26',
        '.8,-24.2],[27.1,-23.6],[28,-22.8],[29.4,-22.1],[29.8,-22.1],[30.3,-22.3],[30.7,-22.2],[31.2,-22.3],[31.7,-23.7],[31.9,-2',
        '4.4],[31.8,-25.5],[31.8,-25.8],[31.3,-25.7],[31,-25.7],[30.9,-26],[30.7,-26.4],[30.7,-26.7],[31.3,-27.3],[31.9,-27.2],[3',
        '2.1,-26.7],[32.8,-26.7],[32.6,-27.5],[32.5,-28.3],[32.2,-28.8],[31.5,-29.3],[31.3,-29.4],[30.9,-29.9],[30.6,-30.4],[30.1',
        ',-31.1],[28.9,-32.2],[28.2,-32.8],[27.5,-33.2],[26.4,-33.6],[25.9,-33.7],[25.8,-33.9],[25.2,-33.8],[24.7,-34],[23.6,-33.',
        '8],[23,-33.9],[22.6,-33.9],[21.5,-34.3],[20.7,-34.4],[20.1,-34.8],[19.6,-34.8],[19.2,-34.5],[18.9,-34.4],[18.4,-34],[18.',
        '4,-34.1],[18.2,-33.9],[18.3,-33.3],[17.9,-32.6],[18.2,-32.4],[18.2,-31.7],[17.6,-30.7],[17.1,-29.9]],[[29,-29],[28.5,-28',
        '.6],[28.1,-28.9],[27.5,-29.2],[27,-29.9],[27.7,-30.6],[28.1,-30.5],[28.3,-30.2],[28.8,-30.1],[29,-29.7],[29.3,-29.3]]]]]',
        ',["LSO","LS","LSO","426","Lesotho",28.2,-29.5,["LSO","LS","426","Lesotho"],[[[[29,-29],[29.3,-29.3],[29,-29.7],[28.8,-30',
        '.1],[28.3,-30.2],[28.1,-30.5],[27.7,-30.6],[27,-29.9],[27.5,-29.2],[28.1,-28.9],[28.5,-28.6]]]]],["MEX","MX","MEX","484"',
        ',"Mexico",-102.3,23.9,["MEX","MX","484","Mexico"],[[[[-117.1,32.5],[-116,32.6],[-114.7,32.7],[-114.8,32.5],[-113.3,32],[',
        '-111,31.3],[-109,31.3],[-108.2,31.3],[-108.2,31.8],[-106.5,31.8],[-106.1,31.4],[-105.6,31.1],[-105,30.6],[-104.7,30.1],[',
        '-104.5,29.6],[-103.9,29.3],[-103.1,29],[-102.5,29.8],[-101.7,29.8],[-101,29.4],[-100.5,28.7],[-100.1,28.1],[-99.5,27.5],',
        '[-99.3,26.8],[-99,26.4],[-98.2,26.1],[-97.5,25.8],[-97.1,25.9],[-97.5,25],[-97.7,24.3],[-97.8,22.9],[-97.9,22.4],[-97.7,',
        '21.9],[-97.4,21.4],[-97.2,20.6],[-96.5,19.9],[-96.3,19.3],[-95.9,18.8],[-94.8,18.6],[-94.4,18.1],[-93.5,18.4],[-92.8,18.',
        '5],[-92,18.7],[-91.4,18.9],[-90.8,19.3],[-90.5,19.9],[-90.5,20.7],[-90.3,21],[-89.6,21.3],[-88.5,21.5],[-87.7,21.5],[-87',
        '.1,21.5],[-86.8,21.3],[-86.8,20.8],[-87.4,20.3],[-87.6,19.6],[-87.4,19.5],[-87.6,19],[-87.8,18.3],[-88.1,18.5],[-88.3,18',
        '.5],[-88.5,18.5],[-88.8,17.9],[-89,18],[-89.2,18],[-89.1,17.8],[-90.1,17.8],[-91,17.8],[-91,17.3],[-91.5,17.3],[-91.1,16',
        '.9],[-90.7,16.7],[-90.6,16.5],[-90.4,16.4],[-90.5,16.1],[-91.7,16.1],[-92.2,15.3],[-92.1,15.1],[-92.2,14.8],[-92.2,14.5]',
        ',[-93.4,15.6],[-93.9,15.9],[-94.7,16.2],[-95.3,16.1],[-96.1,15.8],[-96.6,15.7],[-97.3,15.9],[-98,16.1],[-98.9,16.6],[-99',
        '.7,16.7],[-100.8,17.2],[-101.7,17.6],[-101.9,17.9],[-102.5,18],[-103.5,18.3],[-103.9,18.7],[-105,19.3],[-105.5,19.9],[-1',
        '05.7,20.4],[-105.4,20.5],[-105.5,20.8],[-105.3,21.1],[-105.3,21.4],[-105.6,21.9],[-105.7,22.3],[-106,22.8],[-106.9,23.8]',
        ',[-107.9,24.5],[-108.4,25.2],[-109.3,25.6],[-109.4,25.8],[-109.3,26.4],[-109.8,26.7],[-110.4,27.2],[-110.6,27.9],[-111.2',
        ',27.9],[-111.8,28.5],[-112.2,29],[-112.3,29.3],[-112.8,30],[-113.2,30.8],[-113.1,31.2],[-113.9,31.6],[-114.2,31.5],[-114',
        '.8,31.8],[-114.9,31.4],[-114.8,30.9],[-114.7,30.2],[-114.3,29.8],[-113.6,29.1],[-113.4,28.8],[-113.3,28.8],[-113.1,28.4]',
        ',[-113,28.4],[-112.8,27.8],[-112.5,27.5],[-112.2,27.2],[-111.6,26.7],[-111.3,25.7],[-111,25.3],[-110.7,24.8],[-110.7,24.',
        '3],[-110.2,24.3],[-109.8,23.8],[-109.4,23.4],[-109.4,23.2],[-109.9,22.8],[-110,22.8],[-110.3,23.4],[-110.9,24],[-111.7,2',
        '4.5],[-112.2,24.7],[-112.1,25.5],[-112.3,26],[-112.8,26.3],[-113.5,26.8],[-113.6,26.6],[-113.8,26.9],[-114.5,27.1],[-115',
        '.1,27.7],[-115,27.8],[-114.6,27.7],[-114.2,28.1],[-114.2,28.6],[-114.9,29.3],[-115.5,29.6],[-115.9,30.2],[-116.3,30.8],[',
        '-116.7,31.6]]]]],["URY","UY","URY","858","Uruguay",-56,-33,["URY","UY","858","Uruguay"],[[[[-57.6,-30.2],[-57,-30.1],[-5',
        '6,-30.9],[-55.6,-30.9],[-54.6,-31.5],[-53.8,-32],[-53.2,-32.7],[-53.7,-33.2],[-53.4,-33.8],[-53.8,-34.4],[-54.9,-35],[-5',
        '5.7,-34.8],[-56.2,-34.9],[-57.1,-34.4],[-57.8,-34.5],[-58.4,-33.9],[-58.3,-33.3],[-58.1,-33],[-58.1,-32],[-57.9,-31]]]]]',
        ',["BRA","BR","BRA","076","Brazil",-49.6,-12.1,["BRA","BR","076","Brazil"],[[[[-53.4,-33.8],[-53.7,-33.2],[-53.2,-32.7],[',
        '-53.8,-32],[-54.6,-31.5],[-55.6,-30.9],[-56,-30.9],[-57,-30.1],[-57.6,-30.2],[-56.3,-28.9],[-55.2,-27.9],[-54.5,-27.5],[',
        '-53.6,-26.9],[-53.6,-26.1],[-54.1,-25.5],[-54.6,-25.7],[-54.4,-25.2],[-54.3,-24.6],[-54.3,-24],[-54.7,-23.8],[-55,-24],[',
        '-55.4,-24],[-55.5,-23.6],[-55.6,-22.7],[-55.8,-22.4],[-56.5,-22.1],[-56.9,-22.3],[-57.9,-22.1],[-57.9,-20.7],[-58.2,-20.',
        '2],[-57.9,-20],[-57.9,-19.4],[-57.7,-19],[-57.5,-18.2],[-57.7,-17.6],[-58.3,-17.3],[-58.4,-16.9],[-58.2,-16.3],[-60.2,-1',
        '6.3],[-60.5,-15.1],[-60.3,-15.1],[-60.3,-14.6],[-60.5,-14.4],[-60.5,-13.8],[-61.1,-13.5],[-61.7,-13.5],[-62.1,-13.2],[-6',
        '2.8,-13],[-63.2,-12.6],[-64.3,-12.5],[-65.4,-11.6],[-65.3,-10.9],[-65.4,-10.5],[-65.3,-9.8],[-66.6,-9.9],[-67.2,-10.3],[',
        '-68,-10.7],[-68.3,-11],[-68.8,-11],[-69.5,-11],[-70.1,-11.1],[-70.5,-11],[-70.5,-9.5],[-71.3,-10.1],[-72.2,-10.1],[-72.6',
        ',-9.5],[-73.2,-9.5],[-73,-9],[-73.6,-8.4],[-74,-7.5],[-73.7,-7.3],[-73.7,-6.9],[-73.1,-6.6],[-73.2,-6.1],[-73,-5.7],[-72',
        '.9,-5.3],[-71.7,-4.6],[-70.9,-4.4],[-70.8,-4.3],[-69.9,-4.3],[-69.4,-1.6],[-69.4,-1.1],[-69.6,-0.5],[-70,-0.2],[-70,0.5]',
        ',[-69.5,0.7],[-69.3,0.6],[-69.2,1],[-69.8,1.1],[-69.8,1.7],[-67.9,1.7],[-67.5,2],[-67.3,1.7],[-67.1,1.1],[-66.9,1.3],[-6',
        '6.3,0.7],[-65.5,0.8],[-65.4,1.1],[-64.6,1.3],[-64.2,1.5],[-64.1,1.9],[-63.4,2.2],[-63.4,2.4],[-64.3,2.5],[-64.4,3.1],[-6',
        '4.4,3.8],[-64.8,4.1],[-64.6,4.1],[-63.9,4],[-63.1,3.8],[-62.8,4],[-62.1,4.2],[-61,4.5],[-60.6,4.9],[-60.7,5.2],[-60.2,5.',
        '2],[-60,5],[-60.1,4.6],[-59.8,4.4],[-59.5,4],[-59.8,3.6],[-60,2.8],[-59.7,2.2],[-59.6,1.8],[-59,1.3],[-58.5,1.3],[-58.4,',
        '1.5],[-58.1,1.5],[-57.7,1.7],[-57.3,1.9],[-56.8,1.9],[-56.5,1.9],[-56,1.8],[-55.9,2],[-56.1,2.2],[-56,2.5],[-55.6,2.4],[',
        '-55.1,2.5],[-54.5,2.3],[-54.1,2.1],[-53.8,2.4],[-53.6,2.3],[-53.4,2.1],[-52.9,2.1],[-52.6,2.5],[-52.2,3.2],[-51.7,4.2],[',
        '-51.3,4.2],[-51.1,3.7],[-50.5,1.9],[-50,1.7],[-49.9,1],[-50.7,0.2],[-50.4,-0.1],[-48.6,-0.2],[-48.6,-1.2],[-47.8,-0.6],[',
        '-46.6,-0.9],[-44.9,-1.6],[-44.4,-2.1],[-44.6,-2.7],[-43.4,-2.4],[-41.5,-2.9],[-40,-2.9],[-38.5,-3.7],[-37.2,-4.8],[-36.5',
        ',-5.1],[-35.6,-5.1],[-35.2,-5.5],[-34.9,-6.7],[-34.7,-7.3],[-35.1,-9],[-35.6,-9.6],[-37,-11],[-37.7,-12.2],[-38.4,-13],[',
        '-38.7,-13.1],[-39,-13.8],[-38.9,-15.7],[-39.2,-17.2],[-39.3,-17.9],[-39.6,-18.3],[-39.8,-19.6],[-40.8,-20.9],[-40.9,-21.',
        '9],[-41.8,-22.4],[-42,-23],[-43.1,-23],[-44.6,-23.4],[-45.4,-23.8],[-46.5,-24.1],[-47.6,-24.9],[-48.5,-25.9],[-48.6,-26.',
        '6],[-48.5,-27.2],[-48.7,-28.2],[-48.9,-28.7],[-49.6,-29.2],[-50.7,-31],[-51.6,-31.8],[-52.3,-32.2],[-52.7,-33.2]]]]],["B',
        'OL","BO","BOL","068","Bolivia",-64.6,-16.7,["BOL","BO","068","Bolivia"],[[[[-69.5,-11],[-68.8,-11],[-68.3,-11],[-68,-10.',
        '7],[-67.2,-10.3],[-66.6,-9.9],[-65.3,-9.8],[-65.4,-10.5],[-65.3,-10.9],[-65.4,-11.6],[-64.3,-12.5],[-63.2,-12.6],[-62.8,',
        '-13],[-62.1,-13.2],[-61.7,-13.5],[-61.1,-13.5],[-60.5,-13.8],[-60.5,-14.4],[-60.3,-14.6],[-60.3,-15.1],[-60.5,-15.1],[-6',
        '0.2,-16.3],[-58.2,-16.3],[-58.4,-16.9],[-58.3,-17.3],[-57.7,-17.6],[-57.5,-18.2],[-57.7,-19],[-57.9,-19.4],[-57.9,-20],[',
        '-58.2,-20.2],[-58.2,-19.9],[-59.1,-19.4],[-60,-19.3],[-61.8,-19.6],[-62.3,-20.5],[-62.3,-21.1],[-62.7,-22.2],[-62.8,-22]',
        ',[-64,-22],[-64.4,-22.8],[-65,-22.1],[-66.3,-21.8],[-67.1,-22.7],[-67.8,-22.9],[-68.2,-21.5],[-68.8,-20.4],[-68.4,-19.4]',
        ',[-69,-19],[-69.1,-18.3],[-69.6,-17.6],[-69,-16.5],[-69.4,-15.7],[-69.2,-15.3],[-69.3,-15],[-68.9,-14.5],[-68.9,-13.6],[',
        '-68.9,-12.9],[-68.7,-12.6]]]]],["PER","PE","PER","604","Peru",-72.9,-13,["PER","PE","604","Peru"],[[[[-69.9,-4.3],[-70.8',
        ',-4.3],[-70.9,-4.4],[-71.7,-4.6],[-72.9,-5.3],[-73,-5.7],[-73.2,-6.1],[-73.1,-6.6],[-73.7,-6.9],[-73.7,-7.3],[-74,-7.5],',
        '[-73.6,-8.4],[-73,-9],[-73.2,-9.5],[-72.6,-9.5],[-72.2,-10.1],[-71.3,-10.1],[-70.5,-9.5],[-70.5,-11],[-70.1,-11.1],[-69.',
        '5,-11],[-68.7,-12.6],[-68.9,-12.9],[-68.9,-13.6],[-68.9,-14.5],[-69.3,-15],[-69.2,-15.3],[-69.4,-15.7],[-69,-16.5],[-69.',
        '6,-17.6],[-69.9,-18.1],[-70.4,-18.3],[-71.4,-17.8],[-71.5,-17.4],[-73.4,-16.4],[-75.2,-15.3],[-76,-14.6],[-76.4,-13.8],[',
        '-76.3,-13.5],[-77.1,-12.2],[-78.1,-10.4],[-79,-8.4],[-79.4,-7.9],[-79.8,-7.2],[-80.5,-6.5],[-81.2,-6.1],[-80.9,-5.7],[-8',
        '1.4,-4.7],[-81.1,-4],[-80.3,-3.4],[-80.2,-3.8],[-80.5,-4.1],[-80.4,-4.4],[-80,-4.3],[-79.6,-4.5],[-79.2,-5],[-78.6,-4.5]',
        ',[-78.5,-3.9],[-77.8,-3],[-76.6,-2.6],[-75.5,-1.6],[-75.2,-0.9],[-75.4,-0.2],[-75.1,-0.1],[-74.4,-0.5],[-74.1,-1],[-73.7',
        ',-1.3],[-73.1,-2.3],[-72.3,-2.4],[-71.8,-2.2],[-71.4,-2.3],[-70.8,-2.3],[-70,-2.7],[-70.7,-3.7],[-70.4,-3.8]]]]],["COL",',
        '"CO","COL","170","Colombia",-73.2,3.4,["COL","CO","170","Colombia"],[[[[-66.9,1.3],[-67.1,1.1],[-67.3,1.7],[-67.5,2],[-6',
        '7.9,1.7],[-69.8,1.7],[-69.8,1.1],[-69.2,1],[-69.3,0.6],[-69.5,0.7],[-70,0.5],[-70,-0.2],[-69.6,-0.5],[-69.4,-1.1],[-69.4',
        ',-1.6],[-69.9,-4.3],[-70.4,-3.8],[-70.7,-3.7],[-70,-2.7],[-70.8,-2.3],[-71.4,-2.3],[-71.8,-2.2],[-72.3,-2.4],[-73.1,-2.3',
        '],[-73.7,-1.3],[-74.1,-1],[-74.4,-0.5],[-75.1,-0.1],[-75.4,-0.2],[-75.8,0.1],[-76.3,0.4],[-76.6,0.3],[-77.4,0.4],[-77.7,',
        '0.8],[-77.9,0.8],[-78.9,1.4],[-79,1.7],[-78.6,1.8],[-78.7,2.3],[-78.4,2.6],[-77.9,2.7],[-77.5,3.3],[-77.1,3.8],[-77.5,4.',
        '1],[-77.3,4.7],[-77.5,5.6],[-77.3,5.8],[-77.5,6.7],[-77.9,7.2],[-77.8,7.7],[-77.4,7.6],[-77.2,7.9],[-77.5,8.5],[-77.4,8.',
        '7],[-76.8,8.6],[-76.1,9.3],[-75.7,9.4],[-75.7,9.8],[-75.5,10.6],[-74.9,11.1],[-74.3,11.1],[-74.2,11.3],[-73.4,11.2],[-72',
        '.6,11.7],[-72.2,12],[-71.8,12.4],[-71.4,12.4],[-71.1,12.1],[-71.3,11.8],[-72,11.6],[-72.2,11.1],[-72.6,10.8],[-72.9,10.5',
        '],[-73,9.7],[-73.3,9.2],[-72.8,9.1],[-72.7,8.6],[-72.4,8.4],[-72.4,8],[-72.5,7.6],[-72.4,7.4],[-72.2,7.3],[-72,7],[-70.7',
        ',7.1],[-70.1,7],[-69.4,6.1],[-69,6.2],[-68.3,6.2],[-67.7,6.3],[-67.3,6.1],[-67.5,5.6],[-67.7,5.2],[-67.8,4.5],[-67.6,3.8',
        '],[-67.3,3.5],[-67.3,3.3],[-67.8,2.8],[-67.4,2.6],[-67.2,2.3]]]]],["PAN","PA","PAN","591","Panama",-80.4,8.7,["PAN","PA"',
        ',"591","Panama"],[[[[-77.4,8.7],[-77.5,8.5],[-77.2,7.9],[-77.4,7.6],[-77.8,7.7],[-77.9,7.2],[-78.2,7.5],[-78.4,8.1],[-78',
        '.2,8.3],[-78.4,8.4],[-78.6,8.7],[-79.1,9],[-79.6,8.9],[-79.8,8.6],[-80.2,8.3],[-80.4,8.3],[-80.5,8.1],[-80,7.5],[-80.3,7',
        '.4],[-80.4,7.3],[-80.9,7.2],[-81.1,7.8],[-81.2,7.6],[-81.5,7.7],[-81.7,8.1],[-82.1,8.2],[-82.4,8.3],[-82.8,8.3],[-82.9,8',
        '.1],[-83,8.2],[-82.9,8.4],[-82.8,8.6],[-82.9,8.8],[-82.7,8.9],[-82.9,9.1],[-82.9,9.5],[-82.5,9.6],[-82.2,9.2],[-82.2,9],',
        '[-81.8,9],[-81.7,9],[-81.4,8.8],[-80.9,8.9],[-80.5,9.1],[-79.9,9.3],[-79.6,9.6],[-79,9.6],[-79.1,9.5],[-78.5,9.4],[-78.1',
        ',9.2],[-77.7,8.9]]]]],["CRI","CR","CRI","188","Costa Rica",-84.1,10.1,["CRI","CR","188","Costa Rica"],[[[[-82.5,9.6],[-8',
        '2.9,9.5],[-82.9,9.1],[-82.7,8.9],[-82.9,8.8],[-82.8,8.6],[-82.9,8.4],[-83,8.2],[-83.5,8.4],[-83.7,8.7],[-83.6,8.8],[-83.',
        '6,9.1],[-83.9,9.3],[-84.3,9.5],[-84.6,9.6],[-84.7,9.9],[-85,10.1],[-84.9,9.8],[-85.1,9.6],[-85.3,9.8],[-85.7,9.9],[-85.8',
        ',10.1],[-85.8,10.4],[-85.7,10.8],[-85.9,10.9],[-85.7,11.1],[-85.6,11.2],[-84.9,11],[-84.7,11.1],[-84.4,11],[-84.2,10.8],',
        '[-83.9,10.7],[-83.7,10.9],[-83.4,10.4],[-83,10]]]]],["NIC","NI","NIC","558","Nicaragua",-85.1,12.7,["NIC","NI","558","Ni',
        'caragua"],[[[[-83.7,10.9],[-83.9,10.7],[-84.2,10.8],[-84.4,11],[-84.7,11.1],[-84.9,11],[-85.6,11.2],[-85.7,11.1],[-86.1,',
        '11.4],[-86.5,11.8],[-86.7,12.1],[-87.2,12.5],[-87.7,12.9],[-87.6,13.1],[-87.4,12.9],[-87.3,13],[-87,13],[-86.9,13.3],[-8',
        '6.7,13.3],[-86.8,13.8],[-86.5,13.8],[-86.3,13.8],[-86.1,14],[-85.8,13.8],[-85.7,14],[-85.5,14.1],[-85.2,14.4],[-85.1,14.',
        '6],[-84.9,14.8],[-84.8,14.8],[-84.6,14.7],[-84.4,14.6],[-84.2,14.7],[-84,14.7],[-83.6,14.9],[-83.5,15],[-83.1,15],[-83.2',
        ',14.9],[-83.3,14.7],[-83.2,14.3],[-83.4,14],[-83.5,13.6],[-83.6,13.1],[-83.5,12.9],[-83.5,12.4],[-83.6,12.3],[-83.7,11.9',
        '],[-83.7,11.6],[-83.9,11.4],[-83.8,11.1]]]]],["HND","HN","HND","340","Honduras",-86.9,14.8,["HND","HN","340","Honduras"]',
        ',[[[[-83.1,15],[-83.5,15],[-83.6,14.9],[-84,14.7],[-84.2,14.7],[-84.4,14.6],[-84.6,14.7],[-84.8,14.8],[-84.9,14.8],[-85.',
        '1,14.6],[-85.2,14.4],[-85.5,14.1],[-85.7,14],[-85.8,13.8],[-86.1,14],[-86.3,13.8],[-86.5,13.8],[-86.8,13.8],[-86.7,13.3]',
        ',[-86.9,13.3],[-87,13],[-87.3,13],[-87.5,13.3],[-87.8,13.4],[-87.7,13.8],[-87.9,13.9],[-88.1,14],[-88.5,13.8],[-88.5,14]',
        ',[-88.8,14.1],[-89.1,14.3],[-89.4,14.4],[-89.1,14.7],[-89.2,14.9],[-89.2,15.1],[-88.7,15.3],[-88.2,15.7],[-88.1,15.7],[-',
        '87.9,15.9],[-87.6,15.9],[-87.5,15.8],[-87.4,15.8],[-86.9,15.8],[-86.4,15.8],[-86.1,15.9],[-86,16],[-85.7,16],[-85.4,15.9',
        '],[-85.2,15.9],[-85,16],[-84.5,15.9],[-84.4,15.8],[-84.1,15.6],[-83.8,15.4],[-83.4,15.3]]]]],["SLV","SV","SLV","222","El',
        ' Salvador",-88.9,13.7,["SLV","SV","222","El Salvador"],[[[[-89.4,14.4],[-89.1,14.3],[-88.8,14.1],[-88.5,14],[-88.5,13.8]',
        ',[-88.1,14],[-87.9,13.9],[-87.7,13.8],[-87.8,13.4],[-87.9,13.1],[-88.5,13.2],[-88.8,13.3],[-89.3,13.5],[-89.8,13.5],[-90',
        '.1,13.7],[-90.1,13.9],[-89.7,14.1],[-89.5,14.2],[-89.6,14.4]]]]],["GTM","GT","GTM","320","Guatemala",-90.5,15,["GTM","GT',
        '","320","Guatemala"],[[[[-92.2,14.5],[-92.2,14.8],[-92.1,15.1],[-92.2,15.3],[-91.7,16.1],[-90.5,16.1],[-90.4,16.4],[-90.',
        '6,16.5],[-90.7,16.7],[-91.1,16.9],[-91.5,17.3],[-91,17.3],[-91,17.8],[-90.1,17.8],[-89.1,17.8],[-89.2,17],[-89.2,15.9],[',
        '-88.9,15.9],[-88.6,15.7],[-88.5,15.9],[-88.2,15.7],[-88.7,15.3],[-89.2,15.1],[-89.2,14.9],[-89.1,14.7],[-89.4,14.4],[-89',
        '.6,14.4],[-89.5,14.2],[-89.7,14.1],[-90.1,13.9],[-90.1,13.7],[-90.6,13.9],[-91.2,13.9],[-91.7,14.1]]]]],["BLZ","BZ","BLZ',
        '","084","Belize",-88.7,17.2,["BLZ","BZ","084","Belize"],[[[[-89.1,17.8],[-89.2,18],[-89,18],[-88.8,17.9],[-88.5,18.5],[-',
        '88.3,18.5],[-88.3,18.4],[-88.1,18.3],[-88.1,18.1],[-88.3,17.6],[-88.2,17.5],[-88.3,17.1],[-88.2,17],[-88.4,16.5],[-88.6,',
        '16.3],[-88.7,16.2],[-88.9,15.9],[-89.2,15.9],[-89.2,17]]]]],["VEN","VE","VEN","862","Venezuela",-64.6,7.2,["VEN","VE","8',
        '62","Venezuela"],[[[[-60.7,5.2],[-60.6,4.9],[-61,4.5],[-62.1,4.2],[-62.8,4],[-63.1,3.8],[-63.9,4],[-64.6,4.1],[-64.8,4.1',
        '],[-64.4,3.8],[-64.4,3.1],[-64.3,2.5],[-63.4,2.4],[-63.4,2.2],[-64.1,1.9],[-64.2,1.5],[-64.6,1.3],[-65.4,1.1],[-65.5,0.8',
        '],[-66.3,0.7],[-66.9,1.3],[-67.2,2.3],[-67.4,2.6],[-67.8,2.8],[-67.3,3.3],[-67.3,3.5],[-67.6,3.8],[-67.8,4.5],[-67.7,5.2',
        '],[-67.5,5.6],[-67.3,6.1],[-67.7,6.3],[-68.3,6.2],[-69,6.2],[-69.4,6.1],[-70.1,7],[-70.7,7.1],[-72,7],[-72.2,7.3],[-72.4',
        ',7.4],[-72.5,7.6],[-72.4,8],[-72.4,8.4],[-72.7,8.6],[-72.8,9.1],[-73.3,9.2],[-73,9.7],[-72.9,10.5],[-72.6,10.8],[-72.2,1',
        '1.1],[-72,11.6],[-71.3,11.8],[-71.4,11.5],[-71.9,11.4],[-71.6,11],[-71.6,10.4],[-72.1,9.9],[-71.7,9.1],[-71.3,9.1],[-71,',
        '9.9],[-71.4,10.2],[-71.4,11],[-70.2,11.4],[-70.3,11.8],[-69.9,12.2],[-69.6,11.5],[-68.9,11.4],[-68.2,10.9],[-68.2,10.6],',
        '[-67.3,10.5],[-66.2,10.6],[-65.7,10.2],[-64.9,10.1],[-64.3,10.4],[-64.3,10.6],[-63.1,10.7],[-61.9,10.7],[-62.7,10.4],[-6',
        '2.4,9.9],[-61.6,9.9],[-60.8,9.4],[-60.7,8.6],[-60.2,8.6],[-59.8,8.4],[-60.6,7.8],[-60.6,7.4],[-60.3,7],[-60.5,6.9],[-61.',
        '2,6.7],[-61.1,6.2],[-61.4,6]]]]],["GUY","GY","GUY","328","Guyana",-58.9,5.1,["GUY","GY","328","Guyana"],[[[[-56.5,1.9],[',
        '-56.8,1.9],[-57.3,1.9],[-57.7,1.7],[-58.1,1.5],[-58.4,1.5],[-58.5,1.3],[-59,1.3],[-59.6,1.8],[-59.7,2.2],[-60,2.8],[-59.',
        '8,3.6],[-59.5,4],[-59.8,4.4],[-60.1,4.6],[-60,5],[-60.2,5.2],[-60.7,5.2],[-61.4,6],[-61.1,6.2],[-61.2,6.7],[-60.5,6.9],[',
        '-60.3,7],[-60.6,7.4],[-60.6,7.8],[-59.8,8.4],[-59.1,8],[-58.5,7.3],[-58.5,6.8],[-58.1,6.8],[-57.5,6.3],[-57.1,6],[-57.3,',
        '5.1],[-57.9,4.8],[-57.9,4.6],[-58,4.1],[-57.6,3.3],[-57.3,3.3],[-57.2,2.8]]]]],["SUR","SR","SUR","740","Suriname",-55.9,',
        '4.1,["SUR","SR","740","Suriname"],[[[[-54.5,2.3],[-55.1,2.5],[-55.6,2.4],[-56,2.5],[-56.1,2.2],[-55.9,2],[-56,1.8],[-56.',
        '5,1.9],[-57.2,2.8],[-57.3,3.3],[-57.6,3.3],[-58,4.1],[-57.9,4.6],[-57.9,4.8],[-57.3,5.1],[-57.1,6],[-55.9,5.8],[-55.8,6]',
        ',[-55,6],[-54,5.8],[-54.5,4.9],[-54.4,4.2],[-54,3.6],[-54.2,3.2],[-54.3,2.7]]]]],["FRA","","","","France",2.6,46.7,["FRA',
        '","FR1","F","FR","France"],[[[[-51.7,4.2],[-52.2,3.2],[-52.6,2.5],[-52.9,2.1],[-53.4,2.1],[-53.6,2.3],[-53.8,2.4],[-54.1',
        ',2.1],[-54.5,2.3],[-54.3,2.7],[-54.2,3.2],[-54,3.6],[-54.4,4.2],[-54.5,4.9],[-54,5.8],[-53.6,5.6],[-52.9,5.4],[-51.8,4.6',
        ']]],[[[6.2,49.5],[6.7,49.2],[8.1,49],[7.6,48.3],[7.5,47.6],[7.2,47.4],[6.7,47.5],[6.8,47.3],[6,46.7],[6,46.3],[6.5,46.4]',
        ',[6.8,46],[6.8,45.7],[7.1,45.3],[6.7,45],[7,44.3],[7.5,44.1],[7.4,43.7],[6.5,43.1],[4.6,43.4],[3.1,43.1],[3,42.5],[1.8,4',
        '2.3],[0.7,42.8],[0.3,42.6],[-1.5,43],[-1.9,43.4],[-1.4,44],[-1.2,46],[-2.2,47.1],[-3,47.6],[-4.5,48],[-4.6,48.7],[-3.3,4',
        '8.9],[-1.6,48.6],[-1.9,49.8],[-1,49.3],[1.3,50.1],[1.6,50.9],[2.5,51.1],[2.7,50.8],[3.1,50.8],[3.6,50.4],[4.3,49.9],[4.8',
        ',50],[5.7,49.5],[5.9,49.4]]],[[[8.7,42.6],[9.4,43],[9.6,42.2],[9.2,41.4],[8.8,41.6],[8.5,42.3]]]]],["ECU","EC","ECU","21',
        '8","Ecuador",-78.2,-1.3,["ECU","EC","218","Ecuador"],[[[[-75.4,-0.2],[-75.2,-0.9],[-75.5,-1.6],[-76.6,-2.6],[-77.8,-3],[',
        '-78.5,-3.9],[-78.6,-4.5],[-79.2,-5],[-79.6,-4.5],[-80,-4.3],[-80.4,-4.4],[-80.5,-4.1],[-80.2,-3.8],[-80.3,-3.4],[-79.8,-',
        '2.7],[-80,-2.2],[-80.4,-2.7],[-81,-2.2],[-80.8,-2],[-80.9,-1.1],[-80.6,-0.9],[-80.4,-0.3],[-80,0.4],[-80.1,0.8],[-79.5,1',
        '],[-78.9,1.4],[-77.9,0.8],[-77.7,0.8],[-77.4,0.4],[-76.6,0.3],[-76.3,0.4],[-75.8,0.1]]]]],["PRI","PR","PRI","630","Puert',
        'o Rico",-66.5,18.2,["PRI","PR","630","US1","Puerto Rico","United States of America"],[[[[-66.3,18.5],[-65.8,18.4],[-65.6',
        ',18.2],[-65.8,18],[-66.6,18],[-67.2,17.9],[-67.2,18.4],[-67.1,18.5]]]]],["JAM","JM","JAM","388","Jamaica",-77.3,18.1,["J',
        'AM","JM","388","J","Jamaica"],[[[[-77.6,18.5],[-76.9,18.4],[-76.4,18.2],[-76.2,17.9],[-76.9,17.9],[-77.2,17.7],[-77.8,17',
        '.9],[-78.3,18.2],[-78.2,18.5],[-77.8,18.5]]]]],["CUB","CU","CUB","192","Cuba",-78,21.3,["CUB","CU","192","CU1","Cuba"],[',
        '[[[-82.3,23.2],[-81.4,23.1],[-80.6,23.1],[-79.7,22.8],[-79.3,22.4],[-78.3,22.5],[-78,22.3],[-77.1,21.7],[-76.5,21.2],[-7',
        '6.2,21.2],[-75.6,21],[-75.7,20.7],[-74.9,20.7],[-74.2,20.3],[-74.3,20.1],[-75,19.9],[-75.6,19.9],[-76.3,20],[-77.8,19.9]',
        ',[-77.1,20.4],[-77.5,20.7],[-78.1,20.7],[-78.5,21],[-78.7,21.6],[-79.3,21.6],[-80.2,21.8],[-80.5,22],[-81.8,22.2],[-82.2',
        ',22.4],[-81.8,22.6],[-82.8,22.7],[-83.5,22.2],[-83.9,22.2],[-84.1,21.9],[-84.5,21.8],[-85,21.9],[-84.4,22.2],[-84.2,22.6',
        '],[-83.8,22.8],[-83.3,23],[-82.5,23.1]]]]],["ZWE","ZW","ZWE","716","Zimbabwe",29.9,-18.9,["ZWE","ZW","716","Zimbabwe"],[',
        '[[[31.2,-22.3],[30.7,-22.2],[30.3,-22.3],[29.8,-22.1],[29.4,-22.1],[28.8,-21.6],[28,-21.5],[27.7,-20.9],[27.7,-20.5],[27',
        '.3,-20.4],[26.2,-19.3],[25.9,-18.7],[25.6,-18.5],[25.3,-17.7],[26.4,-17.8],[26.7,-18],[27,-17.9],[27.6,-17.3],[28.5,-16.',
        '5],[28.8,-16.4],[28.9,-16],[29.5,-15.6],[30.3,-15.5],[30.3,-15.9],[31.2,-15.9],[31.6,-16.1],[31.9,-16.3],[32.3,-16.4],[3',
        '2.8,-16.7],[32.8,-18],[32.7,-18.7],[32.6,-19.4],[32.8,-19.7],[32.7,-20.3],[32.5,-20.4],[32.2,-21.1]]]]],["BWA","BW","BWA',
        '","072","Botswana",24.2,-22.1,["BWA","BW","072","Botswana"],[[[[29.4,-22.1],[28,-22.8],[27.1,-23.6],[26.8,-24.2],[26.5,-',
        '24.6],[25.9,-24.7],[25.8,-25.2],[25.7,-25.5],[25,-25.7],[24.2,-25.7],[23.7,-25.4],[23.3,-25.3],[22.8,-25.5],[22.6,-26],[',
        '22.1,-26.3],[21.6,-26.7],[20.9,-26.8],[20.7,-26.5],[20.8,-25.9],[20.2,-24.9],[19.9,-24.8],[19.9,-21.8],[20.9,-21.8],[20.',
        '9,-18.3],[21.7,-18.2],[23.2,-17.9],[23.6,-18.3],[24.2,-17.9],[24.5,-17.9],[25.1,-17.7],[25.3,-17.7],[25.6,-18.5],[25.9,-',
        '18.7],[26.2,-19.3],[27.3,-20.4],[27.7,-20.5],[27.7,-20.9],[28,-21.5],[28.8,-21.6]]]]],["NAM","NA","NAM","516","Namibia",',
        '17.1,-20.6,["NAM","NA","516","Namibia"],[[[[19.9,-24.8],[19.9,-28.5],[19,-29],[18.5,-29],[17.8,-28.9],[17.4,-28.8],[17.2',
        ',-28.4],[16.8,-28.1],[16.3,-28.6],[15.6,-27.8],[15.2,-27.1],[15,-26.1],[14.7,-25.4],[14.4,-23.9],[14.4,-22.7],[14.3,-22.',
        '1],[13.9,-21.7],[13.4,-20.9],[12.8,-19.7],[12.6,-19],[11.8,-18.1],[11.7,-17.3],[12.2,-17.1],[12.8,-16.9],[13.5,-17],[14.',
        '1,-17.4],[14.2,-17.4],[18.3,-17.3],[19,-17.8],[21.4,-17.9],[23.2,-17.5],[24,-17.3],[24.7,-17.4],[25.1,-17.6],[25.1,-17.7',
        '],[24.5,-17.9],[24.2,-17.9],[23.6,-18.3],[23.2,-17.9],[21.7,-18.2],[20.9,-18.3],[20.9,-21.8],[19.9,-21.8]]]]],["SEN","SN',
        '","SEN","686","Senegal",-14.8,15.1,["SEN","SN","686","Senegal"],[[[[-16.7,13.6],[-17.1,14.4],[-17.6,14.7],[-17.2,14.9],[',
        '-16.7,15.6],[-16.5,16.1],[-16.1,16.5],[-15.6,16.4],[-15.1,16.6],[-14.6,16.6],[-14.1,16.3],[-13.4,16],[-12.8,15.3],[-12.2',
        ',14.6],[-12.1,14],[-11.9,13.4],[-11.6,13.1],[-11.5,12.8],[-11.5,12.4],[-11.7,12.4],[-12.2,12.5],[-12.3,12.4],[-12.5,12.3',
        '],[-13.2,12.6],[-13.7,12.6],[-15.5,12.6],[-15.8,12.5],[-16.1,12.5],[-16.7,12.4],[-16.8,13.2],[-15.9,13.1],[-15.7,13.3],[',
        '-15.5,13.3],[-15.1,13.5],[-14.7,13.3],[-14.3,13.3],[-13.8,13.5],[-14,13.8],[-14.4,13.6],[-14.7,13.6],[-15.1,13.9],[-15.4',
        ',13.9],[-15.6,13.6]]]]],["MLI","ML","MLI","466","Mali",-2,18.7,["MLI","ML","466","Mali"],[[[[-11.5,12.4],[-11.5,12.8],[-',
        '11.6,13.1],[-11.9,13.4],[-12.1,14],[-12.2,14.6],[-11.8,14.8],[-11.7,15.4],[-11.3,15.4],[-10.7,15.1],[-10.1,15.3],[-9.7,1',
        '5.3],[-9.6,15.5],[-5.5,15.5],[-5.3,16.2],[-5.5,16.3],[-6,20.6],[-6.5,25],[-4.9,25],[-1.6,22.8],[1.8,20.6],[2.1,20.1],[2.',
        '7,19.9],[3.1,19.7],[3.2,19.1],[4.3,19.2],[4.3,16.9],[3.7,16.2],[3.6,15.6],[2.7,15.4],[1.4,15.3],[1,15],[0.4,14.9],[-0.3,',
        '14.9],[-0.5,15.1],[-1.1,15],[-2,14.6],[-2.2,14.2],[-3,13.8],[-3.1,13.5],[-3.5,13.3],[-4,13.5],[-4.3,13.2],[-4.4,12.5],[-',
        '5.2,11.7],[-5.2,11.4],[-5.5,11],[-5.4,10.4],[-5.8,10.2],[-6.1,10.1],[-6.2,10.5],[-6.5,10.4],[-6.7,10.4],[-6.9,10.1],[-7.',
        '6,10.1],[-7.9,10.3],[-8,10.2],[-8.3,10.5],[-8.3,10.8],[-8.4,10.9],[-8.6,10.8],[-8.6,11.1],[-8.4,11.4],[-8.8,11.8],[-8.9,',
        '12.1],[-9.1,12.3],[-9.3,12.3],[-9.6,12.2],[-9.9,12.1],[-10.2,11.8],[-10.6,11.9],[-10.9,12.2],[-11,12.2],[-11.3,12.1],[-1',
        '1.5,12.1]]]]],["MRT","MR","MRT","478","Mauritania",-9.7,19.6,["MRT","MR","478","Mauritania"],[[[[-17.1,21],[-16.8,21.3],',
        '[-12.9,21.3],[-13.1,22.8],[-12.9,23.3],[-11.9,23.4],[-12,25.9],[-8.7,25.9],[-8.7,27.4],[-4.9,25],[-6.5,25],[-6,20.6],[-5',
        '.5,16.3],[-5.3,16.2],[-5.5,15.5],[-9.6,15.5],[-9.7,15.3],[-10.1,15.3],[-10.7,15.1],[-11.3,15.4],[-11.7,15.4],[-11.8,14.8',
        '],[-12.2,14.6],[-12.8,15.3],[-13.4,16],[-14.1,16.3],[-14.6,16.6],[-15.1,16.6],[-15.6,16.4],[-16.1,16.5],[-16.5,16.1],[-1',
        '6.5,16.7],[-16.3,17.2],[-16.1,18.1],[-16.3,19.1],[-16.4,19.6],[-16.3,20.1],[-16.5,20.6]]]]],["BEN","BJ","BEN","204","Ben',
        'in",2.4,10.3,["BEN","BJ","204","Benin"],[[[[2.7,6.3],[1.9,6.1],[1.6,6.8],[1.7,9.1],[1.5,9.3],[1.4,9.8],[1.1,10.2],[0.8,1',
        '0.5],[0.9,11],[1.2,11.1],[1.4,11.5],[1.9,11.6],[2.2,11.9],[2.5,12.2],[2.8,12.2],[3.6,11.7],[3.6,11.3],[3.8,10.7],[3.6,10',
        '.3],[3.7,10.1],[3.2,9.4],[2.9,9.1],[2.7,8.5],[2.7,7.9]]]]],["NER","NE","NER","562","Niger",9.5,17.4,["NER","NE","562","N',
        'iger"],[[[[14.9,22.9],[15.1,21.3],[15.5,21],[15.5,20.7],[15.9,20.4],[15.7,20],[15.3,17.9],[15.2,16.6],[14,15.7],[13.5,14',
        '.4],[14,14],[14,13.4],[14.6,13.3],[14.5,12.9],[14.2,12.8],[14.2,12.5],[14,12.5],[13.3,13.6],[13.1,13.6],[12.3,13],[11.5,',
        '13.3],[11,13.4],[10.7,13.2],[10.1,13.3],[9.5,12.9],[9,12.8],[7.8,13.3],[7.3,13.1],[6.8,13.1],[6.4,13.5],[5.4,13.9],[4.4,',
        '13.7],[4.1,13.5],[4,13],[3.7,12.6],[3.6,11.7],[2.8,12.2],[2.5,12.2],[2.2,11.9],[2.2,12.6],[1,12.9],[1,13.3],[0.4,14],[0.',
        '3,14.4],[0.4,14.9],[1,15],[1.4,15.3],[2.7,15.4],[3.6,15.6],[3.7,16.2],[4.3,16.9],[4.3,19.2],[5.7,19.6],[8.6,21.6],[12,23',
        '.5],[13.6,23],[14.1,22.5]]]]],["NGA","NG","NGA","566","Nigeria",7.5,9.4,["NGA","NG","566","Nigeria"],[[[[2.7,6.3],[2.7,7',
        '.9],[2.7,8.5],[2.9,9.1],[3.2,9.4],[3.7,10.1],[3.6,10.3],[3.8,10.7],[3.6,11.3],[3.6,11.7],[3.7,12.6],[4,13],[4.1,13.5],[4',
        '.4,13.7],[5.4,13.9],[6.4,13.5],[6.8,13.1],[7.3,13.1],[7.8,13.3],[9,12.8],[9.5,12.9],[10.1,13.3],[10.7,13.2],[11,13.4],[1',
        '1.5,13.3],[12.3,13],[13.1,13.6],[13.3,13.6],[14,12.5],[14.2,12.5],[14.6,12.1],[14.5,11.9],[14.4,11.6],[13.6,10.8],[13.3,',
        '10.2],[13.2,9.6],[13,9.4],[12.8,8.7],[12.2,8.3],[12.1,7.8],[11.8,7.4],[11.7,7],[11.1,6.6],[10.5,7.1],[10.1,7],[9.5,6.5],',
        '[9.2,6.4],[8.8,5.5],[8.5,4.8],[7.5,4.4],[7.1,4.5],[6.7,4.2],[5.9,4.3],[5.4,4.9],[5,5.6],[4.3,6.3],[3.6,6.3]]]]],["CMR","',
        'CM","CMR","120","Cameroon",12.5,4.6,["CMR","CM","120","Cameroon"],[[[[14.5,12.9],[14.9,12.2],[15,11.6],[14.9,10.9],[15.5',
        ',10],[14.9,10],[14.6,9.9],[14.2,10],[14,9.5],[14.5,9],[15,8.8],[15.1,8.4],[15.4,7.7],[15.3,7.4],[14.8,6.4],[14.5,6.2],[1',
        '4.5,5.5],[14.6,5],[14.5,4.7],[15,4.2],[15,3.9],[15.4,3.3],[15.9,3],[15.9,2.6],[16,2.3],[15.9,1.7],[15.1,2],[14.3,2.2],[1',
        '3.1,2.3],[13,2.3],[12.4,2.2],[11.8,2.3],[11.3,2.3],[9.6,2.3],[9.8,3.1],[9.4,3.7],[8.9,3.9],[8.7,4.4],[8.5,4.5],[8.5,4.8]',
        ',[8.8,5.5],[9.2,6.4],[9.5,6.5],[10.1,7],[10.5,7.1],[11.1,6.6],[11.7,7],[11.8,7.4],[12.1,7.8],[12.2,8.3],[12.8,8.7],[13,9',
        '.4],[13.2,9.6],[13.3,10.2],[13.6,10.8],[14.4,11.6],[14.5,11.9],[14.6,12.1],[14.2,12.5],[14.2,12.8]]]]],["TGO","TG","TGO"',
        ',"768","Togo",1.1,8.8,["TGO","TG","768","Togo"],[[[[0.9,11],[0.8,10.5],[1.1,10.2],[1.4,9.8],[1.5,9.3],[1.7,9.1],[1.6,6.8',
        '],[1.9,6.1],[1.1,5.9],[0.8,6.3],[0.6,6.9],[0.5,7.4],[0.7,8.3],[0.5,8.7],[0.4,9.5],[0.4,10.2],[0,10.7],[0,11]]]]],["GHA",',
        '"GH","GHA","288","Ghana",-1,7.7,["GHA","GH","288","Ghana"],[[[[0,11],[0,10.7],[0.4,10.2],[0.4,9.5],[0.5,8.7],[0.7,8.3],[',
        '0.5,7.4],[0.6,6.9],[0.8,6.3],[1.1,5.9],[-0.5,5.3],[-1.1,5],[-2,4.7],[-2.9,5],[-2.8,5.4],[-3.2,6.3],[-3,7.4],[-2.6,8.2],[',
        '-2.8,9.6],[-3,10.4],[-2.9,11],[-1.2,11],[-0.8,10.9],[-0.4,11.1]]]]],["CIV","CI","CIV","384","Ivory Coast",-5.6,7.5,["CIV',
        '","CI","384","Côte d\'Ivoire","Ivory Coast"],[[[[-8,10.2],[-7.9,10.3],[-7.6,10.1],[-6.9,10.1],[-6.7,10.4],[-6.5,10.4],[-6',
        '.2,10.5],[-6.1,10.1],[-5.8,10.2],[-5.4,10.4],[-5,10.2],[-4.8,9.8],[-4.3,9.6],[-4,9.9],[-3.5,9.9],[-2.8,9.6],[-2.6,8.2],[',
        '-3,7.4],[-3.2,6.3],[-2.8,5.4],[-2.9,5],[-3.3,5],[-4,5.2],[-4.6,5.2],[-5.8,5],[-6.5,4.7],[-7.5,4.3],[-7.7,4.4],[-7.6,5.2]',
        ',[-7.5,5.3],[-7.6,5.7],[-8,6.1],[-8.3,6.2],[-8.6,6.5],[-8.4,6.9],[-8.5,7.4],[-8.4,7.7],[-8.3,7.7],[-8.2,8.1],[-8.3,8.3],',
        '[-8.2,8.5],[-7.8,8.6],[-8.1,9.4],[-8.3,9.8],[-8.2,10.1]]]]],["GIN","GN","GIN","324","Guinea",-10,10.6,["GIN","GN","324",',
        '"Guinea"],[[[[-13.7,12.6],[-13.2,12.6],[-12.5,12.3],[-12.3,12.4],[-12.2,12.5],[-11.7,12.4],[-11.5,12.4],[-11.5,12.1],[-1',
        '1.3,12.1],[-11,12.2],[-10.9,12.2],[-10.6,11.9],[-10.2,11.8],[-9.9,12.1],[-9.6,12.2],[-9.3,12.3],[-9.1,12.3],[-8.9,12.1],',
        '[-8.8,11.8],[-8.4,11.4],[-8.6,11.1],[-8.6,10.8],[-8.4,10.9],[-8.3,10.8],[-8.3,10.5],[-8,10.2],[-8.2,10.1],[-8.3,9.8],[-8',
        '.1,9.4],[-7.8,8.6],[-8.2,8.5],[-8.3,8.3],[-8.2,8.1],[-8.3,7.7],[-8.4,7.7],[-8.7,7.7],[-8.9,7.3],[-9.2,7.3],[-9.4,7.5],[-',
        '9.3,7.9],[-9.8,8.5],[-10,8.4],[-10.2,8.4],[-10.5,8.3],[-10.5,8.7],[-10.7,9],[-10.6,9.3],[-10.8,9.7],[-11.1,10],[-11.9,10',
        '],[-12.2,9.9],[-12.4,9.8],[-12.6,9.6],[-12.7,9.3],[-13.2,8.9],[-13.7,9.5],[-14.1,9.9],[-14.3,10],[-14.6,10.2],[-14.7,10.',
        '7],[-14.8,10.9],[-15.1,11],[-14.7,11.5],[-14.4,11.5],[-14.1,11.7],[-13.9,11.7],[-13.7,11.8],[-13.8,12.1],[-13.7,12.2]]]]',
        '],["GNB","GW","GNB","624","Guinea-Bissau",-14.5,12.2,["GNB","GW","624","Guinea-Bissau"],[[[[-16.7,12.4],[-16.1,12.5],[-1',
        '5.8,12.5],[-15.5,12.6],[-13.7,12.6],[-13.7,12.2],[-13.8,12.1],[-13.7,11.8],[-13.9,11.7],[-14.1,11.7],[-14.4,11.5],[-14.7',
        ',11.5],[-15.1,11],[-15.7,11.5],[-16.1,11.5],[-16.3,11.8],[-16.3,12],[-16.6,12.2]]]]],["LBR","LR","LBR","430","Liberia",-',
        '9.5,6.4,["LBR","LR","430","Liberia"],[[[[-8.4,7.7],[-8.5,7.4],[-8.4,6.9],[-8.6,6.5],[-8.3,6.2],[-8,6.1],[-7.6,5.7],[-7.5',
        ',5.3],[-7.6,5.2],[-7.7,4.4],[-8,4.4],[-9,4.8],[-9.9,5.6],[-10.8,6.1],[-11.4,6.8],[-11.2,7.1],[-11.1,7.4],[-10.7,7.9],[-1',
        '0.2,8.4],[-10,8.4],[-9.8,8.5],[-9.3,7.9],[-9.4,7.5],[-9.2,7.3],[-8.9,7.3],[-8.7,7.7]]]]],["SLE","SL","SLE","694","Sierra',
        ' Leone",-11.8,8.6,["SLE","SL","694","Sierra Leone"],[[[[-13.2,8.9],[-12.7,9.3],[-12.6,9.6],[-12.4,9.8],[-12.2,9.9],[-11.',
        '9,10],[-11.1,10],[-10.8,9.7],[-10.6,9.3],[-10.7,9],[-10.5,8.7],[-10.5,8.3],[-10.2,8.4],[-10.7,7.9],[-11.1,7.4],[-11.2,7.',
        '1],[-11.4,6.8],[-11.7,6.9],[-12.4,7.3],[-12.9,7.8],[-13.1,8.2]]]]],["BFA","BF","BFA","854","Burkina Faso",-1.4,12.7,["BF',
        'A","BF","854","Burkina Faso"],[[[[-5.4,10.4],[-5.5,11],[-5.2,11.4],[-5.2,11.7],[-4.4,12.5],[-4.3,13.2],[-4,13.5],[-3.5,1',
        '3.3],[-3.1,13.5],[-3,13.8],[-2.2,14.2],[-2,14.6],[-1.1,15],[-0.5,15.1],[-0.3,14.9],[0.4,14.9],[0.3,14.4],[0.4,14],[1,13.',
        '3],[1,12.9],[2.2,12.6],[2.2,11.9],[1.9,11.6],[1.4,11.5],[1.2,11.1],[0.9,11],[0,11],[-0.4,11.1],[-0.8,10.9],[-1.2,11],[-2',
        '.9,11],[-3,10.4],[-2.8,9.6],[-3.5,9.9],[-4,9.9],[-4.3,9.6],[-4.8,9.8],[-5,10.2]]]]],["CAF","CF","CAF","140","Central Afr',
        'ican Republic",20.9,7,["CAF","CF","140","Central African Rep.","Central African Republic"],[[[[27.4,5.2],[27,5.1],[26.4,',
        '5.2],[25.7,5.3],[25.3,5.2],[25.1,4.9],[24.8,4.9],[24.4,5.1],[23.3,4.6],[22.8,4.7],[22.7,4.6],[22.4,4],[21.7,4.2],[20.9,4',
        '.3],[20.3,4.7],[19.5,5],[18.9,4.7],[18.5,4.2],[18.5,3.5],[17.8,3.6],[17.1,3.7],[16.5,3.2],[16,2.3],[15.9,2.6],[15.9,3],[',
        '15.4,3.3],[15,3.9],[15,4.2],[14.5,4.7],[14.6,5],[14.5,5.5],[14.5,6.2],[14.8,6.4],[15.3,7.4],[16.1,7.5],[16.3,7.8],[16.5,',
        '7.7],[16.7,7.5],[18,7.9],[18.4,8.3],[18.9,8.6],[18.8,9],[19.1,9.1],[20.1,9],[21,9.5],[21.7,10.6],[22.2,11],[22.9,11.1],[',
        '23,10.7],[23.6,10.1],[23.6,9.7],[23.4,9.3],[23.5,9],[23.8,8.7],[24.6,8.2],[25.1,7.8],[25.1,7.5],[25.8,7],[26.2,6.5],[26.',
        '5,5.9],[27.2,5.6]]]]],["COG","CG","COG","178","Republic of the Congo",15.9,0.1,["COG","CG","178","Congo","Republic of th',
        'e Congo"],[[[[18.5,3.5],[18.4,2.9],[18.1,2.4],[17.9,1.7],[17.8,0.9],[17.8,0.3],[17.7,-0.1],[17.6,-0.4],[17.5,-0.7],[16.9',
        ',-1.2],[16.4,-1.7],[16,-2.7],[16,-3.5],[15.8,-3.9],[15.2,-4.3],[14.6,-5],[14.2,-4.8],[14.1,-4.5],[13.6,-4.5],[13.3,-4.9]',
        ',[13,-4.8],[12.6,-4.4],[12.3,-4.6],[11.9,-5],[11.1,-4],[11.9,-3.4],[11.5,-2.8],[11.8,-2.5],[12.5,-2.4],[12.6,-1.9],[13.1',
        ',-2.4],[14,-2.5],[14.3,-2],[14.4,-1.3],[14.3,-0.6],[13.8,0],[14.3,1.2],[14,1.4],[13.3,1.3],[13,1.8],[13.1,2.3],[14.3,2.2',
        '],[15.1,2],[15.9,1.7],[16,2.3],[16.5,3.2],[17.1,3.7],[17.8,3.6]]]]],["GAB","GA","GAB","266","Gabon",11.8,-0.4,["GAB","GA',
        '","266","Gabon"],[[[[11.3,2.3],[11.8,2.3],[12.4,2.2],[13,2.3],[13.1,2.3],[13,1.8],[13.3,1.3],[14,1.4],[14.3,1.2],[13.8,0',
        '],[14.3,-0.6],[14.4,-1.3],[14.3,-2],[14,-2.5],[13.1,-2.4],[12.6,-1.9],[12.5,-2.4],[11.8,-2.5],[11.5,-2.8],[11.9,-3.4],[1',
        '1.1,-4],[10.1,-3],[9.4,-2.1],[8.8,-1.1],[8.8,-0.8],[9,-0.5],[9.3,0.3],[9.5,1],[9.8,1.1],[11.3,1.1]]]]],["GNQ","GQ","GNQ"',
        ',"226","Equatorial Guinea",9,2.3,["GNQ","GQ","226","Eq. Guinea","Equatorial Guinea"],[[[[9.6,2.3],[11.3,2.3],[11.3,1.1],',
        '[9.8,1.1],[9.5,1],[9.3,1.2]]]]],["ZMB","ZM","ZMB","894","Zambia",26.4,-14.7,["ZMB","ZM","894","Zambia"],[[[[30.7,-8.3],[',
        '31.2,-8.6],[31.6,-8.8],[32.2,-8.9],[32.8,-9.2],[33.2,-9.7],[33.5,-10.5],[33.3,-10.8],[33.1,-11.6],[33.3,-12.4],[33,-12.8',
        '],[32.7,-13.7],[33.2,-14],[30.2,-14.8],[30.3,-15.5],[29.5,-15.6],[28.9,-16],[28.8,-16.4],[28.5,-16.5],[27.6,-17.3],[27,-',
        '17.9],[26.7,-18],[26.4,-17.8],[25.3,-17.7],[25.1,-17.7],[25.1,-17.6],[24.7,-17.4],[24,-17.3],[23.2,-17.5],[22.6,-16.9],[',
        '21.9,-16.1],[21.9,-12.9],[24,-12.9],[23.9,-12.6],[24.1,-12.2],[23.9,-11.7],[24,-11.2],[23.9,-10.9],[24.3,-11],[24.3,-11.',
        '3],[24.8,-11.2],[25.4,-11.3],[25.8,-11.8],[26.6,-11.9],[27.2,-11.6],[27.4,-12.1],[28.2,-12.3],[28.5,-12.7],[28.9,-13.2],',
        '[29.7,-13.3],[29.6,-12.2],[29.3,-12.4],[28.6,-12],[28.4,-11.8],[28.5,-10.8],[28.7,-9.6],[28.4,-9.2],[28.7,-8.5],[29,-8.4',
        '],[30.3,-8.2]]]]],["MWI","MW","MWI","454","Malawi",33.6,-13.4,["MWI","MW","454","Malawi"],[[[[32.8,-9.2],[33.7,-9.4],[33',
        '.9,-9.7],[34.3,-10.2],[34.6,-11.5],[34.3,-12.3],[34.6,-13.6],[34.9,-13.6],[35.3,-13.9],[35.7,-14.6],[35.8,-15.9],[35.3,-',
        '16.1],[35,-16.8],[34.4,-16.2],[34.3,-15.5],[34.5,-15],[34.5,-14.6],[34.1,-14.4],[33.8,-14.5],[33.2,-14],[32.7,-13.7],[33',
        ',-12.8],[33.3,-12.4],[33.1,-11.6],[33.3,-10.8],[33.5,-10.5],[33.2,-9.7]]]]],["MOZ","MZ","MOZ","508","Mozambique",37.8,-1',
        '3.9,["MOZ","MZ","508","Mozambique"],[[[[34.6,-11.5],[35.3,-11.4],[36.5,-11.7],[36.8,-11.6],[37.5,-11.6],[37.8,-11.3],[38',
        '.4,-11.3],[39.5,-10.9],[40.3,-10.3],[40.5,-10.8],[40.4,-11.8],[40.6,-12.6],[40.6,-14.2],[40.8,-14.7],[40.5,-15.4],[40.1,',
        '-16.1],[39.5,-16.7],[38.5,-17.1],[37.4,-17.6],[36.3,-18.7],[35.9,-18.8],[35.2,-19.6],[34.8,-19.8],[34.7,-20.5],[35.2,-21',
        '.3],[35.4,-21.8],[35.4,-22.1],[35.6,-22.1],[35.5,-23.1],[35.4,-23.5],[35.6,-23.7],[35.5,-24.1],[35,-24.5],[34.2,-24.8],[',
        '33,-25.4],[32.6,-25.7],[32.7,-26.1],[32.9,-26.2],[32.8,-26.7],[32.1,-26.7],[32,-26.3],[31.8,-25.8],[31.8,-25.5],[31.9,-2',
        '4.4],[31.7,-23.7],[31.2,-22.3],[32.2,-21.1],[32.5,-20.4],[32.7,-20.3],[32.8,-19.7],[32.6,-19.4],[32.7,-18.7],[32.8,-18],',
        '[32.8,-16.7],[32.3,-16.4],[31.9,-16.3],[31.6,-16.1],[31.2,-15.9],[30.3,-15.9],[30.3,-15.5],[30.2,-14.8],[33.2,-14],[33.8',
        ',-14.5],[34.1,-14.4],[34.5,-14.6],[34.5,-15],[34.3,-15.5],[34.4,-16.2],[35,-16.8],[35.3,-16.1],[35.8,-15.9],[35.7,-14.6]',
        ',[35.3,-13.9],[34.9,-13.6],[34.6,-13.6],[34.3,-12.3]]]]],["SWZ","SZ","SWZ","748","Eswatini",31.5,-26.5,["SWZ","SZ","748"',
        ',"ES","eSwatini","Eswatini","Kingdom of eSwatini"],[[[[32.1,-26.7],[31.9,-27.2],[31.3,-27.3],[30.7,-26.7],[30.7,-26.4],[',
        '30.9,-26],[31,-25.7],[31.3,-25.7],[31.8,-25.8],[32,-26.3]]]]],["AGO","AO","AGO","024","Angola",18,-12.2,["AGO","AO","024',
        '","Angola"],[[[[13,-4.8],[12.6,-5],[12.5,-5.2],[12.4,-5.7],[12.2,-5.8],[11.9,-5],[12.3,-4.6],[12.6,-4.4]]],[[[12.3,-6.1]',
        ',[12.7,-6],[13,-6],[13.4,-5.9],[16.3,-5.9],[16.6,-6.6],[16.9,-7.2],[17.1,-7.5],[17.5,-8.1],[18.1,-8],[18.5,-7.8],[19,-8]',
        ',[19.2,-7.7],[19.4,-7.2],[20,-7.1],[20.1,-6.9],[20.6,-6.9],[20.5,-7.3],[21.7,-7.3],[21.7,-7.9],[21.9,-8.3],[21.8,-8.9],[',
        '21.9,-9.5],[22.2,-9.9],[22.2,-11.1],[22.4,-11],[22.8,-11],[23.5,-10.9],[23.9,-10.9],[24,-11.2],[23.9,-11.7],[24.1,-12.2]',
        ',[23.9,-12.6],[24,-12.9],[21.9,-12.9],[21.9,-16.1],[22.6,-16.9],[23.2,-17.5],[21.4,-17.9],[19,-17.8],[18.3,-17.3],[14.2,',
        '-17.4],[14.1,-17.4],[13.5,-17],[12.8,-16.9],[12.2,-17.1],[11.7,-17.3],[11.6,-16.7],[11.8,-15.8],[12.1,-14.9],[12.2,-14.4',
        '],[12.5,-13.5],[12.7,-13.1],[13.3,-12.5],[13.6,-12],[13.7,-11.3],[13.7,-10.7],[13.4,-10.4],[13.1,-9.8],[12.9,-9.2],[12.9',
        ',-9],[13.2,-8.6],[12.9,-7.6],[12.7,-6.9],[12.2,-6.3]]]]],["BDI","BI","BDI","108","Burundi",29.9,-3.3,["BDI","BI","108","',
        'Burundi"],[[[[30.5,-2.4],[30.5,-2.8],[30.7,-3],[30.8,-3.4],[30.5,-3.6],[30.1,-4.1],[29.8,-4.5],[29.3,-4.5],[29.3,-3.3],[',
        '29,-2.8],[29.6,-2.9],[29.9,-2.3]]]]],["ISR","IL","ISR","376","Israel",34.8,30.9,["ISR","IL","376","IS1","IS","Israel"],[',
        '[[[35.7,32.7],[35.5,32.4],[35.2,32.5],[35,31.9],[35.2,31.8],[35,31.6],[34.9,31.4],[35.4,31.5],[35.4,31.1],[34.9,29.5],[3',
        '4.8,29.8],[34.3,31.2],[34.6,31.5],[34.5,31.6],[34.8,32.1],[35,32.8],[35.1,33.1],[35.5,33.1],[35.6,33.3],[35.8,33.3],[35.',
        '8,32.9]]]]],["LBN","LB","LBN","422","Lebanon",36,34.1,["LBN","LB","422","Lebanon"],[[[[35.8,33.3],[35.6,33.3],[35.5,33.1',
        '],[35.1,33.1],[35.5,33.9],[36,34.6],[36.4,34.6],[36.6,34.2],[36.1,33.8]]]]],["MDG","MG","MDG","450","Madagascar",46.7,-1',
        '8.6,["MDG","MG","450","Madagascar"],[[[[49.5,-12.5],[49.8,-12.9],[50.1,-13.6],[50.2,-14.8],[50.5,-15.2],[50.4,-15.7],[50',
        '.2,-16],[49.9,-15.4],[49.7,-15.7],[49.9,-16.5],[49.8,-16.9],[49.5,-17.1],[49.4,-18],[49,-19.1],[48.5,-20.5],[47.9,-22.4]',
        ',[47.5,-23.8],[47.1,-24.9],[46.3,-25.2],[45.4,-25.6],[44.8,-25.3],[44,-25],[43.8,-24.5],[43.7,-23.6],[43.3,-22.8],[43.3,',
        '-22.1],[43.4,-21.3],[43.9,-21.2],[43.9,-20.8],[44.4,-20.1],[44.5,-19.4],[44.2,-19],[44,-18.3],[44,-17.4],[44.3,-16.9],[4',
        '4.4,-16.2],[44.9,-16.2],[45.5,-16],[45.9,-15.8],[46.3,-15.8],[46.9,-15.2],[47.7,-14.6],[48,-14.1],[47.9,-13.7],[48.3,-13',
        '.8],[48.8,-13.1],[48.9,-12.5],[49.2,-12]]]]],["PSX","PS","PSE","275","Palestine",35.3,32,["PSX","PS","PSE","275","IS1","',
        'PAL","GZ","WBG","Palestine","Israel"],[[[[35.4,31.5],[34.9,31.4],[35,31.6],[35.2,31.8],[35,31.9],[35.2,32.5],[35.5,32.4]',
        ',[35.5,31.8]]]]],["GMB","GM","GMB","270","The Gambia",-15,13.6,["GMB","GM","270","Gambia","The Gambia"],[[[[-16.7,13.6],',
        '[-15.6,13.6],[-15.4,13.9],[-15.1,13.9],[-14.7,13.6],[-14.4,13.6],[-14,13.8],[-13.8,13.5],[-14.3,13.3],[-14.7,13.3],[-15.',
        '1,13.5],[-15.5,13.3],[-15.7,13.3],[-15.9,13.1],[-16.8,13.2]]]]],["TUN","TN","TUN","788","Tunisia",9,33.7,["TUN","TN","78',
        '8","Tunisia"],[[[[9.5,30.3],[9.1,32.1],[8.4,32.5],[8.4,32.7],[7.6,33.3],[7.5,34.1],[8.1,34.7],[8.4,35.5],[8.2,36.4],[8.4',
        ',36.9],[9.5,37.3],[10.2,37.2],[10.2,36.7],[11,37.1],[11.1,36.9],[10.6,36.4],[10.6,35.9],[10.9,35.7],[10.8,34.8],[10.1,34',
        '.3],[10.3,33.8],[10.9,33.8],[11.1,33.3],[11.5,33.1],[11.4,32.4],[10.9,32.1],[10.6,31.8],[10,31.4],[10.1,31],[10,30.5]]]]',
        '],["DZA","DZ","DZA","012","Algeria",2.8,27.4,["DZA","DZ","012","Algeria"],[[[[-8.7,27.4],[-8.7,27.6],[-8.7,27.7],[-8.7,2',
        '8.8],[-7.1,29.6],[-6.1,29.7],[-5.2,30],[-4.9,30.5],[-3.7,30.9],[-3.6,31.6],[-3.1,31.7],[-2.6,32.1],[-1.3,32.3],[-1.1,32.',
        '7],[-1.4,32.9],[-1.7,33.9],[-1.8,34.5],[-2.2,35.2],[-1.2,35.7],[-0.1,35.9],[0.5,36.3],[1.5,36.6],[3.2,36.8],[4.8,36.9],[',
        '5.3,36.7],[6.3,37.1],[7.3,37.1],[7.7,36.9],[8.4,36.9],[8.2,36.4],[8.4,35.5],[8.1,34.7],[7.5,34.1],[7.6,33.3],[8.4,32.7],',
        '[8.4,32.5],[9.1,32.1],[9.5,30.3],[9.8,29.4],[9.9,29],[9.7,28.1],[9.8,27.7],[9.6,27.1],[9.7,26.5],[9.3,26.1],[9.9,25.4],[',
        '9.9,24.9],[10.3,24.4],[10.8,24.6],[11.6,24.1],[12,23.5],[8.6,21.6],[5.7,19.6],[4.3,19.2],[3.2,19.1],[3.1,19.7],[2.7,19.9',
        '],[2.1,20.1],[1.8,20.6],[-1.6,22.8],[-4.9,25]]]]],["JOR","JO","JOR","400","Jordan",36.4,30.8,["JOR","JO","400","J","Jord',
        'an"],[[[[35.5,32.4],[35.7,32.7],[36.8,32.3],[38.8,33.4],[39.2,32.2],[39,32],[37,31.5],[38,30.5],[37.7,30.3],[37.5,30],[3',
        '6.7,29.9],[36.5,29.5],[36.1,29.2],[35,29.4],[34.9,29.5],[35.4,31.1],[35.4,31.5],[35.5,31.8]]]]],["ARE","AE","ARE","784",',
        '"United Arab Emirates",54.5,23.5,["ARE","AE","784","United Arab Emirates"],[[[[51.6,24.2],[51.8,24.3],[51.8,24],[52.6,24',
        '.2],[53.4,24.2],[54,24.1],[54.7,24.8],[55.4,25.4],[56.1,26.1],[56.3,25.7],[56.4,24.9],[55.9,24.9],[55.8,24.3],[56,24.1],',
        '[55.5,23.9],[55.5,23.5],[55.2,23.1],[55.2,22.7],[55,22.5],[52,23],[51.6,24]]]]],["QAT","QA","QAT","634","Qatar",51.1,25.',
        '2,["QAT","QA","634","Qatar"],[[[[50.8,24.8],[50.7,25.5],[51,26],[51.3,26.1],[51.6,25.8],[51.6,25.2],[51.4,24.6],[51.1,24',
        '.6]]]]],["KWT","KW","KWT","414","Kuwait",47.3,29.4,["KWT","KW","414","Kuwait"],[[[[48,30],[48.2,29.5],[48.1,29.3],[48.4,',
        '28.6],[47.7,28.5],[47.5,29],[46.6,29.1],[47.3,30.1]]]]],["IRQ","IQ","IRQ","368","Iraq",43.3,33.1,["IRQ","IQ","368","Iraq',
        '"],[[[[39.2,32.2],[38.8,33.4],[41,34.4],[41.4,35.6],[41.3,36.4],[41.8,36.6],[42.3,37.2],[42.8,37.4],[43.9,37.3],[44.3,37',
        '],[44.8,37.2],[45.4,36],[46.1,35.7],[46.2,35.1],[45.6,34.7],[45.4,34],[46.1,33],[47.3,32.5],[47.8,31.7],[47.7,31],[48,31',
        '],[48,30.5],[48.6,29.9],[48,30],[47.3,30.1],[46.6,29.1],[44.7,29.2],[41.9,31.2],[40.4,31.9]]]]],["OMN","OM","OMN","512",',
        '"Oman",57.3,22.1,["OMN","OM","512","Oman"],[[[[55.2,22.7],[55.2,23.1],[55.5,23.5],[55.5,23.9],[56,24.1],[55.8,24.3],[55.',
        '9,24.9],[56.4,24.9],[56.8,24.2],[57.4,23.9],[58.1,23.7],[58.7,23.6],[59.2,23],[59.5,22.7],[59.8,22.5],[59.8,22.3],[59.4,',
        '21.7],[59.3,21.4],[58.9,21.1],[58.5,20.4],[58,20.5],[57.8,20.2],[57.7,19.7],[57.8,19.1],[57.7,18.9],[57.2,18.9],[56.6,18',
        '.6],[56.5,18.1],[56.3,17.9],[55.7,17.9],[55.3,17.6],[55.3,17.2],[54.8,17],[54.2,17],[53.6,16.7],[53.1,16.7],[52.8,17.3],',
        '[52,19],[55,20],[55.7,22]]],[[[56.3,25.7],[56.1,26.1],[56.4,26.4],[56.5,26.3],[56.4,25.9]]]]],["VUT","VU","VUT","548","V',
        'anuatu",166.9,-15.4,["VUT","VU","548","Vanuatu"],[[[[167.2,-15.9],[167.8,-16.5],[167.5,-16.6],[167.2,-16.2]]],[[[166.8,-',
        '15.7],[166.6,-15.4],[166.6,-14.6],[167.1,-14.9],[167.3,-15.7],[167,-15.6]]]]],["KHM","KH","KHM","116","Cambodia",104.5,1',
        '2.6,["KHM","KH","116","Cambodia"],[[[[102.6,12.2],[102.3,13.4],[103,14.2],[104.3,14.4],[105.2,14.3],[106,13.9],[106.5,14',
        '.6],[107.4,14.2],[107.6,13.5],[107.5,12.3],[105.8,11.6],[106.2,11],[105.2,10.9],[104.3,10.5],[103.5,10.6],[103.1,11.2]]]',
        ']],["THA","TH","THA","764","Thailand",101.1,15.5,["THA","TH","764","Thailand"],[[[[105.2,14.3],[104.3,14.4],[103,14.2],[',
        '102.3,13.4],[102.6,12.2],[101.7,12.6],[100.8,12.6],[101,13.4],[100.1,13.4],[100,12.3],[99.5,10.8],[99.2,10],[99.2,9.2],[',
        '99.9,9.2],[100.3,8.3],[100.5,7.4],[101,6.9],[101.6,6.7],[102.1,6.2],[101.8,5.8],[101.2,5.7],[101.1,6.2],[100.3,6.6],[100',
        '.1,6.5],[99.7,6.8],[99.5,7.3],[99,7.9],[98.5,8.4],[98.3,7.8],[98.2,8.4],[98.3,9],[98.6,9.9],[99,11],[99.6,11.9],[99.2,12',
        '.8],[99.2,13.3],[99.1,13.8],[98.4,14.6],[98.2,15.1],[98.5,15.3],[98.9,16.2],[98.5,16.8],[97.9,17.6],[97.4,18.4],[97.8,18',
        '.6],[98.3,19.7],[99,19.8],[99.5,20.2],[100.1,20.4],[100.5,20.1],[100.6,19.5],[101.3,19.5],[101,18.4],[101.1,17.5],[102.1',
        ',18.1],[102.4,17.9],[103,18],[103.2,18.3],[104,18.2],[104.7,17.4],[104.8,16.4],[105.6,15.6],[105.5,14.7]]]]],["LAO","LA"',
        ',"LAO","418","Laos",102.5,19.4,["LAO","LA","418","Laos","Lao PDR"],[[[[107.4,14.2],[106.5,14.6],[106,13.9],[105.2,14.3],',
        '[105.5,14.7],[105.6,15.6],[104.8,16.4],[104.7,17.4],[104,18.2],[103.2,18.3],[103,18],[102.4,17.9],[102.1,18.1],[101.1,17',
        '.5],[101,18.4],[101.3,19.5],[100.6,19.5],[100.5,20.1],[100.1,20.4],[100.3,20.8],[101.2,21.4],[101.3,21.2],[101.8,21.2],[',
        '101.7,22.3],[102.2,22.5],[102.8,21.7],[103.2,20.8],[104.4,20.8],[104.8,19.9],[104.2,19.6],[103.9,19.3],[105.1,18.7],[105',
        '.9,17.5],[106.6,16.6],[107.3,15.9],[107.6,15.2]]]]],["MMR","MM","MMR","104","Myanmar",95.8,21.6,["MMR","MM","104","Myanm',
        'ar"],[[[[100.1,20.4],[99.5,20.2],[99,19.8],[98.3,19.7],[97.8,18.6],[97.4,18.4],[97.9,17.6],[98.5,16.8],[98.9,16.2],[98.5',
        ',15.3],[98.2,15.1],[98.4,14.6],[99.1,13.8],[99.2,13.3],[99.2,12.8],[99.6,11.9],[99,11],[98.6,9.9],[98.5,10.7],[98.8,11.4',
        '],[98.4,12],[98.5,13.1],[98.1,13.6],[97.8,14.8],[97.6,16.1],[97.2,16.9],[96.5,16.4],[95.4,15.7],[94.8,15.8],[94.2,16],[9',
        '4.5,17.3],[94.3,18.2],[93.5,19.4],[93.7,19.7],[93.1,19.9],[92.4,20.7],[92.3,21.5],[92.7,21.3],[92.7,22],[93.2,22.3],[93.',
        '1,22.7],[93.3,23],[93.3,24.1],[94.1,23.9],[94.6,24.7],[94.6,25.2],[95.2,26],[95.1,26.6],[96.4,27.3],[97.1,27.1],[97.1,27',
        '.7],[97.4,27.9],[97.3,28.3],[97.9,28.3],[98.2,27.7],[98.7,27.5],[98.7,26.7],[98.7,25.9],[97.7,25.1],[97.6,23.9],[98.7,24',
        '.1],[98.9,23.1],[99.5,22.9],[99.2,22.1],[100,21.7],[100.4,21.6],[101.2,21.8],[101.2,21.4],[100.3,20.8]]]]],["VNM","VN","',
        'VNM","704","Vietnam",105.4,21.7,["VNM","VN","704","Vietnam"],[[[[104.3,10.5],[105.2,10.9],[106.2,11],[105.8,11.6],[107.5',
        ',12.3],[107.6,13.5],[107.4,14.2],[107.6,15.2],[107.3,15.9],[106.6,16.6],[105.9,17.5],[105.1,18.7],[103.9,19.3],[104.2,19',
        '.6],[104.8,19.9],[104.4,20.8],[103.2,20.8],[102.8,21.7],[102.2,22.5],[102.7,22.7],[103.5,22.7],[104.5,22.8],[105.3,23.4]',
        ',[105.8,23],[106.7,22.8],[106.6,22.2],[107,21.8],[108.1,21.6],[106.7,20.7],[105.9,19.8],[105.7,19.1],[106.4,18],[107.4,1',
        '6.7],[108.3,16.1],[108.9,15.3],[109.3,13.4],[109.2,11.7],[108.4,11],[107.2,10.4],[106.4,9.5],[105.2,8.6],[104.8,9.2],[10',
        '5.1,9.9]]]]],["PRK","KP","PRK","408","North Korea",126.4,39.9,["PRK","KP","408","North Korea","Dem. Rep. Korea"],[[[[130',
        '.6,42.4],[130.8,42.2],[130.4,42.3],[130,41.9],[129.7,41.6],[129.7,40.9],[129.2,40.7],[129,40.5],[128.6,40.2],[128,40],[1',
        '27.5,39.8],[127.5,39.3],[127.4,39.2],[127.8,39.1],[128.3,38.6],[128.2,38.4],[127.8,38.3],[127.1,38.3],[126.7,37.8],[126.',
        '2,37.8],[126.2,37.7],[125.7,37.9],[125.6,37.8],[125.3,37.7],[125.2,37.9],[125,37.9],[124.7,38.1],[125,38.5],[125.2,38.7]',
        ',[125.1,38.8],[125.4,39.4],[125.3,39.6],[124.7,39.7],[124.3,39.9],[125.1,40.6],[126.2,41.1],[126.9,41.8],[127.3,41.5],[1',
        '28.2,41.5],[128.1,42],[129.6,42.4],[130,43]]]]],["KOR","KR","KOR","410","South Korea",128.1,36.4,["KOR","KR","410","Sout',
        'h Korea","Republic of Korea"],[[[[126.2,37.7],[126.2,37.8],[126.7,37.8],[127.1,38.3],[127.8,38.3],[128.2,38.4],[128.3,38',
        '.6],[129.2,37.4],[129.5,36.8],[129.5,35.6],[129.1,35.1],[128.2,34.9],[127.4,34.5],[126.5,34.4],[126.4,34.9],[126.6,35.7]',
        ',[126.1,36.7],[126.9,36.9]]]]],["MNG","MN","MNG","496","Mongolia",104.2,46,["MNG","MN","496","Mongolia"],[[[[87.8,49.3],',
        '[88.8,49.5],[90.7,50.3],[92.2,50.8],[93.1,50.5],[94.1,50.5],[94.8,50],[95.8,50],[97.3,49.7],[98.2,50.4],[97.8,51],[98.9,',
        '52],[100,51.6],[100.9,51.5],[102.1,51.3],[102.3,50.5],[103.7,50.1],[104.6,50.3],[105.9,50.4],[106.9,50.3],[107.9,49.8],[',
        '108.5,49.3],[109.4,49.3],[110.7,49.1],[111.6,49.4],[112.9,49.5],[114.4,50.2],[115,50.1],[115.5,49.8],[116.7,49.9],[116.2',
        ',49.1],[115.5,48.1],[115.7,47.7],[116.3,47.9],[117.3,47.7],[118.1,48.1],[118.9,47.7],[119.8,47],[119.7,46.7],[118.9,46.8',
        '],[117.4,46.7],[116.7,46.4],[116,45.7],[114.5,45.3],[113.5,44.8],[112.4,45],[111.9,45.1],[111.3,44.5],[111.7,44.1],[111.',
        '8,43.7],[111.1,43.4],[110.4,42.9],[109.2,42.5],[107.7,42.5],[106.1,42.1],[105,41.6],[104.5,41.9],[103.3,41.9],[101.8,42.',
        '5],[100.8,42.7],[99.5,42.5],[97.5,42.7],[96.3,42.7],[95.8,43.3],[95.3,44.2],[94.7,44.4],[93.5,45],[92.1,45.1],[90.9,45.3',
        '],[90.6,45.7],[91,46.9],[90.3,47.7],[88.9,48.1],[88,48.6]]]]],["IND","IN","IND","356","India",79.4,22.7,["IND","IN","356',
        '","India"],[[[[97.3,28.3],[97.4,27.9],[97.1,27.7],[97.1,27.1],[96.4,27.3],[95.1,26.6],[95.2,26],[94.6,25.2],[94.6,24.7],',
        '[94.1,23.9],[93.3,24.1],[93.3,23],[93.1,22.7],[93.2,22.3],[92.7,22],[92.1,23.6],[91.9,23.6],[91.7,23],[91.2,23.5],[91.5,',
        '24.1],[91.9,24.1],[92.4,25],[91.8,25.1],[90.9,25.1],[89.9,25.3],[89.8,26],[89.4,26],[88.6,26.4],[88.2,25.8],[88.9,25.2],',
        '[88.3,24.9],[88.1,24.5],[88.7,24.2],[88.5,23.6],[88.9,22.9],[89,22.1],[88.9,21.7],[88.2,21.7],[87,21.5],[87,20.7],[86.5,',
        '20.2],[85.1,19.5],[83.9,18.3],[83.2,17.7],[82.2,17],[82.2,16.6],[81.7,16.3],[80.8,16],[80.3,15.9],[80,15.1],[80.2,13.8],',
        '[80.3,13],[79.9,12.1],[79.9,10.4],[79.3,10.3],[78.9,9.5],[79.2,9.2],[78.3,8.9],[77.9,8.3],[77.5,8],[76.6,8.9],[76.1,10.3',
        '],[75.7,11.3],[75.4,11.8],[74.9,12.7],[74.6,14],[74.4,14.6],[73.5,16],[73.1,17.9],[72.8,19.2],[72.8,20.4],[72.6,21.4],[7',
        '1.2,20.8],[70.5,20.9],[69.2,22.1],[69.6,22.5],[69.3,22.8],[68.2,23.7],[68.8,24.4],[71,24.4],[70.8,25.2],[70.3,25.7],[70.',
        '2,26.5],[69.5,26.9],[70.6,28],[71.8,27.9],[72.8,29],[73.5,30],[74.4,31],[74.4,31.7],[75.3,32.3],[74.5,32.8],[74.1,33.4],',
        '[73.7,34.3],[74.2,34.7],[75.8,34.5],[76.9,34.7],[77.8,35.5],[78.9,34.3],[78.8,33.5],[79.2,33],[79.2,32.5],[78.5,32.6],[7',
        '8.7,31.5],[79.7,30.9],[81.1,30.2],[80.5,29.7],[80.1,28.8],[81.1,28.4],[82,27.9],[83.3,27.4],[84.7,27.2],[85.3,26.7],[86,',
        '26.6],[87.2,26.4],[88.1,26.4],[88.2,26.8],[88,27.4],[88.1,27.9],[88.7,28.1],[88.8,27.3],[88.8,27.1],[89.7,26.7],[90.4,26',
        '.9],[91.2,26.8],[92,26.8],[92.1,27.5],[91.7,27.8],[92.5,27.9],[93.4,28.6],[94.6,29.3],[95.4,29],[96.1,29.5],[96.6,28.8],',
        '[96.2,28.4]]]]],["BGD","BD","BGD","050","Bangladesh",89.7,24.2,["BGD","BD","050","Bangladesh"],[[[[92.7,22],[92.7,21.3],',
        '[92.3,21.5],[92.4,20.7],[92.1,21.2],[92,21.7],[91.8,22.2],[91.4,22.8],[90.5,22.8],[90.6,22.4],[90.3,21.8],[89.8,22],[89.',
        '7,21.9],[89.4,22],[89,22.1],[88.9,22.9],[88.5,23.6],[88.7,24.2],[88.1,24.5],[88.3,24.9],[88.9,25.2],[88.2,25.8],[88.6,26',
        '.4],[89.4,26],[89.8,26],[89.9,25.3],[90.9,25.1],[91.8,25.1],[92.4,25],[91.9,24.1],[91.5,24.1],[91.2,23.5],[91.7,23],[91.',
        '9,23.6],[92.1,23.6]]]]],["BTN","BT","BTN","064","Bhutan",90,27.5,["BTN","BT","064","Bhutan"],[[[[91.7,27.8],[92.1,27.5],',
        '[92,26.8],[91.2,26.8],[90.4,26.9],[89.7,26.7],[88.8,27.1],[88.8,27.3],[89.5,28],[90,28.3],[90.7,28.1],[91.3,28]]]]],["NP',
        'L","NP","NPL","524","Nepal",83.6,28.3,["NPL","NP","524","Nepal"],[[[[88.1,27.9],[88,27.4],[88.2,26.8],[88.1,26.4],[87.2,',
        '26.4],[86,26.6],[85.3,26.7],[84.7,27.2],[83.3,27.4],[82,27.9],[81.1,28.4],[80.1,28.8],[80.5,29.7],[81.1,30.2],[81.5,30.4',
        '],[82.3,30.1],[83.3,29.5],[83.9,29.3],[84.2,28.8],[85,28.6],[85.8,28.2],[87,28]]]]],["PAK","PK","PAK","586","Pakistan",6',
        '8.5,29.3,["PAK","PK","586","Pakistan"],[[[[77.8,35.5],[76.9,34.7],[75.8,34.5],[74.2,34.7],[73.7,34.3],[74.1,33.4],[74.5,',
        '32.8],[75.3,32.3],[74.4,31.7],[74.4,31],[73.5,30],[72.8,29],[71.8,27.9],[70.6,28],[69.5,26.9],[70.2,26.5],[70.3,25.7],[7',
        '0.8,25.2],[71,24.4],[68.8,24.4],[68.2,23.7],[67.4,23.9],[67.1,24.7],[66.4,25.4],[64.5,25.2],[62.9,25.2],[61.5,25.1],[61.',
        '9,26.2],[63.3,26.8],[63.2,27.2],[62.8,27.4],[62.7,28.3],[61.8,28.7],[61.4,29.3],[60.9,29.8],[62.5,29.3],[63.6,29.5],[64.',
        '1,29.3],[64.4,29.6],[65,29.5],[66.3,29.9],[66.4,30.7],[66.9,31.3],[67.7,31.3],[67.8,31.6],[68.6,31.7],[68.9,31.6],[69.3,',
        '31.9],[69.3,32.5],[69.7,33.1],[70.3,33.4],[69.9,34],[70.9,34],[71.2,34.3],[71.1,34.7],[71.6,35.2],[71.5,35.7],[71.3,36.1',
        '],[71.8,36.5],[72.9,36.7],[74.1,36.8],[74.6,37],[75.2,37.1],[75.9,36.7],[76.2,35.9]]]]],["AFG","AF","AFG","004","Afghani',
        'stan",66.5,34.2,["AFG","AF","004","Afghanistan"],[[[[66.5,37.4],[67.1,37.4],[67.8,37.1],[68.1,37],[68.9,37.3],[69.2,37.2',
        '],[69.5,37.6],[70.1,37.6],[70.3,37.7],[70.4,38.1],[70.8,38.5],[71.3,38.3],[71.2,38],[71.5,37.9],[71.4,37.1],[71.8,36.7],',
        '[72.2,36.9],[72.6,37],[73.3,37.5],[73.9,37.4],[75,37.4],[75.2,37.1],[74.6,37],[74.1,36.8],[72.9,36.7],[71.8,36.5],[71.3,',
        '36.1],[71.5,35.7],[71.6,35.2],[71.1,34.7],[71.2,34.3],[70.9,34],[69.9,34],[70.3,33.4],[69.7,33.1],[69.3,32.5],[69.3,31.9',
        '],[68.9,31.6],[68.6,31.7],[67.8,31.6],[67.7,31.3],[66.9,31.3],[66.4,30.7],[66.3,29.9],[65,29.5],[64.4,29.6],[64.1,29.3],',
        '[63.6,29.5],[62.5,29.3],[60.9,29.8],[61.8,30.7],[61.7,31.4],[60.9,31.5],[60.9,32.2],[60.5,33],[61,33.5],[60.5,33.7],[60.',
        '8,34.4],[61.2,35.7],[62.2,35.3],[63,35.4],[63.2,35.9],[64,36],[64.5,36.3],[64.7,37.1],[65.6,37.3],[65.7,37.7],[66.2,37.4',
        ']]]]],["TJK","TJ","TJK","762","Tajikistan",72.6,38.2,["TJK","TJ","762","Tajikistan"],[[[[67.8,37.1],[68.4,38.2],[68.2,38',
        '.9],[67.4,39.1],[67.7,39.6],[68.5,39.5],[69,40.1],[69.3,40.7],[70.7,41],[70.5,40.5],[70.6,40.2],[71,40.2],[70.6,39.9],[6',
        '9.6,40.1],[69.5,39.5],[70.5,39.6],[71.8,39.3],[73.7,39.4],[73.9,38.5],[74.3,38.6],[74.9,38.4],[74.8,38],[75,37.4],[73.9,',
        '37.4],[73.3,37.5],[72.6,37],[72.2,36.9],[71.8,36.7],[71.4,37.1],[71.5,37.9],[71.2,38],[71.3,38.3],[70.8,38.5],[70.4,38.1',
        '],[70.3,37.7],[70.1,37.6],[69.5,37.6],[69.2,37.2],[68.9,37.3],[68.1,37]]]]],["KGZ","KG","KGZ","417","Kyrgyzstan",74.5,41',
        '.7,["KGZ","KG","417","Kyrgyzstan"],[[[[71,42.3],[71.2,42.7],[71.8,42.8],[73.5,42.5],[73.6,43.1],[74.2,43.3],[75.6,42.9],',
        '[76,43],[77.7,43],[79.1,42.9],[79.6,42.5],[80.3,42.3],[80.1,42.1],[78.5,41.6],[78.2,41.2],[76.9,41.1],[76.5,40.4],[75.5,',
        '40.6],[74.8,40.4],[73.8,39.9],[74,39.7],[73.7,39.4],[71.8,39.3],[70.5,39.6],[69.5,39.5],[69.6,40.1],[70.6,39.9],[71,40.2',
        '],[71.8,40.1],[73.1,40.9],[71.9,41.4],[71.2,41.1],[70.4,41.5],[71.3,42.2]]]]],["TKM","TM","TKM","795","Turkmenistan",58.',
        '7,39.9,["TKM","TM","795","Turkmenistan"],[[[[52.5,41.8],[52.9,42.1],[54.1,42.3],[54.8,42],[55.5,41.3],[56,41.3],[57.1,41',
        '.3],[56.9,41.8],[57.8,42.2],[58.6,42.8],[60,42.2],[60.1,41.4],[60.5,41.2],[61.5,41.3],[61.9,41.1],[62.4,40.1],[63.5,39.4',
        '],[64.2,38.9],[65.2,38.4],[66.5,38],[66.5,37.4],[66.2,37.4],[65.7,37.7],[65.6,37.3],[64.7,37.1],[64.5,36.3],[64,36],[63.',
        '2,35.9],[63,35.4],[62.2,35.3],[61.2,35.7],[61.1,36.5],[60.4,36.5],[59.2,37.4],[58.4,37.5],[57.3,38],[56.6,38.1],[56.2,37',
        '.9],[55.5,38],[54.8,37.4],[53.9,37.2],[53.7,37.9],[53.9,39],[53.1,39.3],[53.4,40],[52.7,40],[52.9,40.9],[53.9,40.6],[54.',
        '7,41],[54,41.6],[53.7,42.1],[52.9,41.9],[52.8,41.1]]]]],["IRN","IR","IRN","364","Iran",54.9,32.2,["IRN","IR","364","Iran',
        '"],[[[[48.6,29.9],[48,30.5],[48,31],[47.7,31],[47.8,31.7],[47.3,32.5],[46.1,33],[45.4,34],[45.6,34.7],[46.2,35.1],[46.1,',
        '35.7],[45.4,36],[44.8,37.2],[44.2,38],[44.4,38.3],[44.1,39.4],[44.8,39.7],[45,39.3],[45.5,38.9],[46.1,38.7],[46.5,38.8],',
        '[47.7,39.5],[48.1,39.6],[48.4,39.3],[48,38.8],[48.6,38.3],[48.9,38.3],[49.2,37.6],[50.1,37.4],[50.8,36.9],[52.3,36.7],[5',
        '3.8,37],[53.9,37.2],[54.8,37.4],[55.5,38],[56.2,37.9],[56.6,38.1],[57.3,38],[58.4,37.5],[59.2,37.4],[60.4,36.5],[61.1,36',
        '.5],[61.2,35.7],[60.8,34.4],[60.5,33.7],[61,33.5],[60.5,33],[60.9,32.2],[60.9,31.5],[61.7,31.4],[61.8,30.7],[60.9,29.8],',
        '[61.4,29.3],[61.8,28.7],[62.7,28.3],[62.8,27.4],[63.2,27.2],[63.3,26.8],[61.9,26.2],[61.5,25.1],[59.6,25.4],[58.5,25.6],',
        '[57.4,25.7],[57,27],[56.5,27.1],[55.7,27],[54.7,26.5],[53.5,26.8],[52.5,27.6],[51.5,27.9],[50.9,28.8],[50.1,30.1],[49.6,',
        '30],[48.9,30.3]]]]],["SYR","SY","SYR","760","Syria",38.3,35,["SYR","SY","760","Syria"],[[[[35.7,32.7],[35.8,32.9],[35.8,',
        '33.3],[36.1,33.8],[36.6,34.2],[36.4,34.6],[36,34.6],[35.9,35.4],[36.1,35.8],[36.4,36],[36.7,36.3],[36.7,36.8],[37.1,36.6',
        '],[38.2,36.9],[38.7,36.7],[39.5,36.7],[40.7,37.1],[41.2,37.1],[42.3,37.2],[41.8,36.6],[41.3,36.4],[41.4,35.6],[41,34.4],',
        '[38.8,33.4],[36.8,32.3]]]]],["ARM","AM","ARM","051","Armenia",44.8,40.5,["ARM","AM","051","Armenia"],[[[[46.5,38.8],[46.',
        '1,38.7],[45.7,39.3],[45.7,39.5],[45.3,39.5],[45,39.7],[44.8,39.7],[44.4,40],[43.7,40.3],[43.8,40.7],[43.6,41.1],[45,41.2',
        '],[45.2,41],[45.6,40.8],[45.4,40.6],[45.9,40.2],[45.6,39.9],[46,39.6],[46.5,39.5]]]]],["SWE","SE","SWE","752","Sweden",1',
        '9,65.9,["SWE","SE","752","S","Sweden"],[[[[11,58.9],[11.5,59.4],[12.3,60.1],[12.6,61.3],[12,61.8],[11.9,63.1],[12.6,64.1',
        '],[13.6,64],[13.9,64.4],[13.6,64.8],[15.1,66.2],[16.1,67.3],[16.8,68],[17.7,68],[18,68.6],[19.9,68.4],[20,69.1],[20.6,69',
        '.1],[22,68.6],[23.5,67.9],[23.6,66.4],[23.9,66],[22.2,65.7],[21.2,65],[21.4,64.4],[19.8,63.6],[17.8,62.7],[17.1,61.3],[1',
        '7.8,60.6],[18.8,60.1],[17.9,59],[16.8,58.7],[16.4,57],[15.9,56.1],[14.7,56.2],[14.1,55.4],[12.9,55.4],[12.6,56.3],[11.8,',
        '57.4]]]]],["BLR","BY","BLR","112","Belarus",28.4,53.8,["BLR","BY","112","Belarus"],[[[[28.2,56.2],[29.2,55.9],[29.4,55.7',
        '],[29.9,55.8],[30.9,55.6],[31,55.1],[30.8,54.8],[31.4,54.2],[31.8,54],[31.7,53.8],[32.4,53.6],[32.7,53.4],[32.3,53.1],[3',
        '1.5,53.2],[31.3,53.1],[31.5,52.7],[31.8,52.1],[30.9,52],[30.6,51.8],[30.6,51.3],[30.2,51.4],[29.3,51.4],[29,51.6],[28.6,',
        '51.4],[28.2,51.6],[27.5,51.6],[26.3,51.8],[25.3,51.9],[24.6,51.9],[24,51.6],[23.5,51.6],[23.5,52],[23.2,52.5],[23.8,52.7',
        '],[23.8,53.1],[23.5,53.5],[23.5,53.9],[24.5,53.9],[25.5,54.3],[25.8,54.8],[26.6,55.2],[26.5,55.6],[27.1,55.8]]]]],["UKR"',
        ',"UA","UKR","804","Ukraine",32.1,49.7,["UKR","UA","804","Ukraine"],[[[[31.8,52.1],[32.2,52.1],[32.4,52.3],[32.7,52.2],[3',
        '3.8,52.3],[34.4,51.8],[34.1,51.6],[34.2,51.3],[35,51.2],[35.4,50.8],[35.4,50.6],[36.6,50.2],[37.4,50.4],[38,49.9],[38.6,',
        '49.9],[40.1,49.6],[40.1,49.3],[39.7,48.8],[39.9,48.2],[39.7,47.9],[38.8,47.8],[38.3,47.5],[38.2,47.1],[37.4,47],[36.8,46',
        '.7],[35.8,46.6],[35,46.3],[35,45.7],[34.9,45.8],[34.7,46],[34.4,46],[33.7,46.2],[33.4,46],[33.3,46.1],[31.7,46.3],[31.7,',
        '46.7],[30.7,46.6],[30.4,46],[29.6,45.3],[29.1,45.5],[28.7,45.3],[28.2,45.5],[28.5,45.6],[28.7,45.9],[28.9,46.3],[28.9,46',
        '.4],[29.1,46.5],[29.2,46.4],[29.8,46.3],[30,46.4],[29.8,46.5],[29.9,46.7],[29.6,46.9],[29.4,47.3],[29.1,47.5],[29.1,47.8',
        '],[28.7,48.1],[28.3,48.2],[27.5,48.5],[26.9,48.4],[26.6,48.2],[26.2,48.2],[25.9,48],[25.2,47.9],[24.9,47.7],[24.4,48],[2',
        '3.8,48],[23.1,48.1],[22.7,47.9],[22.6,48.2],[22.1,48.4],[22.3,48.8],[22.6,49.1],[22.8,49],[22.5,49.5],[23.4,50.3],[23.9,',
        '50.4],[24,50.7],[23.5,51.6],[24,51.6],[24.6,51.9],[25.3,51.9],[26.3,51.8],[27.5,51.6],[28.2,51.6],[28.6,51.4],[29,51.6],',
        '[29.3,51.4],[30.2,51.4],[30.6,51.3],[30.6,51.8],[30.9,52]]]]],["POL","PL","POL","616","Poland",19.5,52,["POL","PL","616"',
        ',"Poland"],[[[[23.5,53.9],[23.5,53.5],[23.8,53.1],[23.8,52.7],[23.2,52.5],[23.5,52],[23.5,51.6],[24,50.7],[23.9,50.4],[2',
        '3.4,50.3],[22.5,49.5],[22.8,49],[22.6,49.1],[21.6,49.5],[20.9,49.3],[20.4,49.4],[19.8,49.2],[19.3,49.6],[18.9,49.4],[18.',
        '9,49.5],[18.4,50],[17.6,50],[17.6,50.4],[16.9,50.5],[16.7,50.2],[16.2,50.4],[16.2,50.7],[15.5,50.8],[15,51.1],[14.6,51.7',
        '],[14.7,52.1],[14.4,52.6],[14.1,53],[14.4,53.2],[14.1,53.8],[14.8,54.1],[16.4,54.5],[17.6,54.9],[18.6,54.7],[18.7,54.4],',
        '[19.7,54.4],[20.9,54.3],[22.7,54.3],[23.2,54.2]]]]],["AUT","AT","AUT","040","Austria",14.1,47.5,["AUT","AT","040","A","A',
        'ustria"],[[[[17,48.1],[16.9,47.7],[16.3,47.7],[16.5,47.5],[16.2,46.9],[16,46.7],[15.1,46.7],[14.6,46.4],[13.8,46.5],[12.',
        '4,46.8],[12.2,47.1],[11.2,46.9],[11,46.8],[10.4,46.9],[9.9,46.9],[9.5,47.1],[9.6,47.3],[9.6,47.5],[9.9,47.6],[10.4,47.3]',
        ',[10.5,47.6],[11.4,47.5],[12.1,47.7],[12.6,47.7],[12.9,47.5],[13,47.6],[12.9,48.3],[13.2,48.4],[13.6,48.9],[14.3,48.6],[',
        '14.9,49],[15.3,49],[16,48.7],[16.5,48.8],[17,48.6],[16.9,48.5]]]]],["HUN","HU","HUN","348","Hungary",19.4,47.1,["HUN","H',
        'U","348","Hungary"],[[[[22.1,48.4],[22.6,48.2],[22.7,47.9],[22.1,47.7],[21.6,47],[21,46.3],[20.2,46.1],[19.6,46.2],[18.8',
        ',45.9],[18.5,45.8],[17.6,46],[16.9,46.4],[16.6,46.5],[16.4,46.8],[16.2,46.9],[16.5,47.5],[16.3,47.7],[16.9,47.7],[17,48.',
        '1],[17.5,47.9],[17.9,47.8],[18.7,47.9],[18.8,48.1],[19.2,48.1],[19.7,48.3],[19.8,48.2],[20.2,48.3],[20.5,48.6],[20.8,48.',
        '6],[21.9,48.3]]]]],["MDA","MD","MDA","498","Moldova",28.5,47.4,["MDA","MD","498","Moldova"],[[[[26.6,48.2],[26.9,48.4],[',
        '27.5,48.5],[28.3,48.2],[28.7,48.1],[29.1,47.8],[29.1,47.5],[29.4,47.3],[29.6,46.9],[29.9,46.7],[29.8,46.5],[30,46.4],[29',
        '.8,46.3],[29.2,46.4],[29.1,46.5],[28.9,46.4],[28.9,46.3],[28.7,45.9],[28.5,45.6],[28.2,45.5],[28.1,45.9],[28.2,46.4],[28',
        '.1,46.8],[27.6,47.4],[27.2,47.8],[26.9,48.1]]]]],["ROU","RO","ROU","642","Romania",25,45.7,["ROU","RO","642","ROM","Roma',
        'nia"],[[[[28.2,45.5],[28.7,45.3],[29.1,45.5],[29.6,45.3],[29.6,45],[29.1,44.8],[28.8,44.9],[28.6,43.7],[28,43.8],[27.2,4',
        '4.2],[26.1,43.9],[25.6,43.7],[24.1,43.7],[23.3,43.9],[22.9,43.8],[22.7,44.2],[22.5,44.4],[22.7,44.6],[22.5,44.7],[22.1,4',
        '4.5],[21.6,44.8],[21.5,45.2],[20.9,45.4],[20.8,45.7],[20.2,46.1],[21,46.3],[21.6,47],[22.1,47.7],[22.7,47.9],[23.1,48.1]',
        ',[23.8,48],[24.4,48],[24.9,47.7],[25.2,47.9],[25.9,48],[26.2,48.2],[26.6,48.2],[26.9,48.1],[27.2,47.8],[27.6,47.4],[28.1',
        ',46.8],[28.2,46.4],[28.1,45.9]]]]],["LTU","LT","LTU","440","Lithuania",24.1,55.1,["LTU","LT","440","Lithuania"],[[[[26.5',
        ',55.6],[26.6,55.2],[25.8,54.8],[25.5,54.3],[24.5,53.9],[23.5,53.9],[23.2,54.2],[22.7,54.3],[22.7,54.6],[22.8,54.9],[22.3',
        ',55],[21.3,55.2],[21.1,56],[22.2,56.3],[23.9,56.3],[24.9,56.4],[25,56.2],[25.5,56.1]]]]],["LVA","LV","LVA","428","Latvia',
        '",25.5,57.1,["LVA","LV","428","Latvia"],[[[[27.3,57.5],[27.8,57.2],[27.9,56.8],[28.2,56.2],[27.1,55.8],[26.5,55.6],[25.5',
        ',56.1],[25,56.2],[24.9,56.4],[23.9,56.3],[22.2,56.3],[21.1,56],[21.1,56.8],[21.6,57.4],[22.5,57.8],[23.3,57],[24.1,57],[',
        '24.3,57.8],[25.2,58],[25.6,57.8],[26.5,57.5]]]]],["EST","EE","EST","233","Estonia",25.9,58.7,["EST","EE","233","Estonia"',
        '],[[[[28,59.5],[28.1,59.3],[27.4,58.7],[27.7,57.8],[27.3,57.5],[26.5,57.5],[25.6,57.8],[25.2,58],[24.3,57.8],[24.4,58.4]',
        ',[24.1,58.3],[23.4,58.6],[23.3,59.2],[24.6,59.5],[25.9,59.6],[26.9,59.4]]]]],["DEU","DE","DEU","276","Germany",9.7,51,["',
        'DEU","DE","276","D","Germany"],[[[[14.1,53.8],[14.4,53.2],[14.1,53],[14.4,52.6],[14.7,52.1],[14.6,51.7],[15,51.1],[14.6,',
        '51],[14.3,51.1],[14.1,50.9],[13.3,50.7],[13,50.5],[12.2,50.3],[12.4,50],[12.5,49.5],[13,49.3],[13.6,48.9],[13.2,48.4],[1',
        '2.9,48.3],[13,47.6],[12.9,47.5],[12.6,47.7],[12.1,47.7],[11.4,47.5],[10.5,47.6],[10.4,47.3],[9.9,47.6],[9.6,47.5],[8.5,4',
        '7.8],[8.3,47.6],[7.5,47.6],[7.6,48.3],[8.1,49],[6.7,49.2],[6.2,49.5],[6.2,49.9],[6,50.1],[6.2,50.8],[6,51.9],[6.6,51.9],',
        '[6.8,52.2],[7.1,53.1],[6.9,53.5],[7.1,53.7],[7.9,53.7],[8.1,53.5],[8.8,54],[8.6,54.4],[8.5,55],[9.3,54.8],[9.9,55],[9.9,',
        '54.6],[11,54.4],[10.9,54],[12,54.2],[12.5,54.5],[13.6,54.1]]]]],["BGR","BG","BGR","100","Bulgaria",25.2,42.5,["BGR","BG"',
        ',"100","Bulgaria"],[[[[22.7,44.2],[22.9,43.8],[23.3,43.9],[24.1,43.7],[25.6,43.7],[26.1,43.9],[27.2,44.2],[28,43.8],[28.',
        '6,43.7],[28,43.3],[27.7,42.6],[28,42],[27.1,42.1],[26.1,41.8],[26.1,41.3],[25.2,41.2],[24.5,41.6],[23.7,41.3],[23,41.3],',
        '[22.9,42],[22.4,42.3],[22.5,42.5],[22.4,42.6],[22.6,42.9],[23,43.2],[22.5,43.6],[22.4,44]]]]],["GRC","GR","GRC","300","G',
        'reece",21.7,39.5,["GRC","GR","300","Greece"],[[[[26.3,35.3],[26.2,35],[24.7,34.9],[24.7,35.1],[23.5,35.3],[23.7,35.7],[2',
        '4.2,35.4],[25,35.4],[25.8,35.4],[25.7,35.2]]],[[[23,41.3],[23.7,41.3],[24.5,41.6],[25.2,41.2],[26.1,41.3],[26.1,41.8],[2',
        '6.6,41.6],[26.3,40.9],[26.1,40.8],[25.4,40.9],[24.9,40.9],[23.7,40.7],[24.4,40.1],[23.9,40],[23.3,40],[22.8,40.5],[22.6,',
        '40.3],[22.8,39.7],[23.4,39.2],[23,39],[23.5,38.5],[24,38.2],[24,37.7],[23.1,37.9],[23.4,37.4],[22.8,37.3],[23.2,36.4],[2',
        '2.5,36.4],[21.7,36.8],[21.3,37.6],[21.1,38.3],[20.7,38.8],[20.2,39.3],[20.2,39.6],[20.6,40.1],[20.7,40.4],[21,40.6],[21,',
        '40.8],[21.7,40.9],[22.1,41.1],[22.6,41.1],[22.8,41.3]]]]],["TUR","TR","TUR","792","Turkey",34.5,39.3,["TUR","TR","792","',
        'Turkey"],[[[[44.8,37.2],[44.3,37],[43.9,37.3],[42.8,37.4],[42.3,37.2],[41.2,37.1],[40.7,37.1],[39.5,36.7],[38.7,36.7],[3',
        '8.2,36.9],[37.1,36.6],[36.7,36.8],[36.7,36.3],[36.4,36],[36.1,35.8],[35.8,36.3],[36.2,36.7],[35.6,36.6],[34.7,36.8],[34,',
        '36.2],[32.5,36.1],[31.7,36.6],[30.6,36.7],[30.4,36.3],[29.7,36.1],[28.7,36.7],[27.6,36.7],[27,37.7],[26.3,38.2],[26.8,39',
        '],[26.2,39.5],[27.3,40.4],[28.8,40.5],[29.2,41.2],[31.1,41.1],[32.3,41.7],[33.5,42],[35.2,42],[36.9,41.3],[38.3,40.9],[3',
        '9.5,41.1],[40.4,41],[41.6,41.5],[42.6,41.6],[43.6,41.1],[43.8,40.7],[43.7,40.3],[44.4,40],[44.8,39.7],[44.1,39.4],[44.4,',
        '38.3],[44.2,38]]],[[[26.1,41.8],[27.1,42.1],[28,42],[28.1,41.6],[29,41.3],[28.8,41.1],[27.6,41],[27.2,40.7],[26.4,40.2],',
        '[26,40.6],[26.1,40.8],[26.3,40.9],[26.6,41.6]]]]],["ALB","AL","ALB","008","Albania",20.1,40.7,["ALB","AL","008","Albania',
        '"],[[[[21,40.8],[21,40.6],[20.7,40.4],[20.6,40.1],[20.2,39.6],[20,39.7],[20,39.9],[19.4,40.3],[19.3,40.7],[19.4,41.4],[1',
        '9.5,41.7],[19.4,41.9],[19.3,42.2],[19.7,42.7],[19.8,42.5],[20.1,42.6],[20.3,42.3],[20.5,42.2],[20.6,41.9],[20.5,41.5],[2',
        '0.6,41.1]]]]],["HRV","HR","HRV","191","Croatia",16.4,45.8,["HRV","HR","191","Croatia"],[[[[16.6,46.5],[16.9,46.4],[17.6,',
        '46],[18.5,45.8],[18.8,45.9],[19.1,45.5],[19.4,45.2],[19,44.9],[18.6,45.1],[17.9,45.1],[17,45.2],[16.5,45.2],[16.3,45],[1',
        '6,45.2],[15.8,44.8],[16.2,44.4],[16.5,44],[16.9,43.7],[17.3,43.4],[17.7,43],[18.6,42.7],[18.5,42.5],[17.5,42.8],[16.9,43',
        '.2],[16,43.5],[15.2,44.2],[15.4,44.3],[14.9,44.7],[14.9,45.1],[14.3,45.2],[14,44.8],[13.7,45.1],[13.7,45.5],[14.4,45.5],',
        '[14.6,45.6],[14.9,45.5],[15.3,45.5],[15.3,45.7],[15.7,45.8],[15.8,46.2]]]]],["CHE","CH","CHE","756","Switzerland",7.5,46',
        '.7,["CHE","CH","756","Switzerland"],[[[[9.6,47.5],[9.6,47.3],[9.5,47.1],[9.9,46.9],[10.4,46.9],[10.4,46.5],[9.9,46.3],[9',
        '.2,46.4],[9,46],[8.5,46],[8.3,46.2],[7.8,45.8],[7.3,45.8],[6.8,46],[6.5,46.4],[6,46.3],[6,46.7],[6.8,47.3],[6.7,47.5],[7',
        '.2,47.4],[7.5,47.6],[8.3,47.6],[8.5,47.8]]]]],["LUX","LU","LUX","442","Luxembourg",6.1,49.7,["LUX","LU","442","L","Luxem',
        'bourg"],[[[[6,50.1],[6.2,49.9],[6.2,49.5],[5.9,49.4],[5.7,49.5],[5.8,50.1]]]]],["BEL","BE","BEL","056","Belgium",4.8,50.',
        '8,["BEL","BE","056","B","Belgium"],[[[[6.2,50.8],[6,50.1],[5.8,50.1],[5.7,49.5],[4.8,50],[4.3,49.9],[3.6,50.4],[3.1,50.8',
        '],[2.7,50.8],[2.5,51.1],[3.3,51.3],[4,51.3],[5,51.5],[5.6,51]]]]],["NLD","NL","NLD","528","Netherlands",5.6,52.4,["NLD",',
        '"NL","528","NL1","Netherlands"],[[[[6.9,53.5],[7.1,53.1],[6.8,52.2],[6.6,51.9],[6,51.9],[6.2,50.8],[5.6,51],[5,51.5],[4,',
        '51.3],[3.3,51.3],[3.8,51.6],[4.7,53.1],[6.1,53.5]]]]],["PRT","PT","PRT","620","Portugal",-8.3,39.6,["PRT","PT","620","P"',
        ',"Portugal","PR1"],[[[[-9,41.9],[-8.7,42.1],[-8.3,42.3],[-8,41.8],[-7.4,41.8],[-7.3,41.9],[-6.7,41.9],[-6.4,41.4],[-6.9,',
        '41.1],[-6.9,40.3],[-7,40.2],[-7.1,39.7],[-7.5,39.6],[-7.1,39],[-7.4,38.4],[-7,38.1],[-7.2,37.8],[-7.5,37.4],[-7.5,37.1],',
        '[-7.9,36.8],[-8.4,37],[-8.9,36.9],[-8.7,37.7],[-8.8,38.3],[-9.3,38.4],[-9.5,38.7],[-9.4,39.4],[-9,39.8],[-9,40.2],[-8.8,',
        '40.8],[-8.8,41.2],[-9,41.5]]]]],["ESP","ES","ESP","724","Spain",-3.5,40.1,["ESP","ES","724","E","Spain"],[[[[-7.5,37.1],',
        '[-7.5,37.4],[-7.2,37.8],[-7,38.1],[-7.4,38.4],[-7.1,39],[-7.5,39.6],[-7.1,39.7],[-7,40.2],[-6.9,40.3],[-6.9,41.1],[-6.4,',
        '41.4],[-6.7,41.9],[-7.3,41.9],[-7.4,41.8],[-8,41.8],[-8.3,42.3],[-8.7,42.1],[-9,41.9],[-9,42.6],[-9.4,43],[-8,43.7],[-6.',
        '8,43.6],[-5.4,43.6],[-4.3,43.4],[-3.5,43.5],[-1.9,43.4],[-1.5,43],[0.3,42.6],[0.7,42.8],[1.8,42.3],[3,42.5],[3,41.9],[2.',
        '1,41.2],[0.8,41],[0.7,40.7],[0.1,40.1],[-0.3,39.3],[0.1,38.7],[-0.5,38.3],[-0.7,37.6],[-1.4,37.4],[-2.1,36.7],[-3.4,36.7',
        '],[-4.4,36.7],[-5,36.3],[-5.4,35.9],[-5.9,36],[-6.2,36.4],[-6.5,36.9]]]]],["IRL","IE","IRL","372","Ireland",-7.8,53.1,["',
        'IRL","IE","372","Ireland"],[[[[-6.2,53.9],[-6,53.2],[-6.8,52.3],[-8.6,51.7],[-10,51.8],[-9.2,52.9],[-9.7,53.9],[-8.3,54.',
        '7],[-7.6,55.1],[-7.4,54.6],[-7.6,54.1],[-7,54.1]]]]],["NCL","NC","NCL","540","New Caledonia",165.1,-21.1,["NCL","NC","54',
        '0","FR1","New Caledonia","France"],[[[[165.8,-21.1],[166.6,-21.7],[167.1,-22.2],[166.7,-22.4],[166.2,-22.1],[165.5,-21.7',
        '],[164.8,-21.1],[164.2,-20.4],[164,-20.1],[164.5,-20.1],[165,-20.5],[165.5,-20.8]]]]],["SLB","SB","SLB","090","Solomon I',
        'slands",159.2,-8,["SLB","SB","090","Solomon Is.","Solomon Islands"],[[[[162.1,-10.5],[162.4,-10.8],[161.7,-10.8],[161.3,',
        '-10.2],[161.9,-10.4]]],[[[161.7,-9.6],[161.5,-9.8],[160.8,-8.9],[160.6,-8.3],[160.9,-8.3],[161.3,-9.1]]],[[[160.9,-9.9],',
        '[160.5,-9.9],[159.8,-9.8],[159.6,-9.6],[159.7,-9.2],[160.4,-9.4],[160.7,-9.6]]],[[[159.6,-8],[159.9,-8.3],[159.9,-8.5],[',
        '159.1,-8.1],[158.6,-7.8],[158.2,-7.4],[158.4,-7.3],[158.8,-7.6]]],[[[157.1,-7],[157.5,-7.3],[157.3,-7.4],[156.9,-7.2],[1',
        '56.5,-6.8],[156.5,-6.6]]]]],["NZL","NZ","NZL","554","New Zealand",172.8,-39.8,["NZL","NZ","554","NZ1","New Zealand"],[[[',
        '[176.9,-40.1],[176.5,-40.6],[176,-41.3],[175.2,-41.7],[175.1,-41.4],[174.7,-41.3],[175.2,-40.5],[174.9,-39.9],[173.8,-39',
        '.5],[173.9,-39.1],[174.6,-38.8],[174.7,-38],[174.7,-37.4],[174.3,-36.7],[174.3,-36.5],[173.8,-36.1],[173.1,-35.2],[172.6',
        ',-34.5],[173,-34.5],[173.6,-35],[174.3,-35.3],[174.6,-36.2],[175.3,-37.2],[175.4,-36.5],[175.8,-36.8],[176,-37.6],[176.8',
        ',-37.9],[177.4,-38],[178,-37.6],[178.5,-37.7],[178.3,-38.6],[178,-39.2],[177.2,-39.1],[176.9,-39.4],[177,-39.9]]],[[[169',
        '.7,-43.6],[170.5,-43],[171.1,-42.5],[171.6,-41.8],[171.9,-41.5],[172.1,-41],[172.8,-40.5],[173,-40.9],[173.2,-41.3],[174',
        ',-40.9],[174.2,-41.3],[174.2,-41.8],[173.9,-42.2],[173.2,-43],[172.7,-43.4],[173.1,-43.9],[172.3,-43.9],[171.5,-44.2],[1',
        '71.2,-44.9],[170.6,-45.9],[169.8,-46.4],[169.3,-46.6],[168.4,-46.6],[167.8,-46.3],[166.7,-46.2],[166.5,-45.9],[167,-45.1',
        '],[168.3,-44.1],[168.9,-43.9]]]]],["AUS","AU","AUS","036","Australia",134,-24.1,["AUS","AU","036","AU1","Australia"],[[[',
        '[147.7,-40.8],[148.3,-40.9],[148.4,-42.1],[148,-42.4],[147.9,-43.2],[147.6,-42.9],[146.9,-43.6],[146.7,-43.6],[146,-43.5',
        '],[145.4,-42.7],[145.3,-42],[144.7,-41.2],[144.7,-40.7],[145.4,-40.8],[146.4,-41.1],[146.9,-41]]],[[[126.1,-32.2],[125.1',
        ',-32.7],[124.2,-33],[124,-33.5],[123.7,-33.9],[122.8,-33.9],[122.2,-34],[121.3,-33.8],[120.6,-33.9],[119.9,-34],[119.3,-',
        '34.5],[119,-34.5],[118.5,-34.7],[118,-35.1],[117.3,-35],[116.6,-35],[115.6,-34.4],[115,-34.2],[115,-33.6],[115.5,-33.5],',
        '[115.7,-33.3],[115.7,-32.9],[115.8,-32.2],[115.7,-31.6],[115.2,-30.6],[115,-30],[115,-29.5],[114.6,-28.8],[114.6,-28.5],',
        '[114.2,-28.1],[114,-27.3],[113.5,-26.5],[113.3,-26.1],[113.8,-26.5],[113.4,-25.6],[113.9,-25.9],[114.2,-26.3],[114.2,-25',
        '.8],[113.7,-25],[113.6,-24.7],[113.4,-24.4],[113.5,-23.8],[113.7,-23.6],[113.8,-23.1],[113.7,-22.5],[114.1,-21.8],[114.2',
        ',-22.5],[114.6,-21.8],[115.5,-21.5],[115.9,-21.1],[116.7,-20.7],[117.2,-20.6],[117.4,-20.7],[118.2,-20.4],[118.8,-20.3],',
        '[119,-20],[119.3,-20],[119.8,-20],[120.9,-19.7],[121.4,-19.2],[121.7,-18.7],[122.2,-18.2],[122.3,-17.8],[122.3,-17.3],[1',
        '23,-16.4],[123.4,-17.3],[123.9,-17.1],[123.5,-16.6],[123.8,-16.1],[124.3,-16.3],[124.4,-15.6],[124.9,-15.1],[125.2,-14.7',
        '],[125.7,-14.5],[125.7,-14.2],[126.1,-14.3],[126.1,-14.1],[126.6,-14],[127.1,-13.8],[127.8,-14.3],[128.4,-14.9],[129,-14',
        '.9],[129.6,-15],[129.4,-14.4],[129.9,-13.6],[130.3,-13.4],[130.2,-13.1],[130.6,-12.5],[131.2,-12.2],[131.7,-12.3],[132.6',
        ',-12.1],[132.6,-11.6],[131.8,-11.3],[132.4,-11.1],[133,-11.4],[133.6,-11.8],[134.4,-12],[134.7,-11.9],[135.3,-12.2],[135',
        '.9,-12],[136.3,-12],[136.5,-11.9],[137,-12.4],[136.7,-12.9],[136.3,-13.3],[136,-13.3],[136.1,-13.7],[135.8,-14.2],[135.4',
        ',-14.7],[135.5,-15],[136.3,-15.6],[137.1,-15.9],[137.6,-16.2],[138.3,-16.8],[138.6,-16.8],[139.1,-17.1],[139.3,-17.4],[1',
        '40.2,-17.7],[140.9,-17.4],[141.1,-16.8],[141.3,-16.4],[141.4,-15.8],[141.7,-15],[141.6,-14.6],[141.6,-14.3],[141.5,-13.7',
        '],[141.7,-12.9],[141.8,-12.7],[141.7,-12.4],[141.9,-11.9],[142.1,-11.3],[142.1,-11],[142.5,-10.7],[142.8,-11.2],[142.9,-',
        '11.8],[143.1,-11.9],[143.2,-12.3],[143.5,-12.8],[143.6,-13.4],[143.6,-13.8],[143.9,-14.5],[144.6,-14.2],[144.9,-14.6],[1',
        '45.4,-15],[145.3,-15.4],[145.5,-16.3],[145.6,-16.8],[145.9,-16.9],[146.2,-17.8],[146.1,-18.3],[146.4,-19],[147.5,-19.5],',
        '[148.2,-20],[148.8,-20.4],[148.7,-20.6],[149.3,-21.3],[149.7,-22.3],[150.1,-22.1],[150.5,-22.6],[150.7,-22.4],[150.9,-23',
        '.5],[151.6,-24.1],[152.1,-24.5],[152.9,-25.3],[153.1,-26.1],[153.2,-26.6],[153.1,-27.3],[153.6,-28.1],[153.5,-29],[153.3',
        ',-29.5],[153.1,-30.4],[153.1,-30.9],[152.9,-31.6],[152.5,-32.6],[151.7,-33],[151.3,-33.8],[151,-34.3],[150.7,-35.2],[150',
        '.3,-35.7],[150.1,-36.4],[149.9,-37.1],[150,-37.4],[149.4,-37.8],[148.3,-37.8],[147.4,-38.2],[146.9,-38.6],[146.3,-39],[1',
        '45.5,-38.6],[144.9,-38.4],[145,-37.9],[144.5,-38.1],[143.6,-38.8],[142.7,-38.5],[142.2,-38.4],[141.6,-38.3],[140.6,-38],',
        '[140,-37.4],[139.8,-36.6],[139.6,-36.1],[139.1,-35.7],[138.1,-35.6],[138.4,-35.1],[138.2,-34.4],[137.7,-35.1],[136.8,-35',
        '.3],[137.4,-34.7],[137.5,-34.1],[137.9,-33.6],[137.8,-32.9],[137,-33.8],[136.4,-34.1],[136,-34.9],[135.2,-34.5],[135.2,-',
        '33.9],[134.6,-33.2],[134.1,-32.8],[134.3,-32.6],[133,-32],[132.3,-32],[131.3,-31.5],[129.5,-31.6],[128.2,-31.9],[127.1,-',
        '32.3]]]]],["LKA","LK","LKA","144","Sri Lanka",80.7,7.6,["LKA","LK","144","Sri Lanka"],[[[[81.8,7.5],[81.6,6.5],[81.2,6.2',
        '],[80.3,6],[79.9,6.8],[79.7,8.2],[80.1,9.8],[80.8,9.3],[81.3,8.6]]]]],["CHN","CN","CHN","156","People\'s Republic of Chin',
        'a",106.3,32.5,["CHN","CN","156","CH1","China","People\'s Republic of China"],[[[[109.5,18.2],[108.7,18.5],[108.6,19.4],[1',
        '09.1,19.8],[110.2,20.1],[110.8,20.1],[111,19.7],[110.6,19.3],[110.3,18.7]]],[[[80.3,42.3],[80.2,42.9],[80.9,43.2],[80,44',
        '.9],[81.9,45.3],[82.5,45.5],[83.2,47.3],[85.2,47],[85.7,47.5],[85.8,48.5],[86.6,48.5],[87.4,49.2],[87.8,49.3],[88,48.6],',
        '[88.9,48.1],[90.3,47.7],[91,46.9],[90.6,45.7],[90.9,45.3],[92.1,45.1],[93.5,45],[94.7,44.4],[95.3,44.2],[95.8,43.3],[96.',
        '3,42.7],[97.5,42.7],[99.5,42.5],[100.8,42.7],[101.8,42.5],[103.3,41.9],[104.5,41.9],[105,41.6],[106.1,42.1],[107.7,42.5]',
        ',[109.2,42.5],[110.4,42.9],[111.1,43.4],[111.8,43.7],[111.7,44.1],[111.3,44.5],[111.9,45.1],[112.4,45],[113.5,44.8],[114',
        '.5,45.3],[116,45.7],[116.7,46.4],[117.4,46.7],[118.9,46.8],[119.7,46.7],[119.8,47],[118.9,47.7],[118.1,48.1],[117.3,47.7',
        '],[116.3,47.9],[115.7,47.7],[115.5,48.1],[116.2,49.1],[116.7,49.9],[117.9,49.5],[119.3,50.1],[119.3,50.6],[120.2,51.6],[',
        '120.7,52],[120.7,52.5],[120.2,52.8],[121,53.3],[122.2,53.4],[123.6,53.5],[125.1,53.2],[125.9,52.8],[126.6,51.8],[126.9,5',
        '1.4],[127.3,50.7],[127.7,49.8],[129.4,49.4],[130.6,48.7],[131,47.8],[132.5,47.8],[133.4,48.2],[135,48.5],[134.5,47.6],[1',
        '34.1,47.2],[133.8,46.1],[133.1,45.1],[131.9,45.3],[131,45],[131.3,44.1],[131.1,42.9],[130.6,42.9],[130.6,42.4],[130,43],',
        '[129.6,42.4],[128.1,42],[128.2,41.5],[127.3,41.5],[126.9,41.8],[126.2,41.1],[125.1,40.6],[124.3,39.9],[122.9,39.6],[122.',
        '1,39.2],[121.1,38.9],[121.6,39.4],[121.4,39.8],[122.2,40.4],[121.6,40.9],[120.8,40.6],[119.6,39.9],[119,39.3],[118,39.2]',
        ',[117.5,38.7],[118.1,38.1],[118.9,37.9],[118.9,37.4],[119.7,37.2],[120.8,37.9],[121.7,37.5],[122.4,37.5],[122.5,36.9],[1',
        '21.1,36.7],[120.6,36.1],[119.7,35.6],[119.2,34.9],[120.2,34.4],[120.6,33.4],[121.2,32.5],[121.9,31.7],[121.9,30.9],[121.',
        '3,30.7],[121.5,30.1],[122.1,29.8],[121.9,29],[121.7,28.2],[121.1,28.1],[120.4,27.1],[119.6,25.7],[118.7,24.5],[117.3,23.',
        '6],[115.9,22.8],[114.8,22.7],[114.2,22.2],[113.8,22.5],[113.2,22.1],[111.8,21.6],[110.8,21.4],[110.4,20.3],[109.9,20.3],',
        '[109.6,21],[109.9,21.4],[108.5,21.7],[108.1,21.6],[107,21.8],[106.6,22.2],[106.7,22.8],[105.8,23],[105.3,23.4],[104.5,22',
        '.8],[103.5,22.7],[102.7,22.7],[102.2,22.5],[101.7,22.3],[101.8,21.2],[101.3,21.2],[101.2,21.4],[101.2,21.8],[100.4,21.6]',
        ',[100,21.7],[99.2,22.1],[99.5,22.9],[98.9,23.1],[98.7,24.1],[97.6,23.9],[97.7,25.1],[98.7,25.9],[98.7,26.7],[98.7,27.5],',
        '[98.2,27.7],[97.9,28.3],[97.3,28.3],[96.2,28.4],[96.6,28.8],[96.1,29.5],[95.4,29],[94.6,29.3],[93.4,28.6],[92.5,27.9],[9',
        '1.7,27.8],[91.3,28],[90.7,28.1],[90,28.3],[89.5,28],[88.8,27.3],[88.7,28.1],[88.1,27.9],[87,28],[85.8,28.2],[85,28.6],[8',
        '4.2,28.8],[83.9,29.3],[83.3,29.5],[82.3,30.1],[81.5,30.4],[81.1,30.2],[79.7,30.9],[78.7,31.5],[78.5,32.6],[79.2,32.5],[7',
        '9.2,33],[78.8,33.5],[78.9,34.3],[77.8,35.5],[76.2,35.9],[75.9,36.7],[75.2,37.1],[75,37.4],[74.8,38],[74.9,38.4],[74.3,38',
        '.6],[73.9,38.5],[73.7,39.4],[74,39.7],[73.8,39.9],[74.8,40.4],[75.5,40.6],[76.5,40.4],[76.9,41.1],[78.2,41.2],[78.5,41.6',
        '],[80.1,42.1]]]]],["TWN","CN-TW","TWN","158","Taiwan",120.9,23.7,["TWN","CN-TW","158","TW","Taiwan"],[[[[121.8,24.4],[12',
        '1.2,22.8],[120.7,22],[120.2,22.8],[120.1,23.6],[120.7,24.5],[121.5,25.3],[122,25]]]]],["ITA","IT","ITA","380","Italy",11',
        '.1,44.7,["ITA","IT","380","I","Italy"],[[[[10.4,46.9],[11,46.8],[11.2,46.9],[12.2,47.1],[12.4,46.8],[13.8,46.5],[13.7,46',
        '],[13.9,45.6],[13.1,45.7],[12.3,45.4],[12.4,44.9],[12.3,44.6],[12.6,44.1],[13.5,43.6],[14,42.8],[15.1,42],[15.9,42],[16.',
        '2,41.7],[15.9,41.5],[16.8,41.2],[17.5,40.9],[18.4,40.4],[18.5,40.2],[18.3,39.8],[17.7,40.3],[16.9,40.4],[16.4,39.8],[17.',
        '2,39.4],[17.1,38.9],[16.6,38.8],[16.1,38],[15.7,37.9],[15.7,38.2],[15.9,38.8],[16.1,39],[15.7,39.5],[15.4,40],[15,40.2],',
        '[14.7,40.6],[14.1,40.8],[13.6,41.2],[12.9,41.3],[12.1,41.7],[11.2,42.4],[10.5,42.9],[10.2,43.9],[9.7,44],[8.9,44.4],[8.4',
        ',44.2],[7.9,43.8],[7.4,43.7],[7.5,44.1],[7,44.3],[6.7,45],[7.1,45.3],[6.8,45.7],[6.8,46],[7.3,45.8],[7.8,45.8],[8.3,46.2',
        '],[8.5,46],[9,46],[9.2,46.4],[9.9,46.3],[10.4,46.5]]],[[[14.8,38.1],[15.5,38.2],[15.2,37.4],[15.3,37.1],[15.1,36.6],[14.',
        '3,37],[13.8,37.1],[12.4,37.6],[12.6,38.1],[13.7,38]]],[[[8.7,40.9],[9.2,41.2],[9.8,40.5],[9.7,39.2],[9.2,39.2],[8.8,38.9',
        '],[8.4,39.2],[8.4,40.4],[8.2,41]]]]],["DNK","DK","DNK","208","Denmark",9,56,["DNK","DK","208","DN1","Denmark"],[[[[9.9,5',
        '5],[9.3,54.8],[8.5,55],[8.1,55.5],[8.1,56.5],[8.3,56.8],[8.5,57.1],[9.4,57.2],[9.8,57.4],[10.6,57.7],[10.5,57.2],[10.3,5',
        '6.9],[10.4,56.6],[10.9,56.5],[10.7,56.1],[10.4,56.2],[9.6,55.5]]],[[[12.4,56.1],[12.7,55.6],[12.1,54.8],[11,55.4],[10.9,',
        '55.8]]]]],["GBR","GB","GBR","826","United Kingdom",-2.1,54.4,["GBR","GB","826","GB1","United Kingdom"],[[[[-6.2,53.9],[-',
        '7,54.1],[-7.6,54.1],[-7.4,54.6],[-7.6,55.1],[-6.7,55.2],[-5.7,54.6]]],[[[-3.1,53.4],[-2.9,54],[-3.6,54.6],[-4.8,54.8],[-',
        '5.1,55.1],[-4.7,55.5],[-5,55.8],[-5.6,55.3],[-5.6,56.3],[-6.1,56.8],[-5.8,57.8],[-5,58.6],[-4.2,58.6],[-3,58.6],[-4.1,57',
        '.6],[-3.1,57.7],[-2,57.7],[-2.2,56.9],[-3.1,56],[-2.1,55.9],[-2,55.8],[-1.1,54.6],[-0.4,54.5],[0.2,53.3],[0.5,52.9],[1.7',
        ',52.7],[1.6,52.1],[1.1,51.8],[1.4,51.3],[0.6,50.8],[-0.8,50.8],[-2.5,50.5],[-3,50.7],[-3.6,50.2],[-4.5,50.3],[-5.2,50],[',
        '-5.8,50.2],[-4.3,51.2],[-3.4,51.4],[-5,51.6],[-5.3,52],[-4.2,52.3],[-4.8,52.8],[-4.6,53.5]]]]],["ISL","IS","ISL","352","',
        'Iceland",-18.7,64.8,["ISL","IS","352","Iceland"],[[[[-14.5,66.5],[-14.7,65.8],[-13.6,65.1],[-14.9,64.4],[-17.8,63.7],[-1',
        '8.7,63.5],[-20,63.6],[-22.8,64],[-21.8,64.4],[-24,64.9],[-22.2,65.1],[-22.2,65.4],[-24.3,65.6],[-23.7,66.3],[-22.1,66.4]',
        ',[-20.6,65.7],[-19.1,66.3],[-17.8,66],[-16.2,66.5]]]]],["AZE","AZ","AZE","031","Azerbaijan",47.2,40.4,["AZE","AZ","031",',
        '"Azerbaijan"],[[[[46.4,41.9],[46.7,41.8],[47.4,41.2],[47.8,41.2],[48,41.4],[48.6,41.8],[49.1,41.3],[49.6,40.6],[50.1,40.',
        '5],[50.4,40.3],[49.6,40.2],[49.4,39.4],[49.2,39],[48.9,38.8],[48.9,38.3],[48.6,38.3],[48,38.8],[48.4,39.3],[48.1,39.6],[',
        '47.7,39.5],[46.5,38.8],[46.5,39.5],[46,39.6],[45.6,39.9],[45.9,40.2],[45.4,40.6],[45.6,40.8],[45.2,41],[45,41.2],[45.2,4',
        '1.4],[46,41.1],[46.5,41.1],[46.6,41.2],[46.1,41.7]]],[[[46.1,38.7],[45.5,38.9],[45,39.3],[44.8,39.7],[45,39.7],[45.3,39.',
        '5],[45.7,39.5],[45.7,39.3]]]]],["GEO","GE","GEO","268","Georgia",43.7,41.9,["GEO","GE","268","Georgia"],[[[[40,43.4],[40',
        '.1,43.6],[40.9,43.4],[42.4,43.2],[43.8,42.7],[43.9,42.6],[44.5,42.7],[45.5,42.5],[45.8,42.1],[46.4,41.9],[46.1,41.7],[46',
        '.6,41.2],[46.5,41.1],[46,41.1],[45.2,41.4],[45,41.2],[43.6,41.1],[42.6,41.6],[41.6,41.5],[41.7,42],[41.5,42.6],[40.9,43]',
        ',[40.3,43.1]]]]],["PHL","PH","PHL","608","Philippines",122.5,11.2,["PHL","PH","608","Philippines"],[[[[120.8,12.7],[120.',
        '3,13.5],[121.2,13.4],[121.5,13.1],[121.3,12.2]]],[[[122.6,10],[122.8,10.3],[122.9,10.9],[123.5,10.9],[123.3,10.3],[124.1',
        ',11.2],[124,10.3],[123.6,10],[123.3,9.3],[123,9],[122.4,9.7]]],[[[126.4,8.4],[126.5,7.8],[126.5,7.2],[126.2,6.3],[125.8,',
        '7.3],[125.4,6.8],[125.7,6],[125.4,5.6],[124.2,6.2],[123.9,6.9],[124.2,7.4],[123.6,7.8],[123.3,7.4],[122.8,7.5],[122.1,6.',
        '9],[121.9,7.2],[122.3,8],[122.9,8.3],[123.5,8.7],[123.8,8.2],[124.6,8.5],[124.8,9],[125.5,9],[125.4,9.8],[126.2,9.3],[12',
        '6.3,8.8]]],[[[118.5,9.3],[117.2,8.4],[117.7,9.1],[118.4,9.7],[119,10.4],[119.5,11.4],[119.7,10.6],[119,10]]],[[[122.3,18',
        '.2],[122.2,17.8],[122.5,17.1],[122.3,16.3],[121.7,15.9],[121.5,15.1],[121.7,14.3],[122.3,14.2],[122.7,14.3],[124,13.8],[',
        '123.9,13.2],[124.2,13],[124.1,12.5],[123.3,13],[122.9,13.6],[122.7,13.2],[122,13.8],[121.1,13.6],[120.6,13.9],[120.7,14.',
        '3],[121,14.5],[120.7,14.8],[120.6,14.4],[120.1,15],[119.9,15.4],[119.9,16.4],[120.3,16],[120.4,17.6],[120.7,18.5],[121.3',
        ',18.5],[121.9,18.2],[122.2,18.5]]],[[[122,11.4],[121.9,11.9],[122.5,11.6],[123.1,11.6],[123.1,11.2],[122.6,10.7],[122,10',
        '.4],[122,10.9]]],[[[125.5,12.2],[125.8,11],[125,11.3],[125,11],[125.3,10.4],[124.8,10.1],[124.8,10.8],[124.5,10.9],[124.',
        '3,11.5],[124.9,11.4],[124.9,11.8],[124.3,12.6],[125.2,12.5]]]]],["MYS","MY","MYS","458","Malaysia",113.8,2.5,["MYS","MY"',
        ',"458","Malaysia"],[[[[100.1,6.5],[100.3,6.6],[101.1,6.2],[101.2,5.7],[101.8,5.8],[102.1,6.2],[102.4,6.1],[103,5.5],[103',
        '.4,4.9],[103.4,4.2],[103.3,3.7],[103.4,3.4],[103.5,2.8],[103.9,2.5],[104.2,1.6],[104.2,1.3],[103.5,1.2],[102.6,2],[101.4',
        ',2.8],[101.3,3.3],[100.7,3.9],[100.6,4.8],[100.2,5.3],[100.3,6]]],[[[117.9,4.1],[117,4.3],[115.9,4.3],[115.5,3.2],[115.1',
        ',2.8],[114.6,1.4],[113.8,1.2],[112.9,1.5],[112.4,1.4],[111.8,0.9],[111.2,1],[110.5,0.8],[109.8,1.3],[109.7,2],[110.4,1.7',
        '],[111.2,1.9],[111.4,2.7],[111.8,2.9],[113,3.1],[113.7,3.9],[114.2,4.5],[114.7,4],[114.9,4.3],[115.3,4.3],[115.4,5],[115',
        '.5,5.4],[116.2,6.1],[116.7,6.9],[117.1,6.9],[117.6,6.4],[117.7,6],[118.3,5.7],[119.2,5.4],[119.1,5],[118.4,5],[118.6,4.5',
        ']]]]],["BRN","BN","BRN","096","Brunei",114.6,4.4,["BRN","BN","096","Brunei","Brunei Darussalam"],[[[[115.5,5.4],[115.4,5',
        '],[115.3,4.3],[114.9,4.3],[114.7,4],[114.2,4.5],[114.6,4.9]]]]],["SVN","SI","SVN","705","Slovenia",14.9,46.1,["SVN","SI"',
        ',"705","SLO","Slovenia"],[[[[13.8,46.5],[14.6,46.4],[15.1,46.7],[16,46.7],[16.2,46.9],[16.4,46.8],[16.6,46.5],[15.8,46.2',
        '],[15.7,45.8],[15.3,45.7],[15.3,45.5],[14.9,45.5],[14.6,45.6],[14.4,45.5],[13.7,45.5],[13.9,45.6],[13.7,46]]]]],["FIN","',
        'FI","FIN","246","Finland",27.3,63.3,["FIN","FI","246","FI1","Finland"],[[[[28.6,69.1],[28.4,68.4],[30,67.7],[29.1,66.9],',
        '[30.2,65.8],[29.5,64.9],[30.4,64.2],[30,63.6],[31.5,62.9],[31.1,62.4],[30.2,61.8],[28.1,60.5],[26.3,60.4],[24.5,60.1],[2',
        '2.9,59.8],[22.3,60.4],[21.3,60.7],[21.5,61.7],[21.1,62.6],[21.5,63.2],[22.4,63.8],[24.7,64.9],[25.4,65.1],[25.3,65.5],[2',
        '3.9,66],[23.6,66.4],[23.5,67.9],[22,68.6],[20.6,69.1],[21.2,69.4],[22.4,68.8],[23.7,68.9],[24.7,68.6],[25.7,69.1],[26.2,',
        '69.8],[27.7,70.2],[29,69.8]]]]],["SVK","SK","SVK","703","Slovakia",19,48.7,["SVK","SK","703","Slovakia"],[[[[22.6,49.1],',
        '[22.3,48.8],[22.1,48.4],[21.9,48.3],[20.8,48.6],[20.5,48.6],[20.2,48.3],[19.8,48.2],[19.7,48.3],[19.2,48.1],[18.8,48.1],',
        '[18.7,47.9],[17.9,47.8],[17.5,47.9],[17,48.1],[16.9,48.5],[17,48.6],[17.1,48.8],[17.5,48.8],[17.9,48.9],[17.9,49],[18.1,',
        '49],[18.2,49.3],[18.4,49.3],[18.6,49.5],[18.9,49.5],[18.9,49.4],[19.3,49.6],[19.8,49.2],[20.4,49.4],[20.9,49.3],[21.6,49',
        '.5]]]]],["CZE","CZ","CZE","203","Czech Republic",15.4,49.9,["CZE","CZ","203","Czechia","Czech Republic"],[[[[15,51.1],[1',
        '5.5,50.8],[16.2,50.7],[16.2,50.4],[16.7,50.2],[16.9,50.5],[17.6,50.4],[17.6,50],[18.4,50],[18.9,49.5],[18.6,49.5],[18.4,',
        '49.3],[18.2,49.3],[18.1,49],[17.9,49],[17.9,48.9],[17.5,48.8],[17.1,48.8],[17,48.6],[16.5,48.8],[16,48.7],[15.3,49],[14.',
        '9,49],[14.3,48.6],[13.6,48.9],[13,49.3],[12.5,49.5],[12.4,50],[12.2,50.3],[13,50.5],[13.3,50.7],[14.1,50.9],[14.3,51.1],',
        '[14.6,51]]]]],["ERI","ER","ERI","232","Eritrea",38.3,15.8,["ERI","ER","232","Eritrea"],[[[[36.4,14.4],[36.3,14.8],[36.8,',
        '16.3],[36.9,17],[37.2,17.3],[37.9,17.4],[38.4,18],[39,16.8],[39.3,15.9],[39.8,15.4],[41.2,14.5],[41.7,13.9],[42.3,13.3],',
        '[42.6,13],[43.1,12.7],[42.8,12.5],[42.4,12.5],[42,12.9],[41.6,13.5],[41.2,13.8],[40.9,14.1],[40,14.5],[39.3,14.5],[39.1,',
        '14.7],[38.5,14.5],[37.9,15],[37.6,14.2]]]]],["JPN","JP","JPN","392","Japan",138.4,36.1,["JPN","JP","392","J","Japan"],[[',
        '[[141.9,39.2],[141,38.2],[141,37.1],[140.6,36.3],[140.8,35.8],[140.3,35.1],[139,34.7],[137.2,34.6],[135.8,33.5],[135.1,3',
        '3.8],[135.1,34.6],[133.3,34.4],[132.2,33.9],[131,33.9],[132,33.1],[131.3,31.5],[130.7,31],[130.2,31.4],[130.4,32.3],[129',
        '.8,32.6],[129.4,33.3],[130.4,33.6],[130.9,34.2],[131.9,34.7],[132.6,35.4],[134.6,35.7],[135.7,35.5],[136.7,37.3],[137.4,',
        '36.8],[138.9,37.8],[139.4,38.2],[140.1,39.4],[139.9,40.6],[140.3,41.2],[141.4,41.4],[141.9,40]]],[[[144.6,44],[145.3,44.',
        '4],[145.5,43.3],[144.1,43],[143.2,42],[141.6,42.7],[141.1,41.6],[140,41.6],[139.8,42.6],[140.3,43.3],[141.4,43.4],[141.7',
        ',44.8],[142,45.6],[143.1,44.5],[143.9,44.2]]],[[[132.4,33.5],[132.9,34.1],[133.5,33.9],[133.9,34.4],[134.6,34.1],[134.8,',
        '33.8],[134.2,33.2],[133.8,33.5],[133.3,33.3],[133,32.7],[132.4,33]]]]],["PRY","PY","PRY","600","Paraguay",-60.1,-21.7,["',
        'PRY","PY","600","Paraguay"],[[[[-58.2,-20.2],[-57.9,-20.7],[-57.9,-22.1],[-56.9,-22.3],[-56.5,-22.1],[-55.8,-22.4],[-55.',
        '6,-22.7],[-55.5,-23.6],[-55.4,-24],[-55,-24],[-54.7,-23.8],[-54.3,-24],[-54.3,-24.6],[-54.4,-25.2],[-54.6,-25.7],[-54.8,',
        '-26.6],[-55.7,-27.4],[-56.5,-27.5],[-57.6,-27.4],[-58.6,-27.1],[-57.6,-25.6],[-57.8,-25.2],[-58.8,-24.8],[-60,-24],[-60.',
        '8,-23.9],[-62.7,-22.2],[-62.3,-21.1],[-62.3,-20.5],[-61.8,-19.6],[-60,-19.3],[-59.1,-19.4],[-58.2,-19.9]]]]],["YEM","YE"',
        ',"YEM","887","Yemen",45.9,15.3,["YEM","YE","887","RY","Yemen"],[[[[52,19],[52.8,17.3],[53.1,16.7],[52.4,16.4],[52.2,15.9',
        '],[52.2,15.6],[51.2,15.2],[49.6,14.7],[48.7,14],[48.2,13.9],[47.9,14],[47.4,13.6],[46.7,13.4],[45.9,13.3],[45.6,13.3],[4',
        '5.4,13],[45.1,13],[45,12.7],[44.5,12.7],[44.2,12.6],[43.5,12.6],[43.2,13.2],[43.3,13.8],[43.1,14.1],[42.9,14.8],[42.6,15',
        '.2],[42.8,15.3],[42.7,15.7],[42.8,15.9],[42.8,16.3],[43.2,16.7],[43.1,17.1],[43.4,17.6],[43.8,17.3],[44.1,17.4],[45.2,17',
        '.4],[45.4,17.3],[46.4,17.2],[46.7,17.3],[47,16.9],[47.5,17.1],[48.2,18.2],[49.1,18.6]]]]],["SAU","SA","SAU","682","Saudi',
        ' Arabia",44.7,23.8,["SAU","SA","682","Saudi Arabia"],[[[[35,29.4],[36.1,29.2],[36.5,29.5],[36.7,29.9],[37.5,30],[37.7,30',
        '.3],[38,30.5],[37,31.5],[39,32],[39.2,32.2],[40.4,31.9],[41.9,31.2],[44.7,29.2],[46.6,29.1],[47.5,29],[47.7,28.5],[48.4,',
        '28.6],[48.8,27.7],[49.3,27.5],[49.5,27.1],[50.2,26.7],[50.2,26.3],[50.1,25.9],[50.2,25.6],[50.5,25.3],[50.7,25],[50.8,24',
        '.8],[51.1,24.6],[51.4,24.6],[51.6,24.2],[51.6,24],[52,23],[55,22.5],[55.2,22.7],[55.7,22],[55,20],[52,19],[49.1,18.6],[4',
        '8.2,18.2],[47.5,17.1],[47,16.9],[46.7,17.3],[46.4,17.2],[45.4,17.3],[45.2,17.4],[44.1,17.4],[43.8,17.3],[43.4,17.6],[43.',
        '1,17.1],[43.2,16.7],[42.8,16.3],[42.6,16.8],[42.3,17.1],[42.3,17.5],[41.8,17.8],[41.2,18.7],[40.9,19.5],[40.2,20.2],[39.',
        '8,20.3],[39.1,21.3],[39,22],[39.1,22.6],[38.5,23.7],[38,24.1],[37.5,24.3],[37.2,24.9],[37.2,25.1],[36.9,25.6],[36.6,25.8',
        '],[36.2,26.6],[35.6,27.4],[35.1,28.1],[34.6,28.1],[34.8,28.6],[34.8,29]]]]],["ATA","AQ","ATA","010","Antarctica",35.9,-7',
        '9.8,["ATA","AQ","010","Antarctica"],[[[[-48.7,-78],[-48.2,-78],[-46.7,-77.8],[-45.2,-78],[-43.9,-78.5],[-43.5,-79.1],[-4',
        '3.4,-79.5],[-43.3,-80],[-44.9,-80.3],[-46.5,-80.6],[-48.4,-80.8],[-50.5,-81],[-52.9,-81],[-54.2,-80.6],[-54,-80.2],[-51.',
        '9,-79.9],[-51,-79.6],[-50.4,-79.2],[-49.9,-78.8],[-49.3,-78.5]]],[[[-66.3,-80.3],[-64,-80.3],[-61.9,-80.4],[-61.1,-80],[',
        '-60.6,-79.6],[-59.6,-80],[-59.9,-80.5],[-60.2,-81],[-62.3,-80.9],[-64.5,-80.9],[-65.7,-80.6],[-65.7,-80.5]]],[[[-73.9,-7',
        '1.3],[-73.2,-71.2],[-72.1,-71.2],[-71.8,-70.7],[-71.7,-70.3],[-71.7,-69.5],[-71.2,-69],[-70.3,-68.9],[-69.7,-69.3],[-69.',
        '5,-69.6],[-69.1,-70.1],[-68.7,-70.5],[-68.5,-71],[-68.3,-71.4],[-68.5,-71.8],[-68.8,-72.2],[-70,-72.3],[-71.1,-72.5],[-7',
        '2.4,-72.5],[-71.9,-72.1],[-73.1,-72.2],[-74.2,-72.4],[-75,-72.1],[-75,-71.7]]],[[[-102.3,-71.9],[-101.7,-71.7],[-100.4,-',
        '71.9],[-99,-71.9],[-97.9,-72.1],[-96.8,-72],[-96.2,-72.5],[-97,-72.4],[-98.2,-72.5],[-99.4,-72.4],[-100.8,-72.5],[-101.8',
        ',-72.3]]],[[[-122.6,-73.7],[-122.4,-73.3],[-121.2,-73.5],[-119.9,-73.7],[-118.7,-73.5],[-119.3,-73.8],[-120.2,-74.1],[-1',
        '21.6,-74]]],[[[-127.3,-73.5],[-126.6,-73.2],[-125.6,-73.5],[-124,-73.9],[-124.6,-73.8],[-125.9,-73.7]]],[[[-163.7,-78.6]',
        ',[-163.1,-78.2],[-161.2,-78.4],[-160.2,-78.7],[-159.5,-79],[-159.2,-79.5],[-161.1,-79.6],[-162.4,-79.3],[-163,-78.9],[-1',
        '63.1,-78.9]]],[[[180,-84.7],[180,-90],[-180,-90],[-180,-84.7],[-179.9,-84.7],[-179.1,-84.1],[-177.3,-84.5],[-177.1,-84.4',
        '],[-176.1,-84.1],[-175.9,-84.1],[-175.8,-84.1],[-174.4,-84.5],[-173.1,-84.1],[-172.9,-84.1],[-170,-83.9],[-169,-84.1],[-',
        '168.5,-84.2],[-167,-84.6],[-164.2,-84.8],[-161.9,-85.1],[-158.1,-85.4],[-155.2,-85.1],[-150.9,-85.3],[-148.5,-85.6],[-14',
        '5.9,-85.3],[-143.1,-85],[-142.9,-84.6],[-146.8,-84.5],[-150.1,-84.3],[-150.9,-83.9],[-153.6,-83.7],[-153.4,-83.2],[-153,',
        '-82.8],[-152.7,-82.5],[-152.9,-82],[-154.5,-81.8],[-155.3,-81.4],[-156.8,-81.1],[-154.4,-81.2],[-152.1,-81],[-150.6,-81.',
        '3],[-148.9,-81],[-147.2,-80.7],[-146.4,-80.3],[-146.8,-79.9],[-148.1,-79.7],[-149.5,-79.4],[-151.6,-79.3],[-153.4,-79.2]',
        ',[-155.3,-79.1],[-156,-78.7],[-157.3,-78.4],[-158.1,-78],[-158.4,-76.9],[-157.9,-77],[-157,-77.3],[-155.3,-77.2],[-153.7',
        ',-77.1],[-152.9,-77.5],[-151.3,-77.4],[-150,-77.2],[-148.7,-76.9],[-147.6,-76.6],[-146.1,-76.5],[-146.1,-76.1],[-146.5,-',
        '75.7],[-146.2,-75.4],[-144.9,-75.2],[-144.3,-75.5],[-142.8,-75.3],[-141.6,-75.1],[-140.2,-75.1],[-138.9,-75],[-137.5,-74',
        '.7],[-136.4,-74.5],[-135.2,-74.3],[-134.4,-74.4],[-133.7,-74.4],[-132.3,-74.3],[-130.9,-74.5],[-129.6,-74.5],[-128.2,-74',
        '.3],[-126.9,-74.4],[-125.4,-74.5],[-124,-74.5],[-122.6,-74.5],[-121.1,-74.5],[-119.7,-74.5],[-118.7,-74.2],[-117.5,-74],',
        '[-116.2,-74.2],[-115,-74.1],[-113.9,-73.7],[-113.3,-74],[-112.9,-74.4],[-112.3,-74.7],[-111.3,-74.4],[-110.1,-74.8],[-10',
        '8.7,-74.9],[-107.6,-75.2],[-106.1,-75.1],[-104.9,-74.9],[-103.4,-75],[-102,-75.1],[-100.6,-75.3],[-100.1,-74.9],[-100.8,',
        '-74.5],[-101.3,-74.2],[-102.5,-74.1],[-103.1,-73.7],[-103.3,-73.4],[-103.7,-72.6],[-102.9,-72.8],[-101.6,-72.8],[-100.3,',
        '-72.8],[-99.1,-72.9],[-98.1,-73.2],[-97.7,-73.6],[-96.3,-73.6],[-95,-73.5],[-93.7,-73.3],[-92.4,-73.2],[-91.4,-73.4],[-9',
        '0.1,-73.3],[-89.2,-72.6],[-88.4,-73],[-87.3,-73.2],[-86,-73.1],[-85.2,-73.5],[-83.9,-73.5],[-82.7,-73.6],[-81.5,-73.9],[',
        '-80.7,-73.5],[-80.3,-73.1],[-79.3,-73.5],[-77.9,-73.4],[-76.9,-73.6],[-76.2,-74],[-74.9,-73.9],[-73.9,-73.7],[-72.8,-73.',
        '4],[-71.6,-73.3],[-70.2,-73.1],[-68.9,-73],[-68,-72.8],[-67.4,-72.5],[-67.1,-72],[-67.3,-71.6],[-67.6,-71.2],[-67.9,-70.',
        '9],[-68.2,-70.5],[-68.5,-70.1],[-68.5,-69.7],[-68.4,-69.3],[-68,-69],[-67.6,-68.5],[-67.4,-68.1],[-67.6,-67.7],[-67.7,-6',
        '7.3],[-67.3,-66.9],[-66.7,-66.6],[-66.1,-66.2],[-65.4,-65.9],[-64.6,-65.6],[-64.2,-65.2],[-63.6,-64.9],[-63,-64.6],[-62,',
        '-64.6],[-61.4,-64.3],[-60.7,-64.1],[-59.9,-64],[-59.2,-63.7],[-58.6,-63.4],[-57.8,-63.3],[-57.2,-63.5],[-57.6,-63.9],[-5',
        '8.6,-64.2],[-59,-64.4],[-59.8,-64.2],[-60.6,-64.3],[-61.3,-64.5],[-62,-64.8],[-62.5,-65.1],[-62.6,-65.5],[-62.6,-65.9],[',
        '-62.1,-66.2],[-62.8,-66.4],[-63.7,-66.5],[-64.3,-66.8],[-64.9,-67.2],[-65.5,-67.6],[-65.7,-68],[-65.3,-68.4],[-64.8,-68.',
        '7],[-64,-68.9],[-63.2,-69.2],[-62.8,-69.6],[-62.6,-70],[-62.3,-70.4],[-61.8,-70.7],[-61.5,-71.1],[-61.4,-72],[-61.1,-72.',
        '4],[-61,-72.8],[-60.7,-73.2],[-60.8,-73.7],[-61.4,-74.1],[-62,-74.4],[-63.3,-74.6],[-63.7,-74.9],[-64.4,-75.3],[-65.9,-7',
        '5.6],[-67.2,-75.8],[-68.4,-76],[-69.8,-76.2],[-70.6,-76.6],[-72.2,-76.7],[-74,-76.6],[-75.6,-76.7],[-77.2,-76.7],[-76.9,',
        '-77.1],[-75.4,-77.3],[-74.3,-77.6],[-73.7,-77.9],[-74.8,-78.2],[-76.5,-78.1],[-77.9,-78.4],[-78,-78.8],[-78,-79.2],[-76.',
        '8,-79.5],[-76.6,-79.9],[-75.4,-80.3],[-73.2,-80.4],[-71.4,-80.7],[-70,-81],[-68.2,-81.3],[-65.7,-81.5],[-63.3,-81.7],[-6',
        '1.6,-82],[-59.7,-82.4],[-58.7,-82.8],[-58.2,-83.2],[-57,-82.9],[-55.4,-82.6],[-53.6,-82.3],[-51.5,-82],[-49.8,-81.7],[-4',
        '7.3,-81.7],[-44.8,-81.8],[-42.8,-82.1],[-42.2,-81.7],[-40.8,-81.4],[-38.2,-81.3],[-36.3,-81.1],[-34.4,-80.9],[-32.3,-80.',
        '8],[-30.1,-80.6],[-28.5,-80.3],[-29.3,-80],[-29.7,-79.6],[-29.7,-79.3],[-31.6,-79.3],[-33.7,-79.5],[-35.6,-79.5],[-35.9,',
        '-79.1],[-35.8,-78.3],[-35.3,-78.1],[-33.9,-77.9],[-32.2,-77.7],[-31,-77.4],[-29.8,-77.1],[-28.9,-76.7],[-27.5,-76.5],[-2',
        '6.2,-76.4],[-25.5,-76.3],[-23.9,-76.2],[-22.5,-76.1],[-21.2,-75.9],[-20,-75.7],[-18.9,-75.4],[-17.5,-75.1],[-16.6,-74.8]',
        ',[-15.7,-74.5],[-15.4,-74.1],[-16.5,-73.9],[-16.1,-73.5],[-15.4,-73.1],[-14.4,-73],[-13.3,-72.7],[-12.3,-72.4],[-11.5,-7',
        '2],[-11,-71.5],[-10.3,-71.3],[-9.1,-71.3],[-8.6,-71.7],[-7.4,-71.7],[-7.4,-71.3],[-6.9,-70.9],[-5.8,-71],[-5.5,-71.4],[-',
        '4.3,-71.5],[-3,-71.3],[-1.8,-71.2],[-0.7,-71.2],[-0.2,-71.6],[0.9,-71.3],[1.9,-71.1],[3,-71],[4.1,-70.9],[5.2,-70.6],[6.',
        '3,-70.5],[7.1,-70.2],[7.7,-69.9],[8.5,-70.1],[9.5,-70],[10.2,-70.5],[10.8,-70.8],[12,-70.6],[12.4,-70.2],[13.4,-70],[14.',
        '7,-70],[15.1,-70.4],[15.9,-70],[17,-69.9],[18.2,-69.9],[19.3,-69.9],[20.4,-70],[21.5,-70.1],[21.9,-70.4],[22.6,-70.7],[2',
        '3.7,-70.5],[24.8,-70.5],[26,-70.5],[27.1,-70.5],[28.1,-70.3],[29.2,-70.2],[30,-69.9],[31,-69.8],[32,-69.7],[32.8,-69.4],',
        '[33.3,-68.8],[33.9,-68.5],[34.9,-68.7],[35.3,-69],[36.2,-69.2],[37.2,-69.2],[37.9,-69.5],[38.6,-69.8],[39.7,-69.5],[40,-',
        '69.1],[40.9,-68.9],[42,-68.6],[42.9,-68.5],[44.1,-68.3],[44.9,-68.1],[45.7,-67.8],[46.5,-67.6],[47.4,-67.7],[48.3,-67.4]',
        ',[49,-67.1],[49.9,-67.1],[50.8,-66.9],[50.9,-66.5],[51.8,-66.2],[52.6,-66.1],[53.6,-65.9],[54.5,-65.8],[55.4,-65.9],[56.',
        '4,-66],[57.2,-66.2],[57.3,-66.7],[58.1,-67],[58.7,-67.3],[59.9,-67.4],[60.6,-67.7],[61.4,-68],[62.4,-68],[63.2,-67.8],[6',
        '4.1,-67.4],[65,-67.6],[66,-67.7],[66.9,-67.9],[67.9,-67.9],[68.9,-67.9],[69.7,-69],[69.7,-69.2],[69.6,-69.7],[68.6,-69.9',
        '],[67.8,-70.3],[67.9,-70.7],[69.1,-70.7],[68.9,-71.1],[68.4,-71.4],[67.9,-71.9],[68.7,-72.2],[69.9,-72.3],[71,-72.1],[71',
        '.6,-71.7],[71.9,-71.3],[72.5,-71],[73.1,-70.7],[73.3,-70.4],[73.9,-69.9],[74.5,-69.8],[75.6,-69.7],[76.6,-69.6],[77.6,-6',
        '9.5],[78.1,-69.1],[78.4,-68.7],[79.1,-68.3],[80.1,-68.1],[80.9,-67.9],[81.5,-67.5],[82.1,-67.4],[82.8,-67.2],[83.8,-67.3',
        '],[84.7,-67.2],[85.7,-67.1],[86.8,-67.2],[87.5,-66.9],[88,-66.2],[88.4,-66.5],[88.8,-67],[89.7,-67.2],[90.6,-67.2],[91.6',
        ',-67.1],[92.6,-67.2],[93.5,-67.2],[94.2,-67.1],[95,-67.2],[95.8,-67.4],[96.7,-67.2],[97.8,-67.2],[98.7,-67.1],[99.7,-67.',
        '2],[100.4,-66.9],[100.9,-66.6],[101.6,-66.3],[102.8,-65.6],[103.5,-65.7],[104.2,-66],[104.9,-66.3],[106.2,-66.9],[107.2,',
        '-67],[108.1,-67],[109.2,-66.8],[110.2,-66.7],[111.1,-66.4],[111.7,-66.1],[112.9,-66.1],[113.6,-65.9],[114.4,-66.1],[114.',
        '9,-66.4],[115.6,-66.7],[116.7,-66.7],[117.4,-66.9],[118.6,-67.2],[119.8,-67.3],[120.9,-67.2],[121.7,-66.9],[122.3,-66.6]',
        ',[123.2,-66.5],[124.1,-66.6],[125.2,-66.7],[126.1,-66.6],[127,-66.6],[127.9,-66.7],[128.8,-66.8],[129.7,-66.6],[130.8,-6',
        '6.4],[131.8,-66.4],[132.9,-66.4],[133.9,-66.3],[134.8,-66.2],[135,-65.7],[135.1,-65.3],[135.7,-65.6],[135.9,-66],[136.2,',
        '-66.4],[136.6,-66.8],[137.5,-67],[138.6,-66.9],[139.9,-66.9],[140.8,-66.8],[142.1,-66.8],[143.1,-66.8],[144.4,-66.8],[14',
        '5.5,-66.9],[146.2,-67.2],[146,-67.6],[146.6,-67.9],[147.7,-68.1],[148.8,-68.4],[150.1,-68.6],[151.5,-68.7],[152.5,-68.9]',
        ',[153.6,-68.9],[154.3,-68.6],[155.2,-68.8],[155.9,-69.1],[156.8,-69.4],[158,-69.5],[159.2,-69.6],[159.7,-70],[160.8,-70.',
        '2],[161.6,-70.6],[162.7,-70.7],[163.8,-70.7],[164.9,-70.8],[166.1,-70.8],[167.3,-70.8],[168.4,-71],[169.5,-71.2],[170.5,',
        '-71.4],[171.2,-71.7],[171.1,-72.1],[170.6,-72.4],[170.1,-72.9],[169.8,-73.2],[169.3,-73.7],[168,-73.8],[167.4,-74.2],[16',
        '6.1,-74.4],[165.6,-74.8],[165,-75.1],[164.2,-75.5],[163.8,-75.9],[163.6,-76.2],[163.5,-76.7],[163.5,-77.1],[164.1,-77.5]',
        ',[164.3,-77.8],[164.7,-78.2],[166.6,-78.3],[167,-78.8],[165.2,-78.9],[163.7,-79.1],[161.8,-79.2],[160.9,-79.7],[160.7,-8',
        '0.2],[160.3,-80.6],[159.8,-80.9],[161.1,-81.3],[161.6,-81.7],[162.5,-82.1],[163.7,-82.4],[165.1,-82.7],[166.6,-83],[168.',
        '9,-83.3],[169.4,-83.8],[172.3,-84],[172.5,-84.1],[173.2,-84.4],[176,-84.2],[178.3,-84.5]]]]],["CYN","","","","Turkish Re',
        'public of Northern Cyprus",33.7,35.2,["CYN","CN","N. Cyprus","Turkish Republic of Northern Cyprus","Northern Cyprus"],[[',
        '[[32.7,35.1],[32.8,35.1],[32.9,35.4],[33.7,35.4],[34.6,35.7],[33.9,35.2],[34,35.1],[33.9,35.1],[33.7,35],[33.5,35],[33.5',
        ',35.1],[33.4,35.2],[33.2,35.2],[32.9,35.1]]]]],["CYP","CY","CYP","196","Cyprus",33.1,34.9,["CYP","CY","196","Cyprus"],[[',
        '[[32.7,35.1],[32.9,35.1],[33.2,35.2],[33.4,35.2],[33.5,35.1],[33.5,35],[33.7,35],[33.9,35.1],[34,35.1],[34,35],[33,34.6]',
        ',[32.5,34.7],[32.3,35.1]]]]],["MAR","MA","MAR","504","Morocco",-7.2,31.7,["MAR","MA","504","Morocco"],[[[[-2.2,35.2],[-1',
        '.8,34.5],[-1.7,33.9],[-1.4,32.9],[-1.1,32.7],[-1.3,32.3],[-2.6,32.1],[-3.1,31.7],[-3.6,31.6],[-3.7,30.9],[-4.9,30.5],[-5',
        '.2,30],[-6.1,29.7],[-7.1,29.6],[-8.7,28.8],[-8.7,27.7],[-8.8,27.7],[-8.8,27.1],[-9.4,27.1],[-9.7,26.9],[-10.2,26.9],[-10',
        '.6,27],[-11.4,26.9],[-11.7,26.1],[-12,26],[-12.5,24.8],[-13.9,23.7],[-14.2,22.3],[-14.6,21.9],[-14.8,21.5],[-17,21.4],[-',
        '17,21.9],[-16.6,22.2],[-16.3,22.7],[-16.3,23],[-16,23.7],[-15.4,24.4],[-15.1,24.5],[-14.8,25.1],[-14.8,25.6],[-14.4,26.3',
        '],[-13.8,26.6],[-13.1,27.6],[-13.1,27.7],[-12.6,28],[-11.7,28.1],[-10.9,28.8],[-10.4,29.1],[-9.6,29.9],[-9.8,31.2],[-9.4',
        ',32],[-9.3,32.6],[-8.7,33.2],[-7.7,33.7],[-6.9,34.1],[-6.2,35.1],[-5.9,35.8],[-5.2,35.8],[-4.6,35.3],[-3.6,35.4],[-2.6,3',
        '5.2]]]]],["EGY","EG","EGY","818","Egypt",29.4,26.2,["EGY","EG","818","Egypt"],[[[[36.9,22],[32.9,22],[29,22],[25,22],[25',
        ',25.7],[25,29.2],[24.7,30],[25,30.7],[24.8,31.1],[25.2,31.6],[26.5,31.6],[27.5,31.3],[28.5,31],[28.9,30.9],[29.7,31.2],[',
        '30.1,31.5],[31,31.6],[31.7,31.4],[32,30.9],[32.2,31.3],[33,31],[33.8,31],[34.3,31.2],[34.8,29.8],[34.9,29.5],[34.6,29.1]',
        ',[34.4,28.3],[34.2,27.8],[33.9,27.6],[33.6,28],[33.1,28.4],[32.4,29.9],[32.3,29.8],[32.7,28.7],[33.3,27.7],[34.1,26.1],[',
        '34.5,25.6],[34.8,25],[35.7,23.9],[35.5,23.8],[35.5,23.1],[36.7,22.2]]]]],["LBY","LY","LBY","434","Libya",18,26.6,["LBY",',
        '"LY","434","Libya"],[[[[25,22],[25,20],[23.9,20],[23.8,19.6],[19.8,21.5],[15.9,23.4],[14.9,22.9],[14.1,22.5],[13.6,23],[',
        '12,23.5],[11.6,24.1],[10.8,24.6],[10.3,24.4],[9.9,24.9],[9.9,25.4],[9.3,26.1],[9.7,26.5],[9.6,27.1],[9.8,27.7],[9.7,28.1',
        '],[9.9,29],[9.8,29.4],[9.5,30.3],[10,30.5],[10.1,31],[10,31.4],[10.6,31.8],[10.9,32.1],[11.4,32.4],[11.5,33.1],[12.7,32.',
        '8],[13.1,32.9],[13.9,32.7],[15.2,32.3],[15.7,31.4],[16.6,31.2],[18,30.8],[19.1,30.3],[19.6,30.5],[20.1,31],[19.8,31.8],[',
        '20.1,32.2],[20.9,32.7],[21.5,32.8],[22.9,32.6],[23.2,32.2],[23.6,32.2],[23.9,32],[24.9,31.9],[25.2,31.6],[24.8,31.1],[25',
        ',30.7],[24.7,30],[25,29.2],[25,25.7]]]]],["ETH","ET","ETH","231","Ethiopia",39.1,8,["ETH","ET","231","Ethiopia"],[[[[47.',
        '8,8],[45,5],[43.7,5],[42.8,4.3],[42.1,4.2],[41.9,3.9],[41.2,3.9],[40.8,4.3],[39.9,3.8],[39.6,3.4],[38.9,3.5],[38.7,3.6],',
        '[38.4,3.6],[38.1,3.6],[36.9,4.4],[36.2,4.4],[35.8,4.8],[35.8,5.3],[35.3,5.5],[34.7,6.6],[34.3,6.8],[34.1,7.2],[33.6,7.7]',
        ',[33,7.8],[33.3,8.4],[33.8,8.4],[34,8.7],[34,9.6],[34.3,10.6],[34.7,10.9],[34.8,11.3],[35.3,12.1],[35.9,12.6],[36.3,13.6',
        '],[36.4,14.4],[37.6,14.2],[37.9,15],[38.5,14.5],[39.1,14.7],[39.3,14.5],[40,14.5],[40.9,14.1],[41.2,13.8],[41.6,13.5],[4',
        '2,12.9],[42.4,12.5],[42,12.1],[41.7,11.6],[41.7,11.4],[41.8,11.1],[42.3,11],[42.6,11.1],[42.8,10.9],[42.6,10.6],[42.9,10',
        '],[43.3,9.5],[43.7,9.2],[46.9,8]]]]],["DJI","DJ","DJI","262","Djibouti",42.5,12,["DJI","DJ","262","Djibouti"],[[[[42.4,1',
        '2.5],[42.8,12.5],[43.1,12.7],[43.3,12.4],[43.3,12],[42.7,11.7],[43.1,11.5],[42.8,10.9],[42.6,11.1],[42.3,11],[41.8,11.1]',
        ',[41.7,11.4],[41.7,11.6],[42,12.1]]]]],["SOL","","","","Somaliland",46.7,9.4,["SOL","SL","Somaliland"],[[[[48.9,11.4],[4',
        '8.9,11],[48.9,10],[48.9,9.5],[48.5,8.8],[47.8,8],[46.9,8],[43.7,9.2],[43.3,9.5],[42.9,10],[42.6,10.6],[42.8,10.9],[43.1,',
        '11.5],[43.5,11.3],[43.7,10.9],[44.1,10.4],[44.6,10.4],[45.6,10.7],[46.6,10.8],[47.5,11.1],[48,11.2],[48.4,11.4]]]]],["UG',
        'A","UG","UGA","800","Uganda",32.9,2,["UGA","UG","800","Uganda"],[[[[33.9,-0.9],[31.9,-1],[30.8,-1],[30.4,-1.1],[29.8,-1.',
        '4],[29.6,-1.3],[29.6,-0.6],[29.8,-0.2],[29.9,0.6],[30.1,1.1],[30.5,1.6],[30.9,1.8],[31.2,2.2],[30.8,2.3],[30.8,3.5],[31.',
        '2,3.8],[31.9,3.6],[32.7,3.8],[33.4,3.8],[34,4.2],[34.5,3.6],[34.6,3.1],[35,1.9],[34.7,1.2],[34.2,0.5],[33.9,0.1]]]]],["R',
        'WA","RW","RWA","646","Rwanda",30.1,-1.9,["RWA","RW","646","Rwanda"],[[[[30.4,-1.1],[30.8,-1.7],[30.8,-2.3],[30.5,-2.4],[',
        '29.9,-2.3],[29.6,-2.9],[29,-2.8],[29.1,-2.3],[29.3,-2.2],[29.3,-1.6],[29.6,-1.3],[29.8,-1.4]]]]],["BIH","BA","BIH","070"',
        ',"Bosnia and Herzegovina",18.1,44.1,["BIH","BA","070","BiH","Bosnia and Herz.","Bosnia and Herzegovina"],[[[[18.6,42.7],',
        '[17.7,43],[17.3,43.4],[16.9,43.7],[16.5,44],[16.2,44.4],[15.8,44.8],[16,45.2],[16.3,45],[16.5,45.2],[17,45.2],[17.9,45.1',
        '],[18.6,45.1],[19,44.9],[19.4,44.9],[19.1,44.4],[19.6,44],[19.5,43.6],[19.2,43.5],[19,43.4],[18.7,43.2]]]]],["MKD","MK",',
        '"MKD","807","North Macedonia",21.6,41.6,["MKD","MK","807","NM","North Macedonia"],[[[[22.4,42.3],[22.9,42],[23,41.3],[22',
        '.8,41.3],[22.6,41.1],[22.1,41.1],[21.7,40.9],[21,40.8],[20.6,41.1],[20.5,41.5],[20.6,41.9],[20.7,41.8],[20.8,42.1],[21.4',
        ',42.2],[21.6,42.2],[21.9,42.3]]]]],["SRB","RS","SRB","688","Serbia",20.8,44.2,["SRB","RS","688","YF","Serbia","Republic ',
        'of Serbia"],[[[[18.8,45.9],[19.6,46.2],[20.2,46.1],[20.8,45.7],[20.9,45.4],[21.5,45.2],[21.6,44.8],[22.1,44.5],[22.5,44.',
        '7],[22.7,44.6],[22.5,44.4],[22.7,44.2],[22.4,44],[22.5,43.6],[23,43.2],[22.6,42.9],[22.4,42.6],[22.5,42.5],[22.4,42.3],[',
        '21.9,42.3],[21.6,42.2],[21.5,42.3],[21.7,42.4],[21.8,42.7],[21.6,42.7],[21.4,42.9],[21.3,42.9],[21.1,43.1],[21,43.1],[20',
        '.8,43.3],[20.6,43.2],[20.5,42.9],[20.3,42.8],[20.3,42.9],[20,43.1],[19.6,43.2],[19.5,43.4],[19.2,43.5],[19.5,43.6],[19.6',
        ',44],[19.1,44.4],[19.4,44.9],[19,44.9],[19.4,45.2],[19.1,45.5]]]]],["MNE","ME","MNE","499","Montenegro",19.1,42.8,["MNE"',
        ',"ME","499","Montenegro"],[[[[20.1,42.6],[19.8,42.5],[19.7,42.7],[19.3,42.2],[19.4,41.9],[19.2,42],[18.9,42.3],[18.5,42.',
        '5],[18.6,42.7],[18.7,43.2],[19,43.4],[19.2,43.5],[19.5,43.4],[19.6,43.2],[20,43.1],[20.3,42.9],[20.3,42.8]]]]],["KOS",""',
        ',"","","Kosovo",20.9,42.6,["KOS","KO","KV","KSV","Kosovo"],[[[[20.6,41.9],[20.5,42.2],[20.3,42.3],[20.1,42.6],[20.3,42.8',
        '],[20.5,42.9],[20.6,43.2],[20.8,43.3],[21,43.1],[21.1,43.1],[21.3,42.9],[21.4,42.9],[21.6,42.7],[21.8,42.7],[21.7,42.4],',
        '[21.5,42.3],[21.6,42.2],[21.4,42.2],[20.8,42.1],[20.7,41.8]]]]],["TTO","TT","TTO","780","Trinidad and Tobago",-60.9,11,[',
        '"TTO","TT","780","Trinidad and Tobago"],[[[[-61.7,10.8],[-61.1,10.9],[-60.9,10.9],[-60.9,10.1],[-61.8,10],[-61.9,10.1],[',
        '-61.7,10.4]]]]],["SDS","SS","SSD","728","South Sudan",30.4,7.2,["SDS","SS","SSD","728","S. Sudan","South Sudan"],[[[[30.',
        '8,3.5],[30,4.2],[29.7,4.6],[29.2,4.4],[28.7,4.5],[28.4,4.3],[28,4.4],[27.4,5.2],[27.2,5.6],[26.5,5.9],[26.2,6.5],[25.8,7',
        '],[25.1,7.5],[25.1,7.8],[24.6,8.2],[23.9,8.6],[24.2,8.7],[24.5,8.9],[24.8,9.8],[25.1,10.3],[25.8,10.4],[26,10.1],[26.5,9',
        '.6],[26.8,9.5],[27.1,9.6],[27.8,9.6],[28,9.4],[29,9.4],[29,9.6],[29.5,9.8],[29.6,10.1],[30,10.3],[30.8,9.7],[31.4,9.8],[',
        '31.9,10.5],[32.4,11.1],[32.3,11.7],[32.1,12],[32.7,12],[32.7,12.2],[33.2,12.2],[33.1,11.4],[33.2,10.7],[33.7,10.3],[33.8',
        ',10],[33.8,9.5],[34,9.5],[34,8.7],[33.8,8.4],[33.3,8.4],[33,7.8],[33.6,7.7],[34.1,7.2],[34.3,6.8],[34.7,6.6],[35.3,5.5],',
        '[34.6,4.8],[34,4.2],[33.4,3.8],[32.7,3.8],[31.9,3.6],[31.2,3.8]]]]]]',
    ].join('');
    let countryCache;
    function naturalEarthCountries110m() {
        countryCache ??= JSON.parse(serializedCountries);
        return countryCache;
    }

    const WORLD_ASPECT_RATIO = 2;
    const DEFAULT_BASEMAP = 'natural-earth';
    function optionBoolean$2(context, name, fallback) {
        const value = context.layer.mark.options[name];
        return typeof value === 'boolean' ? value : fallback;
    }
    function optionNumber$6(context, name, fallback) {
        const value = context.layer.mark.options[name];
        return typeof value === 'number' && Number.isFinite(value) ? value : fallback;
    }
    function optionString$6(context, name, fallback) {
        const value = context.layer.mark.options[name];
        return typeof value === 'string' && value.trim() !== '' ? value : fallback;
    }
    /**
     * Returns the undistorted 2:1 equirectangular viewport centered inside a chart plot.
     */
    function geographicViewport(plot) {
        const padding = Math.min(8, plot.width * 0.02, plot.height * 0.04);
        const availableWidth = Math.max(1, plot.width - padding * 2);
        const availableHeight = Math.max(1, plot.height - padding * 2);
        const width = Math.min(availableWidth, availableHeight * WORLD_ASPECT_RATIO);
        const height = width / WORLD_ASPECT_RATIO;
        return {
            x: plot.x + (plot.width - width) / 2,
            y: plot.y + (plot.height - height) / 2,
            width,
            height,
        };
    }
    function isGeographicPosition(longitude, latitude) {
        return (Number.isFinite(longitude) &&
            Number.isFinite(latitude) &&
            longitude >= -180 &&
            longitude <= 180 &&
            latitude >= -90 &&
            latitude <= 90);
    }
    function projectGeographicPosition(plot, longitude, latitude) {
        const viewport = geographicViewport(plot);
        return {
            x: viewport.x + ((longitude + 180) / 360) * viewport.width,
            y: viewport.y + ((90 - latitude) / 180) * viewport.height,
        };
    }
    function normalizedCountryKey(value) {
        return value
            .normalize('NFKD')
            .replace(/\p{Diacritic}/gu, '')
            .replace(/[^\p{Letter}\p{Number}]+/gu, '')
            .toUpperCase();
    }
    let countryIndex;
    function naturalEarthCountryIndex() {
        if (countryIndex !== undefined)
            return countryIndex;
        const index = new Map();
        for (const country of naturalEarthCountries110m()) {
            for (const alias of country[7]) {
                const key = normalizedCountryKey(alias);
                if (key !== '' && !index.has(key))
                    index.set(key, country);
            }
        }
        const aliases = {
            AMERICA: 'USA',
            KOREA: 'KOR',
            REPUBLICOFKOREA: 'KOR',
            SOUTHKOREA: 'KOR',
            대한민국: 'KOR',
            UK: 'GBR',
            UNITEDKINGDOM: 'GBR',
            UNITEDSTATES: 'USA',
        };
        for (const [alias, canonical] of Object.entries(aliases)) {
            const country = index.get(canonical);
            if (country !== undefined)
                index.set(normalizedCountryKey(alias), country);
        }
        countryIndex = index;
        return index;
    }
    function naturalEarthCountry(value) {
        return naturalEarthCountryIndex().get(normalizedCountryKey(value));
    }
    function projectedPolygon(plot, polygon) {
        const rings = polygon
            .map((ring) => ring
            .filter(([longitude, latitude]) => isGeographicPosition(longitude, latitude))
            .map(([longitude, latitude]) => projectGeographicPosition(plot, longitude, latitude)))
            .filter((ring) => ring.length >= 3);
        const points = rings[0];
        if (points === undefined)
            return null;
        return {
            points,
            ...(rings.length > 1 ? { subpaths: rings.slice(1) } : {}),
        };
    }
    function countryPathNodes(context, country, options) {
        const nodes = [];
        country[8].forEach((polygon, polygonIndex) => {
            const projected = projectedPolygon(context.plot, polygon);
            if (projected === null)
                return;
            const hasDatum = options.rowIndex !== undefined && options.datum !== undefined;
            nodes.push({
                type: 'path',
                ...nodeBase(`${options.idPrefix}:${country[0]}:${polygonIndex}`, {
                    zIndex: options.zIndex,
                    opacity: options.opacity ?? 1,
                    interactive: options.interactive ?? false,
                    ...(hasDatum
                        ? {
                            datum: {
                                layerId: context.layer.id,
                                rowIndex: options.rowIndex,
                                datum: options.datum,
                            },
                        }
                        : {}),
                }),
                points: projected.points,
                ...(projected.subpaths === undefined ? {} : { subpaths: projected.subpaths }),
                closed: true,
                fillRule: 'evenodd',
                fill: options.fill,
                stroke: options.stroke,
                lineWidth: options.lineWidth,
                lineJoin: 'round',
            });
        });
        return nodes;
    }
    function worldBasemapNodes(context) {
        const { layer, plot, theme } = context;
        if (optionString$6(context, 'basemap', DEFAULT_BASEMAP) === 'none')
            return [];
        const viewport = geographicViewport(plot);
        const oceanFill = optionString$6(context, 'oceanFill', mixColor$1(theme.colors.background, mappedContinuousColor(theme, 0), 0.2));
        const landFill = optionString$6(context, 'landFill', mixColor$1(theme.colors.surface, theme.colors.grid, theme.mode === 'dark' ? 0.3 : 0.48));
        const countryStroke = optionString$6(context, 'countryStroke', colorWithOpacity(theme.colors.axis, theme.mode === 'dark' ? 0.62 : 0.48));
        const countryLineWidth = Math.max(0, optionNumber$6(context, 'countryLineWidth', 0.55));
        const nodes = [
            {
                type: 'rect',
                ...nodeBase(`${layer.id}:natural-earth:surface`, { zIndex: layer.zIndex - 4 }),
                x: viewport.x,
                y: viewport.y,
                width: viewport.width,
                height: viewport.height,
                fill: oceanFill,
                stroke: theme.colors.grid,
                lineWidth: 0.8,
                cornerRadius: 8,
            },
        ];
        if (optionBoolean$2(context, 'graticule', false)) {
            for (let longitude = -120; longitude <= 120; longitude += 60) {
                const top = projectGeographicPosition(plot, longitude, 90);
                const bottom = projectGeographicPosition(plot, longitude, -90);
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:natural-earth:longitude:${longitude}`, {
                        zIndex: layer.zIndex - 3.5,
                        opacity: 0.5,
                    }),
                    x1: top.x,
                    y1: top.y,
                    x2: bottom.x,
                    y2: bottom.y,
                    stroke: theme.colors.grid,
                    lineWidth: 0.6,
                });
            }
            for (let latitude = -60; latitude <= 60; latitude += 30) {
                const left = projectGeographicPosition(plot, -180, latitude);
                const right = projectGeographicPosition(plot, 180, latitude);
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:natural-earth:latitude:${latitude}`, {
                        zIndex: layer.zIndex - 3.5,
                        opacity: 0.5,
                    }),
                    x1: left.x,
                    y1: left.y,
                    x2: right.x,
                    y2: right.y,
                    stroke: theme.colors.grid,
                    lineWidth: 0.6,
                });
            }
        }
        for (const country of naturalEarthCountries110m()) {
            nodes.push(...countryPathNodes(context, country, {
                idPrefix: `${layer.id}:natural-earth:country`,
                zIndex: layer.zIndex - 3,
                fill: landFill,
                stroke: countryStroke,
                lineWidth: countryLineWidth,
            }));
        }
        if (optionBoolean$2(context, 'attribution', true)) {
            const attribution = {
                type: 'text',
                ...nodeBase(`${layer.id}:natural-earth:attribution`, {
                    zIndex: layer.zIndex - 1.5,
                    opacity: 0.76,
                }),
                x: viewport.x + viewport.width - 5,
                y: viewport.y + viewport.height - 4,
                text: 'Natural Earth · 1:110m',
                fill: theme.colors.mutedText,
                fontFamily: theme.typography.fontFamily,
                fontSize: Math.max(8, theme.typography.fontSize - 3),
                fontWeight: 500,
                align: 'right',
                baseline: 'bottom',
                rotation: 0,
            };
            nodes.push(attribution);
        }
        return nodes;
    }
    function worldCountryOverlayNodes(context, country, rowIndex, fill) {
        return countryPathNodes(context, country, {
            idPrefix: `${context.layer.id}:natural-earth:region`,
            zIndex: context.layer.zIndex,
            fill,
            stroke: context.layer.mark.stroke ?? context.theme.colors.background,
            lineWidth: context.layer.mark.lineWidth ?? 0.8,
            opacity: context.layer.mark.opacity,
            interactive: context.performance.enableHitTesting,
            rowIndex,
            datum: context.table.row(rowIndex),
        });
    }

    const TAU$2 = Math.PI * 2;
    function clamp$a(value, minimum, maximum) {
        return Math.max(minimum, Math.min(maximum, value));
    }
    function optionNumber$5(value, fallback) {
        return typeof value === 'number' && Number.isFinite(value) ? value : fallback;
    }
    function optionString$5(value, fallback) {
        return typeof value === 'string' && value.trim() !== '' ? value : fallback;
    }
    function stringValue$3(value) {
        if (value === null || value === undefined)
            return null;
        return value instanceof Date ? value.toISOString() : String(value);
    }
    function paletteColor$1(context, index, count) {
        return categoricalColor(context.theme, index, count);
    }
    function datumBase$2(context, id, rowIndex, offset = 0, tooltip) {
        const row = context.table.row(rowIndex);
        return nodeBase(id, {
            zIndex: context.layer.zIndex + offset,
            opacity: context.layer.mark.opacity,
            interactive: context.performance.enableHitTesting,
            datum: {
                layerId: context.layer.id,
                rowIndex,
                datum: Object.fromEntries(Object.entries(row).filter(([field]) => !field.startsWith('__graflume_'))),
                ...(tooltip === undefined ? {} : { tooltip }),
            },
        });
    }
    function textNode$7(context, id, x, y, text, options = {}) {
        return {
            type: 'text',
            ...nodeBase(id, { zIndex: context.layer.zIndex + 3 }),
            x,
            y,
            text,
            fill: options.fill ?? context.theme.colors.text,
            fontFamily: context.theme.typography.fontFamily,
            fontSize: options.size ?? context.theme.typography.fontSize,
            fontWeight: options.weight ?? 600,
            align: options.align ?? 'center',
            baseline: options.baseline ?? 'middle',
            rotation: options.rotation ?? 0,
        };
    }
    function pointOnCircle$1(cx, cy, radius, angle) {
        return { x: cx + Math.cos(angle) * radius, y: cy + Math.sin(angle) * radius };
    }
    function sampledArc$1(cx, cy, radius, start, end, segments = 32) {
        return Array.from({ length: segments + 1 }, (_, index) => pointOnCircle$1(cx, cy, radius, start + ((end - start) * index) / segments));
    }
    function annularSector$1(cx, cy, innerRadius, outerRadius, start, end) {
        const segments = Math.max(6, Math.ceil((Math.abs(end - start) / TAU$2) * 64));
        return [
            ...sampledArc$1(cx, cy, outerRadius, start, end, segments),
            ...sampledArc$1(cx, cy, innerRadius, end, start, segments),
        ];
    }
    function quadraticPoints(start, control, end, segments = 24) {
        return Array.from({ length: segments + 1 }, (_, index) => {
            const t = index / segments;
            const inverse = 1 - t;
            return {
                x: inverse * inverse * start.x + 2 * inverse * t * control.x + t * t * end.x,
                y: inverse * inverse * start.y + 2 * inverse * t * control.y + t * t * end.y,
            };
        });
    }
    function smoothPoints(points, subdivisions = 8) {
        return [...interpolateCurve(points, 'cardinal', { samples: subdivisions })];
    }
    const compileSmoothMark = (context) => {
        const { layer, yScale, theme } = context;
        const segments = interpolateSegments(collectCurveSegments(context, 'connect'), curveNameForMark(layer.mark.options, 'cardinal'), curveOptionsForMark(layer.mark.options), context.performance.maxLinePoints);
        if (segments.length === 0)
            return [];
        const stroke = layer.mark.stroke ?? theme.mark.lineColor ?? theme.mark.defaultColor ?? context.color;
        const nodes = [];
        const baseline = yScale.map(optionNumber$5(layer.mark.options.baseline, 0));
        segments.forEach((segment, segmentIndex) => {
            const first = segment.points[0];
            const last = segment.points.at(-1);
            if (first === undefined || last === undefined)
                return;
            const suffix = segments.length === 1 ? '' : `:${segmentIndex}`;
            if (layer.mark.options.area === true) {
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:smooth-area${suffix}`, {
                        zIndex: layer.zIndex,
                        opacity: layer.mark.opacity,
                    }),
                    points: [{ x: first.x, y: baseline }, ...segment.points, { x: last.x, y: baseline }],
                    closed: true,
                    fill: layer.mark.fill ?? themedAreaFill(theme, context.color, colorWithOpacity(stroke, 0.24)),
                    lineWidth: 0,
                });
            }
            nodes.push({
                type: 'path',
                ...nodeBase(`${layer.id}:smooth-line${suffix}`, {
                    zIndex: layer.zIndex + 1,
                    opacity: layer.mark.opacity,
                }),
                points: segment.points,
                closed: false,
                stroke,
                lineWidth: layer.mark.lineWidth ?? theme.mark.lineWidth,
                lineCap: theme.mark.lineCap ?? 'round',
                lineJoin: theme.mark.lineJoin ?? 'round',
            });
        });
        const rows = segments.flatMap(({ source }) => source);
        if (layer.mark.point) {
            for (const row of rows) {
                nodes.push({
                    type: 'circle',
                    ...datumBase$2(context, `${layer.id}:smooth-point:${row.rowIndex}`, row.rowIndex, 2),
                    cx: row.point.x,
                    cy: row.point.y,
                    radius: layer.mark.radius ?? theme.mark.pointRadius,
                    fill: layer.mark.fill ?? themedPointFill(theme, stroke, theme.colors.background),
                    stroke: layer.mark.stroke ?? themedPointStroke(theme, stroke, stroke),
                    lineWidth: 2,
                });
            }
        }
        else {
            rows.forEach((row) => {
                nodes.push({
                    type: 'circle',
                    ...datumBase$2(context, `${layer.id}:smooth-hit:${row.rowIndex}`, row.rowIndex, 2),
                    cx: row.point.x,
                    cy: row.point.y,
                    radius: Math.max(2.5, layer.mark.radius ?? 3),
                    fill: layer.mark.fill ?? themedPointFill(theme, stroke, stroke),
                    stroke: layer.mark.stroke ?? themedPointStroke(theme, stroke, theme.colors.background),
                    lineWidth: 1,
                });
            });
        }
        return nodes;
    };
    const compileRangeMark = (context) => {
        const { layer, table, xScale, yScale, theme, plot } = context;
        const lowField = layer.mark.fields.low ?? 'low';
        const highField = layer.mark.fields.high ?? 'high';
        const mode = optionString$5(layer.mark.options.mode, 'area');
        const rows = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const xValue = scaleInput(table.value(rowIndex, layer.x.field));
            const low = numericDataValue(table.value(rowIndex, lowField));
            const high = numericDataValue(table.value(rowIndex, highField));
            if (xValue === null || low === null || high === null)
                continue;
            const x = xScale.map(xValue);
            const yLow = yScale.map(low);
            const yHigh = yScale.map(high);
            if ([x, yLow, yHigh].every(Number.isFinite)) {
                rows.push({ rowIndex, x, low: yLow, high: yHigh });
            }
        }
        if (rows.length === 0)
            return [];
        const stroke = layer.mark.stroke ??
            themedAreaStroke(theme, context.color, theme.mark.lineColor ?? context.color);
        const nodes = [];
        if (mode === 'area') {
            const highPoints = rows.map((row) => ({ x: row.x, y: row.high }));
            const lowPoints = rows.map((row) => ({ x: row.x, y: row.low })).reverse();
            const smooth = layer.mark.options.smooth === true;
            nodes.push({
                type: 'path',
                ...nodeBase(`${layer.id}:range-band`, {
                    zIndex: layer.zIndex,
                    opacity: layer.mark.opacity,
                }),
                points: [
                    ...(smooth ? smoothPoints(highPoints) : highPoints),
                    ...(smooth ? smoothPoints(lowPoints) : lowPoints),
                ],
                closed: true,
                fill: layer.mark.fill ?? themedAreaFill(theme, context.color, colorWithOpacity(stroke, 0.24)),
                stroke,
                lineWidth: layer.mark.lineWidth ?? 1.5,
                lineJoin: theme.mark.lineJoin ?? 'round',
            });
            for (const row of rows) {
                nodes.push({
                    type: 'circle',
                    ...datumBase$2(context, `${layer.id}:range-hit:${row.rowIndex}`, row.rowIndex, 1),
                    cx: row.x,
                    cy: (row.low + row.high) / 2,
                    radius: Math.max(3, layer.mark.radius ?? 3.5),
                    fill: layer.mark.fill ?? themedPointFill(theme, stroke, stroke),
                    stroke: layer.mark.stroke ?? themedPointStroke(theme, stroke, theme.colors.background),
                    lineWidth: 1,
                });
            }
            return nodes;
        }
        const width = Math.max(5, xScale instanceof BandScale
            ? xScale.bandwidth * 0.55
            : (plot.width / Math.max(2, rows.length * 1.5)) * 0.55);
        for (const row of rows) {
            if (mode === 'column') {
                nodes.push({
                    type: 'rect',
                    ...datumBase$2(context, `${layer.id}:range-column:${row.rowIndex}`, row.rowIndex),
                    x: row.x - width / 2,
                    y: Math.min(row.low, row.high),
                    width,
                    height: Math.max(1, Math.abs(row.low - row.high)),
                    fill: layer.mark.fill ?? colorWithOpacity(stroke, 0.72),
                    stroke,
                    lineWidth: layer.mark.lineWidth ?? 1,
                    cornerRadius: layer.mark.cornerRadius ?? theme.mark.barRadius,
                });
                continue;
            }
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:range-stem:${row.rowIndex}`, { zIndex: layer.zIndex }),
                x1: row.x,
                y1: row.low,
                x2: row.x,
                y2: row.high,
                stroke,
                lineWidth: layer.mark.lineWidth ?? 2,
                lineCap: theme.mark.lineCap ?? 'round',
            });
            const radius = Math.max(3, layer.mark.radius ?? 5);
            for (const [suffix, y, fill] of [
                ['low', row.low, layer.mark.fill ?? themedPointFill(theme, stroke, theme.colors.background)],
                ['high', row.high, layer.mark.fill ?? themedPointFill(theme, stroke, stroke)],
            ]) {
                nodes.push({
                    type: 'circle',
                    ...datumBase$2(context, `${layer.id}:${suffix}:${row.rowIndex}`, row.rowIndex, 1),
                    cx: row.x,
                    cy: y,
                    radius,
                    fill,
                    stroke: layer.mark.stroke ?? themedPointStroke(theme, stroke, stroke),
                    lineWidth: 2,
                });
            }
        }
        return nodes;
    };
    const compileDistributionMark = (context) => {
        const { layer, table, xScale, yScale, plot, theme } = context;
        const sourceField = layer.mark.fields.value ?? layer.y.field;
        const values = [];
        for (let index = 0; index < table.length; index += 1) {
            const value = numericDataValue(table.value(index, sourceField));
            if (value !== null)
                values.push(value);
        }
        const summary = summarizeNormalDistribution(values);
        if (summary === null)
            return [];
        const { mean, standardDeviation: sigma } = summary;
        const samples = clamp$a(Math.floor(optionNumber$5(layer.mark.options.samples, 72)), 24, 160);
        const densities = Array.from({ length: samples + 1 }, (_, index) => {
            const xValue = summary.domainMinimum + ((summary.domainMaximum - summary.domainMinimum) * index) / samples;
            const density = normalDensity(xValue, summary);
            return { xValue, density };
        });
        const points = densities.map(({ xValue, density }) => ({
            x: xScale.map(xValue),
            y: yScale.map(density),
        }));
        const baseline = yScale.map(0);
        const stroke = layer.mark.stroke ?? theme.mark.lineColor ?? theme.mark.defaultColor ?? context.color;
        const tooltip = {
            kind: 'normal-density',
            mean,
            standardDeviation: sigma,
            sampleCount: values.length,
            minimum: summary.observedMinimum,
            maximum: summary.observedMaximum,
        };
        return [
            {
                type: 'path',
                ...datumBase$2(context, `${layer.id}:distribution-area`, 0, 0, tooltip),
                points: [{ x: points[0].x, y: baseline }, ...points, { x: points.at(-1).x, y: baseline }],
                closed: true,
                fill: layer.mark.fill ?? themedAreaFill(theme, context.color, colorWithOpacity(stroke, 0.2)),
                lineWidth: 0,
            },
            {
                type: 'path',
                ...datumBase$2(context, `${layer.id}:distribution-line`, 0, 1, tooltip),
                points,
                closed: false,
                stroke,
                lineWidth: layer.mark.lineWidth ?? theme.mark.lineWidth + 0.5,
                lineCap: theme.mark.lineCap ?? 'round',
                lineJoin: theme.mark.lineJoin ?? 'round',
            },
            textNode$7(context, `${layer.id}:distribution-mean`, xScale.map(mean), plot.y + 14, `μ ${mean.toFixed(2)} · σ ${sigma.toFixed(2)}`, {
                fill: theme.colors.mutedText,
                size: Math.max(9, theme.typography.fontSize - 1),
            }),
        ];
    };
    function distributionObservations(context) {
        const { layer, table } = context;
        const valueField = layer.mark.fields.value ?? layer.x.field;
        const optionWeightField = layer.mark.options.weightField;
        const weightField = layer.mark.fields.weight ??
            (typeof optionWeightField === 'string' ? optionWeightField : undefined);
        const observations = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const value = numericDataValue(table.value(rowIndex, valueField), layer.x.type === 'temporal');
            const weight = weightField === undefined ? 1 : numericDataValue(table.value(rowIndex, weightField));
            if (value === null || weight === null || weight < 0)
                continue;
            observations.push({ value, weight, rowIndex });
        }
        return observations;
    }
    const compileEmpiricalDistributionMark = (context) => {
        const { layer, table, xScale, yScale, theme } = context;
        const complementary = resolveDistributionMode(layer.mark.options.mode) === 'ccdf';
        const observations = distributionObservations(context);
        const empirical = empiricalDistribution(observations, complementary);
        if (empirical.length === 0)
            return [];
        const sampledEmpirical = exactStrideSampleIndices(empirical.length, Math.max(1, Math.floor(context.performance.maxLinePoints / 2))).map((index) => empirical[index]);
        const rawPoints = [];
        let previous = complementary ? 1 : 0;
        for (const point of sampledEmpirical) {
            const x = xScale.map(point.value);
            rawPoints.push({ x, y: yScale.map(previous) }, { x, y: yScale.map(point.probability) });
            previous = point.probability;
        }
        const points = rawPoints;
        const stroke = layer.mark.stroke ?? theme.mark.lineColor ?? context.color;
        const valueField = layer.mark.fields.value ?? layer.x.field;
        const nodes = [
            {
                type: 'path',
                ...nodeBase(`${layer.id}:${complementary ? 'ccdf' : 'ecdf'}-line`, {
                    zIndex: layer.zIndex,
                    interactive: context.performance.enableHitTesting,
                    datum: {
                        layerId: layer.id,
                        rowIndex: empirical[0]?.rowIndices[0] ?? 0,
                        datum: table.row(empirical[0]?.rowIndices[0] ?? 0),
                        tooltip: {
                            kind: complementary ? 'ccdf' : 'ecdf',
                            valueField,
                            weightField: layer.mark.fields.weight ??
                                (typeof layer.mark.options.weightField === 'string'
                                    ? layer.mark.options.weightField
                                    : null),
                            sampleCount: observations.length,
                        },
                    },
                }),
                points,
                closed: false,
                stroke,
                lineWidth: layer.mark.lineWidth ?? theme.mark.lineWidth,
                lineCap: theme.mark.lineCap ?? 'round',
                lineJoin: theme.mark.lineJoin ?? 'round',
            },
        ];
        exactStrideSampleIndices(empirical.length, context.performance.maxPointMarks).forEach((empiricalIndex, index) => {
            const point = empirical[empiricalIndex];
            const rowIndex = point.rowIndices[0] ?? 0;
            nodes.push({
                type: 'circle',
                ...datumBase$2(context, `${layer.id}:${complementary ? 'ccdf' : 'ecdf'}-point:${index}`, rowIndex, 1, {
                    kind: complementary ? 'ccdf-point' : 'ecdf-point',
                    value: point.value,
                    probability: point.probability,
                    weight: point.weight,
                    count: point.count,
                    sourceRowCount: point.rowIndices.length,
                    sourceRowIndices: point.rowIndices.slice(0, 256),
                }),
                cx: xScale.map(point.value),
                cy: yScale.map(point.probability),
                radius: layer.mark.radius ?? 2.5,
                fill: layer.mark.fill ?? stroke,
                stroke: theme.colors.background,
                lineWidth: 0.75,
            });
        });
        return nodes;
    };
    const compileKernelDensityMark = (context) => {
        const { layer, table, xScale, yScale, theme } = context;
        const observations = distributionObservations(context);
        const requestedBandwidth = layer.mark.options.bandwidth;
        const estimate = kernelDensity1d(observations, {
            points: clamp$a(Math.floor(optionNumber$5(layer.mark.options.samples, 96)), 24, Math.min(512, context.performance.maxLinePoints)),
            ...(typeof requestedBandwidth === 'number' && requestedBandwidth > 0
                ? { bandwidth: requestedBandwidth }
                : {}),
        });
        if (estimate.points.length === 0)
            return [];
        const points = estimate.points.map((point) => ({
            x: xScale.map(point.value),
            y: yScale.map(point.density),
        }));
        const stroke = layer.mark.stroke ?? theme.mark.lineColor ?? context.color;
        const rowIndex = observations[0]?.rowIndex ?? 0;
        return [
            {
                type: 'path',
                ...datumBase$2(context, `${layer.id}:kde-line`, rowIndex, 1, {
                    kind: 'kernel-density',
                    bandwidth: estimate.bandwidth,
                    sampleCount: observations.length,
                    valueField: layer.mark.fields.value ?? layer.x.field,
                    weightField: layer.mark.fields.weight ??
                        (typeof layer.mark.options.weightField === 'string'
                            ? layer.mark.options.weightField
                            : null),
                    sourceRowCount: observations.length,
                    sourceRowIndices: observations.slice(0, 256).map(({ rowIndex: source }) => source),
                }),
                points,
                closed: false,
                stroke,
                lineWidth: layer.mark.lineWidth ?? theme.mark.lineWidth,
                lineCap: theme.mark.lineCap ?? 'round',
                lineJoin: theme.mark.lineJoin ?? 'round',
            },
        ];
    };
    const compileBulletMark = (context) => {
        const { layer, table, xScale, yScale, theme, plot } = context;
        const targetField = layer.mark.fields.target ?? 'target';
        const nodes = [];
        const width = Math.max(8, xScale instanceof BandScale
            ? xScale.bandwidth * 0.62
            : plot.width / Math.max(3, table.length * 2));
        const ranges = Array.isArray(layer.mark.options.ranges)
            ? layer.mark.options.ranges.filter((value) => typeof value === 'number')
            : [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const xValue = scaleInput(table.value(rowIndex, layer.x.field));
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            const target = numericDataValue(table.value(rowIndex, targetField));
            if (xValue === null || value === null || target === null)
                continue;
            const x = xScale.map(xValue);
            const baseline = yScale.map(0);
            const rangeValues = ranges.length > 0 ? ranges : [value * 0.65, value * 0.85, Math.max(value, target) * 1.15];
            rangeValues
                .slice()
                .sort((left, right) => right - left)
                .forEach((range, index) => {
                const y = yScale.map(range);
                nodes.push({
                    type: 'rect',
                    ...nodeBase(`${layer.id}:bullet-range:${rowIndex}:${index}`, {
                        zIndex: layer.zIndex - 2 + index * 0.1,
                    }),
                    x: x - width / 2,
                    y: Math.min(y, baseline),
                    width,
                    height: Math.max(1, Math.abs(baseline - y)),
                    fill: mixColor$1(theme.colors.background, theme.colors.mutedText, 0.15 + index * 0.12),
                    lineWidth: 0,
                    cornerRadius: 2,
                });
            });
            const valueY = yScale.map(value);
            const targetY = yScale.map(target);
            nodes.push({
                type: 'rect',
                ...datumBase$2(context, `${layer.id}:bullet-value:${rowIndex}`, rowIndex),
                x: x - width * 0.22,
                y: Math.min(valueY, baseline),
                width: width * 0.44,
                height: Math.max(1, Math.abs(baseline - valueY)),
                fill: layer.mark.fill ?? context.color,
                lineWidth: 0,
                cornerRadius: layer.mark.cornerRadius ?? 2,
            });
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:bullet-target:${rowIndex}`, { zIndex: layer.zIndex + 2 }),
                x1: x - width * 0.42,
                y1: targetY,
                x2: x + width * 0.42,
                y2: targetY,
                stroke: layer.mark.stroke ?? theme.colors.text,
                lineWidth: layer.mark.lineWidth ?? 2.5,
                lineCap: theme.mark.lineCap ?? 'round',
            });
        }
        return nodes;
    };
    const compileCylinderMark = (context) => {
        const { layer, table, xScale, yScale, plot, theme } = context;
        const width = Math.max(8, xScale instanceof BandScale
            ? xScale.bandwidth * 0.58
            : plot.width / Math.max(3, table.length * 1.8));
        const ellipseHeight = clamp$a(width * 0.28, 4, 18);
        const baseline = yScale.map(0);
        const nodes = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const xValue = scaleInput(table.value(rowIndex, layer.x.field));
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            if (xValue === null || value === null)
                continue;
            const x = xScale.map(xValue);
            const y = yScale.map(value);
            if (![x, y, baseline].every(Number.isFinite))
                continue;
            const top = Math.min(y, baseline);
            const bottom = Math.max(y, baseline);
            const color = layer.mark.fill ?? paletteColor$1(context, rowIndex, table.length);
            nodes.push({
                type: 'rect',
                ...datumBase$2(context, `${layer.id}:cylinder-body:${rowIndex}`, rowIndex),
                x: x - width / 2,
                y: top,
                width,
                height: Math.max(1, bottom - top),
                fill: color,
                stroke: layer.mark.stroke ?? mixColor$1(color, theme.colors.text, 0.25),
                lineWidth: layer.mark.lineWidth ?? 1,
                cornerRadius: 0,
            });
            for (const [suffix, centerY, fill] of [
                ['top', top, mixColor$1(color, '#ffffff', 0.22)],
                ['bottom', bottom, mixColor$1(color, theme.colors.text, 0.12)],
            ]) {
                const points = Array.from({ length: 25 }, (_, index) => {
                    const angle = (index / 24) * TAU$2;
                    return {
                        x: x + Math.cos(angle) * (width / 2),
                        y: centerY + Math.sin(angle) * (ellipseHeight / 2),
                    };
                });
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:cylinder-${suffix}:${rowIndex}`, { zIndex: layer.zIndex + 1 }),
                    points,
                    closed: true,
                    fill,
                    stroke: layer.mark.stroke ?? mixColor$1(color, theme.colors.text, 0.25),
                    lineWidth: layer.mark.lineWidth ?? 1,
                });
            }
        }
        return nodes;
    };
    const compileArcDiagramMark = (context) => {
        const { layer, table, plot, theme } = context;
        const sourceField = layer.mark.fields.source ?? layer.x.field;
        const targetField = layer.mark.fields.target ?? 'target';
        const valueField = layer.mark.fields.value ?? layer.y.field;
        const names = [];
        const seen = new Set();
        const links = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const source = stringValue$3(table.value(rowIndex, sourceField));
            const target = table.has(targetField) ? stringValue$3(table.value(rowIndex, targetField)) : null;
            const value = numericDataValue(table.value(rowIndex, valueField)) ?? 1;
            if (source === null || target === null)
                continue;
            for (const name of [source, target]) {
                if (!seen.has(name)) {
                    seen.add(name);
                    names.push(name);
                }
            }
            links.push({ rowIndex, source, target, value: Math.max(0, value) });
        }
        if (names.length === 0)
            return [];
        const baseline = plot.y + plot.height * 0.78;
        const positions = new Map(names.map((name, index) => [
            name,
            plot.x + (plot.width * (index + 0.5)) / Math.max(1, names.length),
        ]));
        const maximum = Math.max(1, ...links.map(({ value }) => value));
        const nodes = [];
        for (const link of links) {
            const startX = positions.get(link.source);
            const endX = positions.get(link.target);
            if (startX === undefined || endX === undefined)
                continue;
            const height = Math.min(plot.height * 0.64, Math.abs(endX - startX) * 0.55 + 18);
            nodes.push({
                type: 'path',
                ...datumBase$2(context, `${layer.id}:arc-link:${link.rowIndex}`, link.rowIndex),
                points: quadraticPoints({ x: startX, y: baseline }, { x: (startX + endX) / 2, y: baseline - height }, { x: endX, y: baseline }, 32),
                closed: false,
                stroke: layer.mark.stroke ?? paletteColor$1(context, names.indexOf(link.source), names.length),
                lineWidth: (layer.mark.lineWidth ?? 1.6) + (link.value / maximum) * 5,
                lineCap: theme.mark.lineCap ?? 'round',
                lineJoin: 'round',
            });
        }
        names.forEach((name, index) => {
            const x = positions.get(name);
            nodes.push({
                type: 'circle',
                ...nodeBase(`${layer.id}:arc-node:${index}`, { zIndex: layer.zIndex + 2 }),
                cx: x,
                cy: baseline,
                radius: layer.mark.radius ?? 6,
                fill: layer.mark.fill ?? paletteColor$1(context, index, names.length),
                stroke: theme.colors.background,
                lineWidth: 2,
            });
            nodes.push(textNode$7(context, `${layer.id}:arc-label:${index}`, x, baseline + 17, name, {
                fill: theme.colors.mutedText,
                size: Math.max(9, theme.typography.fontSize - 1),
            }));
        });
        return nodes;
    };
    const compileItemMark = (context) => {
        const { layer, table, plot, theme } = context;
        const values = [];
        let total = 0;
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const label = stringValue$3(table.value(rowIndex, layer.x.field));
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            if (label === null || value === null || value <= 0)
                continue;
            values.push({ rowIndex, label, value });
            total += value;
        }
        if (values.length === 0 || total <= 0)
            return [];
        const count = clamp$a(Math.floor(optionNumber$5(layer.mark.options.items, 100)), 20, 400);
        const columns = Math.ceil(Math.sqrt((count * plot.width) / Math.max(1, plot.height)));
        const rows = Math.ceil(count / columns);
        const gap = 2;
        const size = Math.max(2, Math.min(plot.width / columns, plot.height / rows) - gap);
        const assignments = [];
        values.forEach((item, colorIndex) => {
            const itemCount = Math.max(1, Math.round((item.value / total) * count));
            for (let index = 0; index < itemCount && assignments.length < count; index += 1) {
                assignments.push({ rowIndex: item.rowIndex, colorIndex });
            }
        });
        while (assignments.length < count)
            assignments.push({ rowIndex: values[0].rowIndex, colorIndex: 0 });
        return assignments.map((assignment, index) => {
            const column = index % columns;
            const row = Math.floor(index / columns);
            return {
                type: 'circle',
                ...datumBase$2(context, `${layer.id}:item:${index}`, assignment.rowIndex),
                cx: plot.x + column * (size + gap) + size / 2,
                cy: plot.y + plot.height - row * (size + gap) - size / 2,
                radius: size / 2,
                fill: layer.mark.fill ?? paletteColor$1(context, assignment.colorIndex, values.length),
                stroke: theme.colors.background,
                lineWidth: 0.8,
            };
        });
    };
    const compileLollipopMark = (context) => {
        const { layer, table, xScale, yScale, theme } = context;
        const baseline = yScale.map(optionNumber$5(layer.mark.options.baseline, 0));
        const nodes = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const xValue = scaleInput(table.value(rowIndex, layer.x.field));
            const yValue = scaleInput(table.value(rowIndex, layer.y.field));
            if (xValue === null || yValue === null)
                continue;
            const x = xScale.map(xValue);
            const y = yScale.map(yValue);
            if (![x, y, baseline].every(Number.isFinite))
                continue;
            const color = layer.mark.fill ?? paletteColor$1(context, rowIndex, table.length);
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:lollipop-stem:${rowIndex}`, { zIndex: layer.zIndex }),
                x1: x,
                y1: baseline,
                x2: x,
                y2: y,
                stroke: layer.mark.stroke ?? mixColor$1(color, theme.colors.background, 0.18),
                lineWidth: layer.mark.lineWidth ?? 2,
                lineCap: theme.mark.lineCap ?? 'round',
            });
            nodes.push({
                type: 'circle',
                ...datumBase$2(context, `${layer.id}:lollipop-head:${rowIndex}`, rowIndex, 1),
                cx: x,
                cy: y,
                radius: layer.mark.radius ?? 7,
                fill: color,
                stroke: theme.colors.background,
                lineWidth: 2,
            });
        }
        return nodes;
    };
    const compilePackedBubbleMark = (context) => {
        const { layer, table, plot, theme } = context;
        const values = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            const label = stringValue$3(table.value(rowIndex, layer.x.field));
            if (value !== null && value >= 0 && label !== null)
                values.push({ rowIndex, value, label });
        }
        if (values.length === 0)
            return [];
        const maximum = Math.max(1, ...values.map(({ value }) => value));
        const center = { x: plot.x + plot.width / 2, y: plot.y + plot.height / 2 };
        const maxRadius = Math.min(plot.width, plot.height) * 0.16;
        const golden = Math.PI * (3 - Math.sqrt(5));
        const placed = [];
        const nodes = [];
        values
            .slice()
            .sort((left, right) => right.value - left.value)
            .forEach((item, index) => {
            const radius = Math.max(8, Math.sqrt(item.value / maximum) * maxRadius);
            let position = center;
            for (let attempt = 0; attempt < 240; attempt += 1) {
                const distance = attempt === 0 ? 0 : Math.sqrt(attempt) * (radius * 0.62 + 4);
                const candidate = {
                    x: center.x + Math.cos(attempt * golden) * distance,
                    y: center.y + Math.sin(attempt * golden) * distance,
                };
                const fits = placed.every((other) => Math.hypot(candidate.x - other.x, candidate.y - other.y) >= radius + other.radius + 2);
                if (fits) {
                    position = candidate;
                    break;
                }
            }
            placed.push({ ...position, radius });
            const color = layer.mark.fill ?? paletteColor$1(context, index, values.length);
            nodes.push({
                type: 'circle',
                ...datumBase$2(context, `${layer.id}:packed:${item.rowIndex}`, item.rowIndex),
                cx: position.x,
                cy: position.y,
                radius,
                fill: colorWithOpacity(color, 0.86),
                stroke: theme.colors.background,
                lineWidth: layer.mark.lineWidth ?? 2,
            });
            if (radius >= 18) {
                nodes.push(textNode$7(context, `${layer.id}:packed-label:${item.rowIndex}`, position.x, position.y, item.label, {
                    fill: readableTextColor(color, '#ffffff', '#0f172a'),
                    size: clamp$a(radius * 0.34, 9, 14),
                }));
            }
        });
        return nodes;
    };
    const compileParetoMark = (context) => {
        const { layer, table, xScale, yScale, plot, theme } = context;
        const rows = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const xValue = scaleInput(table.value(rowIndex, layer.x.field));
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            if (xValue === null || value === null)
                continue;
            const x = xScale.map(xValue);
            if (Number.isFinite(x))
                rows.push({ rowIndex, x, value: Math.max(0, value) });
        }
        if (rows.length === 0)
            return [];
        const total = rows.reduce((sum, row) => sum + row.value, 0) || 1;
        const baseline = yScale.map(0);
        const width = Math.max(5, xScale instanceof BandScale
            ? xScale.bandwidth * 0.62
            : plot.width / Math.max(3, rows.length * 1.6));
        let running = 0;
        const cumulative = [];
        const nodes = [];
        for (const row of rows) {
            const y = yScale.map(row.value);
            running += row.value;
            cumulative.push({ x: row.x, y: plot.y + plot.height * (1 - running / total) });
            nodes.push({
                type: 'rect',
                ...datumBase$2(context, `${layer.id}:pareto-bar:${row.rowIndex}`, row.rowIndex),
                x: row.x - width / 2,
                y: Math.min(y, baseline),
                width,
                height: Math.max(1, Math.abs(baseline - y)),
                fill: layer.mark.fill ?? context.color,
                stroke: theme.colors.background,
                lineWidth: 1,
                cornerRadius: layer.mark.cornerRadius ?? theme.mark.barRadius,
            });
        }
        nodes.push({
            type: 'path',
            ...nodeBase(`${layer.id}:pareto-line`, { zIndex: layer.zIndex + 2 }),
            points: cumulative,
            closed: false,
            stroke: layer.mark.stroke ?? paletteColor$1(context, 3, 4),
            lineWidth: layer.mark.lineWidth ?? 2.5,
            lineCap: theme.mark.lineCap ?? 'round',
            lineJoin: theme.mark.lineJoin ?? 'round',
        });
        cumulative.forEach((point, index) => {
            const row = rows[index];
            nodes.push({
                type: 'circle',
                ...datumBase$2(context, `${layer.id}:pareto-point:${row.rowIndex}`, row.rowIndex, 3),
                cx: point.x,
                cy: point.y,
                radius: 3.5,
                fill: theme.colors.background,
                stroke: layer.mark.stroke ?? paletteColor$1(context, 3, 4),
                lineWidth: 2,
            });
        });
        return nodes;
    };
    const compilePolygonMark = (context) => {
        const { layer, table, xScale, yScale, theme } = context;
        const seriesField = layer.mark.fields.series;
        const groups = new Map();
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const xValue = scaleInput(table.value(rowIndex, layer.x.field));
            const yValue = scaleInput(table.value(rowIndex, layer.y.field));
            if (xValue === null || yValue === null)
                continue;
            const point = { x: xScale.map(xValue), y: yScale.map(yValue) };
            if (![point.x, point.y].every(Number.isFinite))
                continue;
            const key = seriesField === undefined || !table.has(seriesField)
                ? 'Series'
                : (stringValue$3(table.value(rowIndex, seriesField)) ?? 'Series');
            const group = groups.get(key) ?? [];
            group.push({ rowIndex, point });
            groups.set(key, group);
        }
        const nodes = [];
        [...groups.entries()].forEach(([key, rows], groupIndex) => {
            if (rows.length < 3)
                return;
            const color = layer.mark.fill ?? paletteColor$1(context, groupIndex, groups.size);
            nodes.push({
                type: 'path',
                ...datumBase$2(context, `${layer.id}:polygon:${groupIndex}`, rows[0].rowIndex),
                points: rows.map(({ point }) => point),
                closed: true,
                fill: colorWithOpacity(color, 0.24),
                stroke: layer.mark.stroke ?? color,
                lineWidth: layer.mark.lineWidth ?? 2,
                lineJoin: theme.mark.lineJoin ?? 'round',
            });
            rows.forEach(({ rowIndex, point }) => {
                nodes.push({
                    type: 'circle',
                    ...datumBase$2(context, `${layer.id}:polygon-point:${rowIndex}`, rowIndex, 1),
                    cx: point.x,
                    cy: point.y,
                    radius: layer.mark.radius ?? 3.5,
                    fill: theme.colors.background,
                    stroke: layer.mark.stroke ?? color,
                    lineWidth: 1.5,
                });
            });
            if (layer.mark.options.labels === true) {
                const center = rows.reduce((sum, row) => ({
                    x: sum.x + row.point.x / rows.length,
                    y: sum.y + row.point.y / rows.length,
                }), { x: 0, y: 0 });
                nodes.push(textNode$7(context, `${layer.id}:polygon-label:${groupIndex}`, center.x, center.y, key));
            }
        });
        return nodes;
    };
    const compilePyramidMark = (context) => {
        const { layer, table, plot, theme } = context;
        const rows = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const label = stringValue$3(table.value(rowIndex, layer.x.field));
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            if (label !== null && value !== null && value >= 0)
                rows.push({ rowIndex, label, value });
        }
        if (rows.length === 0)
            return [];
        const variant = optionString$5(layer.mark.options.variant, 'pyramid');
        const reverse = variant.includes('funnel');
        const depth = variant.includes('3d')
            ? clamp$a(optionNumber$5(layer.mark.options.depth, 12), 4, 24)
            : 0;
        const sorted = layer.mark.options.sort === false ? rows : rows.slice().sort((a, b) => b.value - a.value);
        const maximum = Math.max(1, ...sorted.map(({ value }) => value));
        const height = plot.height / sorted.length;
        const centerX = plot.x + plot.width / 2 - depth / 2;
        const maxWidth = plot.width * 0.82;
        const nodes = [];
        sorted.forEach((row, index) => {
            const currentRatio = clamp$a(row.value / maximum, 0.06, 1);
            const nextRatio = clamp$a((sorted[index + 1]?.value ?? 0) / maximum, 0.03, 1);
            const topRatio = reverse ? currentRatio : nextRatio;
            const bottomRatio = reverse ? nextRatio : currentRatio;
            const y1 = plot.y + index * height + 1;
            const y2 = plot.y + (index + 1) * height - 1;
            const color = layer.mark.fill ?? paletteColor$1(context, index, sorted.length);
            const front = [
                { x: centerX - (maxWidth * topRatio) / 2, y: y1 },
                { x: centerX + (maxWidth * topRatio) / 2, y: y1 },
                { x: centerX + (maxWidth * bottomRatio) / 2, y: y2 },
                { x: centerX - (maxWidth * bottomRatio) / 2, y: y2 },
            ];
            if (depth > 0) {
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:pyramid-depth:${row.rowIndex}`, { zIndex: layer.zIndex }),
                    points: [
                        front[1],
                        { x: front[1].x + depth, y: front[1].y - depth * 0.45 },
                        { x: front[2].x + depth, y: front[2].y - depth * 0.45 },
                        front[2],
                    ],
                    closed: true,
                    fill: mixColor$1(color, theme.colors.text, 0.18),
                    lineWidth: 0,
                });
            }
            nodes.push({
                type: 'path',
                ...datumBase$2(context, `${layer.id}:pyramid:${row.rowIndex}`, row.rowIndex, 1),
                points: front,
                closed: true,
                fill: color,
                stroke: layer.mark.stroke ?? theme.colors.background,
                lineWidth: layer.mark.lineWidth ?? 1,
                lineJoin: 'round',
            });
            if (Math.min(maxWidth * topRatio, maxWidth * bottomRatio) > 62) {
                nodes.push(textNode$7(context, `${layer.id}:pyramid-label:${row.rowIndex}`, centerX, (y1 + y2) / 2, row.label, {
                    fill: readableTextColor(color, '#ffffff', '#0f172a'),
                    size: Math.max(9, theme.typography.fontSize - 1),
                }));
            }
        });
        return nodes;
    };
    const compileScatter3dMark = (context) => {
        const { layer, table, xScale, yScale, theme } = context;
        const zField = layer.mark.fields.z ?? 'z';
        const extent = table.has(zField) ? table.extent(zField) : null;
        const nodes = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const xValue = scaleInput(table.value(rowIndex, layer.x.field));
            const yValue = scaleInput(table.value(rowIndex, layer.y.field));
            const z = table.has(zField) ? numericDataValue(table.value(rowIndex, zField)) : 0;
            if (xValue === null || yValue === null || z === null)
                continue;
            const ratio = extent === null || extent[1] === extent[0] ? 0.5 : (z - extent[0]) / (extent[1] - extent[0]);
            const perspective = (ratio - 0.5) * clamp$a(optionNumber$5(layer.mark.options.perspective, 18), 0, 42);
            const x = xScale.map(xValue) + perspective;
            const y = yScale.map(yValue) - perspective * 0.55;
            const radius = (layer.mark.radius ?? 5) * (0.72 + ratio * 0.9);
            const color = layer.mark.fill ?? paletteColor$1(context, rowIndex, table.length);
            nodes.push({
                type: 'circle',
                ...nodeBase(`${layer.id}:scatter-shadow:${rowIndex}`, {
                    zIndex: layer.zIndex,
                    opacity: 0.16,
                }),
                cx: x + 3,
                cy: y + 4,
                radius: radius * 1.05,
                fill: theme.colors.text,
                lineWidth: 0,
            });
            nodes.push({
                type: 'circle',
                ...datumBase$2(context, `${layer.id}:scatter-3d:${rowIndex}`, rowIndex, ratio),
                cx: x,
                cy: y,
                radius,
                fill: mixColor$1(color, '#ffffff', (1 - ratio) * 0.18),
                stroke: layer.mark.stroke ?? theme.colors.background,
                lineWidth: layer.mark.lineWidth ?? 1.5,
            });
        }
        return nodes;
    };
    const compileSolidGaugeMark = (context) => {
        const { layer, table, plot, theme } = context;
        const minimum = optionNumber$5(layer.mark.options.min, 0);
        const maximum = optionNumber$5(layer.mark.options.max, 100);
        const start = optionNumber$5(layer.mark.options.startAngle, -Math.PI * 0.75);
        const end = optionNumber$5(layer.mark.options.endAngle, Math.PI * 0.75);
        const cx = plot.x + plot.width / 2;
        const cy = plot.y + plot.height * 0.58;
        const outer = Math.min(plot.width, plot.height) * 0.38;
        const thickness = clamp$a(optionNumber$5(layer.mark.options.thickness, outer * 0.18), 6, outer * 0.45);
        const nodes = [
            {
                type: 'path',
                ...nodeBase(`${layer.id}:solid-track`, { zIndex: layer.zIndex }),
                points: annularSector$1(cx, cy, outer - thickness, outer, start, end),
                closed: true,
                fill: colorWithOpacity(theme.colors.grid, 0.62),
                lineWidth: 0,
            },
        ];
        const count = Math.max(1, table.length);
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            if (value === null)
                continue;
            const ratio = clamp$a((value - minimum) / Math.max(1e-9, maximum - minimum), 0, 1);
            const ringOuter = outer - rowIndex * (thickness + 5);
            const ringInner = Math.max(4, ringOuter - thickness);
            const color = layer.mark.fill ?? paletteColor$1(context, rowIndex, table.length);
            nodes.push({
                type: 'path',
                ...datumBase$2(context, `${layer.id}:solid-value:${rowIndex}`, rowIndex, 1),
                points: annularSector$1(cx, cy, ringInner, ringOuter, start, start + (end - start) * ratio),
                closed: true,
                fill: color,
                stroke: theme.colors.background,
                lineWidth: layer.mark.lineWidth ?? 1,
            });
            if (rowIndex === count - 1) {
                nodes.push(textNode$7(context, `${layer.id}:solid-label`, cx, cy + outer * 0.2, `${Math.round(value)}`, {
                    size: clamp$a(outer * 0.2, 16, 30),
                    weight: 700,
                }));
            }
        }
        return nodes;
    };
    function tilePolygon(shape, x, y, width, height) {
        if (shape === 'diamond') {
            return [
                { x, y: y - height / 2 },
                { x: x + width / 2, y },
                { x, y: y + height / 2 },
                { x: x - width / 2, y },
            ];
        }
        return [
            { x: x - width / 2, y: y - height * 0.25 },
            { x, y: y - height / 2 },
            { x: x + width / 2, y: y - height * 0.25 },
            { x: x + width / 2, y: y + height * 0.25 },
            { x, y: y + height / 2 },
            { x: x - width / 2, y: y + height * 0.25 },
        ];
    }
    const compileTilemapMark = (context) => {
        const { layer, table, xScale, yScale, theme } = context;
        const valueField = layer.mark.fields.value ?? 'value';
        const values = table.has(valueField) ? table.extent(valueField) : null;
        const shape = optionString$5(layer.mark.options.shape, 'hexagon');
        const width = xScale instanceof BandScale ? xScale.bandwidth * 0.92 : 24;
        const height = yScale instanceof BandScale ? yScale.bandwidth * 0.92 : 22;
        const nodes = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const xValue = scaleInput(table.value(rowIndex, layer.x.field));
            const yValue = scaleInput(table.value(rowIndex, layer.y.field));
            const value = table.has(valueField) ? numericDataValue(table.value(rowIndex, valueField)) : 0;
            if (xValue === null || yValue === null || value === null)
                continue;
            const x = xScale.map(xValue);
            const y = yScale.map(yValue);
            const ratio = values === null || values[1] === values[0]
                ? 0.5
                : (value - values[0]) / (values[1] - values[0]);
            const color = layer.mark.fill ?? mappedContinuousColor(theme, ratio);
            if (shape === 'circle') {
                nodes.push({
                    type: 'circle',
                    ...datumBase$2(context, `${layer.id}:tile:${rowIndex}`, rowIndex),
                    cx: x,
                    cy: y,
                    radius: Math.min(width, height) / 2,
                    fill: color,
                    stroke: layer.mark.stroke ?? theme.colors.background,
                    lineWidth: layer.mark.lineWidth ?? 1,
                });
            }
            else if (shape === 'square') {
                nodes.push({
                    type: 'rect',
                    ...datumBase$2(context, `${layer.id}:tile:${rowIndex}`, rowIndex),
                    x: x - width / 2,
                    y: y - height / 2,
                    width,
                    height,
                    fill: color,
                    stroke: layer.mark.stroke ?? theme.colors.background,
                    lineWidth: layer.mark.lineWidth ?? 1,
                    cornerRadius: layer.mark.cornerRadius ?? 2,
                });
            }
            else {
                nodes.push({
                    type: 'path',
                    ...datumBase$2(context, `${layer.id}:tile:${rowIndex}`, rowIndex),
                    points: tilePolygon(shape, x, y, width, height),
                    closed: true,
                    fill: color,
                    stroke: layer.mark.stroke ?? theme.colors.background,
                    lineWidth: layer.mark.lineWidth ?? 1,
                    lineJoin: 'round',
                });
            }
        }
        return nodes;
    };
    const compileVariablePieMark = (context) => {
        const { layer, table, plot, theme } = context;
        const radiusField = layer.mark.fields.radius ?? layer.mark.fields.size ?? 'radius';
        const rows = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const label = stringValue$3(table.value(rowIndex, layer.x.field));
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            const radius = table.has(radiusField)
                ? numericDataValue(table.value(rowIndex, radiusField))
                : value;
            if (label !== null && value !== null && radius !== null && value >= 0)
                rows.push({ rowIndex, label, value, radius: Math.max(0, radius) });
        }
        const total = rows.reduce((sum, row) => sum + row.value, 0);
        if (rows.length === 0 || total <= 0)
            return [];
        const cx = plot.x + plot.width / 2;
        const cy = plot.y + plot.height / 2;
        const maxRadius = Math.min(plot.width, plot.height) * 0.42;
        const minRadius = maxRadius * 0.48;
        const radiusMaximum = Math.max(1, ...rows.map(({ radius }) => radius));
        let angle = -Math.PI / 2;
        const nodes = [];
        rows.forEach((row, index) => {
            const next = angle + (row.value / total) * TAU$2;
            const outer = minRadius + Math.sqrt(row.radius / radiusMaximum) * (maxRadius - minRadius);
            const color = layer.mark.fill ?? paletteColor$1(context, index, rows.length);
            nodes.push({
                type: 'path',
                ...datumBase$2(context, `${layer.id}:variable-pie:${row.rowIndex}`, row.rowIndex),
                points: annularSector$1(cx, cy, optionNumber$5(layer.mark.options.innerRadius, 0), outer, angle, next),
                closed: true,
                fill: color,
                stroke: layer.mark.stroke ?? theme.colors.background,
                lineWidth: layer.mark.lineWidth ?? 2,
                lineJoin: 'round',
            });
            const mid = (angle + next) / 2;
            if (next - angle > 0.28) {
                const labelPoint = pointOnCircle$1(cx, cy, outer * 0.66, mid);
                nodes.push(textNode$7(context, `${layer.id}:variable-label:${row.rowIndex}`, labelPoint.x, labelPoint.y, row.label, {
                    fill: readableTextColor(color, '#ffffff', '#0f172a'),
                    size: Math.max(9, theme.typography.fontSize - 1),
                }));
            }
            angle = next;
        });
        return nodes;
    };
    const compileVariwideMark = (context) => {
        const { layer, table, yScale, plot, theme } = context;
        const widthField = layer.mark.fields.width ?? 'width';
        const rows = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            const width = table.has(widthField) ? numericDataValue(table.value(rowIndex, widthField)) : 1;
            const label = stringValue$3(table.value(rowIndex, layer.x.field));
            if (value !== null && width !== null && width > 0 && label !== null)
                rows.push({ rowIndex, value, width, label });
        }
        const widthTotal = rows.reduce((sum, row) => sum + row.width, 0);
        if (rows.length === 0 || widthTotal <= 0)
            return [];
        const baseline = yScale.map(0);
        const nodes = [];
        let cursor = plot.x;
        rows.forEach((row, index) => {
            const width = (row.width / widthTotal) * plot.width;
            const y = yScale.map(row.value);
            const color = layer.mark.fill ?? paletteColor$1(context, index, rows.length);
            nodes.push({
                type: 'rect',
                ...datumBase$2(context, `${layer.id}:variwide:${row.rowIndex}`, row.rowIndex),
                x: cursor + 1,
                y: Math.min(y, baseline),
                width: Math.max(1, width - 2),
                height: Math.max(1, Math.abs(baseline - y)),
                fill: color,
                stroke: layer.mark.stroke ?? theme.colors.background,
                lineWidth: layer.mark.lineWidth ?? 1,
                cornerRadius: layer.mark.cornerRadius ?? theme.mark.barRadius,
            });
            if (width >= 36)
                nodes.push(textNode$7(context, `${layer.id}:variwide-label:${row.rowIndex}`, cursor + width / 2, baseline + 14, row.label, { fill: theme.colors.mutedText, size: 9 }));
            cursor += width;
        });
        return nodes;
    };
    const compileVennMark = (context) => {
        const { layer, table, plot, theme } = context;
        const rows = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const label = stringValue$3(table.value(rowIndex, layer.x.field));
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            if (label !== null && value !== null && value > 0)
                rows.push({ rowIndex, label, value });
        }
        if (rows.length === 0)
            return [];
        const maximum = Math.max(...rows.map(({ value }) => value));
        const cx = plot.x + plot.width / 2;
        const cy = plot.y + plot.height / 2;
        const orbit = Math.min(plot.width, plot.height) * 0.13;
        const maxRadius = Math.min(plot.width, plot.height) * 0.25;
        const nodes = [];
        rows.slice(0, 6).forEach((row, index, visibleRows) => {
            const angle = -Math.PI / 2 + (index * TAU$2) / Math.max(1, visibleRows.length);
            const center = pointOnCircle$1(cx, cy, visibleRows.length === 1 ? 0 : orbit, angle);
            const radius = Math.max(24, Math.sqrt(row.value / maximum) * maxRadius);
            const color = layer.mark.fill ?? paletteColor$1(context, index, visibleRows.length);
            nodes.push({
                type: 'circle',
                ...datumBase$2(context, `${layer.id}:venn:${row.rowIndex}`, row.rowIndex, index * 0.01),
                cx: center.x,
                cy: center.y,
                radius,
                fill: colorWithOpacity(color, 0.34),
                stroke: layer.mark.stroke ?? color,
                lineWidth: layer.mark.lineWidth ?? 2,
            });
            nodes.push(textNode$7(context, `${layer.id}:venn-label:${row.rowIndex}`, center.x, center.y, row.label, {
                fill: theme.colors.text,
                size: Math.max(10, theme.typography.fontSize),
            }));
        });
        return nodes;
    };
    const compileWindBarbMark = (context) => {
        const { layer, table, xScale, yScale, theme } = context;
        const speedField = layer.mark.fields.speed ?? 'speed';
        const directionField = layer.mark.fields.direction ?? 'direction';
        const nodes = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const xValue = scaleInput(table.value(rowIndex, layer.x.field));
            const yValue = scaleInput(table.value(rowIndex, layer.y.field));
            const speed = table.has(speedField)
                ? numericDataValue(table.value(rowIndex, speedField))
                : null;
            const direction = table.has(directionField)
                ? numericDataValue(table.value(rowIndex, directionField))
                : null;
            if (xValue === null || yValue === null || speed === null || direction === null)
                continue;
            const start = { x: xScale.map(xValue), y: yScale.map(yValue) };
            const length = clamp$a(18 + speed * 0.35, 20, 44);
            const angle = (direction * Math.PI) / 180 - Math.PI / 2;
            const end = pointOnCircle$1(start.x, start.y, length, angle);
            const stroke = layer.mark.stroke ?? context.color;
            nodes.push({
                type: 'line',
                ...datumBase$2(context, `${layer.id}:wind:${rowIndex}`, rowIndex),
                x1: start.x,
                y1: start.y,
                x2: end.x,
                y2: end.y,
                stroke,
                lineWidth: layer.mark.lineWidth ?? 2,
                lineCap: 'round',
            });
            const featherCount = clamp$a(Math.round(speed / 10), 1, 6);
            for (let feather = 0; feather < featherCount; feather += 1) {
                const ratio = 0.32 + (feather / featherCount) * 0.58;
                const anchor = {
                    x: start.x + (end.x - start.x) * ratio,
                    y: start.y + (end.y - start.y) * ratio,
                };
                const tip = pointOnCircle$1(anchor.x, anchor.y, 8, angle - Math.PI * 0.7);
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:wind-feather:${rowIndex}:${feather}`, {
                        zIndex: layer.zIndex + 1,
                    }),
                    x1: anchor.x,
                    y1: anchor.y,
                    x2: tip.x,
                    y2: tip.y,
                    stroke,
                    lineWidth: 1.5,
                    lineCap: 'round',
                });
            }
            nodes.push({
                type: 'circle',
                ...nodeBase(`${layer.id}:wind-origin:${rowIndex}`, { zIndex: layer.zIndex + 2 }),
                cx: start.x,
                cy: start.y,
                radius: 2.5,
                fill: theme.colors.background,
                stroke,
                lineWidth: 1.5,
            });
        }
        return nodes;
    };
    const compileWordCloudMark = (context) => {
        const { layer, table, plot, theme } = context;
        const rows = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const word = stringValue$3(table.value(rowIndex, layer.x.field));
            const weight = numericDataValue(table.value(rowIndex, layer.y.field));
            if (word !== null && word !== '' && weight !== null && weight > 0)
                rows.push({ rowIndex, word, weight });
        }
        if (rows.length === 0)
            return [];
        const minimum = Math.min(...rows.map(({ weight }) => weight));
        const maximum = Math.max(...rows.map(({ weight }) => weight));
        const cx = plot.x + plot.width / 2;
        const cy = plot.y + plot.height / 2;
        const nodes = [];
        const placed = [];
        const visibleCount = Math.min(rows.length, 160, context.performance.maxPointMarks);
        rows
            .slice()
            .sort((left, right) => right.weight - left.weight)
            .slice(0, visibleCount)
            .forEach((row, index) => {
            const ratio = maximum === minimum ? 0.5 : (row.weight - minimum) / (maximum - minimum);
            const size = 10 + ratio * 24;
            const rotation = index % 5 === 0 ? -18 : index % 7 === 0 ? 18 : 0;
            const textWidth = Math.max(size, row.word.length * size * 0.56);
            const textHeight = size * 1.08;
            const radians = (Math.abs(rotation) * Math.PI) / 180;
            const width = textWidth * Math.cos(radians) + textHeight * Math.sin(radians);
            const height = textWidth * Math.sin(radians) + textHeight * Math.cos(radians);
            let x = cx;
            let y = cy;
            let found = false;
            for (let attempt = 0; attempt < 1600; attempt += 1) {
                const angle = attempt * 0.42 + index * 1.17;
                const distance = 3.3 * Math.sqrt(attempt) * (1 + size / 40);
                const candidateX = cx + Math.cos(angle) * distance;
                const candidateY = cy + Math.sin(angle) * distance * 0.64;
                const box = {
                    left: candidateX - width / 2 - 3,
                    right: candidateX + width / 2 + 3,
                    top: candidateY - height / 2 - 2,
                    bottom: candidateY + height / 2 + 2,
                };
                const inside = box.left >= plot.x &&
                    box.right <= plot.x + plot.width &&
                    box.top >= plot.y &&
                    box.bottom <= plot.y + plot.height;
                const free = placed.every((other) => box.right < other.left ||
                    box.left > other.right ||
                    box.bottom < other.top ||
                    box.top > other.bottom);
                if (inside && free) {
                    x = candidateX;
                    y = candidateY;
                    placed.push(box);
                    found = true;
                    break;
                }
            }
            if (!found)
                return;
            const color = layer.mark.fill ?? paletteColor$1(context, index, visibleCount);
            const node = textNode$7(context, `${layer.id}:word-cloud:${row.rowIndex}`, x, y, row.word, {
                fill: color,
                size,
                weight: 520 + Math.round(ratio * 220),
                rotation,
            });
            nodes.push({ ...node, ...datumBase$2(context, node.id, row.rowIndex, ratio) });
        });
        return nodes;
    };
    function indicatorKind(context) {
        return optionString$5(context.layer.mark.options.kind, 'line');
    }
    function indicatorFields(context) {
        const fields = context.layer.mark.options.fields;
        if (Array.isArray(fields)) {
            const valid = fields.filter((value) => typeof value === 'string' && value !== '');
            if (valid.length > 0)
                return valid;
        }
        const configured = [
            context.layer.mark.fields.middle,
            context.layer.mark.fields.signal,
            context.layer.mark.fields.secondary,
        ].filter((value) => value !== undefined && context.table.has(value));
        return configured.length > 0 ? configured : [context.layer.y.field];
    }
    const compileIndicatorMark = (context) => {
        const { layer, table, xScale, yScale, theme, plot } = context;
        const kind = indicatorKind(context);
        const capability = resolveTechnicalIndicatorCapability(kind);
        const lowerField = layer.mark.fields.lower ?? 'lower';
        const upperField = layer.mark.fields.upper ?? 'upper';
        const fields = indicatorFields(context);
        const calculated = layer.mark.options.calculate === true;
        const sourceField = layer.mark.fields.__indicatorSource;
        const indicatorTooltip = (rowIndex, value) => {
            if (!calculated || capability === null)
                return undefined;
            const row = table.row(rowIndex);
            return {
                ...Object.fromEntries(Object.entries(row).filter(([field]) => !field.startsWith('__graflume_'))),
                indicatorId: capability.id,
                indicatorKind: capability.kind,
                indicatorValue: value,
                indicatorSource: sourceField === undefined ? null : row[sourceField],
                indicatorWarmUp: 'null',
                indicatorProvenance: capability.provenance,
            };
        };
        const nodes = [];
        if (table.has(lowerField) && table.has(upperField)) {
            const upper = [];
            const lower = [];
            for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
                const xValue = scaleInput(table.value(rowIndex, layer.x.field));
                const low = numericDataValue(table.value(rowIndex, lowerField));
                const high = numericDataValue(table.value(rowIndex, upperField));
                if (xValue === null || low === null || high === null)
                    continue;
                const x = xScale.map(xValue);
                upper.push({ x, y: yScale.map(high) });
                lower.push({ x, y: yScale.map(low) });
            }
            if (upper.length > 1 && lower.length > 1) {
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:indicator-band`, { zIndex: layer.zIndex, opacity: 0.24 }),
                    points: [...upper, ...lower.reverse()],
                    closed: true,
                    fill: layer.mark.fill ?? colorWithOpacity(context.color, 0.28),
                    stroke: layer.mark.stroke ?? context.color,
                    lineWidth: 1,
                    lineJoin: theme.mark.lineJoin ?? 'round',
                });
            }
        }
        const columnKinds = new Set(['ao', 'macd', 'volume', 'histogram']);
        fields.forEach((field, fieldIndex) => {
            const points = [];
            for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
                const xValue = scaleInput(table.value(rowIndex, layer.x.field));
                const value = table.has(field) ? numericDataValue(table.value(rowIndex, field)) : null;
                if (xValue === null || value === null || value === undefined)
                    continue;
                const point = { x: xScale.map(xValue), y: yScale.map(value) };
                if ([point.x, point.y].every(Number.isFinite))
                    points.push({ rowIndex, point, value });
            }
            const color = fieldIndex === 0
                ? (layer.mark.stroke ?? context.color)
                : paletteColor$1(context, fieldIndex + 1, fields.length + 1);
            if (columnKinds.has(kind)) {
                const baseline = yScale.map(0);
                const width = Math.max(2, plot.width / Math.max(8, table.length * 1.6));
                points.forEach(({ rowIndex, point, value }) => {
                    nodes.push({
                        type: 'rect',
                        ...datumBase$2(context, `${layer.id}:indicator-column:${fieldIndex}:${rowIndex}`, rowIndex, fieldIndex, indicatorTooltip(rowIndex, value)),
                        x: point.x - width / 2,
                        y: Math.min(point.y, baseline),
                        width,
                        height: Math.max(1, Math.abs(baseline - point.y)),
                        fill: layer.mark.fill ?? (value >= 0 ? color : paletteColor$1(context, 3, 4)),
                        lineWidth: 0,
                        cornerRadius: 1,
                    });
                });
            }
            else if (points.length > 0) {
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:indicator-line:${fieldIndex}`, {
                        zIndex: layer.zIndex + fieldIndex + 1,
                    }),
                    points: kind === 'psar'
                        ? points.map(({ point }) => point)
                        : smoothPoints(points.map(({ point }) => point), 4),
                    closed: false,
                    stroke: color,
                    lineWidth: layer.mark.lineWidth ?? (fieldIndex === 0 ? 2.2 : 1.5),
                    ...(fieldIndex > 0 ? { dash: [5, 3] } : {}),
                    lineCap: theme.mark.lineCap ?? 'round',
                    lineJoin: theme.mark.lineJoin ?? 'round',
                });
                points.forEach(({ rowIndex, point }, index) => {
                    if (kind !== 'psar' && index % Math.max(1, Math.ceil(points.length / 24)) !== 0)
                        return;
                    nodes.push({
                        type: 'circle',
                        ...datumBase$2(context, `${layer.id}:indicator-point:${fieldIndex}:${rowIndex}`, rowIndex, fieldIndex + 2, indicatorTooltip(rowIndex, points[index]?.value ?? 0)),
                        cx: point.x,
                        cy: point.y,
                        radius: kind === 'psar' ? 3.2 : 2.2,
                        fill: color,
                        stroke: theme.colors.background,
                        lineWidth: 0.8,
                    });
                });
            }
        });
        return nodes;
    };
    const compileFlagsMark = (context) => {
        const { layer, table, xScale, yScale, theme } = context;
        const titleField = layer.mark.fields.title ?? 'title';
        const nodes = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const xValue = scaleInput(table.value(rowIndex, layer.x.field));
            const yValue = scaleInput(table.value(rowIndex, layer.y.field));
            if (xValue === null || yValue === null)
                continue;
            const x = xScale.map(xValue);
            const y = yScale.map(yValue);
            const label = table.has(titleField)
                ? (stringValue$3(table.value(rowIndex, titleField)) ?? '•')
                : '•';
            const color = layer.mark.fill ?? context.color;
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:flag-pole:${rowIndex}`, { zIndex: layer.zIndex }),
                x1: x,
                y1: y,
                x2: x,
                y2: y - 24,
                stroke: layer.mark.stroke ?? color,
                lineWidth: 1.5,
                lineCap: theme.mark.lineCap ?? 'round',
            });
            nodes.push({
                type: 'path',
                ...datumBase$2(context, `${layer.id}:flag:${rowIndex}`, rowIndex, 1),
                points: [
                    { x, y: y - 27 },
                    { x: x + 30, y: y - 27 },
                    { x: x + 24, y: y - 17 },
                    { x, y: y - 17 },
                ],
                closed: true,
                fill: color,
                stroke: theme.colors.background,
                lineWidth: 1,
                lineJoin: 'round',
            });
            nodes.push(textNode$7(context, `${layer.id}:flag-label:${rowIndex}`, x + 14, y - 22, label.slice(0, 4), {
                fill: readableTextColor(color, '#ffffff', '#0f172a'),
                size: 9,
            }));
        }
        return nodes;
    };
    function financialRows$1(context) {
        const { layer, table, xScale } = context;
        const openField = layer.mark.fields.open ?? 'open';
        const highField = layer.mark.fields.high ?? 'high';
        const lowField = layer.mark.fields.low ?? 'low';
        const closeField = layer.mark.fields.close ?? layer.y.field;
        const rows = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const xValue = scaleInput(table.value(rowIndex, layer.x.field));
            const open = table.has(openField) ? numericDataValue(table.value(rowIndex, openField)) : null;
            const high = table.has(highField) ? numericDataValue(table.value(rowIndex, highField)) : null;
            const low = table.has(lowField) ? numericDataValue(table.value(rowIndex, lowField)) : null;
            const close = table.has(closeField)
                ? numericDataValue(table.value(rowIndex, closeField))
                : null;
            if (xValue === null || high === null || low === null || close === null)
                continue;
            rows.push({ rowIndex, x: xScale.map(xValue), open: open ?? close, high, low, close });
        }
        return rows;
    }
    function heikinRows(rows) {
        let previousOpen = null;
        let previousClose = null;
        return rows.map((row) => {
            const close = (row.open + row.high + row.low + row.close) / 4;
            const open = previousOpen === null || previousClose === null
                ? (row.open + row.close) / 2
                : (previousOpen + previousClose) / 2;
            const transformed = {
                ...row,
                open,
                close,
                high: Math.max(row.high, open, close),
                low: Math.min(row.low, open, close),
            };
            previousOpen = open;
            previousClose = close;
            return transformed;
        });
    }
    const compileFinancialMark = (context) => {
        const { layer, yScale, xScale, theme, plot } = context;
        const kind = optionString$5(layer.mark.options.kind, 'ohlc');
        const rows = kind === 'heikin-ashi' ? heikinRows(financialRows$1(context)) : financialRows$1(context);
        const width = Math.max(5, xScale instanceof BandScale
            ? xScale.bandwidth * 0.56
            : plot.width / Math.max(6, rows.length * 1.8));
        const nodes = [];
        for (const row of rows) {
            const high = yScale.map(row.high);
            const low = yScale.map(row.low);
            const open = yScale.map(row.open);
            const close = yScale.map(row.close);
            const rising = row.close >= row.open;
            const color = rising ? paletteColor$1(context, 1, 4) : paletteColor$1(context, 3, 4);
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:financial-wick:${row.rowIndex}`, { zIndex: layer.zIndex }),
                x1: row.x,
                y1: high,
                x2: row.x,
                y2: low,
                stroke: layer.mark.stroke ?? color,
                lineWidth: layer.mark.lineWidth ?? 1.5,
                lineCap: theme.mark.lineCap ?? 'round',
            });
            if (kind === 'ohlc' || kind === 'hlc') {
                if (kind === 'ohlc')
                    nodes.push({
                        type: 'line',
                        ...nodeBase(`${layer.id}:financial-open:${row.rowIndex}`, { zIndex: layer.zIndex + 1 }),
                        x1: row.x - width / 2,
                        y1: open,
                        x2: row.x,
                        y2: open,
                        stroke: color,
                        lineWidth: 1.8,
                    });
                nodes.push({
                    type: 'line',
                    ...datumBase$2(context, `${layer.id}:financial-close:${row.rowIndex}`, row.rowIndex, 1),
                    x1: row.x,
                    y1: close,
                    x2: row.x + width / 2,
                    y2: close,
                    stroke: color,
                    lineWidth: 1.8,
                });
                continue;
            }
            const hollow = kind === 'hollow-candlestick' && rising;
            nodes.push({
                type: 'rect',
                ...datumBase$2(context, `${layer.id}:financial-body:${row.rowIndex}`, row.rowIndex, 1),
                x: row.x - width / 2,
                y: Math.min(open, close),
                width,
                height: Math.max(1.5, Math.abs(open - close)),
                fill: hollow ? theme.colors.background : (layer.mark.fill ?? color),
                stroke: layer.mark.stroke ?? color,
                lineWidth: layer.mark.lineWidth ?? 1.5,
                cornerRadius: layer.mark.cornerRadius ?? 1.5,
            });
        }
        return nodes;
    };
    const compilePointFigureMark = (context) => {
        const { layer, table, plot, theme } = context;
        const values = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            if (value !== null)
                values.push({ rowIndex, value });
        }
        if (values.length < 2)
            return [];
        const minimum = Math.min(...values.map(({ value }) => value));
        const maximum = Math.max(...values.map(({ value }) => value));
        const box = Math.max(1e-9, optionNumber$5(layer.mark.options.boxSize, (maximum - minimum) / 10 || 1));
        const cellWidth = plot.width / values.length;
        const cellHeight = plot.height / Math.max(4, Math.ceil((maximum - minimum) / box) + 1);
        const nodes = [];
        values.forEach((row, index) => {
            const previous = values[Math.max(0, index - 1)]?.value ?? row.value;
            const rising = row.value >= previous;
            const count = Math.max(1, Math.round(Math.abs(row.value - previous) / box));
            const color = rising ? paletteColor$1(context, 1, 4) : paletteColor$1(context, 3, 4);
            for (let level = 0; level < count; level += 1) {
                const x = plot.x + cellWidth * (index + 0.5);
                const baseLevel = Math.round((Math.min(row.value, previous) - minimum) / box);
                const y = plot.y + plot.height - cellHeight * (baseLevel + level + 0.5);
                nodes.push({
                    ...textNode$7(context, `${layer.id}:point-figure:${row.rowIndex}:${level}`, x, y, rising ? '×' : '○', {
                        fill: layer.mark.stroke ?? color,
                        size: clamp$a(Math.min(cellWidth, cellHeight) * 0.82, 9, 22),
                        weight: 700,
                    }),
                    ...datumBase$2(context, `${layer.id}:point-figure:${row.rowIndex}:${level}`, row.rowIndex),
                });
            }
        });
        return nodes;
    };
    const compileVolumeProfileMark = (context) => {
        const { layer, table, plot, theme } = context;
        const priceField = layer.mark.fields.price ?? layer.y.field;
        const volumeField = layer.mark.fields.volume ?? 'volume';
        const bins = clamp$a(Math.floor(optionNumber$5(layer.mark.options.bins, 12)), 4, 50);
        const priceExtent = table.has(priceField) ? table.extent(priceField) : null;
        if (priceExtent === null || !table.has(volumeField))
            return [];
        const totals = Array.from({ length: bins }, () => 0);
        const rowIndexes = Array.from({ length: bins }, () => -1);
        const span = priceExtent[1] - priceExtent[0] || 1;
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const price = numericDataValue(table.value(rowIndex, priceField));
            const volume = numericDataValue(table.value(rowIndex, volumeField));
            if (price === null || volume === null)
                continue;
            const bin = clamp$a(Math.floor(((price - priceExtent[0]) / span) * bins), 0, bins - 1);
            totals[bin] = (totals[bin] ?? 0) + Math.max(0, volume);
            rowIndexes[bin] = rowIndex;
        }
        const maximum = Math.max(1, ...totals);
        const totalVolume = totals.reduce((sum, volume) => sum + volume, 0);
        const height = plot.height / bins;
        const nodes = [];
        totals.forEach((volume, index) => {
            const width = (volume / maximum) * plot.width * 0.48;
            const rowIndex = rowIndexes[index] ?? -1;
            const base = rowIndex >= 0
                ? datumBase$2(context, `${layer.id}:volume-profile:${index}`, rowIndex, 0, {
                    priceStart: priceExtent[0] + (span * index) / bins,
                    priceEnd: priceExtent[0] + (span * (index + 1)) / bins,
                    volume,
                    proportion: totalVolume === 0 ? 0 : volume / totalVolume,
                })
                : nodeBase(`${layer.id}:volume-profile:${index}`, { zIndex: layer.zIndex });
            nodes.push({
                type: 'rect',
                ...base,
                x: plot.x + plot.width - width,
                y: plot.y + plot.height - (index + 1) * height + 1,
                width,
                height: Math.max(1, height - 2),
                fill: layer.mark.fill ?? colorWithOpacity(context.color, 0.68),
                stroke: layer.mark.stroke ?? theme.colors.background,
                lineWidth: 0.8,
                cornerRadius: 2,
            });
        });
        return nodes;
    };
    const compileGeoLineMark = (context) => {
        const { layer, table, plot, theme } = context;
        const longitude2Field = layer.mark.fields.longitude2 ?? layer.mark.fields.x2 ?? 'longitude2';
        const latitude2Field = layer.mark.fields.latitude2 ?? layer.mark.fields.y2 ?? 'latitude2';
        const valueField = layer.mark.fields.value;
        const valueExtent = valueField !== undefined && table.has(valueField) ? table.extent(valueField) : null;
        const flow = layer.mark.options.flow === true;
        const nodes = worldBasemapNodes(context);
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const longitude = numericDataValue(table.value(rowIndex, layer.x.field));
            const latitude = numericDataValue(table.value(rowIndex, layer.y.field));
            const longitude2 = table.has(longitude2Field)
                ? numericDataValue(table.value(rowIndex, longitude2Field))
                : null;
            const latitude2 = table.has(latitude2Field)
                ? numericDataValue(table.value(rowIndex, latitude2Field))
                : null;
            if (longitude === null ||
                latitude === null ||
                longitude2 === null ||
                latitude2 === null ||
                !isGeographicPosition(longitude, latitude) ||
                !isGeographicPosition(longitude2, latitude2))
                continue;
            const start = projectGeographicPosition(plot, longitude, latitude);
            const end = projectGeographicPosition(plot, longitude2, latitude2);
            const control = {
                x: (start.x + end.x) / 2,
                y: Math.min(start.y, end.y) - Math.abs(end.x - start.x) * 0.16,
            };
            const value = valueField !== undefined && table.has(valueField)
                ? (numericDataValue(table.value(rowIndex, valueField)) ?? 1)
                : 1;
            const ratio = valueExtent === null || valueExtent[1] === valueExtent[0]
                ? 0.5
                : (value - valueExtent[0]) / (valueExtent[1] - valueExtent[0]);
            const points = quadraticPoints(start, control, end, 28);
            const color = layer.mark.stroke ?? paletteColor$1(context, rowIndex, table.length);
            nodes.push({
                type: 'path',
                ...datumBase$2(context, `${layer.id}:geo-line:${rowIndex}`, rowIndex),
                points,
                closed: false,
                stroke: color,
                lineWidth: (layer.mark.lineWidth ?? 1.8) + ratio * 3,
                lineCap: 'round',
                lineJoin: 'round',
            });
            if (flow) {
                const previous = points.at(-2) ?? start;
                const angle = Math.atan2(end.y - previous.y, end.x - previous.x);
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:geo-arrow:${rowIndex}`, { zIndex: layer.zIndex + 1 }),
                    points: [
                        end,
                        pointOnCircle$1(end.x, end.y, 7, angle + Math.PI * 0.82),
                        pointOnCircle$1(end.x, end.y, 7, angle - Math.PI * 0.82),
                    ],
                    closed: true,
                    fill: color,
                    stroke: theme.colors.background,
                    lineWidth: 0.5,
                });
            }
        }
        return nodes;
    };
    const compileGeoHeatmapMark = (context) => {
        const { layer, table, plot, theme } = context;
        const valueField = layer.mark.fields.value ?? 'value';
        const extent = table.has(valueField) ? table.extent(valueField) : null;
        const nodes = worldBasemapNodes(context);
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const longitude = numericDataValue(table.value(rowIndex, layer.x.field));
            const latitude = numericDataValue(table.value(rowIndex, layer.y.field));
            const value = table.has(valueField)
                ? numericDataValue(table.value(rowIndex, valueField))
                : null;
            if (longitude === null ||
                latitude === null ||
                value === null ||
                !isGeographicPosition(longitude, latitude))
                continue;
            const point = projectGeographicPosition(plot, longitude, latitude);
            const ratio = extent === null || extent[1] === extent[0]
                ? 0.5
                : (value - extent[0]) / (extent[1] - extent[0]);
            const color = layer.mark.fill ?? mappedContinuousColor(theme, ratio);
            nodes.push({
                type: 'circle',
                ...nodeBase(`${layer.id}:geo-heat-halo:${rowIndex}`, {
                    zIndex: layer.zIndex - 0.5,
                    opacity: 0.18,
                }),
                cx: point.x,
                cy: point.y,
                radius: 12 + ratio * 18,
                fill: color,
                lineWidth: 0,
            });
            nodes.push({
                type: 'circle',
                ...datumBase$2(context, `${layer.id}:geo-heat:${rowIndex}`, rowIndex),
                cx: point.x,
                cy: point.y,
                radius: 5 + ratio * 9,
                fill: color,
                stroke: theme.colors.background,
                lineWidth: 1.2,
            });
        }
        return nodes;
    };
    const compileTiledMapMark = (context) => {
        const { layer, table, plot, theme } = context;
        const nodes = worldBasemapNodes(context);
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const longitude = numericDataValue(table.value(rowIndex, layer.x.field));
            const latitude = numericDataValue(table.value(rowIndex, layer.y.field));
            if (longitude === null || latitude === null || !isGeographicPosition(longitude, latitude))
                continue;
            const point = projectGeographicPosition(plot, longitude, latitude);
            const color = layer.mark.fill ?? paletteColor$1(context, rowIndex, table.length);
            nodes.push({
                type: 'circle',
                ...datumBase$2(context, `${layer.id}:tiled-point:${rowIndex}`, rowIndex),
                cx: point.x,
                cy: point.y,
                radius: layer.mark.radius ?? 6,
                fill: color,
                stroke: theme.colors.background,
                lineWidth: 2,
            });
        }
        return nodes;
    };
    const compileGeoFlowMark = (context) => compileGeoLineMark({
        ...context,
        layer: {
            ...context.layer,
            mark: {
                ...context.layer.mark,
                options: { ...context.layer.mark.options, flow: true },
            },
        },
    });

    const TAU$1 = Math.PI * 2;
    function clamp$9(value, minimum, maximum) {
        return Math.max(minimum, Math.min(maximum, value));
    }
    function optionNumber$4(options, key, fallback) {
        const value = options[key];
        return typeof value === 'number' && Number.isFinite(value) ? value : fallback;
    }
    function optionString$4(options, key, fallback) {
        const value = options[key];
        return typeof value === 'string' && value.trim() !== '' ? value : fallback;
    }
    function optionStrings$2(value) {
        if (!Array.isArray(value))
            return [];
        return value.filter((entry) => typeof entry === 'string' && entry.trim() !== '');
    }
    function allocatedBudget(total, count, index) {
        if (total <= 0 || count <= 0 || index < 0 || index >= count)
            return 0;
        const base = Math.floor(total / count);
        return base + (index < total % count ? 1 : 0);
    }
    function sampledItems(values, target) {
        return exactStrideSampleIndices(values.length, target).flatMap((index) => {
            const value = values[index];
            return value === undefined ? [] : [value];
        });
    }
    function sampledLineItems(values, target, metric) {
        if (target < 4 || values.length <= target)
            return sampledItems(values, target);
        return minMaxSampleIndices(values.map(metric), target).flatMap((index) => {
            const value = values[index];
            return value === undefined ? [] : [value];
        });
    }
    function stringValue$2(value) {
        if (value === null || value === undefined)
            return null;
        return value instanceof Date ? value.toISOString() : String(value);
    }
    function paletteColor(context, index, count) {
        return categoricalColor(context.theme, index, count);
    }
    function textNode$6(context, id, x, y, text, options = {}) {
        return {
            type: 'text',
            ...nodeBase(id, { zIndex: context.layer.zIndex + 4 }),
            x,
            y,
            text,
            fill: options.fill ?? context.theme.colors.text,
            fontFamily: context.theme.typography.fontFamily,
            fontSize: options.size ?? context.theme.typography.fontSize,
            fontWeight: options.weight ?? 600,
            align: options.align ?? 'center',
            baseline: options.baseline ?? 'middle',
            rotation: options.rotation ?? 0,
        };
    }
    function datumBase$1(context, id, rowIndex, zIndex = 1, tooltip) {
        return nodeBase(id, {
            zIndex: context.layer.zIndex + zIndex,
            opacity: context.layer.mark.opacity,
            interactive: context.performance.enableHitTesting,
            datum: {
                layerId: context.layer.id,
                rowIndex,
                datum: context.table.row(rowIndex),
                ...(tooltip === undefined ? {} : { tooltip }),
            },
        });
    }
    function inferredCellSpan(values, fallback) {
        const sorted = [...new Set(values.filter(Number.isFinite))].sort((left, right) => left - right);
        let smallest = Number.POSITIVE_INFINITY;
        for (let index = 1; index < sorted.length; index += 1) {
            smallest = Math.min(smallest, (sorted[index] ?? 0) - (sorted[index - 1] ?? 0));
        }
        return Number.isFinite(smallest) ? Math.max(1, smallest) : Math.max(1, fallback);
    }
    function byteChannel(value, fallback) {
        const numeric = numericDataValue(value);
        return numeric === null ? fallback : Math.round(clamp$9(numeric, 0, 255));
    }
    /** A raster compiler that keeps every pixel portable as a data row. */
    const compileImageMark = (context) => {
        const { layer, table, xScale, yScale, plot, theme } = context;
        const colorField = layer.mark.fields.color;
        const redField = layer.mark.fields.red ?? 'red';
        const greenField = layer.mark.fields.green ?? 'green';
        const blueField = layer.mark.fields.blue ?? 'blue';
        const alphaField = layer.mark.fields.alpha;
        const xPositions = [];
        const yPositions = [];
        const rasterRows = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const x = scaleInput(table.value(rowIndex, layer.x.field));
            const y = scaleInput(table.value(rowIndex, layer.y.field));
            if (x === null || y === null)
                continue;
            const mappedX = xScale.map(x);
            const mappedY = yScale.map(y);
            if (!Number.isFinite(mappedX) || !Number.isFinite(mappedY))
                continue;
            xPositions.push(mappedX);
            yPositions.push(mappedY);
            rasterRows.push({ rowIndex, x: mappedX, y: mappedY });
        }
        const cellWidth = xScale instanceof BandScale
            ? xScale.bandwidth
            : inferredCellSpan(xPositions, plot.width / Math.max(1, Math.sqrt(table.length)));
        const cellHeight = yScale instanceof BandScale
            ? yScale.bandwidth
            : inferredCellSpan(yPositions, plot.height / Math.max(1, Math.sqrt(table.length)));
        const gap = clamp$9(optionNumber$4(layer.mark.options, 'cellGap', 0), 0, 8);
        const nodes = [];
        for (const { rowIndex, x, y } of sampledItems(rasterRows, context.performance.maxBarMarks)) {
            const explicit = colorField === undefined || !table.has(colorField)
                ? null
                : stringValue$2(table.value(rowIndex, colorField));
            const red = table.has(redField) ? byteChannel(table.value(rowIndex, redField), 0) : 0;
            const green = table.has(greenField) ? byteChannel(table.value(rowIndex, greenField), 0) : 0;
            const blue = table.has(blueField) ? byteChannel(table.value(rowIndex, blueField), 0) : 0;
            const alpha = alphaField !== undefined && table.has(alphaField)
                ? clamp$9(byteChannel(table.value(rowIndex, alphaField), 255) / 255, 0, 1)
                : 1;
            const fill = layer.mark.fill ?? explicit ?? `rgba(${red}, ${green}, ${blue}, ${alpha})`;
            nodes.push({
                type: 'rect',
                ...datumBase$1(context, `${layer.id}:image:${rowIndex}`, rowIndex, 1, {
                    x: table.value(rowIndex, layer.x.field) ?? null,
                    y: table.value(rowIndex, layer.y.field) ?? null,
                    red,
                    green,
                    blue,
                    alpha,
                }),
                x: x - cellWidth / 2 + gap / 2,
                y: y - cellHeight / 2 + gap / 2,
                width: Math.max(1, cellWidth - gap),
                height: Math.max(1, cellHeight - gap),
                fill,
                ...(layer.mark.stroke === undefined && gap <= 0
                    ? {}
                    : { stroke: layer.mark.stroke ?? theme.colors.background }),
                lineWidth: gap > 0 ? (layer.mark.lineWidth ?? 1) : 0,
                cornerRadius: layer.mark.cornerRadius ?? 0,
            });
        }
        return nodes;
    };
    function gaussianKernel(distance) {
        return Math.exp(-0.5 * distance * distance) / Math.sqrt(TAU$1);
    }
    function swapNumbers(values, left, right) {
        const value = values[left];
        if (value === undefined || values[right] === undefined)
            return;
        values[left] = values[right];
        values[right] = value;
    }
    /** Deterministic three-way quickselect that does not allocate a sorted copy. */
    function selectNumber(values, target) {
        let left = 0;
        let right = values.length - 1;
        while (left < right) {
            const pivot = values[Math.floor((left + right) / 2)];
            if (pivot === undefined)
                break;
            let lower = left;
            let cursor = left;
            let upper = right;
            while (cursor <= upper) {
                const value = values[cursor];
                if (value === undefined)
                    break;
                if (value < pivot) {
                    swapNumbers(values, lower, cursor);
                    lower += 1;
                    cursor += 1;
                }
                else if (value > pivot) {
                    swapNumbers(values, cursor, upper);
                    upper -= 1;
                }
                else
                    cursor += 1;
            }
            if (target < lower)
                right = lower - 1;
            else if (target > upper)
                left = upper + 1;
            else
                return values[target] ?? pivot;
        }
        return values[left] ?? 0;
    }
    function exactMedianInPlace(values) {
        const middle = Math.floor(values.length / 2);
        const upper = selectNumber(values, middle);
        return values.length % 2 === 0
            ? (selectNumber(values, Math.max(0, middle - 1)) + upper) / 2
            : upper;
    }
    function summarizeViolinValues(values) {
        if (values.length === 0)
            return null;
        let minimum = Number.POSITIVE_INFINITY;
        let maximum = Number.NEGATIVE_INFINITY;
        let mean = 0;
        let squaredDifference = 0;
        values.forEach((value, index) => {
            minimum = Math.min(minimum, value);
            maximum = Math.max(maximum, value);
            const count = index + 1;
            const difference = value - mean;
            mean += difference / count;
            squaredDifference += difference * (value - mean);
        });
        return {
            minimum,
            maximum,
            mean,
            deviation: Math.sqrt(squaredDifference / Math.max(1, values.length - 1)),
        };
    }
    function compileViolin(context) {
        const { layer, table, xScale, yScale, plot, theme } = context;
        const valueField = layer.mark.fields.value ?? layer.y.field;
        const groupField = layer.mark.fields.group ?? layer.x.field;
        const groups = new Map();
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const group = stringValue$2(table.value(rowIndex, groupField));
            const value = numericDataValue(table.value(rowIndex, valueField));
            if (group === null || value === null)
                continue;
            const bucket = groups.get(group) ?? { rows: [], values: [] };
            bucket.rows.push(rowIndex);
            bucket.values.push(value);
            groups.set(group, bucket);
        }
        const allViolins = [...groups].map(([name, values]) => ({ name, ...values }));
        if (allViolins.length === 0)
            return [];
        const samples = clamp$9(Math.floor(optionNumber$4(layer.mark.options, 'samples', 56)), 20, 160);
        const maximumGroups = Math.max(1, Math.min(context.performance.maxBarMarks, Math.floor(context.performance.maxLinePoints / (2 * (samples + 1)))));
        const violins = sampledItems(allViolins, maximumGroups);
        const width = xScale instanceof BandScale
            ? xScale.bandwidth * 0.78
            : plot.width / Math.max(3, violins.length * 1.6);
        const nodes = [];
        violins.forEach((violin, groupIndex) => {
            const summary = summarizeViolinValues(violin.values);
            if (summary === null)
                return;
            const { minimum, maximum, mean, deviation } = summary;
            const densitySource = sampledItems(violin.values, Math.max(1, allocatedBudget(context.performance.maxPointMarks, violins.length, groupIndex)));
            const span = maximum - minimum || Math.max(1, Math.abs(mean) * 0.1);
            const bandwidth = Math.max(span / samples, optionNumber$4(layer.mark.options, 'bandwidth', 1.06 * (deviation || span / 4) * violin.values.length ** -0.2));
            const density = Array.from({ length: samples + 1 }, (_, index) => {
                const value = minimum - bandwidth + ((span + bandwidth * 2) * index) / samples;
                const amount = densitySource.reduce((sum, sample) => sum + gaussianKernel((value - sample) / bandwidth), 0) /
                    (densitySource.length * bandwidth);
                return { value, amount };
            });
            const maxDensity = Math.max(...density.map(({ amount }) => amount), Number.EPSILON);
            const xInput = scaleInput(table.value(violin.rows[0] ?? 0, groupField));
            if (xInput === null)
                return;
            const center = xScale.map(xInput);
            const right = density.map(({ value, amount }) => ({
                x: center + (amount / maxDensity) * width * 0.5,
                y: yScale.map(value),
            }));
            const left = [...density].reverse().map(({ value, amount }) => ({
                x: center - (amount / maxDensity) * width * 0.5,
                y: yScale.map(value),
            }));
            const color = paletteColor(context, groupIndex, violins.length);
            const fill = layer.mark.fill ?? colorWithOpacity(color, 0.28);
            const stroke = layer.mark.stroke ?? color;
            const representative = violin.rows[0] ?? 0;
            const median = exactMedianInPlace(violin.values);
            nodes.push({
                type: 'path',
                ...datumBase$1(context, `${layer.id}:violin:${groupIndex}`, representative, 1, {
                    group: violin.name,
                    count: violin.values.length,
                    minimum,
                    maximum,
                    mean,
                    median,
                    densitySampleCount: densitySource.length,
                }),
                points: [...right, ...left],
                closed: true,
                fill,
                stroke,
                lineWidth: layer.mark.lineWidth ?? 1.8,
                lineJoin: theme.mark.lineJoin ?? 'round',
            });
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:violin-median:${groupIndex}`, { zIndex: layer.zIndex + 3 }),
                x1: center - width * 0.18,
                y1: yScale.map(median),
                x2: center + width * 0.18,
                y2: yScale.map(median),
                stroke: mixColor$1(stroke, theme.colors.text, 0.25),
                lineWidth: 2.2,
                lineCap: theme.mark.lineCap ?? 'round',
            });
        });
        return nodes;
    }
    function boundedGridDimensions(requestedX, requestedY, maximumCells) {
        const budget = Math.max(4, Math.trunc(maximumCells));
        if (requestedX * requestedY <= budget)
            return [requestedX, requestedY];
        const scale = Math.sqrt(budget / (requestedX * requestedY));
        let x = Math.max(2, Math.min(requestedX, Math.floor(requestedX * scale)));
        let y = Math.max(2, Math.min(requestedY, Math.floor(budget / x)));
        x = Math.max(2, Math.min(requestedX, Math.floor(budget / y)));
        while (x * y > budget) {
            if (x >= y && x > 2)
                x -= 1;
            else if (y > 2)
                y -= 1;
            else
                break;
        }
        return [x, y];
    }
    function compileHistogram2d(context, contours = false) {
        const { layer, table, xScale, yScale, theme } = context;
        const xExtent = table.extent(layer.x.field, layer.x.type === 'temporal');
        const yExtent = table.extent(layer.y.field, layer.y.type === 'temporal');
        if (xExtent === null || yExtent === null)
            return [];
        const requestedBinsX = clamp$9(Math.floor(optionNumber$4(layer.mark.options, 'binsX', 12)), 2, 80);
        const requestedBinsY = clamp$9(Math.floor(optionNumber$4(layer.mark.options, 'binsY', 10)), 2, 80);
        const [binsX, binsY] = boundedGridDimensions(requestedBinsX, requestedBinsY, context.performance.maxBarMarks);
        const counts = Array.from({ length: binsY }, () => Array.from({ length: binsX }, () => 0));
        const rows = Array.from({ length: binsY }, () => Array.from({ length: binsX }, () => []));
        const xSpan = xExtent[1] - xExtent[0] || 1;
        const ySpan = yExtent[1] - yExtent[0] || 1;
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const x = numericDataValue(table.value(rowIndex, layer.x.field), layer.x.type === 'temporal');
            const y = numericDataValue(table.value(rowIndex, layer.y.field), layer.y.type === 'temporal');
            if (x === null || y === null)
                continue;
            const xBin = clamp$9(Math.floor(((x - xExtent[0]) / xSpan) * binsX), 0, binsX - 1);
            const yBin = clamp$9(Math.floor(((y - yExtent[0]) / ySpan) * binsY), 0, binsY - 1);
            const rowCounts = counts[yBin];
            const rowRefs = rows[yBin];
            if (rowCounts === undefined || rowRefs === undefined)
                continue;
            rowCounts[xBin] = (rowCounts[xBin] ?? 0) + 1;
            rowRefs[xBin]?.push(rowIndex);
        }
        const maximum = Math.max(1, ...counts.flat());
        const nodes = [];
        const centerPoints = Array.from({ length: binsY }, () => []);
        for (let yBin = 0; yBin < binsY; yBin += 1) {
            for (let xBin = 0; xBin < binsX; xBin += 1) {
                const count = counts[yBin]?.[xBin] ?? 0;
                const xStart = xExtent[0] + (xSpan * xBin) / binsX;
                const xEnd = xExtent[0] + (xSpan * (xBin + 1)) / binsX;
                const yStart = yExtent[0] + (ySpan * yBin) / binsY;
                const yEnd = yExtent[0] + (ySpan * (yBin + 1)) / binsY;
                const left = xScale.map(xStart);
                const right = xScale.map(xEnd);
                const top = yScale.map(yEnd);
                const bottom = yScale.map(yStart);
                const ratio = count / maximum;
                const fill = layer.mark.fill ?? mappedContinuousColor(theme, ratio);
                const rowIndex = rows[yBin]?.[xBin]?.[0] ?? 0;
                const centerRow = centerPoints[yBin];
                if (centerRow !== undefined) {
                    centerRow[xBin] = { x: (left + right) / 2, y: (top + bottom) / 2 };
                }
                if (contours)
                    continue;
                if (count === 0 && layer.mark.options.empty !== true)
                    continue;
                nodes.push({
                    type: 'rect',
                    ...datumBase$1(context, `${layer.id}:histogram-2d:${xBin}:${yBin}`, rowIndex, 1, {
                        xStart,
                        xEnd,
                        yStart,
                        yEnd,
                        count,
                    }),
                    x: Math.min(left, right),
                    y: Math.min(top, bottom),
                    width: Math.max(1, Math.abs(right - left)),
                    height: Math.max(1, Math.abs(bottom - top)),
                    fill,
                    stroke: layer.mark.stroke ?? colorWithOpacity(theme.colors.background, 0.55),
                    lineWidth: layer.mark.lineWidth ?? 0.8,
                    cornerRadius: layer.mark.cornerRadius ?? 0,
                });
            }
        }
        if (contours) {
            const levelCount = clamp$9(Math.floor(optionNumber$4(layer.mark.options, 'levels', 6)), 2, 16);
            const levels = Array.from({ length: levelCount }, (_, index) => ((index + 1) / (levelCount + 1)) * maximum);
            extractIsolines(counts, centerPoints, levels, rows, {
                maximumSegments: Math.floor(context.performance.maxLinePoints / 2),
            }).forEach((segment, index) => {
                const ratio = segment.levelIndex / Math.max(1, levelCount - 1);
                const stroke = layer.mark.stroke ?? mappedContinuousColor(theme, ratio);
                nodes.push({
                    type: 'path',
                    ...datumBase$1(context, `${layer.id}:histogram-2d-contour:${segment.levelIndex}:${index}`, segment.sourceRows[0] ?? 0, 1, {
                        kind: 'density-isoline',
                        level: levels[segment.levelIndex] ?? 0,
                        minimumCount: 0,
                        maximumCount: maximum,
                        binsX,
                        binsY,
                        sourceRowCount: segment.sourceRows.length,
                        sourceRowIndices: segment.sourceRows.slice(0, 256),
                    }),
                    points: segment.points,
                    closed: segment.closed,
                    stroke,
                    lineWidth: layer.mark.lineWidth ?? 1.8,
                    lineCap: theme.mark.lineCap ?? 'round',
                    lineJoin: theme.mark.lineJoin ?? 'round',
                });
            });
        }
        return nodes;
    }
    /** One implementation surface for histogram, empirical, KDE, box, violin, curve, and bivariate modes. */
    const compileDistributionFamilyMark = (context) => {
        const mode = resolveDistributionMode(context.layer.mark.options.mode);
        if (mode === 'boxplot')
            return compileBoxplotMark(context);
        if (mode === 'violin')
            return compileViolin(context);
        if (mode === 'curve')
            return compileDistributionMark(context);
        if (mode === 'kde')
            return compileKernelDensityMark(context);
        if (mode === 'ecdf' || mode === 'ccdf')
            return compileEmpiricalDistributionMark(context);
        if (mode === 'histogram-2d')
            return compileHistogram2d(context);
        if (mode === 'histogram-2d-contour') {
            return compileHistogram2d(context, true);
        }
        return compileHistogramMark(context);
    };
    function angleValue(value, categories, unit) {
        const numeric = numericDataValue(value);
        if (numeric !== null)
            return unit === 'radians' ? numeric : (numeric * Math.PI) / 180;
        const category = stringValue$2(value);
        if (category === null)
            return null;
        const index = categories.indexOf(category);
        return index < 0 ? null : -Math.PI / 2 + (index * TAU$1) / Math.max(1, categories.length);
    }
    function circlePoint(cx, cy, radius, angle) {
        return { x: cx + Math.cos(angle) * radius, y: cy + Math.sin(angle) * radius };
    }
    function arcPoints$2(cx, cy, radius, start, end) {
        const segments = Math.max(4, Math.ceil((Math.abs(end - start) / TAU$1) * 72));
        return Array.from({ length: segments + 1 }, (_, index) => circlePoint(cx, cy, radius, start + ((end - start) * index) / segments));
    }
    const compilePolarMark = (context) => {
        const { layer, table, plot, theme } = context;
        const mode = optionString$4(layer.mark.options, 'mode', 'line');
        if (mode === 'radar')
            return compileRadarMark(context);
        const unit = optionString$4(layer.mark.options, 'angleUnit', 'degrees');
        const seriesField = layer.mark.fields.series;
        const categoriesSeen = new Set();
        const rowsBySeries = new Map();
        let maximum = optionNumber$4(layer.mark.options, 'max', 0);
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const angle = table.value(rowIndex, layer.x.field);
            const category = stringValue$2(angle);
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            if (category !== null)
                categoriesSeen.add(category);
            if (value === null)
                continue;
            const seriesName = seriesField === undefined
                ? 'Series'
                : (stringValue$2(table.value(rowIndex, seriesField)) ?? 'Series');
            const rows = rowsBySeries.get(seriesName) ?? [];
            rows.push({ angleValue: angle, rowIndex, value });
            rowsBySeries.set(seriesName, rows);
            maximum = Math.max(maximum, value);
        }
        const allCategories = [...categoriesSeen];
        const categoricalAngles = allCategories.length > 0 && allCategories.every((value) => Number.isNaN(Number(value)));
        const maximumSpokes = Math.max(1, Math.min(360, Math.floor(context.performance.maxPointMarks / 4)));
        const categories = categoricalAngles ? sampledItems(allCategories, maximumSpokes) : allCategories;
        if (maximum <= 0)
            maximum = 1;
        const cx = plot.x + plot.width / 2;
        const cy = plot.y + plot.height / 2;
        const radius = Math.max(18, Math.min(plot.width, plot.height) * 0.42);
        const nodes = [];
        for (let ring = 1; ring <= 4; ring += 1) {
            nodes.push({
                type: 'path',
                ...nodeBase(`${layer.id}:polar-ring:${ring}`, { zIndex: layer.zIndex }),
                points: arcPoints$2(cx, cy, (radius * ring) / 4, 0, TAU$1),
                closed: true,
                stroke: colorWithOpacity(theme.colors.grid, 0.78),
                lineWidth: 1,
            });
        }
        const spokes = categoricalAngles
            ? categories.map((value, index) => ({
                value,
                angle: -Math.PI / 2 + (index * TAU$1) / categories.length,
            }))
            : Array.from({ length: 8 }, (_, index) => ({
                value: String(index * 45),
                angle: (index * TAU$1) / 8,
            }));
        spokes.forEach((spoke, index) => {
            const edge = circlePoint(cx, cy, radius, spoke.angle);
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:polar-spoke:${index}`, { zIndex: layer.zIndex }),
                x1: cx,
                y1: cy,
                x2: edge.x,
                y2: edge.y,
                stroke: colorWithOpacity(theme.colors.grid, 0.7),
                lineWidth: 1,
            });
            const label = circlePoint(cx, cy, radius + 14, spoke.angle);
            nodes.push(textNode$6(context, `${layer.id}:polar-label:${index}`, label.x, label.y, spoke.value, {
                fill: theme.colors.mutedText,
                size: Math.max(9, theme.typography.fontSize - 2),
            }));
        });
        const rowBudget = mode === 'bar'
            ? context.performance.maxBarMarks
            : mode === 'line' || mode === 'area'
                ? Math.min(context.performance.maxPointMarks, context.performance.maxLinePoints)
                : context.performance.maxPointMarks;
        const series = sampledItems([...rowsBySeries.keys()], rowBudget);
        series.forEach((seriesName, seriesIndex) => {
            const availableRows = (rowsBySeries.get(seriesName) ?? []).flatMap((row) => {
                const angle = angleValue(row.angleValue, categories, unit);
                if (angle === null)
                    return [];
                return [
                    {
                        point: circlePoint(cx, cy, radius * clamp$9(row.value / maximum, 0, 1), angle),
                        angle,
                        rowIndex: row.rowIndex,
                        value: row.value,
                    },
                ];
            });
            availableRows.sort((left, right) => left.angle - right.angle);
            const seriesBudget = allocatedBudget(rowBudget, series.length, seriesIndex);
            const rows = mode === 'line' || mode === 'area'
                ? sampledLineItems(availableRows, seriesBudget, ({ value }) => value)
                : sampledItems(availableRows, seriesBudget);
            const color = layer.mark.stroke ?? paletteColor(context, seriesIndex, series.length);
            if (mode === 'bar') {
                const barSpan = optionNumber$4(layer.mark.options, 'barAngle', TAU$1 / Math.max(16, rows.length * 2));
                const boundedBarSpan = clamp$9(barSpan, 0.000001, TAU$1);
                rows.forEach((row) => {
                    const outer = radius * clamp$9(row.value / maximum, 0, 1);
                    nodes.push({
                        type: 'path',
                        ...datumBase$1(context, `${layer.id}:polar-bar:${row.rowIndex}`, row.rowIndex),
                        points: [
                            { x: cx, y: cy },
                            ...arcPoints$2(cx, cy, outer, row.angle - boundedBarSpan / 2, row.angle + boundedBarSpan / 2),
                        ],
                        closed: true,
                        fill: layer.mark.fill ?? colorWithOpacity(color, 0.72),
                        stroke: color,
                        lineWidth: layer.mark.lineWidth ?? 1,
                        lineJoin: 'round',
                    });
                });
                return;
            }
            if ((mode === 'line' || mode === 'area') && rows.length > 1) {
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:polar-line:${seriesIndex}`, { zIndex: layer.zIndex + 1 }),
                    points: rows.map(({ point }) => point),
                    closed: layer.mark.options.closed === true,
                    ...(mode === 'area' ? { fill: layer.mark.fill ?? colorWithOpacity(color, 0.18) } : {}),
                    stroke: color,
                    lineWidth: layer.mark.lineWidth ?? 2,
                    lineCap: theme.mark.lineCap ?? 'round',
                    lineJoin: theme.mark.lineJoin ?? 'round',
                });
            }
            rows.forEach((row) => {
                nodes.push({
                    type: 'circle',
                    ...datumBase$1(context, `${layer.id}:polar-point:${row.rowIndex}`, row.rowIndex, 2),
                    cx: row.point.x,
                    cy: row.point.y,
                    radius: layer.mark.radius ?? 4,
                    fill: layer.mark.fill ?? color,
                    stroke: theme.colors.background,
                    lineWidth: 1.2,
                });
            });
        });
        return nodes;
    };
    const compileTernaryMark = (context) => {
        const { layer, table, plot, theme } = context;
        const aField = layer.mark.fields.a ?? layer.x.field;
        const bField = layer.mark.fields.b ?? layer.y.field;
        const cField = layer.mark.fields.c ?? 'c';
        if (!table.has(aField) || !table.has(bField) || !table.has(cField))
            return [];
        const height = Math.min(plot.height, (plot.width * Math.sqrt(3)) / 2) * 0.88;
        const width = (height * 2) / Math.sqrt(3);
        const topY = plot.y + (plot.height - height) / 2;
        const left = { x: plot.x + (plot.width - width) / 2, y: topY + height };
        const right = { x: left.x + width, y: left.y };
        const top = { x: plot.x + plot.width / 2, y: topY };
        const nodes = [
            {
                type: 'path',
                ...nodeBase(`${layer.id}:ternary-frame`, { zIndex: layer.zIndex }),
                points: [left, right, top],
                closed: true,
                stroke: theme.colors.axis,
                lineWidth: 1.5,
                lineJoin: 'round',
            },
        ];
        for (let step = 1; step < 5; step += 1) {
            const ratio = step / 5;
            const pairs = [
                [
                    { x: left.x + (top.x - left.x) * ratio, y: left.y + (top.y - left.y) * ratio },
                    { x: right.x + (top.x - right.x) * ratio, y: right.y + (top.y - right.y) * ratio },
                ],
                [
                    { x: left.x + (right.x - left.x) * ratio, y: left.y },
                    { x: top.x + (right.x - top.x) * ratio, y: top.y + (right.y - top.y) * ratio },
                ],
                [
                    { x: right.x + (left.x - right.x) * ratio, y: right.y },
                    { x: top.x + (left.x - top.x) * ratio, y: top.y + (left.y - top.y) * ratio },
                ],
            ];
            pairs.forEach(([start, end], index) => nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:ternary-grid:${step}:${index}`, { zIndex: layer.zIndex }),
                x1: start.x,
                y1: start.y,
                x2: end.x,
                y2: end.y,
                stroke: colorWithOpacity(theme.colors.grid, 0.65),
                lineWidth: 1,
            }));
        }
        const labels = optionStrings$2(layer.mark.options.labels);
        [labels[0] ?? aField, labels[1] ?? bField, labels[2] ?? cField].forEach((label, index) => {
            const point = [top, right, left][index] ?? top;
            nodes.push(textNode$6(context, `${layer.id}:ternary-label:${index}`, point.x, point.y + (index === 0 ? -15 : 15), label, {
                fill: theme.colors.mutedText,
                weight: 700,
            }));
        });
        const mode = optionString$4(layer.mark.options, 'mode', 'scatter');
        const seriesField = layer.mark.fields.series;
        const pointsBySeries = new Map();
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const a = numericDataValue(table.value(rowIndex, aField));
            const b = numericDataValue(table.value(rowIndex, bField));
            const c = numericDataValue(table.value(rowIndex, cField));
            if (a === null || b === null || c === null || a < 0 || b < 0 || c < 0)
                continue;
            const total = a + b + c;
            if (total <= 0)
                continue;
            const series = seriesField === undefined
                ? 'Series'
                : (stringValue$2(table.value(rowIndex, seriesField)) ?? 'Series');
            const points = pointsBySeries.get(series) ?? [];
            points.push({
                point: {
                    x: (a * top.x + b * right.x + c * left.x) / total,
                    y: (a * top.y + b * right.y + c * left.y) / total,
                },
                rowIndex,
                component: a / total,
            });
            pointsBySeries.set(series, points);
        }
        const pointBudget = mode === 'line'
            ? Math.min(context.performance.maxPointMarks, context.performance.maxLinePoints)
            : context.performance.maxPointMarks;
        const seriesValues = sampledItems([...pointsBySeries.keys()], pointBudget);
        seriesValues.forEach((series, seriesIndex) => {
            const seriesBudget = allocatedBudget(pointBudget, seriesValues.length, seriesIndex);
            const available = pointsBySeries.get(series) ?? [];
            const points = mode === 'line'
                ? sampledLineItems(available, seriesBudget, ({ component }) => component)
                : sampledItems(available, seriesBudget);
            const color = layer.mark.stroke ?? paletteColor(context, seriesIndex, seriesValues.length);
            if (mode === 'line' && points.length > 1)
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:ternary-line:${seriesIndex}`, { zIndex: layer.zIndex + 1 }),
                    points: points.map(({ point }) => point),
                    closed: layer.mark.options.closed === true,
                    stroke: color,
                    lineWidth: layer.mark.lineWidth ?? 2,
                    lineCap: theme.mark.lineCap ?? 'round',
                    lineJoin: theme.mark.lineJoin ?? 'round',
                });
            points.forEach(({ point, rowIndex }) => nodes.push({
                type: 'circle',
                ...datumBase$1(context, `${layer.id}:ternary-point:${rowIndex}`, rowIndex, 2),
                cx: point.x,
                cy: point.y,
                radius: layer.mark.radius ?? 4.5,
                fill: layer.mark.fill ?? color,
                stroke: theme.colors.background,
                lineWidth: 1.2,
            }));
        });
        return nodes;
    };
    /** Maps normalized impedance to reflection coefficient coordinates. */
    function smithPoint(real, imaginary, cx, cy, radius) {
        const denominator = (real + 1) ** 2 + imaginary ** 2;
        if (denominator <= Number.EPSILON)
            return { x: cx - radius, y: cy };
        const reflectedReal = (real * real + imaginary * imaginary - 1) / denominator;
        const reflectedImaginary = (2 * imaginary) / denominator;
        return {
            x: cx + reflectedReal * radius,
            y: cy - reflectedImaginary * radius,
        };
    }
    const compileSmithMark = (context) => {
        const { layer, table, plot, theme } = context;
        const realField = layer.mark.fields.real ?? layer.x.field;
        const imaginaryField = layer.mark.fields.imaginary ?? layer.y.field;
        const cx = plot.x + plot.width / 2;
        const cy = plot.y + plot.height / 2;
        const radius = Math.max(18, Math.min(plot.width, plot.height) * 0.44);
        const nodes = [
            {
                type: 'path',
                ...nodeBase(`${layer.id}:smith-frame`, { zIndex: layer.zIndex }),
                points: arcPoints$2(cx, cy, radius, 0, TAU$1),
                closed: true,
                stroke: theme.colors.axis,
                lineWidth: 1.5,
            },
            {
                type: 'line',
                ...nodeBase(`${layer.id}:smith-axis`, { zIndex: layer.zIndex }),
                x1: cx - radius,
                y1: cy,
                x2: cx + radius,
                y2: cy,
                stroke: theme.colors.axis,
                lineWidth: 1,
            },
        ];
        for (const resistance of [0, 0.2, 0.5, 1, 2, 5]) {
            const center = cx + (radius * resistance) / (resistance + 1);
            const ringRadius = radius / (resistance + 1);
            nodes.push({
                type: 'path',
                ...nodeBase(`${layer.id}:smith-resistance:${resistance}`, { zIndex: layer.zIndex }),
                points: arcPoints$2(center, cy, ringRadius, 0, TAU$1),
                closed: true,
                stroke: colorWithOpacity(theme.colors.grid, 0.72),
                lineWidth: 1,
            });
        }
        for (const reactance of [-5, -2, -1, -0.5, 0.5, 1, 2, 5]) {
            const sampled = [];
            for (let index = 0; index <= 80; index += 1) {
                const real = (index / 80) * 12;
                sampled.push(smithPoint(real, reactance, cx, cy, radius));
            }
            nodes.push({
                type: 'path',
                ...nodeBase(`${layer.id}:smith-reactance:${reactance}`, { zIndex: layer.zIndex }),
                points: sampled,
                closed: false,
                stroke: colorWithOpacity(theme.colors.grid, 0.72),
                lineWidth: 1,
                lineCap: 'round',
            });
        }
        const mode = optionString$4(layer.mark.options, 'mode', 'line');
        const seriesField = layer.mark.fields.series;
        const pointsBySeries = new Map();
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const real = numericDataValue(table.value(rowIndex, realField));
            const imaginary = numericDataValue(table.value(rowIndex, imaginaryField));
            if (real === null || imaginary === null)
                continue;
            const name = seriesField === undefined
                ? 'Series'
                : (stringValue$2(table.value(rowIndex, seriesField)) ?? 'Series');
            const points = pointsBySeries.get(name) ?? [];
            points.push({
                point: smithPoint(real, imaginary, cx, cy, radius),
                rowIndex,
                magnitude: Math.hypot(real, imaginary),
            });
            pointsBySeries.set(name, points);
        }
        const pointBudget = mode === 'scatter'
            ? context.performance.maxPointMarks
            : Math.min(context.performance.maxPointMarks, context.performance.maxLinePoints);
        const series = sampledItems([...pointsBySeries.keys()], pointBudget);
        series.forEach((name, seriesIndex) => {
            const seriesBudget = allocatedBudget(pointBudget, series.length, seriesIndex);
            const available = pointsBySeries.get(name) ?? [];
            const points = mode === 'scatter'
                ? sampledItems(available, seriesBudget)
                : sampledLineItems(available, seriesBudget, ({ magnitude }) => magnitude);
            const color = layer.mark.stroke ?? paletteColor(context, seriesIndex, series.length);
            if (mode !== 'scatter' && points.length > 1)
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:smith-line:${seriesIndex}`, { zIndex: layer.zIndex + 1 }),
                    points: points.map(({ point }) => point),
                    closed: false,
                    stroke: color,
                    lineWidth: layer.mark.lineWidth ?? 2,
                    lineCap: theme.mark.lineCap ?? 'round',
                    lineJoin: theme.mark.lineJoin ?? 'round',
                });
            points.forEach(({ point, rowIndex }) => nodes.push({
                type: 'circle',
                ...datumBase$1(context, `${layer.id}:smith-point:${rowIndex}`, rowIndex, 2),
                cx: point.x,
                cy: point.y,
                radius: layer.mark.radius ?? 4,
                fill: layer.mark.fill ?? color,
                stroke: theme.colors.background,
                lineWidth: 1.2,
            }));
        });
        return nodes;
    };
    const compileScatterMatrixMark = (context) => {
        const { layer, table, plot, theme } = context;
        const requested = optionStrings$2(layer.mark.options.dimensions);
        const candidates = requested.length > 0
            ? requested
            : [layer.x.field, layer.y.field, ...Object.values(layer.mark.fields)];
        const dimensions = candidates
            .filter((field, index, all) => all.indexOf(field) === index && table.has(field))
            .flatMap((field) => {
            const extent = table.extent(field);
            return extent === null ? [] : [{ field, minimum: extent[0], maximum: extent[1] }];
        })
            .slice(0, 8);
        if (dimensions.length < 2)
            return [];
        const gap = clamp$9(optionNumber$4(layer.mark.options, 'gap', 5), 0, 18);
        const cellWidth = plot.width / dimensions.length;
        const cellHeight = plot.height / dimensions.length;
        const colorField = layer.mark.fields.color ?? layer.mark.fields.group;
        const colorKeys = colorField === undefined || !table.has(colorField) ? [] : table.unique(colorField);
        const colorIndexByKey = new Map(colorKeys.map((key, index) => [key, index]));
        const valuesByField = new Map();
        for (const dimension of dimensions) {
            const values = new Float64Array(table.length);
            values.fill(Number.NaN);
            for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
                const value = numericDataValue(table.value(rowIndex, dimension.field));
                if (value !== null)
                    values[rowIndex] = value;
            }
            valuesByField.set(dimension.field, values);
        }
        const nodes = [];
        const offDiagonalCells = dimensions.length * (dimensions.length - 1);
        let offDiagonalIndex = 0;
        const mapCell = (value, dimension, start, span) => start + ((value - dimension.minimum) / (dimension.maximum - dimension.minimum || 1)) * span;
        dimensions.forEach((yDimension, row) => {
            dimensions.forEach((xDimension, column) => {
                const x = plot.x + column * cellWidth + gap / 2;
                const y = plot.y + row * cellHeight + gap / 2;
                const width = Math.max(1, cellWidth - gap);
                const height = Math.max(1, cellHeight - gap);
                nodes.push({
                    type: 'rect',
                    ...nodeBase(`${layer.id}:matrix-cell:${row}:${column}`, { zIndex: layer.zIndex }),
                    x,
                    y,
                    width,
                    height,
                    fill: colorWithOpacity(theme.colors.surface, 0.55),
                    stroke: theme.colors.grid,
                    lineWidth: 1,
                    cornerRadius: 2,
                });
                if (row === column) {
                    const bins = Array.from({ length: 10 }, () => 0);
                    const dimensionValues = valuesByField.get(xDimension.field);
                    if (dimensionValues === undefined)
                        return;
                    for (const value of dimensionValues) {
                        if (!Number.isFinite(value))
                            continue;
                        const bin = clamp$9(Math.floor(((value - xDimension.minimum) / (xDimension.maximum - xDimension.minimum || 1)) *
                            bins.length), 0, bins.length - 1);
                        bins[bin] = (bins[bin] ?? 0) + 1;
                    }
                    const maximum = Math.max(1, ...bins);
                    bins.forEach((count, index) => nodes.push({
                        type: 'rect',
                        ...nodeBase(`${layer.id}:matrix-hist:${row}:${index}`, { zIndex: layer.zIndex + 1 }),
                        x: x + (index / bins.length) * width,
                        y: y + height - (count / maximum) * height * 0.72,
                        width: Math.max(1, width / bins.length - 1),
                        height: Math.max(1, (count / maximum) * height * 0.72),
                        fill: colorWithOpacity(context.color, 0.62),
                        lineWidth: 0,
                        cornerRadius: 1,
                    }));
                    nodes.push(textNode$6(context, `${layer.id}:matrix-label:${row}`, x + width / 2, y + 12, xDimension.field, {
                        fill: theme.colors.mutedText,
                        size: Math.max(8, theme.typography.fontSize - 2),
                        weight: 700,
                    }));
                    return;
                }
                const cellBudget = allocatedBudget(context.performance.maxPointMarks, offDiagonalCells, offDiagonalIndex);
                offDiagonalIndex += 1;
                const xValues = valuesByField.get(xDimension.field);
                const yValues = valuesByField.get(yDimension.field);
                if (xValues === undefined || yValues === undefined)
                    return;
                const validRows = [];
                for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
                    if (Number.isFinite(xValues[rowIndex]) && Number.isFinite(yValues[rowIndex])) {
                        validRows.push(rowIndex);
                    }
                }
                for (const rowIndex of sampledItems(validRows, cellBudget)) {
                    const xValue = xValues[rowIndex];
                    const yValue = yValues[rowIndex];
                    if (xValue === undefined || yValue === undefined)
                        continue;
                    const key = colorField === undefined ? null : stringValue$2(table.value(rowIndex, colorField));
                    const colorIndex = key === null ? 0 : (colorIndexByKey.get(key) ?? 0);
                    nodes.push({
                        type: 'circle',
                        ...datumBase$1(context, `${layer.id}:matrix-point:${row}:${column}:${rowIndex}`, rowIndex, 1),
                        cx: mapCell(xValue, xDimension, x + 3, width - 6),
                        cy: y + height - 3 - mapCell(yValue, yDimension, 0, height - 6),
                        radius: clamp$9(layer.mark.radius ?? 2.2, 1, 6),
                        fill: layer.mark.fill ??
                            colorWithOpacity(paletteColor(context, colorIndex, Math.max(1, colorKeys.length)), 0.72),
                        stroke: theme.colors.background,
                        lineWidth: 0.5,
                    });
                }
            });
        });
        return nodes;
    };
    function boundedGridKeys(aKeys, bKeys, maximumCells) {
        const cellBudget = Math.max(1, Math.trunc(maximumCells));
        if (aKeys.length * bKeys.length <= cellBudget)
            return [aKeys, bKeys];
        const ratio = aKeys.length / Math.max(1, bKeys.length);
        let aCount = Math.max(1, Math.min(aKeys.length, Math.floor(Math.sqrt(cellBudget * ratio))));
        let bCount = Math.max(1, Math.min(bKeys.length, Math.floor(cellBudget / aCount)));
        aCount = Math.max(1, Math.min(aKeys.length, Math.floor(cellBudget / bCount)));
        while (aCount * bCount > cellBudget) {
            if (aCount >= bCount && aCount > 1)
                aCount -= 1;
            else if (bCount > 1)
                bCount -= 1;
            else
                break;
        }
        return [sampledItems(aKeys, aCount), sampledItems(bKeys, bCount)];
    }
    const compileCarpetMark = (context) => {
        const { layer, table, plot, theme } = context;
        const aField = layer.mark.fields.a ?? layer.x.field;
        const bField = layer.mark.fields.b ?? layer.y.field;
        const xField = layer.mark.fields.x ?? 'x';
        const yField = layer.mark.fields.y ?? 'y';
        const valueField = layer.mark.fields.value;
        if (![aField, bField, xField, yField].every((field) => table.has(field)))
            return [];
        const data = [];
        const aGroups = new Map();
        const bGroups = new Map();
        const indexed = new Map();
        let xMinimum = Number.POSITIVE_INFINITY;
        let xMaximum = Number.NEGATIVE_INFINITY;
        let yMinimum = Number.POSITIVE_INFINITY;
        let yMaximum = Number.NEGATIVE_INFINITY;
        let valueMinimum = Number.POSITIVE_INFINITY;
        let valueMaximum = Number.NEGATIVE_INFINITY;
        let valueCount = 0;
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const a = stringValue$2(table.value(rowIndex, aField));
            const b = stringValue$2(table.value(rowIndex, bField));
            const x = numericDataValue(table.value(rowIndex, xField));
            const y = numericDataValue(table.value(rowIndex, yField));
            if (a === null || b === null || x === null || y === null)
                continue;
            const datum = {
                rowIndex,
                a,
                b,
                x,
                y,
                value: valueField === undefined || !table.has(valueField)
                    ? null
                    : numericDataValue(table.value(rowIndex, valueField)),
            };
            data.push(datum);
            const aGroup = aGroups.get(a) ?? [];
            aGroup.push(datum);
            aGroups.set(a, aGroup);
            const bGroup = bGroups.get(b) ?? [];
            bGroup.push(datum);
            bGroups.set(b, bGroup);
            const row = indexed.get(a) ?? new Map();
            if (!row.has(b))
                row.set(b, datum);
            indexed.set(a, row);
            xMinimum = Math.min(xMinimum, x);
            xMaximum = Math.max(xMaximum, x);
            yMinimum = Math.min(yMinimum, y);
            yMaximum = Math.max(yMaximum, y);
            if (datum.value !== null) {
                valueMinimum = Math.min(valueMinimum, datum.value);
                valueMaximum = Math.max(valueMaximum, datum.value);
                valueCount += 1;
            }
        }
        if (data.length === 0)
            return [];
        const position = (datum) => ({
            x: plot.x + ((datum.x - xMinimum) / (xMaximum - xMinimum || 1)) * plot.width,
            y: plot.y + plot.height - ((datum.y - yMinimum) / (yMaximum - yMinimum || 1)) * plot.height,
        });
        const nodes = [];
        const mode = optionString$4(layer.mark.options, 'mode', 'base');
        const totalLineBudget = context.performance.maxLinePoints;
        const gridLineBudget = mode === 'contour' ? Math.floor(totalLineBudget / 2) : totalLineBudget;
        const gridAxes = [
            { axis: 'a', groups: aGroups, budget: Math.ceil(gridLineBudget / 2) },
            { axis: 'b', groups: bGroups, budget: Math.floor(gridLineBudget / 2) },
        ];
        for (const { axis, groups, budget } of gridAxes) {
            const candidates = [...groups].map(([key, path], index) => ({ key, path, index }));
            const selected = sampledItems(candidates, Math.floor(budget / 2));
            selected.forEach(({ key, path: unsorted, index }, selectedIndex) => {
                const groupBudget = allocatedBudget(budget, selected.length, selectedIndex);
                const ordered = [...unsorted].sort((left, right) => axis === 'a' ? left.y - right.y : left.x - right.x);
                const path = sampledItems(ordered, groupBudget);
                if (path.length < 2)
                    return;
                const representative = path[0];
                if (representative === undefined)
                    return;
                nodes.push({
                    type: 'path',
                    ...datumBase$1(context, `${layer.id}:carpet-grid:${axis}:${index}`, representative.rowIndex, 0, {
                        kind: 'carpet-axis',
                        axis: axis === 'a' ? aField : bField,
                        key,
                        logicalCoordinate: axis,
                    }),
                    points: path.map(position),
                    closed: false,
                    stroke: axis === 'a'
                        ? colorWithOpacity(theme.colors.axis, 0.82)
                        : colorWithOpacity(theme.colors.grid, 0.9),
                    lineWidth: axis === 'a' ? 1.35 : 1,
                    lineCap: 'round',
                    lineJoin: 'round',
                });
            });
        }
        if (mode === 'contour') {
            if (valueCount > 0) {
                const contourPointBudget = totalLineBudget - gridLineBudget;
                const maximumSegments = Math.floor(contourPointBudget / 2);
                const [aKeys, bKeys] = boundedGridKeys([...aGroups.keys()], [...bGroups.keys()], Math.min(context.performance.maxBarMarks, Math.max(1, contourPointBudget)));
                const gridValues = bKeys.map((b) => aKeys.map((a) => indexed.get(a)?.get(b)?.value ?? null));
                const gridPoints = bKeys.map((b) => aKeys.map((a) => {
                    const datum = indexed.get(a)?.get(b);
                    return datum === undefined ? null : position(datum);
                }));
                const levelCount = clamp$9(Math.floor(optionNumber$4(layer.mark.options, 'levels', 6)), 2, 16);
                const levels = Array.from({ length: levelCount }, (_, index) => valueMinimum + ((index + 1) / (levelCount + 1)) * (valueMaximum - valueMinimum || 1));
                const gridRows = bKeys.map((b) => aKeys.map((a) => indexed.get(a)?.get(b)?.rowIndex ?? null));
                extractIsolines(gridValues, gridPoints, levels, gridRows, { maximumSegments }).forEach((segment, index) => {
                    const ratio = segment.levelIndex / Math.max(1, levelCount - 1);
                    const stroke = layer.mark.stroke ?? mappedContinuousColor(theme, ratio);
                    nodes.push({
                        type: 'path',
                        ...datumBase$1(context, `${layer.id}:carpet-contour:${segment.levelIndex}:${index}`, data[0]?.rowIndex ?? 0, 2, {
                            kind: 'value-isoline',
                            level: levels[segment.levelIndex] ?? valueMinimum,
                            minimumValue: valueMinimum,
                            maximumValue: valueMaximum,
                            valueField: valueField ?? 'value',
                            sourceRowCount: segment.sourceRows.length,
                            sourceRowIndices: segment.sourceRows.slice(0, 256),
                        }),
                        points: segment.points,
                        closed: segment.closed,
                        stroke,
                        lineWidth: layer.mark.lineWidth ?? 1.8,
                        lineCap: theme.mark.lineCap ?? 'round',
                        lineJoin: theme.mark.lineJoin ?? 'round',
                    });
                });
            }
        }
        else if (mode === 'scatter') {
            sampledItems(data, context.performance.maxPointMarks).forEach((datum) => {
                const point = position(datum);
                nodes.push({
                    type: 'circle',
                    ...datumBase$1(context, `${layer.id}:carpet-point:${datum.rowIndex}`, datum.rowIndex, 2),
                    cx: point.x,
                    cy: point.y,
                    radius: layer.mark.radius ?? 4,
                    fill: layer.mark.fill ?? context.color,
                    stroke: theme.colors.background,
                    lineWidth: 1.2,
                });
            });
        }
        return nodes;
    };

    const analyticalFamilies = new Set([
        'distribution',
        'heatmap',
        'image',
        'ternary',
        'smith',
        'scatter-matrix',
        'carpet',
        'item',
    ]);
    function clamp$8(value, minimum, maximum) {
        return Math.max(minimum, Math.min(maximum, value));
    }
    function object$2(value) {
        return value !== null && typeof value === 'object' && !Array.isArray(value)
            ? value
            : null;
    }
    function optionNumber$3(options, key, fallback) {
        const value = options[key];
        return typeof value === 'number' && Number.isFinite(value) ? value : fallback;
    }
    function optionString$3(options, key, fallback) {
        const value = options[key];
        return typeof value === 'string' && value.trim() !== '' ? value : fallback;
    }
    function optionChoice(options, key, choices, fallback) {
        const value = options[key];
        if (value === undefined)
            return fallback;
        if (typeof value === 'string' && choices.includes(value))
            return value;
        throw new GraflumeError('INVALID_SPEC', `$.mark.options.${key} must be one of ${choices.join(', ')}.`, { path: `$.mark.options.${key}` });
    }
    function optionStrings$1(value) {
        return Array.isArray(value)
            ? value.filter((entry) => typeof entry === 'string' && entry.trim() !== '')
            : [];
    }
    function optionNumbers$1(value) {
        return Array.isArray(value)
            ? value.filter((entry) => typeof entry === 'number' && Number.isFinite(entry))
            : [];
    }
    function stringNumber(value) {
        if (typeof value === 'number' && Number.isFinite(value))
            return value;
        if (typeof value === 'string')
            return value;
        if (value instanceof Date && Number.isFinite(value.getTime()))
            return value.toISOString();
        return null;
    }
    function stringValue$1(value) {
        const resolved = stringNumber(value);
        return resolved === null ? null : String(resolved);
    }
    function palette$1(context, index, count) {
        return categoricalColor(context.theme, index, Math.max(1, count));
    }
    function textNode$5(context, id, x, y, text, options = {}) {
        return {
            type: 'text',
            ...nodeBase(id, { zIndex: context.layer.zIndex + 4 }),
            x,
            y,
            text,
            fill: options.fill ?? context.theme.colors.text,
            fontFamily: context.theme.typography.fontFamily,
            fontSize: options.size ?? context.theme.typography.fontSize,
            fontWeight: options.weight ?? 600,
            align: options.align ?? 'center',
            baseline: options.baseline ?? 'middle',
            rotation: 0,
        };
    }
    function datumNode(context, id, rowIndex, tooltip, zIndex = 1, familyInteraction) {
        return nodeBase(id, {
            zIndex: context.layer.zIndex + zIndex,
            opacity: context.layer.mark.opacity,
            interactive: context.performance.enableHitTesting,
            datum: {
                layerId: context.layer.id,
                rowIndex,
                datum: context.table.row(rowIndex),
                tooltip,
                ...(familyInteraction === undefined ? {} : { familyInteraction }),
            },
        });
    }
    function identity(value) {
        return `${typeof value}:${String(value)}`;
    }
    function familyMode(layer) {
        const type = layer.mark.type;
        if (type === 'distribution')
            return resolveDistributionMode(layer.mark.options.mode);
        if (type === 'heatmap')
            return optionString$3(layer.mark.options, 'colorMode', 'sequential');
        if (type === 'image')
            return object$2(layer.mark.options.raster) === null ? 'rows' : 'raster';
        if (type === 'ternary')
            return optionString$3(layer.mark.options, 'policy', 'reject');
        if (type === 'smith')
            return optionString$3(layer.mark.options, 'mode', 'reflection');
        if (type === 'scatter-matrix') {
            return `${optionString$3(layer.mark.options, 'diagonal', 'kde')}/${optionString$3(layer.mark.options, 'upper', 'scatter')}/${optionString$3(layer.mark.options, 'lower', 'scatter')}`;
        }
        if (type === 'carpet')
            return object$2(layer.mark.options.grid) === null ? 'row-grid' : 'projected-grid';
        if (type === 'item')
            return optionString$3(layer.mark.options, 'mode', 'waffle');
        return 'default';
    }
    /** Renderer-neutral metadata for the eight P0 analytical compiler contracts. */
    function resolveAnalyticalFamilySceneMetadata(layer) {
        if (!analyticalFamilies.has(layer.mark.type))
            return null;
        const contracts = {
            distribution: ['shared-bins', 'weighted-notched-box', 'deterministic-rug-strip'],
            heatmap: ['matrix-pivot', 'irregular-extents', 'missing-pattern', 'linked-brush'],
            image: ['extent-origin', 'nearest-bilinear-bicubic', 'window-colormap-alpha'],
            ternary: [
                'constant-sum',
                'explicit-normalization',
                'component-ticks-format',
                'barycentric-tooltip',
            ],
            smith: [
                'reflection-z-y-s',
                'reference-impedance',
                'impedance-admittance-combined-grid',
                'specialist-labels',
            ],
            'scatter-matrix': ['diagonal-plan', 'upper-lower-plan', 'linked-selection-key'],
            carpet: ['irregular-logical-grid', 'logical-axis-ticks', 'mask', 'dual-coordinate-tooltip'],
            item: ['waffle-isotype', 'partial-units', 'fill-direction'],
        };
        return {
            layerId: layer.id,
            family: layer.mark.type,
            mode: familyMode(layer),
            contracts: contracts[layer.mark.type] ?? [],
            interaction: {
                hitTesting: 'datum',
                selectionKey: layer.mark.type === 'scatter-matrix' ? 'scatter-matrix' : `${layer.mark.type}:${layer.id}`,
                linked: layer.mark.type === 'heatmap' || layer.mark.type === 'scatter-matrix',
            },
        };
    }
    function compileSharedHistogram(context) {
        const { layer, table, xScale, yScale, theme } = context;
        const valueField = layer.mark.fields.value ?? layer.x.field;
        const weightField = layer.mark.fields.weight ?? optionString$3(layer.mark.options, 'weightField', '');
        const seriesField = layer.mark.fields.series ?? layer.mark.fields.group;
        const groups = new Map();
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const value = numericDataValue(table.value(rowIndex, valueField), layer.x.type === 'temporal');
            if (value === null)
                continue;
            const weight = weightField === '' ? 1 : numericDataValue(table.value(rowIndex, weightField));
            if (weight === null || weight < 0)
                continue;
            const label = seriesField === undefined
                ? 'Series'
                : (stringValue$1(table.value(rowIndex, seriesField)) ?? '');
            const key = `${typeof table.value(rowIndex, seriesField ?? valueField)}:${label}`;
            const group = groups.get(key) ?? { label, observations: [] };
            group.observations.push({ value, weight, rowIndex });
            groups.set(key, group);
        }
        const availableEntries = [...groups.values()].filter(({ observations }) => observations.length > 0);
        if (availableEntries.length === 0)
            return [];
        const requestedBinCount = clamp$8(Math.floor(optionNumber$3(layer.mark.options, 'bins', 10)), 1, 100);
        const seriesLimit = Math.max(1, Math.floor(context.performance.maxBarMarks / requestedBinCount));
        const entries = exactStrideSampleIndices(availableEntries.length, Math.min(seriesLimit, availableEntries.length)).map((index) => availableEntries[index]);
        const binCount = Math.min(requestedBinCount, Math.max(1, Math.floor(context.performance.maxBarMarks / entries.length)));
        const bins = sharedHistogramBins(entries.map(({ observations }) => observations), binCount);
        const normalization = optionString$3(layer.mark.options, 'normalization', 'count');
        const cumulative = layer.mark.options.cumulative === true;
        const totals = entries.map(({ observations }) => observations.reduce((sum, observation) => sum + (weightField === '' ? 1 : observation.weight), 0));
        const running = entries.map(() => 0);
        const baseline = yScale.map(0);
        const gap = Math.max(0, theme.mark.histogramGap ?? 2);
        const nodes = [];
        bins.forEach((bin, binIndex) => {
            const x0 = xScale.map(bin.start);
            const x1 = xScale.map(bin.end);
            if (![x0, x1, baseline].every(Number.isFinite))
                return;
            const span = Math.abs(x1 - x0);
            entries.forEach((entry, seriesIndex) => {
                const raw = weightField === '' ? (bin.counts[seriesIndex] ?? 0) : (bin.weights[seriesIndex] ?? 0);
                running[seriesIndex] = (running[seriesIndex] ?? 0) + raw;
                const total = totals[seriesIndex] ?? 0;
                const width = Math.max(Number.EPSILON, bin.end - bin.start);
                const normalized = normalization === 'probability' || normalization === 'normalized'
                    ? total === 0
                        ? 0
                        : raw / total
                    : normalization === 'density'
                        ? total === 0
                            ? 0
                            : raw / total / width
                        : raw;
                const value = cumulative
                    ? normalization === 'density'
                        ? total === 0
                            ? 0
                            : (running[seriesIndex] ?? 0) / total
                        : normalization === 'probability' || normalization === 'normalized'
                            ? total === 0
                                ? 0
                                : (running[seriesIndex] ?? 0) / total
                            : (running[seriesIndex] ?? 0)
                    : normalized;
                const y = yScale.map(value);
                if (!Number.isFinite(y))
                    return;
                const sourceRows = entry.observations
                    .filter(({ value: observation }) => binIndex === bins.length - 1
                    ? observation >= bin.start && observation <= bin.end
                    : observation >= bin.start && observation < bin.end)
                    .map(({ rowIndex }) => rowIndex);
                if (sourceRows.length === 0)
                    return;
                const groupWidth = span / entries.length;
                const left = Math.min(x0, x1) + seriesIndex * groupWidth;
                nodes.push({
                    type: 'rect',
                    ...datumNode(context, `${layer.id}:shared-bin:${binIndex}:${seriesIndex}`, sourceRows[0], {
                        analyticsFamily: 'distribution',
                        analyticsMode: 'shared-histogram',
                        series: entry.label,
                        binStart: bin.start,
                        binEnd: bin.end,
                        count: bin.counts[seriesIndex] ?? 0,
                        weight: bin.weights[seriesIndex] ?? 0,
                        value,
                        normalization,
                        cumulative,
                        sourceRowCount: sourceRows.length,
                        sourceRowIndices: sourceRows.slice(0, 256),
                    }),
                    x: left + gap / 2,
                    y: Math.min(y, baseline),
                    width: Math.max(1, groupWidth - gap),
                    height: Math.max(0.5, Math.abs(baseline - y)),
                    fill: layer.mark.fill ?? palette$1(context, seriesIndex, entries.length),
                    ...(layer.mark.stroke === undefined ? {} : { stroke: layer.mark.stroke }),
                    lineWidth: layer.mark.lineWidth ?? (layer.mark.stroke === undefined ? 0 : 1),
                    cornerRadius: layer.mark.cornerRadius ?? theme.mark.barRadius,
                });
            });
        });
        return nodes;
    }
    function compileWeightedBox(context) {
        const { layer, table, xScale, yScale, theme, plot } = context;
        const summaryFields = [
            layer.mark.fields.min ?? layer.mark.fields.low ?? 'min',
            layer.mark.fields.q1 ?? 'q1',
            layer.mark.fields.median ?? layer.y.field,
            layer.mark.fields.q3 ?? 'q3',
            layer.mark.fields.max ?? layer.mark.fields.high ?? 'max',
        ];
        if (summaryFields.every((field) => table.has(field)))
            return compileDistributionFamilyMark(context);
        const valueField = layer.mark.fields.value ?? layer.y.field;
        const weightField = layer.mark.fields.weight ?? optionString$3(layer.mark.options, 'weightField', '');
        const grouped = new Map();
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const x = table.value(rowIndex, layer.x.field);
            const category = stringValue$1(x);
            const value = numericDataValue(table.value(rowIndex, valueField));
            const weight = weightField === '' ? 1 : numericDataValue(table.value(rowIndex, weightField));
            if (category === null || value === null || weight === null || weight <= 0)
                continue;
            const group = grouped.get(category) ?? { x, observations: [] };
            group.observations.push({ value, weight, rowIndex });
            grouped.set(category, group);
        }
        const availableGroups = [...grouped];
        const retainedGroups = exactStrideSampleIndices(availableGroups.length, Math.min(availableGroups.length, Math.max(1, Math.floor(context.performance.maxBarMarks / 3)))).map((index) => availableGroups[index]);
        const boxWidth = Math.max(8, xScale instanceof BandScale
            ? xScale.bandwidth * 0.58
            : plot.width / Math.max(3, retainedGroups.length * 2));
        const notched = layer.mark.options.notched === true;
        const nodes = [];
        retainedGroups.forEach(([category, group], groupIndex) => {
            const summary = weightedBoxSummary(group.observations);
            const input = scaleInput(group.x);
            if (summary === null || input === null)
                return;
            const x = xScale.map(input);
            const yMinimum = yScale.map(summary.minimum);
            const yQ1 = yScale.map(summary.q1);
            const yMedian = yScale.map(summary.median);
            const yQ3 = yScale.map(summary.q3);
            const yMaximum = yScale.map(summary.maximum);
            const yNotchLow = yScale.map(summary.notch[0]);
            const yNotchHigh = yScale.map(summary.notch[1]);
            if (![x, yMinimum, yQ1, yMedian, yQ3, yMaximum, yNotchLow, yNotchHigh].every(Number.isFinite))
                return;
            const stroke = layer.mark.stroke ?? theme.mark.areaStroke ?? context.color;
            const fill = layer.mark.fill ?? theme.mark.boxplotFill ?? colorWithOpacity(context.color, 0.24);
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:weighted-box-whisker:${groupIndex}`, {
                    zIndex: layer.zIndex,
                }),
                x1: x,
                y1: yMinimum,
                x2: x,
                y2: yMaximum,
                stroke,
                lineWidth: layer.mark.lineWidth ?? 1.5,
                lineCap: theme.mark.lineCap ?? 'round',
            });
            const tooltip = {
                analyticsFamily: 'distribution',
                analyticsMode: notched ? 'weighted-notched-box' : 'weighted-box',
                category,
                valueField,
                weightField: weightField === '' ? null : weightField,
                minimum: summary.minimum,
                q1: summary.q1,
                median: summary.median,
                q3: summary.q3,
                maximum: summary.maximum,
                notch: summary.notch,
                effectiveSampleSize: summary.effectiveSampleSize,
                sourceRowCount: summary.sourceRows.length,
                sourceRowIndices: summary.sourceRows.slice(0, 256),
            };
            if (notched) {
                const half = boxWidth / 2;
                const inner = boxWidth * 0.24;
                nodes.push({
                    type: 'path',
                    ...datumNode(context, `${layer.id}:weighted-notch:${groupIndex}`, summary.sourceRows[0], tooltip),
                    points: [
                        { x: x - half, y: yQ3 },
                        { x: x + half, y: yQ3 },
                        { x: x + half, y: yNotchHigh },
                        { x: x + inner, y: yMedian },
                        { x: x + half, y: yNotchLow },
                        { x: x + half, y: yQ1 },
                        { x: x - half, y: yQ1 },
                        { x: x - half, y: yNotchLow },
                        { x: x - inner, y: yMedian },
                        { x: x - half, y: yNotchHigh },
                    ],
                    closed: true,
                    fill,
                    stroke,
                    lineWidth: layer.mark.lineWidth ?? 1.8,
                    lineJoin: theme.mark.lineJoin ?? 'round',
                });
            }
            else {
                nodes.push({
                    type: 'rect',
                    ...datumNode(context, `${layer.id}:weighted-box:${groupIndex}`, summary.sourceRows[0], tooltip),
                    x: x - boxWidth / 2,
                    y: Math.min(yQ1, yQ3),
                    width: boxWidth,
                    height: Math.max(1, Math.abs(yQ3 - yQ1)),
                    fill,
                    stroke,
                    lineWidth: layer.mark.lineWidth ?? 1.8,
                    cornerRadius: layer.mark.cornerRadius ?? theme.mark.boxplotRadius ?? 3,
                });
            }
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:weighted-box-median:${groupIndex}`, {
                    zIndex: layer.zIndex + 2,
                }),
                x1: x - boxWidth * 0.28,
                y1: yMedian,
                x2: x + boxWidth * 0.28,
                y2: yMedian,
                stroke,
                lineWidth: layer.mark.lineWidth ?? 2.2,
                lineCap: theme.mark.lineCap ?? 'round',
            });
        });
        return nodes;
    }
    function compileRugStrip(context, mode) {
        const { layer, table, xScale, plot, theme } = context;
        const valueField = layer.mark.fields.value ?? layer.x.field;
        const values = [];
        const sourceRows = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const value = numericDataValue(table.value(rowIndex, valueField), layer.x.type === 'temporal');
            if (value === null)
                continue;
            values.push(value);
            sourceRows.push(rowIndex);
        }
        const spread = optionNumber$3(layer.mark.options, 'spread', mode === 'rug' ? 0 : 20);
        const seed = Math.floor(optionNumber$3(layer.mark.options, 'seed', 0));
        const points = rugStrip(values, { spread, seed });
        const sampled = exactStrideSampleIndices(points.length, context.performance.maxPointMarks).map((index) => points[index]);
        const baseline = plot.y + plot.height - 4;
        return sampled.map((point, index) => {
            const rowIndex = sourceRows[point.rowIndex];
            const x = xScale.map(point.value);
            const y = baseline - point.offset;
            const tooltip = {
                analyticsFamily: 'distribution',
                analyticsMode: mode,
                value: point.value,
                offset: point.offset,
                seed,
                selectionKey: `distribution:${layer.id}`,
            };
            if (mode === 'rug') {
                return {
                    type: 'line',
                    ...datumNode(context, `${layer.id}:rug:${index}:${rowIndex}`, rowIndex, tooltip),
                    x1: x,
                    y1: baseline - 10,
                    x2: x,
                    y2: baseline,
                    stroke: layer.mark.stroke ?? context.color,
                    lineWidth: layer.mark.lineWidth ?? 1.4,
                    lineCap: theme.mark.lineCap ?? 'round',
                };
            }
            return {
                type: 'circle',
                ...datumNode(context, `${layer.id}:strip:${index}:${rowIndex}`, rowIndex, tooltip),
                cx: x,
                cy: y,
                radius: layer.mark.radius ?? 3.5,
                fill: layer.mark.fill ?? context.color,
                stroke: theme.colors.background,
                lineWidth: 0.8,
            };
        });
    }
    const compileAnalyticalDistributionMark = (context) => {
        const mode = resolveDistributionMode(context.layer.mark.options.mode);
        if (mode === 'histogram') {
            const shared = context.layer.mark.options.sharedBins === true ||
                context.layer.mark.fields.series !== undefined ||
                context.layer.mark.fields.group !== undefined;
            return shared ? compileSharedHistogram(context) : compileDistributionFamilyMark(context);
        }
        if (mode === 'boxplot') {
            const weightedOrNotched = context.layer.mark.options.notched === true ||
                context.layer.mark.fields.weight !== undefined ||
                typeof context.layer.mark.options.weightField === 'string';
            return weightedOrNotched ? compileWeightedBox(context) : compileDistributionFamilyMark(context);
        }
        if (mode === 'rug' || mode === 'strip')
            return compileRugStrip(context, mode);
        return compileDistributionFamilyMark(context);
    };
    function numericField(table, rowIndex, field) {
        return field === undefined || !table.has(field)
            ? undefined
            : (numericDataValue(table.value(rowIndex, field)) ?? undefined);
    }
    function heatmapKey(row, column) {
        return `${identity(row)}\u0000${identity(column)}`;
    }
    function heatmapPattern(context, cell, rect, stroke) {
        if (cell.missingPattern === null)
            return [];
        const base = `${context.layer.id}:heatmap-missing:${cell.rowIndex}:${cell.columnIndex}`;
        if (cell.missingPattern === 'dots') {
            return [
                {
                    type: 'circle',
                    ...nodeBase(`${base}:dot`, { zIndex: context.layer.zIndex + 2 }),
                    cx: rect.x + rect.width / 2,
                    cy: rect.y + rect.height / 2,
                    radius: Math.max(1.2, Math.min(3, Math.min(rect.width, rect.height) * 0.1)),
                    fill: stroke,
                    lineWidth: 0,
                },
            ];
        }
        if (cell.missingPattern === 'stripes') {
            return [-0.25, 0.25].map((offset, index) => ({
                type: 'line',
                ...nodeBase(`${base}:stripe:${index}`, { zIndex: context.layer.zIndex + 2 }),
                x1: rect.x + rect.width * Math.max(0, offset),
                y1: rect.y + rect.height,
                x2: rect.x + rect.width * Math.min(1, 1 + offset),
                y2: rect.y,
                stroke,
                lineWidth: 1,
            }));
        }
        return [
            {
                type: 'line',
                ...nodeBase(`${base}:cross:0`, { zIndex: context.layer.zIndex + 2 }),
                x1: rect.x + 2,
                y1: rect.y + 2,
                x2: rect.x + rect.width - 2,
                y2: rect.y + rect.height - 2,
                stroke,
                lineWidth: 1,
            },
            {
                type: 'line',
                ...nodeBase(`${base}:cross:1`, { zIndex: context.layer.zIndex + 2 }),
                x1: rect.x + rect.width - 2,
                y1: rect.y + 2,
                x2: rect.x + 2,
                y2: rect.y + rect.height - 2,
                stroke,
                lineWidth: 1,
            },
        ];
    }
    function heatmapLabels(value) {
        if (!Array.isArray(value))
            return undefined;
        if (!value.every((entry) => typeof entry === 'string' || typeof entry === 'number'))
            throw new GraflumeError('INVALID_SPEC', 'Heatmap matrix labels must be strings or numbers.');
        return value;
    }
    function heatmapExtents(value, path) {
        if (value === undefined)
            return undefined;
        if (!Array.isArray(value) ||
            !value.every((entry) => Array.isArray(entry) &&
                entry.length === 2 &&
                entry.every((part) => typeof part === 'number' && Number.isFinite(part))))
            throw new GraflumeError('INVALID_SPEC', `${path} must contain finite [start, end] pairs.`);
        return value;
    }
    function heatmapMatrixOption(context) {
        const raw = context.layer.mark.options.matrix;
        if (raw === undefined)
            return null;
        const authored = object$2(raw);
        const values = authored?.values ?? raw;
        if (!Array.isArray(values) || !values.every(Array.isArray))
            throw new GraflumeError('INVALID_SPEC', '$.mark.options.matrix must be a numeric matrix or an object containing values.');
        const rows = heatmapLabels(authored?.rows ?? context.layer.mark.options.matrixRows);
        const columns = heatmapLabels(authored?.columns ?? context.layer.mark.options.matrixColumns);
        const xExtents = heatmapExtents(authored?.xExtents ?? context.layer.mark.options.xExtents, '$.mark.options.matrix.xExtents');
        const yExtents = heatmapExtents(authored?.yExtents ?? context.layer.mark.options.yExtents, '$.mark.options.matrix.yExtents');
        return {
            values: values,
            ...(rows === undefined ? {} : { rows }),
            ...(columns === undefined ? {} : { columns }),
            ...(xExtents === undefined ? {} : { xExtents }),
            ...(yExtents === undefined ? {} : { yExtents }),
        };
    }
    const compileAnalyticalHeatmapMark = (context) => {
        const { layer, table, plot, theme } = context;
        const matrixInput = heatmapMatrixOption(context);
        const analyticalMatrix = matrixInput !== null ||
            ['x0', 'x1', 'y0', 'y1'].some((role) => layer.mark.fields[role] !== undefined) ||
            layer.encoding.x2 !== undefined ||
            layer.encoding.y2 !== undefined ||
            object$2(layer.mark.options.missing) !== null ||
            object$2(layer.mark.options.brush) !== null ||
            layer.mark.options.colorMode !== undefined ||
            layer.mark.options.color !== undefined ||
            layer.mark.options.rowOrder !== undefined ||
            layer.mark.options.columnOrder !== undefined;
        if (!analyticalMatrix)
            return compileHeatmapMark(context);
        const valueField = layer.mark.fields.value ?? 'value';
        if (matrixInput === null && !table.has(valueField))
            return compileHeatmapMark(context);
        const x0Field = layer.mark.fields.x0;
        const x1Field = layer.mark.fields.x1 ?? layer.encoding.x2?.field;
        const y0Field = layer.mark.fields.y0;
        const y1Field = layer.mark.fields.y1 ?? layer.encoding.y2?.field;
        const rows = [];
        const sourceByCell = new Map();
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const column = stringNumber(table.value(rowIndex, layer.x.field));
            const row = stringNumber(table.value(rowIndex, layer.y.field));
            if (row === null || column === null)
                continue;
            const value = numericDataValue(table.value(rowIndex, valueField));
            rows.push({
                row,
                column,
                value,
                ...(numericField(table, rowIndex, x0Field) === undefined
                    ? {}
                    : { x0: numericField(table, rowIndex, x0Field) }),
                ...(numericField(table, rowIndex, x1Field) === undefined
                    ? {}
                    : { x1: numericField(table, rowIndex, x1Field) }),
                ...(numericField(table, rowIndex, y0Field) === undefined
                    ? {}
                    : { y0: numericField(table, rowIndex, y0Field) }),
                ...(numericField(table, rowIndex, y1Field) === undefined
                    ? {}
                    : { y1: numericField(table, rowIndex, y1Field) }),
            });
            sourceByCell.set(heatmapKey(row, column), rowIndex);
        }
        if (matrixInput === null && rows.length === 0)
            return [];
        const colorMode = optionChoice(layer.mark.options, layer.mark.options.colorMode === undefined && layer.mark.options.color !== undefined
            ? 'color'
            : 'colorMode', ['sequential', 'diverging', 'log', 'symlog', 'quantile'], 'sequential');
        const missing = object$2(layer.mark.options.missing);
        const rowOrder = heatmapLabels(layer.mark.options.rowOrder);
        const columnOrder = heatmapLabels(layer.mark.options.columnOrder);
        const matrix = buildHeatmapMatrix(matrixInput ?? rows, {
            ...(rowOrder === undefined || rowOrder.length === 0 ? {} : { rowOrder }),
            ...(columnOrder === undefined || columnOrder.length === 0 ? {} : { columnOrder }),
            color: colorMode,
            midpoint: optionNumber$3(layer.mark.options, 'midpoint', 0),
            ...(missing === null
                ? {}
                : {
                    missing: {
                        ...(typeof missing.color === 'string' ? { color: missing.color } : {}),
                        ...(missing.pattern === 'cross' ||
                            missing.pattern === 'dots' ||
                            missing.pattern === 'stripes'
                            ? { pattern: missing.pattern }
                            : {}),
                    },
                }),
        });
        if (matrixInput !== null) {
            matrix.cells.forEach((cell) => sourceByCell.set(heatmapKey(cell.row, cell.column), 0));
        }
        if (matrix.cells.length === 0)
            return [];
        const brush = object$2(layer.mark.options.brush);
        const selected = new Set(brush === null
            ? []
            : brushHeatmap(matrix, {
                ...(Array.isArray(brush.rows)
                    ? {
                        rows: brush.rows.filter((entry) => typeof entry === 'string' || typeof entry === 'number'),
                    }
                    : {}),
                ...(Array.isArray(brush.columns)
                    ? {
                        columns: brush.columns.filter((entry) => typeof entry === 'string' || typeof entry === 'number'),
                    }
                    : {}),
                ...(optionNumbers$1(brush.value).length === 2
                    ? { value: optionNumbers$1(brush.value) }
                    : {}),
            }).map(({ row, column }) => heatmapKey(row, column)));
        const minimumX = Math.min(...matrix.cells.map(({ x0 }) => x0));
        const maximumX = Math.max(...matrix.cells.map(({ x1 }) => x1));
        const minimumY = Math.min(...matrix.cells.map(({ y0 }) => y0));
        const maximumY = Math.max(...matrix.cells.map(({ y1 }) => y1));
        const mapX = (value) => plot.x + ((value - minimumX) / Math.max(Number.EPSILON, maximumX - minimumX)) * plot.width;
        const mapY = (value) => plot.y +
            plot.height -
            ((value - minimumY) / Math.max(Number.EPSILON, maximumY - minimumY)) * plot.height;
        const retained = exactStrideSampleIndices(matrix.cells.length, Math.max(1, context.performance.maxBarMarks)).map((index) => matrix.cells[index]);
        const nodes = [];
        retained.forEach((cell) => {
            const x0 = mapX(cell.x0);
            const x1 = mapX(cell.x1);
            const y0 = mapY(cell.y0);
            const y1 = mapY(cell.y1);
            const rect = {
                x: Math.min(x0, x1) + 0.5,
                y: Math.min(y0, y1) + 0.5,
                width: Math.max(1, Math.abs(x1 - x0) - 1),
                height: Math.max(1, Math.abs(y1 - y0) - 1),
            };
            const key = heatmapKey(cell.row, cell.column);
            const sourceRow = sourceByCell.get(key);
            const brushed = selected.has(key);
            const familyInteraction = {
                kind: 'heatmap-cell',
                row: cell.row,
                column: cell.column,
                rowIndex: cell.rowIndex,
                columnIndex: cell.columnIndex,
                rowCount: matrix.rows.length,
                columnCount: matrix.columns.length,
                value: cell.value,
            };
            const fill = cell.value === null
                ? typeof missing?.color === 'string'
                    ? missing.color
                    : colorWithOpacity(theme.colors.surface, 0.72)
                : (layer.mark.fill ?? mappedContinuousColor(theme, cell.colorPosition ?? 0.5));
            const tooltip = {
                analyticsFamily: 'heatmap',
                analyticsMode: colorMode,
                row: cell.row,
                column: cell.column,
                value: cell.value,
                extent: [cell.x0, cell.x1, cell.y0, cell.y1],
                colorPosition: cell.colorPosition,
                missingPattern: cell.missingPattern,
                brushed,
                selectionKey: `heatmap:${key}`,
            };
            const base = sourceRow === undefined && table.length === 0
                ? nodeBase(`${layer.id}:analytic-heatmap:${cell.rowIndex}:${cell.columnIndex}`, {
                    zIndex: layer.zIndex,
                })
                : datumNode(context, `${layer.id}:analytic-heatmap:${cell.rowIndex}:${cell.columnIndex}`, sourceRow ?? 0, tooltip, 1, familyInteraction);
            nodes.push({
                type: 'rect',
                ...base,
                ...rect,
                fill,
                stroke: brushed ? theme.colors.focus : (layer.mark.stroke ?? theme.colors.background),
                lineWidth: brushed ? 2.5 : (layer.mark.lineWidth ?? 1),
                cornerRadius: layer.mark.cornerRadius ?? 1,
            });
            nodes.push(...heatmapPattern(context, cell, rect, theme.colors.mutedText));
            if (cell.value !== null &&
                rect.width >= 32 &&
                rect.height >= 20 &&
                layer.mark.options.labels !== false) {
                nodes.push(textNode$5(context, `${layer.id}:analytic-heatmap-label:${cell.rowIndex}:${cell.columnIndex}`, rect.x + rect.width / 2, rect.y + rect.height / 2, String(cell.value), { fill: readableTextColor(fill, '#ffffff', '#0f172a'), size: 10 }));
            }
        });
        return nodes;
    };
    function rasterOption(value) {
        const input = object$2(value);
        if (input === null)
            return null;
        const width = input.width;
        const height = input.height;
        const channels = input.channels;
        const values = optionNumbers$1(input.values);
        if (typeof width !== 'number' ||
            typeof height !== 'number' ||
            typeof channels !== 'number' ||
            !Array.isArray(input.values) ||
            values.length !== input.values.length) {
            throw new GraflumeError('INVALID_SPEC', 'Image raster requires numeric width, height, channels, and values.', {
                path: '$.mark.options.raster',
            });
        }
        const extent = optionNumbers$1(input.extent);
        const origin = input.origin === 'lower' ? 'lower' : 'upper';
        return {
            width,
            height,
            channels,
            values,
            ...(extent.length === 4 ? { extent: extent } : {}),
            origin,
        };
    }
    const compileAnalyticalImageMark = (context) => {
        const { layer, table, plot, theme, xScale, yScale } = context;
        const raster = rasterOption(layer.mark.options.raster);
        if (raster === null)
            return compileImageMark(context);
        const maximum = Math.max(1, context.performance.maxBarMarks);
        const requestedWidth = clamp$8(Math.floor(optionNumber$3(layer.mark.options, 'outputWidth', raster.width)), 1, 2_048);
        const requestedHeight = clamp$8(Math.floor(optionNumber$3(layer.mark.options, 'outputHeight', raster.height)), 1, 2_048);
        const ratio = Math.min(1, Math.sqrt(maximum / (requestedWidth * requestedHeight)));
        const width = Math.max(1, Math.floor(requestedWidth * ratio));
        const height = Math.max(1, Math.floor(requestedHeight * ratio));
        const extent = raster.extent ?? [0, raster.width, 0, raster.height];
        const resampling = optionString$3(layer.mark.options, 'resampling', 'nearest');
        if (!['nearest', 'bilinear', 'bicubic'].includes(resampling)) {
            throw new GraflumeError('INVALID_SPEC', 'Image resampling must be nearest, bilinear, or bicubic.', {
                path: '$.mark.options.resampling',
            });
        }
        const window = optionNumbers$1(layer.mark.options.window);
        const colormap = optionStrings$1(layer.mark.options.colormap);
        if (layer.mark.options.window !== undefined && window.length !== 2)
            throw new GraflumeError('INVALID_SPEC', 'Image raster window must contain two finite numbers.', {
                path: '$.mark.options.window',
            });
        if (layer.mark.options.colormap !== undefined && !Array.isArray(layer.mark.options.colormap))
            throw new GraflumeError('INVALID_SPEC', 'Image raster colormap must be an array.', {
                path: '$.mark.options.colormap',
            });
        const alpha = optionNumber$3(layer.mark.options, 'alpha', 1);
        const mapX = (value) => xScale.map(value);
        const mapY = (value) => yScale.map(value);
        const imageX0 = mapX(extent[0]);
        const imageX1 = mapX(extent[1]);
        const imageY0 = mapY(extent[2]);
        const imageY1 = mapY(extent[3]);
        if (![imageX0, imageX1, imageY0, imageY1].every(Number.isFinite))
            throw new GraflumeError('INVALID_SPEC', 'Image raster extent must map through finite quantitative x and y scales.', { path: '$.mark.options.raster.extent' });
        const imageBounds = {
            x: Math.min(imageX0, imageX1),
            y: Math.min(imageY0, imageY1),
            width: Math.abs(imageX1 - imageX0),
            height: Math.abs(imageY1 - imageY0),
        };
        const nodes = [];
        for (let row = 0; row < height; row += 1) {
            for (let column = 0; column < width; column += 1) {
                const dataX0 = extent[0] + (column / width) * (extent[1] - extent[0]);
                const dataX1 = extent[0] + ((column + 1) / width) * (extent[1] - extent[0]);
                const dataY0 = extent[2] + (row / height) * (extent[3] - extent[2]);
                const dataY1 = extent[2] + ((row + 1) / height) * (extent[3] - extent[2]);
                const x = (dataX0 + dataX1) / 2;
                const y = (dataY0 + dataY1) / 2;
                const channels = sampleRaster(raster, x, y, resampling);
                const rgba = mapRasterColor(channels, {
                    ...(window.length === 2 ? { window: window } : {}),
                    ...(layer.mark.options.colormap === undefined ? {} : { colormap }),
                    alpha,
                });
                const fill = `rgba(${rgba[0]}, ${rgba[1]}, ${rgba[2]}, ${rgba[3]})`;
                const id = `${layer.id}:analytic-image:${row}:${column}`;
                const base = table.length === 0
                    ? nodeBase(id, { zIndex: layer.zIndex })
                    : datumNode(context, id, 0, {
                        analyticsFamily: 'image',
                        analyticsMode: 'raster',
                        dataX: x,
                        dataY: y,
                        channels,
                        rgba,
                        resampling,
                        origin: raster.origin ?? 'upper',
                        extent,
                        selectionKey: `image:${row}:${column}`,
                    });
                nodes.push({
                    type: 'rect',
                    ...base,
                    x: Math.min(mapX(dataX0), mapX(dataX1)),
                    y: Math.min(mapY(dataY0), mapY(dataY1)),
                    width: Math.max(1, Math.abs(mapX(dataX1) - mapX(dataX0)) + 0.25),
                    height: Math.max(1, Math.abs(mapY(dataY1) - mapY(dataY0)) + 0.25),
                    fill,
                    ...(layer.mark.stroke === undefined ? {} : { stroke: layer.mark.stroke }),
                    lineWidth: layer.mark.stroke === undefined ? 0 : (layer.mark.lineWidth ?? 1),
                    cornerRadius: 0,
                });
            }
        }
        if (layer.mark.options.frame === true) {
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:analytic-image-frame`, { zIndex: layer.zIndex + 2 }),
                ...imageBounds,
                stroke: layer.mark.stroke ?? theme.colors.axis,
                lineWidth: layer.mark.lineWidth ?? 1,
                cornerRadius: 0,
            });
        }
        return nodes;
    };
    function trianglePoint(plot, point) {
        const insetX = Math.min(24, plot.width * 0.05);
        const insetY = Math.min(18, plot.height * 0.05);
        return {
            x: plot.x + insetX + point.x * Math.max(1, plot.width - insetX * 2),
            y: plot.y + insetY + point.y * Math.max(1, plot.height - insetY * 2),
        };
    }
    function ternaryTickFormatter(context, target) {
        const mode = optionChoice(context.layer.mark.options, 'tickFormat', ['auto', 'number', 'percent'], 'auto');
        const digits = clamp$8(Math.floor(optionNumber$3(context.layer.mark.options, 'tickFractionDigits', mode === 'auto' ? 6 : 1)), mode === 'auto' ? 1 : 0, 6);
        return (value) => {
            if (mode === 'auto')
                return String(Number(value.toPrecision(digits)));
            const formatted = Number((mode === 'percent' ? (value / target) * 100 : value).toFixed(digits));
            return `${formatted}${mode === 'percent' ? '%' : ''}`;
        };
    }
    const compileAnalyticalTernaryMark = (context) => {
        const { layer, table, plot, theme } = context;
        if (!['sum', 'policy', 'tolerance', 'ticks', 'tickFormat', 'tickFractionDigits'].some((option) => layer.mark.options[option] !== undefined)) {
            return compileTernaryMark(context);
        }
        const aField = layer.mark.fields.a ?? layer.x.field;
        const bField = layer.mark.fields.b ?? layer.y.field;
        const cField = layer.mark.fields.c ?? 'c';
        const idField = layer.mark.fields.id;
        if (![aField, bField, cField].every((field) => table.has(field)))
            return [];
        const input = [];
        const sourceRows = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const a = numericDataValue(table.value(rowIndex, aField));
            const b = numericDataValue(table.value(rowIndex, bField));
            const c = numericDataValue(table.value(rowIndex, cField));
            if (a === null || b === null || c === null)
                continue;
            input.push({
                a,
                b,
                c,
                id: idField === undefined
                    ? `ternary-${rowIndex}`
                    : (stringValue$1(table.value(rowIndex, idField)) ?? `ternary-${rowIndex}`),
            });
            sourceRows.push(rowIndex);
        }
        if (input.length === 0)
            return [];
        const targetSum = optionNumber$3(layer.mark.options, 'sum', 1);
        const result = projectTernary(input, {
            sum: targetSum,
            policy: optionChoice(layer.mark.options, 'policy', ['reject', 'normalize'], 'reject'),
            tolerance: optionNumber$3(layer.mark.options, 'tolerance', 1e-9),
            ticks: Math.floor(optionNumber$3(layer.mark.options, 'ticks', 5)),
            tickFormat: ternaryTickFormatter(context, targetSum),
        });
        const frame = [
            trianglePoint(plot, { x: 0, y: 1 }),
            trianglePoint(plot, { x: 1, y: 1 }),
            trianglePoint(plot, { x: 0.5, y: 1 - Math.sqrt(3) / 2 }),
        ];
        const nodes = [
            {
                type: 'path',
                ...nodeBase(`${layer.id}:analytic-ternary-frame`, { zIndex: layer.zIndex }),
                points: frame,
                closed: true,
                stroke: layer.mark.stroke ?? theme.colors.axis,
                fill: colorWithOpacity(theme.colors.surface, 0.2),
                lineWidth: layer.mark.lineWidth ?? 1.5,
                lineJoin: theme.mark.lineJoin ?? 'round',
            },
        ];
        const tickCount = Math.max(1, result.axes.a.length - 1);
        for (let index = 1; index < tickCount; index += 1) {
            const ratio = index / tickCount;
            const gridLines = [
                {
                    axis: 'a',
                    first: { x: 1 - ratio, y: 1 },
                    second: { x: (1 - ratio) * 0.5, y: 1 - (1 - ratio) * (Math.sqrt(3) / 2) },
                },
                {
                    axis: 'b',
                    first: { x: ratio, y: 1 },
                    second: { x: (1 + ratio) * 0.5, y: 1 - (1 - ratio) * (Math.sqrt(3) / 2) },
                },
                {
                    axis: 'c',
                    first: { x: ratio * 0.5, y: 1 - ratio * (Math.sqrt(3) / 2) },
                    second: { x: 1 - ratio * 0.5, y: 1 - ratio * (Math.sqrt(3) / 2) },
                },
            ];
            gridLines.forEach(({ axis, first, second }) => {
                const start = trianglePoint(plot, first);
                const end = trianglePoint(plot, second);
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:analytic-ternary-grid:${axis}:${index}`, {
                        zIndex: layer.zIndex,
                    }),
                    x1: start.x,
                    y1: start.y,
                    x2: end.x,
                    y2: end.y,
                    stroke: colorWithOpacity(theme.colors.grid, 0.65),
                    lineWidth: 1,
                });
            });
        }
        const seriesField = layer.mark.fields.series ?? layer.mark.fields.group;
        const series = new Map();
        result.points.forEach((point, pointIndex) => {
            const sourceRow = sourceRows[pointIndex];
            const projected = trianglePoint(plot, point);
            const key = seriesField === undefined
                ? 'Series'
                : (stringValue$1(table.value(sourceRow, seriesField)) ?? 'Series');
            const entries = series.get(key) ?? [];
            entries.push({ point: projected, sourceRow, pointIndex });
            series.set(key, entries);
        });
        const retainedPointIndices = new Set(exactStrideSampleIndices(result.points.length, Math.min(result.points.length, context.performance.maxPointMarks)));
        const lineGroups = [...series].filter(([, entries]) => entries.length > 1);
        const retainedLineGroupKeys = new Set(exactStrideSampleIndices(lineGroups.length, Math.min(lineGroups.length, Math.max(1, Math.floor(context.performance.maxLinePoints / 2)))).map((index) => lineGroups[index][0]));
        [...series].forEach(([name, entries], seriesIndex) => {
            const color = layer.mark.stroke ?? palette$1(context, seriesIndex, series.size);
            if (entries.length > 1 &&
                layer.mark.options.lines !== false &&
                retainedLineGroupKeys.has(name)) {
                const lineBudget = Math.max(2, Math.floor(context.performance.maxLinePoints / retainedLineGroupKeys.size));
                const lineEntries = exactStrideSampleIndices(entries.length, Math.min(entries.length, lineBudget)).map((index) => entries[index]);
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:analytic-ternary-series:${seriesIndex}`, {
                        zIndex: layer.zIndex + 1,
                    }),
                    points: lineEntries.map(({ point }) => point),
                    closed: false,
                    stroke: color,
                    lineWidth: layer.mark.lineWidth ?? 1.6,
                    lineCap: theme.mark.lineCap ?? 'round',
                    lineJoin: theme.mark.lineJoin ?? 'round',
                });
            }
            entries.forEach(({ point: projected, sourceRow, pointIndex }) => {
                if (!retainedPointIndices.has(pointIndex))
                    return;
                const point = result.points[pointIndex];
                nodes.push({
                    type: 'circle',
                    ...datumNode(context, `${layer.id}:analytic-ternary-point:${sourceRow}`, sourceRow, {
                        analyticsFamily: 'ternary',
                        analyticsMode: optionString$3(layer.mark.options, 'policy', 'reject'),
                        id: point.id,
                        series: name,
                        raw: point.raw,
                        normalized: point.normalized,
                        projected: [point.x, point.y],
                        sum: result.sum,
                        detail: point.tooltip,
                        selectionKey: `ternary:${point.id}`,
                    }),
                    cx: projected.x,
                    cy: projected.y,
                    radius: layer.mark.radius ?? 4.5,
                    fill: layer.mark.fill ?? color,
                    stroke: theme.colors.background,
                    lineWidth: 1.2,
                });
            });
        });
        const labelSize = Math.max(9, theme.typography.fontSize - 1);
        result.axes.a.forEach((tick, index) => {
            const ratio = index / tickCount;
            const a = trianglePoint(plot, {
                x: (1 - ratio) * 0.5,
                y: 1 - (1 - ratio) * (Math.sqrt(3) / 2),
            });
            const b = trianglePoint(plot, {
                x: (1 + ratio) * 0.5,
                y: 1 - (1 - ratio) * (Math.sqrt(3) / 2),
            });
            const c = trianglePoint(plot, { x: ratio, y: 1 });
            nodes.push(textNode$5(context, `${layer.id}:ternary-tick:a:${index}`, a.x - 5, a.y, tick.label, {
                fill: theme.colors.mutedText,
                size: labelSize,
                align: 'right',
            }), textNode$5(context, `${layer.id}:ternary-tick:b:${index}`, b.x + 5, b.y, result.axes.b[index].label, {
                fill: theme.colors.mutedText,
                size: labelSize,
                align: 'left',
            }), textNode$5(context, `${layer.id}:ternary-tick:c:${index}`, c.x, c.y + 9, result.axes.c[index].label, {
                fill: theme.colors.mutedText,
                size: labelSize,
            }));
        });
        nodes.push(textNode$5(context, `${layer.id}:ternary-axis:a`, frame[0].x - 8, frame[0].y + 13, aField, {
            fill: theme.colors.mutedText,
            size: labelSize,
            align: 'right',
        }), textNode$5(context, `${layer.id}:ternary-axis:b`, frame[1].x + 8, frame[1].y + 13, bField, {
            fill: theme.colors.mutedText,
            size: labelSize,
            align: 'left',
        }), textNode$5(context, `${layer.id}:ternary-axis:c`, frame[2].x, frame[2].y - 12, cField, {
            fill: theme.colors.mutedText,
            size: labelSize,
        }));
        return nodes;
    };
    function smithPixel(plot, radius, point) {
        return {
            x: plot.x + plot.width / 2 + point.x * radius,
            y: plot.y + plot.height / 2 - point.y * radius,
        };
    }
    function circlePath(cx, cy, radius, segments = 72) {
        return Array.from({ length: segments + 1 }, (_, index) => {
            const angle = (index / segments) * Math.PI * 2;
            return { x: cx + Math.cos(angle) * radius, y: cy + Math.sin(angle) * radius };
        });
    }
    function smithReflection(resistance, reactance) {
        const denominator = (resistance + 1) ** 2 + reactance ** 2;
        return {
            x: (resistance ** 2 + reactance ** 2 - 1) / denominator,
            y: (2 * reactance) / denominator,
        };
    }
    function smithReactancePath(plot, radius, reactance, admittance) {
        return Array.from({ length: 65 }, (_, index) => {
            const ratio = index / 64;
            const resistance = ratio === 1 ? 1_000_000 : (12 * ratio) / (1 - ratio);
            const reflected = smithReflection(resistance, reactance);
            return smithPixel(plot, radius, {
                x: admittance ? -reflected.x : reflected.x,
                y: admittance ? -reflected.y : reflected.y,
            });
        });
    }
    const compileAnalyticalSmithMark = (context) => {
        const { layer, table, plot, theme } = context;
        const requestedMode = layer.mark.options.mode;
        if ((requestedMode === undefined || requestedMode === 'line' || requestedMode === 'scatter') &&
            layer.mark.options.referenceImpedance === undefined &&
            layer.mark.options.grid === undefined) {
            return compileSmithMark(context);
        }
        const realField = layer.mark.fields.real ?? layer.x.field;
        const imaginaryField = layer.mark.fields.imaginary ?? layer.y.field;
        const idField = layer.mark.fields.id;
        const input = [];
        const sourceRows = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const real = numericDataValue(table.value(rowIndex, realField));
            const imaginary = numericDataValue(table.value(rowIndex, imaginaryField));
            if (real === null || imaginary === null)
                continue;
            input.push({
                real,
                imaginary,
                id: idField === undefined
                    ? `smith-${rowIndex}`
                    : (stringValue$1(table.value(rowIndex, idField)) ?? `smith-${rowIndex}`),
            });
            sourceRows.push(rowIndex);
        }
        if (input.length === 0)
            return [];
        const mode = optionChoice(layer.mark.options, 'mode', ['reflection', 'z', 'y', 's'], 'reflection');
        const result = projectSmith(input, {
            mode,
            referenceImpedance: optionNumber$3(layer.mark.options, 'referenceImpedance', 50),
            grid: optionChoice(layer.mark.options, 'grid', ['impedance', 'admittance', 'combined'], 'impedance'),
        });
        const cx = plot.x + plot.width / 2;
        const cy = plot.y + plot.height / 2;
        const radius = Math.max(18, Math.min(plot.width, plot.height) * 0.45);
        const nodes = [
            {
                type: 'path',
                ...nodeBase(`${layer.id}:analytic-smith-frame`, { zIndex: layer.zIndex }),
                points: circlePath(cx, cy, radius),
                closed: true,
                fill: colorWithOpacity(theme.colors.surface, 0.18),
                stroke: layer.mark.stroke ?? theme.colors.axis,
                lineWidth: layer.mark.lineWidth ?? 1.5,
                lineJoin: 'round',
            },
            {
                type: 'line',
                ...nodeBase(`${layer.id}:analytic-smith-axis`, { zIndex: layer.zIndex }),
                x1: cx - radius,
                y1: cy,
                x2: cx + radius,
                y2: cy,
                stroke: theme.colors.grid,
                lineWidth: 1,
            },
        ];
        const labelSize = Math.max(8, theme.typography.fontSize - 2);
        const gridKinds = [
            ...(result.grid === 'impedance' || result.grid === 'combined'
                ? [
                    {
                        id: 'impedance',
                        admittance: false,
                        radial: result.labels.resistance,
                        curved: result.labels.reactance,
                    },
                ]
                : []),
            ...(result.grid === 'admittance' || result.grid === 'combined'
                ? [
                    {
                        id: 'admittance',
                        admittance: true,
                        radial: result.labels.conductance,
                        curved: result.labels.susceptance,
                    },
                ]
                : []),
        ];
        gridKinds.forEach(({ id, admittance, radial, curved }) => {
            const dash = admittance && result.grid === 'combined' ? [3, 3] : undefined;
            radial.slice(1).forEach(({ value, label }, index) => {
                const center = value / (1 + value);
                const normalizedRadius = 1 / (1 + value);
                const sign = admittance ? -1 : 1;
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:analytic-smith-grid:${id}:radial:${index}`, {
                        zIndex: layer.zIndex,
                    }),
                    points: circlePath(cx + sign * center * radius, cy, normalizedRadius * radius, 48),
                    closed: true,
                    stroke: colorWithOpacity(theme.colors.grid, admittance ? 0.48 : 0.62),
                    lineWidth: 1,
                    ...(dash === undefined ? {} : { dash }),
                });
                const axisPosition = smithPixel(plot, radius, {
                    x: sign * ((value - 1) / (value + 1)),
                    y: 0,
                });
                nodes.push(textNode$5(context, `${layer.id}:analytic-smith-label:${id}:radial:${index}`, axisPosition.x, axisPosition.y + (admittance ? -8 : 8), label, { fill: theme.colors.mutedText, size: labelSize }));
            });
            curved.forEach(({ value, label }, index) => {
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:analytic-smith-grid:${id}:curved:${index}`, {
                        zIndex: layer.zIndex,
                    }),
                    points: smithReactancePath(plot, radius, value, admittance),
                    closed: false,
                    stroke: colorWithOpacity(theme.colors.grid, admittance ? 0.48 : 0.62),
                    lineWidth: 1,
                    ...(dash === undefined ? {} : { dash }),
                    lineCap: 'round',
                    lineJoin: 'round',
                });
                const edge = smithReflection(0, value);
                const labelPosition = smithPixel(plot, radius, {
                    x: admittance ? -edge.x : edge.x,
                    y: admittance ? -edge.y : edge.y,
                });
                nodes.push(textNode$5(context, `${layer.id}:analytic-smith-label:${id}:curved:${index}`, labelPosition.x, labelPosition.y, label, { fill: theme.colors.mutedText, size: labelSize }));
            });
        });
        const seriesField = layer.mark.fields.series ?? layer.mark.fields.group;
        const grouped = new Map();
        result.points.forEach((point, pointIndex) => {
            const sourceRow = sourceRows[pointIndex];
            const series = seriesField === undefined
                ? 'Series'
                : (stringValue$1(table.value(sourceRow, seriesField)) ?? 'Series');
            const entries = grouped.get(series) ?? [];
            entries.push({ sourceRow, pointIndex, pixel: smithPixel(plot, radius, point) });
            grouped.set(series, entries);
        });
        const retainedPointIndices = new Set(exactStrideSampleIndices(result.points.length, Math.min(result.points.length, context.performance.maxPointMarks)));
        const lineGroups = [...grouped].filter(([, entries]) => entries.length > 1);
        const retainedLineGroupKeys = new Set(exactStrideSampleIndices(lineGroups.length, Math.min(lineGroups.length, Math.max(1, Math.floor(context.performance.maxLinePoints / 2)))).map((index) => lineGroups[index][0]));
        [...grouped].forEach(([series, entries], seriesIndex) => {
            const color = layer.mark.stroke ?? palette$1(context, seriesIndex, grouped.size);
            if (entries.length > 1 && retainedLineGroupKeys.has(series)) {
                const lineBudget = Math.max(2, Math.floor(context.performance.maxLinePoints / retainedLineGroupKeys.size));
                const lineEntries = exactStrideSampleIndices(entries.length, Math.min(entries.length, lineBudget)).map((index) => entries[index]);
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:analytic-smith-series:${seriesIndex}`, {
                        zIndex: layer.zIndex + 1,
                    }),
                    points: lineEntries.map(({ pixel }) => pixel),
                    closed: false,
                    stroke: color,
                    lineWidth: layer.mark.lineWidth ?? 1.8,
                    lineCap: theme.mark.lineCap ?? 'round',
                    lineJoin: theme.mark.lineJoin ?? 'round',
                });
            }
            entries.forEach(({ sourceRow, pointIndex, pixel }) => {
                if (!retainedPointIndices.has(pointIndex))
                    return;
                const point = result.points[pointIndex];
                nodes.push({
                    type: 'circle',
                    ...datumNode(context, `${layer.id}:analytic-smith-point:${sourceRow}`, sourceRow, {
                        analyticsFamily: 'smith',
                        analyticsMode: result.mode,
                        id: point.id,
                        series,
                        input: [point.input.real, point.input.imaginary],
                        normalized: point.normalized === null ? null : [point.normalized.real, point.normalized.imaginary],
                        openCircuit: point.openCircuit,
                        reflection: [point.reflection.real, point.reflection.imaginary],
                        magnitude: point.magnitude,
                        phase: point.phase,
                        referenceImpedance: result.referenceImpedance,
                        grid: result.grid,
                        detail: point.tooltip,
                        selectionKey: `smith:${point.id}`,
                    }),
                    cx: pixel.x,
                    cy: pixel.y,
                    radius: layer.mark.radius ?? 4,
                    fill: layer.mark.fill ?? color,
                    stroke: theme.colors.background,
                    lineWidth: 1.2,
                });
            });
        });
        return nodes;
    };
    function linkedScatterMatrixBrush(value) {
        const brush = object$2(value);
        if (brush === null || typeof brush.xField !== 'string' || typeof brush.yField !== 'string') {
            return null;
        }
        const x = optionNumbers$1(brush.x);
        const y = optionNumbers$1(brush.y);
        const selectedRows = optionNumbers$1(brush.selectedRows).filter((rowIndex) => Number.isInteger(rowIndex) && rowIndex >= 0);
        if (x.length !== 2 || y.length !== 2)
            return null;
        return {
            xField: brush.xField,
            yField: brush.yField,
            x: [Math.min(x[0], x[1]), Math.max(x[0], x[1])],
            y: [Math.min(y[0], y[1]), Math.max(y[0], y[1])],
            selectedRows: new Set(selectedRows),
            selectedRowsAuthored: Array.isArray(brush.selectedRows),
        };
    }
    function extent(values) {
        if (values.length === 0)
            return [0, 1];
        const minimum = Math.min(...values);
        const maximum = Math.max(...values);
        return minimum === maximum ? [minimum - 0.5, maximum + 0.5] : [minimum, maximum];
    }
    function matrixValue(dimension, value, start, span) {
        return (start +
            ((value - dimension.minimum) /
                Math.max(Number.EPSILON, dimension.maximum - dimension.minimum)) *
                span);
    }
    function pearson(x, y) {
        const rows = [...x.values.keys()].filter((row) => y.values.has(row));
        if (rows.length < 2)
            return { value: null, rows };
        const xMean = rows.reduce((sum, row) => sum + x.values.get(row), 0) / rows.length;
        const yMean = rows.reduce((sum, row) => sum + y.values.get(row), 0) / rows.length;
        let numerator = 0;
        let xSquare = 0;
        let ySquare = 0;
        rows.forEach((row) => {
            const dx = x.values.get(row) - xMean;
            const dy = y.values.get(row) - yMean;
            numerator += dx * dy;
            xSquare += dx * dx;
            ySquare += dy * dy;
        });
        const denominator = Math.sqrt(xSquare * ySquare);
        return { value: denominator === 0 ? null : numerator / denominator, rows };
    }
    function hexagon(cx, cy, radius) {
        return Array.from({ length: 6 }, (_, index) => {
            const angle = Math.PI / 6 + (index * Math.PI) / 3;
            return { x: cx + Math.cos(angle) * radius, y: cy + Math.sin(angle) * radius };
        });
    }
    /** Compiles the helper plan into diagonal, upper, and lower cells with one linked row identity. */
    const compileAnalyticalScatterMatrixMark = (context) => {
        const { layer, table, plot, theme } = context;
        if (!['variables', 'diagonal', 'upper', 'lower', 'hexColumns', 'linkedBrush'].some((option) => layer.mark.options[option] !== undefined)) {
            return compileScatterMatrixMark(context);
        }
        const requested = optionStrings$1(layer.mark.options.variables);
        const dimensions = optionStrings$1(layer.mark.options.dimensions);
        const automatic = [layer.x.field, layer.y.field, ...Object.values(layer.mark.fields)].filter((field, index, all) => all.indexOf(field) === index && table.has(field));
        const variables = (requested.length > 0 ? requested : dimensions.length > 0 ? dimensions : automatic).slice(0, 8);
        const rows = Array.from({ length: table.length }, (_, index) => table.row(index));
        const diagonal = optionChoice(layer.mark.options, 'diagonal', ['histogram', 'kde', 'ecdf'], 'kde');
        const upper = optionChoice(layer.mark.options, 'upper', ['scatter', 'hexbin', 'correlation', 'none'], 'scatter');
        const lower = optionChoice(layer.mark.options, 'lower', ['scatter', 'hexbin', 'correlation', 'none'], 'scatter');
        const plan = scatterMatrixPlan(rows, {
            ...(variables.length > 0 ? { variables } : {}),
            diagonal,
            upper,
            lower,
        });
        const resolved = plan.variables.flatMap((field) => {
            if (!table.has(field))
                return [];
            const values = new Map();
            for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
                const value = numericDataValue(table.value(rowIndex, field));
                if (value !== null)
                    values.set(rowIndex, value);
            }
            if (values.size === 0)
                return [];
            const [minimum, maximum] = extent([...values.values()]);
            return [{ field, minimum, maximum, values }];
        });
        if (resolved.length < 2)
            return [];
        const byField = new Map(resolved.map((dimension) => [dimension.field, dimension]));
        const cellCount = resolved.length;
        const cellWidth = plot.width / cellCount;
        const cellHeight = plot.height / cellCount;
        const gap = clamp$8(optionNumber$3(layer.mark.options, 'gap', 5), 0, 18);
        const linkedBrush = linkedScatterMatrixBrush(layer.mark.options.linkedBrush);
        const linkedBrushRows = new Set();
        if (linkedBrush !== null) {
            if (linkedBrush.selectedRowsAuthored) {
                linkedBrush.selectedRows.forEach((rowIndex) => linkedBrushRows.add(rowIndex));
            }
            else {
                const brushX = byField.get(linkedBrush.xField);
                const brushY = byField.get(linkedBrush.yField);
                if (brushX !== undefined && brushY !== undefined) {
                    for (const [rowIndex, xValue] of brushX.values) {
                        const yValue = brushY.values.get(rowIndex);
                        if (yValue !== undefined &&
                            xValue >= linkedBrush.x[0] &&
                            xValue <= linkedBrush.x[1] &&
                            yValue >= linkedBrush.y[0] &&
                            yValue <= linkedBrush.y[1]) {
                            linkedBrushRows.add(rowIndex);
                        }
                    }
                }
            }
        }
        const linkedSelected = (rowIndex) => linkedBrush === null || linkedBrushRows.has(rowIndex);
        const nodes = [];
        const maximumPoints = Math.max(1, context.performance.maxPointMarks);
        const drawableCells = Math.max(1, plan.cells.filter(({ kind }) => kind === 'scatter' || kind === 'hexbin').length);
        const perCellBudget = Math.max(1, Math.floor(maximumPoints / drawableCells));
        for (const cell of plan.cells) {
            if (!byField.has(cell.x) || !byField.has(cell.y))
                continue;
            const xDimension = byField.get(cell.x);
            const yDimension = byField.get(cell.y);
            const matrixRow = resolved.findIndex(({ field }) => field === cell.y);
            const matrixColumn = resolved.findIndex(({ field }) => field === cell.x);
            const x = plot.x + matrixColumn * cellWidth + gap / 2;
            const y = plot.y + matrixRow * cellHeight + gap / 2;
            const width = Math.max(1, cellWidth - gap);
            const height = Math.max(1, cellHeight - gap);
            const cellSourceRow = [...xDimension.values.keys()].find((rowIndex) => yDimension.values.has(rowIndex)) ?? 0;
            nodes.push({
                type: 'rect',
                ...datumNode(context, `${layer.id}:analytic-scatter-matrix-cell:${matrixRow}:${matrixColumn}`, cellSourceRow, {
                    analyticsFamily: 'scatter-matrix',
                    cellKind: 'interaction-surface',
                    matrixX: cell.x,
                    matrixY: cell.y,
                    xDomain: [xDimension.minimum, xDimension.maximum],
                    yDomain: [yDimension.minimum, yDimension.maximum],
                    linkedSelectionKey: cell.linkedSelectionKey,
                }, 0, {
                    kind: 'scatter-matrix-cell',
                    xField: cell.x,
                    yField: cell.y,
                    row: matrixRow,
                    column: matrixColumn,
                    plot: { x, y, width, height },
                    xDomain: [xDimension.minimum, xDimension.maximum],
                    yDomain: [yDimension.minimum, yDimension.maximum],
                }),
                x,
                y,
                width,
                height,
                fill: colorWithOpacity(theme.colors.surface, 0.56),
                stroke: theme.colors.grid,
                lineWidth: 1,
                cornerRadius: 2,
            });
            if (linkedBrush !== null && cell.x === linkedBrush.xField && cell.y === linkedBrush.yField) {
                const brushX0 = matrixValue(xDimension, linkedBrush.x[0], x, width);
                const brushX1 = matrixValue(xDimension, linkedBrush.x[1], x, width);
                const brushY0 = y + height - matrixValue(yDimension, linkedBrush.y[0], 0, height);
                const brushY1 = y + height - matrixValue(yDimension, linkedBrush.y[1], 0, height);
                nodes.push({
                    type: 'rect',
                    ...nodeBase(`${layer.id}:analytic-scatter-matrix-linked-brush`, {
                        zIndex: layer.zIndex + 3,
                        opacity: 0.2,
                    }),
                    x: Math.min(brushX0, brushX1),
                    y: Math.min(brushY0, brushY1),
                    width: Math.max(1, Math.abs(brushX1 - brushX0)),
                    height: Math.max(1, Math.abs(brushY1 - brushY0)),
                    fill: theme.colors.focus,
                    stroke: theme.colors.focus,
                    lineWidth: 1.5,
                    cornerRadius: 1,
                });
            }
            if (cell.kind === 'none')
                continue;
            if (matrixRow === matrixColumn) {
                const observations = [...xDimension.values].map(([rowIndex, value]) => ({
                    value,
                    rowIndex,
                }));
                const bins = sharedHistogramBins([observations], clamp$8(Math.floor(optionNumber$3(layer.mark.options, 'bins', 10)), 2, 32));
                const maximum = Math.max(1, ...bins.map(({ counts }) => counts[0] ?? 0));
                if (cell.kind === 'histogram') {
                    bins.forEach((bin, binIndex) => {
                        const sourceRows = observations
                            .filter(({ value }) => binIndex === bins.length - 1
                            ? value >= bin.start && value <= bin.end
                            : value >= bin.start && value < bin.end)
                            .map(({ rowIndex }) => rowIndex);
                        const count = bin.counts[0] ?? 0;
                        const selectedCount = sourceRows.filter(linkedSelected).length;
                        const base = sourceRows.length === 0
                            ? nodeBase(`${layer.id}:analytic-scatter-matrix-hist:${matrixRow}:${binIndex}`, {
                                zIndex: layer.zIndex + 1,
                            })
                            : datumNode(context, `${layer.id}:analytic-scatter-matrix-hist:${matrixRow}:${binIndex}`, sourceRows[0], {
                                analyticsFamily: 'scatter-matrix',
                                cellKind: 'histogram',
                                matrixX: cell.x,
                                matrixY: cell.y,
                                binStart: bin.start,
                                binEnd: bin.end,
                                count,
                                sourceRowIndices: sourceRows.slice(0, 256),
                                linkedSelectedRows: selectedCount,
                                linkedSelectionKey: cell.linkedSelectionKey,
                                selectionKey: `scatter-matrix:${cell.x}:${binIndex}`,
                            });
                        nodes.push({
                            type: 'rect',
                            ...base,
                            x: x + (binIndex / bins.length) * width,
                            y: y + height - (count / maximum) * height * 0.72,
                            width: Math.max(1, width / bins.length - 1),
                            height: Math.max(1, (count / maximum) * height * 0.72),
                            fill: colorWithOpacity(context.color, linkedBrush === null || selectedCount > 0 ? 0.68 : 0.1),
                            lineWidth: 0,
                            cornerRadius: 1,
                        });
                    });
                }
                else {
                    const weighted = observations.map(({ value, rowIndex }) => ({
                        value,
                        weight: 1,
                        rowIndex,
                    }));
                    const lineBudget = clamp$8(Math.floor(context.performance.maxLinePoints / Math.max(1, cellCount)), 2, 256);
                    let bandwidth = null;
                    let points;
                    if (cell.kind === 'ecdf') {
                        const empirical = empiricalDistribution(weighted);
                        const retained = exactStrideSampleIndices(empirical.length, Math.min(lineBudget, empirical.length)).map((index) => empirical[index]);
                        points = retained.map((point) => ({
                            x: matrixValue(xDimension, point.value, x, width),
                            y: y + height - point.probability * height * 0.72,
                        }));
                    }
                    else {
                        const density = kernelDensity1d(weighted, {
                            points: lineBudget,
                            ...(typeof layer.mark.options.bandwidth === 'number'
                                ? { bandwidth: layer.mark.options.bandwidth }
                                : {}),
                        });
                        bandwidth = density.bandwidth;
                        const maximumDensity = Math.max(1e-12, ...density.points.map(({ density }) => density));
                        const [densityMinimum, densityMaximum] = extent(density.points.map(({ value }) => value));
                        points = density.points.map((point) => ({
                            x: x +
                                ((point.value - densityMinimum) /
                                    Math.max(Number.EPSILON, densityMaximum - densityMinimum)) *
                                    width,
                            y: y + height - (point.density / maximumDensity) * height * 0.72,
                        }));
                    }
                    if (points.length > 1) {
                        const sourceRow = observations[0]?.rowIndex;
                        const linkedSelectedRows = observations.filter(({ rowIndex }) => linkedSelected(rowIndex)).length;
                        const id = `${layer.id}:analytic-scatter-matrix-${cell.kind}:${matrixRow}`;
                        const base = sourceRow === undefined
                            ? nodeBase(id, { zIndex: layer.zIndex + 1 })
                            : datumNode(context, id, sourceRow, {
                                analyticsFamily: 'scatter-matrix',
                                cellKind: cell.kind,
                                matrixX: cell.x,
                                matrixY: cell.y,
                                sourceRowCount: observations.length,
                                linkedSelectedRows,
                                ...(bandwidth === null ? {} : { bandwidth }),
                                linkedSelectionKey: cell.linkedSelectionKey,
                                selectionKey: `scatter-matrix:${cell.x}:distribution`,
                            });
                        nodes.push({
                            type: 'path',
                            ...base,
                            points,
                            closed: false,
                            stroke: colorWithOpacity(layer.mark.stroke ?? context.color, linkedBrush === null || linkedSelectedRows > 0 ? 1 : 0.14),
                            lineWidth: layer.mark.lineWidth ?? 1.6,
                            lineCap: theme.mark.lineCap ?? 'round',
                            lineJoin: theme.mark.lineJoin ?? 'round',
                        });
                    }
                }
                nodes.push(textNode$5(context, `${layer.id}:analytic-scatter-matrix-label:${matrixRow}`, x + width / 2, y + 11, cell.x, { fill: theme.colors.mutedText, size: Math.max(8, theme.typography.fontSize - 2) }));
                continue;
            }
            const pairedRows = [...xDimension.values.keys()].filter((rowIndex) => yDimension.values.has(rowIndex));
            if (cell.kind === 'correlation') {
                const correlation = pearson(xDimension, yDimension);
                const sourceRow = correlation.rows[0];
                if (sourceRow !== undefined) {
                    nodes.push({
                        type: 'rect',
                        ...datumNode(context, `${layer.id}:analytic-scatter-matrix-correlation-hit:${matrixRow}:${matrixColumn}`, sourceRow, {
                            analyticsFamily: 'scatter-matrix',
                            cellKind: 'correlation',
                            matrixX: cell.x,
                            matrixY: cell.y,
                            correlation: correlation.value,
                            sourceRowCount: correlation.rows.length,
                            sourceRowIndices: correlation.rows.slice(0, 256),
                            linkedSelectedRows: correlation.rows.filter(linkedSelected).length,
                            linkedSelectionKey: cell.linkedSelectionKey,
                            selectionKey: `scatter-matrix:${cell.x}:${cell.y}:correlation`,
                        }, 1),
                        x: x + 1,
                        y: y + 1,
                        width: Math.max(1, width - 2),
                        height: Math.max(1, height - 2),
                        fill: colorWithOpacity(context.color, 0.06),
                        lineWidth: 0,
                        cornerRadius: 2,
                    });
                }
                nodes.push(textNode$5(context, `${layer.id}:analytic-scatter-matrix-correlation:${matrixRow}:${matrixColumn}`, x + width / 2, y + height / 2, correlation.value === null ? 'n/a' : correlation.value.toFixed(2), {
                    fill: theme.colors.text,
                    size: Math.max(10, Math.min(22, Math.min(width, height) * 0.22)),
                    weight: 700,
                }));
                continue;
            }
            if (cell.kind === 'hexbin') {
                const columns = clamp$8(Math.floor(optionNumber$3(layer.mark.options, 'hexColumns', 7)), 2, 24);
                const rowsCount = Math.max(2, Math.round((columns * height) / Math.max(1, width)));
                const buckets = new Map();
                pairedRows.forEach((rowIndex) => {
                    const column = clamp$8(Math.floor(((xDimension.values.get(rowIndex) - xDimension.minimum) /
                        Math.max(Number.EPSILON, xDimension.maximum - xDimension.minimum)) *
                        columns), 0, columns - 1);
                    const row = clamp$8(Math.floor(((yDimension.values.get(rowIndex) - yDimension.minimum) /
                        Math.max(Number.EPSILON, yDimension.maximum - yDimension.minimum)) *
                        rowsCount), 0, rowsCount - 1);
                    const key = `${row}:${column}`;
                    const bucket = buckets.get(key) ?? [];
                    bucket.push(rowIndex);
                    buckets.set(key, bucket);
                });
                const maximum = Math.max(1, ...[...buckets.values()].map((bucket) => bucket.length));
                const selectedBuckets = exactStrideSampleIndices(buckets.size, Math.min(perCellBudget, buckets.size)).map((index) => [...buckets][index]);
                selectedBuckets.forEach(([key, sourceRows]) => {
                    const [row, column] = key.split(':').map(Number);
                    const cx = x + ((column + 0.5) / columns) * width;
                    const cy = y + height - ((row + 0.5) / rowsCount) * height;
                    const radius = Math.max(2, Math.min(width / columns, height / rowsCount) * 0.52);
                    const selectedCount = sourceRows.filter(linkedSelected).length;
                    nodes.push({
                        type: 'path',
                        ...datumNode(context, `${layer.id}:analytic-scatter-matrix-hexbin:${matrixRow}:${matrixColumn}:${row}:${column}`, sourceRows[0], {
                            analyticsFamily: 'scatter-matrix',
                            cellKind: 'hexbin',
                            matrixX: cell.x,
                            matrixY: cell.y,
                            count: sourceRows.length,
                            sourceRowIndices: sourceRows.slice(0, 256),
                            linkedSelectedRows: selectedCount,
                            linkedSelectionKey: cell.linkedSelectionKey,
                            selectionKey: `scatter-matrix:${sourceRows[0]}`,
                        }),
                        points: hexagon(cx, cy, radius),
                        closed: true,
                        fill: colorWithOpacity(mappedContinuousColor(theme, sourceRows.length / maximum), linkedBrush === null || selectedCount > 0 ? 1 : 0.12),
                        stroke: theme.colors.background,
                        lineWidth: 0.8,
                        lineJoin: 'round',
                    });
                });
                continue;
            }
            const sampled = exactStrideSampleIndices(pairedRows.length, Math.min(perCellBudget, pairedRows.length)).map((index) => pairedRows[index]);
            sampled.forEach((rowIndex) => {
                const xValue = xDimension.values.get(rowIndex);
                const yValue = yDimension.values.get(rowIndex);
                const selected = linkedSelected(rowIndex);
                nodes.push({
                    type: 'circle',
                    ...datumNode(context, `${layer.id}:analytic-scatter-matrix-point:${matrixRow}:${matrixColumn}:${rowIndex}`, rowIndex, {
                        analyticsFamily: 'scatter-matrix',
                        cellKind: 'scatter',
                        matrixX: cell.x,
                        matrixY: cell.y,
                        x: xValue,
                        y: yValue,
                        linkedSelected: selected,
                        linkedSelectionKey: cell.linkedSelectionKey,
                        selectionKey: `scatter-matrix:${rowIndex}`,
                    }),
                    cx: matrixValue(xDimension, xValue, x + 3, width - 6),
                    cy: y + height - matrixValue(yDimension, yValue, 3, height - 6),
                    radius: clamp$8((layer.mark.radius ?? 2.2) + (linkedBrush !== null && selected ? 1 : 0), 1, 7),
                    fill: colorWithOpacity(layer.mark.fill ?? context.color, linkedBrush === null ? 0.72 : selected ? 0.94 : 0.1),
                    stroke: theme.colors.background,
                    lineWidth: 0.5,
                });
            });
        }
        return nodes;
    };
    function numericVector(value, path) {
        if (!Array.isArray(value) || value.some((entry) => typeof entry !== 'number')) {
            throw new GraflumeError('INVALID_SPEC', `${path} must be a numeric array.`, { path });
        }
        return value.map((entry, index) => {
            if (!Number.isFinite(entry)) {
                throw new GraflumeError('INVALID_SPEC', `${path}[${index}] must be finite.`, {
                    path: `${path}[${index}]`,
                });
            }
            return entry;
        });
    }
    function numericMatrix(value, path) {
        if (!Array.isArray(value)) {
            throw new GraflumeError('INVALID_SPEC', `${path} must be a numeric matrix.`, { path });
        }
        return value.map((row, index) => numericVector(row, `${path}[${index}]`));
    }
    function maskMatrix(value, path) {
        if (value === undefined)
            return undefined;
        if (!Array.isArray(value) ||
            value.some((row) => !Array.isArray(row) || row.some((entry) => typeof entry !== 'boolean'))) {
            throw new GraflumeError('INVALID_SPEC', `${path} must be a boolean matrix.`, { path });
        }
        return value.map((row) => [...row]);
    }
    function carpetGrid(value) {
        const grid = object$2(value);
        if (grid === null)
            return null;
        const a = numericVector(grid.a, '$.mark.options.grid.a');
        const b = numericVector(grid.b, '$.mark.options.grid.b');
        const x = numericMatrix(grid.x, '$.mark.options.grid.x');
        const y = numericMatrix(grid.y, '$.mark.options.grid.y');
        const mask = maskMatrix(grid.mask, '$.mark.options.grid.mask');
        if (mask !== undefined &&
            (mask.length !== b.length || mask.some((row) => row.length !== a.length))) {
            throw new GraflumeError('INVALID_SPEC', 'Carpet mask dimensions must be b by a.', {
                path: '$.mark.options.grid.mask',
            });
        }
        return { a, b, x, y, ...(mask === undefined ? {} : { mask }) };
    }
    /** Compiles an authored irregular logical grid, its masked cells, and projected interactive points. */
    const compileAnalyticalCarpetMark = (context) => {
        const { layer, table, plot, theme } = context;
        const grid = carpetGrid(layer.mark.options.grid);
        if (grid === null)
            return compileCarpetMark(context);
        const aField = layer.mark.fields.a ?? layer.x.field;
        const bField = layer.mark.fields.b ?? layer.y.field;
        const idField = layer.mark.fields.id;
        const input = [];
        const sourceRows = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const a = numericDataValue(table.value(rowIndex, aField));
            const b = numericDataValue(table.value(rowIndex, bField));
            if (a === null || b === null)
                continue;
            input.push({
                a,
                b,
                id: idField === undefined
                    ? `carpet-${rowIndex}`
                    : (stringValue$1(table.value(rowIndex, idField)) ?? `carpet-${rowIndex}`),
            });
            sourceRows.push(rowIndex);
        }
        const projected = projectCarpet(grid, input);
        const xValues = [...grid.x.flat(), ...projected.points.map(({ projected: point }) => point.x)];
        const yValues = [...grid.y.flat(), ...projected.points.map(({ projected: point }) => point.y)];
        const [xMinimum, xMaximum] = extent(xValues);
        const [yMinimum, yMaximum] = extent(yValues);
        const pixel = (point) => ({
            x: plot.x + ((point.x - xMinimum) / Math.max(Number.EPSILON, xMaximum - xMinimum)) * plot.width,
            y: plot.y +
                plot.height -
                ((point.y - yMinimum) / Math.max(Number.EPSILON, yMaximum - yMinimum)) * plot.height,
        });
        const nodes = [];
        const bLineBudget = Math.floor(context.performance.maxLinePoints / 2);
        const aLineBudget = context.performance.maxLinePoints - bLineBudget;
        const bLineIndices = exactStrideSampleIndices(grid.b.length, Math.min(grid.b.length, Math.max(1, Math.floor(bLineBudget / 2))));
        const aLineIndices = exactStrideSampleIndices(grid.a.length, Math.min(grid.a.length, Math.max(1, Math.floor(aLineBudget / 2))));
        const bLinePointBudget = Math.max(2, Math.floor(bLineBudget / Math.max(1, bLineIndices.length)));
        const aLinePointBudget = Math.max(2, Math.floor(aLineBudget / Math.max(1, aLineIndices.length)));
        for (const bIndex of bLineIndices) {
            const points = exactStrideSampleIndices(grid.a.length, Math.min(grid.a.length, bLinePointBudget)).map((aIndex) => pixel({ x: grid.x[bIndex][aIndex], y: grid.y[bIndex][aIndex] }));
            if (points.length > 1) {
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:analytic-carpet-grid:b:${bIndex}`, { zIndex: layer.zIndex }),
                    points,
                    closed: false,
                    stroke: colorWithOpacity(theme.colors.axis, 0.82),
                    lineWidth: 1.3,
                    lineCap: theme.mark.lineCap ?? 'round',
                    lineJoin: theme.mark.lineJoin ?? 'round',
                });
            }
        }
        for (const aIndex of aLineIndices) {
            const points = exactStrideSampleIndices(grid.b.length, Math.min(grid.b.length, aLinePointBudget)).map((bIndex) => pixel({ x: grid.x[bIndex][aIndex], y: grid.y[bIndex][aIndex] }));
            if (points.length > 1) {
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:analytic-carpet-grid:a:${aIndex}`, { zIndex: layer.zIndex }),
                    points,
                    closed: false,
                    stroke: colorWithOpacity(theme.colors.grid, 0.9),
                    lineWidth: 1,
                    lineCap: theme.mark.lineCap ?? 'round',
                    lineJoin: theme.mark.lineJoin ?? 'round',
                });
            }
        }
        const carpetLabelSize = Math.max(8, theme.typography.fontSize - 2);
        aLineIndices.forEach((aIndex) => {
            const position = pixel({ x: grid.x[0][aIndex], y: grid.y[0][aIndex] });
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:analytic-carpet-tick-line:a:${aIndex}`, {
                    zIndex: layer.zIndex + 1,
                }),
                x1: position.x,
                y1: position.y,
                x2: position.x,
                y2: position.y + 4,
                stroke: theme.colors.axis,
                lineWidth: 1,
            }, textNode$5(context, `${layer.id}:analytic-carpet-tick-label:a:${aIndex}`, position.x, position.y + 11, projected.ticks.a[aIndex].label, { fill: theme.colors.mutedText, size: carpetLabelSize, baseline: 'top' }));
        });
        bLineIndices.forEach((bIndex) => {
            const position = pixel({ x: grid.x[bIndex][0], y: grid.y[bIndex][0] });
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:analytic-carpet-tick-line:b:${bIndex}`, {
                    zIndex: layer.zIndex + 1,
                }),
                x1: position.x,
                y1: position.y,
                x2: position.x - 4,
                y2: position.y,
                stroke: theme.colors.axis,
                lineWidth: 1,
            }, textNode$5(context, `${layer.id}:analytic-carpet-tick-label:b:${bIndex}`, position.x - 7, position.y, projected.ticks.b[bIndex].label, { fill: theme.colors.mutedText, size: carpetLabelSize, align: 'right' }));
        });
        if (aLineIndices.length > 0) {
            const aStart = pixel({ x: grid.x[0][aLineIndices[0]], y: grid.y[0][aLineIndices[0]] });
            const aEnd = pixel({
                x: grid.x[0][aLineIndices.at(-1)],
                y: grid.y[0][aLineIndices.at(-1)],
            });
            nodes.push(textNode$5(context, `${layer.id}:analytic-carpet-axis-label:a`, (aStart.x + aEnd.x) / 2, Math.max(aStart.y, aEnd.y) + 24, aField, { fill: theme.colors.mutedText, size: carpetLabelSize, weight: 700 }));
        }
        if (bLineIndices.length > 0) {
            const bStart = pixel({ x: grid.x[bLineIndices[0]][0], y: grid.y[bLineIndices[0]][0] });
            const bEnd = pixel({
                x: grid.x[bLineIndices.at(-1)][0],
                y: grid.y[bLineIndices.at(-1)][0],
            });
            nodes.push(textNode$5(context, `${layer.id}:analytic-carpet-axis-label:b`, Math.min(bStart.x, bEnd.x) - 18, (bStart.y + bEnd.y) / 2, bField, { fill: theme.colors.mutedText, size: carpetLabelSize, weight: 700 }));
        }
        const maskedCells = [];
        for (let bIndex = 0; bIndex < grid.b.length - 1; bIndex += 1) {
            for (let aIndex = 0; aIndex < grid.a.length - 1; aIndex += 1) {
                const masked = [
                    grid.mask?.[bIndex]?.[aIndex],
                    grid.mask?.[bIndex]?.[aIndex + 1],
                    grid.mask?.[bIndex + 1]?.[aIndex + 1],
                    grid.mask?.[bIndex + 1]?.[aIndex],
                ].some((entry) => entry === false);
                if (!masked)
                    continue;
                maskedCells.push([bIndex, aIndex]);
            }
        }
        const retainedMasks = exactStrideSampleIndices(maskedCells.length, Math.min(maskedCells.length, context.performance.maxBarMarks)).map((index) => maskedCells[index]);
        retainedMasks.forEach(([bIndex, aIndex]) => {
            nodes.push({
                type: 'path',
                ...nodeBase(`${layer.id}:analytic-carpet-mask:${bIndex}:${aIndex}`, {
                    zIndex: layer.zIndex + 1,
                }),
                points: [
                    pixel({ x: grid.x[bIndex][aIndex], y: grid.y[bIndex][aIndex] }),
                    pixel({ x: grid.x[bIndex][aIndex + 1], y: grid.y[bIndex][aIndex + 1] }),
                    pixel({
                        x: grid.x[bIndex + 1][aIndex + 1],
                        y: grid.y[bIndex + 1][aIndex + 1],
                    }),
                    pixel({ x: grid.x[bIndex + 1][aIndex], y: grid.y[bIndex + 1][aIndex] }),
                ],
                closed: true,
                fill: colorWithOpacity(theme.colors.mutedText, 0.16),
                stroke: colorWithOpacity(theme.colors.mutedText, 0.44),
                lineWidth: 1,
                lineJoin: 'round',
            });
        });
        const retainedPoints = exactStrideSampleIndices(projected.points.length, Math.min(projected.points.length, context.performance.maxPointMarks));
        retainedPoints.forEach((pointIndex) => {
            const point = projected.points[pointIndex];
            const sourceRow = sourceRows[pointIndex];
            const position = pixel(point.projected);
            nodes.push({
                type: 'circle',
                ...datumNode(context, `${layer.id}:analytic-carpet-point:${sourceRow}`, sourceRow, {
                    analyticsFamily: 'carpet',
                    analyticsMode: 'projected-grid',
                    id: point.id,
                    logical: [point.logical.a, point.logical.b],
                    projected: [point.projected.x, point.projected.y],
                    masked: point.masked,
                    detail: point.tooltip,
                    selectionKey: `carpet:${point.id}`,
                }),
                cx: position.x,
                cy: position.y,
                radius: layer.mark.radius ?? 4.5,
                fill: layer.mark.fill ?? (point.masked ? theme.colors.mutedText : context.color),
                stroke: theme.colors.background,
                lineWidth: 1.2,
                opacity: point.masked ? Math.min(layer.mark.opacity, 0.45) : layer.mark.opacity,
            });
        });
        return nodes;
    };
    /** Compiles exact waffle/isotype units, including partial units and authored fill direction. */
    const compileAnalyticalItemMark = (context) => {
        const { layer, table, plot, theme } = context;
        if (!['unit', 'mode', 'partial', 'direction', 'columns'].some((option) => layer.mark.options[option] !== undefined)) {
            return compileItemMark(context);
        }
        const groups = [];
        const sourceByGroup = new Map();
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const id = stringValue$1(table.value(rowIndex, layer.x.field));
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            if (id === null || value === null || value < 0)
                continue;
            const existing = groups.find((group) => group.id === id);
            if (existing === undefined) {
                groups.push({ id, value });
                sourceByGroup.set(id, rowIndex);
            }
            else {
                const index = groups.indexOf(existing);
                groups[index] = { id, value: existing.value + value };
            }
        }
        if (groups.length === 0)
            return compileItemMark(context);
        const total = groups.reduce((sum, group) => sum + group.value, 0);
        if (total <= 0)
            return [];
        const mode = optionChoice(layer.mark.options, 'mode', ['waffle', 'isotype'], 'waffle');
        const partial = optionChoice(layer.mark.options, 'partial', ['fraction', 'round', 'floor', 'ceil'], 'fraction');
        const direction = optionChoice(layer.mark.options, 'direction', ['row', 'column', 'row-reverse', 'column-reverse'], 'row');
        const requestedItems = clamp$8(Math.floor(optionNumber$3(layer.mark.options, 'items', 100)), 1, context.performance.maxPointMarks);
        const unit = typeof layer.mark.options.unit === 'number'
            ? optionNumber$3(layer.mark.options, 'unit', 1)
            : total / requestedItems;
        const defaultColumns = Math.max(1, Math.ceil(Math.sqrt((requestedItems * plot.width) / Math.max(1, plot.height))));
        const layout = layoutItems(groups, {
            mode,
            unit,
            columns: Math.floor(optionNumber$3(layer.mark.options, 'columns', defaultColumns)),
            direction,
            partial,
        });
        if (layout.units.length > context.performance.maxPointMarks) {
            throw new GraflumeError('INVALID_SPEC', `Item unit expands to ${layout.units.length} marks, above the ${context.performance.maxPointMarks} mark budget.`, { path: '$.mark.options.unit' });
        }
        const rowCount = Math.max(1, ...layout.units.map(({ row }) => row + 1));
        const columnCount = Math.max(1, ...layout.units.map(({ column }) => column + 1));
        const gap = clamp$8(optionNumber$3(layer.mark.options, 'gap', 2), 0, 12);
        const cellWidth = plot.width / columnCount;
        const cellHeight = plot.height / rowCount;
        const size = Math.max(1, Math.min(cellWidth, cellHeight) - gap);
        const colorByGroup = new Map(groups.map(({ id }, index) => [id, index]));
        return layout.units.map((item) => {
            const rowIndex = sourceByGroup.get(item.group);
            const x = plot.x + item.column * cellWidth + (cellWidth - size) / 2;
            const y = plot.y + plot.height - (item.row + 1) * cellHeight + (cellHeight - size) / 2;
            const color = layer.mark.fill ?? palette$1(context, colorByGroup.get(item.group) ?? 0, groups.length);
            const tooltip = {
                analyticsFamily: 'item',
                analyticsMode: layout.mode,
                group: item.group,
                amount: item.amount,
                fraction: item.fraction,
                unit: layout.unit,
                total: layout.total,
                accessibleLabel: item.accessibleLabel,
                direction,
                selectionKey: `item:${item.group}`,
            };
            if (layout.mode === 'isotype') {
                return {
                    type: 'circle',
                    ...datumNode(context, `${layer.id}:analytic-item:${item.index}`, rowIndex, tooltip),
                    cx: x + size / 2,
                    cy: y + size / 2,
                    radius: (size / 2) * Math.sqrt(item.fraction),
                    fill: color,
                    stroke: theme.colors.background,
                    lineWidth: 0.8,
                };
            }
            return {
                type: 'rect',
                ...datumNode(context, `${layer.id}:analytic-item:${item.index}`, rowIndex, tooltip),
                x,
                y,
                width: Math.max(0.5, size * item.fraction),
                height: size,
                fill: color,
                stroke: theme.colors.background,
                lineWidth: 0.8,
                cornerRadius: layer.mark.cornerRadius ?? 1.5,
            };
        });
    };

    function numberFormatter$1(locale, options) {
        try {
            return new Intl.NumberFormat(locale, options);
        }
        catch {
            return new Intl.NumberFormat(undefined, options);
        }
    }
    function dateFormatter$1(locale, options) {
        try {
            return new Intl.DateTimeFormat(locale, options);
        }
        catch {
            try {
                return new Intl.DateTimeFormat(undefined, options);
            }
            catch {
                const utcOptions = { ...options, timeZone: 'UTC' };
                try {
                    return new Intl.DateTimeFormat(locale, utcOptions);
                }
                catch {
                    try {
                        return new Intl.DateTimeFormat(undefined, utcOptions);
                    }
                    catch {
                        return new Intl.DateTimeFormat(undefined, { dateStyle: 'medium', timeZone: 'UTC' });
                    }
                }
            }
        }
    }
    function finiteFractionDigits$1(value) {
        return value === undefined ? undefined : Math.max(0, Math.min(20, Math.trunc(value)));
    }
    function numericValue$1(value) {
        if (typeof value === 'number')
            return Number.isFinite(value) ? value : null;
        const parsed = Number(value);
        return value.trim() !== '' && Number.isFinite(parsed) ? parsed : null;
    }
    function dateValue(value) {
        if (typeof value === 'number') {
            const date = new Date(value);
            return Number.isFinite(date.getTime()) ? { value: date, dateOnly: false } : null;
        }
        const dateOnly = /^(\d{4})-(\d{2})-(\d{2})$/.exec(value);
        if (dateOnly !== null) {
            const year = Number(dateOnly[1]);
            const month = Number(dateOnly[2]);
            const day = Number(dateOnly[3]);
            const date = new Date(Date.UTC(year, month - 1, day));
            if (date.getUTCFullYear() === year &&
                date.getUTCMonth() === month - 1 &&
                date.getUTCDate() === day) {
                return { value: date, dateOnly: true };
            }
            return null;
        }
        const date = new Date(value);
        return Number.isFinite(date.getTime()) ? { value: date, dateOnly: false } : null;
    }
    function formatNumber(value, format, locale) {
        const fractionDigits = finiteFractionDigits$1(format.fractionDigits);
        const notation = format.type === 'compact'
            ? 'compact'
            : format.type === 'scientific'
                ? 'scientific'
                : format.notation;
        const options = {
            notation,
            useGrouping: format.useGrouping,
            ...(fractionDigits === undefined
                ? format.type === 'integer'
                    ? { maximumFractionDigits: 0 }
                    : format.type === 'percent'
                        ? { maximumFractionDigits: 1 }
                        : { maximumFractionDigits: 6 }
                : { minimumFractionDigits: fractionDigits, maximumFractionDigits: fractionDigits }),
        };
        if (format.type === 'percent')
            options.style = 'percent';
        if (format.type === 'currency') {
            options.style = 'currency';
            options.currency = format.currency ?? 'USD';
            options.currencyDisplay = format.currencyDisplay;
        }
        try {
            return numberFormatter$1(locale, options).format(value);
        }
        catch {
            return numberFormatter$1(locale, {
                useGrouping: format.useGrouping,
                maximumFractionDigits: fractionDigits ?? 6,
            }).format(value);
        }
    }
    function formatDate(parsed, format, locale) {
        const options = {
            timeZone: parsed.dateOnly && format.type === 'date' ? 'UTC' : format.timeZone || 'UTC',
        };
        if (format.type === 'time') {
            options.timeStyle = format.timeStyle;
        }
        else if (format.type === 'datetime') {
            options.dateStyle = format.dateStyle;
            options.timeStyle = format.timeStyle;
        }
        else {
            options.dateStyle = format.dateStyle;
        }
        return dateFormatter$1(locale, options).format(parsed.value);
    }
    /** Format a scale tick without accepting callbacks or executable formatter expressions. */
    function formatAxisTick(tick, format, locale) {
        let value = tick.label;
        if (format.type === 'date' || format.type === 'time' || format.type === 'datetime') {
            const date = dateValue(tick.value);
            if (date !== null)
                value = formatDate(date, format, locale);
        }
        else if (format.type !== 'auto') {
            const numeric = numericValue$1(tick.value);
            if (numeric !== null)
                value = formatNumber(numeric, format, locale);
        }
        return `${format.prefix}${value}${format.suffix}`;
    }
    /** Truncate by Unicode code point so surrogate pairs are never split. */
    function truncateAxisLabel(value, maxLength) {
        if (maxLength === undefined)
            return value;
        const characters = Array.from(value);
        if (characters.length <= maxLength)
            return value;
        if (maxLength <= 1)
            return '…';
        return `${characters.slice(0, maxLength - 1).join('')}…`;
    }

    function position$1(context) {
        const axisChannel = channel(context);
        if (context.axis === false)
            return defaultAxisPosition(context.id, axisChannel);
        const requested = context.axis.position;
        if (axisChannel === 'x') {
            return requested === 'top' || requested === 'bottom'
                ? requested
                : defaultAxisPosition(context.id, axisChannel);
        }
        return requested === 'left' || requested === 'right'
            ? requested
            : defaultAxisPosition(context.id, axisChannel);
    }
    function channel(context) {
        return (context.channel ??
            builtInAxisChannel(context.id) ??
            (context.axis === false ? 'x' : axisPositionChannel(context.axis.position)));
    }
    function mappedFontWeight(weight, fallback) {
        if (typeof weight === 'number')
            return weight;
        switch (weight) {
            case 'normal':
                return 400;
            case 'medium':
                return 500;
            case 'semibold':
                return 600;
            case 'bold':
                return 700;
            default:
                return fallback;
        }
    }
    function resolveTextStyle(font, color, theme, defaultColor, defaultSize, fallbackWeight) {
        return {
            fill: color ?? defaultColor,
            fontFamily: font.family ?? theme.typography.fontFamily,
            fontSize: font.size ?? defaultSize,
            fontWeight: mappedFontWeight(font.weight, fallbackWeight),
            ...(font.style === 'italic' ? { fontStyle: 'italic' } : {}),
        };
    }
    function line$1(id, x1, y1, x2, y2, stroke, lineWidth, zIndex, opacity, dash, lineCap = 'round') {
        return {
            type: 'line',
            ...nodeBase(id, { zIndex, opacity }),
            x1,
            y1,
            x2,
            y2,
            stroke,
            lineWidth,
            ...(dash.length === 0 ? {} : { dash }),
            lineCap,
        };
    }
    function text(id, x, y, value, style, options) {
        return {
            type: 'text',
            ...nodeBase(id, { zIndex: 110 }),
            x,
            y,
            text: value,
            fill: style.fill,
            fontFamily: style.fontFamily,
            fontSize: style.fontSize,
            fontWeight: style.fontWeight,
            ...(style.fontStyle === undefined ? {} : { fontStyle: style.fontStyle }),
            align: options.align,
            baseline: options.baseline,
            rotation: options.rotation,
        };
    }
    function explicitTickLabel(value, scale, locale) {
        if (scale.kind === 'time' || scale.kind === 'utc') {
            const date = new Date(typeof value === 'number' ? value : Date.parse(value));
            if (Number.isFinite(date.getTime())) {
                try {
                    return new Intl.DateTimeFormat(locale, {
                        year: 'numeric',
                        month: 'short',
                        day: 'numeric',
                        ...(scale.kind === 'utc' ? { timeZone: 'UTC' } : {}),
                    }).format(date);
                }
                catch {
                    return new Intl.DateTimeFormat(undefined, {
                        year: 'numeric',
                        month: 'short',
                        day: 'numeric',
                        ...(scale.kind === 'utc' ? { timeZone: 'UTC' } : {}),
                    }).format(date);
                }
            }
        }
        if (!['band', 'point', 'ordinal'].includes(scale.kind) && typeof value === 'number') {
            try {
                return new Intl.NumberFormat(locale, { maximumFractionDigits: 6 }).format(value);
            }
            catch {
                return new Intl.NumberFormat(undefined, { maximumFractionDigits: 6 }).format(value);
            }
        }
        return String(value);
    }
    function requestedTickCount(context) {
        if (context.axis === false)
            return 0;
        const axisChannel = channel(context);
        const length = axisChannel === 'x' ? context.plot.width : context.plot.height;
        const automatic = Math.max(2, Math.floor(length / (axisChannel === 'x' ? 96 : 58)));
        const requested = context.axis.ticks.count ?? automatic;
        if (context.axis.ticks.spacing <= 0)
            return Math.max(1, requested);
        return Math.max(1, Math.min(requested, Math.floor(length / context.axis.ticks.spacing)));
    }
    function pruneTicksBySpacing(ticks, spacing) {
        if (spacing <= 0 || ticks.length <= 1)
            return ticks;
        const ordered = [...ticks].sort((left, right) => left.position - right.position);
        const kept = [];
        for (const tick of ordered) {
            const previous = kept.at(-1);
            if (previous === undefined || tick.position - previous.position >= spacing)
                kept.push(tick);
        }
        return kept;
    }
    function resolveTicks(context) {
        const axis = context.axis;
        if (axis === false)
            return [];
        const configuredValues = axis.ticks.values;
        const rawTicks = configuredValues === undefined
            ? context.scale.ticks(requestedTickCount(context), context.locale)
            : configuredValues.flatMap((value) => {
                const mapped = context.scale.map(value);
                const minimum = channel(context) === 'x' ? context.plot.x : context.plot.y;
                const maximum = minimum + (channel(context) === 'x' ? context.plot.width : context.plot.height);
                return Number.isFinite(mapped) && mapped >= minimum - 0.5 && mapped <= maximum + 0.5
                    ? [
                        {
                            value,
                            position: mapped,
                            label: explicitTickLabel(value, context.scale, context.locale),
                        },
                    ]
                    : [];
            });
        return pruneTicksBySpacing(rawTicks, axis.ticks.spacing).map((tick) => ({
            ...tick,
            formattedLabel: truncateAxisLabel(formatAxisTick(tick, axis.format, context.locale), axis.labels.maxLength),
        }));
    }
    function labelAngle(context, ticks) {
        if (context.axis === false)
            return 0;
        if (context.axis.labels.angle !== undefined)
            return context.axis.labels.angle;
        switch (context.axis.labels.orientation) {
            case 'horizontal':
                return 0;
            case 'vertical-up':
                return -90;
            case 'vertical-down':
                return 90;
            case 'auto':
                return channel(context) === 'x' && context.scale.kind === 'band' && ticks.length > 10
                    ? -35
                    : 0;
        }
    }
    function explicitAlign(align) {
        return align === 'auto' ? null : align;
    }
    function labelAlign(context, axisPosition, angle) {
        if (context.axis === false)
            return 'center';
        const configured = explicitAlign(context.axis.labels.align);
        if (configured !== null)
            return configured;
        if (channel(context) === 'x') {
            if (angle === 0)
                return 'center';
            const startsOutward = (axisPosition === 'bottom' && angle > 0) || (axisPosition === 'top' && angle < 0);
            return startsOutward ? 'left' : 'right';
        }
        if (angle !== 0)
            return 'center';
        return axisPosition === 'left' ? 'right' : 'left';
    }
    function titleAlign(context, angle) {
        if (context.axis === false)
            return 'center';
        const align = context.axis.title.align;
        if (channel(context) !== 'y' || angle >= 0 || align === 'center')
            return align;
        return align === 'start' ? 'end' : 'start';
    }
    function coordinateAlongAxis(plot, axisChannel, align) {
        if (axisChannel === 'x') {
            if (align === 'start')
                return plot.x;
            if (align === 'end')
                return plot.x + plot.width;
            return plot.x + plot.width / 2;
        }
        if (align === 'start')
            return plot.y;
        if (align === 'end')
            return plot.y + plot.height;
        return plot.y + plot.height / 2;
    }
    function axisCoordinate(plot, axisPosition, offset) {
        switch (axisPosition) {
            case 'top':
                return plot.y - offset;
            case 'bottom':
                return plot.y + plot.height + offset;
            case 'left':
                return plot.x - offset;
            case 'right':
                return plot.x + plot.width + offset;
        }
    }
    function outwardSign(axisPosition) {
        return axisPosition === 'top' || axisPosition === 'left' ? -1 : 1;
    }
    function gridIsBoundary(tick, plot, axisChannel) {
        const boundary = axisChannel === 'x' ? plot.x : plot.y + plot.height;
        return Math.abs(tick.position - boundary) <= 0.5;
    }
    function titleText(context) {
        if (context.axis === false || context.axis.title.visible === false)
            return '';
        return context.axis.title.text ?? context.title;
    }
    function minorGridPositions(context, ticks) {
        if (context.axis === false ||
            !context.axis.grid.visible ||
            context.theme.axis.minorGridVisible !== true ||
            (context.id !== 'x' && context.id !== 'y') ||
            ['band', 'point', 'ordinal', 'quantile', 'quantize', 'threshold'].includes(context.scale.kind)) {
            return [];
        }
        const positions = [...new Set(ticks.map((tick) => tick.position))].sort((left, right) => left - right);
        return positions.slice(0, -1).flatMap((tickPosition, index) => {
            const next = positions[index + 1];
            if (next === undefined || next <= tickPosition)
                return [];
            const midpoint = tickPosition + (next - tickPosition) / 2;
            const minimum = channel(context) === 'x' ? context.plot.x : context.plot.y;
            const maximum = minimum + (channel(context) === 'x' ? context.plot.width : context.plot.height);
            return midpoint > minimum && midpoint < maximum ? [midpoint] : [];
        });
    }
    function resolvedTitlePadding(context, ticks, titleStyle) {
        const axis = context.axis;
        if (axis === false || axis.title.themeGap === undefined)
            return axis === false ? 0 : axis.title.padding;
        const axisChannel = channel(context);
        const tickSize = axis.ticks.visible ? (axis.ticks.size ?? context.theme.axis.tickLength) : 0;
        let labelExtent = 0;
        if (axis.labels.visible && ticks.length > 0) {
            const style = resolveTextStyle(axis.labels.font, axis.labels.color, context.theme, context.theme.colors.mutedText, context.theme.typography.axisLabelSize ?? context.theme.typography.fontSize, context.theme.typography.axisLabelWeight ?? 500);
            const angle = labelAngle(context, ticks);
            for (const tick of ticks) {
                const projected = projectedSize(estimatedTextWidth(tick.formattedLabel, style.fontSize), style.fontSize, angle);
                labelExtent = Math.max(labelExtent, axisChannel === 'x' ? projected.height : projected.width);
            }
        }
        const labelStrip = labelExtent === 0
            ? tickSize
            : tickSize + (axis.labels.padding ?? context.theme.axis.labelPadding) + labelExtent;
        return labelStrip + axis.title.themeGap + (axisChannel === 'y' ? titleStyle.fontSize / 2 : 0);
    }
    /** Compile any primary or secondary Cartesian axis into renderer-neutral Scene primitives. */
    function compileAxis(context) {
        const { axis, plot, theme } = context;
        if (axis === false || axis.visible === false)
            return [];
        const nodes = [];
        const axisChannel = channel(context);
        const axisPosition = position$1(context);
        const coordinate = axisCoordinate(plot, axisPosition, axis.offset);
        const sign = outwardSign(axisPosition);
        const prefix = `axis-${context.id}`;
        const ticks = resolveTicks(context);
        const angle = labelAngle(context, ticks);
        const tickSize = axis.ticks.visible ? (axis.ticks.size ?? theme.axis.tickLength) : 0;
        const labelPadding = axis.labels.padding ?? theme.axis.labelPadding;
        const axisLineCap = theme.axis.lineCap ?? 'round';
        if (axis.line.visible) {
            const stroke = axis.line.color ?? theme.colors.axis;
            const width = axis.line.width ?? theme.axis.lineWidth;
            nodes.push(axisChannel === 'x'
                ? line$1(`${prefix}:line`, plot.x, coordinate, plot.x + plot.width, coordinate, stroke, width, 100, axis.line.opacity, axis.line.dash, axisLineCap)
                : line$1(`${prefix}:line`, coordinate, plot.y, coordinate, plot.y + plot.height, stroke, width, 100, axis.line.opacity, axis.line.dash, axisLineCap));
        }
        const labelStyle = resolveTextStyle(axis.labels.font, axis.labels.color, theme, theme.colors.mutedText, theme.typography.axisLabelSize ?? theme.typography.fontSize, theme.typography.axisLabelWeight ?? 500);
        const minorGridStroke = axis.grid.color ?? theme.colors.minorGrid ?? theme.colors.grid;
        const minorGridWidth = axis.grid.width ?? theme.axis.minorGridLineWidth ?? theme.axis.gridLineWidth / 2;
        const minorGridOpacity = Math.min(axis.grid.opacity, theme.axis.minorGridOpacity ?? axis.grid.opacity);
        minorGridPositions(context, ticks).forEach((gridPosition, index) => {
            nodes.push(axisChannel === 'x'
                ? line$1(`${prefix}:grid-minor:${index}`, gridPosition, plot.y, gridPosition, plot.y + plot.height, minorGridStroke, minorGridWidth, -21, minorGridOpacity, axis.grid.dash, axisLineCap)
                : line$1(`${prefix}:grid-minor:${index}`, plot.x, gridPosition, plot.x + plot.width, gridPosition, minorGridStroke, minorGridWidth, -21, minorGridOpacity, axis.grid.dash, axisLineCap));
        });
        ticks.forEach((tick, index) => {
            const isZero = typeof tick.value === 'number' && Math.abs(tick.value) < Number.EPSILON;
            if (axis.grid.visible && !gridIsBoundary(tick, plot, axisChannel)) {
                const defaultZeroStyle = axis.grid.color === undefined && (theme.axis.emphasizeZero ?? true);
                const gridStroke = axis.grid.color ?? (isZero && defaultZeroStyle ? theme.colors.axis : theme.colors.grid);
                const gridWidth = axis.grid.width ?? theme.axis.gridLineWidth;
                nodes.push(axisChannel === 'x'
                    ? line$1(`${prefix}:grid:${index}`, tick.position, plot.y, tick.position, plot.y + plot.height, gridStroke, isZero && defaultZeroStyle ? Math.max(1, gridWidth) : gridWidth, -20, isZero && defaultZeroStyle ? Math.max(0.9, axis.grid.opacity) : axis.grid.opacity, axis.grid.dash, axisLineCap)
                    : line$1(`${prefix}:grid:${index}`, plot.x, tick.position, plot.x + plot.width, tick.position, gridStroke, isZero && defaultZeroStyle ? Math.max(1, gridWidth) : gridWidth, -20, isZero && defaultZeroStyle ? Math.max(0.9, axis.grid.opacity) : axis.grid.opacity, axis.grid.dash, axisLineCap));
            }
            if (axis.ticks.visible && tickSize > 0) {
                const stroke = axis.ticks.color ?? theme.colors.axis;
                const width = axis.ticks.width ?? theme.axis.lineWidth;
                nodes.push(axisChannel === 'x'
                    ? line$1(`${prefix}:tick:${index}`, tick.position, coordinate, tick.position, coordinate + sign * tickSize, stroke, width, 100, axis.ticks.opacity, axis.ticks.dash, axisLineCap)
                    : line$1(`${prefix}:tick:${index}`, coordinate, tick.position, coordinate + sign * tickSize, tick.position, stroke, width, 100, axis.ticks.opacity, axis.ticks.dash, axisLineCap));
            }
            if (!axis.labels.visible)
                return;
            if (axisChannel === 'x') {
                nodes.push(text(`${prefix}:label:${index}`, tick.position, coordinate + sign * (tickSize + labelPadding), tick.formattedLabel, labelStyle, {
                    align: labelAlign(context, axisPosition, angle),
                    baseline: axisPosition === 'top' ? 'bottom' : 'top',
                    rotation: angle,
                }));
            }
            else {
                nodes.push(text(`${prefix}:label:${index}`, coordinate + sign * (tickSize + labelPadding), tick.position, tick.formattedLabel, labelStyle, {
                    align: labelAlign(context, axisPosition, angle),
                    baseline: 'middle',
                    rotation: angle,
                }));
            }
        });
        const resolvedTitle = titleText(context);
        if (resolvedTitle !== '') {
            const titleStyle = resolveTextStyle(axis.title.font, axis.title.color, theme, theme.colors.axisTitle ?? theme.colors.mutedText, theme.typography.axisTitleSize ?? theme.typography.fontSize, theme.typography.axisTitleWeight ?? 600);
            const titlePosition = coordinateAlongAxis(plot, axisChannel, axis.title.align);
            const titleAngle = axis.title.angle ?? (axisChannel === 'x' ? 0 : axisPosition === 'left' ? -90 : 90);
            const titleCoordinate = coordinate + sign * resolvedTitlePadding(context, ticks, titleStyle);
            if (axisChannel === 'x') {
                nodes.push(text(`${prefix}:title`, titlePosition, titleCoordinate, resolvedTitle, titleStyle, {
                    align: titleAlign(context, titleAngle),
                    baseline: axisPosition === 'top' ? 'bottom' : 'top',
                    rotation: titleAngle,
                }));
            }
            else {
                nodes.push(text(`${prefix}:title`, axisPosition === 'left' ? Math.max(12, titleCoordinate) : titleCoordinate, titlePosition, resolvedTitle, titleStyle, { align: titleAlign(context, titleAngle), baseline: 'middle', rotation: titleAngle }));
            }
        }
        return nodes;
    }
    function estimatedTextWidth(value, fontSize) {
        let units = 0;
        for (const character of Array.from(value)) {
            if (/\s/u.test(character))
                units += 0.33;
            else if (/[^\u0000-\u024f]/u.test(character))
                units += 1;
            else
                units += 0.6;
        }
        return Math.max(fontSize * 0.6, units * fontSize);
    }
    function projectedSize(width, height, angle) {
        const radians = (angle * Math.PI) / 180;
        const cosine = Math.abs(Math.cos(radians));
        const sine = Math.abs(Math.sin(radians));
        return {
            width: width * cosine + height * sine,
            height: width * sine + height * cosine,
        };
    }
    function usesLegacyPrimaryGutter(context) {
        const axis = context.axis;
        if (axis === false || (context.id !== 'x' && context.id !== 'y'))
            return false;
        const expectedPosition = context.id === 'x' ? 'bottom' : 'left';
        const expectedTitlePadding = context.id === 'x' ? 32 : 46;
        return (axis.position === expectedPosition &&
            axis.offset === 0 &&
            axis.line.width === undefined &&
            axis.ticks.spacing === 0 &&
            axis.ticks.size === undefined &&
            axis.ticks.values === undefined &&
            axis.labels.orientation === 'auto' &&
            axis.labels.angle === undefined &&
            axis.labels.align === 'auto' &&
            axis.labels.padding === undefined &&
            axis.labels.maxLength === undefined &&
            axis.labels.font.family === undefined &&
            axis.labels.font.size === undefined &&
            axis.labels.font.weight === undefined &&
            axis.labels.font.style === 'normal' &&
            axis.title.angle === undefined &&
            axis.title.themeGap === undefined &&
            axis.title.padding === expectedTitlePadding &&
            axis.title.font.family === undefined &&
            axis.title.font.size === undefined &&
            axis.title.font.weight === undefined &&
            axis.title.font.style === 'normal' &&
            axis.format.type === 'auto' &&
            axis.format.fractionDigits === undefined &&
            axis.format.notation === 'standard' &&
            axis.format.useGrouping &&
            axis.format.currency === undefined &&
            axis.format.prefix === '' &&
            axis.format.suffix === '');
    }
    /**
     * Deterministically estimate the outward axis gutter from the same ticks, formatting,
     * truncation, fonts and rotations used by compileAxis().
     */
    function measureAxisGutter(context) {
        const { axis, theme } = context;
        if (axis === false || axis.visible === false)
            return 0;
        const axisChannel = channel(context);
        let required = measureAxisLabelGutter(context);
        const resolvedTitle = titleText(context);
        if (resolvedTitle !== '') {
            const style = resolveTextStyle(axis.title.font, axis.title.color, theme, theme.colors.axisTitle ?? theme.colors.mutedText, theme.typography.axisTitleSize ?? theme.typography.fontSize, theme.typography.axisTitleWeight ?? 600);
            const axisPosition = position$1(context);
            const titleAngle = axis.title.angle ?? (axisChannel === 'x' ? 0 : axisPosition === 'left' ? -90 : 90);
            const projected = projectedSize(estimatedTextWidth(resolvedTitle, style.fontSize), style.fontSize, titleAngle);
            const outwardTextExtent = axisChannel === 'x'
                ? projected.height
                : axis.title.align === 'center'
                    ? projected.width / 2
                    : projected.width;
            required = Math.max(required, axis.offset + resolvedTitlePadding(context, resolveTicks(context), style) + outwardTextExtent);
        }
        const measured = Math.ceil(required);
        if (!usesLegacyPrimaryGutter(context))
            return measured;
        return Math.min(measured, context.id === 'x' ? 44 : 56);
    }
    /** Measure the interactive tick/label strip without extending it through the axis title. */
    function measureAxisLabelGutter(context) {
        const { axis, theme } = context;
        if (axis === false || axis.visible === false)
            return 0;
        const axisChannel = channel(context);
        const ticks = resolveTicks(context);
        const angle = labelAngle(context, ticks);
        const tickSize = axis.ticks.visible ? (axis.ticks.size ?? theme.axis.tickLength) : 0;
        const labelPadding = axis.labels.padding ?? theme.axis.labelPadding;
        const lineWidth = axis.line.visible ? (axis.line.width ?? theme.axis.lineWidth) : 0;
        let required = axis.offset + lineWidth / 2;
        if (axis.labels.visible && ticks.length > 0) {
            const style = resolveTextStyle(axis.labels.font, axis.labels.color, theme, theme.colors.mutedText, theme.typography.axisLabelSize ?? theme.typography.fontSize, theme.typography.axisLabelWeight ?? 500);
            let labelExtent = 0;
            for (const tick of ticks) {
                const projected = projectedSize(estimatedTextWidth(tick.formattedLabel, style.fontSize), style.fontSize, angle);
                labelExtent = Math.max(labelExtent, axisChannel === 'x' ? projected.height : projected.width);
            }
            required = Math.max(required, axis.offset + tickSize + labelPadding + labelExtent);
        }
        else if (axis.ticks.visible) {
            required = Math.max(required, axis.offset + tickSize);
        }
        return Math.ceil(required);
    }

    const ROW_TARGET_MARKS = new Set(['area', 'line', 'smooth', 'stepped-area', 'trendline']);
    function pathBounds(node) {
        let minX = Number.POSITIVE_INFINITY;
        let maxX = Number.NEGATIVE_INFINITY;
        let minY = Number.POSITIVE_INFINITY;
        let maxY = Number.NEGATIVE_INFINITY;
        for (const points of [node.points, ...(node.subpaths ?? [])]) {
            for (const point of points) {
                minX = Math.min(minX, point.x);
                maxX = Math.max(maxX, point.x);
                minY = Math.min(minY, point.y);
                maxY = Math.max(maxY, point.y);
            }
        }
        if (!Number.isFinite(minX))
            return null;
        return { x: minX, y: minY, width: maxX - minX, height: maxY - minY };
    }
    function anchor(node) {
        switch (node.type) {
            case 'circle':
                return { x: node.cx, y: node.cy };
            case 'rect':
                return { x: node.x + node.width / 2, y: node.y + node.height / 2 };
            case 'line':
                return { x: (node.x1 + node.x2) / 2, y: (node.y1 + node.y2) / 2 };
            case 'text':
                return { x: node.x, y: node.y };
            case 'path': {
                const path = pathBounds(node);
                return path === null ? null : { x: path.x + path.width / 2, y: path.y + path.height / 2 };
            }
        }
    }
    function bounds(node) {
        switch (node.type) {
            case 'circle':
                return {
                    x: node.cx - node.radius,
                    y: node.cy - node.radius,
                    width: node.radius * 2,
                    height: node.radius * 2,
                };
            case 'rect':
                return { x: node.x, y: node.y, width: node.width, height: node.height };
            case 'line':
                return {
                    x: Math.min(node.x1, node.x2),
                    y: Math.min(node.y1, node.y2),
                    width: Math.abs(node.x2 - node.x1),
                    height: Math.abs(node.y2 - node.y1),
                };
            case 'text':
                return { x: node.x, y: node.y, width: 0, height: 0 };
            case 'path': {
                return pathBounds(node) ?? { x: 0, y: 0, width: 0, height: 0 };
            }
        }
    }
    function intersectsPlot(plot, target) {
        return (target.x + target.width >= plot.x &&
            target.x <= plot.x + plot.width &&
            target.y + target.height >= plot.y &&
            target.y <= plot.y + plot.height);
    }
    function clampToPlot(plot, x, y) {
        return {
            x: Math.max(plot.x, Math.min(plot.x + plot.width, x)),
            y: Math.max(plot.y, Math.min(plot.y + plot.height, y)),
        };
    }
    function scaleValue(scale, value) {
        if (value === null ||
            value === undefined ||
            typeof value === 'boolean' ||
            (typeof value !== 'number' && typeof value !== 'string' && !(value instanceof Date))) {
            return null;
        }
        const position = scale.map(value);
        return Number.isFinite(position) ? position : null;
    }
    function collectAxisTooltipTargets(context) {
        if (!context.performance.enableHitTesting)
            return [];
        const { axis, channel, scales, plot } = context;
        const layerDataById = new Map(scales.layers.map((layerData) => [layerData.layer.id, layerData]));
        const targets = [];
        const representedRows = new Set();
        let order = 0;
        const visit = (node, parentOpacity) => {
            const opacity = parentOpacity * node.opacity;
            if (!node.visible || opacity <= 0)
                return;
            if (node.type === 'group') {
                const children = [...node.children].sort((left, right) => left.zIndex - right.zIndex);
                for (const child of children)
                    visit(child, opacity);
                return;
            }
            if (node.interactive !== true || node.datum === undefined)
                return;
            const geometry = anchor(node);
            if (geometry === null || !intersectsPlot(plot, bounds(node)))
                return;
            const clippedGeometry = clampToPlot(plot, geometry.x, geometry.y);
            const layerData = layerDataById.get(node.datum.layerId);
            if (layerData !== undefined &&
                (channel === 'x' ? layerData.xAxisId : layerData.yAxisId) !== axis) {
                return;
            }
            let x = clippedGeometry.x;
            let y = clippedGeometry.y;
            if (node.datum.tooltip === undefined && layerData !== undefined) {
                const encoding = layerData.layer[channel];
                const scale = channel === 'x' ? layerData.xScale : layerData.yScale;
                const encoded = scaleValue(scale, node.datum.datum[encoding.field]);
                if (encoded !== null) {
                    if (channel === 'x')
                        x = encoded;
                    else
                        y = encoded;
                }
            }
            targets.push({
                ...node.datum,
                nodeId: `axis:${node.id}`,
                x,
                y,
                order,
            });
            representedRows.add(`${node.datum.layerId}\u0000${node.datum.rowIndex}`);
            order += 1;
        };
        for (const group of context.layerGroups)
            visit(group, 1);
        for (const layerData of scales.layers) {
            if (!ROW_TARGET_MARKS.has(layerData.layer.mark.type))
                continue;
            if ((channel === 'x' ? layerData.xAxisId : layerData.yAxisId) !== axis)
                continue;
            const indices = strideSampleIndices(layerData.table.length, context.performance.maxPointMarks);
            for (const rowIndex of indices) {
                if (representedRows.has(`${layerData.layer.id}\u0000${rowIndex}`))
                    continue;
                const datum = layerData.table.row(rowIndex);
                if (context.datumVisible?.(layerData.layer.id, rowIndex, datum) === false)
                    continue;
                const x = scaleValue(layerData.xScale, datum[layerData.layer.x.field]);
                const y = scaleValue(layerData.yScale, datum[layerData.layer.y.field]);
                if (x === null || y === null)
                    continue;
                const position = clampToPlot(plot, x, y);
                targets.push({
                    layerId: layerData.layer.id,
                    rowIndex,
                    datum,
                    nodeId: `axis:${layerData.layer.id}:row:${rowIndex}`,
                    x: position.x,
                    y: position.y,
                    order,
                });
                order += 1;
            }
        }
        return targets;
    }

    /** Marks whose geometry is usually laid out independently of Cartesian x/y axes. */
    const AXISLESS_MARKS = new Set([
        'arc-diagram',
        'calendar',
        'carpet',
        'chord',
        'funnel',
        'gauge',
        'geo-flow',
        'geo-heatmap',
        'geo-line',
        'graph',
        'geo',
        'item',
        'map',
        'org',
        'packed-bubble',
        'parallel',
        'pie',
        'polar',
        'pyramid',
        'radar',
        'sankey',
        'scatter-matrix',
        'smith',
        'solid-gauge',
        'sunburst',
        'table',
        'ternary',
        'tiled-map',
        'tilemap',
        'tree',
        'treemap',
        'variable-pie',
        'venn',
        'word-cloud',
        'word-tree',
    ]);
    /**
     * Resolve coordinate semantics from the normalized layer, not just the mark name.
     * `pyramid` serves both radial funnel/pyramid layouts and the Cartesian
     * `column-pyramid` compatibility variant.
     */
    function isAxislessLayer(layer) {
        if (layer.mark.type === 'pyramid') {
            return layer.mark.options.variant !== 'column-pyramid';
        }
        return AXISLESS_MARKS.has(layer.mark.type);
    }

    const ISO_DATE_PREFIX = /^\d{4}-\d{2}-\d{2}(?:T|$)/;
    function inferFieldType(table, field) {
        const column = table.column(field);
        for (let index = 0; index < table.length; index += 1) {
            const value = column[index];
            if (value === null || value === undefined)
                continue;
            if (value instanceof Date)
                return 'temporal';
            if (typeof value === 'number')
                return 'quantitative';
            if (typeof value === 'string' &&
                ISO_DATE_PREFIX.test(value) &&
                Number.isFinite(Date.parse(value))) {
                return 'temporal';
            }
            return 'nominal';
        }
        return 'nominal';
    }

    const domainViewVersion = 1;
    function invalid$1(message, path = '$.interaction.domainNavigation') {
        throw new GraflumeError('INVALID_SPEC', message, { path });
    }
    function assertPlainObject(value, label) {
        if (typeof value !== 'object' || value === null || Array.isArray(value)) {
            invalid$1(`${label} must be an object.`);
        }
    }
    function assertClosedKeys(value, allowed, label) {
        const allowedKeys = new Set(allowed);
        const unknown = Object.keys(value).filter((key) => !allowedKeys.has(key));
        if (unknown.length > 0)
            invalid$1(`${label} contains unknown key "${unknown[0]}".`);
    }
    function normalizedWindow(input) {
        assertPlainObject(input, 'Domain axis window');
        assertClosedKeys(input, ['start', 'end'], 'Domain axis window');
        if (!Number.isFinite(input.start) ||
            !Number.isFinite(input.end) ||
            input.start < 0 ||
            input.end > 1 ||
            input.start >= input.end) {
            invalid$1('Domain axis windows require finite 0 <= start < end <= 1 values.');
        }
        return Object.freeze({ start: input.start, end: input.end });
    }
    function normalizeDomainViewState(input) {
        assertPlainObject(input, 'Domain view state');
        assertClosedKeys(input, ['version', 'axes'], 'Domain view state');
        if (input.version !== domainViewVersion) {
            invalid$1(`Domain view state.version must be ${domainViewVersion}.`);
        }
        assertPlainObject(input.axes, 'Domain view axes');
        const axes = {};
        for (const [axis, window] of Object.entries(input.axes)) {
            if (!isSafeAxisId(axis)) {
                invalid$1(`Unknown domain view axis "${axis}".`);
            }
            if (window !== undefined)
                axes[axis] = normalizedWindow(window);
        }
        return Object.freeze({ version: domainViewVersion, axes: Object.freeze(axes) });
    }
    function emptyDomainViewState() {
        return Object.freeze({ version: domainViewVersion, axes: Object.freeze({}) });
    }
    function domainAxisWindow(state, axis) {
        const window = state.axes[axis];
        return window === undefined ? Object.freeze({ start: 0, end: 1 }) : window;
    }
    function boundedWindow$1(start, width) {
        const boundedWidth = Math.max(Number.EPSILON, Math.min(1, width));
        const boundedStart = Math.max(0, Math.min(1 - boundedWidth, start));
        return Object.freeze({ start: boundedStart, end: boundedStart + boundedWidth });
    }
    function withWindow(state, axis, window) {
        const axes = { ...state.axes };
        const normalized = normalizedWindow(window);
        if (Math.abs(normalized.start) < 1e-12 && Math.abs(normalized.end - 1) < 1e-12) {
            delete axes[axis];
        }
        else
            axes[axis] = normalized;
        return normalizeDomainViewState({ version: domainViewVersion, axes });
    }
    function zoomDomainAxisWindow(state, axis, factor, anchor, maxZoom) {
        if (!Number.isFinite(factor) || factor <= 0)
            invalid$1('Domain zoom factor must be positive.');
        if (!Number.isFinite(anchor))
            invalid$1('Domain zoom anchor must be finite.');
        if (!Number.isFinite(maxZoom) || maxZoom < 1)
            invalid$1('Domain maxZoom must be at least 1.');
        const current = domainAxisWindow(state, axis);
        const width = current.end - current.start;
        const boundedAnchor = Math.max(current.start, Math.min(current.end, anchor));
        const local = width === 0 ? 0.5 : (boundedAnchor - current.start) / width;
        const nextWidth = Math.max(1 / maxZoom, Math.min(1, width / factor));
        return withWindow(state, axis, boundedWindow$1(boundedAnchor - local * nextWidth, nextWidth));
    }
    function panDomainAxisWindow(state, axis, delta) {
        if (!Number.isFinite(delta))
            invalid$1('Domain pan delta must be finite.');
        const current = domainAxisWindow(state, axis);
        return withWindow(state, axis, boundedWindow$1(current.start + delta, current.end - current.start));
    }
    function scaleFor(context, axis) {
        const scale = context.axes[axis];
        if (scale === undefined)
            invalid$1(`Axis "${axis}" is not resolved.`, `$.axes.${axis}`);
        if (scale.invert === undefined && scale.kind !== 'band' && scale.kind !== 'point') {
            throw new GraflumeError('INCOMPATIBLE_SCALE', `Axis "${axis}" cannot be navigated.`, {
                path: `$.axes.${axis}`,
            });
        }
        return scale;
    }
    function rangeRatio(scale, pixel) {
        if (!Number.isFinite(pixel))
            invalid$1('Domain navigation pixel must be finite.');
        const range = scale.range();
        const start = range[0];
        const end = range.at(-1);
        if (start === undefined || end === undefined || start === end) {
            throw new GraflumeError('INCOMPATIBLE_SCALE', 'Navigable scale range must be non-empty.');
        }
        return Math.max(0, Math.min(1, (pixel - start) / (end - start)));
    }
    function zoomDomainAtPixel(state, context, axis, factor, pixel, maxZoom) {
        const scale = scaleFor(context, axis);
        const current = domainAxisWindow(state, axis);
        const ratio = rangeRatio(scale, pixel);
        const anchor = current.start + ratio * (current.end - current.start);
        return zoomDomainAxisWindow(state, axis, factor, anchor, maxZoom);
    }
    function panDomainByPixels(state, context, axis, deltaPixels) {
        const scale = scaleFor(context, axis);
        if (!Number.isFinite(deltaPixels))
            invalid$1('Domain pan pixel delta must be finite.');
        const range = scale.range();
        const start = range[0];
        const end = range.at(-1);
        if (start === undefined || end === undefined || start === end) {
            throw new GraflumeError('INCOMPATIBLE_SCALE', 'Navigable scale range must be non-empty.');
        }
        const current = domainAxisWindow(state, axis);
        // Grab-to-pan: dragging marks toward positive pixels reveals the preceding
        // authored domain. A descending layout range naturally reverses this sign.
        const delta = (-deltaPixels / (end - start)) * (current.end - current.start);
        return panDomainAxisWindow(state, axis, delta);
    }
    function domainForAxisWindow(scale, window) {
        if (scale.invert === undefined) {
            if (scale.kind !== 'band' && scale.kind !== 'point') {
                throw new GraflumeError('INCOMPATIBLE_SCALE', `Data-domain navigation cannot resolve the "${scale.kind}" scale.`);
            }
            const domain = scale.domain();
            if (domain.length === 0) {
                throw new GraflumeError('INCOMPATIBLE_SCALE', 'Navigable categorical domains are non-empty.');
            }
            const start = Math.min(domain.length - 1, Math.floor(window.start * domain.length));
            const end = Math.max(start + 1, Math.min(domain.length, Math.ceil(window.end * domain.length)));
            return Object.freeze(domain.slice(start, end));
        }
        const range = scale.range();
        const start = range[0];
        const end = range.at(-1);
        if (start === undefined || end === undefined || start === end) {
            throw new GraflumeError('INCOMPATIBLE_SCALE', 'Navigable scale range must be non-empty.');
        }
        const first = scale.invert(start + (end - start) * window.start);
        const last = scale.invert(start + (end - start) * window.end);
        if (typeof first !== 'number' ||
            typeof last !== 'number' ||
            !Number.isFinite(first) ||
            !Number.isFinite(last) ||
            first === last) {
            throw new GraflumeError('INCOMPATIBLE_SCALE', `Data-domain navigation requires finite numeric endpoints from "${scale.kind}".`);
        }
        return Object.freeze([first, last]);
    }
    function domainViewIsIdentity(state) {
        return Object.keys(state.axes).length === 0;
    }

    const ADVANCED_CANDLESTICK_OPTIONS = Object.freeze([
        'aggregateIntervalMs',
        'timeZone',
        'sessionStartMinute',
        'sessionEndMinute',
        'tradingDays',
        'excludedDates',
        'includedDates',
        'extendedHours',
        'navigator',
        'navigatorStart',
        'navigatorEnd',
    ]);
    function finiteMarkOption(layer, name) {
        const value = layer.mark.options[name];
        return typeof value === 'number' && Number.isFinite(value) ? value : undefined;
    }
    function numberMarkArray(layer, name) {
        const value = layer.mark.options[name];
        return Array.isArray(value) &&
            value.every((entry) => typeof entry === 'number' && Number.isFinite(entry))
            ? [...value]
            : undefined;
    }
    function stringMarkArray(layer, name) {
        const value = layer.mark.options[name];
        return Array.isArray(value) && value.every((entry) => typeof entry === 'string')
            ? [...value]
            : undefined;
    }
    function advancedCandlestick(layer) {
        return (layer.mark.type === 'candlestick' &&
            ADVANCED_CANDLESTICK_OPTIONS.some((name) => layer.mark.options[name] !== undefined));
    }
    function observedCandlestickInterval(layer) {
        const times = Array.from({ length: layer.table.length }, (_, index) => layer.table.numericValue(index, layer.layer.x.field))
            .filter((value) => value !== null)
            .sort((left, right) => left - right);
        const gaps = times
            .slice(1)
            .map((value, index) => value - times[index])
            .filter((value) => value > 0);
        return Math.max(1, Math.min(...gaps, 86_400_000));
    }
    function candlestickBuckets(layer) {
        const mark = layer.layer.mark;
        const authoredExtended = mark.options.extendedHours;
        const extendedHours = authoredExtended === 'exclude' || authoredExtended === 'separate'
            ? authoredExtended
            : 'include';
        const tradingDays = numberMarkArray(layer.layer, 'tradingDays');
        const excludedDates = stringMarkArray(layer.layer, 'excludedDates');
        const includedDates = stringMarkArray(layer.layer, 'includedDates');
        return aggregateOhlc(Array.from({ length: layer.table.length }, (_, rowIndex) => layer.table.row(rowIndex)), {
            timeField: layer.layer.x.field,
            openField: mark.fields.open ?? 'open',
            highField: mark.fields.high ?? 'high',
            lowField: mark.fields.low ?? 'low',
            closeField: mark.fields.close ?? layer.layer.y.field,
            ...(mark.fields.volume === undefined ? {} : { volumeField: mark.fields.volume }),
            intervalMs: finiteMarkOption(layer.layer, 'aggregateIntervalMs') ?? observedCandlestickInterval(layer),
            timeZone: typeof mark.options.timeZone === 'string' ? mark.options.timeZone : 'UTC',
            session: {
                startMinute: finiteMarkOption(layer.layer, 'sessionStartMinute') ?? 0,
                endMinute: finiteMarkOption(layer.layer, 'sessionEndMinute') ?? 1_440,
            },
            ...(tradingDays === undefined ? {} : { tradingDays }),
            ...(excludedDates === undefined ? {} : { excludedDates }),
            ...(includedDates === undefined ? {} : { includedDates }),
            extendedHours,
        });
    }
    function visibleCandlestickBuckets(layer) {
        const buckets = candlestickBuckets(layer);
        const [from, to] = navigatorWindow(buckets.length, finiteMarkOption(layer.layer, 'navigatorStart') ?? 0, finiteMarkOption(layer.layer, 'navigatorEnd') ?? buckets.length);
        return buckets.slice(from, to);
    }
    function temporalInput(value) {
        if (value instanceof Date)
            return value.getTime();
        return typeof value === 'number' ? value : Date.parse(value);
    }
    function tradingScale(layer, range, type, domainWindow) {
        let visible = [...visibleCandlestickBuckets(layer)];
        if (visible.length === 0)
            return null;
        if (domainWindow !== undefined) {
            const start = Math.floor(domainWindow.start * visible.length);
            const end = Math.max(start + 1, Math.ceil(domainWindow.end * visible.length));
            visible = visible.slice(start, Math.min(visible.length, end));
        }
        const timestamps = visible.map(({ time }) => time);
        const reverse = layer.layer.x.scale.reverse === true;
        const scaleRange = Object.freeze(reverse ? [range[1], range[0]] : [...range]);
        const slot = (scaleRange[1] - scaleRange[0]) / timestamps.length;
        const positionForIndex = (index) => scaleRange[0] + (index + 0.5) * slot;
        const fractionalIndex = (timestamp) => {
            if (timestamps.length === 1)
                return 0;
            if (timestamp <= timestamps[0]) {
                const span = timestamps[1] - timestamps[0] || 1;
                return (timestamp - timestamps[0]) / span;
            }
            const last = timestamps.length - 1;
            if (timestamp >= timestamps[last]) {
                const span = timestamps[last] - timestamps[last - 1] || 1;
                return last + (timestamp - timestamps[last]) / span;
            }
            let lower = 0;
            let upper = last;
            while (upper - lower > 1) {
                const middle = Math.floor((lower + upper) / 2);
                if (timestamps[middle] <= timestamp)
                    lower = middle;
                else
                    upper = middle;
            }
            const span = timestamps[upper] - timestamps[lower] || 1;
            return lower + (timestamp - timestamps[lower]) / span;
        };
        const rawDomain = timestamps.length === 1
            ? [timestamps[0] - 0.5, timestamps[0] + 0.5]
            : [timestamps[0], timestamps.at(-1)];
        const descriptor = Object.freeze({
            type,
            domain: Object.freeze([...rawDomain]),
            range: scaleRange,
            reverse,
            rangeDirection: scaleRange[1] < scaleRange[0] ? 'descending' : 'ascending',
            outOfBounds: 'extrapolate',
        });
        const map = (value) => {
            const timestamp = temporalInput(value);
            return Number.isFinite(timestamp) ? positionForIndex(fractionalIndex(timestamp)) : Number.NaN;
        };
        const invert = (position) => {
            const index = (position - scaleRange[0]) / slot - 0.5;
            if (timestamps.length === 1)
                return timestamps[0];
            const lower = Math.max(0, Math.min(timestamps.length - 2, Math.floor(index)));
            const amount = index - lower;
            return timestamps[lower] + (timestamps[lower + 1] - timestamps[lower]) * amount;
        };
        const timeZone = typeof layer.layer.mark.options.timeZone === 'string'
            ? layer.layer.mark.options.timeZone
            : type === 'utc'
                ? 'UTC'
                : undefined;
        const interval = finiteMarkOption(layer.layer, 'aggregateIntervalMs') ?? 86_400_000;
        return Object.freeze({
            kind: type,
            bandwidth: Math.abs(slot),
            descriptor,
            domain: () => descriptor.domain,
            range: () => descriptor.range,
            map,
            invert,
            ticks: (count, locale) => {
                const requested = Math.max(1, Math.floor(count));
                const step = Math.max(1, Math.ceil(timestamps.length / requested));
                const indices = timestamps
                    .map((_timestamp, index) => index)
                    .filter((index) => index % step === 0 || index === timestamps.length - 1);
                return indices.map((index) => {
                    const value = timestamps[index];
                    let label;
                    if (type === 'linear')
                        label = new Intl.NumberFormat(locale, { maximumFractionDigits: 6 }).format(value);
                    else
                        try {
                            label = new Intl.DateTimeFormat(locale, {
                                year: 'numeric',
                                month: 'short',
                                day: 'numeric',
                                ...(interval < 86_400_000 ? { hour: '2-digit', minute: '2-digit' } : {}),
                                ...(timeZone === undefined ? {} : { timeZone }),
                            }).format(new Date(value));
                        }
                        catch {
                            label = new Date(value).toISOString();
                        }
                    return { value, label, position: map(value) };
                });
            },
        });
    }
    function typeFamily(type) {
        if (type === 'nominal' || type === 'ordinal')
            return 'categorical';
        return type === 'temporal' ? 'temporal' : 'numeric';
    }
    function resolveCommonType(types, axis) {
        const families = new Set(types.map(typeFamily));
        if (families.size > 1) {
            throw new GraflumeError('INCOMPATIBLE_SCALE', `Layers use incompatible ${axis}-axis field types: ${[...families].join(', ')}.`, { path: `$.layers[].${axis}.type` });
        }
        const first = types[0] ?? 'nominal';
        if (families.has('categorical'))
            return first === 'ordinal' ? 'ordinal' : 'nominal';
        return first;
    }
    function explicitNumericDomain(layers, axis) {
        for (const { layer } of layers) {
            const domain = layer[axis].scale.domain;
            if (domain?.length === 2 && typeof domain[0] === 'number' && typeof domain[1] === 'number') {
                return [domain[0], domain[1]];
            }
        }
        return null;
    }
    function numericDomain(layers, axis, fieldType) {
        const explicit = explicitNumericDomain(layers, axis);
        if (explicit !== null)
            return explicit;
        let min = Number.POSITIVE_INFINITY;
        let max = Number.NEGATIVE_INFINITY;
        let includeZero = false;
        for (const { layer, table, technicalIndicator } of layers) {
            const encoding = layer[axis];
            const distributionMode = layer.mark.type === 'distribution' ? resolveDistributionMode(layer.mark.options.mode) : null;
            const densitySummary = distributionMode === 'curve'
                ? summarizeNormalDistribution(Array.from({ length: table.length }, (_, index) => {
                    const value = table.value(index, layer.mark.fields.value ?? layer.y.field);
                    return value instanceof Date ? value.getTime() : value;
                }).filter((value) => typeof value === 'number'))
                : null;
            const distributionObservations = distributionMode === 'kde' ||
                distributionMode === 'ecdf' ||
                distributionMode === 'ccdf' ||
                distributionMode === 'histogram'
                ? Array.from({ length: table.length }, (_, rowIndex) => {
                    const field = layer.mark.fields.value ?? layer.x.field;
                    const value = table.numericValue(rowIndex, field);
                    const optionWeightField = layer.mark.options.weightField;
                    const weightField = layer.mark.fields.weight ??
                        (typeof optionWeightField === 'string' ? optionWeightField : undefined);
                    const weight = weightField === undefined ? 1 : table.numericValue(rowIndex, weightField);
                    return value === null || weight === null || weight < 0
                        ? null
                        : { value, weight, rowIndex };
                }).filter((value) => value !== null)
                : [];
            const kde = distributionMode === 'kde'
                ? kernelDensity1d(distributionObservations, {
                    points: typeof layer.mark.options.samples === 'number' ? layer.mark.options.samples : 96,
                    ...(typeof layer.mark.options.bandwidth === 'number' && layer.mark.options.bandwidth > 0
                        ? { bandwidth: layer.mark.options.bandwidth }
                        : {}),
                })
                : null;
            const fields = densitySummary !== null ||
                (axis === 'y' &&
                    (layer.mark.type === 'histogram' ||
                        layer.mark.type === 'theme-river' ||
                        distributionMode === 'histogram' ||
                        distributionMode === 'kde' ||
                        distributionMode === 'ecdf' ||
                        distributionMode === 'ccdf'))
                ? []
                : [encoding.field];
            const rangeEncoding = axis === 'x' ? layer.encoding.x2 : layer.encoding.y2;
            if (rangeEncoding?.field !== undefined)
                fields.push(rangeEncoding.field);
            const stackFields = preparedSeriesStackFields(layer);
            const stackAxis = layer.mark.type === 'bar' && layer.mark.orientation === 'horizontal' ? 'x' : 'y';
            if (stackFields !== null &&
                axis === stackAxis &&
                table.has(stackFields.start) &&
                table.has(stackFields.end)) {
                fields.splice(0, fields.length, stackFields.start, stackFields.end);
            }
            if (axis === 'x' && (layer.mark.type === 'timeline' || layer.mark.type === 'gantt')) {
                const authoredDomain = layer.mark.options.domain;
                if (Array.isArray(authoredDomain) &&
                    authoredDomain.length === 2 &&
                    authoredDomain.every((value) => typeof value === 'number' && Number.isFinite(value))) {
                    fields.splice(0, fields.length);
                    min = Math.min(min, authoredDomain[0], authoredDomain[1]);
                    max = Math.max(max, authoredDomain[0], authoredDomain[1]);
                }
                else
                    fields.push(layer.mark.fields.end ?? 'end');
            }
            if (axis === 'x' && (layer.mark.type === 'lines' || layer.mark.type === 'custom')) {
                const x2 = layer.mark.fields.x2;
                if (x2 !== undefined)
                    fields.push(x2);
            }
            if (axis === 'y' && layer.mark.type === 'candlestick') {
                fields.push(layer.mark.fields.open ?? 'open', layer.mark.fields.high ?? 'high', layer.mark.fields.low ?? 'low', layer.mark.fields.close ?? encoding.field);
            }
            if (axis === 'y' && layer.mark.type === 'indicator' && technicalIndicator !== undefined) {
                const optionFields = Array.isArray(layer.mark.options.fields)
                    ? layer.mark.options.fields.filter((role) => typeof role === 'string' && technicalIndicator.outputs[role] !== undefined)
                    : [];
                const configuredFields = [
                    layer.mark.fields.middle,
                    layer.mark.fields.signal,
                    layer.mark.fields.secondary,
                ].filter((field) => field !== undefined && table.has(field));
                const renderedFields = optionFields.length > 0
                    ? optionFields.map((role) => role === 'value' ? layer.y.field : (layer.mark.fields[role] ?? role))
                    : configuredFields.length > 0
                        ? configuredFields
                        : [layer.y.field];
                for (const role of ['lower', 'upper']) {
                    if (technicalIndicator.outputs[role] !== undefined) {
                        renderedFields.push(layer.mark.fields[role] ?? role);
                    }
                }
                fields.splice(0, fields.length, ...new Set(renderedFields));
            }
            if (axis === 'y' &&
                (layer.mark.type === 'financial' ||
                    layer.mark.type === 'range' ||
                    layer.mark.type === 'bullet' ||
                    (layer.mark.type === 'indicator' && technicalIndicator === undefined) ||
                    layer.mark.type === 'volume-profile')) {
                fields.push(...Object.entries(layer.mark.fields).flatMap(([role, field]) => role.startsWith('__') ? [] : [field]));
                const optionFields = layer.mark.options.fields;
                if (Array.isArray(optionFields)) {
                    fields.push(...optionFields.filter((field) => typeof field === 'string' && field.trim() !== ''));
                }
            }
            if (axis === 'y' && (layer.mark.type === 'boxplot' || distributionMode === 'boxplot')) {
                const rawValueField = layer.mark.fields.value;
                if (rawValueField !== undefined)
                    fields.push(rawValueField);
                fields.push(layer.mark.fields.min ?? layer.mark.fields.low ?? 'min', layer.mark.fields.q1 ?? 'q1', layer.mark.fields.median ?? encoding.field, layer.mark.fields.q3 ?? 'q3', layer.mark.fields.max ?? layer.mark.fields.high ?? 'max');
            }
            if (axis === 'y' && (layer.mark.type === 'lines' || layer.mark.type === 'custom')) {
                const y2 = layer.mark.fields.y2;
                if (y2 !== undefined)
                    fields.push(y2);
            }
            if (axis === 'y' && layer.mark.type === 'interval') {
                fields.push(layer.mark.fields.low ?? 'low', layer.mark.fields.high ?? 'high');
            }
            if (axis === 'y' && layer.mark.type === 'diff') {
                fields.push(layer.mark.fields.baseline ?? layer.mark.fields.old ?? 'old', layer.mark.fields.comparison ?? layer.mark.fields.new ?? encoding.field);
            }
            if (layer.mark.type === 'image' && layer.mark.options.raster !== undefined) {
                const raster = layer.mark.options.raster;
                const extent = raster !== null && typeof raster === 'object' && !Array.isArray(raster)
                    ? raster.extent
                    : undefined;
                if (Array.isArray(extent) &&
                    extent.length === 4 &&
                    extent.every((value) => typeof value === 'number' && Number.isFinite(value))) {
                    const first = axis === 'x' ? extent[0] : extent[2];
                    const second = axis === 'x' ? extent[1] : extent[3];
                    fields.splice(0, fields.length);
                    min = Math.min(min, first, second);
                    max = Math.max(max, first, second);
                }
            }
            for (const field of new Set(fields)) {
                if (!table.has(field))
                    continue;
                const extent = table.extent(field, fieldType === 'temporal');
                if (extent !== null) {
                    min = Math.min(min, extent[0]);
                    max = Math.max(max, extent[1]);
                }
            }
            const intervalOrientation = layer.mark.options.orientation === 'horizontal' ? 'horizontal' : 'vertical';
            const rawInterval = layer.mark.type === 'interval' &&
                (layer.mark.options.rawEstimator === true || typeof layer.mark.options.kind === 'string');
            const intervalValueAxis = intervalOrientation === 'vertical' ? 'y' : 'x';
            if (rawInterval && axis === intervalValueAxis) {
                const categoryField = intervalOrientation === 'vertical' ? layer.x.field : layer.y.field;
                const valueField = intervalOrientation === 'vertical' ? layer.y.field : layer.x.field;
                const grouped = new Map();
                for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
                    const key = table.value(rowIndex, categoryField);
                    const value = table.numericValue(rowIndex, valueField);
                    if (key === null || key === undefined || value === null)
                        continue;
                    const signature = key instanceof Date ? key.toISOString() : String(key);
                    grouped.set(signature, [...(grouped.get(signature) ?? []), value]);
                }
                const authoredKind = layer.mark.options.kind;
                const kind = authoredKind === 'PI' ||
                    authoredKind === 'SE' ||
                    authoredKind === 'SD' ||
                    authoredKind === 'IQR' ||
                    authoredKind === 'HDI'
                    ? authoredKind
                    : 'CI';
                for (const values of grouped.values()) {
                    const interval = estimateInterval(values, {
                        kind,
                        confidence: typeof layer.mark.options.confidence === 'number'
                            ? layer.mark.options.confidence
                            : 0.95,
                        estimator: layer.mark.options.estimator === 'median' ? 'median' : 'mean',
                        orientation: intervalOrientation,
                    });
                    if (interval !== null) {
                        min = Math.min(min, interval.low);
                        max = Math.max(max, interval.high);
                    }
                }
            }
            if (densitySummary !== null) {
                if (axis === 'x') {
                    min = Math.min(min, densitySummary.domainMinimum);
                    max = Math.max(max, densitySummary.domainMaximum);
                }
                else {
                    min = Math.min(min, 0);
                    max = Math.max(max, densitySummary.maximumDensity);
                }
            }
            if (kde !== null && kde.points.length > 0) {
                if (axis === 'x') {
                    min = Math.min(min, kde.points[0].value);
                    max = Math.max(max, kde.points.at(-1).value);
                }
                else {
                    min = Math.min(min, 0);
                    max = Math.max(max, ...kde.points.map(({ density }) => density));
                }
            }
            if (axis === 'y' && (distributionMode === 'ecdf' || distributionMode === 'ccdf')) {
                min = Math.min(min, 0);
                max = Math.max(max, 1);
            }
            if (axis === 'y' && (layer.mark.type === 'histogram' || distributionMode === 'histogram')) {
                const binCount = Math.max(1, Math.min(100, Math.floor(typeof layer.mark.options.bins === 'number' ? layer.mark.options.bins : 10)));
                const sourceExtent = table.extent(layer.x.field, layer.x.type === 'temporal');
                if (sourceExtent !== null) {
                    const optionWeightField = layer.mark.options.weightField;
                    const weightField = layer.mark.fields.weight ??
                        (typeof optionWeightField === 'string' ? optionWeightField : undefined);
                    const observations = Array.from({ length: table.length }, (_, rowIndex) => {
                        const value = table.numericValue(rowIndex, layer.x.field);
                        const weight = weightField === undefined ? 1 : table.numericValue(rowIndex, weightField);
                        return value === null || weight === null || weight < 0
                            ? null
                            : { value, weight, rowIndex };
                    }).filter((value) => value !== null);
                    const normalizationOption = layer.mark.options.normalization;
                    const normalization = normalizationOption === 'probability' ||
                        normalizationOption === 'normalized' ||
                        layer.mark.options.normalized === true
                        ? 'probability'
                        : normalizationOption === 'density'
                            ? 'density'
                            : 'count';
                    const counts = weightedHistogram(observations, {
                        bins: binCount,
                        extent: sourceExtent,
                        normalization,
                        cumulative: layer.mark.options.cumulative === true,
                    }).map(({ value }) => value);
                    min = Math.min(min, 0);
                    max = Math.max(max, ...counts);
                }
            }
            if (axis === 'y' && layer.mark.type === 'waterfall') {
                let total = 0;
                min = Math.min(min, 0);
                max = Math.max(max, 0);
                for (let index = 0; index < table.length; index += 1) {
                    const value = table.numericValue(index, layer.y.field);
                    if (value === null)
                        continue;
                    const previous = total;
                    total += value;
                    min = Math.min(min, previous, total);
                    max = Math.max(max, previous, total);
                }
            }
            if (axis === 'y' && layer.mark.type === 'theme-river' && stackFields === null) {
                const totals = new Map();
                for (let index = 0; index < table.length; index += 1) {
                    const rawKey = table.value(index, layer.x.field);
                    const key = rawKey instanceof Date
                        ? `date:${rawKey.toISOString()}`
                        : `${typeof rawKey}:${String(rawKey ?? '')}`;
                    const value = table.numericValue(index, layer.y.field);
                    if (value === null)
                        continue;
                    totals.set(key, (totals.get(key) ?? 0) + Math.max(0, value));
                }
                let maximumTotal = 1;
                for (const total of totals.values())
                    maximumTotal = Math.max(maximumTotal, total);
                min = Math.min(min, -maximumTotal / 2);
                max = Math.max(max, maximumTotal / 2);
            }
            const zeroCompatible = !['log', 'logit', 'probit'].includes(encoding.scale.type ?? '');
            if (zeroCompatible &&
                (encoding.scale.zero === true ||
                    (axis === 'y' && layer.mark.options.missing === 'zero') ||
                    (axis === 'y' &&
                        (layer.mark.type === 'bar' ||
                            layer.mark.type === 'area' ||
                            layer.mark.type === 'bullet' ||
                            layer.mark.type === 'cylinder' ||
                            layer.mark.type === 'histogram' ||
                            distributionMode === 'histogram' ||
                            layer.mark.type === 'item' ||
                            layer.mark.type === 'lollipop' ||
                            layer.mark.type === 'packed-bubble' ||
                            layer.mark.type === 'pareto' ||
                            layer.mark.type === 'pictorial-bar' ||
                            layer.mark.type === 'pyramid' ||
                            layer.mark.type === 'solid-gauge' ||
                            layer.mark.type === 'stepped-area' ||
                            layer.mark.type === 'theme-river' ||
                            layer.mark.type === 'variable-pie' ||
                            layer.mark.type === 'variwide' ||
                            layer.mark.type === 'volume-profile' ||
                            layer.mark.type === 'waterfall')) ||
                    (axis === 'x' && layer.mark.type === 'bar' && layer.mark.orientation === 'horizontal'))) {
                includeZero = true;
            }
        }
        if (!Number.isFinite(min) || !Number.isFinite(max)) {
            throw new GraflumeError('INVALID_DATA', `No numeric values are available for the ${axis}-axis.`, {
                path: `$.layers[].${axis}.field`,
            });
        }
        if (includeZero && fieldType !== 'temporal') {
            min = Math.min(0, min);
            max = Math.max(0, max);
        }
        if (min === max) {
            const delta = min === 0 ? 1 : Math.abs(min) * 0.05;
            min -= delta;
            max += delta;
        }
        return [min, max];
    }
    function categoricalDomain(layers, axis) {
        const seen = new Set();
        const domain = [];
        for (const { layer, table } of layers) {
            const explicit = layer[axis].scale.domain;
            const rangeEncoding = axis === 'x' ? layer.encoding.x2 : layer.encoding.y2;
            const values = explicit?.map(String) ?? [
                ...table.unique(layer[axis].field),
                ...(rangeEncoding?.field === undefined ? [] : table.unique(rangeEncoding.field)),
            ];
            for (const value of values) {
                if (seen.has(value))
                    continue;
                seen.add(value);
                domain.push(value);
            }
        }
        return domain;
    }
    function resolveAxisScale(id, channel, layers, plot, domainWindow) {
        const fieldType = resolveCommonType(layers.map((layer) => (channel === 'x' ? layer.xType : layer.yType)), channel);
        const firstEncoding = layers[0]?.layer[channel];
        const requestedScaleTypes = new Set(layers
            .map((layer) => layer.layer[channel].scale.type)
            .filter((type) => type !== undefined));
        if (requestedScaleTypes.size > 1) {
            throw new GraflumeError('INCOMPATIBLE_SCALE', `Layers bound to ${id} request incompatible scale types: ${[...requestedScaleTypes].join(', ')}.`, { path: `$.layers[].${channel}.scale.type` });
        }
        const requestedScaleType = [...requestedScaleTypes][0];
        const family = typeFamily(fieldType);
        const continuousAxisTypes = new Set([
            'linear',
            'log',
            'symlog',
            'asinh',
            'pow',
            'sqrt',
            'time',
            'utc',
            'probability',
            'logit',
            'probit',
        ]);
        if ((requestedScaleType !== undefined &&
            !continuousAxisTypes.has(requestedScaleType) &&
            requestedScaleType !== 'band' &&
            requestedScaleType !== 'point') ||
            ((requestedScaleType === 'band' || requestedScaleType === 'point') &&
                family !== 'categorical') ||
            (requestedScaleType !== undefined &&
                continuousAxisTypes.has(requestedScaleType) &&
                requestedScaleType !== 'time' &&
                requestedScaleType !== 'utc' &&
                family !== 'numeric') ||
            ((requestedScaleType === 'time' || requestedScaleType === 'utc') && family !== 'temporal')) {
            throw new GraflumeError('INCOMPATIBLE_SCALE', `Scale type "${requestedScaleType}" is incompatible with the ${id} field type "${fieldType}".`, { path: `$.layers[].${channel}.scale.type` });
        }
        const categorical = requestedScaleType === 'band' || requestedScaleType === 'point' || family === 'categorical';
        let scale;
        let usesTradingScale = false;
        if (categorical) {
            const domain = categoricalDomain(layers, channel);
            scale = createPositionScale(firstEncoding?.scale ?? {}, {
                type: requestedScaleType === 'point' ? 'point' : 'band',
                domain,
                range: channel === 'x' ? [plot.x, plot.x + plot.width] : [plot.y, plot.y + plot.height],
            });
        }
        else {
            const normalRange = channel === 'x' ? [plot.x, plot.x + plot.width] : [plot.y + plot.height, plot.y];
            // Portable temporal encodings default to UTC so the same function-free
            // spec produces identical ticks and snapshots in every host time zone.
            // Authors who need civil-time semantics can still request `time`
            // explicitly; the registry keeps that scale's local-zone behavior.
            const type = requestedScaleType ?? (fieldType === 'temporal' ? 'utc' : 'linear');
            if (!continuousAxisTypes.has(type)) {
                throw new GraflumeError('INCOMPATIBLE_SCALE', `Scale type "${type}" cannot be used as a Cartesian position scale.`, { path: `$.layers[].${channel}.scale.type` });
            }
            const tradingLayer = channel === 'x' ? layers.find(({ layer }) => advancedCandlestick(layer)) : undefined;
            const resolvedTradingScale = tradingLayer === undefined || (type !== 'linear' && type !== 'time' && type !== 'utc')
                ? null
                : tradingScale(tradingLayer, normalRange, type, domainWindow);
            if (resolvedTradingScale === null) {
                scale = createPositionScale(firstEncoding?.scale ?? {}, {
                    type: type,
                    domain: numericDomain(layers, channel, fieldType),
                    range: normalRange,
                });
            }
            else {
                scale = resolvedTradingScale;
                usesTradingScale = true;
            }
        }
        if (domainWindow !== undefined && !usesTradingScale) {
            const domain = domainForAxisWindow(scale, domainWindow);
            const range = channel === 'x' ? [plot.x, plot.x + plot.width] : [plot.y + plot.height, plot.y];
            scale = createPositionScale({ ...(firstEncoding?.scale ?? {}), domain }, { type: scale.kind, domain, range });
        }
        return { id, channel, fieldType, scale };
    }
    function resolveScales(spec, plot, domainWindows = {}) {
        const preparedLayers = spec.layers
            .filter((layer) => layer.visible)
            .map((layer) => {
            const stacked = prepareSeriesStackLayer(layer);
            const preparedLayer = stacked?.layer ?? layer;
            const transformed = executeTransformsWithNamedLineage(preparedLayer.data, preparedLayer.transform, `layer:${layer.id}`);
            const indicator = prepareTechnicalIndicator(preparedLayer, transformed);
            const resolvedLayer = indicator.layer;
            const table = DataTable.from(indicator.result.data);
            return {
                layer: resolvedLayer,
                table,
                lineage: indicator.result.lineage,
                xType: resolvedLayer.x.type ?? inferFieldType(table, resolvedLayer.x.field),
                yType: resolvedLayer.y.type ?? inferFieldType(table, resolvedLayer.y.field),
                xAxisId: resolvedLayer.x.axisId,
                yAxisId: resolvedLayer.y.axisId,
                ...(indicator.calculation === null ? {} : { technicalIndicator: indicator.calculation }),
            };
        });
        if (preparedLayers.length === 0) {
            throw new GraflumeError('INVALID_SPEC', 'At least one visible layer is required.', {
                path: '$.layers',
            });
        }
        const grouped = new Map();
        for (const layer of preparedLayers) {
            for (const id of [layer.xAxisId, layer.yAxisId]) {
                const entries = grouped.get(id) ?? [];
                entries.push(layer);
                grouped.set(id, entries);
            }
        }
        const partialAxes = {};
        for (const [id, entries] of grouped) {
            if (entries === undefined || entries.length === 0)
                continue;
            const channel = entries[0].xAxisId === id ? 'x' : 'y';
            if (entries.some((entry) => (channel === 'x' ? entry.xAxisId : entry.yAxisId) !== id)) {
                throw new GraflumeError('INVALID_SPEC', `Axis "${id}" is bound across Cartesian channels.`, {
                    path: '$.layers',
                });
            }
            partialAxes[id] = resolveAxisScale(id, channel, entries, plot, domainWindows[id]);
        }
        const layers = preparedLayers.map((layer) => {
            const xResolved = partialAxes[layer.xAxisId];
            const yResolved = partialAxes[layer.yAxisId];
            if (xResolved === undefined || yResolved === undefined) {
                throw new GraflumeError('INVALID_SPEC', 'Unable to resolve layer axis scales.', {
                    path: `$.layers[${layer.layer.id}]`,
                });
            }
            return {
                ...layer,
                xScale: xResolved.scale,
                yScale: yResolved.scale,
            };
        });
        const axes = {};
        for (const [id, resolved] of Object.entries(partialAxes)) {
            if (resolved === undefined)
                continue;
            axes[id] = {
                ...resolved,
                layers: layers.filter((layer) => resolved.channel === 'x' ? layer.xAxisId === id : layer.yAxisId === id),
            };
        }
        const resolvedX = axes.x ?? Object.values(axes).find((axis) => axis?.channel === 'x');
        const resolvedY = axes.y ?? Object.values(axes).find((axis) => axis?.channel === 'y');
        if (resolvedX === undefined || resolvedY === undefined) {
            throw new GraflumeError('INVALID_SPEC', 'Both x and y scales are required.', {
                path: '$.layers',
            });
        }
        return {
            layers,
            axes,
            xType: resolvedX.fieldType,
            yType: resolvedY.fieldType,
            xScale: resolvedX.scale,
            yScale: resolvedY.scale,
        };
    }

    function createLayout(spec, width, height, theme, minimumInsets = {}, additionalInsets = {}) {
        const separatePlotMargin = theme.spacing.plotMargin !== undefined;
        const measuredTitleBlock = spec.title === undefined
            ? 0
            : theme.typography.titleSize +
                (spec.title.subtitle === undefined
                    ? theme.spacing.lg
                    : theme.typography.subtitleSize + theme.spacing.lg + theme.spacing.xs);
        const titleBlock = Math.max(measuredTitleBlock, theme.spacing.minimumTitleBlock ?? 0);
        const insets = {
            // A top axis shares the space between the chart heading and the plot. Keep
            // the caller's outer top padding for the heading, then reserve the measured
            // axis gutter inside it. The other sides retain the legacy contract where
            // the normalized padding already includes the primary-axis gutter.
            top: spec.padding.top +
                Math.max(0, minimumInsets.top ?? 0) +
                Math.max(0, additionalInsets.top ?? 0),
            right: (separatePlotMargin
                ? spec.padding.right + Math.max(0, minimumInsets.right ?? 0)
                : Math.max(spec.padding.right, minimumInsets.right ?? 0)) +
                Math.max(0, additionalInsets.right ?? 0),
            bottom: (separatePlotMargin
                ? spec.padding.bottom + Math.max(0, minimumInsets.bottom ?? 0)
                : Math.max(spec.padding.bottom, minimumInsets.bottom ?? 0)) +
                Math.max(0, additionalInsets.bottom ?? 0),
            left: (separatePlotMargin
                ? spec.padding.left + Math.max(0, minimumInsets.left ?? 0)
                : Math.max(spec.padding.left, minimumInsets.left ?? 0)) +
                Math.max(0, additionalInsets.left ?? 0),
        };
        const plotX = insets.left;
        const plotY = insets.top + titleBlock;
        const plotWidth = Math.max(1, width - insets.left - insets.right);
        const plotHeight = Math.max(1, height - plotY - insets.bottom);
        return {
            width,
            height,
            plot: { x: plotX, y: plotY, width: plotWidth, height: plotHeight },
            insets,
            titleY: spec.padding.top,
            subtitleY: spec.padding.top + theme.typography.titleSize + theme.spacing.xs,
        };
    }

    function union$2(left, right) {
        if (left === null)
            return right;
        if (right === null)
            return left;
        const x = Math.min(left.x, right.x);
        const y = Math.min(left.y, right.y);
        const endX = Math.max(left.x + left.width, right.x + right.width);
        const endY = Math.max(left.y + left.height, right.y + right.height);
        return { x, y, width: endX - x, height: endY - y };
    }
    function textBounds(node) {
        const width = Math.max(node.fontSize * 0.6, Array.from(node.text).length * node.fontSize * 0.6);
        const height = Math.max(1, node.fontSize * 1.2);
        const left = node.align === 'center'
            ? -width / 2
            : node.align === 'right' || node.align === 'end'
                ? -width
                : 0;
        const top = node.baseline === 'middle'
            ? -height / 2
            : node.baseline === 'bottom' || node.baseline === 'ideographic'
                ? -height
                : node.baseline === 'alphabetic'
                    ? -height * 0.8
                    : 0;
        if (node.rotation === 0)
            return { x: node.x + left, y: node.y + top, width, height };
        const angle = (node.rotation * Math.PI) / 180;
        const corners = [
            [left, top],
            [left + width, top],
            [left + width, top + height],
            [left, top + height],
        ];
        const points = corners.map(([x, y]) => ({
            x: node.x + x * Math.cos(angle) - y * Math.sin(angle),
            y: node.y + x * Math.sin(angle) + y * Math.cos(angle),
        }));
        const xs = points.map((point) => point.x);
        const ys = points.map((point) => point.y);
        const x = Math.min(...xs);
        const y = Math.min(...ys);
        return { x, y, width: Math.max(...xs) - x, height: Math.max(...ys) - y };
    }
    function sceneNodeBounds(node) {
        if (!node.visible || node.opacity <= 0)
            return null;
        switch (node.type) {
            case 'circle':
                return {
                    x: node.cx - node.radius,
                    y: node.cy - node.radius,
                    width: node.radius * 2,
                    height: node.radius * 2,
                };
            case 'rect':
                return {
                    x: Math.min(node.x, node.x + node.width),
                    y: Math.min(node.y, node.y + node.height),
                    width: Math.abs(node.width),
                    height: Math.abs(node.height),
                };
            case 'line': {
                const padding = Math.max(0.5, node.lineWidth / 2);
                const x = Math.min(node.x1, node.x2) - padding;
                const y = Math.min(node.y1, node.y2) - padding;
                return {
                    x,
                    y,
                    width: Math.abs(node.x2 - node.x1) + padding * 2,
                    height: Math.abs(node.y2 - node.y1) + padding * 2,
                };
            }
            case 'path': {
                const points = [node.points, ...(node.subpaths ?? [])].flat();
                if (points.length === 0)
                    return null;
                const padding = Math.max(0, node.lineWidth / 2);
                const xs = points.map((point) => point.x);
                const ys = points.map((point) => point.y);
                const x = Math.min(...xs) - padding;
                const y = Math.min(...ys) - padding;
                return {
                    x,
                    y,
                    width: Math.max(...xs) - Math.min(...xs) + padding * 2,
                    height: Math.max(...ys) - Math.min(...ys) + padding * 2,
                };
            }
            case 'text':
                return textBounds(node);
            case 'group': {
                let bounds = null;
                for (const child of node.children)
                    bounds = union$2(bounds, sceneNodeBounds(child));
                if (bounds === null || node.clip === undefined)
                    return bounds;
                const x = Math.max(bounds.x, node.clip.x);
                const y = Math.max(bounds.y, node.clip.y);
                const endX = Math.min(bounds.x + bounds.width, node.clip.x + node.clip.width);
                const endY = Math.min(bounds.y + bounds.height, node.clip.y + node.clip.height);
                return endX < x || endY < y ? null : { x, y, width: endX - x, height: endY - y };
            }
        }
    }
    function nodePaint(node) {
        if (node.type === 'rect' || node.type === 'circle' || node.type === 'path') {
            return node.fill ?? node.stroke;
        }
        if (node.type === 'line')
            return node.stroke;
        if (node.type === 'text')
            return node.fill;
        for (const child of node.children) {
            const paint = nodePaint(child);
            if (paint !== undefined)
                return paint;
        }
        return undefined;
    }

    function intersectionArea(left, right) {
        const width = Math.max(0, Math.min(left.x + left.width, right.x + right.width) - Math.max(left.x, right.x));
        const height = Math.max(0, Math.min(left.y + left.height, right.y + right.height) - Math.max(left.y, right.y));
        return width * height;
    }
    function overlapArea(bounds, obstacles) {
        return obstacles.reduce((sum, obstacle) => sum + intersectionArea(bounds, obstacle), 0);
    }
    function outsideArea(bounds, boundary) {
        return Math.max(0, bounds.width * bounds.height - intersectionArea(bounds, boundary));
    }
    function clampBounds(bounds, boundary) {
        const minimumX = boundary.x;
        const minimumY = boundary.y;
        const maximumX = Math.max(minimumX, boundary.x + boundary.width - bounds.width);
        const maximumY = Math.max(minimumY, boundary.y + boundary.height - bounds.height);
        return {
            ...bounds,
            x: Math.max(minimumX, Math.min(maximumX, bounds.x)),
            y: Math.max(minimumY, Math.min(maximumY, bounds.y)),
        };
    }
    function candidateBounds(placement, alignment, options) {
        const gap = options.gap ?? 18;
        const targetCenterX = options.target.x + options.target.width / 2;
        const targetCenterY = options.target.y + options.target.height / 2;
        let x = targetCenterX - options.width / 2;
        let y = targetCenterY - options.height / 2;
        if (placement === 'top' || placement === 'bottom') {
            x += alignment * Math.max(options.width * 0.58, options.target.width / 2 + gap);
            y =
                placement === 'top'
                    ? options.target.y - options.height - gap
                    : options.target.y + options.target.height + gap;
        }
        else {
            y += alignment * Math.max(options.height * 0.58, options.target.height / 2 + gap);
            x =
                placement === 'left'
                    ? options.target.x - options.width - gap
                    : options.target.x + options.target.width + gap;
        }
        return {
            x: x + (options.offsetX ?? 0),
            y: y + (options.offsetY ?? 0),
            width: options.width,
            height: options.height,
        };
    }
    function score(candidate, options) {
        const rawOverflow = outsideArea(candidate.rawBounds, options.boundary);
        const protectedOverlap = overlapArea(candidate.bounds, options.protectedObstacles ?? []);
        const occupiedOverlap = overlapArea(candidate.bounds, options.occupiedCallouts ?? []);
        const targetOverlap = intersectionArea(candidate.bounds, options.target);
        const dataOverlap = overlapArea(candidate.bounds, options.dataObstacles ?? []);
        const targetCenterX = options.target.x + options.target.width / 2;
        const targetCenterY = options.target.y + options.target.height / 2;
        const candidateCenterX = candidate.bounds.x + candidate.bounds.width / 2;
        const candidateCenterY = candidate.bounds.y + candidate.bounds.height / 2;
        const distance = Math.hypot(candidateCenterX - targetCenterX, candidateCenterY - targetCenterY);
        return (rawOverflow * 1_000_000 +
            occupiedOverlap * 80_000 +
            protectedOverlap * 60_000 +
            targetOverlap * 40_000 +
            dataOverlap * 16 +
            distance * 0.01 +
            candidate.order * 0.0001);
    }
    function explicitIsUnsafe(candidate, options) {
        const area = Math.max(1, candidate.bounds.width * candidate.bounds.height);
        return (outsideArea(candidate.rawBounds, options.boundary) > 0.5 ||
            overlapArea(candidate.bounds, options.protectedObstacles ?? []) > area * 0.08 ||
            overlapArea(candidate.bounds, options.occupiedCallouts ?? []) > area * 0.08 ||
            intersectionArea(candidate.bounds, options.target) > area * 0.25 ||
            overlapArea(candidate.bounds, options.dataObstacles ?? []) > area * 0.72);
    }
    /**
     * Select a deterministic, renderer-neutral perimeter position for a callout.
     * Authored cardinal placement wins while it remains in bounds and avoids a
     * severe collision; `auto` and unsafe authored positions use the lowest score.
     */
    function placeCallout(options) {
        const targetCenterX = options.target.x + options.target.width / 2;
        const targetCenterY = options.target.y + options.target.height / 2;
        const boundaryCenterX = options.boundary.x + options.boundary.width / 2;
        const boundaryCenterY = options.boundary.y + options.boundary.height / 2;
        const horizontalFirst = targetCenterX <= boundaryCenterX ? ['right', 'left'] : ['left', 'right'];
        const verticalFirst = targetCenterY <= boundaryCenterY ? ['bottom', 'top'] : ['top', 'bottom'];
        const preferred = options.placement === undefined ? 'auto' : options.placement;
        const placements = preferred === 'auto'
            ? [horizontalFirst[0], verticalFirst[0], horizontalFirst[1], verticalFirst[1]]
            : [preferred, ...[...horizontalFirst, ...verticalFirst].filter((item) => item !== preferred)];
        const candidates = [];
        let order = 0;
        for (const placement of placements) {
            for (const alignment of [0, -1, 1]) {
                const rawBounds = candidateBounds(placement, alignment, options);
                const bounds = clampBounds(rawBounds, options.boundary);
                candidates.push({
                    x: bounds.x,
                    y: bounds.y,
                    bounds,
                    placement,
                    rawBounds,
                    order,
                });
                order += 1;
            }
        }
        const explicit = candidates[0];
        if (preferred !== 'auto' && !explicitIsUnsafe(explicit, options)) {
            return {
                x: explicit.x,
                y: explicit.y,
                bounds: explicit.bounds,
                placement: explicit.placement,
            };
        }
        const best = candidates.reduce((winner, candidate) => score(candidate, options) < score(winner, options) ? candidate : winner);
        return { x: best.x, y: best.y, bounds: best.bounds, placement: best.placement };
    }

    function cloneAnnotation$1(annotation) {
        return {
            ...annotation,
            target: { ...annotation.target },
            ...(typeof annotation.connector === 'object'
                ? { connector: { ...annotation.connector, dash: [...(annotation.connector.dash ?? [])] } }
                : {}),
            ...(annotation.style === undefined ? {} : { style: { ...annotation.style } }),
        };
    }
    function snapshotKey(annotations) {
        return JSON.stringify(annotations);
    }
    class AnnotationAuthoringHistory {
        #annotations;
        #undo = [];
        #redo = [];
        #limit;
        constructor(annotations = [], limit = 50) {
            this.#annotations = annotations.map(cloneAnnotation$1);
            this.#limit = Math.max(1, Math.trunc(limit));
        }
        reset(annotations) {
            this.#annotations = annotations.map(cloneAnnotation$1);
            this.#undo = [];
            this.#redo = [];
        }
        annotations() {
            return this.#annotations.map(cloneAnnotation$1);
        }
        replace(annotations) {
            const next = annotations.map(cloneAnnotation$1);
            if (snapshotKey(next) === snapshotKey(this.#annotations))
                return false;
            this.#undo.push(this.annotations());
            if (this.#undo.length > this.#limit)
                this.#undo.splice(0, this.#undo.length - this.#limit);
            this.#annotations = next;
            this.#redo = [];
            return true;
        }
        preview(annotations) {
            const next = annotations.map(cloneAnnotation$1);
            if (snapshotKey(next) === snapshotKey(this.#annotations))
                return false;
            this.#annotations = next;
            return true;
        }
        commit(previous) {
            if (snapshotKey(previous) === snapshotKey(this.#annotations))
                return false;
            this.#undo.push(previous.map(cloneAnnotation$1));
            if (this.#undo.length > this.#limit)
                this.#undo.splice(0, this.#undo.length - this.#limit);
            this.#redo = [];
            return true;
        }
        restore(annotations) {
            this.#annotations = annotations.map(cloneAnnotation$1);
        }
        update(id, patch) {
            const index = this.#annotations.findIndex((annotation) => annotation.id === id);
            if (index < 0)
                return false;
            return this.replace(this.#annotations.map((annotation, candidate) => candidate === index
                ? cloneAnnotation$1({ ...annotation, ...patch, id })
                : annotation));
        }
        undo() {
            const previous = this.#undo.pop();
            if (previous === undefined)
                return false;
            this.#redo.push(this.annotations());
            this.#annotations = previous.map(cloneAnnotation$1);
            return true;
        }
        redo() {
            const next = this.#redo.pop();
            if (next === undefined)
                return false;
            this.#undo.push(this.annotations());
            this.#annotations = next.map(cloneAnnotation$1);
            return true;
        }
        canUndo() {
            return this.#undo.length > 0;
        }
        canRedo() {
            return this.#redo.length > 0;
        }
    }
    /** Nine pointer targets: the callout body plus eight deterministic resize handles. */
    function annotationAuthoringHandles(bounds) {
        const left = bounds.x;
        const centerX = bounds.x + bounds.width / 2;
        const right = bounds.x + bounds.width;
        const top = bounds.y;
        const centerY = bounds.y + bounds.height / 2;
        const bottom = bounds.y + bounds.height;
        return Object.freeze([
            { id: 'move', x: centerX, y: centerY, cursor: 'move' },
            { id: 'north', x: centerX, y: top, cursor: 'ns-resize' },
            { id: 'north-east', x: right, y: top, cursor: 'nesw-resize' },
            { id: 'east', x: right, y: centerY, cursor: 'ew-resize' },
            { id: 'south-east', x: right, y: bottom, cursor: 'nwse-resize' },
            { id: 'south', x: centerX, y: bottom, cursor: 'ns-resize' },
            { id: 'south-west', x: left, y: bottom, cursor: 'nesw-resize' },
            { id: 'west', x: left, y: centerY, cursor: 'ew-resize' },
            { id: 'north-west', x: left, y: top, cursor: 'nwse-resize' },
        ]);
    }
    function hitTestAnnotationHandle(bounds, x, y, tolerance = 8) {
        const handles = annotationAuthoringHandles(bounds);
        const resize = handles
            .slice(1)
            .find((handle) => Math.hypot(handle.x - x, handle.y - y) <= tolerance);
        if (resize !== undefined)
            return resize.id;
        return x >= bounds.x &&
            x <= bounds.x + bounds.width &&
            y >= bounds.y &&
            y <= bounds.y + bounds.height
            ? 'move'
            : null;
    }
    function snapped(value, grid) {
        return grid === false ? value : Math.round(value / grid) * grid;
    }
    /** Applies a pointer drag/resize as portable annotation offsets and maxWidth. */
    function editAnnotationByPointer(options) {
        const grid = options.grid ?? false;
        const minimumWidth = Math.max(24, options.minimumWidth ?? 48);
        if (options.handle === 'move') {
            return cloneAnnotation$1({
                ...options.annotation,
                offsetX: snapped((options.annotation.offsetX ?? 0) + options.deltaX, grid),
                offsetY: snapped((options.annotation.offsetY ?? 0) + options.deltaY, grid),
            });
        }
        const west = options.handle.includes('west');
        const east = options.handle.includes('east');
        const north = options.handle.includes('north');
        const south = options.handle.includes('south');
        const widthDelta = east ? options.deltaX : west ? -options.deltaX : 0;
        const nextWidth = snapped(Math.max(minimumWidth, (options.annotation.style?.maxWidth ?? options.bounds.width) + widthDelta), grid);
        return cloneAnnotation$1({
            ...options.annotation,
            offsetX: (options.annotation.offsetX ?? 0) +
                (west ? options.deltaX / 2 : east ? options.deltaX / 2 : 0),
            offsetY: (options.annotation.offsetY ?? 0) + (north ? options.deltaY : south ? options.deltaY : 0),
            style: { ...options.annotation.style, maxWidth: Math.max(minimumWidth, nextWidth) },
        });
    }
    /** Arrow-key authoring mirrors pointer move; Alt+horizontal arrows resize the label. */
    function editAnnotationByKeyboard(options) {
        const step = Math.max(0.1, options.step ?? 1) * (options.coarse === true ? 10 : 1);
        const horizontal = options.key === 'ArrowLeft' ? -step : options.key === 'ArrowRight' ? step : 0;
        const vertical = options.key === 'ArrowUp' ? -step : options.key === 'ArrowDown' ? step : 0;
        if (options.resize === true && horizontal !== 0) {
            const minimumWidth = Math.max(24, options.minimumWidth ?? 48);
            return cloneAnnotation$1({
                ...options.annotation,
                style: {
                    ...options.annotation.style,
                    maxWidth: Math.max(minimumWidth, (options.annotation.style?.maxWidth ?? 220) + horizontal),
                },
            });
        }
        return cloneAnnotation$1({
            ...options.annotation,
            offsetX: (options.annotation.offsetX ?? 0) + horizontal,
            offsetY: (options.annotation.offsetY ?? 0) + vertical,
        });
    }

    const defaultDefinitions = [
        {
            id: 'callout',
            targetTypes: ['datum', 'layer', 'range', 'plot'],
            resizable: true,
            description: 'Collision-aware text bubble with an optional connector.',
        },
        {
            id: 'label',
            targetTypes: ['datum', 'layer', 'range', 'plot'],
            resizable: true,
            description: 'Unboxed data-coordinate text label.',
        },
        {
            id: 'point',
            targetTypes: ['datum', 'plot'],
            resizable: false,
            description: 'Point emphasis marker with an attached label.',
        },
        {
            id: 'rule',
            targetTypes: ['datum', 'layer', 'range', 'plot'],
            resizable: false,
            description: 'Data-coordinate reference rule with an attached label.',
        },
        {
            id: 'band',
            targetTypes: ['layer', 'range', 'plot'],
            resizable: true,
            description: 'Data-coordinate highlighted region with an attached label.',
        },
    ];
    /** Closed, deterministic registry used by portable annotation compilation and authoring. */
    class AnnotationPrimitiveRegistry {
        #definitions = new Map();
        constructor(definitions = defaultDefinitions) {
            for (const definition of definitions)
                this.register(definition);
        }
        register(definition) {
            if (this.#definitions.has(definition.id)) {
                throw new GraflumeError('INVALID_SPEC', `Annotation primitive "${definition.id}" is already registered.`);
            }
            if (definition.targetTypes.length === 0) {
                throw new GraflumeError('INVALID_SPEC', `Annotation primitive "${definition.id}" requires at least one target type.`);
            }
            this.#definitions.set(definition.id, Object.freeze({ ...definition, targetTypes: Object.freeze([...definition.targetTypes]) }));
            return this;
        }
        resolve(id) {
            const definition = this.#definitions.get(id);
            if (definition === undefined) {
                throw new GraflumeError('INVALID_SPEC', `Unknown annotation primitive "${id}".`);
            }
            return definition;
        }
        validateTarget(id, target) {
            const definition = this.resolve(id);
            if (!definition.targetTypes.includes(target.type)) {
                throw new GraflumeError('INVALID_SPEC', `Annotation primitive "${id}" does not support a "${target.type}" target.`);
            }
        }
        catalog() {
            return Object.freeze([...this.#definitions.values()]);
        }
    }
    const annotationPrimitiveRegistry = new AnnotationPrimitiveRegistry();
    annotationPrimitiveRegistry.catalog();

    function union$1(left, right) {
        if (left === null)
            return right;
        if (right === null)
            return left;
        const x = Math.min(left.x, right.x);
        const y = Math.min(left.y, right.y);
        const endX = Math.max(left.x + left.width, right.x + right.width);
        const endY = Math.max(left.y + left.height, right.y + right.height);
        return { x, y, width: endX - x, height: endY - y };
    }
    function scalarEqual$1(left, right) {
        if (left instanceof Date) {
            return typeof right === 'string'
                ? left.toISOString() === right || left.toISOString().slice(0, 10) === right
                : typeof right === 'number' && left.getTime() === right;
        }
        return left === right;
    }
    function datumMatches(target, node) {
        const reference = node.datum;
        if (reference === undefined)
            return false;
        if (target.layerId !== undefined && reference.layerId !== target.layerId)
            return false;
        if (target.rowIndex !== undefined) {
            const rows = Array.isArray(target.rowIndex) ? target.rowIndex : [target.rowIndex];
            if (!rows.includes(reference.rowIndex))
                return false;
        }
        if (target.field !== undefined) {
            const sources = [reference.tooltip, reference.datum].filter((source) => source !== undefined);
            const matches = (source) => {
                const candidate = source[target.field];
                if (target.values !== undefined)
                    return target.values.some((value) => scalarEqual$1(candidate, value));
                return scalarEqual$1(candidate, target.value ?? null);
            };
            if (!sources.some(matches))
                return false;
        }
        return true;
    }
    function descendants$2(node, ancestorVisible = true) {
        const effectivelyVisible = ancestorVisible && node.visible && node.opacity > 0;
        if (!effectivelyVisible)
            return [];
        if (node.type !== 'group')
            return [node];
        return node.children.flatMap((child) => descendants$2(child, effectivelyVisible));
    }
    function nodesForTarget(target, layerGroups) {
        if (target.type === 'layer') {
            const groupNode = layerGroups.find((node) => node.id === `${target.layerId}:group`);
            return groupNode === undefined ? [] : descendants$2(groupNode);
        }
        if (target.type !== 'datum')
            return [];
        return layerGroups
            .flatMap((node) => descendants$2(node))
            .filter((node) => datumMatches(target, node));
    }
    function fallbackDatumBounds(target, scales, datumVisible) {
        let bounds = null;
        for (const layerData of scales.layers) {
            if (target.layerId !== undefined && target.layerId !== layerData.layer.id)
                continue;
            if (isAxislessLayer(layerData.layer))
                continue;
            for (let rowIndex = 0; rowIndex < layerData.table.length; rowIndex += 1) {
                const datum = layerData.table.row(rowIndex);
                if (datumVisible?.(layerData.layer.id, rowIndex, datum) === false)
                    continue;
                const synthetic = {
                    datum: {
                        layerId: layerData.layer.id,
                        rowIndex,
                        datum,
                    }};
                if (!datumMatches(target, synthetic))
                    continue;
                const xValue = layerData.table.value(rowIndex, layerData.layer.x.field);
                const yValue = layerData.table.value(rowIndex, layerData.layer.y.field);
                if (xValue === undefined ||
                    xValue === null ||
                    (typeof xValue === 'object' && !(xValue instanceof Date)) ||
                    typeof xValue === 'boolean' ||
                    yValue === undefined ||
                    yValue === null ||
                    (typeof yValue === 'object' && !(yValue instanceof Date)) ||
                    typeof yValue === 'boolean')
                    continue;
                const x = layerData.xScale.map(xValue);
                const y = layerData.yScale.map(yValue);
                if (!Number.isFinite(x) || !Number.isFinite(y))
                    continue;
                bounds = union$1(bounds, { x, y, width: 0, height: 0 });
            }
        }
        return bounds;
    }
    function rangeBounds(target, scales, plot) {
        let x1 = plot.x;
        let x2 = plot.x + plot.width;
        let y1 = plot.y;
        let y2 = plot.y + plot.height;
        if (target.x !== undefined) {
            const resolved = scales.axes[target.x.axis ?? 'x'];
            if (resolved === undefined)
                return null;
            const mappedFrom = resolved.scale.map(target.x.from);
            const mappedTo = resolved.scale.map(target.x.to);
            const padding = resolved.scale.bandwidth / 2;
            x1 = Math.min(mappedFrom, mappedTo) - padding;
            x2 = Math.max(mappedFrom, mappedTo) + padding;
        }
        if (target.y !== undefined) {
            const resolved = scales.axes[target.y.axis ?? 'y'];
            if (resolved === undefined)
                return null;
            const mappedFrom = resolved.scale.map(target.y.from);
            const mappedTo = resolved.scale.map(target.y.to);
            const padding = resolved.scale.bandwidth / 2;
            y1 = Math.min(mappedFrom, mappedTo) - padding;
            y2 = Math.max(mappedFrom, mappedTo) + padding;
        }
        if (![x1, x2, y1, y2].every(Number.isFinite))
            return null;
        return {
            x: Math.min(x1, x2),
            y: Math.min(y1, y2),
            width: Math.abs(x2 - x1),
            height: Math.abs(y2 - y1),
        };
    }
    function targetBounds(target, layerGroups, scales, plot, datumVisible) {
        if (target.type === 'range') {
            const bounds = rangeBounds(target, scales, plot);
            return { bounds: bounds ?? { x: 0, y: 0, width: 0, height: 0 }, found: bounds !== null };
        }
        if (target.type === 'plot') {
            return {
                bounds: {
                    x: plot.x + plot.width * target.x,
                    y: plot.y + plot.height * target.y,
                    width: plot.width * (target.width ?? 0),
                    height: plot.height * (target.height ?? 0),
                },
                found: true,
            };
        }
        let bounds = null;
        for (const node of nodesForTarget(target, layerGroups))
            bounds = union$1(bounds, sceneNodeBounds(node));
        if (bounds === null && target.type === 'datum')
            bounds = fallbackDatumBounds(target, scales, datumVisible);
        return { bounds: bounds ?? { x: 0, y: 0, width: 0, height: 0 }, found: bounds !== null };
    }
    function highlightNodes(highlight, index, resolution, theme, zIndex) {
        if (!resolution.found)
            return [];
        const padding = highlight.padding ?? 5;
        const bounds = resolution.bounds;
        const stroke = highlight.stroke ?? theme.colors.focus;
        const fill = highlight.fill ?? 'rgba(79,70,229,0.12)';
        const lineWidth = highlight.lineWidth ?? 2;
        const opacity = highlight.opacity ?? 1;
        const dash = [...(highlight.dash ?? [])];
        const id = highlight.id ?? `highlight-${index}`;
        if (bounds.width <= 2 && bounds.height <= 2) {
            return [
                {
                    type: 'circle',
                    ...nodeBase(`decoration:${id}`, { zIndex, opacity }),
                    cx: bounds.x + bounds.width / 2,
                    cy: bounds.y + bounds.height / 2,
                    radius: highlight.radius ?? Math.max(7, padding + 3),
                    fill,
                    stroke,
                    lineWidth,
                },
            ];
        }
        return [
            {
                type: 'rect',
                ...nodeBase(`decoration:${id}`, { zIndex, opacity }),
                x: bounds.x - padding,
                y: bounds.y - padding,
                width: Math.max(1, bounds.width + padding * 2),
                height: Math.max(1, bounds.height + padding * 2),
                fill,
                stroke,
                lineWidth,
                cornerRadius: highlight.radius ?? 7,
                ...(dash.length === 0 ? {} : { dash }),
            },
        ];
    }
    function graphemes(text) {
        try {
            return [...new Intl.Segmenter(undefined, { granularity: 'grapheme' }).segment(text)].map(({ segment }) => segment);
        }
        catch {
            return Array.from(text);
        }
    }
    function graphemeWidth(value, fontSize) {
        // Canvas cannot measure text during compilation, and a consumer may provide
        // a wider custom face than the built-in theme. Keep the estimates
        // deliberately conservative so wrapping happens before a glyph reaches the
        // content edge. The extra factor also covers bold title text and bearings.
        const customFontSafety = 1.08;
        if (/^\s+$/u.test(value))
            return fontSize * 0.42 * customFontSafety;
        if (/\p{Extended_Pictographic}/u.test(value))
            return fontSize * 1.25 * customFontSafety;
        if (/^[\p{Script=Han}\p{Script=Hangul}\p{Script=Hiragana}\p{Script=Katakana}]$/u.test(value))
            return fontSize * 1.08 * customFontSafety;
        if (/^[\p{Script=Arabic}\p{Script=Hebrew}]$/u.test(value))
            return fontSize * 0.92 * customFontSafety;
        if (/^[WM]$/u.test(value))
            return fontSize * 1.05 * customFontSafety;
        if (/^[mw]$/u.test(value))
            return fontSize * 0.94 * customFontSafety;
        if (/^[ilI1|!.,:;'`]$/u.test(value))
            return fontSize * 0.48 * customFontSafety;
        if (/^[A-Z0-9]$/u.test(value))
            return fontSize * 0.82 * customFontSafety;
        if (/^[a-z]$/u.test(value))
            return fontSize * 0.72 * customFontSafety;
        return fontSize * customFontSafety;
    }
    function textWidth(text, fontSize) {
        return graphemes(text).reduce((sum, value) => sum + graphemeWidth(value, fontSize), 0);
    }
    function ellipsizeLine(text, maxWidth, fontSize) {
        const suffix = '…';
        if (textWidth(suffix, fontSize) > maxWidth)
            return '';
        const output = [...graphemes(text)];
        while (output.length > 0 && textWidth(`${output.join('')}${suffix}`, fontSize) > maxWidth)
            output.pop();
        return `${output.join('')}${suffix}`;
    }
    function wrapText(text, maxWidth, fontSize, maxLines = 6) {
        const words = text.trim().split(/\s+/u).filter(Boolean);
        const lines = [];
        let line = '';
        let truncated = false;
        const pushLine = () => {
            if (line === '')
                return true;
            if (lines.length >= maxLines) {
                truncated = true;
                return false;
            }
            lines.push(line);
            line = '';
            return true;
        };
        outer: for (const word of words) {
            const combined = line === '' ? word : `${line} ${word}`;
            if (textWidth(combined, fontSize) <= maxWidth) {
                line = combined;
                continue;
            }
            if (!pushLine())
                break;
            let chunk = '';
            for (const value of graphemes(word)) {
                if (chunk !== '' && textWidth(`${chunk}${value}`, fontSize) > maxWidth) {
                    line = chunk;
                    if (!pushLine())
                        break outer;
                    chunk = '';
                }
                if (chunk === '' && graphemeWidth(value, fontSize) > maxWidth) {
                    line = value;
                    if (!pushLine())
                        break outer;
                }
                else {
                    chunk += value;
                }
            }
            line = chunk;
        }
        if (line !== '') {
            if (lines.length < maxLines)
                lines.push(line);
            else
                truncated = true;
        }
        if (truncated && lines.length > 0)
            lines[lines.length - 1] = ellipsizeLine(lines[lines.length - 1], maxWidth, fontSize);
        return { lines, truncated };
    }
    function clippedBounds(bounds, clip) {
        const x = Math.max(bounds.x, clip.x);
        const y = Math.max(bounds.y, clip.y);
        const endX = Math.min(bounds.x + bounds.width, clip.x + clip.width);
        const endY = Math.min(bounds.y + bounds.height, clip.y + clip.height);
        if (endX < x || endY < y)
            return null;
        return { x, y, width: Math.max(2, endX - x), height: Math.max(2, endY - y) };
    }
    function segmentObstacleBounds(start, end, lineWidth, plot) {
        const cellWidth = Math.max(1, plot.width / 12);
        const cellHeight = Math.max(1, plot.height / 8);
        const horizontalSpan = Math.abs(end.x - start.x) / cellWidth;
        const verticalSpan = Math.abs(end.y - start.y) / cellHeight;
        const stepCount = Math.max(1, Math.min(32, Math.ceil(Math.max(horizontalSpan, verticalSpan) * 2)));
        const padding = Math.max(2, lineWidth / 2 + 1.25);
        const pieces = [];
        for (let step = 0; step < stepCount; step += 1) {
            const fromRatio = step / stepCount;
            const toRatio = (step + 1) / stepCount;
            const fromX = start.x + (end.x - start.x) * fromRatio;
            const fromY = start.y + (end.y - start.y) * fromRatio;
            const toX = start.x + (end.x - start.x) * toRatio;
            const toY = start.y + (end.y - start.y) * toRatio;
            pieces.push({
                x: Math.min(fromX, toX) - padding,
                y: Math.min(fromY, toY) - padding,
                width: Math.abs(toX - fromX) + padding * 2,
                height: Math.abs(toY - fromY) + padding * 2,
            });
        }
        return pieces;
    }
    function localObstacleBounds(node, plot) {
        if (node.type === 'line') {
            return segmentObstacleBounds({ x: node.x1, y: node.y1 }, { x: node.x2, y: node.y2 }, node.lineWidth, plot);
        }
        if (node.type !== 'path') {
            const bounds = sceneNodeBounds(node);
            return bounds === null ? [] : [bounds];
        }
        const pieces = [];
        for (const points of [node.points, ...(node.subpaths ?? [])]) {
            if (points.length === 1) {
                const point = points[0];
                const radius = Math.max(2, node.lineWidth / 2 + 1.25);
                pieces.push({
                    x: point.x - radius,
                    y: point.y - radius,
                    width: radius * 2,
                    height: radius * 2,
                });
                continue;
            }
            for (let index = 1; index < points.length; index += 1) {
                pieces.push(...segmentObstacleBounds(points[index - 1], points[index], node.lineWidth, plot));
            }
            if (node.closed && points.length > 2)
                pieces.push(...segmentObstacleBounds(points.at(-1), points[0], node.lineWidth, plot));
        }
        return pieces;
    }
    function dataObstacleBounds(layerGroups, plot) {
        const columns = 12;
        const rows = 8;
        const buckets = new Map();
        for (const node of layerGroups.flatMap((layer) => descendants$2(layer))) {
            for (const bounds of localObstacleBounds(node, plot)) {
                const clipped = clippedBounds(bounds, plot);
                if (clipped === null)
                    continue;
                const centerX = clipped.x + clipped.width / 2;
                const centerY = clipped.y + clipped.height / 2;
                const column = Math.max(0, Math.min(columns - 1, Math.floor(((centerX - plot.x) / Math.max(1, plot.width)) * columns)));
                const row = Math.max(0, Math.min(rows - 1, Math.floor(((centerY - plot.y) / Math.max(1, plot.height)) * rows)));
                const key = row * columns + column;
                const prior = buckets.get(key);
                buckets.set(key, {
                    bounds: union$1(prior?.bounds ?? null, clipped) ?? clipped,
                    count: (prior?.count ?? 0) + 1,
                });
            }
        }
        return [...buckets.values()].flatMap(({ bounds, count }) => Array.from({ length: Math.min(8, Math.max(1, Math.ceil(Math.log2(count + 1)))) }, () => bounds));
    }
    function controlStripBounds(spec, width) {
        const controls = spec.interaction.controls;
        if (controls === false)
            return null;
        const buttonSize = width <= 560 ? 44 : 28;
        const height = width <= 560 ? 44 : 30;
        const buttonCount = (controls.zoom ? 2 : 0) +
            (controls.reset ? 1 : 0) +
            (controls.fullscreen ? 1 : 0) +
            (controls.export ? 1 : 0) +
            (controls.annotations ? 1 : 0) +
            (controls.playback ? 4 : 0);
        if (buttonCount === 0)
            return null;
        const navigationGroup = controls.zoom || controls.reset;
        const utilityGroup = controls.fullscreen || controls.export || controls.annotations;
        const separators = (navigationGroup && (utilityGroup || controls.playback) ? 1 : 0) +
            (utilityGroup && controls.playback ? 1 : 0);
        const stripWidth = Math.min(Math.max(1, width - 12), buttonCount * buttonSize + separators * 5 + 2);
        return { x: Math.max(0, width - stripWidth - 6), y: 6, width: stripWidth, height };
    }
    function annotationNodes(annotation, index, resolution, plot, theme, locale, dataObstacles, protectedObstacles, occupiedCallouts) {
        if (!resolution.found)
            return { nodes: [], bounds: null };
        const primitive = annotation.primitive ?? 'callout';
        annotationPrimitiveRegistry.validateTarget(primitive, annotation.target);
        const style = annotation.style ?? {};
        const availableWidth = Math.max(1, plot.width);
        const availableHeight = Math.max(1, plot.height);
        const maxWidth = Math.min(style.maxWidth ?? 220, availableWidth);
        const padding = Math.min(style.padding ?? 10, Math.max(0, Math.min(maxWidth / 5, availableHeight / 6)));
        const fontSize = Math.min(style.fontSize ?? 12, Math.max(1, Math.min(maxWidth / 3, availableHeight / 3)));
        const detailFontSize = Math.max(1, fontSize - 1);
        const maximumContentWidth = Math.max(1, maxWidth - padding * 2);
        const glyphGuard = Math.min(maximumContentWidth / 4, Math.max(0.75, fontSize * 0.12));
        const wrappingWidth = Math.max(1, maximumContentWidth - glyphGuard * 2);
        const wrappedTitle = wrapText(annotation.text, wrappingWidth, fontSize);
        const wrappedDetail = annotation.detail === undefined
            ? { lines: [], truncated: false }
            : wrapText(annotation.detail, wrappingWidth, detailFontSize);
        let titleLines = wrappedTitle.lines;
        let detailLines = wrappedDetail.lines;
        const lineHeight = fontSize * 1.35;
        const detailLineHeight = detailFontSize * 1.35;
        const lineBudget = Math.max(1, Math.floor((availableHeight - padding * 2) / detailLineHeight));
        titleLines = titleLines.slice(0, Math.max(1, Math.min(titleLines.length, lineBudget)));
        detailLines = detailLines.slice(0, Math.max(0, lineBudget - titleLines.length));
        const ellipsizeLast = (lines, truncated, lineFontSize) => {
            if (!truncated || lines.length === 0)
                return lines;
            const last = lines[lines.length - 1];
            return [
                ...lines.slice(0, -1),
                last.endsWith('…') ? last : ellipsizeLine(last, wrappingWidth, lineFontSize),
            ];
        };
        titleLines = ellipsizeLast(titleLines, wrappedTitle.truncated || titleLines.length < wrappedTitle.lines.length, fontSize);
        detailLines = ellipsizeLast(detailLines, wrappedDetail.truncated || detailLines.length < wrappedDetail.lines.length, detailFontSize);
        const longestWidth = Math.max(fontSize * 4, ...titleLines.map((line) => textWidth(line, fontSize)), ...detailLines.map((line) => textWidth(line, detailFontSize)));
        const width = Math.min(maxWidth, Math.max(Math.min(96, maxWidth), longestWidth + padding * 2 + glyphGuard * 2));
        const height = Math.min(availableHeight, padding * 2 +
            titleLines.length * lineHeight +
            detailLines.length * detailLineHeight +
            (detailLines.length > 0 ? 4 : 0));
        const target = resolution.bounds;
        const anchor = { x: target.x + target.width / 2, y: target.y + target.height / 2 };
        const placed = placeCallout({
            target,
            width,
            height,
            boundary: plot,
            placement: annotation.placement ?? 'auto',
            offsetX: annotation.offsetX ?? 0,
            offsetY: annotation.offsetY ?? 0,
            dataObstacles,
            protectedObstacles,
            occupiedCallouts,
        });
        const { x, y } = placed;
        const id = annotation.id ?? `annotation-${index}`;
        const bubbleCenter = { x: x + width / 2, y: y + height / 2 };
        const connector = typeof annotation.connector === 'object' ? annotation.connector : {};
        const connectorVisible = typeof annotation.connector === 'boolean' ? annotation.connector : (connector.visible ?? true);
        const nodes = [];
        const textNodes = [];
        const rtl = locale !== undefined && /^(ar|fa|he|ur)(?:-|$)/i.test(locale);
        const logicalAlign = style.align ?? 'start';
        const textAlign = logicalAlign === 'center'
            ? 'center'
            : logicalAlign === 'start'
                ? rtl
                    ? 'right'
                    : 'left'
                : rtl
                    ? 'left'
                    : 'right';
        const textX = logicalAlign === 'center'
            ? x + width / 2
            : textAlign === 'right'
                ? x + width - padding - glyphGuard
                : x + padding + glyphGuard;
        if (primitive === 'band') {
            nodes.push({
                type: 'rect',
                ...nodeBase(`annotation:${id}:band`, { zIndex: 699, opacity: style.opacity ?? 0.16 }),
                ...resolution.bounds,
                fill: style.background ?? theme.colors.focus,
                stroke: style.border ?? theme.colors.focus,
                lineWidth: 1.25,
                cornerRadius: 2,
            });
        }
        else if (primitive === 'rule') {
            const horizontal = target.width >= target.height;
            nodes.push({
                type: 'line',
                ...nodeBase(`annotation:${id}:rule`, { zIndex: 699, opacity: style.opacity ?? 0.92 }),
                x1: horizontal ? target.x : anchor.x,
                y1: horizontal ? anchor.y : target.y,
                x2: horizontal ? target.x + target.width : anchor.x,
                y2: horizontal ? anchor.y : target.y + target.height,
                stroke: style.border ?? theme.colors.focus,
                lineWidth: 1.75,
                lineCap: 'round',
            });
        }
        else if (primitive === 'point') {
            nodes.push({
                type: 'circle',
                ...nodeBase(`annotation:${id}:point`, { zIndex: 699, opacity: style.opacity ?? 1 }),
                cx: anchor.x,
                cy: anchor.y,
                radius: 5,
                fill: style.background ?? theme.colors.focus,
                stroke: style.border ?? theme.colors.surface,
                lineWidth: 2,
            });
        }
        if (connectorVisible && primitive !== 'label') {
            nodes.push({
                type: 'line',
                ...nodeBase(`annotation:${id}:connector`, { zIndex: 700 }),
                x1: anchor.x,
                y1: anchor.y,
                x2: bubbleCenter.x,
                y2: bubbleCenter.y,
                stroke: connector.color ?? style.border ?? theme.colors.focus,
                lineWidth: connector.width ?? 1.5,
                dash: [...(connector.dash ?? [])],
                lineCap: 'round',
            });
        }
        nodes.push({
            type: 'rect',
            ...nodeBase(`annotation:${id}:bubble`, { zIndex: 701, opacity: style.opacity ?? 0.97 }),
            x,
            y,
            width,
            height,
            ...(primitive === 'label' ? {} : { fill: style.background ?? theme.colors.surface }),
            ...(primitive === 'label' ? {} : { stroke: style.border ?? theme.colors.focus }),
            lineWidth: primitive === 'label' ? 0 : 1.25,
            cornerRadius: primitive === 'label' ? 0 : 9,
        });
        let textY = y + padding;
        for (const [lineIndex, text] of titleLines.entries()) {
            const node = {
                type: 'text',
                ...nodeBase(`annotation:${id}:title:${lineIndex}`, { zIndex: 702 }),
                x: textX,
                y: textY,
                text,
                fill: style.color ?? theme.colors.text,
                fontFamily: theme.typography.fontFamily,
                fontSize,
                fontWeight: 700,
                align: textAlign,
                baseline: 'top',
                rotation: 0,
            };
            textNodes.push(node);
            textY += lineHeight;
        }
        if (detailLines.length > 0)
            textY += 4;
        for (const [lineIndex, text] of detailLines.entries()) {
            textNodes.push({
                type: 'text',
                ...nodeBase(`annotation:${id}:detail:${lineIndex}`, { zIndex: 702 }),
                x: textX,
                y: textY,
                text,
                fill: style.color ?? theme.colors.mutedText,
                fontFamily: theme.typography.fontFamily,
                fontSize: detailFontSize,
                fontWeight: 400,
                align: textAlign,
                baseline: 'top',
                rotation: 0,
            });
            textY += detailLineHeight;
        }
        if (textNodes.length > 0)
            nodes.push(group(`annotation:${id}:content`, textNodes, {
                zIndex: 702,
                clip: {
                    x: x + padding,
                    y: y + padding,
                    width: Math.max(0, width - padding * 2),
                    height: Math.max(0, height - padding * 2),
                },
            }));
        return { nodes, bounds: placed.bounds };
    }
    function compileDecorations(options) {
        const underlay = [];
        const overlay = [];
        const annotationEntries = [];
        options.spec.highlights.forEach((highlight, index) => {
            const resolution = targetBounds(highlight.target, options.layerGroups, options.scales, options.plot, options.datumVisible);
            const nodes = highlightNodes(highlight, index, resolution, options.theme, highlight.target.type === 'range' || highlight.target.type === 'plot' ? -200 : 600);
            if (highlight.target.type === 'range' || highlight.target.type === 'plot')
                underlay.push(...nodes);
            else
                overlay.push(...nodes);
        });
        if (options.spec.interaction.selection !== false) {
            const selectionStyle = options.spec.interaction.selection.highlight;
            for (const [index, target] of (options.runtime?.selection ?? []).entries()) {
                const resolution = targetBounds(target, options.layerGroups, options.scales, options.plot, options.datumVisible);
                overlay.push(...highlightNodes({ id: `selection-${index}`, ...selectionStyle }, index, resolution, options.theme, 650));
            }
        }
        const annotations = options.runtime?.annotations ?? options.spec.annotations;
        if (options.runtime?.annotationsVisible === false)
            return {
                underlay: underlay.length === 0
                    ? []
                    : [group('decorations:underlay', underlay, { zIndex: -200, clip: options.plot })],
                overlay: overlay.length === 0 ? [] : [group('decorations:overlay', overlay, { zIndex: 600 })],
                annotations: [],
            };
        const dataObstacles = dataObstacleBounds(options.layerGroups, options.plot);
        const protectedObstacles = [
            options.legendBounds,
            controlStripBounds(options.spec, options.width),
        ].filter((bounds) => bounds !== undefined && bounds !== null);
        const occupiedCallouts = [];
        annotations.forEach((annotation, index) => {
            const resolution = targetBounds(annotation.target, options.layerGroups, options.scales, options.plot, options.datumVisible);
            const compiled = annotationNodes(annotation, index, resolution, options.plot, options.theme, options.spec.locale, dataObstacles, protectedObstacles, occupiedCallouts);
            overlay.push(...compiled.nodes);
            if (compiled.bounds !== null) {
                occupiedCallouts.push(compiled.bounds);
                const primitive = annotation.primitive ?? 'callout';
                annotationEntries.push({
                    id: annotation.id ?? `annotation-${index}`,
                    primitive,
                    bounds: compiled.bounds,
                    targetBounds: resolution.bounds,
                    resizable: annotationPrimitiveRegistry.resolve(primitive).resizable,
                });
            }
        });
        const active = annotationEntries.find(({ id }) => id === options.runtime?.activeAnnotationId);
        if (active !== undefined) {
            overlay.push({
                type: 'rect',
                ...nodeBase(`annotation:${active.id}:authoring-outline`, { zIndex: 710 }),
                ...active.bounds,
                stroke: options.theme.colors.focus,
                lineWidth: 1.25,
                dash: [4, 3],
                cornerRadius: 4,
            });
            overlay.push(...annotationAuthoringHandles(active.bounds)
                .filter(({ id }) => id !== 'move' && active.resizable)
                .map(({ id, x, y }) => ({
                type: 'rect',
                ...nodeBase(`annotation:${active.id}:handle:${id}`, { zIndex: 711 }),
                x: x - 3.5,
                y: y - 3.5,
                width: 7,
                height: 7,
                fill: options.theme.colors.surface,
                stroke: options.theme.colors.focus,
                lineWidth: 1.25,
                cornerRadius: 1,
            })));
        }
        return {
            underlay: underlay.length === 0
                ? []
                : [group('decorations:underlay', underlay, { zIndex: -200, clip: options.plot })],
            overlay: overlay.length === 0 ? [] : [group('decorations:overlay', overlay, { zIndex: 600 })],
            annotations: annotationEntries,
        };
    }

    const automaticLabelValue = '__graflumeAutomaticLabelValue';
    const automaticLabelId = '__graflumeAutomaticLabelId';
    function union(left, right) {
        if (left === null)
            return right;
        if (right === null)
            return left;
        const x = Math.min(left.x, right.x);
        const y = Math.min(left.y, right.y);
        const endX = Math.max(left.x + left.width, right.x + right.width);
        const endY = Math.max(left.y + left.height, right.y + right.height);
        return { x, y, width: endX - x, height: endY - y };
    }
    function descendants$1(node, ancestorVisible = true) {
        const visible = ancestorVisible && node.visible && node.opacity > 0;
        if (!visible)
            return [];
        if (node.type !== 'group')
            return [node];
        return node.children.flatMap((child) => descendants$1(child, visible));
    }
    function collectDatumGeometry(layerGroups, scales, maximum, locale) {
        const semantic = buildSemanticIndex(group('mark-labels:semantic-source', layerGroups), scales.layers, maximum, locale);
        if (semantic.length > 0) {
            return semantic.map((mark) => ({
                layerId: mark.layerId,
                rowIndex: mark.rowIndex,
                datum: mark.datum,
                bounds: mark.bounds,
            }));
        }
        const byDatum = new Map();
        for (const node of layerGroups.flatMap((candidate) => descendants$1(candidate))) {
            const reference = node.datum;
            if (reference === undefined)
                continue;
            const bounds = sceneNodeBounds(node);
            if (bounds === null)
                continue;
            const key = JSON.stringify([reference.layerId, reference.rowIndex]);
            const current = byDatum.get(key);
            const datum = { ...reference.datum, ...reference.tooltip };
            if (current === undefined) {
                byDatum.set(key, {
                    layerId: reference.layerId,
                    rowIndex: reference.rowIndex,
                    datum,
                    bounds,
                });
            }
            else
                current.bounds = union(current.bounds, bounds);
        }
        return [...byDatum.values()].flatMap((entry) => entry.bounds === null ? [] : [{ ...entry, bounds: entry.bounds }]);
    }
    function jsonPrimitive(value) {
        return (value === null ||
            typeof value === 'string' ||
            typeof value === 'boolean' ||
            (typeof value === 'number' && Number.isFinite(value)));
    }
    function scalarEqual(left, right) {
        if (left instanceof Date) {
            return typeof right === 'string'
                ? left.toISOString() === right || left.toISOString().slice(0, 10) === right
                : typeof right === 'number' && left.getTime() === right;
        }
        return Object.is(left, right);
    }
    function markLabelTargetMatches(target, candidate) {
        if (target.layerId !== undefined && target.layerId !== candidate.layerId)
            return false;
        if (target.rowIndex !== undefined) {
            const rows = Array.isArray(target.rowIndex) ? target.rowIndex : [target.rowIndex];
            if (!rows.includes(candidate.rowIndex))
                return false;
        }
        if (target.field !== undefined) {
            const value = candidate.datum[target.field];
            const values = target.values ?? (target.value === undefined ? [] : [target.value]);
            if (!values.some((entry) => scalarEqual(value, entry)))
                return false;
        }
        return target.rowIndex !== undefined || target.field !== undefined;
    }
    function targetForDatum(geometry, keyField) {
        const automaticId = geometry.datum[automaticLabelId];
        if (jsonPrimitive(automaticId)) {
            return {
                type: 'datum',
                layerId: geometry.layerId,
                field: automaticLabelId,
                value: automaticId,
            };
        }
        const value = keyField === undefined ? undefined : geometry.datum[keyField];
        return keyField !== undefined && jsonPrimitive(value)
            ? { type: 'datum', layerId: geometry.layerId, field: keyField, value }
            : { type: 'datum', layerId: geometry.layerId, rowIndex: geometry.rowIndex };
    }
    function seriesAutomaticGeometry(input, spec) {
        const output = [];
        for (const layer of spec.layers) {
            const layerRows = input.filter(({ layerId }) => layerId === layer.id);
            const seriesField = layer.mark.fields.series;
            if (seriesField === undefined || (layer.mark.type !== 'area' && layer.mark.type !== 'bar')) {
                output.push(...layerRows);
                continue;
            }
            const derived = layerRows.filter(({ datum }) => typeof datum.stackSeries === 'string' &&
                typeof datum.stackValue === 'number' &&
                Number.isFinite(datum.stackValue));
            if (derived.length === 0) {
                output.push(...layerRows);
                continue;
            }
            if (layer.mark.type === 'area') {
                const endpoints = new Map();
                for (const row of derived) {
                    const series = String(row.datum.stackSeries);
                    const current = endpoints.get(series);
                    if (current === undefined ||
                        row.bounds.x + row.bounds.width > current.bounds.x + current.bounds.width) {
                        endpoints.set(series, row);
                    }
                }
                for (const [series, row] of endpoints) {
                    output.push({
                        ...row,
                        datum: {
                            ...row.datum,
                            [automaticLabelValue]: series,
                            [automaticLabelId]: `series:${series}`,
                        },
                    });
                }
                continue;
            }
            // Stacked/grouped bars expose one label per segment plus one net total per
            // category. Both values come from the compiler's calculated stack
            // semantics, rather than re-reading an arbitrary authored y field.
            for (const row of derived) {
                output.push({
                    ...row,
                    datum: {
                        ...row.datum,
                        [automaticLabelValue]: row.datum.stackValue,
                        [automaticLabelId]: `segment:${String(row.datum.stackCategory)}:${String(row.datum.stackSeries)}`,
                    },
                });
            }
            const categories = new Map();
            for (const row of derived) {
                const key = `${typeof row.datum.stackCategory}:${String(row.datum.stackCategory)}`;
                const rows = categories.get(key) ?? [];
                rows.push(row);
                categories.set(key, rows);
            }
            for (const [key, rows] of categories) {
                const bounds = rows.reduce((current, row) => union(current, row.bounds), null);
                const representative = rows[0];
                if (bounds === null || representative === undefined)
                    continue;
                const total = representative.datum.stackNetTotal;
                if (typeof total !== 'number' || !Number.isFinite(total))
                    continue;
                output.push({
                    ...representative,
                    bounds,
                    datum: {
                        ...representative.datum,
                        [automaticLabelValue]: total,
                        [automaticLabelId]: `total:${key}`,
                    },
                });
            }
        }
        return output;
    }
    function hash(value) {
        let result = 0x811c9dc5;
        for (let index = 0; index < value.length; index += 1) {
            result ^= value.charCodeAt(index);
            result = Math.imul(result, 0x01000193);
        }
        return (result >>> 0).toString(36);
    }
    function targetId(target) {
        return `mark-label:${target.layerId ?? 'layer'}:${hash(JSON.stringify(target))}`;
    }
    function formattedValue(value, locale) {
        if (value === null || value === undefined)
            return '';
        if (value instanceof Date) {
            try {
                return new Intl.DateTimeFormat(locale, { dateStyle: 'medium', timeZone: 'UTC' }).format(value);
            }
            catch {
                return value.toISOString();
            }
        }
        if (typeof value === 'number') {
            if (!Number.isFinite(value))
                return '';
            try {
                return new Intl.NumberFormat(locale, { maximumFractionDigits: 6 }).format(value);
            }
            catch {
                return String(value);
            }
        }
        if (typeof value === 'string' || typeof value === 'boolean')
            return String(value);
        return '';
    }
    function truncateLabel(text, maximumWidth, fontSize) {
        const characters = Array.from(text);
        const maximumCharacters = Math.max(1, Math.floor(maximumWidth / Math.max(1, fontSize * 0.6)));
        if (characters.length <= maximumCharacters)
            return text;
        if (maximumCharacters === 1)
            return '…';
        return `${characters.slice(0, maximumCharacters - 1).join('')}…`;
    }
    function labelSize(text, fontSize, padding, maximumWidth) {
        const resolvedText = truncateLabel(text, maximumWidth, fontSize);
        const textWidth = Math.min(maximumWidth, Math.max(fontSize * 0.6, Array.from(resolvedText).length * fontSize * 0.6));
        return {
            text: resolvedText,
            width: textWidth + padding * 2,
            height: fontSize * 1.2 + padding * 2,
            textWidth,
        };
    }
    function overlaps(left, right, padding = 1) {
        return !(left.x + left.width + padding <= right.x ||
            right.x + right.width + padding <= left.x ||
            left.y + left.height + padding <= right.y ||
            right.y + right.height + padding <= left.y);
    }
    function inside(inner, outer) {
        return (inner.x >= outer.x &&
            inner.y >= outer.y &&
            inner.x + inner.width <= outer.x + outer.width &&
            inner.y + inner.height <= outer.y + outer.height);
    }
    function centerForPlacement(mark, size, placement, offset) {
        const markCenter = { x: mark.x + mark.width / 2, y: mark.y + mark.height / 2 };
        switch (placement) {
            case 'top':
                return { x: markCenter.x, y: mark.y - offset - size.height / 2 };
            case 'right':
                return {
                    x: mark.x + mark.width + offset + size.width / 2,
                    y: markCenter.y,
                };
            case 'bottom':
                return {
                    x: markCenter.x,
                    y: mark.y + mark.height + offset + size.height / 2,
                };
            case 'left':
                return { x: mark.x - offset - size.width / 2, y: markCenter.y };
            case 'center':
                return markCenter;
        }
    }
    function rectAt(center, size, offsetX = 0, offsetY = 0) {
        return {
            x: center.x + offsetX - size.width / 2,
            y: center.y + offsetY - size.height / 2,
            width: size.width,
            height: size.height,
        };
    }
    function placementCandidates(placement) {
        if (placement !== 'auto')
            return [placement];
        return ['top', 'right', 'bottom', 'left', 'center'];
    }
    function shiftCandidates(placement) {
        if (placement === 'center')
            return [{ x: 0, y: 0 }];
        const distances = [0, -12, 12, -24, 24, -36, 36, -48, 48];
        return distances.map((distance) => placement === 'top' || placement === 'bottom' ? { x: distance, y: 0 } : { x: 0, y: distance });
    }
    function nearestPoint(rect, point) {
        return {
            x: Math.max(rect.x, Math.min(rect.x + rect.width, point.x)),
            y: Math.max(rect.y, Math.min(rect.y + rect.height, point.y)),
        };
    }
    function layerForId(spec, layerId) {
        return spec.layers.find((layer) => layer.id === layerId);
    }
    function positionForDatum(positions, geometry) {
        return positions.find((position) => markLabelTargetMatches(position.target, geometry));
    }
    function labelNodes(options) {
        const nodes = [];
        const edge = nearestPoint(options.bounds, options.anchor);
        if (options.connector &&
            (Math.abs(edge.x - options.anchor.x) > 0.5 || Math.abs(edge.y - options.anchor.y) > 0.5)) {
            nodes.push({
                type: 'line',
                ...nodeBase(`${options.id}:connector`, { zIndex: 540, opacity: options.opacity }),
                x1: options.anchor.x,
                y1: options.anchor.y,
                x2: edge.x,
                y2: edge.y,
                stroke: options.connectorColor,
                lineWidth: options.connectorWidth,
                dash: options.connectorDash,
                lineCap: 'round',
            });
        }
        if (options.background !== undefined || options.border !== undefined || options.active) {
            nodes.push({
                type: 'rect',
                ...nodeBase(`${options.id}:body`, {
                    zIndex: 550,
                    opacity: options.opacity,
                    interactive: true,
                }),
                ...options.bounds,
                ...(options.background === undefined ? {} : { fill: options.background }),
                ...(options.border === undefined ? {} : { stroke: options.border }),
                lineWidth: options.border === undefined ? 0 : 1,
                cornerRadius: options.radius,
            });
        }
        const text = {
            type: 'text',
            ...nodeBase(`${options.id}:text`, {
                zIndex: 551,
                opacity: options.opacity,
                interactive: true,
            }),
            x: options.bounds.x + options.bounds.width / 2,
            y: options.bounds.y + options.bounds.height / 2,
            text: options.text,
            fill: options.color,
            fontFamily: options.fontFamily,
            fontSize: options.fontSize,
            fontWeight: options.fontWeight,
            align: 'center',
            baseline: 'middle',
            rotation: 0,
        };
        nodes.push(text);
        if (options.active) {
            nodes.push({
                type: 'rect',
                ...nodeBase(`${options.id}:selection`, { zIndex: 558 }),
                x: options.bounds.x - 2,
                y: options.bounds.y - 2,
                width: options.bounds.width + 4,
                height: options.bounds.height + 4,
                stroke: options.focus,
                lineWidth: 1.5,
                dash: [4, 3],
                cornerRadius: options.radius + 2,
            });
            const corners = [
                { x: options.bounds.x, y: options.bounds.y },
                { x: options.bounds.x + options.bounds.width, y: options.bounds.y },
                { x: options.bounds.x + options.bounds.width, y: options.bounds.y + options.bounds.height },
                { x: options.bounds.x, y: options.bounds.y + options.bounds.height },
            ];
            corners.forEach((corner, index) => {
                nodes.push({
                    type: 'circle',
                    ...nodeBase(`${options.id}:handle:${index}`, { zIndex: 559, interactive: true }),
                    cx: corner.x,
                    cy: corner.y,
                    radius: 4,
                    fill: options.background ?? '#ffffff',
                    stroke: options.focus,
                    lineWidth: 1.5,
                });
            });
        }
        return nodes;
    }
    /** Compile portable automatic labels for every data-bearing Canvas mark family. */
    function compileMarkLabels(options) {
        const labels = options.spec.markLabels;
        if (labels === false || !labels.visible)
            return { nodes: [], entries: [] };
        const positions = options.runtime?.markLabelPositions ?? labels.positions;
        const layerIds = new Set(labels.layerIds);
        const collected = collectDatumGeometry(options.layerGroups, options.scales, labels.maxLabels, options.spec.locale).filter((entry) => layerIds.size === 0 || layerIds.has(entry.layerId));
        const geometry = (labels.field === undefined ? seriesAutomaticGeometry(collected, options.spec) : collected).slice(0, labels.maxLabels);
        const occupied = [];
        const nodes = [];
        const entries = [];
        const fontSize = labels.style.fontSize ?? options.theme.typography.fontSize;
        const fontWeight = labels.style.fontWeight ?? options.theme.typography.fontWeight ?? 400;
        const padding = labels.style.padding ?? 3;
        const maximumWidth = labels.style.maxWidth ?? 160;
        const opacity = labels.style.opacity ?? 1;
        const connector = labels.connector !== false && (labels.connector.visible ?? true);
        const connectorColor = labels.connector === false
            ? options.theme.colors.mutedText
            : (labels.connector.color ?? options.theme.colors.mutedText);
        const connectorWidth = labels.connector === false ? 1 : (labels.connector.width ?? 1);
        const connectorDash = labels.connector === false ? [] : (labels.connector.dash ?? []);
        const markObstacles = geometry.map(({ bounds }) => bounds);
        geometry.forEach((datum, datumIndex) => {
            const layer = layerForId(options.spec, datum.layerId);
            if (layer === undefined)
                return;
            const field = labels.field ?? layer.encoding.text?.field ?? layer.y.field;
            const sourceValue = datum.datum[automaticLabelValue] ?? datum.datum[field];
            const value = formattedValue(sourceValue, options.spec.locale);
            if (value === '')
                return;
            const size = labelSize(value, fontSize, padding, maximumWidth);
            const target = targetForDatum(datum, labels.key);
            const id = targetId(target);
            const authored = positionForDatum(positions, datum);
            if (authored?.hidden === true)
                return;
            const authoredOffsetX = authored?.offsetX ?? 0;
            const authoredOffsetY = authored?.offsetY ?? 0;
            const candidates = placementCandidates(labels.placement);
            let selected;
            candidateLoop: for (const placement of candidates) {
                const baseCenter = centerForPlacement(datum.bounds, size, placement, labels.offset);
                const shifts = labels.collision === 'avoid' ? shiftCandidates(placement) : [{ x: 0, y: 0 }];
                for (const shift of shifts) {
                    const bounds = rectAt(baseCenter, size, shift.x, shift.y);
                    const collidesWithLabel = occupied.some((candidate) => overlaps(bounds, candidate));
                    const collidesWithMark = markObstacles.some((candidate, index) => index !== datumIndex && overlaps(bounds, candidate, 0));
                    const collision = collidesWithLabel || collidesWithMark;
                    if (!inside(bounds, options.plot))
                        continue;
                    if (labels.collision !== 'none' && collision)
                        continue;
                    selected = {
                        baseCenter: { x: baseCenter.x + shift.x, y: baseCenter.y + shift.y },
                        bounds,
                    };
                    break candidateLoop;
                }
                if (labels.collision === 'hide')
                    break;
            }
            if (selected === undefined)
                return;
            if (authored !== undefined) {
                selected = {
                    baseCenter: selected.baseCenter,
                    bounds: rectAt(selected.baseCenter, size, authoredOffsetX, authoredOffsetY),
                };
            }
            occupied.push(selected.bounds);
            const anchor = {
                x: datum.bounds.x + datum.bounds.width / 2,
                y: datum.bounds.y + datum.bounds.height / 2,
            };
            const active = options.runtime?.activeMarkLabelId === id && labels.authoring !== false;
            nodes.push(...labelNodes({
                id,
                text: size.text,
                bounds: selected.bounds,
                anchor,
                active,
                connector,
                connectorColor,
                connectorWidth,
                connectorDash,
                color: labels.style.color ?? options.theme.colors.text,
                ...(labels.style.background === undefined ? {} : { background: labels.style.background }),
                ...(labels.style.border === undefined ? {} : { border: labels.style.border }),
                opacity,
                fontFamily: options.theme.typography.fontFamily,
                fontSize,
                fontWeight,
                radius: labels.style.radius ?? 2,
                focus: options.theme.colors.focus,
            }));
            entries.push({
                id,
                target,
                text: size.text,
                anchor,
                baseCenter: selected.baseCenter,
                bounds: selected.bounds,
                offsetX: authoredOffsetX,
                offsetY: authoredOffsetY,
                editable: labels.authoring !== false,
            });
        });
        return {
            nodes: nodes.length === 0 ? [] : [group('mark-labels:overlay', nodes, { zIndex: 540 })],
            entries,
        };
    }

    const legendByScene = new WeakMap();
    const categoricalMarks = new Set([
        'pie',
        'variable-pie',
        'treemap',
        'sunburst',
        'packed-bubble',
        'funnel',
        'pyramid',
        'venn',
        'word-cloud',
    ]);
    const continuousMarks = new Set([
        'heatmap',
        'geo',
        'geo-heatmap',
        'map',
        'contour',
        'volume-profile',
    ]);
    const lineMarks = new Set([
        'annotation',
        'area',
        'diff',
        'line',
        'lines',
        'smooth',
        'stepped-area',
        'trendline',
    ]);
    const pointMarks = new Set([
        'bubble',
        'effect-scatter',
        'item',
        'lollipop',
        'packed-bubble',
        'point',
        'scatter-3d',
        'scatter-matrix',
        'ternary',
    ]);
    /** Category filtering is safe only when a row owns independent geometry. */
    const CATEGORY_LEGEND_TOGGLE_MARKS = new Set([
        'bar',
        'bubble',
        'candlestick',
        'calendar',
        'cylinder',
        'effect-scatter',
        'financial',
        'flags',
        'funnel',
        'gantt',
        'heatmap',
        'interval',
        'item',
        'lollipop',
        'packed-bubble',
        'pie',
        'point',
        'pictorial-bar',
        'pyramid',
        'range',
        'scatter-3d',
        'scatter-matrix',
        'sunburst',
        'ternary',
        'timeline',
        'tilemap',
        'tiled-map',
        'treemap',
        'variable-pie',
        'variwide',
        'venn',
        'waterfall',
        'wind-barb',
        'word-cloud',
    ]);
    function markSymbol(mark) {
        if (mark !== undefined && lineMarks.has(mark))
            return 'line';
        if (mark !== undefined && pointMarks.has(mark))
            return 'point';
        return 'rect';
    }
    function valueKey$1(value) {
        return `${value === null ? 'null' : typeof value}:${String(value)}`;
    }
    function safeId(value) {
        return value.replace(/[^a-zA-Z0-9_-]+/g, '-').replace(/^-+|-+$/g, '') || 'item';
    }
    /** Keep inferred category state stable across row reordering and slug collisions. */
    function stableCategoryId(value) {
        const canonical = JSON.stringify([value === null ? 'null' : typeof value, value]);
        let encoded = '';
        for (let index = 0; index < canonical.length; index += 1) {
            encoded += canonical.charCodeAt(index).toString(16).padStart(4, '0');
        }
        return encoded;
    }
    function autoMode(spec) {
        if (spec.legend === false)
            return 'layers';
        if (spec.legend.mode !== 'auto')
            return spec.legend.mode;
        if (spec.layers.length > 1)
            return 'layers';
        const mark = spec.layers[0]?.mark.type;
        if (mark !== undefined && continuousMarks.has(mark))
            return 'continuous';
        if (mark !== undefined && categoricalMarks.has(mark))
            return 'categories';
        return 'layers';
    }
    function categoryItems(spec, legend) {
        const layer = spec.layers.find((candidate) => candidate.id === legend.layerId) ?? spec.layers[0];
        if (layer === undefined)
            return [];
        const field = legend.field ?? layer.x.field;
        const table = DataTable.from(layer.data);
        const seen = new Set();
        const output = [];
        for (let index = 0; index < table.length && output.length < legend.maxItems; index += 1) {
            const raw = table.value(index, field);
            if (raw === undefined ||
                raw instanceof Date ||
                (typeof raw === 'number' && !Number.isFinite(raw)))
                continue;
            const value = raw;
            const key = valueKey$1(value);
            if (seen.has(key))
                continue;
            seen.add(key);
            output.push({
                id: `category-${stableCategoryId(value)}`,
                label: raw === null ? '—' : String(raw),
                layerId: layer.id,
                value,
                symbol: markSymbol(layer.mark.type),
            });
        }
        return output;
    }
    function layerItems(spec) {
        return spec.layers.map((layer, index) => ({
            id: `layer-${safeId(layer.id)}-${index}`,
            label: layer.name,
            layerId: layer.id,
            symbol: markSymbol(layer.mark.type),
        }));
    }
    function legendItemToggleable(model, item) {
        return (model.spec.interactive &&
            model.mode !== 'continuous' &&
            (model.mode === 'categories'
                ? item.layerId === undefined
                    ? model.categoryToggleableLayerIds.size > 0
                    : model.categoryToggleableLayerIds.has(item.layerId)
                : item.layerId !== undefined));
    }
    function continuousItems(spec, legend) {
        const layer = spec.layers.find((candidate) => candidate.id === legend.layerId) ?? spec.layers[0];
        if (layer === undefined)
            return [];
        const field = legend.field ?? layer.mark.fields.value ?? layer.y.field;
        const table = DataTable.from(layer.data);
        const extent = table.extent(field, false);
        if (extent === null)
            return [{ id: 'continuous-scale', label: field, layerId: layer.id, symbol: 'rect' }];
        let formatter;
        try {
            formatter = new Intl.NumberFormat(spec.locale, { maximumFractionDigits: 6 });
        }
        catch {
            formatter = new Intl.NumberFormat(undefined, { maximumFractionDigits: 6 });
        }
        return [
            {
                id: 'continuous-min',
                label: formatter.format(extent[0]),
                layerId: layer.id,
                value: extent[0],
                symbol: 'rect',
            },
            {
                id: 'continuous-max',
                label: formatter.format(extent[1]),
                layerId: layer.id,
                value: extent[1],
                symbol: 'rect',
            },
        ];
    }
    function resolveLegendModel(spec, theme, availableWidth = 640, availableHeight = 400) {
        const legend = spec.legend;
        if (legend === false || !legend.visible)
            return null;
        const mode = autoMode(spec);
        const selectedLayer = spec.layers.find((candidate) => candidate.id === legend.layerId) ?? spec.layers[0];
        const field = mode === 'categories'
            ? (legend.field ?? selectedLayer?.x.field)
            : mode === 'continuous'
                ? (legend.field ?? selectedLayer?.mark.fields.value ?? selectedLayer?.y.field)
                : undefined;
        const inferred = legend.items.length > 0
            ? legend.items
            : mode === 'layers'
                ? layerItems(spec)
                : mode === 'categories'
                    ? categoryItems(spec, legend)
                    : continuousItems(spec, legend);
        let items = inferred.slice(0, legend.maxItems).map((item) => {
            if (item.symbol !== 'auto')
                return item;
            const layer = spec.layers.find((candidate) => candidate.id === item.layerId);
            return { ...item, symbol: markSymbol(layer?.mark.type) };
        });
        if (items.length === 0)
            return null;
        const titleHeight = legend.title === undefined
            ? 0
            : (theme.typography.legendTitleSize ?? theme.typography.fontSize) + 8;
        const maxWidth = Math.max(1, availableWidth - 8);
        const maxHeight = Math.max(1, availableHeight - 8);
        const swatchSize = theme.legend?.swatchSize ?? 12;
        const horizontalItemPadding = swatchSize + 22;
        const direction = spec.locale !== undefined && /^(ar|fa|he|ur)(?:-|$)/i.test(spec.locale) ? 'rtl' : 'ltr';
        const categoryToggleableLayerIds = new Set(spec.layers
            .filter((layer) => CATEGORY_LEGEND_TOGGLE_MARKS.has(layer.mark.type))
            .map((layer) => layer.id));
        if (mode === 'continuous' && items.length >= 2) {
            return {
                spec: legend,
                mode,
                items,
                ...(field === undefined ? {} : { field }),
                width: Math.min(maxWidth, Math.max(120, (legend.title?.length ?? 0) * 7 + (legend.title === undefined ? 0 : 24), 168)),
                height: Math.min(maxHeight, titleHeight + 42),
                direction,
                categoryToggleableLayerIds,
            };
        }
        if (legend.orientation === 'horizontal') {
            const totalWidth = items.reduce((sum, item) => sum + Math.max(64, item.label.length * 7 + horizontalItemPadding), 0);
            const modelWidth = Math.min(maxWidth, Math.max(96, totalWidth + 20));
            const usableWidth = Math.max(48, modelWidth - 20);
            let rowWidth = 0;
            let rows = 1;
            for (const item of items) {
                const itemWidth = Math.max(64, item.label.length * 7 + horizontalItemPadding);
                if (rowWidth > 0 && rowWidth + itemWidth > usableWidth) {
                    rows += 1;
                    rowWidth = 0;
                }
                rowWidth += itemWidth;
            }
            const visibleRows = Math.max(1, Math.floor((maxHeight - titleHeight - 12) / 24));
            if (rows > visibleRows) {
                let usedRows = 1;
                rowWidth = 0;
                items = items.filter((item) => {
                    const itemWidth = Math.max(64, item.label.length * 7 + horizontalItemPadding);
                    if (rowWidth > 0 && rowWidth + itemWidth > usableWidth) {
                        usedRows += 1;
                        rowWidth = 0;
                    }
                    if (usedRows > visibleRows)
                        return false;
                    rowWidth += itemWidth;
                    return true;
                });
                rows = visibleRows;
            }
            return {
                spec: legend,
                mode,
                items,
                ...(field === undefined ? {} : { field }),
                width: modelWidth,
                height: Math.min(maxHeight, titleHeight + rows * 24 + 12),
                direction,
                categoryToggleableLayerIds,
            };
        }
        const maxVisibleItems = Math.max(1, Math.floor((maxHeight - titleHeight - 12) / 24));
        items = items.slice(0, maxVisibleItems);
        return {
            spec: legend,
            mode,
            items,
            ...(field === undefined ? {} : { field }),
            width: Math.min(maxWidth, Math.min(220, Math.max(72, legend.title === undefined ? 0 : legend.title.length * 7 + 20, ...items.map((item) => item.label.length * 7 + swatchSize + 30)))),
            height: Math.min(maxHeight, titleHeight + items.length * 24 + 12),
            direction,
            categoryToggleableLayerIds,
        };
    }
    function truncateText(text, width, fontSize) {
        const maximum = Math.max(1, Math.floor(width / Math.max(5, fontSize * 0.58)));
        let graphemes;
        try {
            graphemes = [...new Intl.Segmenter(undefined, { granularity: 'grapheme' }).segment(text)].map(({ segment }) => segment);
        }
        catch {
            graphemes = Array.from(text);
        }
        if (graphemes.length <= maximum)
            return text;
        return `${graphemes.slice(0, Math.max(1, maximum - 1)).join('')}…`;
    }
    function legendExternalInsets(model) {
        const empty = { top: 0, right: 0, bottom: 0, left: 0 };
        if (model === null)
            return empty;
        switch (model.spec.position) {
            case 'top':
                return { ...empty, top: model.height + 8 };
            case 'right':
                return { ...empty, right: model.width + 8 };
            case 'bottom':
                return { ...empty, bottom: model.height + 8 };
            case 'left':
                return { ...empty, left: model.width + 8 };
            default:
                return empty;
        }
    }
    function descendants(node) {
        if (node.type !== 'group')
            return [node];
        return node.children.flatMap(descendants);
    }
    function itemPaint(item, index, mode, layerGroups, field, theme, itemCount) {
        if (item.color !== undefined)
            return item.color;
        if (mode === 'continuous') {
            return continuousColor(theme, index === 0 ? 0 : 1);
        }
        const groups = item.layerId === undefined
            ? layerGroups
            : layerGroups.filter((candidate) => candidate.id === `${item.layerId}:group`);
        for (const group of groups) {
            const nodes = descendants(group);
            const matching = mode === 'categories' && field !== undefined && item.value !== undefined
                ? nodes.find((node) => [node.datum?.tooltip, node.datum?.datum].some((datum) => datum !== undefined &&
                    Object.prototype.hasOwnProperty.call(datum, field) &&
                    valueKey$1(datum[field]) === valueKey$1(item.value)))
                : nodes.find((node) => nodePaint(node) !== undefined);
            const paint = matching === undefined ? undefined : nodePaint(matching);
            if (paint !== undefined)
                return paint;
        }
        return categoricalColor(theme, index, Math.max(1, itemCount));
    }
    function legendOrigin(model, plot, width, height) {
        const gap = 8;
        const origin = (() => {
            switch (model.spec.position) {
                case 'top':
                    return { x: plot.x, y: Math.max(gap, plot.y - model.height - gap) };
                case 'right':
                    return { x: plot.x + plot.width + gap, y: plot.y };
                case 'bottom':
                    return { x: plot.x, y: height - model.height - gap / 2 };
                case 'left':
                    return { x: Math.max(gap, plot.x - model.width - gap), y: plot.y };
                case 'inside-top-left':
                    return { x: plot.x + gap, y: plot.y + gap };
                case 'inside-bottom-left':
                    return { x: plot.x + gap, y: plot.y + plot.height - model.height - gap };
                case 'inside-bottom-right':
                    return {
                        x: plot.x + plot.width - model.width - gap,
                        y: plot.y + plot.height - model.height - gap,
                    };
                case 'inside-top-right':
                default:
                    return { x: plot.x + plot.width - model.width - gap, y: plot.y + gap };
            }
        })();
        return {
            x: Math.max(4, Math.min(Math.max(4, width - model.width - 4), origin.x)),
            y: Math.max(4, Math.min(Math.max(4, height - model.height - 4), origin.y)),
        };
    }
    function compileLegend(model, layerGroups, plot, width, height, theme, hiddenItems = new Set()) {
        if (model === null)
            return { nodes: [], layout: null };
        const origin = legendOrigin(model, plot, width, height);
        const background = {
            type: 'rect',
            ...nodeBase('legend:surface', {
                zIndex: 500,
                opacity: theme.legend?.surfaceOpacity ?? 0.94,
            }),
            x: origin.x,
            y: origin.y,
            width: model.width,
            height: model.height,
            fill: theme.colors.surface,
            stroke: theme.legend?.borderColor ?? theme.colors.axis,
            lineWidth: theme.legend?.borderWidth ?? 1,
            cornerRadius: theme.legend?.cornerRadius ?? 8,
        };
        const nodes = [background];
        const inset = Math.min(10, Math.max(0, model.width / 4));
        let cursorX = origin.x + inset;
        let cursorY = origin.y + 10;
        if (model.spec.title !== undefined) {
            nodes.push({
                type: 'text',
                ...nodeBase('legend:title', { zIndex: 502 }),
                x: model.direction === 'rtl' ? origin.x + model.width - inset : cursorX,
                y: cursorY,
                text: truncateText(model.spec.title, model.width - inset * 2, theme.typography.fontSize),
                fill: theme.colors.text,
                fontFamily: theme.typography.fontFamily,
                fontSize: theme.typography.legendTitleSize ?? theme.typography.fontSize,
                fontWeight: theme.typography.legendTitleWeight ?? 700,
                align: model.direction === 'rtl' ? 'right' : 'left',
                baseline: 'top',
                rotation: 0,
            });
            cursorY += (theme.typography.legendTitleSize ?? theme.typography.fontSize) + 8;
        }
        const entries = [];
        const field = model.field;
        if (model.mode === 'continuous' && model.items.length >= 2) {
            const scaleX = origin.x + inset;
            const scaleWidth = Math.max(1, model.width - inset * 2);
            const paletteSize = Math.max(1, Math.floor(theme.legend?.continuousSamples ??
                (theme.colors.paletteMode === 'ggplot2-hue' ||
                    theme.colors.continuousInterpolation !== undefined
                    ? 16
                    : theme.colors.sequential.length)));
            const palette = model.spec.items.length > 0
                ? model.items.map((item, index) => {
                    return (item.color ?? continuousColor(theme, index / Math.max(1, model.items.length - 1)));
                })
                : Array.from({ length: paletteSize }, (_value, index) => continuousColor(theme, index / Math.max(1, paletteSize - 1)));
            palette.forEach((paint, paletteIndex) => {
                nodes.push({
                    type: 'rect',
                    ...nodeBase(`legend:scale:${paletteIndex}`, { zIndex: 501 }),
                    x: scaleX + (scaleWidth * paletteIndex) / palette.length,
                    y: cursorY,
                    width: scaleWidth / palette.length + 0.5,
                    height: 10,
                    fill: paint,
                    lineWidth: 0,
                    cornerRadius: 0,
                });
            });
            const endpoints = [model.items[0], model.items[model.items.length - 1]];
            endpoints.forEach((item, index) => {
                const color = itemPaint(item, index, model.mode, layerGroups, field, theme, model.items.length);
                const left = index === 0;
                nodes.push({
                    type: 'text',
                    ...nodeBase(`legend:item:${item.id}:label`, { zIndex: 502 }),
                    x: model.direction === 'rtl'
                        ? left
                            ? scaleX + scaleWidth
                            : scaleX
                        : left
                            ? scaleX
                            : scaleX + scaleWidth,
                    y: cursorY + 14,
                    text: truncateText(item.label, scaleWidth / 2 - 4, theme.typography.legendLabelSize ?? theme.typography.fontSize),
                    fill: theme.colors.mutedText,
                    fontFamily: theme.typography.fontFamily,
                    fontSize: theme.typography.legendLabelSize ?? theme.typography.fontSize,
                    fontWeight: theme.typography.legendLabelWeight ?? 500,
                    align: model.direction === 'rtl' ? (left ? 'right' : 'left') : left ? 'left' : 'right',
                    baseline: 'top',
                    rotation: 0,
                });
                entries.push({
                    ...item,
                    color,
                    bounds: {
                        x: left ? scaleX : scaleX + scaleWidth / 2,
                        y: cursorY + 12,
                        width: scaleWidth / 2,
                        height: 20,
                    },
                    toggleable: false,
                    visible: true,
                });
            });
            return {
                nodes: [
                    group('legend:group', nodes, {
                        zIndex: 500,
                        clip: { ...origin, width: model.width, height: model.height },
                    }),
                ],
                layout: {
                    mode: model.mode,
                    entries,
                    bounds: { ...origin, width: model.width, height: model.height },
                },
            };
        }
        const swatchSize = theme.legend?.swatchSize ?? 12;
        const lineSwatchLength = theme.legend?.swatchSize ?? 13;
        const rtlSwatchInset = theme.legend?.swatchSize ?? 16;
        const swatchLineWidth = theme.legend?.lineWidth ?? 2.5;
        const swatchPointRadius = theme.legend?.pointRadius ?? 5;
        const swatchPointStrokeWidth = theme.legend?.pointStrokeWidth ?? 0;
        for (const [index, item] of model.items.entries()) {
            const itemWidth = Math.max(64, item.label.length * 7 + swatchSize + 22);
            if (model.spec.orientation === 'horizontal' &&
                cursorX > origin.x + 10 &&
                cursorX + itemWidth > origin.x + model.width - 10) {
                cursorX = origin.x + 10;
                cursorY += 24;
            }
            const bounds = {
                x: cursorX - 4,
                y: cursorY - 4,
                width: model.spec.orientation === 'horizontal'
                    ? Math.max(1, Math.min(itemWidth, origin.x + model.width - (cursorX - 4) - 6))
                    : model.width - 12,
                height: 22,
            };
            const visible = !hiddenItems.has(item.id);
            const color = itemPaint(item, index, model.mode, layerGroups, field, theme, model.items.length);
            const swatchX = model.direction === 'rtl' ? bounds.x + bounds.width - rtlSwatchInset : cursorX;
            if (item.symbol === 'line') {
                nodes.push({
                    type: 'line',
                    ...nodeBase(`legend:item:${item.id}:swatch`, {
                        zIndex: 501,
                        opacity: visible ? 1 : 0.28,
                    }),
                    x1: swatchX,
                    y1: cursorY + swatchSize / 2,
                    x2: swatchX + lineSwatchLength,
                    y2: cursorY + swatchSize / 2,
                    stroke: color,
                    lineWidth: swatchLineWidth,
                    lineCap: theme.legend?.lineCap ?? 'round',
                });
            }
            else if (item.symbol === 'point') {
                nodes.push({
                    type: 'circle',
                    ...nodeBase(`legend:item:${item.id}:swatch`, {
                        zIndex: 501,
                        opacity: visible ? 1 : 0.28,
                    }),
                    cx: swatchX + swatchSize / 2,
                    cy: cursorY + swatchSize / 2,
                    radius: swatchPointRadius,
                    fill: color,
                    ...(swatchPointStrokeWidth === 0 ? {} : { stroke: color }),
                    lineWidth: swatchPointStrokeWidth,
                });
            }
            else {
                nodes.push({
                    type: 'rect',
                    ...nodeBase(`legend:item:${item.id}:swatch`, {
                        zIndex: 501,
                        opacity: visible ? 1 : 0.28,
                    }),
                    x: swatchX,
                    y: cursorY,
                    width: swatchSize,
                    height: swatchSize,
                    fill: color,
                    lineWidth: 0,
                    cornerRadius: theme.legend?.swatchRadius ?? 3,
                });
            }
            nodes.push({
                type: 'text',
                ...nodeBase(`legend:item:${item.id}:label`, { zIndex: 502, opacity: visible ? 1 : 0.45 }),
                x: model.direction === 'rtl' ? swatchX - 7 : cursorX + swatchSize + 7,
                y: cursorY + swatchSize / 2,
                text: truncateText(item.label, Math.max(8, bounds.width - swatchSize - 14), theme.typography.legendLabelSize ?? theme.typography.fontSize),
                fill: theme.colors.text,
                fontFamily: theme.typography.fontFamily,
                fontSize: theme.typography.legendLabelSize ?? theme.typography.fontSize,
                fontWeight: theme.typography.legendLabelWeight ?? 500,
                align: model.direction === 'rtl' ? 'right' : 'left',
                baseline: 'middle',
                rotation: 0,
            });
            const toggleable = legendItemToggleable(model, item);
            entries.push({ ...item, color, bounds, toggleable, visible });
            if (model.spec.orientation === 'horizontal')
                cursorX += itemWidth;
            else
                cursorY += 24;
        }
        return {
            nodes: [
                group('legend:group', nodes, {
                    zIndex: 500,
                    clip: { ...origin, width: model.width, height: model.height },
                }),
            ],
            layout: {
                mode: model.mode,
                entries,
                bounds: { ...origin, width: model.width, height: model.height },
            },
        };
    }
    function registerLegendLayout(scene, layout) {
        if (layout !== null)
            legendByScene.set(scene, layout);
    }
    function sceneLegendLayout(scene) {
        return legendByScene.get(scene) ?? null;
    }

    const compositionProperties = [
        'layer',
        'facet',
        'repeat',
        'hconcat',
        'vconcat',
        'concat',
        'inset',
        'spec',
        'columns',
        'spacing',
        'resolve',
    ];
    const minimumCellWidth = 80;
    const minimumCellHeight = 80;
    const facetHeaderHeight = 24;
    function fail$2(message, path = '$') {
        throw new GraflumeError('INVALID_SPEC', message, { path });
    }
    function mutableSpec(input) {
        return { ...input };
    }
    function withoutComposition(input) {
        const output = mutableSpec(input);
        for (const key of compositionProperties)
            delete output[key];
        return output;
    }
    function withoutDimensions(input) {
        const output = mutableSpec(input);
        delete output.width;
        delete output.height;
        return output;
    }
    function safeToken(value) {
        const token = value
            .normalize('NFKC')
            .toLowerCase()
            .replace(/[^a-z0-9]+/g, '-')
            .replace(/^-+|-+$/g, '')
            .slice(0, 32);
        return token === '' ? 'view' : token;
    }
    function inheritedChild(child, parent, options = {}) {
        const ownsData = child.data !== undefined;
        const parentTransforms = options.transformed === true || ownsData ? [] : (parent.transform ?? []);
        return {
            ...withoutDimensions(child),
            ...(child.data === undefined && parent.data !== undefined ? { data: parent.data } : {}),
            transform: [...parentTransforms, ...(child.transform ?? [])],
            theme: child.theme ?? parent.theme ?? defaultThemeId,
            ...(child.locale === undefined && parent.locale !== undefined ? { locale: parent.locale } : {}),
            renderer: child.renderer ?? parent.renderer ?? 'canvas',
            performance: child.performance ?? parent.performance ?? 'auto',
            ...(child.accessibility?.maxRows === undefined && parent.accessibility?.maxRows !== undefined
                ? {
                    accessibility: {
                        ...child.accessibility,
                        maxRows: parent.accessibility.maxRows,
                    },
                }
                : {}),
            width: 'container',
            height: 'container',
        };
    }
    function localSelection(runtime, viewId) {
        return runtime.selection?.flatMap((target) => {
            if (target.layerId === undefined)
                return [target];
            const prefix = `${viewId}/`;
            return target.layerId.startsWith(prefix)
                ? [{ ...target, layerId: target.layerId.slice(prefix.length) }]
                : [];
        });
    }
    function runtimeForView(runtime, viewId) {
        const selection = localSelection(runtime, viewId);
        const analyticSelection = runtime.analyticSelection;
        const localAnalyticSelection = analyticSelection === undefined
            ? undefined
            : {
                ...analyticSelection,
                selections: analyticSelection.selections.flatMap((candidate) => {
                    if (candidate.type !== 'point' || candidate.target?.layerId === undefined) {
                        return [candidate];
                    }
                    const prefix = `${viewId}/`;
                    return candidate.target.layerId.startsWith(prefix)
                        ? [
                            {
                                ...candidate,
                                target: {
                                    ...candidate.target,
                                    layerId: candidate.target.layerId.slice(prefix.length),
                                },
                            },
                        ]
                        : [];
                }),
            };
        return {
            ...(runtime.annotationsVisible === undefined
                ? {}
                : { annotationsVisible: runtime.annotationsVisible }),
            ...(selection === undefined ? {} : { selection }),
            ...(localAnalyticSelection === undefined ? {} : { analyticSelection: localAnalyticSelection }),
            ...(runtime.domainView === undefined ? {} : { domainView: runtime.domainView }),
            ...(runtime.analyticSelectionDraft === undefined
                ? {}
                : { analyticSelectionDraft: runtime.analyticSelectionDraft }),
        };
    }
    function interactionForView(child, parent) {
        const interaction = parent.interaction;
        return {
            ...child,
            interaction: {
                ...child.interaction,
                ...(interaction?.hover === undefined ? {} : { hover: interaction.hover }),
                ...(interaction?.click === undefined ? {} : { click: interaction.click }),
                tooltip: false,
                navigation: false,
                ...(interaction?.domainNavigation === undefined
                    ? {}
                    : { domainNavigation: interaction.domainNavigation }),
                playback: false,
                controls: false,
                ...(interaction?.selection === undefined ? {} : { selection: interaction.selection }),
            },
        };
    }
    function layerFromShorthand(child, childIndex) {
        if (child.mark === undefined)
            fail$2('Layer composition child is missing mark.', `$.layer[${childIndex}].mark`);
        return {
            id: `layer-${childIndex}`,
            ...(child.title === undefined
                ? {}
                : { name: typeof child.title === 'string' ? child.title : child.title.text }),
            ...(child.data === undefined ? {} : { data: child.data }),
            ...(child.transform === undefined ? {} : { transform: child.transform }),
            mark: child.mark,
            ...(child.encoding === undefined ? { x: child.x, y: child.y } : { encoding: child.encoding }),
        };
    }
    function flattenLayerComposition(input) {
        const children = input.layer ?? [];
        const layers = children.flatMap((child, childIndex) => {
            if (isCompositionSpec(child)) {
                fail$2('Layer composition children must be unit charts.', `$.layer[${childIndex}]`);
            }
            if (child.layers === undefined)
                return [layerFromShorthand(child, childIndex)];
            return child.layers.map((layer, layerIndex) => ({
                ...layer,
                id: `layer-${childIndex}-${safeToken(layer.id ?? String(layerIndex))}`,
                ...(layer.data === undefined && child.data !== undefined ? { data: child.data } : {}),
                transform: [...(child.transform ?? []), ...(layer.transform ?? [])],
            }));
        });
        if (layers.length > maximumCompositionLayers) {
            fail$2(`Layer composition exceeds ${maximumCompositionLayers} compiled layers.`, '$.layer');
        }
        const output = mutableSpec(withoutComposition(input));
        delete output.mark;
        delete output.x;
        delete output.y;
        delete output.encoding;
        output.layers = layers;
        return output;
    }
    function fieldName(input) {
        return typeof input === 'string' ? input : input.field;
    }
    function fieldTitle(input) {
        return typeof input === 'string' ? input : (input.title ?? input.field);
    }
    function facetSort(input) {
        return typeof input === 'string' ? 'input' : (input.sort ?? 'input');
    }
    function valueKey(value) {
        if (value instanceof Date)
            return `date:${value.toISOString()}`;
        if (value === undefined)
            return 'undefined:';
        if (value === null)
            return 'null:';
        if (Array.isArray(value))
            fail$2('Facet fields must contain scalar values.', '$.facet');
        return `${typeof value}:${String(value)}`;
    }
    function valueLabel(value) {
        if (value instanceof Date)
            return value.toISOString();
        if (value === null || value === undefined)
            return '\u2014';
        if (Array.isArray(value))
            fail$2('Facet fields must contain scalar values.', '$.facet');
        return String(value);
    }
    function compareFacetValue(left, right) {
        if (Object.is(left, right))
            return 0;
        const numeric = (value) => value instanceof Date
            ? value.getTime()
            : typeof value === 'number' && Number.isFinite(value)
                ? value
                : null;
        const a = numeric(left);
        const b = numeric(right);
        if (a !== null && b !== null)
            return a - b;
        return valueLabel(left).localeCompare(valueLabel(right), 'en');
    }
    function uniqueValues(rows, field, maximum = maximumCompositionViews) {
        const name = fieldName(field);
        const seen = new Set();
        const values = [];
        for (const row of rows) {
            const value = row[name];
            const key = valueKey(value);
            if (seen.has(key))
                continue;
            seen.add(key);
            values.push(value);
            if (values.length > maximum) {
                fail$2(`Facet view count exceeds ${maximumCompositionViews}.`, '$.facet');
            }
        }
        const sort = facetSort(field);
        return sort === 'input'
            ? values
            : values.sort((left, right) => sort === 'descending' ? -compareFacetValue(left, right) : compareFacetValue(left, right));
    }
    function materializeFacet(input) {
        if (input.data === undefined || input.facet === undefined || input.spec === undefined) {
            fail$2('Facet composition requires data, facet, and spec.', '$.facet');
        }
        const transformed = executeTransformsWithNamedLineage(input.data, input.transform ?? [], 'composition:facet');
        const rows = transformed.data;
        const facet = input.facet;
        const views = [];
        if (facet.wrap !== undefined) {
            const values = uniqueValues(rows, facet.wrap);
            const columns = Math.min(facet.columns ?? Math.max(1, Math.ceil(Math.sqrt(values.length))), 16);
            const valueIndices = new Map(values.map((value, index) => [valueKey(value), index]));
            const matches = values.map(() => []);
            rows.forEach((row, rowIndex) => {
                const index = valueIndices.get(valueKey(row[fieldName(facet.wrap)]));
                if (index !== undefined)
                    matches[index].push({ row, rowIndex });
            });
            values.forEach((value, index) => {
                const matched = matches[index];
                const label = `${fieldTitle(facet.wrap)} = ${valueLabel(value)}`;
                views.push({
                    id: `facet-${index}-${safeToken(valueLabel(value))}`,
                    label,
                    row: Math.floor(index / columns),
                    column: index % columns,
                    spec: inheritedChild({ ...input.spec, data: matched.map(({ row }) => row) }, input, {
                        transformed: true,
                    }),
                    sourceMap: {
                        rows: matched.map(({ rowIndex }) => transformed.lineage.rowSources[rowIndex] ?? []),
                        sourceRows: transformed.lineage.sourceRows,
                        lineage: transformed.lineage,
                    },
                });
            });
            return {
                views,
                rows: Math.max(1, Math.ceil(views.length / columns)),
                columns,
                showLabels: true,
            };
        }
        const rowField = facet.row;
        const columnField = facet.column;
        const rowValues = rowField === undefined ? [undefined] : uniqueValues(rows, rowField);
        const columnValues = columnField === undefined ? [undefined] : uniqueValues(rows, columnField);
        const rowIndices = new Map(rowValues.map((value, index) => [valueKey(value), index]));
        const columnIndices = new Map(columnValues.map((value, index) => [valueKey(value), index]));
        const grouped = new Map();
        rows.forEach((row, sourceIndex) => {
            const rowIndex = rowIndices.get(valueKey(rowField === undefined ? undefined : row[fieldName(rowField)]));
            const columnIndex = columnIndices.get(valueKey(columnField === undefined ? undefined : row[fieldName(columnField)]));
            if (rowIndex === undefined || columnIndex === undefined)
                return;
            const key = `${rowIndex}:${columnIndex}`;
            let group = grouped.get(key);
            if (group === undefined) {
                if (grouped.size >= maximumCompositionViews) {
                    fail$2(`Facet view count exceeds ${maximumCompositionViews}.`, '$.facet');
                }
                group = { rowIndex, columnIndex, matched: [] };
                grouped.set(key, group);
            }
            group.matched.push({ row, sourceIndex });
        });
        const observed = [...grouped.values()].sort((left, right) => left.rowIndex - right.rowIndex || left.columnIndex - right.columnIndex);
        for (const { rowIndex, columnIndex, matched } of observed) {
            const rowValue = rowValues[rowIndex];
            const columnValue = columnValues[columnIndex];
            const labels = [
                ...(rowField === undefined ? [] : [`${fieldTitle(rowField)} = ${valueLabel(rowValue)}`]),
                ...(columnField === undefined
                    ? []
                    : [`${fieldTitle(columnField)} = ${valueLabel(columnValue)}`]),
            ];
            const label = labels.join(' \u00b7 ');
            views.push({
                id: `facet-${rowIndex}-${columnIndex}`,
                label,
                row: rowIndex,
                column: columnIndex,
                spec: inheritedChild({ ...input.spec, data: matched.map(({ row }) => row) }, input, {
                    transformed: true,
                }),
                sourceMap: {
                    rows: matched.map(({ sourceIndex }) => transformed.lineage.rowSources[sourceIndex] ?? []),
                    sourceRows: transformed.lineage.sourceRows,
                    lineage: transformed.lineage,
                },
            });
        }
        return {
            views,
            rows: Math.max(1, rowValues.length),
            columns: Math.max(1, columnValues.length),
            showLabels: true,
        };
    }
    function positionWithField(input, field) {
        if (field === undefined)
            return input;
        if (typeof input === 'string' || input === undefined)
            return { field };
        return { ...input, field };
    }
    function channelWithField(input, field) {
        if (field === undefined)
            return input;
        if (typeof input === 'string' || input === undefined)
            return { field };
        const { value: _value, ...rest } = input;
        return { ...rest, field };
    }
    function repeatFields(input, item) {
        if (isCompositionSpec(input))
            fail$2('Repeat templates must currently be unit charts.', '$.spec');
        const rewriteLayer = (layer) => layer.encoding === undefined
            ? {
                ...layer,
                x: positionWithField(layer.x, item.x),
                y: positionWithField(layer.y, item.y),
            }
            : {
                ...layer,
                encoding: {
                    ...layer.encoding,
                    x: channelWithField(layer.encoding.x, item.x),
                    y: channelWithField(layer.encoding.y, item.y),
                },
            };
        if (input.layers !== undefined) {
            return { ...input, layers: input.layers.map(rewriteLayer) };
        }
        return input.encoding === undefined
            ? {
                ...input,
                x: positionWithField(input.x, item.x),
                y: positionWithField(input.y, item.y),
            }
            : {
                ...input,
                encoding: {
                    ...input.encoding,
                    x: channelWithField(input.encoding.x, item.x),
                    y: channelWithField(input.encoding.y, item.y),
                },
            };
    }
    function materializeRepeat(input) {
        if (input.repeat === undefined || input.spec === undefined) {
            fail$2('Repeat composition requires repeat and spec.', '$.repeat');
        }
        const columns = Math.min(input.repeat.columns ?? Math.max(1, Math.ceil(Math.sqrt(input.repeat.items.length))), 16);
        const views = input.repeat.items.map((item, index) => ({
            id: `repeat-${index}-${safeToken(item.id)}`,
            label: item.label ?? item.id,
            row: Math.floor(index / columns),
            column: index % columns,
            spec: inheritedChild(repeatFields(input.spec, item), input),
        }));
        return {
            views,
            rows: Math.max(1, Math.ceil(views.length / columns)),
            columns,
            showLabels: true,
        };
    }
    function materializeConcat(input, kind) {
        const children = input[kind] ?? [];
        const columns = kind === 'hconcat'
            ? children.length
            : kind === 'vconcat'
                ? 1
                : Math.min(input.columns ?? Math.max(1, Math.ceil(Math.sqrt(children.length))), 16);
        return {
            views: children.map((child, index) => ({
                id: `${kind}-${index}`,
                label: `${kind} ${index + 1}`,
                row: Math.floor(index / columns),
                column: index % columns,
                spec: inheritedChild(child, input),
            })),
            rows: Math.max(1, Math.ceil(children.length / columns)),
            columns: Math.max(1, columns),
            showLabels: false,
        };
    }
    function materializeInset(input) {
        if (input.inset === undefined)
            fail$2('Inset composition requires inset.', '$.inset');
        return {
            views: [
                {
                    id: 'inset-base',
                    label: 'Base view',
                    row: 0,
                    column: 0,
                    spec: inheritedChild(input.inset.base, input),
                },
                {
                    id: 'inset-view',
                    label: input.inset.label ?? 'Inset view',
                    row: 0,
                    column: 0,
                    spec: inheritedChild(input.inset.view, input),
                },
            ],
            rows: 1,
            columns: 1,
            showLabels: false,
        };
    }
    function forcePositionDomain(input, domain) {
        if (input === undefined)
            return input;
        if (typeof input === 'string')
            return { field: input, scale: { domain } };
        return { ...input, scale: { ...input.scale, domain } };
    }
    function forceChannelDomain(input, domain) {
        if (input === undefined)
            return input;
        if (typeof input === 'string')
            return { field: input, scale: { domain } };
        return { ...input, scale: { ...input.scale, domain } };
    }
    function positionAxisId(channel, input) {
        return typeof input === 'object' && input.axisId !== undefined ? input.axisId : channel;
    }
    function forceSharedDomains(input, domains) {
        const rewrittenPosition = (value, channel) => {
            const domain = domains[positionAxisId(channel, value)];
            return domain === undefined ? value : forcePositionDomain(value, domain);
        };
        const rewrittenChannel = (value, channel) => {
            const domain = domains[positionAxisId(channel, value)];
            return domain === undefined ? value : forceChannelDomain(value, domain);
        };
        const rewrite = (layer) => {
            if (layer.encoding === undefined) {
                const x = rewrittenPosition(layer.x, 'x');
                const y = rewrittenPosition(layer.y, 'y');
                return {
                    ...layer,
                    ...(x === undefined ? {} : { x }),
                    ...(y === undefined ? {} : { y }),
                };
            }
            const x = rewrittenChannel(layer.encoding.x, 'x');
            const y = rewrittenChannel(layer.encoding.y, 'y');
            return {
                ...layer,
                encoding: {
                    ...layer.encoding,
                    ...(x === undefined ? {} : { x }),
                    ...(y === undefined ? {} : { y }),
                },
            };
        };
        if (input.layers !== undefined)
            return { ...input, layers: input.layers.map(rewrite) };
        if (input.encoding === undefined) {
            const x = rewrittenPosition(input.x, 'x');
            const y = rewrittenPosition(input.y, 'y');
            return {
                ...input,
                ...(x === undefined ? {} : { x }),
                ...(y === undefined ? {} : { y }),
            };
        }
        const x = rewrittenChannel(input.encoding.x, 'x');
        const y = rewrittenChannel(input.encoding.y, 'y');
        return {
            ...input,
            encoding: {
                ...input.encoding,
                ...(x === undefined ? {} : { x }),
                ...(y === undefined ? {} : { y }),
            },
        };
    }
    function unionDomains(domains, axis, scaleType) {
        const values = domains.flat();
        const allNumbers = values.every((value) => typeof value === 'number');
        const allStrings = values.every((value) => typeof value === 'string');
        if (scaleType === 'band' || scaleType === 'point') {
            if (!allNumbers && !allStrings) {
                throw new GraflumeError('INCOMPATIBLE_SCALE', `Shared ${axis} categorical scale domains cannot mix number and string identities.`, { path: '$.resolve.scale' });
            }
            const seen = new Set();
            return values.filter((value) => {
                const key = `${typeof value}:${String(value)}`;
                if (seen.has(key))
                    return false;
                seen.add(key);
                return true;
            });
        }
        if (allNumbers) {
            return [Math.min(...values), Math.max(...values)];
        }
        if (allStrings)
            return [...new Set(values)];
        throw new GraflumeError('INCOMPATIBLE_SCALE', `Shared ${axis} scale domains cannot mix numeric and categorical values.`, { path: '$.resolve.scale' });
    }
    function scaleParameterSignature(scale, descriptor) {
        const type = descriptor.type;
        return JSON.stringify({
            type,
            reverse: descriptor.reverse,
            outOfBounds: descriptor.outOfBounds,
            ...(type === 'log' ? { base: scale.base ?? 10 } : {}),
            ...(type === 'pow' ? { exponent: scale.exponent ?? 1 } : {}),
            ...(type === 'symlog' || type === 'asinh' ? { constant: scale.constant ?? 1 } : {}),
            ...(type === 'linear' || type === 'time' || type === 'utc' ? { nice: scale.nice ?? true } : {}),
            ...(type === 'band'
                ? { paddingInner: scale.paddingInner ?? 0.1, paddingOuter: scale.paddingOuter ?? 0.05 }
                : {}),
            ...(type === 'point' ? { paddingOuter: scale.paddingOuter ?? 0.5 } : {}),
        });
    }
    function harmonizeSharedScales(views, registry) {
        const resolved = views.map((view) => {
            if (isCompositionSpec(view.spec)) {
                fail$2('Shared position scales currently require unit child views.', '$.resolve.scale');
            }
            const theme = registry.themes.resolve(view.spec.theme ?? defaultThemeId);
            const spec = normalizeSpec$1(view.spec, theme);
            const scales = resolveScales(spec, { x: 0, y: 0, width: 100, height: 100 });
            const axisScale = (axisId) => {
                const channel = scales.axes[axisId]?.channel;
                if (channel === undefined)
                    return {};
                return spec.layers.find((layer) => layer[channel].axisId === axisId)?.[channel].scale ?? {};
            };
            return {
                view,
                scales,
                signatures: Object.fromEntries(Object.values(scales.axes).flatMap((resolvedAxis) => resolvedAxis === undefined
                    ? []
                    : [
                        [
                            resolvedAxis.id,
                            scaleParameterSignature(axisScale(resolvedAxis.id), resolvedAxis.scale.descriptor),
                        ],
                    ])),
            };
        });
        const domains = {};
        const axisIds = new Set(resolved.flatMap(({ scales }) => Object.values(scales.axes).flatMap((axis) => (axis === undefined ? [] : [axis.id]))));
        for (const axis of axisIds) {
            const participants = resolved.filter(({ scales }) => scales.axes[axis] !== undefined);
            if (participants.length === 0)
                continue;
            const signatures = participants.map(({ signatures }) => signatures[axis]);
            const first = signatures[0];
            if (first === undefined || signatures.some((signature) => signature !== first)) {
                throw new GraflumeError('INCOMPATIBLE_SCALE', `Shared ${axis} scales require the same mathematical parameters, scale type, reverse direction, and out-of-bounds policy.`, { path: '$.resolve.scale' });
            }
            const channel = participants[0].scales.axes[axis].channel;
            if (participants.some(({ scales }) => scales.axes[axis].channel !== channel)) {
                throw new GraflumeError('INCOMPATIBLE_SCALE', `Shared axis "${axis}" cannot bind both x and y channels.`, { path: '$.resolve.scale' });
            }
            domains[axis] = unionDomains(participants.map(({ scales }) => scales.axes[axis].scale.domain()), axis, participants[0].scales.axes[axis].scale.descriptor.type);
        }
        return resolved.map(({ view }) => ({
            ...view,
            spec: forceSharedDomains(view.spec, domains),
        }));
    }
    function interiorAxis(input) {
        if (input === false)
            return false;
        return {
            ...input,
            line: false,
            ticks: false,
            labels: false,
            title: false,
        };
    }
    function boundaryAxisInput(input, channel, boundary) {
        if (input === undefined || boundary.has(positionAxisId(channel, input)))
            return input;
        return typeof input === 'string'
            ? { field: input, axis: interiorAxis(undefined) }
            : { ...input, axis: interiorAxis(input.axis) };
    }
    function sharedAxisViewSpec(input, parentAxes, boundary) {
        const axes = { ...input.axes, ...parentAxes };
        for (const id of new Set([...builtInAxisIds, ...Object.keys(axes)])) {
            if (!boundary.has(id))
                axes[id] = interiorAxis(axes[id]);
        }
        const rewriteLayer = (layer) => layer.encoding === undefined
            ? {
                ...layer,
                x: boundaryAxisInput(layer.x, 'x', boundary),
                y: boundaryAxisInput(layer.y, 'y', boundary),
            }
            : {
                ...layer,
                encoding: {
                    ...layer.encoding,
                    x: boundaryAxisInput(layer.encoding.x, 'x', boundary),
                    y: boundaryAxisInput(layer.encoding.y, 'y', boundary),
                },
            };
        const output = {
            ...input,
            axes,
            ...(input.layers === undefined ? {} : { layers: input.layers.map(rewriteLayer) }),
        };
        if (input.layers !== undefined)
            return output;
        return input.encoding === undefined
            ? {
                ...output,
                x: boundaryAxisInput(input.x, 'x', boundary),
                y: boundaryAxisInput(input.y, 'y', boundary),
            }
            : {
                ...output,
                encoding: {
                    ...input.encoding,
                    x: boundaryAxisInput(input.encoding.x, 'x', boundary),
                    y: boundaryAxisInput(input.encoding.y, 'y', boundary),
                },
            };
    }
    function sharedAxisViews(input, views) {
        const topByColumn = new Map();
        const bottomByColumn = new Map();
        const leftByRow = new Map();
        const rightByRow = new Map();
        for (const view of views) {
            topByColumn.set(view.column, Math.min(topByColumn.get(view.column) ?? view.row, view.row));
            bottomByColumn.set(view.column, Math.max(bottomByColumn.get(view.column) ?? view.row, view.row));
            leftByRow.set(view.row, Math.min(leftByRow.get(view.row) ?? view.column, view.column));
            rightByRow.set(view.row, Math.max(rightByRow.get(view.row) ?? view.column, view.column));
        }
        return views.map((view) => {
            const boundary = new Set();
            const axes = { ...view.spec.axes, ...input.axes };
            for (const id of new Set([...builtInAxisIds, ...Object.keys(axes)])) {
                const channel = axisChannel(id, axes);
                if (channel === undefined)
                    continue;
                const authored = axes[id];
                const position = authored === false
                    ? defaultAxisPosition(id, channel)
                    : (authored?.position ?? defaultAxisPosition(id, channel));
                if ((position === 'bottom' && bottomByColumn.get(view.column) === view.row) ||
                    (position === 'top' && topByColumn.get(view.column) === view.row) ||
                    (position === 'left' && leftByRow.get(view.row) === view.column) ||
                    (position === 'right' && rightByRow.get(view.row) === view.column)) {
                    boundary.add(id);
                }
            }
            return { ...view, spec: sharedAxisViewSpec(view.spec, input.axes, boundary) };
        });
    }
    const visualColorChannels = ['color', 'fill', 'stroke'];
    function channelField(input) {
        return typeof input === 'string' ? input : input?.field;
    }
    function forceVisualDomain(input, field, domain) {
        const rewriteEncoding = (encoding) => {
            const output = { ...encoding };
            for (const channel of visualColorChannels) {
                const value = encoding[channel];
                if (channelField(value) === field)
                    output[channel] = forceChannelDomain(value, domain);
            }
            return output;
        };
        const rewriteLayer = (layer) => layer.encoding === undefined ? layer : { ...layer, encoding: rewriteEncoding(layer.encoding) };
        if (input.layers !== undefined)
            return { ...input, layers: input.layers.map(rewriteLayer) };
        return input.encoding === undefined
            ? input
            : { ...input, encoding: rewriteEncoding(input.encoding) };
    }
    function withCompositionLegend(input, parent) {
        return parent.legend === undefined ? input : { ...input, legend: parent.legend };
    }
    function visualFieldValues(spec, field) {
        const values = [];
        let matchedLayers = 0;
        for (const layer of spec.layers) {
            const matches = visualColorChannels.some((channel) => layer.encoding[channel]?.field === field);
            if (!matches)
                continue;
            matchedLayers += 1;
            const table = DataTable.from(layer.data);
            for (let index = 0; index < table.length; index += 1)
                values.push(table.value(index, field));
        }
        return { values, matchedLayers };
    }
    function harmonizeSharedLegendDomains(parent, views, registry, shareLegend, shareColorbar) {
        const normalized = views.map((view) => {
            const spec = withCompositionLegend(view.spec, parent);
            const theme = registry.themes.resolve(spec.theme ?? defaultThemeId);
            const value = normalizeSpec$1(spec, theme);
            return { view, spec, normalized: value, model: resolveLegendModel(value, theme) };
        });
        const models = normalized.flatMap(({ model }) => (model === null ? [] : [model]));
        if (models.length === 0 || models.every(({ mode }) => mode === 'layers')) {
            return normalized.map(({ view, spec }) => ({ ...view, spec }));
        }
        const mode = models[0].mode;
        if (models.some((model) => model.mode !== mode)) {
            fail$2('Shared legends require every participating view to use the same legend mode.', '$.resolve');
        }
        if ((mode === 'continuous' && !shareColorbar) || (mode !== 'continuous' && !shareLegend)) {
            return normalized.map(({ view, spec }) => ({ ...view, spec }));
        }
        const field = models[0].field;
        if (field === undefined || models.some((model) => model.field !== field)) {
            fail$2('Shared category legends and colorbars require one common field.', '$.legend.field');
        }
        const explicitColors = models.every((model) => model.spec.items.length > 0 && model.spec.items.every((item) => item.color !== undefined));
        const collected = normalized.map(({ normalized: spec }) => visualFieldValues(spec, field));
        if (!explicitColors && collected.some(({ matchedLayers }) => matchedLayers === 0)) {
            fail$2('Shared category legends and colorbars require a portable color, fill, or stroke encoding for their field, or explicit colored legend items.', '$.legend.field');
        }
        if (explicitColors)
            return normalized.map(({ view, spec }) => ({ ...view, spec }));
        const values = collected.flatMap(({ values: entries }) => entries);
        const domain = mode === 'continuous'
            ? (() => {
                const numbers = values.flatMap((value) => typeof value === 'number' && Number.isFinite(value)
                    ? [value]
                    : value instanceof Date
                        ? [value.getTime()]
                        : []);
                if (numbers.length === 0) {
                    fail$2('Shared colorbar field must contain finite numeric or temporal values.', '$.legend.field');
                }
                const minimum = Math.min(...numbers);
                const maximum = Math.max(...numbers);
                return minimum === maximum ? [minimum - 1, maximum + 1] : [minimum, maximum];
            })()
            : (() => {
                const seen = new Set();
                const categories = [];
                for (const value of values) {
                    const normalizedValue = value instanceof Date ? value.toISOString() : value;
                    if (typeof normalizedValue !== 'number' && typeof normalizedValue !== 'string')
                        continue;
                    const key = `${typeof normalizedValue}:${String(normalizedValue)}`;
                    if (seen.has(key))
                        continue;
                    seen.add(key);
                    categories.push(normalizedValue);
                }
                if (categories.length === 0) {
                    fail$2('Shared category legend field must contain string or number values.', '$.legend.field');
                }
                return categories;
            })();
        return normalized.map(({ view, spec }) => ({
            ...view,
            spec: forceVisualDomain(spec, field, domain),
        }));
    }
    function scopedLegendLayer(layer, view) {
        return {
            ...layer,
            id: `${view.id}/${layer.id}`,
            name: view.label === '' ? layer.name : `${view.label}: ${layer.name}`,
        };
    }
    function sharedLegendItemId(viewId, id) {
        // Legend item ids are already normalized, and inferred category ids carry a
        // full canonical hex token. Keep that suffix intact so values with a common
        // prefix cannot collapse to the same interactive id.
        return `shared-${safeToken(viewId)}-${id}`;
    }
    function mergeSharedLegendItems(models, mode, locale) {
        if (mode === 'layers') {
            return models.flatMap(({ view, model }) => model.items.map((item) => ({
                ...item,
                id: sharedLegendItemId(view.id, item.id),
                label: view.label === '' ? item.label : `${view.label}: ${item.label}`,
                ...(item.layerId === undefined ? {} : { layerId: `${view.id}/${item.layerId}` }),
            })));
        }
        if (mode === 'categories') {
            const seen = new Set();
            return models.flatMap(({ model }) => model.items.flatMap((item) => {
                const key = valueKey(item.value);
                if (seen.has(key))
                    return [];
                seen.add(key);
                const { layerId: _layerId, ...unscoped } = item;
                return [{ ...unscoped, id: `shared-${item.id}` }];
            }));
        }
        const numericItems = models
            .flatMap(({ model }) => model.items)
            .filter((item) => typeof item.value === 'number' && Number.isFinite(item.value));
        if (numericItems.length === 0)
            return models[0]?.model.items ?? [];
        const minimum = numericItems.reduce((left, right) => (left.value <= right.value ? left : right));
        const maximum = numericItems.reduce((left, right) => (left.value >= right.value ? left : right));
        let formatter;
        try {
            formatter = new Intl.NumberFormat(locale, { maximumFractionDigits: 6 });
        }
        catch {
            formatter = new Intl.NumberFormat(undefined, { maximumFractionDigits: 6 });
        }
        return [
            { ...minimum, id: 'shared-continuous-min', label: formatter.format(minimum.value) },
            { ...maximum, id: 'shared-continuous-max', label: formatter.format(maximum.value) },
        ];
    }
    function resolveSharedLegend(parent, views, registry, width, height, shareLegend, shareColorbar) {
        const resolved = views.flatMap((view) => {
            const spec = withCompositionLegend(view.spec, parent);
            const theme = registry.themes.resolve(spec.theme ?? defaultThemeId);
            const normalized = normalizeSpec$1(spec, theme);
            const model = resolveLegendModel(normalized, theme, width, height);
            return model === null ? [] : [{ view, normalized, model }];
        });
        if (resolved.length === 0)
            return null;
        const mode = resolved[0].model.mode;
        if (resolved.some(({ model }) => model.mode !== mode)) {
            fail$2('Shared legends require every participating view to use the same legend mode.', '$.resolve');
        }
        if ((mode === 'continuous' && !shareColorbar) || (mode !== 'continuous' && !shareLegend)) {
            return null;
        }
        const first = resolved[0];
        const items = mergeSharedLegendItems(resolved, mode, first.normalized.locale);
        const { layerId: _layerId, items: _items, mode: _mode, ...legendRest } = first.model.spec;
        const combined = {
            ...first.normalized,
            layers: resolved.flatMap(({ view, normalized }) => normalized.layers.map((layer) => scopedLegendLayer(layer, view))),
            legend: { ...legendRest, mode, items },
        };
        const theme = registry.themes.resolve(parent.theme ?? defaultThemeId);
        return resolveLegendModel(combined, theme, width, height);
    }
    function insetContent(content, insets) {
        return {
            x: content.x + insets.left,
            y: content.y + insets.top,
            width: Math.max(1, content.width - insets.left - insets.right),
            height: Math.max(1, content.height - insets.top - insets.bottom),
        };
    }
    function sharedLegendVisibility(node, model, hidden) {
        const hiddenLayers = new Set(model.items.flatMap((item) => hidden.has(item.id) &&
            legendItemToggleable(model, item) &&
            item.layerId !== undefined &&
            (model.mode === 'layers' || item.value === undefined)
            ? [item.layerId]
            : []));
        const hiddenValues = model.items.flatMap((item) => hidden.has(item.id) && legendItemToggleable(model, item) && item.value !== undefined
            ? [item.value]
            : []);
        const visit = (candidate) => {
            if (candidate.type === 'group') {
                const layerId = candidate.id.endsWith(':group')
                    ? candidate.id.slice(0, -':group'.length)
                    : null;
                return {
                    ...candidate,
                    ...(layerId !== null && hiddenLayers.has(layerId) ? { visible: false } : {}),
                    children: candidate.children.map(visit),
                };
            }
            if (model.mode !== 'categories' || model.field === undefined || hiddenValues.length === 0) {
                return candidate;
            }
            const sources = [];
            if (candidate.datum?.tooltip !== undefined)
                sources.push(candidate.datum.tooltip);
            if (candidate.datum?.datum !== undefined)
                sources.push(candidate.datum.datum);
            const concealed = sources.some((source) => hiddenValues.some((value) => valueKey(source[model.field]) === valueKey(value)));
            return concealed ? { ...candidate, visible: false } : candidate;
        };
        return visit(node);
    }
    function compositionContentBounds(spec, width, height) {
        const titleHeight = spec.title === undefined ? 0 : spec.title.subtitle === undefined ? 36 : 56;
        return {
            x: spec.padding.left,
            y: spec.padding.top + titleHeight,
            width: Math.max(0, width - spec.padding.left - spec.padding.right),
            height: Math.max(0, height - spec.padding.top - spec.padding.bottom - titleHeight),
        };
    }
    function layoutViews(input, grid, content, spacing) {
        if (input.inset !== undefined) {
            const base = grid.views[0];
            const inset = grid.views[1];
            const insetBounds = {
                x: content.x + content.width * input.inset.x,
                y: content.y + content.height * input.inset.y,
                width: content.width * input.inset.width,
                height: content.height * input.inset.height,
            };
            return [
                { ...base, bounds: content, chartBounds: content },
                { ...inset, bounds: insetBounds, chartBounds: insetBounds },
            ];
        }
        const cellWidth = (content.width - spacing * Math.max(0, grid.columns - 1)) / grid.columns;
        const cellHeight = (content.height - spacing * Math.max(0, grid.rows - 1)) / grid.rows;
        const header = grid.showLabels ? facetHeaderHeight : 0;
        if (cellWidth < minimumCellWidth || cellHeight - header < minimumCellHeight) {
            fail$2(`Composition cells require at least ${minimumCellWidth}x${minimumCellHeight}px after spacing and labels.`, '$.width');
        }
        return grid.views.map((view) => {
            const bounds = {
                x: content.x + view.column * (cellWidth + spacing),
                y: content.y + view.row * (cellHeight + spacing),
                width: cellWidth,
                height: cellHeight,
            };
            return {
                ...view,
                bounds,
                chartBounds: { ...bounds, y: bounds.y + header, height: bounds.height - header },
            };
        });
    }
    function translatedRect(rect, dx, dy) {
        return { ...rect, x: rect.x + dx, y: rect.y + dy };
    }
    function scopeNode(node, viewId, dx, dy) {
        const scoped = {
            id: `${viewId}/${node.id}`,
            ...(node.datum === undefined
                ? {}
                : { datum: { ...node.datum, layerId: `${viewId}/${node.datum.layerId}` } }),
        };
        switch (node.type) {
            case 'group':
                return {
                    ...node,
                    ...scoped,
                    children: node.children.map((child) => scopeNode(child, viewId, dx, dy)),
                    ...(node.clip === undefined ? {} : { clip: translatedRect(node.clip, dx, dy) }),
                };
            case 'line':
                return {
                    ...node,
                    ...scoped,
                    x1: node.x1 + dx,
                    y1: node.y1 + dy,
                    x2: node.x2 + dx,
                    y2: node.y2 + dy,
                };
            case 'path':
                return {
                    ...node,
                    ...scoped,
                    points: node.points.map(({ x, y }) => ({ x: x + dx, y: y + dy })),
                    ...(node.subpaths === undefined
                        ? {}
                        : {
                            subpaths: node.subpaths.map((path) => path.map(({ x, y }) => ({ x: x + dx, y: y + dy }))),
                        }),
                };
            case 'rect':
                return { ...node, ...scoped, x: node.x + dx, y: node.y + dy };
            case 'circle':
                return { ...node, ...scoped, cx: node.cx + dx, cy: node.cy + dy };
            case 'text':
                return { ...node, ...scoped, x: node.x + dx, y: node.y + dy };
        }
    }
    function sourceIndices(indices, sourceMap) {
        if (sourceMap === undefined)
            return [...indices];
        return [...new Set(indices.flatMap((index) => sourceMap.rows[index] ?? []))].sort((left, right) => left - right);
    }
    function scopedLineage(lineage, viewId, sourceMap) {
        const inheritedTransforms = sourceMap?.lineage.transforms ?? [];
        const transforms = [
            ...inheritedTransforms,
            ...lineage.transforms.map((step) => ({
                ...step,
                index: step.index + inheritedTransforms.length,
            })),
        ];
        const sourceId = sourceMap?.lineage.sourceId ?? lineage.sourceId;
        const sourceRows = sourceMap?.sourceRows ?? lineage.sourceRows;
        return {
            ...lineage,
            sourceId: `${viewId}/${sourceId}`,
            sourceRows,
            transforms,
            rowSources: lineage.rowSources.map((indices) => sourceIndices(indices, sourceMap)),
            summary: `[${viewId}] ${sourceRows} source rows, ${transforms.length} ordered transforms, ${lineage.outputRows} output rows.`,
        };
    }
    function scopedSemantic(mark, view) {
        const dx = view.chartBounds.x;
        const dy = view.chartBounds.y;
        const remapped = sourceIndices(mark.lineage.sourceRowIndices, view.sourceMap);
        const semanticViewId = mark.viewId === 'plot' ? view.id : `${view.id}/${mark.viewId}`;
        return {
            ...mark,
            id: `${view.id}/${mark.id}`,
            viewId: semanticViewId,
            layerId: `${view.id}/${mark.layerId}`,
            bounds: translatedRect(mark.bounds, dx, dy),
            label: view.label === '' ? mark.label : `${view.label}. ${mark.label}`,
            lineage: {
                ...mark.lineage,
                sourceId: `${view.id}/${view.sourceMap?.lineage.sourceId ?? mark.lineage.sourceId}`,
                sourceRowIndices: remapped,
                truncated: mark.lineage.truncated || remapped.length > mark.lineage.sourceRowIndices.length,
            },
        };
    }
    function scopedLayer(layer, view) {
        return {
            ...layer,
            id: `${view.id}/${layer.id}`,
            name: view.label === '' ? layer.name : `${view.label}: ${layer.name}`,
        };
    }
    function titleNodes$1(spec, theme, width) {
        if (spec.title === undefined)
            return [];
        const align = spec.title.align ?? 'left';
        const x = align === 'left'
            ? spec.padding.left
            : align === 'right'
                ? width - spec.padding.right
                : width / 2;
        const nodes = [
            {
                type: 'text',
                ...nodeBase('composition:title', { zIndex: 1000 }),
                x,
                y: spec.padding.top,
                text: spec.title.text,
                fill: theme.colors.text,
                fontFamily: theme.typography.fontFamily,
                fontSize: theme.typography.titleSize,
                fontWeight: theme.typography.titleWeight ?? 700,
                align,
                baseline: 'top',
                rotation: 0,
            },
        ];
        if (spec.title.subtitle !== undefined) {
            nodes.push({
                type: 'text',
                ...nodeBase('composition:subtitle', { zIndex: 1000 }),
                x,
                y: spec.padding.top + theme.typography.titleSize + 6,
                text: spec.title.subtitle,
                fill: theme.colors.subtitle ?? theme.colors.mutedText,
                fontFamily: theme.typography.fontFamily,
                fontSize: theme.typography.subtitleSize,
                fontWeight: theme.typography.subtitleWeight ?? 400,
                align,
                baseline: 'top',
                rotation: 0,
            });
        }
        return nodes;
    }
    function labelNode$1(view, theme) {
        if (view.chartBounds.y === view.bounds.y || view.label === '')
            return [];
        return [
            {
                type: 'text',
                ...nodeBase(`composition:label:${view.id}`, { zIndex: 900 }),
                x: view.bounds.x + 4,
                y: view.bounds.y + 3,
                text: view.label,
                fill: theme.colors.text,
                fontFamily: theme.typography.fontFamily,
                fontSize: theme.typography.fontSize,
                fontWeight: 700,
                align: 'left',
                baseline: 'top',
                rotation: 0,
            },
        ];
    }
    function performanceProfile(results) {
        const order = { standard: 0, large: 1, ultra: 2 };
        return results.reduce((profile, result) => order[result.scene.metadata.performanceProfile] > order[profile]
            ? result.scene.metadata.performanceProfile
            : profile, 'standard');
    }
    function accessibilityDescription(spec, layouts, results) {
        const children = results.slice(0, 16).flatMap((result, index) => {
            const label = layouts[index]?.label ?? `View ${index + 1}`;
            const description = result.scene.accessibility.description;
            return description === undefined ? [label] : [`${label}: ${description}`];
        });
        if (results.length > 16)
            children.push(`${results.length - 16} additional views.`);
        const description = [spec.description, spec.accessibility.summary, ...children]
            .filter((value) => value !== undefined && value !== '')
            .join(' ');
        return description === '' ? undefined : description;
    }
    function gridFor(input, kind) {
        switch (kind) {
            case 'facet':
                return materializeFacet(input);
            case 'repeat':
                return materializeRepeat(input);
            case 'hconcat':
            case 'vconcat':
            case 'concat':
                return materializeConcat(input, kind);
            case 'inset':
                return materializeInset(input);
        }
    }
    /** Compile a validated composition into one renderer-neutral Canvas scene. */
    function compileCompositionWithRegistry(input, registry, options, runtime, compiler) {
        assertValidSpec(input);
        const kind = compositionKind(input);
        if (kind === null)
            fail$2('Exactly one composition operator is required.');
        const resolve = resolveComposition(input.resolve, kind);
        if (kind === 'layer') {
            const result = compiler.compileUnit(flattenLayerComposition(input), registry, options, runtime);
            return {
                ...result,
                scene: {
                    ...result.scene,
                    metadata: {
                        ...result.scene.metadata,
                        composition: {
                            kind,
                            viewCount: 1,
                            viewIds: ['plot'],
                            resolve,
                        },
                    },
                },
            };
        }
        const theme = registry.themes.resolve(input.theme ?? defaultThemeId);
        const spec = normalizeSpec$1(input, theme);
        const width = Math.max(1, spec.width === 'container' ? (options.width ?? 640) : spec.width);
        const height = Math.max(1, spec.height === 'container' ? (options.height ?? 400) : spec.height);
        const baseContent = compositionContentBounds(spec, width, height);
        const spacing = input.spacing ?? 16;
        const grid = gridFor(input, kind);
        if (grid.views.length === 0)
            fail$2('Composition produced no views.', `$.${kind}`);
        if (grid.views.length > maximumCompositionViews) {
            fail$2(`Composition exceeds ${maximumCompositionViews} materialized views.`, `$.${kind}`);
        }
        let views = resolve.scale === 'shared' ? harmonizeSharedScales(grid.views, registry) : grid.views;
        const shareLegend = resolve.legend === 'shared';
        const shareColorbar = resolve.colorbar === 'shared';
        if (shareLegend || shareColorbar) {
            views = harmonizeSharedLegendDomains(input, views, registry, shareLegend, shareColorbar);
        }
        if (resolve.axis === 'shared')
            views = sharedAxisViews(input, views);
        const sharedLegendModel = resolveSharedLegend(input, views, registry, width, height, shareLegend, shareColorbar);
        const content = insetContent(baseContent, legendExternalInsets(sharedLegendModel));
        const layouts = layoutViews(input, { ...grid, views }, content, spacing);
        const results = layouts.map((view) => {
            const child = interactionForView(sharedLegendModel === null ? view.spec : { ...view.spec, legend: false }, input);
            return compiler.compile(child, registry, { width: view.chartBounds.width, height: view.chartBounds.height }, runtimeForView(runtime, view.id));
        });
        const layers = layouts.flatMap((view, index) => results[index].spec.layers.map((layer) => scopedLayer(layer, view)));
        if (layers.length > maximumCompositionLayers) {
            fail$2(`Composition exceeds ${maximumCompositionLayers} compiled layers.`, `$.${kind}`);
        }
        const dataLineage = {};
        layouts.forEach((view, index) => {
            for (const [layerId, lineage] of Object.entries(results[index].dataLineage)) {
                dataLineage[`${view.id}/${layerId}`] = scopedLineage(lineage, view.id, view.sourceMap);
            }
        });
        const hiddenLegendItems = runtime.hiddenLegendItemIds ?? new Set();
        const scopedChildren = layouts.map((view, index) => results[index].scene.root.children.map((node) => {
            const scoped = scopeNode(node, view.id, view.chartBounds.x, view.chartBounds.y);
            return sharedLegendModel === null
                ? scoped
                : sharedLegendVisibility(scoped, sharedLegendModel, hiddenLegendItems);
        }));
        const viewGroups = layouts.map((view, index) => {
            results[index];
            const children = [...labelNode$1(view, theme), ...scopedChildren[index]];
            return group(`composition:view:${view.id}`, children, {
                zIndex: kind === 'inset' ? index * 1000 : index,
                clip: view.bounds,
            });
        });
        const scopedLayerGroups = scopedChildren.flatMap((children) => children.filter((node) => node.type === 'group' && node.id.endsWith(':group')));
        const sharedLegend = compileLegend(sharedLegendModel, scopedLayerGroups, content, width, height, theme, hiddenLegendItems);
        const scopedTechnicalIndicators = layouts.flatMap((view, index) => (results[index].scene.metadata.technicalIndicators ?? []).map((indicator) => ({
            ...indicator,
            layerId: `${view.id}/${indicator.layerId}`,
        })));
        const technicalIndicatorPanels = scopedTechnicalIndicators.length === 0
            ? []
            : layouts.flatMap((view, index) => {
                const result = results[index];
                const coordinateView = result.coordinateViews[0];
                if (coordinateView === undefined)
                    return [];
                const declared = result.scene.metadata.technicalIndicatorPanels?.[0];
                const id = declared?.id ?? (index === 0 ? 'price' : view.id);
                return [
                    {
                        id,
                        bounds: translatedRect(coordinateView.bounds, view.chartBounds.x, view.chartBounds.y),
                        layerIds: result.spec.layers.map(({ id: layerId }) => `${view.id}/${layerId}`),
                        placement: declared?.placement ?? (index === 0 ? 'price' : 'indicator'),
                    },
                ];
            });
        const technicalCrosshairPositions = runtime.technicalCrosshairValue === undefined
            ? []
            : layouts.flatMap((view, index) => {
                const result = results[index];
                const coordinateView = result.coordinateViews[0];
                const panel = technicalIndicatorPanels[index];
                const xScale = coordinateView?.coordinates.axes.x;
                if (coordinateView === undefined || panel === undefined || xScale === undefined)
                    return [];
                const localX = xScale.map(runtime.technicalCrosshairValue);
                const x = localX + view.chartBounds.x;
                if (!Number.isFinite(x) || x < panel.bounds.x || x > panel.bounds.x + panel.bounds.width)
                    return [];
                return [{ panelId: panel.id, x, bounds: panel.bounds }];
            });
        const technicalCrosshairNodes = technicalCrosshairPositions.map(({ panelId, x, bounds }) => ({
            type: 'line',
            ...nodeBase(`technical-crosshair:${panelId}`, { zIndex: 1_800, opacity: 0.72 }),
            x1: x,
            y1: bounds.y,
            x2: x,
            y2: bounds.y + bounds.height,
            stroke: theme.colors.text,
            lineWidth: 1,
            dash: [4, 3],
        }));
        const root = group('scene:root', [
            ...titleNodes$1(spec, theme, width),
            ...viewGroups,
            ...technicalCrosshairNodes,
            ...sharedLegend.nodes,
        ]);
        const semanticIndex = layouts
            .flatMap((view, index) => results[index].scene.semanticIndex.map((mark) => scopedSemantic(mark, view)))
            .slice(0, spec.accessibility.maxRows);
        const totalRows = results.reduce((sum, result) => sum + result.scene.metadata.rowCount, 0);
        const description = accessibilityDescription(spec, layouts, results);
        const scene = {
            width,
            height,
            background: theme.colors.background,
            root,
            accessibility: {
                label: spec.accessibility.label ??
                    `${spec.title?.text ?? 'Graflume composition'}. ${layouts.length} views.`,
                ...(description === undefined ? {} : { description }),
            },
            semanticIndex,
            metadata: {
                rowCount: totalRows,
                renderedNodeCount: countSceneNodes(root),
                performanceProfile: performanceProfile(results),
                hitTestingEnabled: results.every((result) => result.scene.metadata.hitTestingEnabled),
                dataLineage: Object.values(dataLineage).map(({ summary }) => summary),
                ...(scopedTechnicalIndicators.length === 0
                    ? {}
                    : {
                        technicalIndicators: scopedTechnicalIndicators,
                        technicalIndicatorPanels,
                        ...(runtime.technicalCrosshairValue === undefined
                            ? {}
                            : {
                                technicalIndicatorCrosshair: {
                                    value: runtime.technicalCrosshairValue,
                                    panelIds: technicalCrosshairPositions.map(({ panelId }) => panelId),
                                    positions: technicalCrosshairPositions.map(({ panelId, x }) => ({
                                        panelId,
                                        x,
                                    })),
                                },
                            }),
                    }),
                composition: {
                    kind,
                    viewCount: layouts.length,
                    viewIds: layouts.map(({ id }) => id),
                    resolve,
                },
            },
        };
        const normalized = {
            ...spec,
            layers,
            legend: sharedLegendModel?.spec ?? false,
            highlights: [],
            annotations: [],
        };
        const coordinates = Object.freeze({
            plot: Object.freeze({ ...content }),
            axes: Object.freeze({}),
        });
        registerLegendLayout(scene, sharedLegend.layout);
        const coordinateViews = layouts.flatMap((view, index) => results[index].coordinateViews.map((coordinateView) => ({
            ...coordinateView,
            id: coordinateView.id === 'plot' ? view.id : `${view.id}/${coordinateView.id}`,
            label: coordinateView.label === ''
                ? view.label
                : view.label === ''
                    ? coordinateView.label
                    : `${view.label}: ${coordinateView.label}`,
            bounds: translatedRect(coordinateView.bounds, view.chartBounds.x, view.chartBounds.y),
            offsetX: coordinateView.offsetX + view.chartBounds.x,
            offsetY: coordinateView.offsetY + view.chartBounds.y,
        })));
        return {
            scene,
            spec: normalized,
            theme,
            dataLineage,
            // Keep the historical aggregate coordinate surface axis-free. Runtime
            // interaction resolves the explicit leaf contexts in coordinateViews.
            coordinates,
            coordinateViews,
        };
    }

    function analyticValue(value, type) {
        if (type === 'nominal' || type === 'ordinal') {
            if (value === null || value === undefined || Array.isArray(value))
                return undefined;
            return value instanceof Date ? value.toISOString() : String(value);
        }
        if (value instanceof Date) {
            const timestamp = value.getTime();
            return Number.isFinite(timestamp) ? timestamp : undefined;
        }
        if (type === 'temporal' && typeof value === 'string') {
            const timestamp = Date.parse(value);
            return Number.isFinite(timestamp) ? timestamp : undefined;
        }
        return typeof value === 'number' && Number.isFinite(value) ? value : undefined;
    }
    function sampleFor(layer, rowIndex) {
        const datum = layer.table.row(rowIndex);
        const x = analyticValue(layer.table.value(rowIndex, layer.layer.x.field), layer.xType);
        const y = analyticValue(layer.table.value(rowIndex, layer.layer.y.field), layer.yType);
        return {
            ...(x === undefined ? {} : { [layer.xAxisId]: x }),
            ...(y === undefined ? {} : { [layer.yAxisId]: y }),
            layerId: layer.layer.id,
            rowIndex,
            datum,
        };
    }
    function filteredLineage(lineage, retained, inputRows) {
        return {
            ...lineage,
            outputRows: retained.length,
            rowSources: retained.map((index) => lineage.rowSources[index] ?? []),
            summary: `${lineage.summary} Runtime analytic selection retained ${retained.length} of ${inputRows} rows.`,
        };
    }
    function filteredLayer(layer, matches) {
        const retained = [];
        for (let index = 0; index < layer.table.length; index += 1) {
            if (matches(sampleFor(layer, index)))
                retained.push(index);
        }
        const rows = retained.map((index) => layer.table.row(index));
        return {
            layer: {
                ...layer,
                table: DataTable.fromRows(rows),
                lineage: filteredLineage(layer.lineage, retained, layer.table.length),
            },
            inputRows: layer.table.length,
            outputRows: rows.length,
        };
    }
    /**
     * Apply a transient analytic predicate after scale resolution. Axes retain the
     * authored/full-data domains while marks, semantic rows, hit targets, labels,
     * and lineage consume only matching rows.
     */
    function filterScaleResolutionByAnalyticSelection(scales, state) {
        if (state.selections.length === 0) {
            const rows = scales.layers.reduce((sum, layer) => sum + layer.table.length, 0);
            return {
                scales,
                summary: { inputRows: rows, outputRows: rows, layers: Object.freeze({}) },
            };
        }
        const matches = analyticSelectionPredicate(state);
        const filtered = scales.layers.map((layer) => filteredLayer(layer, matches));
        const layers = filtered.map((entry) => entry.layer);
        const byId = new Map(layers.map((layer) => [layer.layer.id, layer]));
        const axes = Object.fromEntries(Object.entries(scales.axes).map(([axis, resolved]) => [
            axis,
            resolved === undefined
                ? resolved
                : {
                    ...resolved,
                    layers: resolved.layers.flatMap((layer) => {
                        const candidate = byId.get(layer.layer.id);
                        return candidate === undefined ? [] : [candidate];
                    }),
                },
        ]));
        const summaries = Object.freeze(Object.fromEntries(filtered.map((entry) => [
            entry.layer.layer.id,
            { inputRows: entry.inputRows, outputRows: entry.outputRows },
        ])));
        return {
            scales: { ...scales, layers, axes },
            summary: {
                inputRows: filtered.reduce((sum, entry) => sum + entry.inputRows, 0),
                outputRows: filtered.reduce((sum, entry) => sum + entry.outputRows, 0),
                layers: summaries,
            },
        };
    }

    function calculatedIndicatorPanel(layer) {
        if (typeof layer.mark === 'string' || layer.mark.type !== 'indicator')
            return null;
        const mark = layer.mark;
        if (mark.options?.calculate !== true)
            return null;
        const kind = typeof mark.options.kind === 'string' ? mark.options.kind : 'sma';
        const presentation = resolveTechnicalIndicatorPresentation(kind);
        return presentation?.placement === 'panel' ? presentation.panelId : null;
    }
    function paneChild(input, layers) {
        return {
            layers,
            ...(input.axes === undefined ? {} : { axes: input.axes }),
            ...(input.markLabels === undefined ? {} : { markLabels: input.markLabels }),
            ...(input.interaction === undefined ? {} : { interaction: input.interaction }),
            ...(input.accessibility === undefined ? {} : { accessibility: input.accessibility }),
            ...(input.renderer === undefined ? {} : { renderer: input.renderer }),
            ...(input.performance === undefined ? {} : { performance: input.performance }),
            ...(input.locale === undefined ? {} : { locale: input.locale }),
        };
    }
    /**
     * Converts a flat price/indicator layer chart into independent vertically
     * stacked panes. Overlay indicators remain with price; oscillator-like
     * indicators are grouped by their stable registry panel ID.
     */
    function materializeTechnicalIndicatorPanes(input) {
        if (input.layers === undefined || input.layers.length === 0)
            return input;
        const priceLayers = [];
        const indicatorPanes = new Map();
        for (const layer of input.layers) {
            const panelId = calculatedIndicatorPanel(layer);
            if (panelId === null) {
                priceLayers.push(layer);
                continue;
            }
            const layers = indicatorPanes.get(panelId) ?? [];
            layers.push(layer);
            indicatorPanes.set(panelId, layers);
        }
        const viewCount = indicatorPanes.size + (priceLayers.length === 0 ? 0 : 1);
        if (indicatorPanes.size === 0 || viewCount < 2)
            return input;
        const parent = { ...input };
        delete parent.layers;
        delete parent.mark;
        delete parent.x;
        delete parent.y;
        delete parent.encoding;
        const views = [
            ...(priceLayers.length === 0 ? [] : [paneChild(input, priceLayers)]),
            ...[...indicatorPanes.values()].map((layers) => paneChild(input, layers)),
        ];
        return {
            ...parent,
            vconcat: views,
            spacing: Math.min(12, input.spacing ?? 8),
            resolve: {
                ...input.resolve,
                scale: 'independent',
                axis: 'independent',
                legend: input.resolve?.legend ?? 'shared',
            },
        };
    }

    function activeAxes(scales) {
        return Object.values(scales.axes).flatMap((resolved) => {
            if (resolved === undefined)
                return [];
            const layerData = resolved.layers.find(({ layer }) => !isAxislessLayer(layer));
            if (layerData === undefined)
                return [];
            const encoding = resolved.channel === 'x' ? layerData.layer.x : layerData.layer.y;
            return [
                {
                    id: resolved.id,
                    channel: resolved.channel,
                    axis: encoding.axis,
                    scale: resolved.scale,
                    title: encoding.title,
                },
            ];
        });
    }
    function intersectRects(left, right) {
        const x = Math.max(left.x, right.x);
        const y = Math.max(left.y, right.y);
        const endX = Math.min(left.x + left.width, right.x + right.width);
        const endY = Math.min(left.y + left.height, right.y + right.height);
        return { x, y, width: Math.max(0, endX - x), height: Math.max(0, endY - y) };
    }
    function resolveLayerClip(layerData, scales, plot) {
        const authored = layerData.layer.clip;
        if (authored === false)
            return undefined;
        if (authored === true)
            return plot;
        if (authored.type === 'plot') {
            return intersectRects(plot, {
                x: plot.x + authored.x * plot.width,
                y: plot.y + authored.y * plot.height,
                width: authored.width * plot.width,
                height: authored.height * plot.height,
            });
        }
        const mapped = (channel, range) => {
            const id = range.axis ?? (channel === 'x' ? layerData.xAxisId : layerData.yAxisId);
            const resolved = scales.axes[id];
            if (resolved === undefined || resolved.channel !== channel) {
                throw new GraflumeError('INVALID_SPEC', `Layer "${layerData.layer.id}" clip references unresolved ${channel}-axis "${id}".`, { path: `$.layers[${layerData.layer.id}].clip.${channel}.axis` });
            }
            const from = resolved.scale.map(range.from);
            const to = resolved.scale.map(range.to);
            if (!Number.isFinite(from) || !Number.isFinite(to)) {
                throw new GraflumeError('INVALID_SPEC', `Layer "${layerData.layer.id}" clip range cannot be mapped on axis "${id}".`, { path: `$.layers[${layerData.layer.id}].clip.${channel}` });
            }
            return [Math.min(from, to), Math.max(from, to)];
        };
        const x = authored.x === undefined ? [plot.x, plot.x + plot.width] : mapped('x', authored.x);
        const y = authored.y === undefined ? [plot.y, plot.y + plot.height] : mapped('y', authored.y);
        return intersectRects(plot, { x: x[0], y: y[0], width: x[1] - x[0], height: y[1] - y[0] });
    }
    function axisContext(axis, plot, theme, locale) {
        return {
            ...axis,
            plot,
            theme,
            ...(locale === undefined ? {} : { locale }),
        };
    }
    function samePlot(left, right) {
        return (left.x === right.x &&
            left.y === right.y &&
            left.width === right.width &&
            left.height === right.height);
    }
    function titleNodes(spec, theme, width, plot, titleY, subtitleY) {
        if (spec.title === undefined)
            return [];
        const align = spec.title.align ?? 'left';
        const titleLeft = theme.typography.titlePosition === 'panel' ? plot.x : spec.padding.left;
        const titleRight = theme.typography.titlePosition === 'panel' ? plot.x + plot.width : width - spec.padding.right;
        const x = align === 'left' ? titleLeft : align === 'right' ? titleRight : (titleLeft + titleRight) / 2;
        const canvasAlign = align;
        const nodes = [
            {
                type: 'text',
                ...nodeBase('chart:title', { zIndex: 200 }),
                x,
                y: titleY,
                text: spec.title.text,
                fill: theme.colors.text,
                fontFamily: theme.typography.fontFamily,
                fontSize: theme.typography.titleSize,
                fontWeight: theme.typography.titleWeight ?? 700,
                align: canvasAlign,
                baseline: 'top',
                rotation: 0,
            },
        ];
        if (spec.title.subtitle !== undefined) {
            nodes.push({
                type: 'text',
                ...nodeBase('chart:subtitle', { zIndex: 200 }),
                x,
                y: subtitleY,
                text: spec.title.subtitle,
                fill: theme.colors.subtitle ?? theme.colors.mutedText,
                fontFamily: theme.typography.fontFamily,
                fontSize: theme.typography.subtitleSize,
                fontWeight: theme.typography.subtitleWeight ?? 400,
                align: canvasAlign,
                baseline: 'top',
                rotation: 0,
            });
        }
        return nodes;
    }
    function accessibilityLabel(spec, rowCount) {
        if (spec.accessibility.label !== undefined)
            return spec.accessibility.label;
        const title = spec.title?.text ?? 'Graflume chart';
        const layerSummary = `${spec.layers.length} ${spec.layers.length === 1 ? 'layer' : 'layers'}`;
        const rowSummary = `${rowCount.toLocaleString()} ${rowCount === 1 ? 'row' : 'rows'}`;
        return `${title}. ${layerSummary}, ${rowSummary}.`;
    }
    function compileUnitWithRegistry(input, registry, options = {}, runtime = {}) {
        const theme = registry.themes.resolve(input.theme ?? defaultThemeId);
        const spec = normalizeSpec$1(input, theme);
        const width = Math.max(1, spec.width === 'container' ? (options.width ?? 640) : spec.width);
        const height = Math.max(1, spec.height === 'container' ? (options.height ?? 400) : spec.height);
        const legendModel = resolveLegendModel(spec, theme, width, height);
        const legendInsets = legendExternalInsets(legendModel);
        let layout = createLayout(spec, width, height, theme, {}, legendInsets);
        let scales = resolveScales(spec, layout.plot, runtime.domainView?.axes);
        let axes = activeAxes(scales);
        const minimumInsets = { top: 0, right: 0, bottom: 0, left: 0 };
        for (const axis of axes) {
            if (axis.axis === false || axis.axis.visible === false)
                continue;
            const required = measureAxisGutter(axisContext(axis, layout.plot, theme, spec.locale));
            minimumInsets[axis.axis.position] = Math.max(minimumInsets[axis.axis.position], required);
        }
        const measuredLayout = createLayout(spec, width, height, theme, minimumInsets, legendInsets);
        if (!samePlot(layout.plot, measuredLayout.plot)) {
            layout = measuredLayout;
            scales = resolveScales(spec, layout.plot, runtime.domainView?.axes);
            axes = activeAxes(scales);
        }
        if (spec.interaction.selection !== false &&
            spec.interaction.selection.filter &&
            runtime.analyticSelection !== undefined) {
            scales = filterScaleResolutionByAnalyticSelection(scales, runtime.analyticSelection).scales;
            axes = activeAxes(scales);
        }
        const totalRows = scales.layers.reduce((sum, layer) => sum + layer.table.length, 0);
        const dataLineage = Object.fromEntries(scales.layers.map(({ layer, lineage }) => [layer.id, lineage]));
        const performance = resolvePerformanceSettings(spec.performance, totalRows, layout.plot.width);
        const axisNodes = axes.flatMap((axis) => compileAxis(axisContext(axis, layout.plot, theme, spec.locale)));
        const groupedBarLayers = new Map();
        for (const layerData of scales.layers) {
            if (layerData.layer.mark.type !== 'bar' || layerData.layer.mark.position !== 'group')
                continue;
            const key = `${layerData.layer.mark.orientation}:${layerData.xAxisId}:${layerData.yAxisId}`;
            groupedBarLayers.set(key, scales.layers.filter((candidate) => candidate.layer.mark.type === 'bar' &&
                candidate.layer.mark.position === 'group' &&
                candidate.layer.mark.orientation === layerData.layer.mark.orientation &&
                candidate.xAxisId === layerData.xAxisId &&
                candidate.yAxisId === layerData.yAxisId));
        }
        const hiddenLegendItems = runtime.hiddenLegendItemIds ?? new Set();
        const hiddenLayerIds = new Set((legendModel?.items ?? [])
            .filter((item) => hiddenLegendItems.has(item.id) &&
            legendModel !== null &&
            legendItemToggleable(legendModel, item) &&
            item.layerId !== undefined &&
            (legendModel.mode === 'layers' || item.value === undefined))
            .map((item) => item.layerId));
        const hiddenCategories = legendModel?.mode === 'categories' && legendModel.field !== undefined
            ? legendModel.items.filter((item) => hiddenLegendItems.has(item.id) &&
                legendItemToggleable(legendModel, item) &&
                item.value !== undefined)
            : [];
        const datumVisible = (layerId, _rowIndex, datum) => {
            if (hiddenLayerIds.has(layerId))
                return false;
            if (legendModel?.field === undefined)
                return true;
            return !hiddenCategories.some((item) => (item.layerId === undefined || item.layerId === layerId) &&
                datum[legendModel.field] === item.value);
        };
        const inheritSiblingDatum = (nodes) => {
            const nested = nodes.map((node) => node.type === 'group' ? { ...node, children: inheritSiblingDatum(node.children) } : node);
            const owners = new Map();
            for (const node of nested) {
                if (node.datum === undefined)
                    continue;
                const suffix = node.id.slice(node.id.lastIndexOf(':') + 1);
                const prior = owners.get(suffix);
                owners.set(suffix, prior === undefined ? node.datum : null);
            }
            return nested.map((node) => {
                if (node.datum !== undefined)
                    return node;
                const suffix = node.id.slice(node.id.lastIndexOf(':') + 1);
                const owner = owners.get(suffix);
                return owner === undefined || owner === null ? node : { ...node, datum: owner };
            });
        };
        const layerGroups = scales.layers.map((layerData, layerIndex) => {
            const color = categoricalColor(theme, layerIndex, scales.layers.length);
            const barGroupKey = `${layerData.layer.mark.orientation}:${layerData.xAxisId}:${layerData.yAxisId}`;
            const barLayers = groupedBarLayers.get(barGroupKey) ?? [];
            const barGroupIndex = barLayers.findIndex(({ layer }) => layer.id === layerData.layer.id);
            const compiler = registry.mark(layerData.layer.mark.type);
            const compiledChildren = compiler({
                ...layerData,
                xScale: layerData.xScale,
                yScale: layerData.yScale,
                plot: layout.plot,
                theme,
                ...(spec.locale === undefined ? {} : { locale: spec.locale }),
                tableFormatters: registry.tableFormatters,
                color,
                performance,
                barGroup: {
                    count: barGroupIndex < 0 ? 1 : barLayers.length,
                    index: Math.max(0, barGroupIndex),
                },
            });
            let children = legendModel?.mode === 'categories' &&
                legendModel.categoryToggleableLayerIds.has(layerData.layer.id)
                ? inheritSiblingDatum(compiledChildren)
                : compiledChildren;
            if (legendModel?.mode === 'categories' && legendModel.field !== undefined) {
                if (hiddenCategories.length > 0) {
                    const hide = (node) => {
                        if (node.type === 'group')
                            return { ...node, children: node.children.map(hide) };
                        const sources = [];
                        if (node.datum?.tooltip !== undefined)
                            sources.push(node.datum.tooltip);
                        if (node.datum?.datum !== undefined)
                            sources.push(node.datum.datum);
                        const hidden = sources.length > 0 &&
                            sources.some((datum) => !datumVisible(layerData.layer.id, node.datum?.rowIndex ?? -1, datum));
                        return hidden ? { ...node, visible: false } : node;
                    };
                    children = children.map(hide);
                }
            }
            const clip = resolveLayerClip(layerData, scales, layout.plot);
            return group(`${layerData.layer.id}:group`, children, {
                zIndex: layerData.layer.zIndex,
                ...(clip === undefined ? {} : { clip }),
                visible: !hiddenLayerIds.has(layerData.layer.id),
            });
        });
        const legend = compileLegend(legendModel, layerGroups, layout.plot, width, height, theme, hiddenLegendItems);
        const decorations = compileDecorations({
            spec,
            layerGroups,
            scales,
            plot: layout.plot,
            theme,
            width,
            ...(legend.layout === null ? {} : { legendBounds: legend.layout.bounds }),
            runtime,
            datumVisible,
        });
        const markLabels = compileMarkLabels({
            spec,
            layerGroups,
            scales,
            plot: layout.plot,
            theme,
            runtime,
        });
        const coordinates = Object.freeze({
            plot: Object.freeze({ ...layout.plot }),
            axes: Object.freeze(Object.fromEntries(Object.values(scales.axes).flatMap((resolved) => resolved === undefined ? [] : [[resolved.id, resolved.scale]]))),
            channels: Object.freeze(Object.fromEntries(Object.values(scales.axes).flatMap((resolved) => resolved === undefined ? [] : [[resolved.id, resolved.channel]]))),
        });
        const analyticSelectionNodes = runtime.analyticSelection === undefined || spec.interaction.selection === false
            ? []
            : compileAnalyticSelectionOverlay(runtime.analyticSelection, coordinates, spec.interaction.selection.highlight);
        const analyticSelectionDraftNodes = runtime.analyticSelectionDraft === undefined || spec.interaction.selection === false
            ? []
            : compileAnalyticSelectionOverlay({
                selections: [runtime.analyticSelectionDraft],
            }, coordinates, {
                ...spec.interaction.selection.highlight,
                opacity: Math.min(1, spec.interaction.selection.highlight.opacity * 0.72),
                dash: spec.interaction.selection.highlight.dash.length === 0
                    ? [5, 4]
                    : spec.interaction.selection.highlight.dash,
            }, 'analytic-selection:draft');
        const panelNode = theme.colors.panel === undefined
            ? []
            : [
                {
                    type: 'rect',
                    ...nodeBase('chart:panel', { zIndex: -1e3 }),
                    x: layout.plot.x,
                    y: layout.plot.y,
                    width: layout.plot.width,
                    height: layout.plot.height,
                    fill: theme.colors.panel,
                    lineWidth: 0,
                    cornerRadius: 0,
                },
            ];
        const boxExcludedMarks = new Set(theme.axis.boxExcludedMarks ?? []);
        const visibleMarkTypes = spec.layers
            .filter(({ visible }) => visible)
            .map(({ mark }) => mark.type);
        const plotBoxVisible = theme.axis.boxVisible === true &&
            (visibleMarkTypes.length === 0 || visibleMarkTypes.some((type) => !boxExcludedMarks.has(type)));
        const plotBoxNode = plotBoxVisible
            ? [
                {
                    type: 'rect',
                    ...nodeBase('chart:plot-box', { zIndex: 100 }),
                    x: layout.plot.x,
                    y: layout.plot.y,
                    width: layout.plot.width,
                    height: layout.plot.height,
                    stroke: theme.colors.axis,
                    lineWidth: theme.axis.boxLineWidth ?? theme.axis.lineWidth,
                    cornerRadius: 0,
                },
            ]
            : [];
        const technicalIndicatorLayers = scales.layers.filter(({ technicalIndicator }) => technicalIndicator !== undefined);
        const technicalPanelId = technicalIndicatorLayers.find(({ technicalIndicator }) => technicalIndicator?.presentation.placement === 'panel')?.technicalIndicator?.presentation.panelId ?? 'price';
        const technicalCrosshairX = runtime.technicalCrosshairValue === undefined
            ? undefined
            : coordinates.axes[technicalIndicatorLayers[0]?.xAxisId ?? 'x']?.map(runtime.technicalCrosshairValue);
        const technicalCrosshairNodes = technicalIndicatorLayers.length === 0 ||
            technicalCrosshairX === undefined ||
            !Number.isFinite(technicalCrosshairX) ||
            technicalCrosshairX < layout.plot.x ||
            technicalCrosshairX > layout.plot.x + layout.plot.width
            ? []
            : [
                {
                    type: 'line',
                    ...nodeBase(`technical-crosshair:${technicalPanelId}`, {
                        zIndex: 850,
                        opacity: 0.72,
                    }),
                    x1: technicalCrosshairX,
                    y1: layout.plot.y,
                    x2: technicalCrosshairX,
                    y2: layout.plot.y + layout.plot.height,
                    stroke: theme.colors.text,
                    lineWidth: 1,
                    dash: [4, 3],
                },
            ];
        const children = [
            ...panelNode,
            ...decorations.underlay,
            ...axisNodes,
            ...layerGroups,
            ...plotBoxNode,
            ...markLabels.nodes,
            ...decorations.overlay,
            ...analyticSelectionNodes,
            ...analyticSelectionDraftNodes,
            ...technicalCrosshairNodes,
            ...legend.nodes,
            ...titleNodes(spec, theme, width, layout.plot, layout.titleY, layout.subtitleY),
        ];
        const root = group('scene:root', children);
        const semanticIndex = buildSemanticIndex(root, scales.layers, spec.accessibility.maxRows, spec.locale);
        const scene = {
            width,
            height,
            background: theme.colors.background,
            root,
            accessibility: {
                label: accessibilityLabel(spec, totalRows),
                ...(() => {
                    const base = spec.accessibility.description ?? spec.description;
                    const annotationText = (runtime.annotations ?? spec.annotations)
                        .map((annotation) => annotation.detail === undefined
                        ? annotation.text
                        : `${annotation.text}: ${annotation.detail}`)
                        .join('. ');
                    const markLabelText = markLabels.entries.length === 0
                        ? ''
                        : `Mark labels: ${markLabels.entries
                        .slice(0, 20)
                        .map((entry) => entry.text)
                        .join(', ')}${markLabels.entries.length > 20 ? ', and more' : ''}`;
                    const authoringText = spec.markLabels !== false && spec.markLabels.authoring !== false
                        ? 'Label authoring is enabled. Press Enter to select a label, use arrow keys to move it, Escape to clear the handle, and Control or Command Z to undo.'
                        : '';
                    const selectionAuthoringText = spec.interaction.selection !== false &&
                        spec.interaction.selection.kind !== 'point' &&
                        spec.interaction.selection.keyboard
                        ? `Keyboard ${spec.interaction.selection.kind} selection is enabled. Press S to start, use arrow keys to shape the selection${spec.interaction.selection.kind === 'lasso'
                        ? ', Space to add each lasso vertex'
                        : ''}, Enter to apply, and Escape to cancel.`
                        : '';
                    const description = [
                        base,
                        spec.accessibility.summary,
                        annotationText,
                        markLabelText,
                        authoringText,
                        selectionAuthoringText,
                    ]
                        .filter(Boolean)
                        .join('. ');
                    const legendText = legendModel === null
                        ? ''
                        : `${legendModel.spec.title ?? 'Legend'}: ${legendModel.items
                        .slice(0, 12)
                        .map((item) => item.label)
                        .join(', ')}`;
                    const provenanceText = scales.layers
                        .filter(({ lineage }) => lineage.transforms.length > 0)
                        .map(({ lineage }) => lineage.summary)
                        .join(' ');
                    const accessibleDescription = [description, legendText, provenanceText]
                        .filter(Boolean)
                        .join('. ');
                    return accessibleDescription === '' ? {} : { description: accessibleDescription };
                })(),
            },
            semanticIndex,
            metadata: {
                rowCount: totalRows,
                renderedNodeCount: countSceneNodes(root),
                performanceProfile: performance.profile,
                hitTestingEnabled: performance.enableHitTesting,
                dataLineage: scales.layers.map(({ lineage }) => lineage.summary),
                ...(decorations.annotations.length === 0
                    ? {}
                    : {
                        annotations: {
                            entries: decorations.annotations,
                            ...(runtime.activeAnnotationId === undefined
                                ? {}
                                : { activeId: runtime.activeAnnotationId }),
                        },
                    }),
                ...(scales.layers.some(({ technicalIndicator }) => technicalIndicator !== undefined)
                    ? {
                        technicalIndicators: scales.layers.flatMap(({ layer, technicalIndicator }) => technicalIndicator === undefined
                            ? []
                            : [
                                {
                                    layerId: layer.id,
                                    id: technicalIndicator.capability.id,
                                    kind: technicalIndicator.capability.kind,
                                    requiredInputs: [...technicalIndicator.capability.requiredInputs],
                                    outputFields: technicalIndicator.capability.outputs.map((role) => role === 'value' ? layer.y.field : (layer.mark.fields[role] ?? role)),
                                    warmUpRows: technicalIndicator.warmUpRows,
                                    parameters: { ...technicalIndicator.parameters },
                                    provenance: technicalIndicator.provenance,
                                    session: {
                                        mode: technicalIndicator.session.mode,
                                        reset: technicalIndicator.session.reset,
                                        boundaries: [...technicalIndicator.session.boundaries],
                                    },
                                    presentation: {
                                        placement: technicalIndicator.presentation.placement,
                                        panelId: technicalIndicator.presentation.panelId,
                                        synchronizedCrosshair: {
                                            axis: technicalIndicator.presentation.synchronizedCrosshair.axis,
                                            sharedDomain: technicalIndicator.presentation.synchronizedCrosshair.sharedDomain,
                                            fields: technicalIndicator.presentation.synchronizedCrosshair.fields.map((role) => role === 'value' ? layer.y.field : (layer.mark.fields[role] ?? role)),
                                        },
                                    },
                                },
                            ]),
                        technicalIndicatorPanels: [
                            {
                                id: technicalPanelId,
                                bounds: { ...layout.plot },
                                layerIds: scales.layers.map(({ layer }) => layer.id),
                                placement: technicalPanelId === 'price' ? 'price' : 'indicator',
                            },
                        ],
                        ...(technicalCrosshairX === undefined || !Number.isFinite(technicalCrosshairX)
                            ? {}
                            : {
                                technicalIndicatorCrosshair: {
                                    value: runtime.technicalCrosshairValue,
                                    panelIds: [technicalPanelId],
                                    positions: [{ panelId: technicalPanelId, x: technicalCrosshairX }],
                                },
                            }),
                    }
                    : {}),
                ...(() => {
                    const analyticalFamilies = scales.layers.flatMap(({ layer }) => {
                        const metadata = resolveAnalyticalFamilySceneMetadata(layer);
                        return metadata === null ? [] : [metadata];
                    });
                    return analyticalFamilies.length === 0 ? {} : { analyticalFamilies };
                })(),
                ...(spec.markLabels === false
                    ? {}
                    : {
                        markLabels: {
                            entries: markLabels.entries,
                            plot: { ...layout.plot },
                            ...(runtime.activeMarkLabelId === undefined
                                ? {}
                                : { activeId: runtime.activeMarkLabelId }),
                        },
                    }),
            },
        };
        registerLegendLayout(scene, legend.layout);
        const tooltip = spec.interaction.tooltip;
        if (tooltip !== false && tooltip.trigger === 'axis' && tooltip.axis !== undefined) {
            const axis = tooltip.axis;
            const activeAxis = axes.find((candidate) => candidate.id === axis);
            const axisVisible = activeAxis !== undefined && activeAxis.axis !== false && activeAxis.axis.visible;
            const context = activeAxis === undefined
                ? undefined
                : axisContext(activeAxis, layout.plot, theme, spec.locale);
            let axisStripSize = context === undefined ? 0 : measureAxisLabelGutter(context);
            if (context !== undefined &&
                context.axis !== false &&
                (context.axis.labels.angle ?? 0) === 0 &&
                (context.axis.labels.orientation === 'auto' ||
                    context.axis.labels.orientation === 'horizontal')) {
                const horizontal = activeAxis?.channel === 'x';
                const tickSize = context.axis.ticks.visible
                    ? (context.axis.ticks.size ?? theme.axis.tickLength)
                    : 0;
                const labelPadding = context.axis.labels.padding ?? theme.axis.labelPadding;
                const fontSize = context.axis.labels.font.size ??
                    theme.typography.axisLabelSize ??
                    theme.typography.fontSize;
                const readableStrip = context.axis.offset + tickSize + labelPadding + fontSize * (horizontal ? 1.5 : 4);
                axisStripSize = Math.min(axisStripSize, readableStrip);
            }
            registerAxisTooltipIndex(scene, {
                axis,
                channel: activeAxis?.channel ?? 'x',
                ...(activeAxis === undefined || activeAxis.axis === false
                    ? {}
                    : { position: activeAxis.axis.position }),
                plot: layout.plot,
                axisVisible,
                axisStripSize: Math.max(0, axisStripSize),
                targets: collectAxisTooltipTargets({
                    axis,
                    channel: activeAxis?.channel ?? 'x',
                    layerGroups,
                    scales,
                    plot: layout.plot,
                    performance,
                    datumVisible,
                }),
            });
        }
        return {
            scene,
            spec,
            theme,
            dataLineage,
            coordinates,
            coordinateViews: [
                {
                    id: 'plot',
                    label: '',
                    bounds: { ...coordinates.plot },
                    offsetX: 0,
                    offsetY: 0,
                    coordinates,
                },
            ],
        };
    }
    function compileWithRegistry(input, registry, options = {}, runtime = {}) {
        input = materializeSpecDataflow(input);
        if (!isCompositionSpec(input))
            input = materializeTechnicalIndicatorPanes(input);
        if (!isCompositionSpec(input))
            return compileUnitWithRegistry(input, registry, options, runtime);
        return compileCompositionWithRegistry(input, registry, options, runtime, {
            compile: compileWithRegistry,
            compileUnit: compileUnitWithRegistry,
        });
    }

    function finite$9(value, path) {
        if (typeof value !== 'number' || !Number.isFinite(value)) {
            throw new GraflumeError('INVALID_DATA', `${path} must be a finite number.`, { path });
        }
        return value;
    }
    function positive(value, path) {
        const result = finite$9(value, path);
        if (result <= 0)
            throw new GraflumeError('INVALID_DATA', `${path} must be greater than zero.`, { path });
        return result;
    }
    function clamp$7(value, low, high) {
        return Math.max(low, Math.min(high, value));
    }
    function normalizeBars(input) {
        return input
            .map((bar, sourceIndex) => {
            const open = finite$9(bar.open, `$.data[${sourceIndex}].open`);
            const high = finite$9(bar.high, `$.data[${sourceIndex}].high`);
            const low = finite$9(bar.low, `$.data[${sourceIndex}].low`);
            const close = finite$9(bar.close, `$.data[${sourceIndex}].close`);
            if (low > Math.min(open, close) || high < Math.max(open, close) || high < low) {
                throw new GraflumeError('INVALID_DATA', `$.data[${sourceIndex}] has inconsistent OHLC bounds.`);
            }
            return {
                id: bar.id?.trim() || `bar-${sourceIndex}`,
                time: finite$9(bar.time, `$.data[${sourceIndex}].time`),
                open,
                high,
                low,
                close,
                volume: Math.max(0, finite$9(bar.volume ?? 0, `$.data[${sourceIndex}].volume`)),
                session: bar.session?.trim() || 'default',
                sourceIndex,
            };
        })
            .sort((left, right) => left.time - right.time || left.sourceIndex - right.sourceIndex);
    }
    function provenance(bars) {
        const first = bars[0];
        const last = bars.at(-1);
        return {
            sourceIndexes: bars.map(({ sourceIndex }) => sourceIndex),
            sourceIds: bars.map(({ id }) => id),
            timeStart: first.time,
            timeEnd: last.time,
            sourceOpen: first.open,
            sourceHigh: Math.max(...bars.map(({ high }) => high)),
            sourceLow: Math.min(...bars.map(({ low }) => low)),
            sourceClose: last.close,
            sourceVolume: bars.reduce((sum, { volume }) => sum + volume, 0),
        };
    }
    function trueRanges(bars) {
        return bars.map((bar, index) => {
            const previous = bars[index - 1]?.close ?? bar.open;
            return Math.max(bar.high - bar.low, Math.abs(bar.high - previous), Math.abs(bar.low - previous));
        });
    }
    function atrSeries(bars, period) {
        const ranges = trueRanges(bars);
        let average = ranges.slice(0, period).reduce((sum, value) => sum + value, 0) /
            Math.min(period, ranges.length);
        return ranges.map((range, index) => {
            if (index === 0)
                average = range;
            else if (index < period)
                average = (average * index + range) / (index + 1);
            else
                average = (average * (period - 1) + range) / period;
            return average;
        });
    }
    function sizingResolver(bars, sizing) {
        if (sizing.mode === 'fixed') {
            const size = positive(sizing.value, '$.sizing.value');
            return () => size;
        }
        if (sizing.mode === 'percent') {
            const ratio = positive(sizing.value, '$.sizing.value') / 100;
            return (_index, anchor) => Math.max(Number.EPSILON, Math.abs(anchor) * ratio);
        }
        if (sizing.mode === 'log') {
            const logStep = Math.log1p(positive(sizing.value, '$.sizing.value') / 100);
            return (_index, anchor, direction = 1) => Math.max(Number.EPSILON, anchor * (direction > 0 ? Math.expm1(logStep) : 1 - Math.exp(-logStep)));
        }
        const period = clamp$7(Math.floor(sizing.period ?? 14), 1, 10_000);
        const multiplier = positive(sizing.multiplier ?? 1, '$.sizing.multiplier');
        const atr = atrSeries(bars, period);
        return (index) => Math.max(Number.EPSILON, atr[index] * multiplier);
    }
    function defaultSizing(bars) {
        const minimum = Math.min(...bars.map(({ low }) => low));
        const maximum = Math.max(...bars.map(({ high }) => high));
        return { mode: 'fixed', value: Math.max(Number.EPSILON, (maximum - minimum) / 12 || 1) };
    }
    function sourceSlice(bars, start, end) {
        return bars.slice(Math.min(start, end), Math.max(start, end) + 1);
    }
    function sourcePosition(bars, sourceIndex, fallback = 0) {
        if (sourceIndex === undefined)
            return fallback;
        const position = bars.findIndex((bar) => bar.sourceIndex === sourceIndex);
        return position < 0 ? fallback : position;
    }
    function buildRenko(bars, options) {
        const sizeAt = sizingResolver(bars, options.sizing ?? defaultSizing(bars));
        const output = [];
        let anchor = bars[0].close;
        let sourceStart = 0;
        for (let index = 1; index < bars.length; index += 1) {
            if (options.resetBySession === true && bars[index].session !== bars[index - 1].session) {
                anchor = bars[index].open;
                sourceStart = index;
            }
            const close = bars[index].close;
            for (let guard = 0; guard < 100_000; guard += 1) {
                const direction = close > anchor ? 1 : -1;
                const size = sizeAt(index, anchor, direction);
                if (Math.abs(close - anchor) + Number.EPSILON < size)
                    break;
                const next = anchor + direction * size;
                const sources = sourceSlice(bars, sourceStart, index);
                output.push({
                    id: `renko-${output.length}`,
                    mode: 'renko',
                    open: anchor,
                    high: Math.max(anchor, next),
                    low: Math.min(anchor, next),
                    close: next,
                    direction: direction > 0 ? 'up' : 'down',
                    size,
                    session: bars[index].session,
                    provenance: provenance(sources),
                });
                anchor = next;
                sourceStart = index;
            }
        }
        return output;
    }
    function buildKagi(bars, options) {
        const sizeAt = sizingResolver(bars, options.sizing ?? defaultSizing(bars));
        const reversal = positive(options.reversal ?? 1, '$.reversal');
        const output = [];
        let pivot = bars[0].close;
        let extreme = pivot;
        let direction = 0;
        let thick = false;
        let shoulder = pivot;
        let waist = pivot;
        let sourceStart = 0;
        const append = (index, close, nextDirection, reversed) => {
            if (nextDirection > 0 && close > shoulder)
                thick = true;
            if (nextDirection < 0 && close < waist)
                thick = false;
            const sources = sourceSlice(bars, sourceStart, index);
            output.push({
                id: `kagi-${output.length}`,
                mode: 'kagi',
                open: pivot,
                high: Math.max(pivot, close),
                low: Math.min(pivot, close),
                close,
                direction: nextDirection > 0 ? 'up' : 'down',
                size: Math.abs(close - pivot),
                session: bars[index].session,
                thick,
                reversal: reversed,
                provenance: provenance(sources),
            });
            pivot = close;
            sourceStart = index;
        };
        for (let index = 1; index < bars.length; index += 1) {
            if (options.resetBySession === true && bars[index].session !== bars[index - 1].session) {
                pivot = extreme = bars[index].open;
                direction = 0;
                sourceStart = index;
            }
            const close = bars[index].close;
            const thresholdDirection = direction === 0 ? (close >= pivot ? 1 : -1) : direction > 0 ? -1 : 1;
            const amount = sizeAt(index, extreme, thresholdDirection) * reversal;
            if (direction === 0) {
                if (Math.abs(close - pivot) < amount)
                    continue;
                direction = close > pivot ? 1 : -1;
                extreme = close;
                append(index, extreme, direction, false);
                continue;
            }
            if ((direction > 0 && close >= extreme) || (direction < 0 && close <= extreme)) {
                extreme = close;
                const previous = output.pop();
                if (previous !== undefined) {
                    pivot = previous.open;
                    sourceStart = sourcePosition(bars, previous.provenance.sourceIndexes[0]);
                }
                append(index, extreme, direction, false);
                continue;
            }
            if (Math.abs(close - extreme) < amount)
                continue;
            if (direction > 0)
                shoulder = extreme;
            else
                waist = extreme;
            pivot = extreme;
            direction = direction > 0 ? -1 : 1;
            extreme = close;
            append(index, extreme, direction, true);
        }
        return output;
    }
    function buildLineBreak(bars, options) {
        const lineCount = clamp$7(Math.floor(options.lineBreaks ?? 3), 1, 100);
        const output = [];
        for (let index = 1; index < bars.length; index += 1) {
            const close = bars[index].close;
            const recent = output.slice(-lineCount);
            const previousClose = output.at(-1)?.close ?? bars[index - 1].close;
            const upper = recent.length === 0
                ? previousClose
                : Math.max(...recent.flatMap(({ open, close: end }) => [open, end]));
            const lower = recent.length === 0
                ? previousClose
                : Math.min(...recent.flatMap(({ open, close: end }) => [open, end]));
            if (close <= upper && close >= lower)
                continue;
            const direction = close > upper ? 'up' : 'down';
            const open = previousClose;
            output.push({
                id: `three-line-break-${output.length}`,
                mode: 'three-line-break',
                open,
                high: Math.max(open, close),
                low: Math.min(open, close),
                close,
                direction,
                size: Math.abs(close - open),
                session: bars[index].session,
                reversal: output.length > 0 && direction !== output.at(-1).direction,
                provenance: provenance(sourceSlice(bars, sourcePosition(bars, output.at(-1)?.provenance.sourceIndexes.at(-1)), index)),
            });
        }
        return output;
    }
    function intrabarPrices(bar, path) {
        if (path === 'close-only')
            return [bar.close];
        const resolved = path === 'auto' ? (bar.close >= bar.open ? 'low-high' : 'high-low') : path;
        return resolved === 'high-low'
            ? [bar.open, bar.high, bar.low, bar.close]
            : [bar.open, bar.low, bar.high, bar.close];
    }
    function buildRangeBars(bars, options) {
        const sizeAt = sizingResolver(bars, options.sizing ?? defaultSizing(bars));
        const output = [];
        let open = bars[0].open;
        let high = open;
        let low = open;
        let sourceStart = 0;
        for (let index = 0; index < bars.length; index += 1) {
            for (const price of intrabarPrices(bars[index], options.intrabarPath)) {
                high = Math.max(high, price);
                low = Math.min(low, price);
                for (let guard = 0; guard < 100_000; guard += 1) {
                    const upSize = sizeAt(index, open, 1);
                    const downSize = sizeAt(index, open, -1);
                    const up = high - open >= upSize;
                    const down = open - low >= downSize;
                    if (!up && !down)
                        break;
                    const direction = up && down ? (price >= open ? 1 : -1) : up ? 1 : -1;
                    const size = direction > 0 ? upSize : downSize;
                    const close = open + direction * size;
                    output.push({
                        id: `range-bar-${output.length}`,
                        mode: 'range-bars',
                        open,
                        high: Math.max(open, close),
                        low: Math.min(open, close),
                        close,
                        direction: direction > 0 ? 'up' : 'down',
                        size,
                        session: bars[index].session,
                        provenance: provenance(sourceSlice(bars, sourceStart, index)),
                    });
                    open = close;
                    sourceStart = index;
                    high = Math.max(open, price);
                    low = Math.min(open, price);
                }
            }
        }
        return output;
    }
    /** Builds path-dependent financial blocks with complete OHLCV source provenance. */
    function buildPriceBlocks(input, options = {}) {
        const bars = normalizeBars(input);
        if (bars.length < 2)
            return [];
        if (options.sizing?.mode === 'log' &&
            bars.some(({ open, high, low, close }) => open <= 0 || high <= 0 || low <= 0 || close <= 0))
            throw new GraflumeError('INVALID_DATA', 'Log price blocks require positive OHLC values.');
        const mode = options.mode ?? 'renko';
        if (mode === 'kagi')
            return buildKagi(bars, options);
        if (mode === 'three-line-break')
            return buildLineBreak(bars, options);
        if (mode === 'range-bars')
            return buildRangeBars(bars, options);
        return buildRenko(bars, options);
    }
    function periodKey(time, period, offsetMinutes) {
        const shifted = new Date(time + offsetMinutes * 60_000);
        const year = shifted.getUTCFullYear();
        const month = shifted.getUTCMonth();
        if (period === 'month')
            return `${year}-${String(month + 1).padStart(2, '0')}`;
        const day = Math.floor(Date.UTC(year, month, shifted.getUTCDate()) / 86_400_000);
        if (period === 'week')
            return `week-${Math.floor((day + 3) / 7)}`;
        return `day-${day}`;
    }
    function groupProfileBars(bars, scope) {
        if (scope.mode === 'fixed')
            return [{ id: 'fixed', bars: [...bars] }];
        if (scope.mode === 'visible') {
            const [start, end] = scope.time;
            const low = finite$9(Math.min(start, end), '$.scope.time[0]');
            const high = finite$9(Math.max(start, end), '$.scope.time[1]');
            return [{ id: 'visible', bars: bars.filter(({ time }) => time >= low && time <= high) }];
        }
        const groups = new Map();
        bars.forEach((bar) => {
            const key = scope.mode === 'session'
                ? bar.session
                : periodKey(bar.time, scope.period, scope.timezoneOffsetMinutes ?? 0);
            const group = groups.get(key) ?? [];
            group.push(bar);
            groups.set(key, group);
        });
        return [...groups].map(([id, grouped]) => ({ id, bars: grouped }));
    }
    function resolveRowSize(bars, rows) {
        const low = Math.min(...bars.map(({ low: value }) => value));
        const high = Math.max(...bars.map(({ high: value }) => value));
        if (rows.mode === 'size')
            return positive(rows.size, '$.rows.size');
        if (rows.mode === 'tick') {
            return (positive(rows.tick, '$.rows.tick') * clamp$7(Math.floor(rows.ticksPerRow ?? 1), 1, 100_000));
        }
        const count = clamp$7(Math.floor(rows.count), 1, 10_000);
        return Math.max(Number.EPSILON, (high - low) / count || 1);
    }
    function valueArea(rows, target) {
        let poc = 0;
        for (let index = 1; index < rows.length; index += 1)
            if (rows[index].volume > rows[poc].volume)
                poc = index;
        const total = rows.reduce((sum, row) => sum + row.volume, 0);
        const required = total * clamp$7(target, 0, 1);
        let cumulative = rows[poc]?.volume ?? 0;
        let low = poc;
        let high = poc;
        while (cumulative < required && (low > 0 || high < rows.length - 1)) {
            const below = low > 0 ? rows[low - 1].volume : -1;
            const above = high < rows.length - 1 ? rows[high + 1].volume : -1;
            if (above > below) {
                high += 1;
                cumulative += rows[high].volume;
            }
            else {
                low -= 1;
                cumulative += rows[low].volume;
            }
        }
        return { low, high, poc };
    }
    function oneVolumeProfile(id, bars, options) {
        if (bars.length === 0)
            return null;
        const rowSize = resolveRowSize(bars, options.rows ?? { mode: 'count', count: 24 });
        const minimum = Math.floor(Math.min(...bars.map(({ low }) => low)) / rowSize) * rowSize;
        const maximum = Math.max(...bars.map(({ high }) => high));
        const count = clamp$7(Math.ceil((maximum - minimum) / rowSize) || 1, 1, 10_000);
        const volumes = Array.from({ length: count }, () => 0);
        const sources = Array.from({ length: count }, () => new Set());
        const allocation = options.allocation ?? 'uniform-range';
        bars.forEach((bar) => {
            const add = (index, volume) => {
                const bounded = clamp$7(index, 0, count - 1);
                volumes[bounded] = (volumes[bounded] ?? 0) + volume;
                sources[bounded].add(bar.sourceIndex);
            };
            if (allocation !== 'uniform-range') {
                const price = allocation === 'close' ? bar.close : (bar.high + bar.low + bar.close) / 3;
                add(Math.floor((price - minimum) / rowSize), bar.volume);
                return;
            }
            const first = clamp$7(Math.floor((bar.low - minimum) / rowSize), 0, count - 1);
            const last = clamp$7(Math.floor((bar.high - minimum) / rowSize), 0, count - 1);
            const touched = last - first + 1;
            for (let index = first; index <= last; index += 1)
                add(index, bar.volume / touched);
        });
        const totalVolume = volumes.reduce((sum, volume) => sum + volume, 0);
        const area = valueArea(volumes.map((volume) => ({ volume })), options.valueArea ?? 0.7);
        const rows = volumes.map((volume, index) => ({
            index,
            priceLow: minimum + index * rowSize,
            priceHigh: minimum + (index + 1) * rowSize,
            volume,
            proportion: totalVolume === 0 ? 0 : volume / totalVolume,
            sourceIndexes: [...sources[index]].sort((left, right) => left - right),
            pointOfControl: index === area.poc,
            inValueArea: index >= area.low && index <= area.high,
        }));
        return {
            id,
            scope: options.scope?.mode ?? 'fixed',
            timeStart: bars[0].time,
            timeEnd: bars.at(-1).time,
            priceLow: minimum,
            priceHigh: minimum + count * rowSize,
            rowSize,
            rows,
            totalVolume,
            poc: rows[area.poc].priceLow + rowSize / 2,
            vah: rows[area.high].priceHigh,
            val: rows[area.low].priceLow,
            placement: options.placement ?? 'right',
            sourceIndexes: bars.map(({ sourceIndex }) => sourceIndex),
        };
    }
    /** Aggregates fixed, visible, session, or periodic volume-at-price profiles with POC/VAH/VAL. */
    function buildVolumeProfiles(input, options = {}) {
        const bars = normalizeBars(input);
        if (bars.length === 0)
            return [];
        const scope = options.scope ?? { mode: 'fixed' };
        return groupProfileBars(bars, scope).flatMap(({ id, bars: group }) => {
            const profile = oneVolumeProfile(id, group, { ...options, scope });
            return profile === null ? [] : [profile];
        });
    }

    function optionString$2(context, name) {
        const value = context.layer.mark.options[name];
        return typeof value === 'string' ? value : undefined;
    }
    function optionNumber$2(context, name) {
        const value = context.layer.mark.options[name];
        return typeof value === 'number' && Number.isFinite(value) ? value : undefined;
    }
    function optionBoolean$1(context, name) {
        const value = context.layer.mark.options[name];
        return typeof value === 'boolean' ? value : undefined;
    }
    function field$3(context, name, fallback) {
        return context.layer.mark.fields[name] ?? fallback;
    }
    function timeValue(value, fallback) {
        if (value instanceof Date)
            return value.getTime();
        if (typeof value === 'number' && Number.isFinite(value))
            return value;
        if (typeof value === 'string') {
            const parsed = Date.parse(value);
            if (Number.isFinite(parsed))
                return parsed;
        }
        return fallback;
    }
    function financialRows(context) {
        const { table, layer } = context;
        const openField = field$3(context, 'open', 'open');
        const highField = field$3(context, 'high', 'high');
        const lowField = field$3(context, 'low', 'low');
        const closeField = field$3(context, 'close', layer.y.field);
        const volumeField = field$3(context, 'volume', 'volume');
        const sessionField = field$3(context, 'session', 'session');
        const idField = field$3(context, 'id', 'id');
        const output = [];
        let previousClose = null;
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const close = numericDataValue(table.value(rowIndex, closeField));
            if (close === null)
                continue;
            const open = table.has(openField)
                ? (numericDataValue(table.value(rowIndex, openField)) ?? previousClose ?? close)
                : (previousClose ?? close);
            const sourceHigh = table.has(highField)
                ? (numericDataValue(table.value(rowIndex, highField)) ?? Math.max(open, close))
                : Math.max(open, close);
            const sourceLow = table.has(lowField)
                ? (numericDataValue(table.value(rowIndex, lowField)) ?? Math.min(open, close))
                : Math.min(open, close);
            const rawId = table.has(idField) ? table.value(rowIndex, idField) : undefined;
            const rawSession = table.has(sessionField) ? table.value(rowIndex, sessionField) : undefined;
            output.push({
                id: rawId === null || rawId === undefined ? `row-${rowIndex}` : String(rawId),
                time: timeValue(table.value(rowIndex, layer.x.field), rowIndex),
                open,
                high: Math.max(sourceHigh, open, close),
                low: Math.min(sourceLow, open, close),
                close,
                volume: table.has(volumeField)
                    ? (numericDataValue(table.value(rowIndex, volumeField)) ?? 0)
                    : 0,
                session: rawSession === null || rawSession === undefined || String(rawSession).trim() === ''
                    ? 'default'
                    : String(rawSession),
            });
            previousClose = close;
        }
        return output;
    }
    function priceBlockSizing(context) {
        const mode = optionString$2(context, 'sizing') ?? optionString$2(context, 'sizeMode');
        if (mode === 'fixed') {
            const value = optionNumber$2(context, 'brickSize') ?? optionNumber$2(context, 'size');
            return value === undefined ? undefined : { mode, value };
        }
        if (mode === 'percent' || mode === 'log') {
            const value = optionNumber$2(context, 'percent') ?? optionNumber$2(context, 'size');
            return value === undefined ? undefined : { mode, value };
        }
        if (mode === 'atr') {
            const period = optionNumber$2(context, 'atrPeriod');
            const multiplier = optionNumber$2(context, 'atrMultiplier');
            return {
                mode,
                ...(period === undefined ? {} : { period }),
                ...(multiplier === undefined ? {} : { multiplier }),
            };
        }
        const legacy = optionNumber$2(context, 'brickSize');
        return legacy === undefined ? undefined : { mode: 'fixed', value: legacy };
    }
    /** Price-block mark compiler for Renko, Kagi, three-line break, and range bars. */
    const compilePriceBlocksMark = (context) => {
        const modeValue = optionString$2(context, 'mode') ?? optionString$2(context, 'kind') ?? 'renko';
        const mode = modeValue === 'kagi' || modeValue === 'three-line-break' || modeValue === 'range-bars'
            ? modeValue
            : 'renko';
        const sizing = priceBlockSizing(context);
        const reversal = optionNumber$2(context, 'reversal');
        const lineBreaks = optionNumber$2(context, 'lineBreaks');
        const resetBySession = optionBoolean$1(context, 'resetBySession');
        const options = {
            mode,
            ...(sizing === undefined ? {} : { sizing }),
            ...(reversal === undefined ? {} : { reversal }),
            ...(lineBreaks === undefined ? {} : { lineBreaks }),
            ...(optionString$2(context, 'intrabarPath') === undefined
                ? {}
                : {
                    intrabarPath: optionString$2(context, 'intrabarPath'),
                }),
            ...(resetBySession === undefined ? {} : { resetBySession }),
        };
        const blocks = buildPriceBlocks(financialRows(context), options);
        if (blocks.length === 0)
            return [];
        const { layer, plot, theme, performance } = context;
        const minimum = Math.min(...blocks.map(({ low }) => low));
        const maximum = Math.max(...blocks.map(({ high }) => high));
        const span = Math.max(Number.EPSILON, maximum - minimum);
        const width = plot.width / Math.max(1, blocks.length);
        const mapY = (value) => plot.y + plot.height - ((value - minimum) / span) * plot.height;
        const risingColor = theme.colors.palette[1] ?? context.color;
        const fallingColor = theme.colors.palette[3] ?? context.color;
        const nodes = [];
        blocks.forEach((block, index) => {
            const color = block.direction === 'up' ? risingColor : fallingColor;
            const base = nodeBase(`${layer.id}:${block.mode}:${index}`, {
                zIndex: layer.zIndex,
                opacity: layer.mark.opacity,
                interactive: performance.enableHitTesting,
                datum: {
                    layerId: layer.id,
                    rowIndex: block.provenance.sourceIndexes.at(-1) ?? index,
                    datum: {
                        id: block.id,
                        mode: block.mode,
                        open: block.open,
                        high: block.high,
                        low: block.low,
                        close: block.close,
                        direction: block.direction,
                        size: block.size,
                        session: block.session,
                        sourceIndexes: [...block.provenance.sourceIndexes],
                        sourceIds: [...block.provenance.sourceIds],
                    },
                    tooltip: {
                        mode: block.mode,
                        open: block.open,
                        high: block.high,
                        low: block.low,
                        close: block.close,
                        size: block.size,
                        direction: block.direction,
                        session: block.session,
                        sourceOpen: block.provenance.sourceOpen,
                        sourceHigh: block.provenance.sourceHigh,
                        sourceLow: block.provenance.sourceLow,
                        sourceClose: block.provenance.sourceClose,
                        sourceVolume: block.provenance.sourceVolume,
                        timeStart: block.provenance.timeStart,
                        timeEnd: block.provenance.timeEnd,
                        sourceIds: block.provenance.sourceIds.join(', '),
                    },
                },
            });
            const x = plot.x + index * width + width / 2;
            const open = mapY(block.open);
            const close = mapY(block.close);
            if (block.mode === 'kagi') {
                nodes.push({
                    type: 'path',
                    ...base,
                    points: [
                        { x: x - width / 2, y: open },
                        { x, y: open },
                        { x, y: close },
                        { x: x + width / 2, y: close },
                    ],
                    closed: false,
                    stroke: layer.mark.stroke ?? color,
                    lineWidth: block.thick
                        ? Math.max(3, layer.mark.lineWidth ?? 3.6)
                        : (layer.mark.lineWidth ?? 1.5),
                    lineCap: theme.mark.lineCap ?? 'round',
                    lineJoin: theme.mark.lineJoin ?? 'round',
                });
                return;
            }
            nodes.push({
                type: 'rect',
                ...base,
                x: plot.x + index * width + 1,
                y: Math.min(open, close),
                width: Math.max(1.5, width - 2),
                height: Math.max(1.5, Math.abs(open - close)),
                fill: layer.mark.fill ?? colorWithOpacity(color, 0.74),
                stroke: layer.mark.stroke ?? color,
                lineWidth: layer.mark.lineWidth ?? 1.4,
                cornerRadius: layer.mark.cornerRadius ?? 1,
            });
        });
        return nodes;
    };
    function volumeScope(context) {
        const mode = optionString$2(context, 'scope') ?? 'fixed';
        if (mode === 'visible') {
            const domain = context.xScale
                .domain()
                .map((value) => numericDataValue(value, context.layer.x.type === 'temporal'))
                .filter((value) => value !== null);
            const start = optionNumber$2(context, 'visibleStart') ?? Math.min(...domain);
            const end = optionNumber$2(context, 'visibleEnd') ?? Math.max(...domain);
            if (!Number.isFinite(start) || !Number.isFinite(end))
                throw new GraflumeError('INVALID_SPEC', 'Visible volume profile scope requires a finite active x-domain or visibleStart/visibleEnd.');
            return { mode, time: [start, end] };
        }
        if (mode === 'session')
            return { mode };
        if (mode === 'periodic') {
            const raw = optionString$2(context, 'period');
            const timezoneOffsetMinutes = optionNumber$2(context, 'timezoneOffsetMinutes');
            return {
                mode,
                period: raw === 'week' || raw === 'month' ? raw : 'day',
                ...(timezoneOffsetMinutes === undefined ? {} : { timezoneOffsetMinutes }),
            };
        }
        return { mode: 'fixed' };
    }
    function volumeRows(context) {
        const mode = optionString$2(context, 'rowMode') ?? 'count';
        if (mode === 'size')
            return { mode, size: optionNumber$2(context, 'rowSize') ?? 1 };
        if (mode === 'tick') {
            return {
                mode,
                tick: optionNumber$2(context, 'tickSize') ?? 1,
                ticksPerRow: optionNumber$2(context, 'ticksPerRow') ?? 1,
            };
        }
        return { mode: 'count', count: optionNumber$2(context, 'bins') ?? 24 };
    }
    /** Volume-at-price compiler with fixed/visible/session/periodic profiles and POC/VAH/VAL. */
    const compileAdvancedVolumeProfileMark = (context) => {
        const advancedOptionNames = [
            'scope',
            'rowMode',
            'rowSize',
            'tickSize',
            'ticksPerRow',
            'allocation',
            'valueArea',
            'placement',
            'visibleStart',
            'visibleEnd',
            'period',
            'timezoneOffsetMinutes',
        ];
        const hasOhlcInput = ['open', 'high', 'low', 'close'].some((name) => {
            const configured = context.layer.mark.fields[name];
            return configured !== undefined ? context.table.has(configured) : context.table.has(name);
        });
        if (!hasOhlcInput &&
            !advancedOptionNames.some((name) => Object.hasOwn(context.layer.mark.options, name))) {
            return compileVolumeProfileMark(context);
        }
        const options = {
            scope: volumeScope(context),
            rows: volumeRows(context),
            allocation: optionString$2(context, 'allocation') === 'close' ||
                optionString$2(context, 'allocation') === 'typical'
                ? optionString$2(context, 'allocation')
                : 'uniform-range',
            valueArea: optionNumber$2(context, 'valueArea') ?? 0.7,
            placement: optionString$2(context, 'placement') === 'left' ? 'left' : 'right',
        };
        const profiles = buildVolumeProfiles(financialRows(context), options);
        if (profiles.length === 0)
            return [];
        const { layer, plot, theme, performance } = context;
        const slotWidth = plot.width / profiles.length;
        const nodes = [];
        profiles.forEach((profile, profileIndex) => {
            const maximum = Math.max(1, ...profile.rows.map(({ volume }) => volume));
            const priceSpan = Math.max(Number.EPSILON, profile.priceHigh - profile.priceLow);
            const mapY = (value) => plot.y + plot.height - ((value - profile.priceLow) / priceSpan) * plot.height;
            profile.rows.forEach((row) => {
                const width = (row.volume / maximum) * slotWidth * 0.88;
                const left = plot.x + profileIndex * slotWidth;
                const x = profile.placement === 'left' ? left : left + slotWidth - width;
                const y0 = mapY(row.priceLow);
                const y1 = mapY(row.priceHigh);
                const fill = row.pointOfControl
                    ? (theme.colors.focus ?? context.color)
                    : row.inValueArea
                        ? colorWithOpacity(context.color, 0.78)
                        : colorWithOpacity(context.color, 0.38);
                nodes.push({
                    type: 'rect',
                    ...nodeBase(`${layer.id}:profile:${profile.id}:${row.index}`, {
                        zIndex: layer.zIndex,
                        opacity: layer.mark.opacity,
                        interactive: performance.enableHitTesting,
                        datum: {
                            layerId: layer.id,
                            rowIndex: row.sourceIndexes.at(-1) ?? row.index,
                            datum: { ...row },
                            tooltip: {
                                profile: profile.id,
                                priceLow: row.priceLow,
                                priceHigh: row.priceHigh,
                                volume: row.volume,
                                proportion: row.proportion,
                                pointOfControl: row.pointOfControl,
                                inValueArea: row.inValueArea,
                                POC: profile.poc,
                                VAH: profile.vah,
                                VAL: profile.val,
                                sourceRows: row.sourceIndexes.join(', '),
                            },
                        },
                    }),
                    x,
                    y: Math.min(y0, y1) + 0.5,
                    width: Math.max(0.5, width),
                    height: Math.max(0.5, Math.abs(y0 - y1) - 1),
                    fill: layer.mark.fill ?? fill,
                    stroke: layer.mark.stroke ?? theme.colors.background,
                    lineWidth: layer.mark.lineWidth ?? 0.8,
                    cornerRadius: layer.mark.cornerRadius ?? 1,
                });
            });
            for (const [name, value] of [
                ['poc', profile.poc],
                ['vah', profile.vah],
                ['val', profile.val],
            ]) {
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:profile:${profile.id}:${name}`, {
                        zIndex: layer.zIndex + 1,
                    }),
                    x1: plot.x + profileIndex * slotWidth,
                    x2: plot.x + (profileIndex + 1) * slotWidth,
                    y1: mapY(value),
                    y2: mapY(value),
                    stroke: name === 'poc' ? theme.colors.focus : theme.colors.mutedText,
                    lineWidth: name === 'poc' ? 1.8 : 1,
                    dash: name === 'poc' ? [] : [4, 3],
                });
            }
        });
        return nodes;
    };

    function finite$8(value, path) {
        if (typeof value !== 'number' || !Number.isFinite(value)) {
            throw new GraflumeError('INVALID_DATA', `${path} must be a finite number.`, { path });
        }
        return value;
    }
    function clamp$6(value, low, high) {
        return Math.max(low, Math.min(high, value));
    }
    function isIrregular(field) {
        return 'points' in field;
    }
    function regularGrid(field) {
        const width = Math.floor(finite$8(field.width, '$.width'));
        const height = Math.floor(finite$8(field.height, '$.height'));
        if (width < 2 || height < 2 || field.values.length !== width * height) {
            throw new GraflumeError('INVALID_DATA', 'Regular scalar field needs at least 2x2 values matching width*height.');
        }
        if (field.x !== undefined && field.x.length !== width)
            throw new GraflumeError('INVALID_DATA', '$.x length must equal width.');
        if (field.y !== undefined && field.y.length !== height)
            throw new GraflumeError('INVALID_DATA', '$.y length must equal height.');
        if (field.mask !== undefined && field.mask.length !== field.values.length)
            throw new GraflumeError('INVALID_DATA', '$.mask length must equal values length.');
        const grid = Array.from({ length: height }, (_, row) => Array.from({ length: width }, (_, column) => {
            const index = row * width + column;
            if (field.mask?.[index] === false)
                return null;
            const value = field.values[index];
            return value === null ? null : finite$8(value, `$.values[${index}]`);
        }));
        const points = Array.from({ length: height }, (_, row) => Array.from({ length: width }, (_, column) => ({
            x: field.x === undefined ? column : finite$8(field.x[column], `$.x[${column}]`),
            y: field.y === undefined ? row : finite$8(field.y[row], `$.y[${row}]`),
        })));
        const triangles = [];
        for (let row = 0; row < height - 1; row += 1) {
            for (let column = 0; column < width - 1; column += 1) {
                const indices = [
                    row * width + column,
                    row * width + column + 1,
                    (row + 1) * width + column + 1,
                    (row + 1) * width + column,
                ];
                const vertices = indices.map((index) => {
                    const value = field.mask?.[index] === false ? null : field.values[index];
                    if (value === null)
                        return null;
                    const r = Math.floor(index / width);
                    const c = index % width;
                    return {
                        x: points[r][c].x,
                        y: points[r][c].y,
                        value: finite$8(value, `$.values[${index}]`),
                        source: index,
                    };
                });
                if (vertices.some((vertex) => vertex === null))
                    continue;
                const [a, b, c, d] = vertices;
                const source = row * (width - 1) + column;
                triangles.push({ vertices: [a, b, c], source }, { vertices: [a, c, d], source });
            }
        }
        return { grid, points, triangles };
    }
    function irregularTriangles(field) {
        const points = field.points.map((point, index) => ({
            x: finite$8(point.x, `$.points[${index}].x`),
            y: finite$8(point.y, `$.points[${index}].y`),
            value: finite$8(point.value, `$.points[${index}].value`),
            source: point.source ?? index,
        }));
        return field.triangles.map((triangle, index) => {
            const indices = [...triangle];
            if (new Set(indices).size !== 3 ||
                indices.some((value) => !Number.isInteger(value) || value < 0 || value >= points.length)) {
                throw new GraflumeError('INVALID_DATA', `$.triangles[${index}] must reference three unique points.`);
            }
            const vertices = indices.map((pointIndex) => points[pointIndex]);
            const area = (vertices[1].x - vertices[0].x) * (vertices[2].y - vertices[0].y) -
                (vertices[1].y - vertices[0].y) * (vertices[2].x - vertices[0].x);
            if (Math.abs(area) < 1e-12)
                throw new GraflumeError('INVALID_DATA', `$.triangles[${index}] is degenerate.`);
            return {
                vertices: area > 0 ? vertices : [vertices[0], vertices[2], vertices[1]],
                source: index,
            };
        });
    }
    function interpolateVertex(a, b, level) {
        const ratio = a.value === b.value ? 0.5 : clamp$6((level - a.value) / (b.value - a.value), 0, 1);
        return {
            x: a.x + (b.x - a.x) * ratio,
            y: a.y + (b.y - a.y) * ratio,
            value: level,
            source: a.source,
        };
    }
    function clipScalar(polygon, level, keepAbove) {
        const output = [];
        for (let index = 0; index < polygon.length; index += 1) {
            const current = polygon[index];
            const next = polygon[(index + 1) % polygon.length];
            const currentInside = keepAbove ? current.value >= level : current.value <= level;
            const nextInside = keepAbove ? next.value >= level : next.value <= level;
            if (currentInside)
                output.push(current);
            if (currentInside !== nextInside)
                output.push(interpolateVertex(current, next, level));
        }
        return output;
    }
    function pointKey(point) {
        return `${Math.round(point.x * 1e9)}:${Math.round(point.y * 1e9)}`;
    }
    function edgeKey(a, b) {
        const first = pointKey(a);
        const second = pointKey(b);
        return first < second ? `${first}|${second}` : `${second}|${first}`;
    }
    function signedArea(points) {
        let area = 0;
        for (let index = 0; index < points.length - 1; index += 1) {
            const a = points[index];
            const b = points[index + 1];
            area += a.x * b.y - b.x * a.y;
        }
        return area / 2;
    }
    function pointInRing(point, ring) {
        let inside = false;
        for (let current = 0, previous = ring.length - 1; current < ring.length; previous = current, current += 1) {
            const a = ring[current];
            const b = ring[previous];
            if (a.y > point.y !== b.y > point.y &&
                point.x < ((b.x - a.x) * (point.y - a.y)) / (b.y - a.y) + a.x)
                inside = !inside;
        }
        return inside;
    }
    function smoothRing(points, iterations) {
        let output = [...points];
        const closed = pointKey(output[0]) === pointKey(output.at(-1));
        for (let iteration = 0; iteration < iterations; iteration += 1) {
            const source = closed ? output.slice(0, -1) : output;
            const next = [];
            for (let index = 0; index < source.length - (closed ? 0 : 1); index += 1) {
                const a = source[index];
                const b = source[(index + 1) % source.length];
                next.push({ x: a.x * 0.75 + b.x * 0.25, y: a.y * 0.75 + b.y * 0.25 }, { x: a.x * 0.25 + b.x * 0.75, y: a.y * 0.25 + b.y * 0.75 });
            }
            if (!closed)
                (next.unshift(source[0]), next.push(source.at(-1)));
            else
                next.push(next[0]);
            output = next;
        }
        return output;
    }
    function bandRegions(triangles, low, high, smoothing) {
        const boundary = new Map();
        triangles.forEach((triangle) => {
            const above = clipScalar(triangle.vertices, low, true);
            const polygon = above.length < 3 ? [] : clipScalar(above, high, false);
            if (polygon.length < 3)
                return;
            for (let index = 0; index < polygon.length; index += 1) {
                const a = polygon[index];
                const b = polygon[(index + 1) % polygon.length];
                const key = edgeKey(a, b);
                if (boundary.has(key))
                    boundary.delete(key);
                else
                    boundary.set(key, { a, b, sources: new Set([triangle.source]) });
            }
        });
        const remaining = [...boundary.values()];
        const rings = [];
        while (remaining.length > 0) {
            const seed = remaining.shift();
            const points = [seed.a, seed.b];
            const sources = new Set(seed.sources);
            let current = seed.b;
            for (let guard = 0; guard < boundary.size + 2; guard += 1) {
                const index = remaining.findIndex(({ a, b }) => pointKey(a) === pointKey(current) || pointKey(b) === pointKey(current));
                if (index < 0)
                    break;
                const edge = remaining.splice(index, 1)[0];
                edge.sources.forEach((source) => sources.add(source));
                current = pointKey(edge.a) === pointKey(current) ? edge.b : edge.a;
                points.push(current);
                if (pointKey(current) === pointKey(points[0]))
                    break;
            }
            if (points.length >= 4 && pointKey(points[0]) === pointKey(points.at(-1)))
                rings.push({ points: smoothRing(points, smoothing), sources });
        }
        const ordered = rings.sort((a, b) => Math.abs(signedArea(b.points)) - Math.abs(signedArea(a.points)));
        const regions = [];
        ordered.forEach((ring) => {
            const parent = regions.find((region) => pointInRing(ring.points[0], region.outer));
            if (parent === undefined) {
                regions.push({
                    low,
                    high,
                    outer: ring.points,
                    holes: [],
                    sourceTriangles: [...ring.sources].sort((a, b) => a - b),
                });
            }
            else {
                const index = regions.indexOf(parent);
                regions[index] = {
                    ...parent,
                    holes: [...parent.holes, ring.points],
                    sourceTriangles: [...new Set([...parent.sourceTriangles, ...ring.sources])].sort((a, b) => a - b),
                };
            }
        });
        return regions;
    }
    function triangleIsolines(triangles, levels, smoothing) {
        const output = [];
        levels.forEach((level, levelIndex) => {
            const segments = [];
            triangles.forEach((triangle) => {
                const intersections = [];
                for (let index = 0; index < 3; index += 1) {
                    const a = triangle.vertices[index];
                    const b = triangle.vertices[(index + 1) % 3];
                    if ((a.value < level && b.value >= level) || (b.value < level && a.value >= level))
                        intersections.push(interpolateVertex(a, b, level));
                }
                if (intersections.length === 2)
                    segments.push({ a: intersections[0], b: intersections[1], source: triangle.source });
            });
            while (segments.length > 0) {
                const seed = segments.shift();
                const points = [seed.a, seed.b];
                const sources = new Set([seed.source]);
                let current = seed.b;
                for (;;) {
                    const index = segments.findIndex(({ a, b }) => pointKey(a) === pointKey(current) || pointKey(b) === pointKey(current));
                    if (index < 0)
                        break;
                    const segment = segments.splice(index, 1)[0];
                    sources.add(segment.source);
                    current = pointKey(segment.a) === pointKey(current) ? segment.b : segment.a;
                    points.push(current);
                    if (pointKey(current) === pointKey(points[0]))
                        break;
                }
                output.push({
                    level,
                    levelIndex,
                    points: smoothRing(points, smoothing),
                    closed: pointKey(points[0]) === pointKey(points.at(-1)),
                    sourceRows: [...sources].sort((a, b) => a - b),
                });
            }
        });
        return output;
    }
    /** Extracts regular or triangulated irregular isolines and filled/banded polygon-hole topology. */
    function contourField(field, options = {}) {
        const smoothing = clamp$6(Math.floor(options.smoothing ?? 0), 0, 4);
        let triangles;
        let grid = null;
        let gridPoints = null;
        if (isIrregular(field))
            triangles = irregularTriangles(field);
        else {
            const regular = regularGrid(field);
            triangles = regular.triangles;
            grid = regular.grid;
            gridPoints = regular.points;
        }
        const values = triangles.flatMap(({ vertices }) => vertices.map(({ value }) => value));
        const thresholdGrid = grid ?? [values];
        const thresholds = contourThresholds(thresholdGrid, {
            ...(options.thresholds === undefined ? {} : { thresholds: options.thresholds }),
            ...(options.levels === undefined ? {} : { levels: options.levels }),
            ...(options.method === undefined ? {} : { method: options.method }),
        });
        const isolines = grid !== null && gridPoints !== null
            ? extractIsolines(grid, gridPoints, thresholds, undefined, {
                saddle: options.saddle ?? 'asymptotic',
            }).map((line) => ({ ...line, points: smoothRing(line.points, smoothing) }))
            : triangleIsolines(triangles, thresholds, smoothing);
        const minimum = Math.min(...values);
        const maximum = Math.max(...values);
        const bounds = [...new Set([minimum, ...thresholds, maximum])].sort((a, b) => a - b);
        const bands = options.filled === false
            ? []
            : bounds
                .slice(0, -1)
                .flatMap((low, index) => bandRegions(triangles, low, bounds[index + 1], smoothing));
        return {
            thresholds,
            isolines,
            bands,
            input: isIrregular(field) ? 'triangulated-irregular' : 'regular-grid',
        };
    }
    function nearestVector(data, x, y) {
        let nearest = null;
        let distance = Infinity;
        data.forEach((datum) => {
            const current = (datum.x - x) ** 2 + (datum.y - y) ** 2;
            if (current < distance) {
                distance = current;
                nearest = datum;
            }
        });
        return nearest;
    }
    function integrateVector(field, seed, sign, bounds, options) {
        const points = [];
        let x = seed[0];
        let y = seed[1];
        let step = finite$8(options.step ?? Math.max(bounds[2] - bounds[0], bounds[3] - bounds[1]) / 80, '$.step');
        const tolerance = finite$8(options.tolerance ?? step * 0.05, '$.tolerance');
        const maximumSteps = clamp$6(Math.floor(options.maximumSteps ?? 500), 1, 20_000);
        const minimumMagnitude = Math.max(0, finite$8(options.minimumMagnitude ?? 1e-9, '$.minimumMagnitude'));
        const vectorAt = (px, py) => {
            const vector = nearestVector(field, px, py);
            if (vector === null)
                return null;
            const magnitude = Math.hypot(vector.u, vector.v);
            if (magnitude < minimumMagnitude)
                return null;
            return {
                u: (vector.u / magnitude) * sign,
                v: (vector.v / magnitude) * sign,
                magnitude,
            };
        };
        const advance = (px, py, amount) => {
            const first = vectorAt(px, py);
            if (first === null)
                return null;
            const second = vectorAt(px + (first.u * amount) / 2, py + (first.v * amount) / 2);
            if (second === null)
                return null;
            const third = vectorAt(px + (second.u * amount) / 2, py + (second.v * amount) / 2);
            if (third === null)
                return null;
            const fourth = vectorAt(px + third.u * amount, py + third.v * amount);
            if (fourth === null)
                return null;
            return {
                x: px + (amount * (first.u + 2 * second.u + 2 * third.u + fourth.u)) / 6,
                y: py + (amount * (first.v + 2 * second.v + 2 * third.v + fourth.v)) / 6,
                magnitude: first.magnitude,
            };
        };
        for (let index = 0; index < maximumSteps; index += 1) {
            const full = advance(x, y, step);
            const half = advance(x, y, step / 2);
            const twoHalf = half === null ? null : advance(half.x, half.y, step / 2);
            if (full === null || twoHalf === null)
                break;
            const error = Math.hypot(full.x - twoHalf.x, full.y - twoHalf.y);
            if (error > tolerance && step > 1e-9) {
                step *= 0.5;
                continue;
            }
            x = twoHalf.x;
            y = twoHalf.y;
            if (x < bounds[0] || x > bounds[2] || y < bounds[1] || y > bounds[3])
                break;
            points.push({ x, y, magnitude: twoHalf.magnitude });
            if (error < tolerance / 4)
                step *= 1.5;
            if (points.length > 2 && Math.hypot(points[0].x - x, points[0].y - y) < step * 0.5)
                break;
        }
        return points;
    }
    /** Computes magnitude/direction, normalized/sampled quivers and seeded adaptive forward/backward streamlines. */
    function analyzeVectorField(data, options = {}) {
        if (data.length === 0)
            return { vectors: [], streamlines: [], bounds: [0, 0, 1, 1] };
        const raw = data.map((datum, index) => {
            const x = finite$8(datum.x, `$.data[${index}].x`);
            const y = finite$8(datum.y, `$.data[${index}].y`);
            const u = finite$8(datum.u, `$.data[${index}].u`);
            const v = finite$8(datum.v, `$.data[${index}].v`);
            return {
                id: datum.id?.trim() || `vector-${index}`,
                x,
                y,
                u,
                v,
                magnitude: Math.hypot(u, v),
                direction: Math.atan2(v, u),
            };
        });
        const maximum = Math.max(...raw.map(({ magnitude }) => magnitude), Number.EPSILON);
        const normalized = raw.map((datum) => {
            const divisor = options.normalize === 'unit'
                ? datum.magnitude || 1
                : options.normalize === 'maximum'
                    ? maximum
                    : 1;
            return { ...datum, u: datum.u / divisor, v: datum.v / divisor };
        });
        const bounds = [
            Math.min(...raw.map(({ x }) => x)),
            Math.min(...raw.map(({ y }) => y)),
            Math.max(...raw.map(({ x }) => x)),
            Math.max(...raw.map(({ y }) => y)),
        ];
        const columns = clamp$6(Math.floor(options.sample?.columns ?? Math.ceil(Math.sqrt(raw.length))), 1, 1_000);
        const rows = clamp$6(Math.floor(options.sample?.rows ?? columns), 1, 1_000);
        const sampled = [];
        const sampledIds = new Set();
        for (let row = 0; row < rows; row += 1)
            for (let column = 0; column < columns; column += 1) {
                const x = bounds[0] + ((bounds[2] - bounds[0]) * (column + 0.5)) / columns;
                const y = bounds[1] + ((bounds[3] - bounds[1]) * (row + 0.5)) / rows;
                const nearest = nearestVector(normalized, x, y);
                if (nearest !== null && !sampledIds.has(nearest.id)) {
                    sampledIds.add(nearest.id);
                    sampled.push(nearest);
                }
            }
        const seeds = options.seeds ?? sampled.map(({ x, y }) => [x, y]);
        const direction = options.direction ?? 'both';
        const streamlines = seeds
            .map((seed, index) => {
            const forward = direction === 'backward' ? [] : integrateVector(normalized, seed, 1, bounds, options);
            const backward = direction === 'forward'
                ? []
                : integrateVector(normalized, seed, -1, bounds, options).reverse();
            return {
                id: `streamline-${index}`,
                seed: [...seed],
                points: [
                    ...backward,
                    {
                        x: seed[0],
                        y: seed[1],
                        magnitude: Math.hypot(nearestVector(normalized, seed[0], seed[1])?.u ?? 0, nearestVector(normalized, seed[0], seed[1])?.v ?? 0),
                    },
                    ...forward,
                ],
            };
        })
            .filter(({ points }) => points.length > 1);
        return { vectors: sampled, streamlines, bounds };
    }

    function stringOption$3(context, name) {
        const value = context.layer.mark.options[name];
        return typeof value === 'string' ? value : undefined;
    }
    function numberOption$3(context, name) {
        const value = context.layer.mark.options[name];
        return typeof value === 'number' && Number.isFinite(value) ? value : undefined;
    }
    function booleanOption$3(context, name) {
        const value = context.layer.mark.options[name];
        return typeof value === 'boolean' ? value : undefined;
    }
    function numberArrayOption$2(context, name) {
        const value = context.layer.mark.options[name];
        if (!Array.isArray(value) ||
            !value.every((entry) => typeof entry === 'number' && Number.isFinite(entry)))
            return undefined;
        return [...value];
    }
    function triangleOption(context) {
        const value = context.layer.mark.options.triangles;
        if (!Array.isArray(value) ||
            !value.every((entry) => Array.isArray(entry) &&
                entry.length === 3 &&
                entry.every((index) => typeof index === 'number' && Number.isInteger(index))))
            return undefined;
        return value.map((entry) => [entry[0], entry[1], entry[2]]);
    }
    function scalarField(context) {
        const { table, layer } = context;
        const valueField = layer.mark.fields.value ?? 'value';
        const rows = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const x = numericDataValue(table.value(rowIndex, layer.x.field));
            const y = numericDataValue(table.value(rowIndex, layer.y.field));
            const value = numericDataValue(table.value(rowIndex, valueField));
            if (x !== null && y !== null)
                rows.push({ x, y, value, rowIndex });
        }
        if (rows.length < 3)
            return null;
        const triangles = triangleOption(context);
        if (triangles !== undefined) {
            return {
                points: rows.flatMap(({ x, y, value, rowIndex }) => value === null ? [] : [{ x, y, value, source: rowIndex }]),
                triangles,
            };
        }
        const xs = [...new Set(rows.map(({ x }) => x))].sort((left, right) => left - right);
        const ys = [...new Set(rows.map(({ y }) => y))].sort((left, right) => left - right);
        if (xs.length * ys.length !== rows.length || xs.length < 2 || ys.length < 2)
            return null;
        const byPosition = new Map(rows.map((row) => [`${row.x}:${row.y}`, row]));
        return {
            width: xs.length,
            height: ys.length,
            x: xs,
            y: ys,
            values: ys.flatMap((y) => xs.map((x) => byPosition.get(`${x}:${y}`)?.value ?? null)),
            mask: ys.flatMap((y) => xs.map((x) => byPosition.get(`${x}:${y}`)?.value !== null)),
        };
    }
    function scalarExtent(field) {
        if ('points' in field) {
            return [
                Math.min(...field.points.map(({ x }) => x)),
                Math.min(...field.points.map(({ y }) => y)),
                Math.max(...field.points.map(({ x }) => x)),
                Math.max(...field.points.map(({ y }) => y)),
            ];
        }
        return [
            Math.min(...(field.x ?? Array.from({ length: field.width }, (_, index) => index))),
            Math.min(...(field.y ?? Array.from({ length: field.height }, (_, index) => index))),
            Math.max(...(field.x ?? Array.from({ length: field.width }, (_, index) => index))),
            Math.max(...(field.y ?? Array.from({ length: field.height }, (_, index) => index))),
        ];
    }
    /** Regular/irregular contour compiler with filled bands, masks, holes, smoothing, and saddle policy. */
    const compileAdvancedContourMark = (context) => {
        const field = scalarField(context);
        if (field === null)
            return [];
        const thresholds = numberArrayOption$2(context, 'thresholds');
        const method = stringOption$3(context, 'thresholdMethod');
        const saddle = stringOption$3(context, 'saddle');
        const levels = numberOption$3(context, 'levels');
        const filled = booleanOption$3(context, 'filled');
        const smoothing = numberOption$3(context, 'smoothing');
        const options = {
            ...(thresholds === undefined ? {} : { thresholds }),
            ...(levels === undefined ? {} : { levels }),
            ...(method === 'quantile' ? { method } : method === 'linear' ? { method } : {}),
            ...(filled === undefined ? {} : { filled }),
            ...(smoothing === undefined ? {} : { smoothing }),
            ...(saddle === 'high' || saddle === 'low' || saddle === 'asymptotic' ? { saddle } : {}),
        };
        const result = contourField(field, options);
        const bounds = scalarExtent(field);
        const spanX = Math.max(Number.EPSILON, bounds[2] - bounds[0]);
        const spanY = Math.max(Number.EPSILON, bounds[3] - bounds[1]);
        const map = (point) => ({
            x: context.plot.x + ((point.x - bounds[0]) / spanX) * context.plot.width,
            y: context.plot.y + context.plot.height - ((point.y - bounds[1]) / spanY) * context.plot.height,
        });
        const nodes = [];
        const regularSourceRows = 'points' in field
            ? undefined
            : Array.from({ length: context.table.length }, (_, index) => index);
        result.bands.forEach((band, index) => {
            const ratio = index / Math.max(1, result.bands.length - 1);
            const color = categoricalColor(context.theme, index, Math.max(2, result.bands.length));
            nodes.push({
                type: 'path',
                ...nodeBase(`${context.layer.id}:contour-band:${index}`, {
                    zIndex: context.layer.zIndex,
                    opacity: context.layer.mark.opacity,
                    interactive: context.performance.enableHitTesting,
                    datum: {
                        layerId: context.layer.id,
                        rowIndex: band.sourceTriangles[0] ?? index,
                        datum: {
                            low: band.low,
                            high: band.high,
                            sourceTriangles: [...band.sourceTriangles],
                        },
                        tooltip: {
                            low: band.low,
                            high: band.high,
                            input: result.input,
                            holes: band.holes.length,
                            sourceTriangles: band.sourceTriangles.join(', '),
                        },
                    },
                }),
                points: band.outer.map(map),
                subpaths: band.holes.map((hole) => hole.map(map)),
                closed: true,
                fill: context.layer.mark.fill ?? colorWithOpacity(color, 0.3 + ratio * 0.5),
                fillRule: 'evenodd',
                stroke: context.layer.mark.stroke ?? color,
                lineWidth: Math.max(0.4, context.layer.mark.lineWidth ?? 0.8),
                lineJoin: 'round',
            });
        });
        result.isolines.forEach((line, index) => {
            const sourceRowIndices = line.sourceRows.length > 0 ? [...line.sourceRows] : (regularSourceRows ?? []);
            const color = context.layer.mark.stroke ?? categoricalColor(context.theme, index, result.isolines.length);
            nodes.push({
                type: 'path',
                ...nodeBase(`${context.layer.id}:contour-line:${line.levelIndex}:${index}`, {
                    zIndex: context.layer.zIndex + 1,
                    opacity: context.layer.mark.opacity,
                    interactive: context.performance.enableHitTesting,
                    datum: {
                        layerId: context.layer.id,
                        rowIndex: line.sourceRows[0] ?? index,
                        datum: { level: line.level, sourceRows: sourceRowIndices },
                        tooltip: {
                            kind: 'scalar-isoline',
                            level: line.level,
                            closed: line.closed,
                            input: result.input,
                            sourceRows: sourceRowIndices.join(', '),
                            sourceRowIndices,
                        },
                    },
                }),
                points: line.points.map(map),
                closed: line.closed,
                stroke: color,
                lineWidth: context.layer.mark.lineWidth ?? 1.6,
                lineCap: context.theme.mark.lineCap ?? 'round',
                lineJoin: context.theme.mark.lineJoin ?? 'round',
            });
        });
        return nodes;
    };
    function vectorData(context) {
        const { table, layer } = context;
        const uField = layer.mark.fields.u ?? 'u';
        const vField = layer.mark.fields.v ?? 'v';
        const magnitudeField = layer.mark.fields.magnitude ?? 'magnitude';
        const directionField = layer.mark.fields.direction ?? 'direction';
        const idField = layer.mark.fields.id ?? 'id';
        return Array.from({ length: table.length }, (_, rowIndex) => {
            const x = numericDataValue(table.value(rowIndex, layer.x.field));
            const y = numericDataValue(table.value(rowIndex, layer.y.field));
            if (x === null || y === null)
                return null;
            let u = table.has(uField) ? numericDataValue(table.value(rowIndex, uField)) : null;
            let v = table.has(vField) ? numericDataValue(table.value(rowIndex, vField)) : null;
            if (u === null || v === null) {
                const magnitude = table.has(magnitudeField)
                    ? (numericDataValue(table.value(rowIndex, magnitudeField)) ?? 1)
                    : 1;
                const degrees = table.has(directionField)
                    ? (numericDataValue(table.value(rowIndex, directionField)) ?? 0)
                    : 0;
                const radians = (degrees * Math.PI) / 180;
                u = Math.sin(radians) * magnitude;
                v = Math.cos(radians) * magnitude;
            }
            const rawId = table.has(idField) ? table.value(rowIndex, idField) : undefined;
            return {
                x,
                y,
                u,
                v,
                id: rawId === null || rawId === undefined ? `row-${rowIndex}` : String(rawId),
            };
        }).filter((datum) => datum !== null);
    }
    function seedOption(context) {
        const value = context.layer.mark.options.seeds;
        if (!Array.isArray(value))
            return undefined;
        const seeds = value.flatMap((entry) => Array.isArray(entry) &&
            entry.length === 2 &&
            entry.every((part) => typeof part === 'number' && Number.isFinite(part))
            ? [[entry[0], entry[1]]]
            : []);
        return seeds.length === value.length ? seeds : undefined;
    }
    /** Vector compiler with transform-derived magnitude/direction, normalized sampling, and adaptive streamlines. */
    const compileAdvancedVectorMark = (context) => {
        const data = vectorData(context);
        if (data.length === 0)
            return [];
        const normalize = stringOption$3(context, 'normalize');
        const direction = stringOption$3(context, 'streamlineDirection');
        const seeds = seedOption(context);
        const columns = numberOption$3(context, 'sampleColumns');
        const rows = numberOption$3(context, 'sampleRows');
        const step = numberOption$3(context, 'streamlineStep');
        const tolerance = numberOption$3(context, 'streamlineTolerance');
        const maximumSteps = numberOption$3(context, 'streamlineMaximumSteps');
        const minimumMagnitude = numberOption$3(context, 'minimumMagnitude');
        const options = {
            ...(normalize === 'unit' || normalize === 'maximum' || normalize === 'none'
                ? { normalize }
                : {}),
            ...(columns === undefined && rows === undefined
                ? {}
                : {
                    sample: {
                        ...(columns === undefined ? {} : { columns }),
                        ...(rows === undefined ? {} : { rows }),
                    },
                }),
            ...(seeds === undefined ? {} : { seeds }),
            ...(direction === 'forward' || direction === 'backward' || direction === 'both'
                ? { direction }
                : {}),
            ...(step === undefined ? {} : { step }),
            ...(tolerance === undefined ? {} : { tolerance }),
            ...(maximumSteps === undefined ? {} : { maximumSteps }),
            ...(minimumMagnitude === undefined ? {} : { minimumMagnitude }),
        };
        const result = analyzeVectorField(data, options);
        const spanX = Math.max(Number.EPSILON, result.bounds[2] - result.bounds[0]);
        const spanY = Math.max(Number.EPSILON, result.bounds[3] - result.bounds[1]);
        const map = (x, y) => ({
            x: context.plot.x + ((x - result.bounds[0]) / spanX) * context.plot.width,
            y: context.plot.y + context.plot.height - ((y - result.bounds[1]) / spanY) * context.plot.height,
        });
        const nodes = [];
        result.streamlines.forEach((streamline, index) => {
            nodes.push({
                type: 'path',
                ...nodeBase(`${context.layer.id}:streamline:${index}`, {
                    zIndex: context.layer.zIndex,
                    opacity: context.layer.mark.opacity,
                    interactive: context.performance.enableHitTesting,
                    datum: {
                        layerId: context.layer.id,
                        rowIndex: index,
                        datum: { id: streamline.id, seed: [...streamline.seed] },
                        tooltip: {
                            id: streamline.id,
                            seedX: streamline.seed[0],
                            seedY: streamline.seed[1],
                            steps: streamline.points.length,
                        },
                    },
                }),
                points: streamline.points.map(({ x, y }) => map(x, y)),
                closed: false,
                stroke: context.layer.mark.stroke ?? colorWithOpacity(context.color, 0.62),
                lineWidth: context.layer.mark.lineWidth ?? 1.2,
                lineCap: context.theme.mark.lineCap ?? 'round',
                lineJoin: context.theme.mark.lineJoin ?? 'round',
            });
        });
        result.vectors.forEach((vector, index) => {
            const start = map(vector.x, vector.y);
            const normalizedMagnitude = Math.hypot(vector.u, vector.v);
            const ratio = options.normalize === 'unit'
                ? normalizedMagnitude === 0
                    ? 0
                    : 1
                : options.normalize === 'maximum'
                    ? normalizedMagnitude
                    : normalizedMagnitude / (1 + normalizedMagnitude);
            const length = 8 + Math.max(0, Math.min(1, ratio)) * 24;
            const denominator = normalizedMagnitude || 1;
            const dx = (vector.u / denominator) * length;
            const dy = (-vector.v / denominator) * length;
            const end = { x: start.x + dx, y: start.y + dy };
            const color = context.layer.mark.stroke ?? context.color;
            const angle = Math.atan2(dy, dx);
            const head = 5;
            const datum = {
                layerId: context.layer.id,
                rowIndex: index,
                datum: {
                    id: vector.id,
                    x: vector.x,
                    y: vector.y,
                    u: vector.u,
                    v: vector.v,
                    magnitude: vector.magnitude,
                    normalizedMagnitude,
                    normalization: options.normalize ?? 'none',
                    direction: vector.direction,
                },
                tooltip: {
                    id: vector.id,
                    x: vector.x,
                    y: vector.y,
                    u: vector.u,
                    v: vector.v,
                    magnitude: vector.magnitude,
                    normalizedMagnitude,
                    normalization: options.normalize ?? 'none',
                    directionRadians: vector.direction,
                },
            };
            nodes.push({
                type: 'line',
                ...nodeBase(`${context.layer.id}:vector:${vector.id}`, {
                    zIndex: context.layer.zIndex + 1,
                    opacity: context.layer.mark.opacity,
                    interactive: context.performance.enableHitTesting,
                    datum,
                }),
                x1: start.x,
                y1: start.y,
                x2: end.x,
                y2: end.y,
                stroke: color,
                lineWidth: (context.layer.mark.lineWidth ?? 1.3) + ratio * 1.6,
                lineCap: context.theme.mark.lineCap ?? 'round',
            });
            nodes.push({
                type: 'path',
                ...nodeBase(`${context.layer.id}:vector-head:${vector.id}`, {
                    zIndex: context.layer.zIndex + 2,
                    opacity: context.layer.mark.opacity,
                }),
                points: [
                    end,
                    {
                        x: end.x - Math.cos(angle - Math.PI / 5) * head,
                        y: end.y - Math.sin(angle - Math.PI / 5) * head,
                    },
                    {
                        x: end.x - Math.cos(angle + Math.PI / 5) * head,
                        y: end.y - Math.sin(angle + Math.PI / 5) * head,
                    },
                ],
                closed: true,
                fill: color,
                lineWidth: 0,
            });
        });
        return nodes;
    };

    function finite$7(value, path) {
        if (typeof value !== 'number' || !Number.isFinite(value)) {
            throw new GraflumeError('INVALID_DATA', `${path} must be a finite number.`, { path });
        }
        return value;
    }
    function label(value, path) {
        const normalized = value.trim();
        if (normalized === '')
            throw new GraflumeError('INVALID_DATA', `${path} must be non-empty.`, { path });
        return normalized;
    }
    function clamp$5(value, low, high) {
        return Math.max(low, Math.min(high, value));
    }
    /** Resolves pie zero/negative, minimum-slice, sorting, padding and keyboard traversal semantics. */
    function layoutPie(data, options = {}) {
        const negative = options.negative ?? 'reject';
        const zero = options.zero ?? 'hide';
        const normalized = data.flatMap((datum, index) => {
            const id = label(datum.id, `$.data[${index}].id`);
            const rawValue = finite$7(datum.value, `$.data[${index}].value`);
            if (rawValue < 0 && negative === 'reject')
                throw new GraflumeError('INVALID_DATA', 'Negative pie values require an explicit policy.');
            if (rawValue < 0 && negative === 'hide')
                return [];
            if (rawValue === 0 && zero === 'hide')
                return [];
            return [
                {
                    id,
                    label: datum.label?.trim() || id,
                    rawValue,
                    value: Math.abs(rawValue),
                    inputIndex: index,
                    sourceRows: [index],
                },
            ];
        });
        // Quick APIs use the semantic category as the slice id, so repeated source
        // categories are folded deterministically before sorting and angle layout.
        const aggregated = new Map();
        normalized.forEach((datum) => {
            const existing = aggregated.get(datum.id);
            aggregated.set(datum.id, existing === undefined
                ? datum
                : {
                    ...existing,
                    rawValue: existing.rawValue + datum.rawValue,
                    value: existing.value + datum.value,
                    sourceRows: [...existing.sourceRows, ...datum.sourceRows],
                });
        });
        const values = [...aggregated.values()];
        if (options.sort === 'ascending')
            values.sort((a, b) => a.value - b.value || a.inputIndex - b.inputIndex);
        if (options.sort === 'descending')
            values.sort((a, b) => b.value - a.value || a.inputIndex - b.inputIndex);
        const start = finite$7(options.startAngle ?? -Math.PI / 2, '$.startAngle');
        const end = finite$7(options.endAngle ?? start + Math.PI * 2, '$.endAngle');
        if (end <= start || end - start > Math.PI * 2 + 1e-9)
            throw new GraflumeError('INVALID_SPEC', 'Pie angle range must be ascending and at most one turn.');
        const pad = clamp$5(finite$7(options.padAngle ?? 0, '$.padAngle'), 0, (end - start) / Math.max(1, values.length * 2));
        const minimum = clamp$5(finite$7(options.minimumAngle ?? (zero === 'minimum' ? 0.01 : 0), '$.minimumAngle'), 0, (end - start) / Math.max(1, values.length));
        const available = end - start - pad * values.length;
        const rawTotal = values.reduce((sum, { value }) => sum + value, 0);
        const provisional = values.map(({ value }) => rawTotal === 0 ? 0 : (value / rawTotal) * available);
        const fixed = provisional.map((angle) => angle < minimum);
        const fixedTotal = fixed.filter(Boolean).length * minimum;
        const flexibleTotal = provisional.reduce((sum, angle, index) => sum + (fixed[index] ? 0 : angle), 0);
        if (fixedTotal > available + 1e-9)
            throw new GraflumeError('INVALID_SPEC', 'minimumAngle cannot fit all pie slices.');
        const angles = provisional.map((angle, index) => fixed[index]
            ? minimum
            : flexibleTotal === 0
                ? 0
                : (angle * (available - fixedTotal)) / flexibleTotal);
        let cursor = start;
        return values.map((datum, index) => {
            const sliceStart = cursor + pad / 2;
            const sliceEnd = sliceStart + angles[index];
            cursor += angles[index] + pad;
            const proportion = rawTotal === 0 ? 0 : datum.value / rawTotal;
            return {
                id: datum.id,
                label: datum.label,
                sourceRows: datum.sourceRows,
                rawValue: datum.rawValue,
                value: datum.value,
                proportion,
                startAngle: sliceStart,
                endAngle: sliceEnd,
                padAngle: pad,
                minimumApplied: fixed[index],
                tabIndex: index === 0 ? 0 : -1,
                accessibleLabel: `${datum.label}: ${datum.value} (${(proportion * 100).toFixed(1)}%)`,
            };
        });
    }
    /** Roving keyboard focus for circular slice traversal. */
    function nextPieSlice(slices, current, direction) {
        if (slices.length === 0)
            return null;
        if (direction === 'first')
            return slices[0].id;
        if (direction === 'last')
            return slices.at(-1).id;
        const index = Math.max(0, slices.findIndex(({ id }) => id === current));
        const delta = direction === 'next' ? 1 : -1;
        return slices[(index + delta + slices.length) % slices.length].id;
    }
    /** Packs overlapping intervals into grouped lanes with milestones, clipping, durations and navigator domain. */
    function layoutTimeline(data, options = {}) {
        const normalized = data.map((datum, index) => {
            const id = label(datum.id, `$.data[${index}].id`);
            const start = finite$7(datum.start, `$.data[${index}].start`);
            const end = datum.end === undefined ? start : finite$7(datum.end, `$.data[${index}].end`);
            if (end < start)
                throw new GraflumeError('INVALID_DATA', 'Timeline end must be at or after start.');
            return {
                id,
                start,
                end,
                group: datum.group?.trim() || 'default',
                label: datum.label?.trim() || id,
                milestone: datum.milestone === true || end === start,
                dependencies: [...(datum.dependencies ?? [])],
            };
        });
        if (new Set(normalized.map(({ id }) => id)).size !== normalized.length)
            throw new GraflumeError('INVALID_DATA', 'Timeline ids must be unique.');
        const ids = new Set(normalized.map(({ id }) => id));
        normalized.forEach(({ dependencies, id }) => dependencies.forEach((dependency) => {
            if (!ids.has(dependency))
                throw new GraflumeError('INVALID_DATA', `Timeline item "${id}" references unknown dependency "${dependency}".`);
        }));
        const rawObserved = normalized.length === 0
            ? [0, 1]
            : [
                Math.min(...normalized.map(({ start }) => start)),
                Math.max(...normalized.map(({ end }) => end)),
            ];
        const observed = rawObserved[0] === rawObserved[1]
            ? [
                rawObserved[0] - (rawObserved[0] === 0 ? 1 : Math.abs(rawObserved[0]) * 0.05),
                rawObserved[1] + (rawObserved[1] === 0 ? 1 : Math.abs(rawObserved[1]) * 0.05),
            ]
            : rawObserved;
        const authoredDomain = options.domain ?? observed;
        if (authoredDomain[1] < authoredDomain[0])
            throw new GraflumeError('INVALID_SPEC', 'Timeline domain must be ascending.');
        const domain = authoredDomain[0] === authoredDomain[1]
            ? [
                authoredDomain[0] - (authoredDomain[0] === 0 ? 1 : Math.abs(authoredDomain[0]) * 0.05),
                authoredDomain[1] + (authoredDomain[1] === 0 ? 1 : Math.abs(authoredDomain[1]) * 0.05),
            ]
            : authoredDomain;
        const groupOrder = options.groupOrder ?? [...new Set(normalized.map(({ group }) => group))];
        const groupIndex = new Map(groupOrder.map((group, index) => [group, index]));
        normalized.forEach(({ group }) => {
            if (!groupIndex.has(group))
                throw new GraflumeError('INVALID_SPEC', `Timeline groupOrder omits "${group}".`);
        });
        const laneEnds = new Map();
        const items = [...normalized]
            .sort((a, b) => groupIndex.get(a.group) - groupIndex.get(b.group) ||
            a.start - b.start ||
            a.end - b.end ||
            a.id.localeCompare(b.id))
            .flatMap((datum) => {
            const clip = options.clip !== false;
            const clippedStart = clip ? Math.max(domain[0], datum.start) : datum.start;
            const clippedEnd = clip ? Math.min(domain[1], datum.end) : datum.end;
            if (clip && clippedEnd < clippedStart)
                return [];
            const lanes = laneEnds.get(datum.group) ?? [];
            let lane = lanes.findIndex((end) => end <= datum.start);
            if (lane < 0) {
                lane = lanes.length;
                lanes.push(datum.end);
            }
            else
                lanes[lane] = datum.end;
            laneEnds.set(datum.group, lanes);
            const duration = datum.end - datum.start;
            const visibleDuration = Math.max(0, clippedEnd - clippedStart);
            return [
                {
                    id: datum.id,
                    group: datum.group,
                    lane,
                    start: datum.start,
                    end: datum.end,
                    clippedStart,
                    clippedEnd,
                    duration,
                    visibleDuration,
                    milestone: datum.milestone,
                    clipped: clip && (clippedStart !== datum.start || clippedEnd !== datum.end),
                    dependencies: datum.dependencies,
                    durationLabel: datum.milestone ? 'milestone' : `${duration}`,
                },
            ];
        });
        const maximumLane = Math.max(0, ...items.map(({ lane }) => lane));
        return {
            domain,
            groups: groupOrder.map((group) => ({ group, lanes: laneEnds.get(group)?.length ?? 0 })),
            items,
            navigator: { minimum: observed[0], maximum: observed[1], start: domain[0], end: domain[1] },
            laneCount: maximumLane + 1,
        };
    }
    /** Builds radial/linear gauge bands, targets, custom ticks and an exact accessible numeric summary. */
    function gaugeModel(value, options = {}) {
        const minimum = finite$7(options.minimum ?? 0, '$.minimum');
        const maximum = finite$7(options.maximum ?? 100, '$.maximum');
        if (maximum <= minimum)
            throw new GraflumeError('INVALID_SPEC', 'Gauge maximum must exceed minimum.');
        const current = finite$7(value, '$.value');
        const format = options.format ?? ((number) => String(Number(number.toPrecision(8))));
        const bands = (options.bands ?? []).map((band, index) => {
            const from = finite$7(band.from, `$.bands[${index}].from`);
            const to = finite$7(band.to, `$.bands[${index}].to`);
            if (to <= from || from < minimum || to > maximum)
                throw new GraflumeError('INVALID_SPEC', 'Gauge bands must be ascending and inside the domain.');
            return { ...band, from, to };
        });
        for (let index = 1; index < bands.length; index += 1)
            if (bands[index].from < bands[index - 1].to)
                throw new GraflumeError('INVALID_SPEC', 'Gauge bands must not overlap.');
        const authoredTicks = typeof options.ticks === 'number' ? undefined : options.ticks;
        const tickCount = clamp$5(Math.floor(typeof options.ticks === 'number' ? options.ticks : 5), 2, 50);
        const ticks = authoredTicks === undefined
            ? Array.from({ length: tickCount }, (_, index) => minimum + ((maximum - minimum) * index) / (tickCount - 1))
            : authoredTicks.map((tick, index) => finite$7(tick, `$.ticks[${index}]`));
        const targets = (options.targets ?? []).map((target, index) => finite$7(target, `$.targets[${index}]`));
        const activeBand = bands.find((band) => current >= band.from && current <= band.to) ?? null;
        return {
            type: options.type ?? 'radial',
            value: current,
            minimum,
            maximum,
            position: clamp$5((current - minimum) / (maximum - minimum), 0, 1),
            bands: bands.map((band) => ({
                ...band,
                start: (band.from - minimum) / (maximum - minimum),
                end: (band.to - minimum) / (maximum - minimum),
            })),
            activeBand,
            targets: targets.map((target) => ({
                value: target,
                position: (target - minimum) / (maximum - minimum),
            })),
            ticks: ticks.map((tick) => ({
                value: tick,
                position: (tick - minimum) / (maximum - minimum),
                label: format(tick),
            })),
            accessibleSummary: `Value ${format(current)}; range ${format(minimum)} to ${format(maximum)}${targets.length === 0 ? '' : `; targets ${targets.map(format).join(', ')}`}${activeBand?.label === undefined ? '' : `; band ${activeBand.label}`}.`,
        };
    }
    class TableFormatterRegistry {
        #formatters = new Map();
        register(id, formatter) {
            const normalized = label(id, '$.id');
            if (this.#formatters.has(normalized))
                throw new GraflumeError('INVALID_SPEC', `Duplicate table formatter "${id}".`);
            this.#formatters.set(normalized, formatter);
        }
        format(id, value, row, locale) {
            const formatter = this.#formatters.get(id);
            if (formatter === undefined)
                throw new GraflumeError('INVALID_SPEC', `Unknown table formatter "${id}".`);
            return formatter(value, row, locale);
        }
        ids() {
            return [...this.#formatters.keys()];
        }
    }
    function tableDate(value) {
        const date = value instanceof Date ? value : new Date(String(value));
        return Number.isFinite(date.getTime()) ? date : null;
    }
    /** Creates the locale-aware built-in registry used by compiled tables and custom runtimes. */
    function createTableFormatterRegistry() {
        const registry = new TableFormatterRegistry();
        registry.register('string', (value) => String(value ?? ''));
        registry.register('number', (value, _row, locale) => typeof value === 'number'
            ? new Intl.NumberFormat(locale, { maximumFractionDigits: 4 }).format(value)
            : String(value ?? ''));
        registry.register('integer', (value, _row, locale) => typeof value === 'number'
            ? new Intl.NumberFormat(locale, { maximumFractionDigits: 0 }).format(value)
            : String(value ?? ''));
        registry.register('percent', (value, _row, locale) => typeof value === 'number'
            ? new Intl.NumberFormat(locale, { style: 'percent', maximumFractionDigits: 2 }).format(value)
            : String(value ?? ''));
        registry.register('date', (value, _row, locale) => {
            const date = tableDate(value);
            return date === null
                ? String(value ?? '')
                : new Intl.DateTimeFormat(locale, { dateStyle: 'medium', timeZone: 'UTC' }).format(date);
        });
        registry.register('datetime', (value, _row, locale) => {
            const date = tableDate(value);
            return date === null
                ? String(value ?? '')
                : new Intl.DateTimeFormat(locale, {
                    dateStyle: 'medium',
                    timeStyle: 'medium',
                    timeZone: 'UTC',
                }).format(date);
        });
        registry.register('json', (value) => JSON.stringify(value));
        return registry;
    }
    function compareValues(left, right) {
        if (left === right)
            return 0;
        if (left === null || left === undefined)
            return 1;
        if (right === null || right === undefined)
            return -1;
        if (typeof left === 'number' && typeof right === 'number')
            return left - right;
        return String(left).localeCompare(String(right), undefined, { numeric: true });
    }
    function matchesFilter(row, filter) {
        const value = row[filter.field];
        if (filter.operator === 'equals')
            return Object.is(value, filter.value);
        if (filter.operator === 'not-equals')
            return !Object.is(value, filter.value);
        if (filter.operator === 'contains')
            return String(value ?? '')
                .toLocaleLowerCase()
                .includes(String(filter.value ?? '').toLocaleLowerCase());
        const numeric = typeof value === 'number' ? value : Number(value);
        if (!Number.isFinite(numeric))
            return false;
        const threshold = finite$7(filter.value, `$.filters.${filter.field}.value`);
        if (filter.operator === 'greater')
            return numeric > threshold;
        if (filter.operator === 'greater-or-equal')
            return numeric >= threshold;
        if (filter.operator === 'less')
            return numeric < threshold;
        return numeric <= threshold;
    }
    function aggregateRows(rows, group) {
        const buckets = new Map();
        rows.forEach((row) => {
            const keyValues = group.fields.map((field) => row[field]);
            const key = JSON.stringify(keyValues);
            const bucket = buckets.get(key);
            if (bucket === undefined)
                buckets.set(key, {
                    keys: Object.fromEntries(group.fields.map((field, index) => [field, keyValues[index]])),
                    rows: [row],
                });
            else
                buckets.set(key, { ...bucket, rows: [...bucket.rows, row] });
        });
        return [...buckets.values()].map(({ keys, rows: values }) => ({
            ...keys,
            ...Object.fromEntries(group.aggregates.map(({ field, op, as }) => {
                const numeric = values.map((row) => Number(row[field])).filter(Number.isFinite);
                const result = op === 'count'
                    ? values.length
                    : numeric.length === 0
                        ? null
                        : op === 'sum'
                            ? numeric.reduce((sum, value) => sum + value, 0)
                            : op === 'mean'
                                ? numeric.reduce((sum, value) => sum + value, 0) / numeric.length
                                : op === 'min'
                                    ? Math.min(...numeric)
                                    : Math.max(...numeric);
                return [as, result];
            })),
            __count: values.length,
        }));
    }
    function pivotRows(rows, pivot) {
        const grouped = new Map();
        rows.forEach((row) => {
            const rowKey = String(row[pivot.row] ?? '');
            const columnKey = String(row[pivot.column] ?? '');
            const columns = grouped.get(rowKey) ?? new Map();
            const values = columns.get(columnKey) ?? [];
            if ((pivot.op ?? 'sum') === 'count')
                values.push(1);
            else {
                const value = Number(row[pivot.value]);
                if (Number.isFinite(value))
                    values.push(value);
            }
            columns.set(columnKey, values);
            grouped.set(rowKey, columns);
        });
        return [...grouped].map(([rowKey, columns]) => ({
            [pivot.row]: rowKey,
            ...Object.fromEntries([...columns].map(([column, values]) => [
                column,
                (pivot.op ?? 'sum') === 'mean'
                    ? values.reduce((sum, value) => sum + value, 0) / Math.max(1, values.length)
                    : values.reduce((sum, value) => sum + value, 0),
            ])),
        }));
    }
    /** Applies filter→group/pivot→sort→two-dimensional virtual windows with frozen regions. */
    function buildTableModel(rows, options = {}) {
        const sourceColumns = [
            ...new Set(rows.flatMap((row) => Object.keys(row).filter((field) => !field.startsWith('__')))),
        ];
        const schemaColumns = options.group !== undefined
            ? [...options.group.fields, ...options.group.aggregates.map(({ as }) => as)]
            : options.pivot !== undefined
                ? [
                    options.pivot.row,
                    ...new Set(rows
                        .map((row) => row[options.pivot.column])
                        .filter((value) => value !== null && value !== undefined)
                        .map(String)),
                ]
                : sourceColumns;
        let output = rows.map((row, sourceIndex) => ({
            ...row,
            __sourceIndex: sourceIndex,
        }));
        output = output.filter((row) => (options.filters ?? []).every((filter) => matchesFilter(row, filter)));
        if (options.group !== undefined)
            output = aggregateRows(output, options.group);
        if (options.pivot !== undefined)
            output = pivotRows(output, options.pivot);
        output = [...output].sort((left, right) => {
            for (const sort of options.sort ?? []) {
                const comparison = compareValues(left[sort.field], right[sort.field]);
                if (comparison !== 0)
                    return sort.direction === 'descending' ? -comparison : comparison;
            }
            return compareValues(left.__sourceIndex, right.__sourceIndex);
        });
        const totalRows = output.length;
        const offset = clamp$5(Math.floor(options.window?.offset ?? 0), 0, totalRows);
        const limit = clamp$5(Math.floor(options.window?.limit ?? totalRows), 0, 100_000);
        const columns = [
            ...new Set([
                ...schemaColumns,
                ...output.flatMap((row) => Object.keys(row).filter((field) => !field.startsWith('__'))),
            ]),
        ];
        const frozenRows = clamp$5(Math.floor(options.frozenRows ?? 0), 0, totalRows);
        const frozenColumns = clamp$5(Math.floor(options.frozenColumns ?? 0), 0, columns.length);
        const visibleIndices = [
            ...new Set([
                ...Array.from({ length: frozenRows }, (_, index) => index),
                ...Array.from({ length: Math.max(0, Math.min(totalRows, offset + limit) - offset) }, (_, index) => offset + index),
            ]),
        ];
        const rowEntries = visibleIndices.map((index) => ({
            row: output[index],
            index,
            frozen: index < frozenRows,
        }));
        const columnOffset = clamp$5(Math.floor(options.columnWindow?.offset ?? 0), 0, columns.length);
        const columnLimit = clamp$5(Math.floor(options.columnWindow?.limit ?? columns.length), 0, 10_000);
        const visibleColumnIndices = [
            ...new Set([
                ...Array.from({ length: frozenColumns }, (_, index) => index),
                ...Array.from({
                    length: Math.max(0, Math.min(columns.length, columnOffset + columnLimit) - columnOffset),
                }, (_, index) => columnOffset + index),
            ]),
        ];
        const columnEntries = visibleColumnIndices.map((index) => ({
            field: columns[index],
            index,
            frozen: index < frozenColumns,
        }));
        return {
            rows: rowEntries.map(({ row }) => row),
            rowEntries,
            columns,
            columnEntries,
            totalRows,
            totalColumns: columns.length,
            window: { offset, limit, end: Math.min(totalRows, offset + limit) },
            columnWindow: {
                offset: columnOffset,
                limit: columnLimit,
                end: Math.min(columns.length, columnOffset + columnLimit),
            },
            frozen: {
                rows: frozenRows,
                columns: frozenColumns,
            },
        };
    }
    /** WAI-ARIA-grid style bounded keyboard cell navigation. */
    function moveTableCell(position, key, bounds) {
        let row = clamp$5(Math.floor(position.row), 0, Math.max(0, bounds.rows - 1));
        let column = clamp$5(Math.floor(position.column), 0, Math.max(0, bounds.columns - 1));
        if (key === 'ArrowUp')
            row -= 1;
        if (key === 'ArrowDown')
            row += 1;
        if (key === 'ArrowLeft')
            column -= 1;
        if (key === 'ArrowRight')
            column += 1;
        if (key === 'Home')
            column = 0;
        if (key === 'End')
            column = bounds.columns - 1;
        if (key === 'PageUp')
            row -= Math.max(1, Math.floor(bounds.pageSize ?? 10));
        if (key === 'PageDown')
            row += Math.max(1, Math.floor(bounds.pageSize ?? 10));
        return {
            row: clamp$5(row, 0, Math.max(0, bounds.rows - 1)),
            column: clamp$5(column, 0, Math.max(0, bounds.columns - 1)),
        };
    }
    /** Resolves zero/direction/wrap, radial transforms, angular bins and stacked/normalized radial bars. */
    function layoutPolar(data, options = {}) {
        const wrap = options.wrap ?? [0, 360];
        if (wrap[1] <= wrap[0])
            throw new GraflumeError('INVALID_SPEC', 'Polar wrap must be ascending.');
        const bins = clamp$5(Math.floor(options.bins ?? Math.max(1, data.length)), 1, 720);
        const binWidth = (wrap[1] - wrap[0]) / bins;
        const normalizeAngle = (angle) => {
            const span = wrap[1] - wrap[0];
            const wrapped = ((((angle - wrap[0]) % span) + span) % span) + wrap[0];
            const directed = (options.direction ?? 'clockwise') === 'clockwise' ? wrapped : wrap[1] - (wrapped - wrap[0]);
            return directed + (options.zero ?? 0);
        };
        const normalized = data.map((datum, index) => ({
            id: datum.id?.trim() || `polar-${index}`,
            series: datum.series?.trim() || 'series',
            angle: normalizeAngle(finite$7(datum.angle, `$.data[${index}].angle`)),
            value: finite$7(datum.value, `$.data[${index}].value`),
        }));
        if (options.radiusScale === 'log' && normalized.some(({ value }) => value <= 0)) {
            throw new GraflumeError('INVALID_DATA', 'Polar log radius requires positive values.');
        }
        const grouped = new Map();
        normalized.forEach((datum) => {
            const bin = clamp$5(Math.floor(((((datum.angle - (options.zero ?? 0) - wrap[0]) % (wrap[1] - wrap[0])) +
                (wrap[1] - wrap[0])) %
                (wrap[1] - wrap[0])) /
                binWidth), 0, bins - 1);
            const values = grouped.get(bin) ?? [];
            values.push(datum);
            grouped.set(bin, values);
        });
        const stackMode = options.stack ?? 'none';
        const maximum = stackMode === 'normalize'
            ? 1
            : stackMode === 'stack'
                ? Math.max(...[...grouped.values()].map((values) => values.reduce((sum, { value }) => sum + Math.max(0, value), 0)), Number.EPSILON)
                : Math.max(...normalized.map(({ value }) => value), Number.EPSILON);
        const radius = (value) => {
            if (options.radiusScale === 'log') {
                if (value < 0)
                    throw new GraflumeError('INVALID_DATA', 'Polar log radius requires positive values.');
                if (value === 0)
                    return 0;
                return Math.log1p(value) / Math.log1p(maximum);
            }
            if (options.radiusScale === 'sqrt')
                return Math.sqrt(Math.max(0, value) / maximum);
            return Math.max(0, value) / maximum;
        };
        const segments = [...grouped.entries()].flatMap(([bin, values]) => {
            const total = values.reduce((sum, { value }) => sum + Math.max(0, value), 0);
            let cursor = 0;
            return values.map((datum) => {
                const amount = stackMode === 'normalize'
                    ? total === 0
                        ? 0
                        : Math.max(0, datum.value) / total
                    : Math.max(0, datum.value);
                const inner = stackMode === 'none' ? 0 : cursor;
                const outer = inner + amount;
                cursor = outer;
                return {
                    ...datum,
                    bin,
                    startAngle: (options.zero ?? 0) + wrap[0] + bin * binWidth,
                    endAngle: (options.zero ?? 0) + wrap[0] + (bin + 1) * binWidth,
                    innerValue: inner,
                    outerValue: outer,
                    innerRadius: radius(inner),
                    outerRadius: radius(outer),
                    proportion: total === 0 ? 0 : Math.max(0, datum.value) / total,
                };
            });
        });
        return {
            wrap,
            zero: options.zero ?? 0,
            direction: options.direction ?? 'clockwise',
            bins,
            binWidth,
            segments,
        };
    }
    /** Computes weighted counts, deterministic sort/rank and stable rank-change metadata for interactive bars. */
    function rankBars(data, options = {}) {
        const grouped = new Map();
        data.forEach((datum, index) => {
            const category = datum.category?.trim() || label(datum.id, `$.data[${index}].id`);
            const contribution = options.aggregate === 'count'
                ? 1
                : options.aggregate === 'weighted-count'
                    ? finite$7(datum.weight ?? 1, `$.data[${index}].weight`)
                    : finite$7(datum.value ?? 0, `$.data[${index}].value`);
            if (contribution < 0 && options.aggregate === 'weighted-count')
                throw new GraflumeError('INVALID_DATA', 'Bar weights must be non-negative.');
            const bucket = grouped.get(category) ?? { value: 0, sourceIds: [] };
            bucket.value += contribution;
            bucket.sourceIds.push(datum.id);
            grouped.set(category, bucket);
        });
        const direction = options.direction ?? 'descending';
        return [...grouped]
            .sort((a, b) => direction === 'descending'
            ? b[1].value - a[1].value || a[0].localeCompare(b[0])
            : a[1].value - b[1].value || a[0].localeCompare(b[0]))
            .map(([id, value], index) => {
            const rank = index + 1;
            const previous = options.previousRanks?.[id] ?? null;
            return {
                id,
                rank,
                previousRank: previous,
                rankChange: previous === null ? null : previous - rank,
                ...value,
            };
        });
    }

    function optionNumber$1(options, name, fallback) {
        const value = options[name];
        return typeof value === 'number' && Number.isFinite(value) ? value : fallback;
    }
    function arcPoints$1(cx, cy, outerRadius, startAngle, endAngle, innerRadius) {
        const span = Math.abs(endAngle - startAngle);
        const steps = Math.max(8, Math.ceil((span / (Math.PI * 2)) * 72));
        const outer = Array.from({ length: steps + 1 }, (_, index) => {
            const angle = startAngle + ((endAngle - startAngle) * index) / steps;
            return { x: cx + Math.cos(angle) * outerRadius, y: cy + Math.sin(angle) * outerRadius };
        });
        if (innerRadius <= 0)
            return [{ x: cx, y: cy }, ...outer];
        const inner = Array.from({ length: steps + 1 }, (_, index) => {
            const angle = endAngle - ((endAngle - startAngle) * index) / steps;
            return { x: cx + Math.cos(angle) * innerRadius, y: cy + Math.sin(angle) * innerRadius };
        });
        return [...outer, ...inner];
    }
    function labelNode(id, x, y, text, context, fontSize = 12, options = {}) {
        return {
            type: 'text',
            ...nodeBase(id, { zIndex: context.layer.zIndex + 1 }),
            x,
            y,
            text,
            fill: options.fill ?? context.theme.colors.text,
            fontFamily: context.theme.typography.fontFamily,
            fontSize,
            fontWeight: options.weight ?? 600,
            align: options.align ?? 'center',
            baseline: 'middle',
            rotation: 0,
        };
    }
    function compileNumberGauge(context, showDelta) {
        const { table, layer, plot, theme, performance } = context;
        const referenceField = layer.mark.fields.reference ?? 'reference';
        const nodesPerRow = showDelta ? 4 : 3;
        const rowBudget = Math.max(1, Math.floor(performance.maxBarMarks / nodesPerRow));
        const rowIndices = exactStrideSampleIndices(table.length, rowBudget);
        const count = Math.max(1, rowIndices.length);
        const slotWidth = plot.width / count;
        const nodes = [];
        for (let outputIndex = 0; outputIndex < rowIndices.length; outputIndex += 1) {
            const rowIndex = rowIndices[outputIndex];
            if (rowIndex === undefined)
                continue;
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            const rawLabel = table.value(rowIndex, layer.x.field);
            if (value === null || rawLabel === null || rawLabel === undefined)
                continue;
            const reference = table.has(referenceField)
                ? numericDataValue(table.value(rowIndex, referenceField))
                : null;
            const delta = reference === null ? null : value - reference;
            const x = plot.x + slotWidth * outputIndex + 4;
            const y = plot.y + 6;
            const width = Math.max(1, slotWidth - 8);
            const height = Math.max(1, plot.height - 12);
            const fill = layer.mark.fill ?? theme.colors.surface;
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:gauge-${showDelta ? 'delta' : 'number'}:${rowIndex}`, {
                    zIndex: layer.zIndex,
                    opacity: layer.mark.opacity,
                    interactive: performance.enableHitTesting,
                    datum: {
                        layerId: layer.id,
                        rowIndex,
                        datum: table.row(rowIndex),
                        tooltip: {
                            label: String(rawLabel),
                            value,
                            ...(reference === null ? {} : { reference }),
                            ...(delta === null ? {} : { delta }),
                        },
                    },
                }),
                x,
                y,
                width,
                height,
                fill,
                stroke: mixColor$1(theme.colors.grid, theme.colors.axis, 0.22),
                lineWidth: 1,
                cornerRadius: layer.mark.cornerRadius ?? 9,
            });
            nodes.push(labelNode(`${layer.id}:gauge-${showDelta ? 'delta' : 'number'}-label:${rowIndex}`, x + width / 2, y + height * 0.24, String(rawLabel), context, 11, { fill: theme.colors.mutedText, weight: 650 }), labelNode(`${layer.id}:gauge-${showDelta ? 'delta-current' : 'number-value'}:${rowIndex}`, x + width / 2, y + height * (showDelta ? 0.5 : 0.58), String(value), context, Math.max(18, Math.min(34, width * 0.2)), { fill: theme.colors.text, weight: 780 }));
            if (showDelta) {
                const positive = (delta ?? 0) >= 0;
                nodes.push(labelNode(`${layer.id}:gauge-delta-value:${rowIndex}`, x + width / 2, y + height * 0.75, delta === null ? '—' : `${positive ? '+' : ''}${delta}`, context, 13, {
                    fill: delta === null ? theme.colors.mutedText : positive ? '#15803d' : '#b91c1c',
                    weight: 750,
                }));
            }
        }
        return nodes;
    }
    function compileBulletGauge(context) {
        const { table, layer, plot, theme, performance } = context;
        const minimum = optionNumber$1(layer.mark.options, 'min', 0);
        const maximum = optionNumber$1(layer.mark.options, 'max', 100);
        const span = maximum - minimum || 1;
        const targetField = layer.mark.fields.target ?? 'target';
        const rowBudget = Math.max(1, Math.floor(performance.maxBarMarks / 4));
        const rowIndices = exactStrideSampleIndices(table.length, rowBudget);
        const count = Math.max(1, rowIndices.length);
        const slotHeight = plot.height / count;
        const nodes = [];
        for (let outputIndex = 0; outputIndex < rowIndices.length; outputIndex += 1) {
            const rowIndex = rowIndices[outputIndex];
            if (rowIndex === undefined)
                continue;
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            const rawLabel = table.value(rowIndex, layer.x.field);
            if (value === null || rawLabel === null || rawLabel === undefined)
                continue;
            const target = table.has(targetField)
                ? numericDataValue(table.value(rowIndex, targetField))
                : null;
            const ratio = Math.max(0, Math.min(1, (value - minimum) / span));
            const targetRatio = target === null ? null : Math.max(0, Math.min(1, (target - minimum) / span));
            const trackX = plot.x + Math.min(110, plot.width * 0.28);
            const trackWidth = Math.max(24, plot.x + plot.width - trackX - 12);
            const cy = plot.y + slotHeight * (outputIndex + 0.5);
            const trackHeight = Math.max(12, Math.min(28, slotHeight * 0.48));
            const fill = layer.mark.fill ?? categoricalColor(theme, rowIndex, table.length);
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:gauge-bullet-track:${rowIndex}`, { zIndex: layer.zIndex }),
                x: trackX,
                y: cy - trackHeight / 2,
                width: trackWidth,
                height: trackHeight,
                fill: mixColor$1(theme.colors.grid, theme.colors.surface, 0.32),
                stroke: 'transparent',
                lineWidth: 0,
                cornerRadius: trackHeight / 2,
            });
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:gauge-bullet-value:${rowIndex}`, {
                    zIndex: layer.zIndex + 0.2,
                    opacity: layer.mark.opacity,
                    interactive: performance.enableHitTesting,
                    datum: {
                        layerId: layer.id,
                        rowIndex,
                        datum: table.row(rowIndex),
                        tooltip: {
                            label: String(rawLabel),
                            value,
                            ...(target === null ? {} : { target }),
                        },
                    },
                }),
                x: trackX,
                y: cy - trackHeight * 0.28,
                width: Math.max(1, trackWidth * ratio),
                height: trackHeight * 0.56,
                fill,
                stroke: 'transparent',
                lineWidth: 0,
                cornerRadius: trackHeight * 0.28,
            });
            if (targetRatio !== null) {
                const targetX = trackX + trackWidth * targetRatio;
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:gauge-bullet-target:${rowIndex}`, {
                        zIndex: layer.zIndex + 0.5,
                    }),
                    x1: targetX,
                    y1: cy - trackHeight * 0.7,
                    x2: targetX,
                    y2: cy + trackHeight * 0.7,
                    stroke: theme.colors.text,
                    lineWidth: 2,
                    lineCap: 'round',
                });
            }
            nodes.push(labelNode(`${layer.id}:gauge-bullet-label:${rowIndex}`, trackX - 8, cy, String(rawLabel), context, 10, { align: 'right', fill: theme.colors.mutedText, weight: 650 }));
        }
        return nodes;
    }
    const compileGaugeMark = (context) => {
        const mode = context.layer.mark.options.mode;
        if (mode === 'number')
            return compileNumberGauge(context, false);
        if (mode === 'delta')
            return compileNumberGauge(context, true);
        if (mode === 'bullet')
            return compileBulletGauge(context);
        const { table, layer, plot, theme, performance } = context;
        const minimum = optionNumber$1(layer.mark.options, 'min', 0);
        const maximum = optionNumber$1(layer.mark.options, 'max', 100);
        const span = maximum - minimum || 1;
        const count = Math.max(1, table.length);
        const slotWidth = plot.width / count;
        const radius = Math.max(12, Math.min(slotWidth * 0.42, plot.height * 0.36));
        const nodes = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            const label = table.value(rowIndex, layer.x.field);
            if (value === null || label === null || label === undefined)
                continue;
            const ratio = Math.max(0, Math.min(1, (value - minimum) / span));
            const cx = plot.x + slotWidth * (rowIndex + 0.5);
            const cy = plot.y + plot.height * 0.62;
            const inner = radius * 0.7;
            const fill = layer.mark.fill ?? categoricalColor(theme, rowIndex, table.length);
            nodes.push({
                type: 'path',
                ...nodeBase(`${layer.id}:gauge-background:${rowIndex}`, { zIndex: layer.zIndex }),
                points: arcPoints$1(cx, cy, radius, Math.PI, Math.PI * 2, inner),
                closed: true,
                fill: mixColor$1(theme.colors.grid, theme.colors.surface, 0.3),
                lineWidth: 0,
            });
            nodes.push({
                type: 'path',
                ...nodeBase(`${layer.id}:gauge-value:${rowIndex}`, {
                    zIndex: layer.zIndex + 0.1,
                    opacity: layer.mark.opacity,
                    interactive: performance.enableHitTesting,
                    datum: { layerId: layer.id, rowIndex, datum: table.row(rowIndex) },
                }),
                points: arcPoints$1(cx, cy, radius, Math.PI, Math.PI + Math.PI * ratio, inner),
                closed: true,
                fill,
                lineWidth: 0,
            });
            for (let tickIndex = 0; tickIndex <= 4; tickIndex += 1) {
                const tickAngle = Math.PI + (Math.PI * tickIndex) / 4;
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:gauge-tick:${rowIndex}:${tickIndex}`, {
                        zIndex: layer.zIndex + 0.35,
                        opacity: 0.72,
                    }),
                    x1: cx + Math.cos(tickAngle) * radius * 0.76,
                    y1: cy + Math.sin(tickAngle) * radius * 0.76,
                    x2: cx + Math.cos(tickAngle) * radius * 0.84,
                    y2: cy + Math.sin(tickAngle) * radius * 0.84,
                    stroke: theme.colors.background,
                    lineWidth: 1.25,
                    lineCap: 'round',
                });
            }
            const needleAngle = Math.PI + Math.PI * ratio;
            const needle = {
                type: 'line',
                ...nodeBase(`${layer.id}:gauge-needle:${rowIndex}`, { zIndex: layer.zIndex + 0.5 }),
                x1: cx,
                y1: cy,
                x2: cx + Math.cos(needleAngle) * radius * 0.62,
                y2: cy + Math.sin(needleAngle) * radius * 0.62,
                stroke: theme.colors.text,
                lineWidth: 2,
                lineCap: 'round',
            };
            nodes.push(needle);
            const hub = {
                type: 'circle',
                ...nodeBase(`${layer.id}:gauge-hub:${rowIndex}`, { zIndex: layer.zIndex + 0.6 }),
                cx,
                cy,
                radius: 4,
                fill: theme.colors.text,
                stroke: theme.colors.background,
                lineWidth: 1.5,
            };
            nodes.push(hub);
            nodes.push(labelNode(`${layer.id}:gauge-value-label:${rowIndex}`, cx, cy - 15, String(value), context, 17, { weight: 750 }));
            nodes.push(labelNode(`${layer.id}:gauge-label:${rowIndex}`, cx, cy + 23, String(label), context, 11, {
                fill: theme.colors.mutedText,
                weight: 650,
            }));
        }
        return nodes;
    };

    function stringOption$2(context, name) {
        const value = context.layer.mark.options[name];
        return typeof value === 'string' ? value : undefined;
    }
    function numberOption$2(context, name) {
        const value = context.layer.mark.options[name];
        return typeof value === 'number' && Number.isFinite(value) ? value : undefined;
    }
    function booleanOption$2(context, name) {
        const value = context.layer.mark.options[name];
        return typeof value === 'boolean' ? value : undefined;
    }
    function numberArrayOption$1(context, name) {
        const value = context.layer.mark.options[name];
        return Array.isArray(value) &&
            value.every((entry) => typeof entry === 'number' && Number.isFinite(entry))
            ? [...value]
            : undefined;
    }
    function stringArrayOption$1(context, name) {
        const value = context.layer.mark.options[name];
        return Array.isArray(value) && value.every((entry) => typeof entry === 'string')
            ? [...value]
            : undefined;
    }
    function textNode$4(context, id, x, y, text, options = {}) {
        return {
            type: 'text',
            ...nodeBase(id, { zIndex: options.zIndex ?? context.layer.zIndex + 3 }),
            x,
            y,
            text,
            fill: options.fill ?? context.theme.colors.text,
            fontFamily: context.theme.typography.fontFamily,
            fontSize: options.size ?? context.theme.typography.fontSize,
            fontWeight: options.weight ?? 600,
            align: options.align ?? 'center',
            baseline: options.baseline ?? 'middle',
            rotation: options.rotation ?? 0,
        };
    }
    function arcPoints(cx, cy, outer, start, end, inner = 0) {
        const count = Math.max(6, Math.ceil((Math.abs(end - start) / (Math.PI * 2)) * 96));
        const outside = Array.from({ length: count + 1 }, (_, index) => {
            const angle = start + ((end - start) * index) / count;
            return { x: cx + Math.cos(angle) * outer, y: cy + Math.sin(angle) * outer };
        });
        if (inner <= 0)
            return [{ x: cx, y: cy }, ...outside];
        const inside = Array.from({ length: count + 1 }, (_, index) => {
            const angle = end - ((end - start) * index) / count;
            return { x: cx + Math.cos(angle) * inner, y: cy + Math.sin(angle) * inner };
        });
        return [...outside, ...inside];
    }
    /** Pie compiler with dense outside-label solver and roving slice semantics. */
    const compileAdvancedPieMark = (context) => {
        const idField = context.layer.mark.fields.id ?? context.layer.x.field;
        const labelField = context.layer.mark.fields.label ?? context.layer.x.field;
        const data = Array.from({ length: context.table.length }, (_, rowIndex) => {
            const rawId = context.table.value(rowIndex, idField);
            const rawLabel = context.table.value(rowIndex, labelField);
            return {
                rowIndex,
                id: rawId === null || rawId === undefined ? `slice-${rowIndex}` : String(rawId),
                label: rawLabel === null || rawLabel === undefined ? `slice-${rowIndex}` : String(rawLabel),
                value: numericDataValue(context.table.value(rowIndex, context.layer.y.field)) ?? 0,
            };
        });
        const negative = stringOption$2(context, 'negative');
        const zero = stringOption$2(context, 'zero');
        const sort = stringOption$2(context, 'sort');
        const startAngle = numberOption$2(context, 'startAngle') ?? context.theme.mark.pieStartAngle ?? -Math.PI / 2;
        const endAngle = numberOption$2(context, 'endAngle') ?? startAngle + Math.PI * 2;
        const direction = stringOption$2(context, 'direction') ?? context.theme.mark.pieDirection ?? 'clockwise';
        const runtimeFocusedSlice = stringOption$2(context, 'runtimeFocusedSlice');
        const slices = layoutPie(data, {
            negative: negative === 'absolute' || negative === 'hide' ? negative : 'reject',
            zero: zero === 'minimum' ? zero : 'hide',
            minimumAngle: numberOption$2(context, 'minimumAngle') ?? 0,
            sort: sort === 'ascending' || sort === 'descending' ? sort : 'input',
            padAngle: numberOption$2(context, 'padAngle') ?? 0,
            startAngle,
            endAngle,
        });
        if (slices.length === 0)
            return [];
        const { plot, layer, theme } = context;
        const cx = plot.x + plot.width / 2;
        const cy = plot.y + plot.height / 2;
        const radius = Math.max(8, Math.min(plot.width, plot.height) * 0.34);
        const inner = radius * Math.max(0, Math.min(0.9, numberOption$2(context, 'innerRadius') ?? 0));
        const nodes = [];
        const labels = [];
        slices.forEach((slice, index) => {
            const piePalette = theme.mark.piePalette;
            const color = layer.mark.fill ??
                (piePalette === undefined || piePalette.length === 0
                    ? categoricalColor(theme, index, slices.length)
                    : (piePalette[index % piePalette.length] ?? categoricalColor(theme, index, slices.length)));
            const renderAngle = (angle) => direction === 'counterclockwise' ? startAngle - (angle - startAngle) : angle;
            const renderedStart = renderAngle(slice.startAngle);
            const renderedEnd = renderAngle(slice.endAngle);
            const middle = (renderedStart + renderedEnd) / 2;
            const rowIndex = slice.sourceRows[0] ?? index;
            const focused = runtimeFocusedSlice === slice.id;
            nodes.push({
                type: 'path',
                ...nodeBase(`${layer.id}:slice:${slice.id}`, {
                    zIndex: layer.zIndex,
                    opacity: layer.mark.opacity,
                    interactive: context.performance.enableHitTesting,
                    datum: {
                        layerId: layer.id,
                        rowIndex,
                        datum: {
                            ...context.table.row(rowIndex),
                            id: slice.id,
                            label: slice.label,
                            sourceRows: [...slice.sourceRows],
                            rawValue: slice.rawValue,
                            value: slice.value,
                            proportion: slice.proportion,
                            minimumApplied: slice.minimumApplied,
                            tabIndex: runtimeFocusedSlice === undefined ? slice.tabIndex : focused ? 0 : -1,
                        },
                        tooltip: {
                            label: slice.label,
                            sourceRows: slice.sourceRows.join(', '),
                            sourceRowCount: slice.sourceRows.length,
                            sourceRowIndices: [...slice.sourceRows],
                            rawValue: slice.rawValue,
                            value: slice.value,
                            proportion: slice.proportion,
                            accessibleLabel: slice.accessibleLabel,
                            minimumApplied: slice.minimumApplied,
                            tabIndex: runtimeFocusedSlice === undefined ? slice.tabIndex : focused ? 0 : -1,
                        },
                        familyInteraction: {
                            kind: 'pie-slice',
                            id: slice.id,
                            index,
                            count: slices.length,
                        },
                    },
                }),
                points: arcPoints(cx, cy, radius, renderedStart, renderedEnd, inner),
                closed: true,
                fill: color,
                stroke: focused
                    ? theme.colors.focus
                    : (layer.mark.stroke ?? theme.mark.pieStroke ?? theme.colors.background),
                lineWidth: focused
                    ? Math.max(3, layer.mark.lineWidth ?? theme.mark.pieStrokeWidth ?? 1.5)
                    : (layer.mark.lineWidth ?? theme.mark.pieStrokeWidth ?? 1.5),
                lineJoin: 'round',
            });
            const side = Math.cos(middle) >= 0 ? 1 : -1;
            labels.push({
                id: slice.id,
                label: slice.accessibleLabel,
                side,
                desiredY: cy + Math.sin(middle) * radius * 1.1,
                edge: { x: cx + Math.cos(middle) * radius * 0.96, y: cy + Math.sin(middle) * radius * 0.96 },
                elbow: { x: cx + Math.cos(middle) * radius * 1.08, y: cy + Math.sin(middle) * radius * 1.08 },
                color,
            });
        });
        for (const side of [-1, 1]) {
            const group = labels
                .filter((label) => label.side === side)
                .sort((a, b) => a.desiredY - b.desiredY);
            const availableHeight = Math.max(0, plot.height - 12);
            const readableLimit = Math.max(1, Math.floor(availableHeight / 7) + 1);
            const visible = group.length <= readableLimit
                ? group
                : Array.from({ length: readableLimit }, (_, index) => group[Math.round((index * (group.length - 1)) / Math.max(1, readableLimit - 1))]);
            const preferredSpacing = Math.max(11, Math.min(18, plot.height / Math.max(3, slices.length)));
            const spacing = visible.length <= 1
                ? 0
                : Math.min(preferredSpacing, availableHeight / Math.max(1, visible.length - 1));
            const fontSize = Math.max(6, Math.min(10, spacing - 0.5));
            let cursor = plot.y + 6;
            visible.forEach((label, index) => {
                const remaining = visible.length - index - 1;
                const maximumY = plot.y + plot.height - 6 - remaining * spacing;
                const y = Math.max(cursor, Math.min(maximumY, label.desiredY));
                cursor = y + spacing;
                const x = side > 0 ? plot.x + plot.width - 4 : plot.x + 4;
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:slice-leader:${label.id}`, { zIndex: layer.zIndex + 1 }),
                    points: [label.edge, label.elbow, { x: x - side * 4, y }],
                    closed: false,
                    stroke: label.color,
                    lineWidth: 1,
                    lineCap: 'round',
                    lineJoin: 'round',
                });
                nodes.push(textNode$4(context, `${layer.id}:slice-label:${label.id}`, x, y, label.label, {
                    align: side > 0 ? 'right' : 'left',
                    size: fontSize,
                }));
            });
        }
        if (inner > radius * 0.34) {
            const total = slices.reduce((sum, slice) => sum + slice.value, 0);
            nodes.push(textNode$4(context, `${layer.id}:center-label`, cx, cy - 9, stringOption$2(context, 'centerLabel') ?? 'Total', { size: 10, fill: theme.colors.mutedText, weight: 600 }), textNode$4(context, `${layer.id}:center-value`, cx, cy + 10, String(total), {
                size: 18,
                fill: theme.colors.text,
                weight: 750,
            }));
        }
        return nodes;
    };
    function gaugeBands(context) {
        const value = context.layer.mark.options.bands;
        if (!Array.isArray(value))
            return [];
        return value.flatMap((entry) => {
            if (entry === null || typeof entry !== 'object' || Array.isArray(entry))
                return [];
            const from = Reflect.get(entry, 'from');
            const to = Reflect.get(entry, 'to');
            if (typeof from !== 'number' || typeof to !== 'number')
                return [];
            const color = Reflect.get(entry, 'color');
            const label = Reflect.get(entry, 'label');
            return [
                {
                    from,
                    to,
                    ...(typeof color === 'string' ? { color } : {}),
                    ...(typeof label === 'string' ? { label } : {}),
                },
            ];
        });
    }
    /** Radial/linear gauge compiler with threshold bands, targets, ticks, and exact numeric summary. */
    const compileAdvancedGaugeMark = (context) => {
        const legacyMode = stringOption$2(context, 'mode');
        if (legacyMode === 'number' || legacyMode === 'delta' || legacyMode === 'bullet') {
            return compileGaugeMark(context);
        }
        const advancedOptions = ['type', 'minimum', 'maximum', 'bands', 'targets', 'ticks', 'tickCount'];
        if (!advancedOptions.some((name) => Object.hasOwn(context.layer.mark.options, name))) {
            return compileGaugeMark(context);
        }
        const value = Array.from({ length: context.table.length }, (_, rowIndex) => numericDataValue(context.table.value(rowIndex, context.layer.y.field))).find((entry) => entry !== null);
        if (value === undefined)
            return [];
        const type = stringOption$2(context, 'type') === 'linear' ? 'linear' : 'radial';
        const ticks = numberArrayOption$1(context, 'ticks') ?? numberOption$2(context, 'tickCount') ?? 5;
        const model = gaugeModel(value, {
            type,
            minimum: numberOption$2(context, 'minimum') ?? 0,
            maximum: numberOption$2(context, 'maximum') ?? 100,
            targets: numberArrayOption$1(context, 'targets') ?? [],
            bands: gaugeBands(context),
            ticks,
        });
        const { plot, layer, theme } = context;
        const nodes = [];
        if (model.type === 'linear') {
            const x = plot.x + plot.width * 0.1;
            const width = plot.width * 0.8;
            const y = plot.y + plot.height * 0.46;
            const height = Math.max(12, plot.height * 0.12);
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:gauge-track`, { zIndex: layer.zIndex }),
                x,
                y,
                width,
                height,
                fill: theme.colors.surface,
                stroke: theme.colors.axis,
                lineWidth: 1,
                cornerRadius: height / 2,
            });
            model.bands.forEach((band, index) => nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:gauge-band:${index}`, { zIndex: layer.zIndex + 1 }),
                x: x + band.start * width,
                y,
                width: Math.max(0, (band.end - band.start) * width),
                height,
                fill: band.color ?? categoricalColor(theme, index, model.bands.length),
                lineWidth: 0,
                cornerRadius: 0,
            }));
            const currentX = x + model.position * width;
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:gauge-value`, {
                    zIndex: layer.zIndex + 3,
                    interactive: context.performance.enableHitTesting,
                    datum: {
                        layerId: layer.id,
                        rowIndex: 0,
                        datum: { value: model.value, minimum: model.minimum, maximum: model.maximum },
                        tooltip: {
                            value: model.value,
                            minimum: model.minimum,
                            maximum: model.maximum,
                            summary: model.accessibleSummary,
                        },
                    },
                }),
                x1: currentX,
                x2: currentX,
                y1: y - 8,
                y2: y + height + 8,
                stroke: layer.mark.stroke ?? theme.colors.focus,
                lineWidth: layer.mark.lineWidth ?? 3,
            });
            model.targets.forEach((target, index) => {
                const targetX = x + target.position * width;
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:gauge-target:${index}`, { zIndex: layer.zIndex + 4 }),
                    x1: targetX,
                    x2: targetX,
                    y1: y - 5,
                    y2: y + height + 5,
                    stroke: theme.colors.text,
                    lineWidth: 1.5,
                    dash: [3, 2],
                });
            });
            model.ticks.forEach((tick, index) => {
                const tickX = x + tick.position * width;
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:gauge-tick:${index}`, { zIndex: layer.zIndex + 3 }),
                    x1: tickX,
                    x2: tickX,
                    y1: y + height,
                    y2: y + height + 5,
                    stroke: theme.colors.axis,
                    lineWidth: 1,
                });
                nodes.push(textNode$4(context, `${layer.id}:gauge-tick-label:${index}`, tickX, y + height + 13, tick.label, { size: 9 }));
            });
        }
        else {
            const cx = plot.x + plot.width / 2;
            const cy = plot.y + plot.height * 0.62;
            const radius = Math.min(plot.width * 0.34, plot.height * 0.48);
            const start = Math.PI * 0.8;
            const span = Math.PI * 1.4;
            nodes.push({
                type: 'path',
                ...nodeBase(`${layer.id}:gauge-track`, { zIndex: layer.zIndex }),
                points: arcPoints(cx, cy, radius, start, start + span, radius * 0.72),
                closed: true,
                fill: theme.colors.surface,
                stroke: theme.colors.axis,
                lineWidth: 1,
            });
            model.bands.forEach((band, index) => nodes.push({
                type: 'path',
                ...nodeBase(`${layer.id}:gauge-band:${index}`, { zIndex: layer.zIndex + 1 }),
                points: arcPoints(cx, cy, radius, start + band.start * span, start + band.end * span, radius * 0.72),
                closed: true,
                fill: band.color ?? categoricalColor(theme, index, model.bands.length),
                lineWidth: 0,
            }));
            const angle = start + model.position * span;
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:gauge-value`, {
                    zIndex: layer.zIndex + 3,
                    interactive: context.performance.enableHitTesting,
                    datum: {
                        layerId: layer.id,
                        rowIndex: 0,
                        datum: { value: model.value, minimum: model.minimum, maximum: model.maximum },
                        tooltip: {
                            value: model.value,
                            minimum: model.minimum,
                            maximum: model.maximum,
                            summary: model.accessibleSummary,
                        },
                    },
                }),
                x1: cx,
                y1: cy,
                x2: cx + Math.cos(angle) * radius * 0.82,
                y2: cy + Math.sin(angle) * radius * 0.82,
                stroke: layer.mark.stroke ?? theme.colors.focus,
                lineWidth: layer.mark.lineWidth ?? 3,
                lineCap: 'round',
            });
            model.targets.forEach((target, index) => {
                const targetAngle = start + target.position * span;
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:gauge-target:${index}`, { zIndex: layer.zIndex + 4 }),
                    x1: cx + Math.cos(targetAngle) * radius * 0.69,
                    y1: cy + Math.sin(targetAngle) * radius * 0.69,
                    x2: cx + Math.cos(targetAngle) * radius * 1.04,
                    y2: cy + Math.sin(targetAngle) * radius * 1.04,
                    stroke: theme.colors.text,
                    lineWidth: 1.5,
                });
            });
            model.ticks.forEach((tick, index) => {
                const tickAngle = start + tick.position * span;
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:gauge-tick:${index}`, { zIndex: layer.zIndex + 3 }),
                    x1: cx + Math.cos(tickAngle) * radius * 0.92,
                    y1: cy + Math.sin(tickAngle) * radius * 0.92,
                    x2: cx + Math.cos(tickAngle) * radius * 1.02,
                    y2: cy + Math.sin(tickAngle) * radius * 1.02,
                    stroke: theme.colors.axis,
                    lineWidth: 1,
                });
                nodes.push(textNode$4(context, `${layer.id}:gauge-tick-label:${index}`, cx + Math.cos(tickAngle) * radius * 1.13, cy + Math.sin(tickAngle) * radius * 1.13, tick.label, { size: 9 }));
            });
        }
        nodes.push(textNode$4(context, `${layer.id}:gauge-summary`, plot.x + plot.width / 2, plot.y + plot.height - 15, model.accessibleSummary, { size: 10, weight: 600 }));
        return nodes;
    };
    function dataArray(value) {
        return Array.isArray(value) ? value.map(String) : [];
    }
    /** Timeline compiler with group lane packing, milestones, dependency routes, clipping, duration, and navigator. */
    const compileAdvancedTimelineMark = (context) => {
        const idField = context.layer.mark.fields.id ?? 'id';
        const startField = context.layer.mark.fields.start ?? context.layer.x.field;
        const endField = context.layer.mark.fields.end ?? context.layer.y.field;
        const groupField = context.layer.mark.fields.group ?? 'group';
        const labelField = context.layer.mark.fields.label ?? 'label';
        const milestoneField = context.layer.mark.fields.milestone ?? 'milestone';
        const dependencyField = context.layer.mark.fields.dependencies ?? 'dependencies';
        const data = Array.from({ length: context.table.length }, (_, rowIndex) => {
            const rawId = context.table.has(idField) ? context.table.value(rowIndex, idField) : undefined;
            const rawGroup = context.table.has(groupField)
                ? context.table.value(rowIndex, groupField)
                : undefined;
            const rawLabel = context.table.has(labelField)
                ? context.table.value(rowIndex, labelField)
                : undefined;
            const end = context.table.has(endField)
                ? (numericDataValue(context.table.value(rowIndex, endField), true) ?? undefined)
                : undefined;
            return {
                id: rawId === null || rawId === undefined ? `item-${rowIndex}` : String(rawId),
                start: numericDataValue(context.table.value(rowIndex, startField), true) ?? rowIndex,
                ...(end === undefined ? {} : { end }),
                group: rawGroup === null || rawGroup === undefined ? 'default' : String(rawGroup),
                ...(rawLabel === null || rawLabel === undefined ? {} : { label: String(rawLabel) }),
                milestone: context.table.has(milestoneField)
                    ? context.table.value(rowIndex, milestoneField) === true
                    : false,
                dependencies: context.table.has(dependencyField)
                    ? dataArray(context.table.value(rowIndex, dependencyField))
                    : [],
            };
        });
        const domain = numberArrayOption$1(context, 'domain');
        const groupOrder = stringArrayOption$1(context, 'groupOrder');
        const model = layoutTimeline(data, {
            ...(domain !== undefined && domain.length === 2
                ? { domain: [domain[0], domain[1]] }
                : {}),
            ...(groupOrder === undefined ? {} : { groupOrder }),
            clip: booleanOption$2(context, 'clip') !== false,
        });
        if (model.items.length === 0)
            return [];
        const navigatorHeight = booleanOption$2(context, 'navigator') === false ? 0 : Math.min(26, context.plot.height * 0.12);
        const usableHeight = context.plot.height - navigatorHeight - (navigatorHeight > 0 ? 5 : 0);
        const groupOffsets = new Map();
        let laneCursor = 0;
        model.groups.forEach(({ group, lanes }) => {
            groupOffsets.set(group, laneCursor);
            laneCursor += Math.max(1, lanes);
        });
        const laneCount = Math.max(1, laneCursor);
        const laneHeight = usableHeight / laneCount;
        const span = Math.max(Number.EPSILON, model.domain[1] - model.domain[0]);
        const mapX = (value) => context.plot.x + ((value - model.domain[0]) / span) * context.plot.width;
        const centers = new Map();
        const nodes = [];
        model.items.forEach((item, index) => {
            const globalLane = (groupOffsets.get(item.group) ?? 0) + item.lane;
            const y = context.plot.y + globalLane * laneHeight + laneHeight * 0.18;
            const height = Math.max(3, laneHeight * 0.64);
            const start = mapX(item.clippedStart);
            const end = mapX(item.clippedEnd);
            const color = categoricalColor(context.theme, model.groups.findIndex(({ group }) => group === item.group), model.groups.length);
            centers.set(item.id, { x: item.milestone ? start : (start + end) / 2, y: y + height / 2 });
            const datum = {
                layerId: context.layer.id,
                rowIndex: data.findIndex(({ id }) => id === item.id),
                datum: {
                    id: item.id,
                    group: item.group,
                    lane: item.lane,
                    start: item.start,
                    end: item.end,
                    duration: item.duration,
                    visibleDuration: item.visibleDuration,
                    milestone: item.milestone,
                    clipped: item.clipped,
                    dependencies: [...item.dependencies],
                },
                tooltip: {
                    id: item.id,
                    group: item.group,
                    lane: item.lane,
                    start: item.start,
                    end: item.end,
                    duration: item.duration,
                    visibleDuration: item.visibleDuration,
                    durationLabel: item.durationLabel,
                    milestone: item.milestone,
                    clipped: item.clipped,
                    dependencies: item.dependencies.join(', '),
                },
            };
            if (item.milestone) {
                const radius = Math.max(4, height * 0.46);
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${context.layer.id}:timeline:${item.id}`, {
                        zIndex: context.layer.zIndex + 2,
                        interactive: context.performance.enableHitTesting,
                        datum,
                    }),
                    points: [
                        { x: start, y },
                        { x: start + radius, y: y + height / 2 },
                        { x: start, y: y + height },
                        { x: start - radius, y: y + height / 2 },
                    ],
                    closed: true,
                    fill: color,
                    stroke: context.theme.colors.background,
                    lineWidth: 1,
                });
            }
            else {
                nodes.push({
                    type: 'rect',
                    ...nodeBase(`${context.layer.id}:timeline:${item.id}`, {
                        zIndex: context.layer.zIndex + 2,
                        interactive: context.performance.enableHitTesting,
                        datum,
                    }),
                    x: Math.min(start, end),
                    y,
                    width: Math.max(2, Math.abs(end - start)),
                    height,
                    fill: colorWithOpacity(color, item.clipped ? 0.5 : 0.78),
                    stroke: color,
                    lineWidth: 1,
                    cornerRadius: context.layer.mark.cornerRadius ?? 2,
                });
            }
            if (height >= 12)
                nodes.push(textNode$4(context, `${context.layer.id}:timeline-label:${item.id}`, Math.max(context.plot.x + 3, Math.min(context.plot.x + context.plot.width - 3, item.milestone ? start + 8 : (start + end) / 2)), y + height / 2, data[index]?.label ?? item.id, {
                    align: item.milestone ? 'left' : 'center',
                    size: Math.min(11, height * 0.52),
                    fill: readableTextColor(color, '#ffffff', '#0f172a'),
                }));
        });
        model.items.forEach((item) => {
            const target = centers.get(item.id);
            if (target === undefined)
                return;
            item.dependencies.forEach((dependency, index) => {
                const source = centers.get(dependency);
                if (source === undefined)
                    return;
                const middle = (source.x + target.x) / 2;
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${context.layer.id}:timeline-dependency:${item.id}:${index}`, {
                        zIndex: context.layer.zIndex,
                    }),
                    points: [source, { x: middle, y: source.y }, { x: middle, y: target.y }, target],
                    closed: false,
                    stroke: context.theme.colors.mutedText,
                    lineWidth: 1,
                    dash: [4, 2],
                    lineCap: 'round',
                    lineJoin: 'round',
                });
            });
        });
        if (navigatorHeight > 0) {
            const y = context.plot.y + context.plot.height - navigatorHeight;
            const observedSpan = Math.max(Number.EPSILON, model.navigator.maximum - model.navigator.minimum);
            nodes.push({
                type: 'rect',
                ...nodeBase(`${context.layer.id}:timeline-navigator-track`, {
                    zIndex: context.layer.zIndex + 3,
                }),
                x: context.plot.x,
                y,
                width: context.plot.width,
                height: navigatorHeight,
                fill: context.theme.colors.surface,
                stroke: context.theme.colors.axis,
                lineWidth: 1,
                cornerRadius: 2,
            });
            nodes.push({
                type: 'rect',
                ...nodeBase(`${context.layer.id}:timeline-navigator-window`, {
                    zIndex: context.layer.zIndex + 4,
                    interactive: true,
                    datum: {
                        layerId: context.layer.id,
                        rowIndex: 0,
                        datum: { ...model.navigator },
                        tooltip: { ...model.navigator },
                        familyInteraction: {
                            kind: 'navigator-window',
                            family: 'timeline',
                            minimum: model.navigator.minimum,
                            maximum: model.navigator.maximum,
                            start: model.navigator.start,
                            end: model.navigator.end,
                            plot: {
                                x: context.plot.x,
                                y,
                                width: context.plot.width,
                                height: navigatorHeight,
                            },
                        },
                    },
                }),
                x: context.plot.x +
                    ((model.navigator.start - model.navigator.minimum) / observedSpan) * context.plot.width,
                y: y + 2,
                width: Math.max(4, ((model.navigator.end - model.navigator.start) / observedSpan) * context.plot.width),
                height: Math.max(2, navigatorHeight - 4),
                fill: colorWithOpacity(context.theme.colors.focus, 0.28),
                stroke: context.theme.colors.focus,
                lineWidth: 1,
                cornerRadius: 2,
            });
        }
        return nodes;
    };
    function safeObject(value) {
        return value !== null && typeof value === 'object' && !Array.isArray(value)
            ? Object.fromEntries(Object.entries(value))
            : undefined;
    }
    function filtersOption(context) {
        const value = context.layer.mark.options.filters;
        if (!Array.isArray(value))
            return undefined;
        const output = [];
        value.forEach((entry) => {
            const object = safeObject(entry);
            if (object === undefined ||
                typeof object.field !== 'string' ||
                typeof object.operator !== 'string')
                return;
            if (['equals', 'not-equals', 'contains'].includes(object.operator))
                output.push({
                    field: object.field,
                    operator: object.operator,
                    value: object.value,
                });
            if (['greater', 'greater-or-equal', 'less', 'less-or-equal'].includes(object.operator) &&
                typeof object.value === 'number')
                output.push({
                    field: object.field,
                    operator: object.operator,
                    value: object.value,
                });
        });
        return output;
    }
    function portableValue(value) {
        if (value === null || value === undefined)
            return null;
        if (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean')
            return value;
        if (value instanceof Date)
            return value.toISOString();
        return JSON.stringify(value);
    }
    function sortOption(context) {
        const value = context.layer.mark.options.sort;
        if (!Array.isArray(value))
            return undefined;
        return value.flatMap((entry) => {
            const object = safeObject(entry);
            return object !== undefined && typeof object.field === 'string'
                ? [
                    {
                        field: object.field,
                        direction: object.direction === 'descending' ? 'descending' : 'ascending',
                    },
                ]
                : [];
        });
    }
    function groupOption(context) {
        const object = safeObject(context.layer.mark.options.group);
        if (object === undefined || !Array.isArray(object.fields) || !Array.isArray(object.aggregates))
            return undefined;
        const fields = object.fields.filter((field) => typeof field === 'string');
        const aggregates = object.aggregates.flatMap((entry) => {
            const aggregate = safeObject(entry);
            if (aggregate === undefined ||
                typeof aggregate.field !== 'string' ||
                typeof aggregate.as !== 'string' ||
                !['count', 'sum', 'mean', 'min', 'max'].includes(String(aggregate.op)))
                return [];
            return [
                {
                    field: aggregate.field,
                    as: aggregate.as,
                    op: aggregate.op,
                },
            ];
        });
        return { fields, aggregates };
    }
    function pivotOption(context) {
        const object = safeObject(context.layer.mark.options.pivot);
        if (object === undefined ||
            typeof object.row !== 'string' ||
            typeof object.column !== 'string' ||
            typeof object.value !== 'string')
            return undefined;
        return {
            row: object.row,
            column: object.column,
            value: object.value,
            op: object.op === 'count' || object.op === 'mean' ? object.op : 'sum',
        };
    }
    /** Table compiler with filter/group/pivot/sort, virtual windows, frozen regions, and formatter registry. */
    const compileAdvancedTableMark = (context) => {
        const group = groupOption(context);
        const pivot = pivotOption(context);
        const filters = filtersOption(context);
        const sort = sortOption(context);
        const model = buildTableModel(Array.from({ length: context.table.length }, (_, index) => context.table.row(index)), {
            ...(filters === undefined ? {} : { filters }),
            ...(sort === undefined ? {} : { sort }),
            ...(group === undefined ? {} : { group }),
            ...(pivot === undefined ? {} : { pivot }),
            window: {
                offset: numberOption$2(context, 'windowOffset') ?? 0,
                limit: numberOption$2(context, 'windowLimit') ?? Math.min(100, context.table.length),
            },
            columnWindow: {
                offset: numberOption$2(context, 'columnOffset') ?? 0,
                limit: numberOption$2(context, 'columnLimit') ?? 100,
            },
            frozenRows: numberOption$2(context, 'frozenRows') ?? 0,
            frozenColumns: numberOption$2(context, 'frozenColumns') ?? 0,
        });
        if (model.columnEntries.length === 0)
            return [];
        const formatters = safeObject(context.layer.mark.options.formatters) ?? {};
        const registry = context.tableFormatters;
        const runtimeFocusedCell = safeObject(context.layer.mark.options.runtimeFocusedCell);
        const focusedRow = runtimeFocusedCell !== undefined &&
            typeof runtimeFocusedCell.row === 'number' &&
            Number.isInteger(runtimeFocusedCell.row)
            ? runtimeFocusedCell.row
            : -1;
        const focusedColumn = runtimeFocusedCell !== undefined &&
            typeof runtimeFocusedCell.column === 'number' &&
            Number.isInteger(runtimeFocusedCell.column)
            ? runtimeFocusedCell.column
            : -1;
        const rowCount = model.rowEntries.length + 1;
        const rowHeight = context.plot.height / Math.max(2, rowCount);
        const columnWidth = context.plot.width / model.columnEntries.length;
        const nodes = [];
        model.columnEntries.forEach(({ field: column, index: absoluteColumn, frozen }, displayColumn) => {
            const x = context.plot.x + displayColumn * columnWidth;
            nodes.push({
                type: 'rect',
                ...nodeBase(`${context.layer.id}:table-header:${absoluteColumn}`, {
                    zIndex: context.layer.zIndex + (frozen ? 5 : 2),
                    interactive: context.performance.enableHitTesting,
                    datum: {
                        layerId: context.layer.id,
                        rowIndex: 0,
                        datum: { field: column, column: absoluteColumn, displayColumn, frozen },
                        tooltip: {
                            kind: 'table-header',
                            field: column,
                            column: absoluteColumn,
                            displayColumn,
                            frozen,
                        },
                        familyInteraction: {
                            kind: 'table-header',
                            field: column,
                            column: absoluteColumn,
                            columns: model.totalColumns,
                        },
                    },
                }),
                x,
                y: context.plot.y,
                width: columnWidth,
                height: rowHeight,
                fill: frozen
                    ? colorWithOpacity(context.theme.colors.focus, 0.2)
                    : context.theme.colors.surface,
                stroke: context.theme.colors.axis,
                lineWidth: 0.7,
                cornerRadius: 0,
            });
            nodes.push(textNode$4(context, `${context.layer.id}:table-header-label:${absoluteColumn}`, x + 5, context.plot.y + rowHeight / 2, column, {
                align: 'left',
                size: Math.min(11, rowHeight * 0.42),
                weight: 700,
                zIndex: context.layer.zIndex + 6,
            }));
        });
        model.rowEntries.forEach(({ row, index: absoluteRow, frozen: frozenRow }, rowIndex) => {
            model.columnEntries.forEach(({ field: column, index: absoluteColumn, frozen: frozenColumn }, displayColumn) => {
                const x = context.plot.x + displayColumn * columnWidth;
                const y = context.plot.y + (rowIndex + 1) * rowHeight;
                const frozen = frozenRow || frozenColumn;
                const rawFormatter = formatters[column];
                if (rawFormatter !== undefined && typeof rawFormatter !== 'string') {
                    throw new GraflumeError('INVALID_SPEC', `Table formatter for "${column}" must be a registered formatter id.`, { path: `$.layers[].mark.options.formatters.${column}` });
                }
                const formatter = rawFormatter ?? 'string';
                const formatted = registry.format(formatter, row[column], row, context.locale);
                const value = portableValue(row[column]);
                const focused = absoluteRow === focusedRow && absoluteColumn === focusedColumn;
                nodes.push({
                    type: 'rect',
                    ...nodeBase(`${context.layer.id}:table-cell:${absoluteRow}:${absoluteColumn}`, {
                        zIndex: context.layer.zIndex + (frozen ? 4 : 1),
                        interactive: context.performance.enableHitTesting,
                        datum: {
                            layerId: context.layer.id,
                            rowIndex: typeof row.__sourceIndex === 'number' ? row.__sourceIndex : absoluteRow,
                            datum: {
                                row: absoluteRow,
                                displayRow: rowIndex,
                                column: absoluteColumn,
                                displayColumn,
                                field: column,
                                value,
                                formatted,
                                frozen,
                                frozenRow,
                                frozenColumn,
                            },
                            tooltip: {
                                row: absoluteRow,
                                column,
                                columnIndex: absoluteColumn,
                                displayColumn,
                                value,
                                formatted,
                                formatter,
                                frozen,
                                frozenRow,
                                frozenColumn,
                                totalRows: model.totalRows,
                            },
                            familyInteraction: {
                                kind: 'table-cell',
                                field: column,
                                row: absoluteRow,
                                column: absoluteColumn,
                                rows: model.totalRows,
                                columns: model.totalColumns,
                                windowOffset: model.window.offset,
                                windowLimit: model.window.limit,
                                columnOffset: model.columnWindow.offset,
                                columnLimit: model.columnWindow.limit,
                            },
                        },
                    }),
                    x,
                    y,
                    width: columnWidth,
                    height: rowHeight,
                    fill: frozen
                        ? colorWithOpacity(context.theme.colors.focus, 0.08)
                        : rowIndex % 2 === 0
                            ? context.theme.colors.background
                            : context.theme.colors.surface,
                    stroke: focused ? context.theme.colors.focus : context.theme.colors.axis,
                    lineWidth: focused ? 2.5 : 0.45,
                    cornerRadius: 0,
                });
                nodes.push(textNode$4(context, `${context.layer.id}:table-cell-label:${absoluteRow}:${absoluteColumn}`, x + 5, y + rowHeight / 2, formatted, {
                    align: 'left',
                    size: Math.min(11, rowHeight * 0.38),
                    zIndex: context.layer.zIndex + (frozen ? 5 : 2),
                }));
            });
        });
        if (model.totalRows === 0) {
            nodes.push(textNode$4(context, `${context.layer.id}:table-empty`, context.plot.x + context.plot.width / 2, context.plot.y + Math.min(context.plot.height - 12, rowHeight + 24), 'No matching rows', {
                fill: context.theme.colors.mutedText,
                size: Math.min(12, context.theme.typography.fontSize),
                weight: 500,
                zIndex: context.layer.zIndex + 2,
            }));
        }
        return nodes;
    };
    /** Polar compiler with zero/direction/wrap, nonlinear radius, angular bins, and radial stacking. */
    const compileAdvancedPolarMark = (context) => {
        const advancedOptionNames = ['zero', 'direction', 'wrap', 'radiusScale', 'bins', 'radialStack'];
        if (!advancedOptionNames.some((name) => Object.hasOwn(context.layer.mark.options, name))) {
            return compilePolarMark(context);
        }
        const angleField = context.layer.mark.fields.angle ?? context.layer.x.field;
        const valueField = context.layer.mark.fields.value ?? context.layer.y.field;
        const seriesField = context.layer.mark.fields.series ?? 'series';
        const idField = context.layer.mark.fields.id ?? 'id';
        const data = Array.from({ length: context.table.length }, (_, rowIndex) => {
            const rawSeries = context.table.has(seriesField)
                ? context.table.value(rowIndex, seriesField)
                : undefined;
            const rawId = context.table.has(idField) ? context.table.value(rowIndex, idField) : undefined;
            return {
                angle: numericDataValue(context.table.value(rowIndex, angleField)) ?? 0,
                value: numericDataValue(context.table.value(rowIndex, valueField)) ?? 0,
                series: rawSeries === null || rawSeries === undefined ? 'series' : String(rawSeries),
                id: rawId === null || rawId === undefined ? `polar-${rowIndex}` : String(rawId),
            };
        });
        const wrap = numberArrayOption$1(context, 'wrap');
        const radiusScale = stringOption$2(context, 'radiusScale');
        const stack = stringOption$2(context, 'radialStack') ?? stringOption$2(context, 'stack');
        const model = layoutPolar(data, {
            zero: numberOption$2(context, 'zero') ?? 0,
            direction: stringOption$2(context, 'direction') === 'counterclockwise' ? 'counterclockwise' : 'clockwise',
            ...(wrap !== undefined && wrap.length === 2 ? { wrap: [wrap[0], wrap[1]] } : {}),
            radiusScale: radiusScale === 'log' || radiusScale === 'sqrt' ? radiusScale : 'linear',
            bins: numberOption$2(context, 'bins') ?? Math.max(1, data.length),
            stack: stack === 'stack' || stack === 'normalize' ? stack : 'none',
        });
        const cx = context.plot.x + context.plot.width / 2;
        const cy = context.plot.y + context.plot.height / 2;
        const radius = Math.max(5, Math.min(context.plot.width, context.plot.height) * 0.42);
        const series = [...new Set(model.segments.map(({ series: value }) => value))];
        return model.segments.map((segment, index) => {
            const start = (segment.startAngle * Math.PI) / 180;
            const end = (segment.endAngle * Math.PI) / 180;
            const color = categoricalColor(context.theme, series.indexOf(segment.series), series.length);
            return {
                type: 'path',
                ...nodeBase(`${context.layer.id}:polar:${segment.id}:${index}`, {
                    zIndex: context.layer.zIndex,
                    opacity: context.layer.mark.opacity,
                    interactive: context.performance.enableHitTesting,
                    datum: {
                        layerId: context.layer.id,
                        rowIndex: data.findIndex(({ id }) => id === segment.id),
                        datum: { ...segment },
                        tooltip: {
                            id: segment.id,
                            series: segment.series,
                            angle: segment.angle,
                            value: segment.value,
                            bin: segment.bin,
                            innerValue: segment.innerValue,
                            outerValue: segment.outerValue,
                            innerRadius: segment.innerRadius,
                            outerRadius: segment.outerRadius,
                            proportion: segment.proportion,
                            direction: model.direction,
                            zero: model.zero,
                        },
                    },
                }),
                points: arcPoints(cx, cy, radius * segment.outerRadius, start, end, radius * segment.innerRadius),
                closed: true,
                fill: context.layer.mark.fill ?? colorWithOpacity(color, 0.76),
                stroke: context.layer.mark.stroke ?? context.theme.colors.background,
                lineWidth: context.layer.mark.lineWidth ?? 1,
                lineJoin: 'round',
            };
        });
    };

    function finite$6(value, path) {
        if (typeof value !== 'number' || !Number.isFinite(value)) {
            throw new GraflumeError('INVALID_DATA', `${path} must be a finite number.`, { path });
        }
        return value;
    }
    function nonEmpty(value, path) {
        const normalized = value.trim();
        if (normalized === '') {
            throw new GraflumeError('INVALID_DATA', `${path} must be a non-empty string.`, { path });
        }
        return normalized;
    }
    function clamp$4(value, minimum, maximum) {
        return Math.max(minimum, Math.min(maximum, value));
    }
    function seededRandom$1(seed) {
        let state = Math.floor(seed) >>> 0;
        return () => {
            state = (Math.imul(state, 1_664_525) + 1_013_904_223) >>> 0;
            return state / 0x1_0000_0000;
        };
    }
    function hierarchyTree(data) {
        if (data.length === 0) {
            throw new GraflumeError('INVALID_DATA', 'Hierarchy data must contain at least one node.');
        }
        const byId = new Map();
        data.forEach((datum, index) => {
            const id = nonEmpty(datum.id, `$.data[${index}].id`);
            if (byId.has(id)) {
                throw new GraflumeError('INVALID_DATA', `Duplicate hierarchy id "${id}".`, {
                    path: `$.data[${index}].id`,
                });
            }
            const value = datum.value === undefined ? 1 : finite$6(datum.value, `$.data[${index}].value`);
            if (value < 0) {
                throw new GraflumeError('INVALID_DATA', 'Hierarchy values must be non-negative.', {
                    path: `$.data[${index}].value`,
                });
            }
            byId.set(id, {
                id,
                parent: datum.parent == null || datum.parent.trim() === '' ? null : datum.parent.trim(),
                label: datum.label?.trim() || id,
                ownValue: value,
                aggregate: value,
                depth: 0,
                x: 0,
                y: 0,
                radius: 0,
                children: [],
            });
        });
        const roots = [];
        for (const node of byId.values()) {
            if (node.parent === null) {
                roots.push(node);
                continue;
            }
            const parent = byId.get(node.parent);
            if (parent === undefined) {
                throw new GraflumeError('INVALID_DATA', `Unknown hierarchy parent "${node.parent}".`, {
                    path: '$.data',
                });
            }
            parent.children.push(node);
        }
        if (roots.length === 0) {
            throw new GraflumeError('INVALID_DATA', 'Hierarchy must contain at least one root.');
        }
        const visiting = new Set();
        const visited = new Set();
        const aggregate = (node, depth) => {
            if (visiting.has(node.id)) {
                throw new GraflumeError('INVALID_DATA', `Hierarchy cycle detected at "${node.id}".`);
            }
            if (visited.has(node.id))
                return node.aggregate;
            visiting.add(node.id);
            node.depth = depth;
            const children = node.children.reduce((sum, child) => sum + aggregate(child, depth + 1), 0);
            node.aggregate = Math.max(node.ownValue, children || node.ownValue);
            visiting.delete(node.id);
            visited.add(node.id);
            return node.aggregate;
        };
        roots.forEach((root) => aggregate(root, 0));
        if (visited.size !== data.length) {
            throw new GraflumeError('INVALID_DATA', 'Hierarchy contains an unreachable cycle.');
        }
        return { byId, roots };
    }
    function hierarchyBreadcrumbs(node, byId) {
        const values = [];
        let current = node;
        while (current !== undefined) {
            values.push(current.id);
            current = current.parent === null ? undefined : byId.get(current.parent);
        }
        return values.reverse();
    }
    function visibleHierarchy(root, collapsed) {
        const nodes = [];
        const visit = (node) => {
            nodes.push(node);
            if (collapsed.has(node.id))
                return;
            node.children.forEach(visit);
        };
        visit(root);
        return nodes;
    }
    function layoutDendrogram(nodes, radial) {
        const visible = new Set(nodes.map(({ id }) => id));
        const children = new Map();
        nodes.forEach((node) => {
            if (node.parent !== null && visible.has(node.parent)) {
                const values = children.get(node.parent) ?? [];
                values.push(node);
                children.set(node.parent, values);
            }
        });
        const leaves = nodes.filter((node) => (children.get(node.id)?.length ?? 0) === 0);
        leaves.forEach((leaf, index) => {
            leaf.x = leaves.length === 1 ? 0.5 : index / (leaves.length - 1);
        });
        const byDepth = [...nodes].sort((left, right) => right.depth - left.depth);
        byDepth.forEach((node) => {
            const visibleChildren = children.get(node.id) ?? [];
            if (visibleChildren.length > 0) {
                node.x = visibleChildren.reduce((sum, child) => sum + child.x, 0) / visibleChildren.length;
            }
        });
        const rootDepth = Math.min(...nodes.map(({ depth }) => depth));
        const maximumDepth = Math.max(...nodes.map(({ depth }) => depth));
        nodes.forEach((node) => {
            const ratio = maximumDepth === rootDepth ? 0 : (node.depth - rootDepth) / (maximumDepth - rootDepth);
            if (radial) {
                const angle = -Math.PI / 2 + node.x * Math.PI * 2;
                const radius = ratio * 0.48;
                node.x = 0.5 + Math.cos(angle) * radius;
                node.y = 0.5 + Math.sin(angle) * radius;
            }
            else {
                node.y = 0.04 + ratio * 0.92;
            }
            node.radius = clamp$4(Math.sqrt(node.aggregate) * 0.008, 0.007, 0.035);
        });
    }
    function layoutCirclePack(nodes, padding) {
        const visible = new Set(nodes.map(({ id }) => id));
        const root = nodes[0];
        const maximum = Math.max(root.aggregate, Number.EPSILON);
        root.x = 0.5;
        root.y = 0.5;
        root.radius = 0.49;
        const place = (parent) => {
            const children = parent.children
                .filter(({ id }) => visible.has(id))
                .sort((left, right) => right.aggregate - left.aggregate || left.id.localeCompare(right.id));
            if (children.length === 0)
                return;
            const total = children.reduce((sum, child) => sum + child.aggregate, 0) || 1;
            const scale = parent.radius * 0.78;
            const placed = [];
            children.forEach((child, index) => {
                child.radius = Math.max(parent.radius * 0.025, Math.sqrt(child.aggregate / total) * scale * 0.47);
                const golden = Math.PI * (3 - Math.sqrt(5));
                let distance = index === 0 ? 0 : child.radius;
                let angle = index * golden;
                let attempts = 0;
                for (;;) {
                    const candidateX = parent.x + Math.cos(angle) * distance;
                    const candidateY = parent.y + Math.sin(angle) * distance;
                    const inside = Math.hypot(candidateX - parent.x, candidateY - parent.y) + child.radius <= parent.radius;
                    const free = placed.every((other) => Math.hypot(candidateX - other.x, candidateY - other.y) >=
                        child.radius + other.radius + padding);
                    if (inside && free) {
                        child.x = candidateX;
                        child.y = candidateY;
                        break;
                    }
                    attempts += 1;
                    angle += 0.41;
                    distance = Math.min(parent.radius - child.radius, distance + parent.radius * 0.004);
                    if (attempts > 20_000) {
                        child.radius *= 0.98;
                        attempts = 0;
                        distance = child.radius;
                    }
                }
                placed.push(child);
                place(child);
            });
        };
        place(root);
        nodes.forEach((node) => {
            if (node.radius === 0)
                node.radius = Math.max(0.006, Math.sqrt(node.aggregate / maximum) * 0.02);
        });
    }
    /** Deterministic circle-pack, dendrogram, and radial-tree layout with re-root, collapse, breadcrumbs and search. */
    function layoutHierarchy(data, options = {}) {
        const { byId, roots } = hierarchyTree(data);
        const requested = options.root === undefined ? roots[0] : byId.get(options.root);
        if (requested === undefined) {
            throw new GraflumeError('INVALID_SPEC', `Unknown hierarchy root "${options.root}".`, {
                path: '$.root',
            });
        }
        const collapsed = new Set(options.collapsed ?? []);
        for (const id of collapsed) {
            if (!byId.has(id)) {
                throw new GraflumeError('INVALID_SPEC', `Unknown collapsed hierarchy id "${id}".`);
            }
        }
        const visible = visibleHierarchy(requested, collapsed);
        const baseDepth = requested.depth;
        visible.forEach((node) => {
            node.depth -= baseDepth;
        });
        const mode = options.mode ?? 'circle-pack';
        if (mode === 'circle-pack')
            layoutCirclePack(visible, clamp$4(options.padding ?? 0.004, 0, 0.05));
        else
            layoutDendrogram(visible, mode === 'radial-tree');
        const query = options.query?.trim().toLocaleLowerCase() ?? '';
        const matches = visible
            .filter(({ id, label }) => query !== '' && `${id}\u0000${label}`.toLocaleLowerCase().includes(query))
            .map(({ id }) => id);
        const visibleIds = new Set(visible.map(({ id }) => id));
        return {
            mode,
            root: requested.id,
            breadcrumbs: hierarchyBreadcrumbs(requested, byId),
            matches,
            nodes: visible.map((node) => ({
                id: node.id,
                parent: node.parent,
                label: node.label,
                depth: node.depth,
                value: node.ownValue,
                aggregate: node.aggregate,
                x: node.x,
                y: node.y,
                radius: node.radius,
                leaf: node.children.every(({ id }) => !visibleIds.has(id)),
                collapsed: collapsed.has(node.id),
                matched: matches.includes(node.id),
            })),
            links: visible.flatMap((node) => node.parent !== null && visibleIds.has(node.parent)
                ? [{ source: node.parent, target: node.id }]
                : []),
        };
    }
    function graphCycles(adjacency) {
        const found = [];
        const stack = [];
        const active = new Set();
        const done = new Set();
        const visit = (id) => {
            if (done.has(id))
                return;
            if (active.has(id)) {
                const start = stack.indexOf(id);
                const cycle = [...stack.slice(Math.max(0, start)), id];
                const signature = cycle.slice(0, -1).sort().join('\u0000');
                if (!found.some((item) => item.slice(0, -1).sort().join('\u0000') === signature))
                    found.push(cycle);
                return;
            }
            active.add(id);
            stack.push(id);
            for (const target of adjacency.get(id) ?? [])
                visit(target);
            stack.pop();
            active.delete(id);
            done.add(id);
        };
        for (const id of adjacency.keys())
            visit(id);
        return found;
    }
    /** Multi-stage Sankey/alluvial graph validation and deterministic iterative layout. */
    function layoutFlow(edges, options = {}) {
        if (edges.length === 0)
            return { nodes: [], links: [], cycles: [], imbalances: [] };
        const ids = [];
        const seen = new Set();
        const normalized = edges.map((edge, index) => {
            const source = nonEmpty(edge.source, `$.edges[${index}].source`);
            const target = nonEmpty(edge.target, `$.edges[${index}].target`);
            const value = finite$6(edge.value, `$.edges[${index}].value`);
            if (value < 0)
                throw new GraflumeError('INVALID_DATA', 'Flow values must be non-negative.');
            for (const id of [source, target]) {
                if (!seen.has(id)) {
                    seen.add(id);
                    ids.push(id);
                }
            }
            return { source, target, value, id: edge.id?.trim() || `flow-${index}`, inputIndex: index };
        });
        const linkIds = new Set();
        normalized.forEach(({ id }) => {
            if (linkIds.has(id))
                throw new GraflumeError('INVALID_DATA', `Duplicate flow edge id "${id}".`);
            linkIds.add(id);
        });
        const linkSort = options.linkSort ?? 'input';
        const linkOrder = options.linkOrder ?? [];
        let authoredLinkRanks = new Map();
        if (linkSort === 'authored') {
            if (linkOrder.length !== normalized.length ||
                new Set(linkOrder).size !== linkOrder.length ||
                linkOrder.some((id) => !linkIds.has(id))) {
                throw new GraflumeError('INVALID_SPEC', '$.linkOrder must contain every flow edge id exactly once for authored link sorting.');
            }
            authoredLinkRanks = new Map(linkOrder.map((id, index) => [id, index]));
        }
        else if (linkOrder.length > 0) {
            throw new GraflumeError('INVALID_SPEC', '$.linkOrder requires linkSort "authored".');
        }
        const compareLinks = (left, right) => {
            if (linkSort === 'ascending')
                return left.value - right.value || left.inputIndex - right.inputIndex;
            if (linkSort === 'descending')
                return right.value - left.value || left.inputIndex - right.inputIndex;
            if (linkSort === 'authored')
                return authoredLinkRanks.get(left.id) - authoredLinkRanks.get(right.id);
            return left.inputIndex - right.inputIndex;
        };
        const outgoing = new Map();
        const incoming = new Map();
        ids.forEach((id) => {
            outgoing.set(id, []);
            incoming.set(id, []);
        });
        normalized.forEach(({ source, target }) => {
            outgoing.get(source).push(target);
            incoming.get(target).push(source);
        });
        const cycles = graphCycles(outgoing);
        if (cycles.length > 0 && (options.cycle ?? 'reject') === 'reject') {
            throw new GraflumeError('INVALID_DATA', `Flow cycle detected: ${cycles[0].join(' -> ')}.`);
        }
        const isCycleEdge = (source, target) => source === target || cycles.some((cycle) => cycle.includes(source) && cycle.includes(target));
        const columns = new Map(ids.map((id) => [id, 0]));
        for (let iteration = 0; iteration < ids.length; iteration += 1) {
            let changed = false;
            normalized.forEach(({ source, target }) => {
                if (isCycleEdge(source, target))
                    return;
                const next = Math.max(columns.get(target) ?? 0, (columns.get(source) ?? 0) + 1);
                if (next !== columns.get(target)) {
                    columns.set(target, next);
                    changed = true;
                }
            });
            if (!changed)
                break;
        }
        const maximumColumn = Math.max(...columns.values(), 0);
        if (options.alignment === 'right') {
            const sinkDistances = new Map(ids.map((id) => [id, 0]));
            for (let iteration = 0; iteration < ids.length; iteration += 1) {
                let changed = false;
                normalized.forEach(({ source, target }) => {
                    if (isCycleEdge(source, target))
                        return;
                    const next = Math.max(sinkDistances.get(source) ?? 0, (sinkDistances.get(target) ?? 0) + 1);
                    if (next !== sinkDistances.get(source)) {
                        sinkDistances.set(source, next);
                        changed = true;
                    }
                });
                if (!changed)
                    break;
            }
            ids.forEach((id) => {
                columns.set(id, Math.max(0, maximumColumn - (sinkDistances.get(id) ?? 0)));
            });
        }
        else if (options.alignment === 'justify') {
            ids.forEach((id) => {
                if ((outgoing.get(id)?.length ?? 0) === 0)
                    columns.set(id, maximumColumn);
            });
        }
        else if (options.alignment === 'center') {
            const leftColumns = new Map(columns);
            ids.forEach((id) => {
                if ((incoming.get(id)?.length ?? 0) > 0)
                    return;
                const targetColumns = (outgoing.get(id) ?? []).map((target) => leftColumns.get(target) ?? 0);
                if (targetColumns.length > 0)
                    columns.set(id, Math.max(0, Math.min(...targetColumns) - 1));
            });
        }
        const sums = new Map(ids.map((id) => [id, { input: 0, output: 0 }]));
        normalized.forEach(({ source, target, value }) => {
            sums.get(source).output += value;
            sums.get(target).input += value;
        });
        const grouped = new Map();
        ids.forEach((id) => {
            const column = columns.get(id) ?? 0;
            const values = grouped.get(column) ?? [];
            values.push(id);
            grouped.set(column, values);
        });
        const valueOf = (id) => Math.max(sums.get(id).input, sums.get(id).output);
        grouped.forEach((values) => {
            if (options.order === 'ascending')
                values.sort((a, b) => valueOf(a) - valueOf(b) || a.localeCompare(b));
            else if (options.order === 'descending')
                values.sort((a, b) => valueOf(b) - valueOf(a) || a.localeCompare(b));
        });
        const requestedIterations = Math.floor(options.iterations ?? 8);
        if (requestedIterations < 0 || requestedIterations > 128) {
            throw new GraflumeError('INVALID_SPEC', '$.iterations must be from 0 to 128.');
        }
        for (let iteration = 0; iteration < requestedIterations; iteration += 1) {
            const forward = iteration % 2 === 0;
            const columnKeys = [...grouped.keys()].sort((a, b) => (forward ? a - b : b - a));
            const positions = new Map();
            grouped.forEach((values) => values.forEach((id, index) => positions.set(id, index)));
            columnKeys.forEach((column) => {
                const values = grouped.get(column);
                values.sort((a, b) => {
                    const neighbors = (forward ? incoming : outgoing).get(a) ?? [];
                    const otherNeighbors = (forward ? incoming : outgoing).get(b) ?? [];
                    const mean = (items) => items.length === 0
                        ? Number.POSITIVE_INFINITY
                        : items.reduce((sum, id) => sum + (positions.get(id) ?? 0), 0) / items.length;
                    return mean(neighbors) - mean(otherNeighbors) || a.localeCompare(b);
                });
            });
        }
        const nodes = [];
        const positionById = new Map();
        const tolerance = Math.max(0, options.balanceTolerance ?? 1e-9);
        grouped.forEach((values, column) => {
            const total = values.reduce((sum, id) => sum + valueOf(id), 0) || 1;
            let cursor = 0;
            values.forEach((id, order) => {
                const amount = valueOf(id);
                const authored = options.positions?.[id];
                if (authored !== undefined) {
                    finite$6(authored.x, `$.positions.${id}.x`);
                    finite$6(authored.y, `$.positions.${id}.y`);
                }
                const input = sums.get(id).input;
                const output = sums.get(id).output;
                const node = {
                    id,
                    column,
                    order,
                    x: authored?.x ?? (maximumColumn === 0 ? 0.5 : column / maximumColumn),
                    y: authored?.y ?? (cursor + amount / 2) / total,
                    input,
                    output,
                    value: amount,
                    height: amount / total,
                    balanced: input === 0 || output === 0 || Math.abs(input - output) <= tolerance,
                };
                cursor += amount;
                nodes.push(node);
                positionById.set(id, node);
            });
        });
        const maximumValue = Math.max(...normalized.map(({ value }) => value), Number.EPSILON);
        const sourceLinks = new Map();
        const targetLinks = new Map();
        normalized.forEach((edge) => {
            sourceLinks.set(edge.source, [...(sourceLinks.get(edge.source) ?? []), edge]);
            targetLinks.set(edge.target, [...(targetLinks.get(edge.target) ?? []), edge]);
        });
        sourceLinks.forEach((links) => links.sort(compareLinks));
        targetLinks.forEach((links) => links.sort(compareLinks));
        const endpoint = (edge, node, links) => {
            let preceding = 0;
            for (const candidate of links) {
                if (candidate.id === edge.id)
                    break;
                preceding += candidate.value;
            }
            return (node.y -
                node.height / 2 +
                ((preceding + edge.value / 2) / Math.max(node.value, Number.EPSILON)) * node.height);
        };
        const links = [...normalized].sort(compareLinks).map((edge) => {
            const source = positionById.get(edge.source);
            const target = positionById.get(edge.target);
            const orderedSourceLinks = sourceLinks.get(edge.source);
            const orderedTargetLinks = targetLinks.get(edge.target);
            const sourceLinkOrder = orderedSourceLinks.findIndex(({ id }) => id === edge.id);
            const targetLinkOrder = orderedTargetLinks.findIndex(({ id }) => id === edge.id);
            const sourceY = endpoint(edge, source, orderedSourceLinks);
            const targetY = endpoint(edge, target, orderedTargetLinks);
            const middle = (source.x + target.x) / 2;
            return {
                id: edge.id,
                source: edge.source,
                target: edge.target,
                value: edge.value,
                sourceOrder: source.order,
                targetOrder: target.order,
                sourceLinkOrder,
                targetLinkOrder,
                thickness: edge.value / maximumValue,
                path: [
                    { x: source.x, y: sourceY },
                    { x: middle, y: sourceY },
                    { x: middle, y: targetY },
                    { x: target.x, y: targetY },
                ],
            };
        });
        return {
            nodes: nodes.sort((a, b) => a.column - b.column || a.order - b.order),
            links,
            cycles,
            imbalances: nodes
                .filter(({ input, output, balanced }) => input > 0 && output > 0 && !balanced)
                .map(({ id, input, output }) => ({ id, difference: input - output })),
        };
    }
    /** Returns every upstream/downstream node and link reachable from a flow node or link. */
    function traverseFlowPath(result, start, direction = 'both') {
        const startLink = result.links.find(({ id }) => id === start);
        const seeds = startLink === undefined ? [start] : [startLink.source, startLink.target];
        const nodes = new Set(seeds);
        const links = new Set();
        const queue = [...seeds];
        while (queue.length > 0) {
            const id = queue.shift();
            result.links.forEach((link) => {
                const matchDownstream = direction !== 'upstream' && link.source === id;
                const matchUpstream = direction !== 'downstream' && link.target === id;
                if (!matchDownstream && !matchUpstream)
                    return;
                links.add(link.id);
                const next = matchDownstream ? link.target : link.source;
                if (!nodes.has(next)) {
                    nodes.add(next);
                    queue.push(next);
                }
            });
        }
        if (startLink !== undefined)
            links.add(startLink.id);
        return { nodes: [...nodes], links: [...links] };
    }
    function isChordMatrixInput(input) {
        return !Array.isArray(input);
    }
    function validateChordIds(ids) {
        const normalized = ids.map((id, index) => nonEmpty(id, `$.ids[${index}]`));
        if (new Set(normalized).size !== normalized.length)
            throw new GraflumeError('INVALID_DATA', 'Chord matrix ids must be unique.', {
                path: '$.ids',
            });
        return normalized;
    }
    function validateChordMatrix(input) {
        const ids = validateChordIds(input.ids);
        if (input.matrix.length !== ids.length)
            throw new GraflumeError('INVALID_DATA', 'Chord matrix must have one row per id.', {
                path: '$.matrix',
            });
        const matrix = input.matrix.map((row, rowIndex) => {
            if (row.length !== ids.length)
                throw new GraflumeError('INVALID_DATA', 'Chord matrix must be square.', {
                    path: `$.matrix[${rowIndex}]`,
                });
            return row.map((raw, columnIndex) => {
                const value = finite$6(raw, `$.matrix[${rowIndex}][${columnIndex}]`);
                if (value < 0)
                    throw new GraflumeError('INVALID_DATA', 'Chord matrix values must be non-negative.', {
                        path: `$.matrix[${rowIndex}][${columnIndex}]`,
                    });
                return value;
            });
        });
        return { ids, matrix };
    }
    /** Converts a stable edge list to its square matrix without losing authored node order. */
    function chordEdgesToMatrix(edges, options = {}) {
        const ids = options.ids === undefined ? [] : validateChordIds(options.ids);
        const seen = new Set(ids);
        const normalized = edges.map((edge, index) => {
            const source = nonEmpty(edge.source, `$.edges[${index}].source`);
            const target = nonEmpty(edge.target, `$.edges[${index}].target`);
            const value = finite$6(edge.value, `$.edges[${index}].value`);
            if (value < 0)
                throw new GraflumeError('INVALID_DATA', 'Chord values must be non-negative.');
            for (const id of [source, target]) {
                if (options.ids !== undefined && !seen.has(id))
                    throw new GraflumeError('INVALID_DATA', `Chord edge references unknown id "${id}".`, {
                        path: `$.edges[${index}]`,
                    });
                if (!seen.has(id)) {
                    seen.add(id);
                    ids.push(id);
                }
            }
            return { source, target, value };
        });
        const indexById = new Map(ids.map((id, index) => [id, index]));
        const matrix = ids.map(() => ids.map(() => 0));
        normalized.forEach(({ source, target, value }) => {
            const sourceIndex = indexById.get(source);
            const targetIndex = indexById.get(target);
            matrix[sourceIndex][targetIndex] = matrix[sourceIndex][targetIndex] + value;
            if (options.directed !== true && source !== target)
                matrix[targetIndex][sourceIndex] = matrix[targetIndex][sourceIndex] + value;
        });
        return { ids, matrix };
    }
    /** Converts a square flow matrix to a stable edge list; undirected input must be symmetric. */
    function chordMatrixToEdges(input, options = {}) {
        const { ids, matrix } = validateChordMatrix(input);
        const directed = options.directed === true;
        if (!directed) {
            for (let row = 0; row < matrix.length; row += 1) {
                for (let column = row + 1; column < matrix.length; column += 1) {
                    if (Math.abs(matrix[row][column] - matrix[column][row]) > 1e-12)
                        throw new GraflumeError('INVALID_DATA', 'Undirected chord matrices must be symmetric; use directed: true for asymmetric flow.', { path: `$.matrix[${row}][${column}]` });
                }
            }
        }
        const edges = [];
        for (let row = 0; row < matrix.length; row += 1) {
            const start = directed ? 0 : row;
            for (let column = start; column < matrix.length; column += 1) {
                const value = matrix[row][column];
                if (value > 0)
                    edges.push({ source: ids[row], target: ids[column], value });
            }
        }
        return edges;
    }
    /** Lays out either a square matrix or an edge list with explicit direction and self-loop semantics. */
    function layoutChord(input, options = {}) {
        const matrixInput = isChordMatrixInput(input) ? validateChordMatrix(input) : null;
        const edges = matrixInput === null
            ? input
            : chordMatrixToEdges(matrixInput, options);
        const ids = matrixInput === null ? [] : [...matrixInput.ids];
        const seen = new Set(ids);
        const normalized = edges.map((edge, index) => {
            const source = nonEmpty(edge.source, `$.edges[${index}].source`);
            const target = nonEmpty(edge.target, `$.edges[${index}].target`);
            const value = finite$6(edge.value, `$.edges[${index}].value`);
            if (value < 0)
                throw new GraflumeError('INVALID_DATA', 'Chord values must be non-negative.');
            for (const id of [source, target])
                if (!seen.has(id)) {
                    seen.add(id);
                    ids.push(id);
                }
            return { source, target, value, inputIndex: index };
        });
        const matrix = matrixInput?.matrix.map((row) => [...row]) ??
            chordEdgesToMatrix(normalized, {
                ...(options.directed === undefined ? {} : { directed: options.directed }),
                ids,
            }).matrix.map((row) => [...row]);
        const totals = ids.map((id, index) => {
            const outbound = matrix[index].reduce((sum, value) => sum + value, 0);
            const inbound = matrix.reduce((sum, row) => sum + row[index], 0);
            return {
                id,
                original: index,
                outbound,
                inbound,
                value: options.directed === true ? Math.max(outbound, inbound) : outbound,
            };
        });
        if (options.groupOrder === 'ascending')
            totals.sort((a, b) => a.value - b.value || a.id.localeCompare(b.id));
        if (options.groupOrder === 'descending')
            totals.sort((a, b) => b.value - a.value || a.id.localeCompare(b.id));
        const padding = clamp$4(options.padAngle ?? 0.02, 0, Math.PI / Math.max(1, totals.length));
        const totalValue = totals.reduce((sum, group) => sum + group.value, 0) || 1;
        const available = Math.PI * 2 - padding * totals.length;
        let angle = -Math.PI / 2;
        const groups = totals.map((group, index) => {
            const startAngle = angle;
            angle += (group.value / totalValue) * available;
            const value = { ...group, index, startAngle, endAngle: angle };
            angle += padding;
            return value;
        });
        const groupById = new Map(groups.map((group) => [group.id, group]));
        const sortSubgroups = (values) => {
            values.sort((left, right) => {
                if (options.subgroupOrder === 'ascending')
                    return left.value - right.value || left.inputIndex - right.inputIndex;
                if (options.subgroupOrder === 'descending')
                    return right.value - left.value || left.inputIndex - right.inputIndex;
                return left.inputIndex - right.inputIndex;
            });
        };
        const portions = (id, value, total, cursors) => {
            const group = groupById.get(id);
            const start = cursors.get(id);
            const end = total === 0 ? start : start + ((group.endAngle - group.startAngle) * value) / total;
            cursors.set(id, end);
            return [start, end];
        };
        const endpointByEdge = new Map();
        const sourceByEdge = new Map();
        const targetByEdge = new Map();
        if (options.directed === true) {
            const outgoing = new Map();
            const incoming = new Map();
            normalized.forEach((edge) => {
                outgoing.set(edge.source, [...(outgoing.get(edge.source) ?? []), edge]);
                incoming.set(edge.target, [...(incoming.get(edge.target) ?? []), edge]);
            });
            const sourceCursors = new Map(groups.map((group) => [group.id, group.startAngle]));
            const targetCursors = new Map(groups.map((group) => [group.id, group.startAngle]));
            outgoing.forEach((values, id) => {
                sortSubgroups(values);
                const total = groupById.get(id).outbound;
                values.forEach((edge, order) => {
                    sourceByEdge.set(edge.inputIndex, {
                        interval: portions(id, edge.value, total, sourceCursors),
                        order,
                    });
                });
            });
            incoming.forEach((values, id) => {
                sortSubgroups(values);
                const total = groupById.get(id).inbound;
                values.forEach((edge, order) => {
                    targetByEdge.set(edge.inputIndex, {
                        interval: portions(id, edge.value, total, targetCursors),
                        order,
                    });
                });
            });
        }
        else {
            const incident = new Map();
            normalized.forEach((edge) => {
                incident.set(edge.source, [...(incident.get(edge.source) ?? []), edge]);
                if (edge.target !== edge.source)
                    incident.set(edge.target, [...(incident.get(edge.target) ?? []), edge]);
            });
            const cursors = new Map(groups.map((group) => [group.id, group.startAngle]));
            incident.forEach((values, id) => {
                sortSubgroups(values);
                const total = groupById.get(id).value;
                values.forEach((edge, order) => {
                    endpointByEdge.set(`${edge.inputIndex}\u0000${id}`, {
                        interval: portions(id, edge.value, total, cursors),
                        order,
                    });
                });
            });
        }
        const ribbons = normalized.map((edge) => {
            const source = options.directed === true
                ? sourceByEdge.get(edge.inputIndex)
                : endpointByEdge.get(`${edge.inputIndex}\u0000${edge.source}`);
            const target = options.directed === true
                ? targetByEdge.get(edge.inputIndex)
                : endpointByEdge.get(`${edge.inputIndex}\u0000${edge.target}`);
            return {
                source: edge.source,
                target: edge.target,
                value: edge.value,
                sourceStartAngle: source.interval[0],
                sourceEndAngle: source.interval[1],
                targetStartAngle: target.interval[0],
                targetEndAngle: target.interval[1],
                sourceSubgroupOrder: source.order,
                targetSubgroupOrder: target.order,
                directed: options.directed === true,
                selfLoop: edge.source === edge.target,
            };
        });
        return { ids, matrix, groups, ribbons };
    }
    /** Resolves funnel conversion/drop-off/cumulative semantics, neck geometry, ordering and label collisions. */
    function funnelStages(data, options = {}) {
        const stages = data.map((stage, index) => {
            const id = nonEmpty(stage.id, `$.data[${index}].id`);
            const value = finite$6(stage.value, `$.data[${index}].value`);
            if (value < 0)
                throw new GraflumeError('INVALID_DATA', 'Funnel values must be non-negative.');
            return { ...stage, id, label: stage.label?.trim() || id, value, inputIndex: index };
        });
        if (new Set(stages.map(({ id }) => id)).size !== stages.length) {
            throw new GraflumeError('INVALID_DATA', 'Funnel stage ids must be unique.');
        }
        if (options.sort === 'value-descending')
            stages.sort((a, b) => b.value - a.value || a.inputIndex - b.inputIndex);
        if (options.sort === 'order')
            stages.sort((a, b) => (a.order ?? a.inputIndex) - (b.order ?? b.inputIndex));
        const first = stages[0]?.value ?? 0;
        const maximum = Math.max(first, ...stages.map(({ value }) => value), Number.EPSILON);
        const neckWidth = clamp$4(options.neckWidth ?? 0.3, 0.02, 1);
        const neckHeight = clamp$4(options.neckHeight ?? 0.25, 0, 1);
        const labelGap = clamp$4(options.labelGap ?? 0.055, 0.01, 0.25);
        const naturalLabels = stages.map((_, index) => (index + 0.5) / Math.max(1, stages.length));
        const effectiveLabelGap = stages.length <= 1 ? 0 : Math.min(labelGap, 1 / Math.max(1, stages.length - 1));
        const labelPositions = [...naturalLabels];
        for (let index = 1; index < labelPositions.length; index += 1)
            labelPositions[index] = Math.max(labelPositions[index], labelPositions[index - 1] + effectiveLabelGap);
        if (labelPositions.length > 0) {
            labelPositions[labelPositions.length - 1] = Math.min(1, labelPositions.at(-1));
            for (let index = labelPositions.length - 2; index >= 0; index -= 1)
                labelPositions[index] = Math.min(labelPositions[index], labelPositions[index + 1] - effectiveLabelGap);
        }
        return stages.map((stage, index) => {
            const previous = stages[index - 1]?.value ?? stage.value;
            const next = stages[index + 1]?.value ?? stage.value;
            const y0 = index / Math.max(1, stages.length);
            const y1 = (index + 1) / Math.max(1, stages.length);
            const width = (value, y) => y >= 1 - neckHeight ? neckWidth : Math.max(neckWidth, value / maximum);
            const labelY = clamp$4(labelPositions[index] ?? (y0 + y1) / 2, 0, 1);
            return {
                id: stage.id,
                label: stage.label,
                value: stage.value,
                input: previous,
                output: stage.value,
                conversion: previous === 0 ? null : stage.value / previous,
                dropoff: Math.max(0, previous - stage.value),
                dropoffRate: previous === 0 ? null : Math.max(0, previous - stage.value) / previous,
                cumulative: first === 0 ? null : stage.value / first,
                topWidth: width(stage.value, y0),
                bottomWidth: width(next, y1),
                y0,
                y1,
                labelY,
            };
        });
    }
    /** Normalizes linear/log/ordinal parallel axes with missing routes, reorder/invert and linked multi-brush filtering. */
    function projectParallelRows(rows, axes, brushes = [], combine = 'intersection') {
        if (axes.length < 2)
            throw new GraflumeError('INVALID_SPEC', 'Parallel coordinates need at least two axes.');
        const fields = axes.map(({ field }, index) => nonEmpty(field, `$.axes[${index}].field`));
        if (new Set(fields).size !== fields.length)
            throw new GraflumeError('INVALID_SPEC', 'Parallel axis fields must be unique.');
        const descriptors = axes.map((axis, axisIndex) => {
            const field = fields[axisIndex];
            const type = axis.type ?? 'linear';
            const observed = rows
                .map((row) => row[field])
                .filter((value) => value !== null && value !== undefined);
            if (type === 'ordinal') {
                const domain = axis.domain?.map(String) ?? [...new Set(observed.map(String))];
                return { axis, field, type, domain };
            }
            const numeric = observed.map((value) => Number(value)).filter(Number.isFinite);
            const authored = axis.domain?.map(Number).filter(Number.isFinite);
            const domain = authored?.length === 2 ? authored : [Math.min(...numeric), Math.max(...numeric)];
            if (domain.length !== 2 || !domain.every(Number.isFinite)) {
                throw new GraflumeError('INVALID_DATA', `Parallel axis "${field}" has no finite domain.`);
            }
            if (type === 'log' && domain[0] <= 0) {
                throw new GraflumeError('INVALID_DATA', `Parallel log axis "${field}" requires positive values.`);
            }
            return { axis, field, type, domain };
        });
        const project = (descriptor, raw) => {
            if (raw === null || raw === undefined || raw === '') {
                const route = descriptor.axis.missing ?? 'gap';
                return route === 'gap' ? null : route === 'top' ? 1 : route === 'bottom' ? 0 : 0.5;
            }
            let ratio;
            if (descriptor.type === 'ordinal') {
                const index = descriptor.domain.indexOf(String(raw));
                if (index < 0)
                    return null;
                ratio = descriptor.domain.length <= 1 ? 0.5 : index / (descriptor.domain.length - 1);
            }
            else {
                const value = Number(raw);
                if (!Number.isFinite(value) || (descriptor.type === 'log' && value <= 0))
                    return null;
                const low = descriptor.type === 'log'
                    ? Math.log(descriptor.domain[0])
                    : descriptor.domain[0];
                const high = descriptor.type === 'log'
                    ? Math.log(descriptor.domain[1])
                    : descriptor.domain[1];
                const current = descriptor.type === 'log' ? Math.log(value) : value;
                ratio = high === low ? 0.5 : (current - low) / (high - low);
            }
            ratio = clamp$4(ratio, 0, 1);
            return descriptor.axis.invert === true ? 1 - ratio : ratio;
        };
        const normalizedBrushes = brushes.map((brush, index) => {
            if (!fields.includes(brush.field))
                throw new GraflumeError('INVALID_SPEC', `Unknown brush field "${brush.field}".`);
            return {
                field: brush.field,
                extents: brush.extents.map((extent, extentIndex) => {
                    const low = finite$6(extent[0], `$.brushes[${index}].extents[${extentIndex}][0]`);
                    const high = finite$6(extent[1], `$.brushes[${index}].extents[${extentIndex}][1]`);
                    return [Math.min(low, high), Math.max(low, high)];
                }),
            };
        });
        return {
            axes,
            rows: rows.map((row, index) => {
                const values = Object.fromEntries(descriptors.map((descriptor) => [
                    descriptor.field,
                    project(descriptor, row[descriptor.field]),
                ]));
                const matches = normalizedBrushes.map(({ field, extents }) => {
                    const value = values[field];
                    return (value !== null &&
                        value !== undefined &&
                        extents.some(([low, high]) => value >= low && value <= high));
                });
                return {
                    index,
                    values,
                    selected: matches.length === 0 ||
                        (combine === 'intersection' ? matches.every(Boolean) : matches.some(Boolean)),
                };
            }),
        };
    }
    function circleOverlapArea(first, second) {
        const distance = Math.hypot(first.x - second.x, first.y - second.y);
        if (distance >= first.radius + second.radius)
            return 0;
        if (distance <= Math.abs(first.radius - second.radius))
            return Math.PI * Math.min(first.radius, second.radius) ** 2;
        const a = Math.acos((distance * distance + first.radius ** 2 - second.radius ** 2) / (2 * distance * first.radius));
        const b = Math.acos((distance * distance + second.radius ** 2 - first.radius ** 2) / (2 * distance * second.radius));
        const triangle = 0.5 *
            Math.sqrt(Math.max(0, (-distance + first.radius + second.radius) *
                (distance + first.radius - second.radius) *
                (distance - first.radius + second.radius) *
                (distance + first.radius + second.radius)));
        return first.radius ** 2 * a + second.radius ** 2 * b - triangle;
    }
    function solveCircleDistance(firstRadius, secondRadius, target) {
        let low = Math.abs(firstRadius - secondRadius);
        let high = firstRadius + secondRadius;
        for (let iteration = 0; iteration < 64; iteration += 1) {
            const distance = (low + high) / 2;
            const overlap = circleOverlapArea({ x: 0, y: 0, radius: firstRadius}, { x: distance, y: 0, radius: secondRadius});
            if (overlap > target)
                low = distance;
            else
                high = distance;
        }
        return (low + high) / 2;
    }
    function circleIntersectionPoints(first, second) {
        const dx = second.x - first.x;
        const dy = second.y - first.y;
        const distance = Math.hypot(dx, dy);
        if (distance <= 1e-14 ||
            distance > first.radius + second.radius + 1e-14 ||
            distance < Math.abs(first.radius - second.radius) - 1e-14)
            return [];
        const along = (first.radius ** 2 - second.radius ** 2 + distance ** 2) / (2 * distance);
        const height = Math.sqrt(Math.max(0, first.radius ** 2 - along ** 2));
        const baseX = first.x + (along * dx) / distance;
        const baseY = first.y + (along * dy) / distance;
        const offsetX = (-dy * height) / distance;
        const offsetY = (dx * height) / distance;
        return [
            { x: baseX + offsetX, y: baseY + offsetY },
            { x: baseX - offsetX, y: baseY - offsetY },
        ];
    }
    function adaptiveSimpson(evaluate, left, right, tolerance) {
        const middle = (left + right) / 2;
        const leftValue = evaluate(left);
        const middleValue = evaluate(middle);
        const rightValue = evaluate(right);
        const estimate = ((right - left) * (leftValue + 4 * middleValue + rightValue)) / 6;
        const recurse = (start, end, startValue, centerValue, endValue, whole, epsilon, depth) => {
            const center = (start + end) / 2;
            const leftCenter = (start + center) / 2;
            const rightCenter = (center + end) / 2;
            const leftCenterValue = evaluate(leftCenter);
            const rightCenterValue = evaluate(rightCenter);
            const leftEstimate = ((center - start) * (startValue + 4 * leftCenterValue + centerValue)) / 6;
            const rightEstimate = ((end - center) * (centerValue + 4 * rightCenterValue + endValue)) / 6;
            const combined = leftEstimate + rightEstimate;
            const correction = combined - whole;
            if (depth === 0 || Math.abs(correction) <= 15 * epsilon)
                return combined + correction / 15;
            return (recurse(start, center, startValue, leftCenterValue, centerValue, leftEstimate, epsilon / 2, depth - 1) +
                recurse(center, end, centerValue, rightCenterValue, endValue, rightEstimate, epsilon / 2, depth - 1));
        };
        return recurse(left, right, leftValue, middleValue, rightValue, estimate, tolerance, 16);
    }
    function tripleCircleOverlapArea(circles, tolerance = 1e-9) {
        const insideAll = ({ x, y }) => circles.every((circle) => Math.hypot(x - circle.x, y - circle.y) <= circle.radius + 1e-12);
        const candidates = [
            ...circles.map(({ x, y }) => ({ x, y })),
            ...circleIntersectionPoints(circles[0], circles[1]),
            ...circleIntersectionPoints(circles[0], circles[2]),
            ...circleIntersectionPoints(circles[1], circles[2]),
        ];
        if (!candidates.some(insideAll))
            return 0;
        const left = Math.max(...circles.map((circle) => circle.x - circle.radius));
        const right = Math.min(...circles.map((circle) => circle.x + circle.radius));
        if (right <= left)
            return 0;
        const height = (x) => {
            let lower = Number.NEGATIVE_INFINITY;
            let upper = Number.POSITIVE_INFINITY;
            for (const circle of circles) {
                const horizontal = x - circle.x;
                if (Math.abs(horizontal) > circle.radius)
                    return 0;
                const vertical = Math.sqrt(Math.max(0, circle.radius ** 2 - horizontal ** 2));
                lower = Math.max(lower, circle.y - vertical);
                upper = Math.min(upper, circle.y + vertical);
            }
            return Math.max(0, upper - lower);
        };
        const breakpoints = [
            left,
            ...circleIntersectionPoints(circles[0], circles[1]).map(({ x }) => x),
            ...circleIntersectionPoints(circles[0], circles[2]).map(({ x }) => x),
            ...circleIntersectionPoints(circles[1], circles[2]).map(({ x }) => x),
            right,
        ]
            .filter((value) => value >= left && value <= right)
            .sort((a, b) => a - b)
            .filter((value, index, values) => index === 0 || Math.abs(value - values[index - 1]) > 1e-12);
        let area = 0;
        for (let index = 1; index < breakpoints.length; index += 1) {
            const start = breakpoints[index - 1];
            const end = breakpoints[index];
            if (end - start <= 1e-14)
                continue;
            area += adaptiveSimpson(height, start, end, tolerance / breakpoints.length);
        }
        return Math.max(0, area);
    }
    function setAtomicRegionQuality(circles, setIds, expectedCounts, areaScale, integrationTolerance = 1e-9) {
        const areas = circles.map(({ radius }) => Math.PI * radius ** 2);
        const pair = new Map();
        for (let left = 0; left < circles.length; left += 1)
            for (let right = left + 1; right < circles.length; right += 1)
                pair.set(`${left}:${right}`, circleOverlapArea(circles[left], circles[right]));
        const triple = circles.length === 3
            ? Math.min(pair.get('0:1'), pair.get('0:2'), pair.get('1:2'), tripleCircleOverlapArea(circles, integrationTolerance))
            : 0;
        const actualForMask = (mask) => {
            if (circles.length === 1)
                return areas[0];
            if (circles.length === 2) {
                const overlap = pair.get('0:1');
                if (mask === 1)
                    return areas[0] - overlap;
                if (mask === 2)
                    return areas[1] - overlap;
                return overlap;
            }
            const overlap01 = pair.get('0:1');
            const overlap02 = pair.get('0:2');
            const overlap12 = pair.get('1:2');
            if (mask === 1)
                return areas[0] - overlap01 - overlap02 + triple;
            if (mask === 2)
                return areas[1] - overlap01 - overlap12 + triple;
            if (mask === 3)
                return overlap01 - triple;
            if (mask === 4)
                return areas[2] - overlap02 - overlap12 + triple;
            if (mask === 5)
                return overlap02 - triple;
            if (mask === 6)
                return overlap12 - triple;
            return triple;
        };
        const regions = Array.from({ length: 2 ** circles.length - 1 }, (_, index) => index + 1).map((mask) => {
            const expected = (expectedCounts.get(mask) ?? 0) * areaScale;
            const actual = Math.max(0, actualForMask(mask));
            const relativeError = Math.abs(actual - expected) / Math.max(expected, areaScale);
            return {
                sets: setIds.filter((_, index) => (mask & (1 << index)) !== 0),
                expected,
                actual,
                relativeError,
            };
        });
        return {
            stress: Math.sqrt(regions.reduce((sum, { relativeError }) => sum + relativeError ** 2, 0) / regions.length),
            maximumRelativeError: Math.max(0, ...regions.map(({ relativeError }) => relativeError)),
            regions,
        };
    }
    function optimizeThreeSetCircles(circles, setIds, expectedCounts, areaScale) {
        const maximumRadius = Math.max(...circles.map(({ radius }) => radius));
        const bound = maximumRadius * 2;
        const symmetric = circles.every(({ radius }) => Math.abs(radius - circles[0].radius) <= 1e-12) &&
            (expectedCounts.get(1) ?? 0) === (expectedCounts.get(2) ?? 0) &&
            (expectedCounts.get(1) ?? 0) === (expectedCounts.get(4) ?? 0) &&
            (expectedCounts.get(3) ?? 0) === (expectedCounts.get(5) ?? 0) &&
            (expectedCounts.get(3) ?? 0) === (expectedCounts.get(6) ?? 0);
        if (symmetric) {
            const evaluateDistance = (distance) => {
                const bounded = clamp$4(distance, 0, bound);
                const candidate = [
                    { ...circles[0], x: 0, y: 0 },
                    { ...circles[1], x: bounded, y: 0 },
                    {
                        ...circles[2],
                        x: bounded / 2,
                        y: (bounded * Math.sqrt(3)) / 2,
                    },
                ];
                return {
                    circles: candidate,
                    distance: bounded,
                    quality: setAtomicRegionQuality(candidate, setIds, expectedCounts, areaScale, 1e-7),
                };
            };
            const betterDistance = (left, right) => left.quality.stress < right.quality.stress - 1e-12 ||
                (Math.abs(left.quality.stress - right.quality.stress) <= 1e-12 &&
                    (left.quality.maximumRelativeError < right.quality.maximumRelativeError - 1e-12 ||
                        (Math.abs(left.quality.maximumRelativeError - right.quality.maximumRelativeError) <=
                            1e-12 &&
                            left.distance < right.distance)));
            let best = evaluateDistance(0);
            for (let index = 1; index <= 128; index += 1) {
                const candidate = evaluateDistance((bound * index) / 128);
                if (betterDistance(candidate, best))
                    best = candidate;
            }
            let step = bound / 128;
            for (let iteration = 0; iteration < 64 && step > maximumRadius * 1e-7; iteration += 1) {
                const lower = evaluateDistance(best.distance - step);
                const upper = evaluateDistance(best.distance + step);
                if (betterDistance(lower, best))
                    best = lower;
                else if (betterDistance(upper, best))
                    best = upper;
                else
                    step /= 2;
            }
            return best.circles;
        }
        const initial = [circles[1].x, circles[2].x, Math.max(0, circles[2].y)];
        const starts = [0, 0.5, 0.75, 1, 1.25, 1.5].map((factor) => initial.map((value) => value * factor));
        starts.push([bound, 0, bound], [0, 0, 0]);
        const constrain = ([secondX, thirdX, thirdY]) => [
            clamp$4(secondX ?? 0, 0, bound),
            clamp$4(thirdX ?? 0, -bound, bound),
            clamp$4(thirdY ?? 0, 0, bound),
        ];
        const evaluate = (position) => {
            const [secondX, thirdX, thirdY] = constrain(position);
            const candidate = [
                { ...circles[0], x: 0, y: 0 },
                { ...circles[1], x: secondX, y: 0 },
                { ...circles[2], x: thirdX, y: thirdY },
            ];
            return {
                circles: candidate,
                position: [secondX, thirdX, thirdY],
                quality: setAtomicRegionQuality(candidate, setIds, expectedCounts, areaScale, 1e-7),
            };
        };
        const better = (left, right) => left.quality.stress < right.quality.stress - 1e-12 ||
            (Math.abs(left.quality.stress - right.quality.stress) <= 1e-12 &&
                (left.quality.maximumRelativeError < right.quality.maximumRelativeError - 1e-12 ||
                    (Math.abs(left.quality.maximumRelativeError - right.quality.maximumRelativeError) <=
                        1e-12 &&
                        left.position.join(',') < right.position.join(','))));
        let best = evaluate(initial);
        for (const start of starts) {
            let current = evaluate(start);
            let step = maximumRadius / 2;
            for (let iteration = 0; iteration < 96 && step > maximumRadius * 1e-5; iteration += 1) {
                let improved = false;
                for (let coordinate = 0; coordinate < 3; coordinate += 1) {
                    for (const direction of [-1, 1]) {
                        const position = [...current.position];
                        position[coordinate] = position[coordinate] + direction * step;
                        const candidate = evaluate(position);
                        if (better(candidate, current)) {
                            current = candidate;
                            improved = true;
                        }
                    }
                }
                if (!improved)
                    step /= 2;
            }
            if (better(current, best))
                best = current;
        }
        return best.circles;
    }
    /** Computes membership/intersection input plus a deterministic proportional 2/3-set Euler layout and quality metric. */
    function analyzeSets(data) {
        const setIds = [];
        const seenSets = new Set();
        const modes = new Set(data.map((datum) => ('size' in datum ? 'intersection' : 'membership')));
        if (modes.size > 1)
            throw new GraflumeError('INVALID_DATA', 'Set input must use either membership rows or pre-aggregated intersection rows.');
        const memberIds = new Set();
        const normalized = data.map((datum, index) => {
            const sets = [
                ...new Set(datum.sets.map((set, setIndex) => nonEmpty(set, `$.data[${index}].sets[${setIndex}]`))),
            ].sort();
            sets.forEach((set) => {
                if (!seenSets.has(set)) {
                    seenSets.add(set);
                    setIds.push(set);
                }
            });
            if ('size' in datum) {
                const size = finite$6(datum.size, `$.data[${index}].size`);
                if (size < 0)
                    throw new GraflumeError('INVALID_DATA', 'Set intersection sizes must be non-negative.', {
                        path: `$.data[${index}].size`,
                    });
                const members = (datum.members ?? []).map((id, memberIndex) => nonEmpty(id, `$.data[${index}].members[${memberIndex}]`));
                members.forEach((id) => {
                    if (memberIds.has(id))
                        throw new GraflumeError('INVALID_DATA', `Duplicate set member id "${id}".`);
                    memberIds.add(id);
                });
                if (members.length > size)
                    throw new GraflumeError('INVALID_DATA', 'Set intersection members cannot exceed its aggregate size.', { path: `$.data[${index}].members` });
                return { sets, size, members };
            }
            const id = nonEmpty(datum.id, `$.data[${index}].id`);
            if (memberIds.has(id))
                throw new GraflumeError('INVALID_DATA', 'Set member ids must be unique.');
            memberIds.add(id);
            return { sets, size: 1, members: [id] };
        });
        if (setIds.length > 3)
            throw new GraflumeError('INVALID_SPEC', 'Proportional Venn layout supports two or three sets; use intersection bars for more sets.');
        const sets = setIds.map((id) => ({
            id,
            size: normalized.reduce((sum, datum) => sum + (datum.sets.includes(id) ? datum.size : 0), 0),
        }));
        const combinations = new Map();
        normalized.forEach(({ size, sets, members }) => {
            const key = sets.join('\u0000');
            const previous = combinations.get(key) ?? { size: 0, members: [] };
            combinations.set(key, {
                size: previous.size + size,
                members: [...previous.members, ...members],
            });
        });
        const intersections = [...combinations.entries()].map(([key, aggregate]) => ({
            sets: key === '' ? [] : key.split('\u0000'),
            size: aggregate.size,
            members: aggregate.members,
        }));
        const areaScale = 1 / Math.max(1, ...sets.map(({ size }) => size));
        let circles = sets.map(({ id, size }, index) => ({
            id,
            x: index,
            y: 0,
            radius: Math.sqrt((size * areaScale) / Math.PI),
            size,
        }));
        const desiredOverlap = (a, b) => normalized.reduce((sum, datum) => sum + (datum.sets.includes(a) && datum.sets.includes(b) ? datum.size : 0), 0) * areaScale;
        if (circles.length >= 2)
            circles[1] = {
                ...circles[1],
                x: solveCircleDistance(circles[0].radius, circles[1].radius, desiredOverlap(circles[0].id, circles[1].id)),
            };
        if (circles.length === 3) {
            const d02 = solveCircleDistance(circles[0].radius, circles[2].radius, desiredOverlap(circles[0].id, circles[2].id));
            const d12 = solveCircleDistance(circles[1].radius, circles[2].radius, desiredOverlap(circles[1].id, circles[2].id));
            const x = circles[1].x === 0 ? 0 : (d02 ** 2 - d12 ** 2 + circles[1].x ** 2) / (2 * circles[1].x);
            const y = Math.sqrt(Math.max(0, d02 ** 2 - x ** 2));
            circles[2] = { ...circles[2], x, y };
        }
        const setIndex = new Map(setIds.map((id, index) => [id, index]));
        const expectedCounts = new Map();
        normalized.forEach(({ sets, size }) => {
            const mask = sets.reduce((value, id) => value | (1 << setIndex.get(id)), 0);
            if (mask !== 0)
                expectedCounts.set(mask, (expectedCounts.get(mask) ?? 0) + size);
        });
        if (circles.length === 3)
            circles = optimizeThreeSetCircles(circles, setIds, expectedCounts, areaScale);
        const quality = setAtomicRegionQuality(circles, setIds, expectedCounts, areaScale);
        return {
            sets,
            intersections: intersections.sort((a, b) => b.size - a.size || a.sets.join().localeCompare(b.sets.join())),
            circles,
            quality,
        };
    }
    /** Exact membership query used by Venn/Euler region hit testing and linked filters. */
    function querySetRegion(data, included, excluded = []) {
        const required = new Set(included);
        const forbidden = new Set(excluded);
        return data
            .filter(({ sets }) => [...required].every((set) => sets.includes(set)) &&
            [...forbidden].every((set) => !sets.includes(set)))
            .map(({ id }) => id);
    }
    /** Returns the exact inside/outside circle signature for pointer-based region hit testing. */
    function hitSetRegion(circles, point) {
        finite$6(point.x, '$.point.x');
        finite$6(point.y, '$.point.y');
        return circles
            .filter((circle) => Math.hypot(point.x - circle.x, point.y - circle.y) <= circle.radius)
            .map(({ id }) => id);
    }
    function stemEnglish(token) {
        if (token.length > 5 && token.endsWith('ing'))
            return token.slice(0, -3);
        if (token.length > 4 && token.endsWith('ed'))
            return token.slice(0, -2);
        if (token.length > 4 && token.endsWith('es'))
            return token.slice(0, -2);
        if (token.length > 3 && token.endsWith('s'))
            return token.slice(0, -1);
        return token;
    }
    /** Shared Unicode tokenizer/stopword/n-gram/stemming contract for Word tree and Word cloud. */
    function tokenizeWords(text, options = {}) {
        const ngram = Math.floor(options.ngram ?? 1);
        if (ngram < 1 || ngram > 8)
            throw new GraflumeError('INVALID_SPEC', '$.ngram must be from 1 to 8.');
        let tokens = [...(text.normalize('NFKC').match(/[\p{L}\p{N}_'-]+/gu) ?? [])];
        if (options.case === 'upper')
            tokens = tokens.map((token) => token.toLocaleUpperCase(options.locale));
        else if (options.case !== 'preserve')
            tokens = tokens.map((token) => token.toLocaleLowerCase(options.locale));
        if (options.stemming === 'simple-en')
            tokens = tokens.map(stemEnglish);
        const stopwords = new Set((options.stopwords ?? []).map((value) => value.toLocaleLowerCase(options.locale)));
        tokens = tokens.filter((token) => !stopwords.has(token.toLocaleLowerCase(options.locale)));
        if (ngram === 1)
            return tokens;
        return Array.from({ length: Math.max(0, tokens.length - ngram + 1) }, (_, index) => tokens.slice(index, index + ngram).join(' '));
    }
    /** Builds prefix, suffix, or reverse phrase trees with deterministic aggregation and pruning. */
    function buildWordTree(texts, rootPhrase, options = {}) {
        const rootTokens = tokenizeWords(rootPhrase, { ...options, ngram: 1 });
        if (rootTokens.length === 0)
            throw new GraflumeError('INVALID_SPEC', '$.rootPhrase must contain a token.');
        const direction = options.direction ?? 'prefix';
        const maximumDepth = clamp$4(Math.floor(options.maximumDepth ?? 6), 1, 32);
        const minimumCount = clamp$4(Math.floor(options.minimumCount ?? 1), 1, Number.MAX_SAFE_INTEGER);
        const maximumChildren = clamp$4(Math.floor(options.maximumChildren ?? 12), 1, 100);
        const counts = new Map();
        const rootKey = rootTokens.join(' ');
        counts.set(rootKey, {
            key: rootKey,
            token: rootTokens.at(-1),
            phrase: rootKey,
            depth: 0,
            parent: null,
            count: 0,
        });
        texts.forEach((text) => {
            const tokens = [...tokenizeWords(text, { ...options, ngram: 1 })];
            const backward = direction === 'suffix' || direction === 'reverse';
            const needle = backward ? [...rootTokens].reverse() : rootTokens;
            const source = backward ? [...tokens].reverse() : tokens;
            for (let index = 0; index <= source.length - needle.length; index += 1) {
                if (!needle.every((token, offset) => source[index + offset] === token))
                    continue;
                counts.get(rootKey).count += 1;
                let parent = rootKey;
                const phrase = [...needle];
                for (let depth = 1; depth <= maximumDepth; depth += 1) {
                    const token = source[index + needle.length + depth - 1];
                    if (token === undefined)
                        break;
                    phrase.push(token);
                    const key = phrase.join('\u0000');
                    const entry = counts.get(key) ?? {
                        key,
                        token,
                        phrase: (backward ? [...phrase].reverse() : phrase).join(' '),
                        depth,
                        parent,
                        count: 0,
                    };
                    entry.count += 1;
                    counts.set(key, entry);
                    parent = key;
                }
            }
        });
        const kept = new Map();
        for (const [key, value] of counts) {
            if (key === rootKey || value.count >= minimumCount) {
                const children = kept.get(value.parent ?? '') ?? [];
                children.push(value);
                kept.set(value.parent ?? '', children);
            }
        }
        const output = [];
        const visit = (entry, id, parent) => {
            output.push({
                id,
                token: entry.token,
                phrase: entry.phrase,
                count: entry.count,
                depth: entry.depth,
                parent,
            });
            const children = [...(kept.get(entry.key) ?? [])]
                .sort((a, b) => b.count - a.count || a.token.localeCompare(b.token))
                .slice(0, maximumChildren);
            children.forEach((child, index) => visit(child, `${id}/${encodeURIComponent(child.token)}-${index}`, id));
        };
        visit(counts.get(rootKey), rootKey, null);
        return output;
    }
    /** Deterministic bounded spiral word-cloud layout with collision padding and allowed rotations. */
    function layoutWordCloud(texts, options = {}) {
        const width = finite$6(options.width ?? 640, '$.width');
        const height = finite$6(options.height ?? 360, '$.height');
        if (width <= 0 || height <= 0)
            throw new GraflumeError('INVALID_SPEC', 'Word-cloud dimensions must be positive.');
        const padding = clamp$4(finite$6(options.padding ?? 2, '$.padding'), 0, 64);
        const rotations = options.rotations ?? [0];
        if (rotations.length === 0 || rotations.some((value) => !Number.isFinite(value)))
            throw new GraflumeError('INVALID_SPEC', '$.rotations must contain finite angles.');
        const frequencies = new Map();
        texts.forEach((text) => tokenizeWords(text, options).forEach((token) => frequencies.set(token, (frequencies.get(token) ?? 0) + 1)));
        const minimum = clamp$4(Math.floor(options.minimumFrequency ?? 1), 1, Number.MAX_SAFE_INTEGER);
        const maximumWords = clamp$4(Math.floor(options.maximumWords ?? 200), 1, 2_000);
        const words = [...frequencies]
            .filter(([, count]) => count >= minimum)
            .sort((a, b) => b[1] - a[1] || a[0].localeCompare(b[0]))
            .slice(0, maximumWords);
        if (words.length === 0)
            return [];
        const maximum = words[0][1];
        const minimumCount = words.at(-1)[1];
        const random = seededRandom$1(options.seed ?? 1);
        const spiralStep = Math.max(1.2, Math.min(width, height) / 90);
        const placements = [];
        const overlaps = (candidate) => placements.some((placed) => Math.abs(candidate.x - placed.x) * 2 < candidate.width + placed.width + padding * 2 &&
            Math.abs(candidate.y - placed.y) * 2 < candidate.height + placed.height + padding * 2);
        words.forEach(([word, frequency], wordIndex) => {
            const ratio = maximum === minimumCount ? 0.6 : (frequency - minimumCount) / (maximum - minimumCount);
            let fontSize = 12 + Math.sqrt(Math.max(0, ratio)) * 42;
            const rotation = rotations[Math.floor(random() * rotations.length)];
            const radians = (rotation * Math.PI) / 180;
            const cosine = Math.abs(Math.cos(radians));
            const sine = Math.abs(Math.sin(radians));
            for (let shrink = 0; shrink < 8; shrink += 1) {
                const textWidth = Math.max(fontSize * 0.6, word.length * fontSize * 0.58);
                const textHeight = fontSize * 1.15;
                const boxWidth = textWidth * cosine + textHeight * sine;
                const boxHeight = textWidth * sine + textHeight * cosine;
                for (let step = 0; step < 4_000; step += 1) {
                    const angle = wordIndex * 0.37 + step * 0.31;
                    const radius = spiralStep * Math.sqrt(step);
                    const candidate = {
                        word,
                        frequency,
                        x: width / 2 + Math.cos(angle) * radius,
                        y: height / 2 + Math.sin(angle) * radius * 0.62,
                        width: boxWidth,
                        height: boxHeight,
                        fontSize,
                        rotation,
                    };
                    const inside = candidate.x - boxWidth / 2 >= 0 &&
                        candidate.x + boxWidth / 2 <= width &&
                        candidate.y - boxHeight / 2 >= 0 &&
                        candidate.y + boxHeight / 2 <= height;
                    if (inside && !overlaps(candidate)) {
                        placements.push(candidate);
                        return;
                    }
                }
                fontSize *= 0.88;
            }
        });
        return placements;
    }

    function finite$5(value, path) {
        if (typeof value !== 'number' || !Number.isFinite(value)) {
            throw new GraflumeError('INVALID_DATA', `${path} must be a finite number.`, { path });
        }
        return value;
    }
    function name(value, path) {
        const normalized = value.trim();
        if (normalized === '')
            throw new GraflumeError('INVALID_DATA', `${path} must be non-empty.`, { path });
        return normalized;
    }
    function clamp$3(value, minimum, maximum) {
        return Math.max(minimum, Math.min(maximum, value));
    }
    function seededRandom(seed) {
        let state = Math.floor(seed) >>> 0;
        return () => {
            state = (Math.imul(state, 1_664_525) + 1_013_904_223) >>> 0;
            return state / 0x1_0000_0000;
        };
    }
    function networkCycles(nodes, edges) {
        const outgoing = new Map(nodes.map((id) => [id, []]));
        edges.forEach(({ source, target, directed }) => {
            outgoing.get(source)?.push(target);
            if (!directed && source !== target)
                outgoing.get(target)?.push(source);
        });
        const cycles = [];
        const active = new Set();
        const visited = new Set();
        const path = [];
        const walk = (id, parent) => {
            if (active.has(id)) {
                const offset = path.indexOf(id);
                const cycle = [...path.slice(Math.max(0, offset)), id];
                const signature = [...new Set(cycle)].sort().join('\u0000');
                if (!cycles.some((existing) => [...new Set(existing)].sort().join('\u0000') === signature))
                    cycles.push(cycle);
                return;
            }
            if (visited.has(id))
                return;
            active.add(id);
            path.push(id);
            for (const next of outgoing.get(id) ?? [])
                if (next !== parent || edges.some((edge) => edge.directed))
                    walk(next, id);
            path.pop();
            active.delete(id);
            visited.add(id);
        };
        nodes.forEach((id) => walk(id, null));
        return cycles;
    }
    function topologicalOrder(nodes, edges) {
        const indegree = new Map(nodes.map((id) => [id, 0]));
        const outgoing = new Map(nodes.map((id) => [id, []]));
        edges
            .filter(({ directed, source, target }) => directed && source !== target)
            .forEach(({ source, target }) => {
            indegree.set(target, (indegree.get(target) ?? 0) + 1);
            outgoing.get(source)?.push(target);
        });
        const queue = nodes.filter((id) => indegree.get(id) === 0).sort();
        const output = [];
        while (queue.length > 0) {
            const id = queue.shift();
            output.push(id);
            for (const target of outgoing.get(id) ?? []) {
                indegree.set(target, (indegree.get(target) ?? 1) - 1);
                if (indegree.get(target) === 0) {
                    queue.push(target);
                    queue.sort();
                }
            }
        }
        nodes
            .filter((id) => !output.includes(id))
            .sort()
            .forEach((id) => output.push(id));
        return output;
    }
    function collapseNetwork(nodes, edges, collapsed) {
        const byId = new Map(nodes.map((node) => [node.id, node]));
        const owner = (id) => {
            let current = byId.get(id);
            const visited = new Set();
            while (current?.parent !== null && current?.parent !== undefined && !visited.has(current.id)) {
                visited.add(current.id);
                if (collapsed.has(current.parent))
                    return current.parent;
                current = byId.get(current.parent);
            }
            return id;
        };
        const visibleNodes = nodes.filter((node) => owner(node.id) === node.id);
        visibleNodes.forEach((node) => {
            node.hiddenCount = nodes.filter((other) => other.id !== node.id && owner(other.id) === node.id).length;
        });
        const aggregated = new Map();
        edges.forEach((edge) => {
            const source = owner(edge.source);
            const target = owner(edge.target);
            const changed = source !== edge.source || target !== edge.target;
            const key = changed
                ? `collapsed\u0000${source}\u0000${target}\u0000${edge.directed}`
                : `edge\u0000${edge.id}`;
            const previous = aggregated.get(key);
            if (previous === undefined) {
                aggregated.set(key, {
                    ...edge,
                    id: changed ? `collapsed:${source}:${target}` : edge.id,
                    source,
                    target,
                    sourcePort: source === edge.source ? edge.sourcePort : null,
                    targetPort: target === edge.target ? edge.targetPort : null,
                });
            }
            else
                previous.weight += edge.weight;
        });
        return { nodes: visibleNodes, edges: [...aggregated.values()] };
    }
    function layoutGrid(nodes) {
        const columns = Math.max(1, Math.ceil(Math.sqrt(nodes.length)));
        const rows = Math.max(1, Math.ceil(nodes.length / columns));
        nodes.forEach((node, index) => {
            if (node.pinned)
                return;
            node.x = columns === 1 ? 0.5 : (index % columns) / (columns - 1);
            node.y = rows === 1 ? 0.5 : Math.floor(index / columns) / (rows - 1);
        });
    }
    function layoutRadial(nodes, edges) {
        const degree = new Map(nodes.map(({ id }) => [id, 0]));
        edges.forEach(({ source, target }) => {
            degree.set(source, (degree.get(source) ?? 0) + 1);
            degree.set(target, (degree.get(target) ?? 0) + 1);
        });
        const ordered = [...nodes].sort((a, b) => (degree.get(b.id) ?? 0) - (degree.get(a.id) ?? 0) || a.id.localeCompare(b.id));
        const center = ordered[0];
        if (center !== undefined && !center.pinned) {
            center.x = 0.5;
            center.y = 0.5;
        }
        ordered.slice(1).forEach((node, index, values) => {
            if (node.pinned)
                return;
            const angle = -Math.PI / 2 + (index / Math.max(1, values.length)) * Math.PI * 2;
            const ring = 0.36 + 0.08 * (index % 2);
            node.x = 0.5 + Math.cos(angle) * ring;
            node.y = 0.5 + Math.sin(angle) * ring;
        });
    }
    function layoutDag(nodes, edges) {
        const order = topologicalOrder(nodes.map(({ id }) => id), edges);
        const rank = new Map(order.map((id) => [id, 0]));
        for (let iteration = 0; iteration < nodes.length; iteration += 1) {
            let changed = false;
            edges
                .filter(({ directed }) => directed)
                .forEach(({ source, target }) => {
                const next = Math.max(rank.get(target) ?? 0, (rank.get(source) ?? 0) + 1);
                if (next !== rank.get(target)) {
                    rank.set(target, next);
                    changed = true;
                }
            });
            if (!changed)
                break;
        }
        const maximum = Math.max(...rank.values(), 0);
        const groups = new Map();
        nodes.forEach((node) => {
            const value = rank.get(node.id) ?? 0;
            const list = groups.get(value) ?? [];
            list.push(node);
            groups.set(value, list);
        });
        groups.forEach((values, value) => values
            .sort((a, b) => order.indexOf(a.id) - order.indexOf(b.id))
            .forEach((node, index) => {
            if (node.pinned)
                return;
            node.x = maximum === 0 ? 0.5 : value / maximum;
            node.y = (index + 1) / (values.length + 1);
        }));
        return order;
    }
    function layoutForce(nodes, edges, iterations, spacing, seed) {
        const random = seededRandom(seed);
        nodes.forEach((node) => {
            if (!Number.isFinite(node.x))
                node.x = 0.1 + random() * 0.8;
            if (!Number.isFinite(node.y))
                node.y = 0.1 + random() * 0.8;
        });
        const byId = new Map(nodes.map((node) => [node.id, node]));
        const velocity = new Map(nodes.map(({ id }) => [id, { x: 0, y: 0 }]));
        for (let iteration = 0; iteration < iterations; iteration += 1) {
            const temperature = 0.025 * (1 - iteration / Math.max(1, iterations));
            for (let left = 0; left < nodes.length; left += 1)
                for (let right = left + 1; right < nodes.length; right += 1) {
                    const a = nodes[left];
                    const b = nodes[right];
                    let dx = a.x - b.x;
                    let dy = a.y - b.y;
                    let distance = Math.hypot(dx, dy);
                    if (distance < 1e-6) {
                        dx = (random() - 0.5) * 0.01;
                        dy = (random() - 0.5) * 0.01;
                        distance = Math.hypot(dx, dy);
                    }
                    const force = Math.min(0.02, (spacing * spacing) / (distance * distance));
                    velocity.get(a.id).x += (dx / distance) * force;
                    velocity.get(a.id).y += (dy / distance) * force;
                    velocity.get(b.id).x -= (dx / distance) * force;
                    velocity.get(b.id).y -= (dy / distance) * force;
                }
            edges.forEach(({ source, target, weight }) => {
                const a = byId.get(source);
                const b = byId.get(target);
                const dx = b.x - a.x;
                const dy = b.y - a.y;
                const distance = Math.max(1e-6, Math.hypot(dx, dy));
                const force = (distance - spacing * 4) * 0.035 * Math.sqrt(Math.max(weight, 0.01));
                velocity.get(a.id).x += (dx / distance) * force;
                velocity.get(a.id).y += (dy / distance) * force;
                velocity.get(b.id).x -= (dx / distance) * force;
                velocity.get(b.id).y -= (dy / distance) * force;
            });
            nodes.forEach((node) => {
                if (node.pinned)
                    return;
                const current = velocity.get(node.id);
                current.x *= 0.72;
                current.y *= 0.72;
                node.x = clamp$3(node.x + clamp$3(current.x, -temperature, temperature), 0.03, 0.97);
                node.y = clamp$3(node.y + clamp$3(current.y, -temperature, temperature), 0.03, 0.97);
            });
        }
    }
    function portPosition(node, portId, target) {
        if (portId !== null) {
            const port = node.ports.find(({ id }) => id === portId);
            const angle = ((port.angle ?? 0) * Math.PI) / 180;
            return { x: node.x + Math.cos(angle) * node.radius, y: node.y + Math.sin(angle) * node.radius };
        }
        const angle = Math.atan2(target.y - node.y, target.x - node.x);
        return { x: node.x + Math.cos(angle) * node.radius, y: node.y + Math.sin(angle) * node.radius };
    }
    function edgePoints(source, target, sourcePort, targetPort, routing, parallelIndex, parallelCount) {
        if (source.id === target.id) {
            const radius = source.radius * (2.5 + parallelIndex * 0.8);
            return [
                { x: source.x + source.radius, y: source.y },
                { x: source.x + radius, y: source.y - radius },
                { x: source.x - radius, y: source.y - radius },
                { x: source.x - source.radius, y: source.y },
            ];
        }
        const start = portPosition(source, sourcePort, target);
        const end = portPosition(target, targetPort, source);
        if (routing === 'straight' && parallelCount === 1)
            return [start, end];
        const offset = (parallelIndex - (parallelCount - 1) / 2) * 0.035;
        if (routing === 'orthogonal') {
            const dx = end.x - start.x;
            const dy = end.y - start.y;
            if (Math.abs(dx) >= Math.abs(dy)) {
                const middle = (start.y + end.y) / 2 + offset;
                return [start, { x: start.x, y: middle }, { x: end.x, y: middle }, end];
            }
            const middle = (start.x + end.x) / 2 + offset;
            return [start, { x: middle, y: start.y }, { x: middle, y: end.y }, end];
        }
        const dx = end.x - start.x;
        const dy = end.y - start.y;
        const length = Math.max(1e-6, Math.hypot(dx, dy));
        return [
            start,
            {
                x: (start.x + end.x) / 2 - (dy / length) * offset,
                y: (start.y + end.y) / 2 + (dx / length) * offset,
            },
            end,
        ];
    }
    /** Validates and lays out directed, multiedge, self-loop, compound and port-aware networks. */
    function layoutNetwork(nodeInput, edgeInput, options = {}) {
        const nodes = nodeInput.map((node, index) => {
            const id = name(node.id, `$.nodes[${index}].id`);
            const radius = finite$5(node.radius ?? 0.025, `$.nodes[${index}].radius`);
            if (radius <= 0 || radius > 0.25)
                throw new GraflumeError('INVALID_DATA', 'Network node radius must be in (0, 0.25].');
            const ports = (node.ports ?? []).map((port, portIndex) => ({
                id: name(port.id, `$.nodes[${index}].ports[${portIndex}].id`),
                ...(port.angle === undefined
                    ? {}
                    : { angle: finite$5(port.angle, `$.nodes[${index}].ports[${portIndex}].angle`) }),
            }));
            if (new Set(ports.map(({ id }) => id)).size !== ports.length)
                throw new GraflumeError('INVALID_DATA', `Network node "${id}" has duplicate ports.`);
            return {
                id,
                parent: node.parent == null || node.parent.trim() === '' ? null : node.parent.trim(),
                group: node.group?.trim() || null,
                radius,
                ports,
                x: node.x === undefined ? Number.NaN : finite$5(node.x, `$.nodes[${index}].x`),
                y: node.y === undefined ? Number.NaN : finite$5(node.y, `$.nodes[${index}].y`),
                pinned: node.pinned === true,
                hiddenCount: 0,
            };
        });
        if (new Set(nodes.map(({ id }) => id)).size !== nodes.length)
            throw new GraflumeError('INVALID_DATA', 'Network node ids must be unique.');
        const byId = new Map(nodes.map((node) => [node.id, node]));
        nodes.forEach((node) => {
            if (node.parent !== null && !byId.has(node.parent))
                throw new GraflumeError('INVALID_DATA', `Unknown compound parent "${node.parent}".`);
            if (node.parent === node.id)
                throw new GraflumeError('INVALID_DATA', `Network node "${node.id}" cannot be its own compound parent.`);
            if (node.pinned && (!Number.isFinite(node.x) || !Number.isFinite(node.y)))
                throw new GraflumeError('INVALID_DATA', `Pinned node "${node.id}" needs x and y.`);
        });
        const parentState = new Map();
        const parentStack = [];
        const visitParent = (id) => {
            const state = parentState.get(id);
            if (state === 'visited')
                return;
            if (state === 'visiting') {
                const start = parentStack.indexOf(id);
                const cycle = [...parentStack.slice(Math.max(0, start)), id];
                throw new GraflumeError('INVALID_DATA', `Compound parent cycle detected: ${cycle.join(' -> ')}.`);
            }
            parentState.set(id, 'visiting');
            parentStack.push(id);
            const parent = byId.get(id)?.parent;
            if (parent !== null && parent !== undefined)
                visitParent(parent);
            parentStack.pop();
            parentState.set(id, 'visited');
        };
        nodes.forEach(({ id }) => visitParent(id));
        const edges = edgeInput.map((edge, index) => {
            const source = name(edge.source, `$.edges[${index}].source`);
            const target = name(edge.target, `$.edges[${index}].target`);
            if (!byId.has(source) || !byId.has(target))
                throw new GraflumeError('INVALID_DATA', `Network edge ${index} references an unknown node.`);
            const sourcePort = edge.sourcePort?.trim() || null;
            const targetPort = edge.targetPort?.trim() || null;
            if (sourcePort !== null && !byId.get(source).ports.some(({ id }) => id === sourcePort))
                throw new GraflumeError('INVALID_DATA', `Unknown source port "${sourcePort}".`);
            if (targetPort !== null && !byId.get(target).ports.some(({ id }) => id === targetPort))
                throw new GraflumeError('INVALID_DATA', `Unknown target port "${targetPort}".`);
            return {
                id: edge.id?.trim() || `edge-${index}`,
                source,
                target,
                sourcePort,
                targetPort,
                weight: finite$5(edge.weight ?? 1, `$.edges[${index}].weight`),
                directed: edge.directed ?? options.directed ?? false,
            };
        });
        if (new Set(edges.map(({ id }) => id)).size !== edges.length)
            throw new GraflumeError('INVALID_DATA', 'Network edge ids must be unique.');
        if (edges.some(({ weight }) => weight < 0))
            throw new GraflumeError('INVALID_DATA', 'Network edge weights must be non-negative.');
        if (options.allowSelfLoops === false && edges.some(({ source, target }) => source === target))
            throw new GraflumeError('INVALID_DATA', 'Self-loop edges are disabled.');
        if (options.allowMultiedges === false) {
            const signatures = edges.map(({ source, target, directed }) => directed ? `${source}\u0000${target}` : [source, target].sort().join('\u0000'));
            if (new Set(signatures).size !== signatures.length)
                throw new GraflumeError('INVALID_DATA', 'Multiedges are disabled.');
        }
        const collapsed = new Set(options.collapsed ?? []);
        collapsed.forEach((id) => {
            if (!byId.has(id))
                throw new GraflumeError('INVALID_SPEC', `Unknown collapsed node "${id}".`);
        });
        const visible = collapseNetwork(nodes, edges, collapsed);
        const layout = options.layout ?? 'force';
        let order = [];
        if (layout === 'grid')
            layoutGrid(visible.nodes);
        else if (layout === 'radial')
            layoutRadial(visible.nodes, visible.edges);
        else if (layout === 'dag')
            order = layoutDag(visible.nodes, visible.edges);
        else {
            const iterations = Math.floor(options.iterations ?? 120);
            if (iterations < 0 || iterations > 2_000)
                throw new GraflumeError('INVALID_SPEC', '$.iterations must be from 0 to 2000.');
            layoutForce(visible.nodes, visible.edges, iterations, clamp$3(options.nodeSpacing ?? 0.08, 0.01, 0.4), options.seed ?? 1);
        }
        if (order.length === 0)
            order = topologicalOrder(visible.nodes.map(({ id }) => id), visible.edges);
        const visibleById = new Map(visible.nodes.map((node) => [node.id, node]));
        const groups = new Map();
        visible.edges.forEach((edge) => {
            const key = edge.directed
                ? `${edge.source}\u0000${edge.target}`
                : [edge.source, edge.target].sort().join('\u0000');
            const values = groups.get(key) ?? [];
            values.push(edge);
            groups.set(key, values);
        });
        const routing = options.routing ?? 'quadratic';
        const outputEdges = [...groups.values()].flatMap((values) => values.map((edge, index) => ({
            ...edge,
            parallelIndex: index,
            parallelCount: values.length,
            selfLoop: edge.source === edge.target,
            points: edgePoints(visibleById.get(edge.source), visibleById.get(edge.target), edge.sourcePort, edge.targetPort, routing, index, values.length),
        })));
        return {
            layout,
            topologicalOrder: order,
            cycles: networkCycles(visible.nodes.map(({ id }) => id), visible.edges),
            nodes: visible.nodes.map((node) => ({
                id: node.id,
                parent: node.parent,
                group: node.group,
                x: node.x,
                y: node.y,
                radius: node.radius,
                pinned: node.pinned,
                compound: nodes.some(({ parent }) => parent === node.id),
                collapsed: collapsed.has(node.id),
                hiddenCount: node.hiddenCount,
                ports: node.ports.map((port) => {
                    const angle = ((port.angle ?? 0) * Math.PI) / 180;
                    return {
                        id: port.id,
                        x: node.x + Math.cos(angle) * node.radius,
                        y: node.y + Math.sin(angle) * node.radius,
                    };
                }),
            })),
            edges: outputEdges,
        };
    }
    function pointInPolygon(point, polygon) {
        let inside = false;
        for (let current = 0, previous = polygon.length - 1; current < polygon.length; previous = current, current += 1) {
            const a = polygon[current];
            const b = polygon[previous];
            if (a.y > point.y !== b.y > point.y &&
                point.x < ((b.x - a.x) * (point.y - a.y)) / (b.y - a.y) + a.x)
                inside = !inside;
        }
        return inside;
    }
    /** Polygon/lasso selection over laid-out nodes, suitable for linked selection stores. */
    function selectNetworkNodes(result, polygon) {
        if (polygon.length < 3)
            throw new GraflumeError('INVALID_SPEC', 'Network lasso needs at least three points.');
        polygon.forEach((point, index) => {
            finite$5(point.x, `$.polygon[${index}].x`);
            finite$5(point.y, `$.polygon[${index}].y`);
        });
        return result.nodes.filter(({ x, y }) => pointInPolygon({ x, y }, polygon)).map(({ id }) => id);
    }
    /** Returns a new serializable node array after drag/pin without mutating source data. */
    function moveNetworkNode(nodes, id, position, pin = true) {
        const x = finite$5(position.x, '$.position.x');
        const y = finite$5(position.y, '$.position.y');
        if (!nodes.some((node) => node.id === id))
            throw new GraflumeError('INVALID_SPEC', `Unknown network node "${id}".`);
        return nodes.map((node) => (node.id === id ? { ...node, x, y, pinned: pin } : { ...node }));
    }

    function optionString$1(options, name) {
        const value = options[name];
        return typeof value === 'string' ? value : undefined;
    }
    function textNode$3(id, x, y, text, context, options = {}) {
        return {
            type: 'text',
            ...nodeBase(id, { zIndex: context.layer.zIndex + 2 }),
            x,
            y,
            text,
            fill: options.fill ?? context.theme.colors.text,
            fontFamily: context.theme.typography.fontFamily,
            fontSize: options.size ?? context.theme.typography.fontSize,
            fontWeight: options.weight ?? 500,
            align: options.align ?? 'center',
            baseline: options.baseline ?? 'middle',
            rotation: 0,
        };
    }
    const compileCalendarMark = (context) => {
        const { table, layer, plot, theme, performance } = context;
        const values = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const rawDate = table.value(rowIndex, layer.x.field);
            const date = rawDate instanceof Date ? rawDate : new Date(String(rawDate));
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            if (!Number.isFinite(date.getTime()) || value === null)
                continue;
            values.push({ rowIndex, date, value });
        }
        if (values.length === 0)
            return [];
        values.sort((left, right) => left.date.getTime() - right.date.getTime());
        const minimum = Math.min(...values.map((item) => item.value));
        const maximum = Math.max(...values.map((item) => item.value));
        const first = values[0]?.date;
        if (first === undefined)
            return [];
        const calendarYear = first.getUTCFullYear();
        const start = new Date(Date.UTC(calendarYear, 0, 1));
        const day = 24 * 60 * 60 * 1000;
        const weekCount = Math.max(1, Math.ceil((Math.max(...values.map((item) => item.date.getTime())) - start.getTime()) / day / 7) + 1);
        const gap = 2;
        const cell = Math.max(3, Math.min((plot.width - 36) / weekCount - gap, (plot.height - 34) / 7 - gap));
        const originX = plot.x + 34;
        const originY = plot.y + 20;
        const nodes = [];
        const monthPositions = new Map();
        values.forEach((item) => {
            const offset = Math.floor((item.date.getTime() - start.getTime()) / day);
            const week = Math.floor((offset + start.getUTCDay()) / 7);
            const month = item.date.getUTCMonth();
            if (!monthPositions.has(month))
                monthPositions.set(month, week);
        });
        const monthFormatter = new Intl.DateTimeFormat('en', { month: 'short', timeZone: 'UTC' });
        monthPositions.forEach((week, month) => {
            nodes.push(textNode$3(`${layer.id}:month:${month}`, originX + week * (cell + gap), plot.y + 7, monthFormatter.format(new Date(Date.UTC(calendarYear, month, 1))), context, { align: 'left', size: 9, weight: 650, fill: theme.colors.mutedText }));
        });
        ['S', 'M', 'T', 'W', 'T', 'F', 'S'].forEach((label, index) => nodes.push(textNode$3(`${layer.id}:weekday:${index}`, plot.x + 18, originY + index * (cell + gap) + cell / 2, label, context, { size: 9 })));
        values.forEach((item) => {
            const offset = Math.floor((item.date.getTime() - start.getTime()) / day);
            const week = Math.floor((offset + start.getUTCDay()) / 7);
            const weekday = item.date.getUTCDay();
            const ratio = maximum === minimum ? 0.6 : (item.value - minimum) / (maximum - minimum);
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:day:${item.rowIndex}`, {
                    zIndex: layer.zIndex,
                    opacity: layer.mark.opacity,
                    interactive: performance.enableHitTesting,
                    datum: { layerId: layer.id, rowIndex: item.rowIndex, datum: table.row(item.rowIndex) },
                }),
                x: originX + week * (cell + gap),
                y: originY + weekday * (cell + gap),
                width: cell,
                height: cell,
                fill: layer.mark.fill ?? mappedContinuousColor(theme, ratio, 'endpoints'),
                stroke: theme.colors.background,
                lineWidth: 0.5,
                cornerRadius: Math.min(2, cell * 0.15),
            });
        });
        return nodes;
    };
    const compileGeoMark = (context) => {
        const { table, layer, plot, theme, performance } = context;
        const nodes = worldBasemapNodes(context);
        const rows = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const region = String(table.value(rowIndex, layer.x.field) ?? '').trim();
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            const country = naturalEarthCountry(region);
            if (country === undefined || value === null)
                continue;
            rows.push({ rowIndex, value, country });
        }
        if (rows.length === 0)
            return nodes;
        const minimum = Math.min(...rows.map(({ value }) => value));
        const maximum = Math.max(...rows.map(({ value }) => value));
        const mode = optionString$1(layer.mark.options, 'mode') ?? 'bubble';
        for (const { rowIndex, value, country } of rows) {
            const ratio = maximum === minimum ? 0.6 : (value - minimum) / (maximum - minimum);
            const fill = layer.mark.fill ??
                themedPointFill(theme, context.color, theme.mark.defaultColor ?? theme.colors.focus);
            if (mode === 'choropleth') {
                nodes.push(...worldCountryOverlayNodes(context, country, rowIndex, layer.mark.fill ?? mappedContinuousColor(theme, ratio, 'endpoints')));
                continue;
            }
            const point = projectGeographicPosition(plot, country[5], country[6]);
            const radius = 5 + Math.sqrt(Math.max(0, ratio)) * 12;
            nodes.push({
                type: 'circle',
                ...nodeBase(`${layer.id}:region-halo:${rowIndex}`, {
                    zIndex: layer.zIndex - 0.1,
                }),
                cx: point.x,
                cy: point.y,
                radius: radius + 4,
                fill: colorWithOpacity(fill, theme.mode === 'dark' ? 0.2 : 0.14),
                lineWidth: 0,
            });
            nodes.push({
                type: 'circle',
                ...nodeBase(`${layer.id}:region:${rowIndex}`, {
                    zIndex: layer.zIndex,
                    opacity: layer.mark.opacity,
                    interactive: performance.enableHitTesting,
                    datum: { layerId: layer.id, rowIndex, datum: table.row(rowIndex) },
                }),
                cx: point.x,
                cy: point.y,
                radius,
                fill,
                stroke: theme.colors.background,
                lineWidth: 1.5,
            });
        }
        return nodes;
    };
    const compileMapMark = (context) => {
        const { table, layer, plot, theme, performance } = context;
        const nodes = worldBasemapNodes(context);
        const sizeField = layer.mark.fields.size;
        const extent = sizeField === undefined || !table.has(sizeField) ? null : table.extent(sizeField);
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const longitude = numericDataValue(table.value(rowIndex, layer.x.field));
            const latitude = numericDataValue(table.value(rowIndex, layer.y.field));
            if (longitude === null || latitude === null || !isGeographicPosition(longitude, latitude))
                continue;
            const rawSize = sizeField === undefined ? null : numericDataValue(table.value(rowIndex, sizeField));
            const ratio = rawSize === null || extent === null || extent[1] === extent[0]
                ? 0.5
                : (rawSize - extent[0]) / (extent[1] - extent[0]);
            const point = projectGeographicPosition(plot, longitude, latitude);
            const radius = layer.mark.radius ?? 5 + Math.sqrt(Math.max(0, ratio)) * 10;
            const fill = layer.mark.fill ?? theme.colors.focus;
            nodes.push({
                type: 'circle',
                ...nodeBase(`${layer.id}:map-halo:${rowIndex}`, { zIndex: layer.zIndex - 0.1 }),
                cx: point.x,
                cy: point.y,
                radius: radius + 4,
                fill: colorWithOpacity(fill, theme.mode === 'dark' ? 0.2 : 0.14),
                lineWidth: 0,
            });
            nodes.push({
                type: 'circle',
                ...nodeBase(`${layer.id}:map-point:${rowIndex}`, {
                    zIndex: layer.zIndex,
                    opacity: layer.mark.opacity,
                    interactive: performance.enableHitTesting,
                    datum: { layerId: layer.id, rowIndex, datum: table.row(rowIndex) },
                }),
                cx: point.x,
                cy: point.y,
                radius,
                fill,
                stroke: theme.colors.background,
                lineWidth: layer.mark.lineWidth ?? 1.5,
            });
        }
        return nodes;
    };
    function treeItems(context) {
        const { table, layer } = context;
        const parentField = layer.mark.fields.parent ?? layer.y.field;
        const weightField = layer.mark.fields.weight;
        const idField = layer.mark.fields.id ?? layer.x.field;
        const items = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const rawId = table.value(rowIndex, idField);
            if (rawId === null || rawId === undefined)
                continue;
            const rawParent = table.has(parentField) ? table.value(rowIndex, parentField) : null;
            const weight = weightField !== undefined && table.has(weightField)
                ? (numericDataValue(table.value(rowIndex, weightField)) ?? 1)
                : (numericDataValue(table.value(rowIndex, layer.y.field)) ?? 1);
            items.push({
                rowIndex,
                id: String(rawId),
                parent: rawParent === null || rawParent === undefined ? '' : String(rawParent),
                weight,
            });
        }
        return items;
    }
    function treeDepths(items) {
        const parents = new Map(items.map((item) => [item.id, item.parent]));
        const depths = new Map();
        const resolve = (id, trail) => {
            const existing = depths.get(id);
            if (existing !== undefined)
                return existing;
            const parent = parents.get(id);
            if (parent === undefined || parent === '' || !parents.has(parent) || trail.has(id)) {
                depths.set(id, 0);
                return 0;
            }
            const nextTrail = new Set(trail);
            nextTrail.add(id);
            const depth = resolve(parent, nextTrail) + 1;
            depths.set(id, depth);
            return depth;
        };
        items.forEach((item) => resolve(item.id, new Set()));
        return depths;
    }
    const compileOrgMark = (context) => {
        const { layer, plot, theme, table, performance } = context;
        const items = treeItems(context);
        const depths = treeDepths(items);
        const groups = new Map();
        items.forEach((item) => {
            const depth = depths.get(item.id) ?? 0;
            const group = groups.get(depth) ?? [];
            group.push(item);
            groups.set(depth, group);
        });
        const maxDepth = Math.max(0, ...groups.keys());
        const positions = new Map();
        const nodeWidth = Math.max(64, Math.min(128, plot.width / Math.max(2, Math.max(...[...groups.values()].map((group) => group.length))) - 16));
        const nodeHeight = Math.max(28, Math.min(44, plot.height / Math.max(2, maxDepth + 1) - 18));
        for (const [depth, group] of groups) {
            group.forEach((item, index) => {
                positions.set(item.id, {
                    x: plot.x + (plot.width * (index + 1)) / (group.length + 1),
                    y: plot.y + (plot.height * (depth + 0.5)) / Math.max(1, maxDepth + 1),
                });
            });
        }
        const nodes = [];
        items.forEach((item) => {
            const position = positions.get(item.id);
            const parent = positions.get(item.parent);
            if (position === undefined || parent === undefined)
                return;
            const middleY = parent.y + (position.y - parent.y) / 2;
            nodes.push({
                type: 'path',
                ...nodeBase(`${layer.id}:edge:${item.rowIndex}`, { zIndex: layer.zIndex - 1 }),
                points: [
                    { x: parent.x, y: parent.y + nodeHeight / 2 },
                    { x: parent.x, y: middleY },
                    { x: position.x, y: middleY },
                    { x: position.x, y: position.y - nodeHeight / 2 },
                ],
                closed: false,
                stroke: theme.colors.axis,
                lineWidth: 1.4,
                lineCap: 'round',
                lineJoin: 'round',
            });
        });
        items.forEach((item) => {
            const position = positions.get(item.id);
            if (position === undefined)
                return;
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:node:${item.rowIndex}`, {
                    zIndex: layer.zIndex,
                    opacity: layer.mark.opacity,
                    interactive: performance.enableHitTesting,
                    datum: { layerId: layer.id, rowIndex: item.rowIndex, datum: table.row(item.rowIndex) },
                }),
                x: position.x - nodeWidth / 2,
                y: position.y - nodeHeight / 2,
                width: nodeWidth,
                height: nodeHeight,
                fill: layer.mark.fill ?? theme.colors.surface,
                stroke: layer.mark.stroke ?? theme.colors.axis,
                lineWidth: layer.mark.lineWidth ?? 1.2,
                cornerRadius: layer.mark.cornerRadius ?? 9,
            });
            const depth = depths.get(item.id) ?? 0;
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:node-accent:${item.rowIndex}`, {
                    zIndex: layer.zIndex + 0.1,
                }),
                x: position.x - nodeWidth / 2 + 1,
                y: position.y - nodeHeight / 2 + 1,
                width: nodeWidth - 2,
                height: 4,
                fill: categoricalColor(theme, depth, maxDepth + 1),
                lineWidth: 0,
                cornerRadius: 4,
            });
            nodes.push(textNode$3(`${layer.id}:node-label:${item.rowIndex}`, position.x, position.y, item.id, context, { size: 10.5, weight: 700 }));
        });
        return nodes;
    };
    function cubicPoint(start, control1, control2, end, ratio) {
        const inverse = 1 - ratio;
        return {
            x: inverse ** 3 * start.x +
                3 * inverse ** 2 * ratio * control1.x +
                3 * inverse * ratio ** 2 * control2.x +
                ratio ** 3 * end.x,
            y: inverse ** 3 * start.y +
                3 * inverse ** 2 * ratio * control1.y +
                3 * inverse * ratio ** 2 * control2.y +
                ratio ** 3 * end.y,
        };
    }
    function sankeyBandPoints(sourceX, sourceY, sourceHeight, targetX, targetY, targetHeight) {
        const controlOffset = (targetX - sourceX) * 0.44;
        const sample = (startY, endY) => Array.from({ length: 13 }, (_, index) => {
            const ratio = index / 12;
            return cubicPoint({ x: sourceX, y: startY }, { x: sourceX + controlOffset, y: startY }, { x: targetX - controlOffset, y: endY }, { x: targetX, y: endY }, ratio);
        });
        return [
            ...sample(sourceY, targetY),
            ...sample(sourceY + sourceHeight, targetY + targetHeight).reverse(),
        ];
    }
    const compileSankeyMark = (context) => {
        const { table, layer, plot, theme, performance } = context;
        const targetField = layer.mark.fields.target ?? 'target';
        const edges = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const source = table.value(rowIndex, layer.x.field);
            const target = table.has(targetField) ? table.value(rowIndex, targetField) : null;
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            if (source === null ||
                source === undefined ||
                target === null ||
                target === undefined ||
                value === null ||
                value <= 0)
                continue;
            edges.push({ rowIndex, source: String(source), target: String(target), value });
        }
        const sources = [...new Set(edges.map((edge) => edge.source))];
        const targets = [...new Set(edges.map((edge) => edge.target))];
        const sourceTotals = new Map(sources.map((source) => [
            source,
            edges.filter((edge) => edge.source === source).reduce((sum, edge) => sum + edge.value, 0),
        ]));
        const targetTotals = new Map(targets.map((target) => [
            target,
            edges.filter((edge) => edge.target === target).reduce((sum, edge) => sum + edge.value, 0),
        ]));
        const maxTotal = Math.max(1, ...sourceTotals.values(), ...targetTotals.values());
        const nodeWidth = 14;
        const sourcePositions = new Map();
        const targetPositions = new Map();
        const position = (names, totals, output) => {
            const gap = 12;
            const available = Math.max(1, plot.height - gap * Math.max(0, names.length - 1));
            const sum = names.reduce((total, name) => total + (totals.get(name) ?? 0), 0) || 1;
            let y = plot.y;
            names.forEach((name) => {
                const height = Math.max(10, ((totals.get(name) ?? 0) / sum) * available);
                output.set(name, { y, height });
                y += height + gap;
            });
        };
        position(sources, sourceTotals, sourcePositions);
        position(targets, targetTotals, targetPositions);
        const sourceOffsets = new Map();
        const targetOffsets = new Map();
        const nodes = [];
        edges.forEach((edge, index) => {
            const source = sourcePositions.get(edge.source);
            const target = targetPositions.get(edge.target);
            if (source === undefined || target === undefined)
                return;
            const sourceHeight = Math.max(2, (edge.value / (sourceTotals.get(edge.source) ?? maxTotal)) * source.height);
            const targetHeight = Math.max(2, (edge.value / (targetTotals.get(edge.target) ?? maxTotal)) * target.height);
            const sy = source.y + (sourceOffsets.get(edge.source) ?? 0);
            const ty = target.y + (targetOffsets.get(edge.target) ?? 0);
            sourceOffsets.set(edge.source, (sourceOffsets.get(edge.source) ?? 0) + sourceHeight);
            targetOffsets.set(edge.target, (targetOffsets.get(edge.target) ?? 0) + targetHeight);
            nodes.push({
                type: 'path',
                ...nodeBase(`${layer.id}:flow:${edge.rowIndex}`, {
                    zIndex: layer.zIndex - 0.5,
                    opacity: Math.min(0.75, layer.mark.opacity * 0.55),
                    interactive: performance.enableHitTesting,
                    datum: { layerId: layer.id, rowIndex: edge.rowIndex, datum: table.row(edge.rowIndex) },
                }),
                points: sankeyBandPoints(plot.x + nodeWidth, sy, sourceHeight, plot.x + plot.width - nodeWidth, ty, targetHeight),
                closed: true,
                fill: layer.mark.fill ?? categoricalColor(theme, index, edges.length),
                lineWidth: 0,
                lineJoin: 'round',
            });
        });
        sources.forEach((name, index) => {
            const item = sourcePositions.get(name);
            if (item === undefined)
                return;
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:source:${index}`, { zIndex: layer.zIndex }),
                x: plot.x,
                y: item.y,
                width: nodeWidth,
                height: item.height,
                fill: layer.mark.fill ?? categoricalColor(theme, index, sources.length + targets.length),
                lineWidth: 0,
                cornerRadius: 4,
            });
            nodes.push(textNode$3(`${layer.id}:source-label:${index}`, plot.x + nodeWidth + 5, item.y + item.height / 2, name, context, { align: 'left', size: 10.5, weight: 650 }));
        });
        targets.forEach((name, index) => {
            const item = targetPositions.get(name);
            if (item === undefined)
                return;
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:target:${index}`, { zIndex: layer.zIndex }),
                x: plot.x + plot.width - nodeWidth,
                y: item.y,
                width: nodeWidth,
                height: item.height,
                fill: layer.mark.fill ??
                    categoricalColor(theme, sources.length + index, sources.length + targets.length),
                lineWidth: 0,
                cornerRadius: 4,
            });
            nodes.push(textNode$3(`${layer.id}:target-label:${index}`, plot.x + plot.width - nodeWidth - 5, item.y + item.height / 2, name, context, { align: 'right', size: 10.5, weight: 650 }));
        });
        return nodes;
    };
    function compileTimeline(context, gantt) {
        const { table, layer, xScale, yScale, theme, performance } = context;
        const endField = layer.mark.fields.end ?? 'end';
        const progressField = layer.mark.fields.progress;
        const idField = layer.mark.fields.id;
        const dependencyField = layer.mark.fields.dependencies;
        const barHeight = Math.max(8, yScale instanceof BandScale ? yScale.bandwidth * 0.58 : 18);
        const nodes = [];
        const positions = new Map();
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const start = scaleInput(table.value(rowIndex, layer.x.field));
            const end = table.has(endField) ? scaleInput(table.value(rowIndex, endField)) : null;
            const row = scaleInput(table.value(rowIndex, layer.y.field));
            if (start === null || end === null || row === null)
                continue;
            const x1 = xScale.map(start);
            const x2 = xScale.map(end);
            const y = yScale.map(row);
            if (![x1, x2, y].every(Number.isFinite))
                continue;
            const fill = categoricalColor(theme, rowIndex, table.length);
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:${'task' }:${rowIndex}`, {
                    zIndex: layer.zIndex,
                    opacity: layer.mark.opacity,
                    interactive: performance.enableHitTesting,
                    datum: { layerId: layer.id, rowIndex, datum: table.row(rowIndex) },
                }),
                x: Math.min(x1, x2),
                y: y - barHeight / 2,
                width: Math.max(2, Math.abs(x2 - x1)),
                height: barHeight,
                fill: layer.mark.fill ?? fill,
                ...(layer.mark.stroke === undefined ? {} : { stroke: layer.mark.stroke }),
                lineWidth: layer.mark.lineWidth ?? 0,
                cornerRadius: layer.mark.cornerRadius ?? 6,
            });
            if (progressField !== undefined && table.has(progressField)) {
                const progress = numericDataValue(table.value(rowIndex, progressField));
                if (progress !== null) {
                    nodes.push({
                        type: 'rect',
                        ...nodeBase(`${layer.id}:progress:${rowIndex}`, {
                            zIndex: layer.zIndex + 0.1,
                            opacity: 0.58,
                        }),
                        x: Math.min(x1, x2),
                        y: y - barHeight / 2,
                        width: Math.max(0, (Math.abs(x2 - x1) * Math.max(0, Math.min(100, progress))) / 100),
                        height: barHeight,
                        fill: mixColor$1(fill, theme.colors.text, theme.mode === 'dark' ? 0.18 : 0.26),
                        lineWidth: 0,
                        cornerRadius: layer.mark.cornerRadius ?? 6,
                    });
                }
            }
            if (idField !== undefined && table.has(idField)) {
                positions.set(String(table.value(rowIndex, idField)), {
                    x: Math.max(x1, x2),
                    y,
                    width: Math.abs(x2 - x1),
                });
            }
        }
        if (dependencyField !== undefined &&
            idField !== undefined &&
            table.has(dependencyField)) {
            for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
                const id = String(table.value(rowIndex, idField) ?? '');
                const task = positions.get(id);
                const dependencies = String(table.value(rowIndex, dependencyField) ?? '')
                    .split(',')
                    .map((item) => item.trim())
                    .filter(Boolean);
                if (task === undefined)
                    continue;
                dependencies.forEach((dependency, index) => {
                    const parent = positions.get(dependency);
                    if (parent === undefined)
                        return;
                    nodes.push({
                        type: 'line',
                        ...nodeBase(`${layer.id}:dependency:${rowIndex}:${index}`, {
                            zIndex: layer.zIndex + 0.5,
                        }),
                        x1: parent.x,
                        y1: parent.y,
                        x2: task.x - task.width,
                        y2: task.y,
                        stroke: theme.colors.axis,
                        lineWidth: 1.2,
                        dash: [4, 2],
                        lineCap: 'round',
                    });
                });
            }
        }
        return nodes;
    }
    const compileGanttMark = (context) => compileTimeline(context);
    function compileIcicleMark(context) {
        const { table, layer, plot, theme, performance } = context;
        const parentField = layer.mark.fields.parent ?? 'parent';
        const items = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const rawLabel = table.value(rowIndex, layer.x.field);
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            if (rawLabel === null || rawLabel === undefined || value === null || value < 0)
                continue;
            const rawParent = table.has(parentField) ? table.value(rowIndex, parentField) : undefined;
            const parent = rawParent === null || rawParent === undefined || String(rawParent).trim() === ''
                ? undefined
                : String(rawParent);
            items.push({
                rowIndex,
                label: String(rawLabel),
                value,
                ...(parent === undefined ? {} : { parent }),
            });
        }
        if (items.length === 0)
            return [];
        const byLabel = new Map();
        items.forEach((item) => byLabel.set(item.label, { ...item, children: [] }));
        const orderedNodes = [...byLabel.values()];
        const parentByNode = new Map();
        orderedNodes.forEach((node) => {
            const parent = node.parent === undefined ? undefined : byLabel.get(node.parent);
            if (parent !== undefined && parent !== node)
                parentByNode.set(node, parent);
        });
        // Each node has at most one parent, so cycles can be broken in linear time by
        // walking the parent chains once. Breaking the first repeated edge keeps the
        // result deterministic while making every malformed component renderable.
        const resolved = new Set();
        orderedNodes.forEach((start) => {
            if (resolved.has(start))
                return;
            const path = [];
            const positions = new Map();
            let cursor = start;
            while (cursor !== undefined && !resolved.has(cursor)) {
                if (positions.has(cursor)) {
                    parentByNode.delete(cursor);
                    break;
                }
                positions.set(cursor, path.length);
                path.push(cursor);
                cursor = parentByNode.get(cursor);
            }
            path.forEach((node) => resolved.add(node));
        });
        parentByNode.forEach((parent, node) => parent.children.push(node));
        const roots = orderedNodes.filter((node) => !parentByNode.has(node));
        // Memoize subtree weight and depth in one iterative leaf-to-root pass. This
        // avoids both the previous repeated subtree scans and call-stack exhaustion
        // on deep, valid hierarchies without allocating one frame object per edge.
        const weights = new Map();
        const depths = new Map();
        const remainingChildren = new Map();
        const accumulatedWeights = new Map();
        const accumulatedDepths = new Map();
        const ready = [];
        orderedNodes.forEach((node) => {
            remainingChildren.set(node, node.children.length);
            if (node.children.length === 0)
                ready.push(node);
        });
        for (let readyIndex = 0; readyIndex < ready.length; readyIndex += 1) {
            const node = ready[readyIndex];
            if (node === undefined)
                continue;
            const weight = Math.max(node.value, accumulatedWeights.get(node) ?? 0, 0.000001);
            const depth = 1 + (accumulatedDepths.get(node) ?? 0);
            weights.set(node, weight);
            depths.set(node, depth);
            const parent = parentByNode.get(node);
            if (parent === undefined)
                continue;
            accumulatedWeights.set(parent, (accumulatedWeights.get(parent) ?? 0) + weight);
            accumulatedDepths.set(parent, Math.max(accumulatedDepths.get(parent) ?? 0, depth));
            const remaining = (remainingChildren.get(parent) ?? 1) - 1;
            remainingChildren.set(parent, remaining);
            if (remaining === 0)
                ready.push(parent);
        }
        let maxDepth = 1;
        roots.forEach((root) => {
            maxDepth = Math.max(maxDepth, depths.get(root) ?? 1);
        });
        const levelHeight = plot.height / maxDepth;
        const rootTotal = roots.reduce((sum, root) => sum + (weights.get(root) ?? 0), 0);
        const nodes = [];
        const markBudget = Math.max(1, Math.floor(performance.maxBarMarks));
        const renderStack = [];
        let rootX = plot.x;
        const rootFrames = [];
        roots.forEach((root, rootIndex) => {
            const rootWidth = plot.width * ((weights.get(root) ?? 0) / Math.max(rootTotal, 0.000001));
            if (rootIndex < markBudget) {
                rootFrames.push({ node: root, level: 0, x: rootX, width: rootWidth, colorIndex: rootIndex });
            }
            rootX += rootWidth;
        });
        for (let index = rootFrames.length - 1; index >= 0; index -= 1) {
            const frame = rootFrames[index];
            if (frame !== undefined)
                renderStack.push(frame);
        }
        let renderedMarks = 0;
        while (renderStack.length > 0 && renderedMarks < markBudget) {
            const frame = renderStack.pop();
            if (frame === undefined || frame.width <= 0)
                continue;
            const { node, level, x, width, colorIndex } = frame;
            const gap = Math.min(1.5, levelHeight * 0.12, width * 0.02);
            const y = plot.y + level * levelHeight;
            const fill = layer.mark.fill ??
                mixColor$1(categoricalColor(theme, colorIndex, items.length), theme.colors.background, Math.min(0.5, level * 0.1));
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:icicle:${node.rowIndex}`, {
                    zIndex: layer.zIndex + level * 0.01,
                    opacity: layer.mark.opacity,
                    interactive: performance.enableHitTesting,
                    datum: {
                        layerId: layer.id,
                        rowIndex: node.rowIndex,
                        datum: table.row(node.rowIndex),
                        tooltip: { label: node.label, value: node.value, depth: level },
                    },
                }),
                x: x + gap,
                y: y + gap,
                width: Math.max(1, width - gap * 2),
                height: Math.max(1, levelHeight - gap * 2),
                fill,
                stroke: colorWithOpacity(theme.colors.background, 0.78),
                lineWidth: 1,
                cornerRadius: layer.mark.cornerRadius ?? 3,
            });
            renderedMarks += 1;
            if (width > 42 && levelHeight > 20) {
                nodes.push(textNode$3(`${layer.id}:icicle-label:${node.rowIndex}`, x + 8, y + levelHeight / 2, node.label, context, {
                    align: 'left',
                    size: Math.max(8, Math.min(12, levelHeight * 0.28)),
                    weight: 700,
                    fill: readableTextColor(fill, '#ffffff', '#0f172a'),
                }));
            }
            if (node.children.length === 0 || level + 1 >= maxDepth)
                continue;
            const childTotal = node.children.reduce((sum, child) => sum + (weights.get(child) ?? 0), 0);
            let childX = x;
            const childFrames = [];
            const childLimit = Math.min(node.children.length, markBudget - renderedMarks);
            for (let childIndex = 0; childIndex < childLimit; childIndex += 1) {
                const child = node.children[childIndex];
                if (child === undefined)
                    continue;
                const childWidth = width * ((weights.get(child) ?? 0) / Math.max(childTotal, 0.000001));
                childFrames.push({
                    node: child,
                    level: level + 1,
                    x: childX,
                    width: childWidth,
                    colorIndex: colorIndex + childIndex + 1,
                });
                childX += childWidth;
            }
            for (let index = childFrames.length - 1; index >= 0; index -= 1) {
                const childFrame = childFrames[index];
                if (childFrame !== undefined)
                    renderStack.push(childFrame);
            }
        }
        return nodes;
    }
    function layoutTreemap(items, rectangle) {
        if (items.length === 0)
            return [];
        const first = items[0];
        if (items.length === 1 && first !== undefined)
            return [{ ...first, ...rectangle }];
        const total = items.reduce((sum, item) => sum + item.value, 0);
        let cumulative = 0;
        let splitIndex = 1;
        let bestDistance = Number.POSITIVE_INFINITY;
        for (let index = 1; index < items.length; index += 1) {
            cumulative += items[index - 1]?.value ?? 0;
            const distance = Math.abs(total / 2 - cumulative);
            if (distance < bestDistance) {
                bestDistance = distance;
                splitIndex = index;
            }
        }
        const leading = items.slice(0, splitIndex);
        const trailing = items.slice(splitIndex);
        const leadingValue = leading.reduce((sum, item) => sum + item.value, 0);
        const ratio = total <= 0 ? 0.5 : leadingValue / total;
        if (rectangle.width >= rectangle.height) {
            const leadingWidth = rectangle.width * ratio;
            return [
                ...layoutTreemap(leading, { ...rectangle, width: leadingWidth }),
                ...layoutTreemap(trailing, {
                    x: rectangle.x + leadingWidth,
                    y: rectangle.y,
                    width: rectangle.width - leadingWidth,
                    height: rectangle.height,
                }),
            ];
        }
        const leadingHeight = rectangle.height * ratio;
        return [
            ...layoutTreemap(leading, { ...rectangle, height: leadingHeight }),
            ...layoutTreemap(trailing, {
                x: rectangle.x,
                y: rectangle.y + leadingHeight,
                width: rectangle.width,
                height: rectangle.height - leadingHeight,
            }),
        ];
    }
    const compileTreemapMark = (context) => {
        if (context.layer.mark.options.mode === 'icicle')
            return compileIcicleMark(context);
        const { table, layer, plot, theme, performance } = context;
        const items = [];
        for (let rowIndex = 0; rowIndex < table.length; rowIndex += 1) {
            const label = table.value(rowIndex, layer.x.field);
            const value = numericDataValue(table.value(rowIndex, layer.y.field));
            if (label === null || label === undefined || value === null || value <= 0)
                continue;
            items.push({ rowIndex, label: String(label), value });
        }
        if (items.length === 0)
            return [];
        const nodes = [];
        const tiles = layoutTreemap(items, plot);
        tiles.forEach((item, index) => {
            const base = categoricalColor(theme, index, tiles.length);
            const fill = mixColor$1(base, theme.colors.background, theme.mode === 'dark' ? 0.06 : 0.02);
            const gap = 2;
            const x = item.x + gap;
            const y = item.y + gap;
            const width = Math.max(1, item.width - gap * 2);
            const height = Math.max(1, item.height - gap * 2);
            nodes.push({
                type: 'rect',
                ...nodeBase(`${layer.id}:treemap:${item.rowIndex}`, {
                    zIndex: layer.zIndex,
                    opacity: layer.mark.opacity,
                    interactive: performance.enableHitTesting,
                    datum: { layerId: layer.id, rowIndex: item.rowIndex, datum: table.row(item.rowIndex) },
                }),
                x,
                y,
                width,
                height,
                fill: layer.mark.fill ?? fill,
                stroke: colorWithOpacity(theme.colors.background, 0.72),
                lineWidth: 1,
                cornerRadius: layer.mark.cornerRadius ?? 7,
            });
            if (width > 52 && height > 30) {
                const labelColor = readableTextColor(layer.mark.fill ?? fill, '#ffffff', '#0f172a');
                nodes.push(textNode$3(`${layer.id}:treemap-label:${item.rowIndex}`, x + 10, y + 13, item.label, context, {
                    align: 'left',
                    baseline: 'top',
                    size: Math.max(9, Math.min(14, Math.min(width, height) / 6)),
                    weight: 750,
                    fill: labelColor,
                }));
                if (height > 52) {
                    nodes.push(textNode$3(`${layer.id}:treemap-value:${item.rowIndex}`, x + 10, y + height - 10, String(item.value), context, {
                        align: 'left',
                        baseline: 'bottom',
                        size: 10,
                        weight: 600,
                        fill: colorWithOpacity(labelColor, 0.82),
                    }));
                }
            }
        });
        return nodes;
    };
    const compileWordTreeMark = (context) => {
        const { layer, plot, theme, table, performance } = context;
        const items = treeItems(context);
        const depths = treeDepths(items);
        const groups = new Map();
        items.forEach((item) => {
            const depth = depths.get(item.id) ?? 0;
            const group = groups.get(depth) ?? [];
            group.push(item);
            groups.set(depth, group);
        });
        const maxDepth = Math.max(0, ...groups.keys());
        const positions = new Map();
        for (const [depth, group] of groups) {
            group.forEach((item, index) => {
                positions.set(item.id, {
                    x: plot.x + (plot.width * (depth + 0.5)) / Math.max(1, maxDepth + 1),
                    y: plot.y + (plot.height * (index + 1)) / (group.length + 1),
                });
            });
        }
        const maximumWeight = Math.max(1, ...items.map((item) => item.weight));
        const nodes = [];
        items.forEach((item) => {
            const position = positions.get(item.id);
            const parent = positions.get(item.parent);
            if (position === undefined)
                return;
            if (parent !== undefined) {
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:branch:${item.rowIndex}`, { zIndex: layer.zIndex - 1 }),
                    x1: parent.x,
                    y1: parent.y,
                    x2: position.x,
                    y2: position.y,
                    stroke: theme.colors.grid,
                    lineWidth: 1.6,
                    lineCap: 'round',
                });
            }
            const fontSize = 10 + Math.sqrt(item.weight / maximumWeight) * 16;
            const label = textNode$3(`${layer.id}:word:${item.rowIndex}`, position.x, position.y, item.id, context, {
                size: fontSize,
                weight: 650,
                fill: categoricalColor(theme, depths.get(item.id) ?? 0, maxDepth + 1),
            });
            Object.assign(label, {
                interactive: performance.enableHitTesting,
                datum: { layerId: layer.id, rowIndex: item.rowIndex, datum: table.row(item.rowIndex) },
            });
            nodes.push(label);
        });
        return nodes;
    };

    const TAU = Math.PI * 2;
    function clamp$2(value, minimum, maximum) {
        return Math.max(minimum, Math.min(maximum, value));
    }
    function stringValue(value) {
        if (value === null || value === undefined)
            return null;
        if (value instanceof Date)
            return value.toISOString();
        if (Array.isArray(value))
            return value.map(String).join(', ');
        return String(value);
    }
    function numericValue(value) {
        const number = value instanceof Date ? value.getTime() : Number(value);
        return Number.isFinite(number) ? number : null;
    }
    function optionObject(value) {
        return value !== null && typeof value === 'object' && !Array.isArray(value)
            ? value
            : null;
    }
    function optionNumber(value, fallback) {
        return typeof value === 'number' && Number.isFinite(value) ? value : fallback;
    }
    function optionBoolean(value, fallback = false) {
        return typeof value === 'boolean' ? value : fallback;
    }
    function optionString(value, allowed, fallback) {
        return typeof value === 'string' && allowed.includes(value) ? value : fallback;
    }
    function optionStrings(value) {
        return Array.isArray(value)
            ? value.filter((item) => typeof item === 'string' && item.trim() !== '')
            : [];
    }
    function optionNumbers(value) {
        return Array.isArray(value)
            ? value.filter((item) => typeof item === 'number' && Number.isFinite(item))
            : [];
    }
    function hasAnyOption(context, keys) {
        return keys.some((key) => context.layer.mark.options[key] !== undefined);
    }
    function rowIndex(index, context) {
        return clamp$2(Math.floor(index), 0, Math.max(0, context.table.length - 1));
    }
    function publicRow(row) {
        return Object.fromEntries(Object.entries(row).filter(([field]) => !field.startsWith('__graflume_')));
    }
    function datumBase(context, id, index, offset = 0, tooltip, familyInteraction) {
        const safeIndex = rowIndex(index, context);
        return nodeBase(id, {
            zIndex: context.layer.zIndex + offset,
            opacity: context.layer.mark.opacity,
            interactive: context.performance.enableHitTesting,
            datum: {
                layerId: context.layer.id,
                rowIndex: safeIndex,
                datum: publicRow(context.table.row(safeIndex)),
                ...(tooltip === undefined ? {} : { tooltip }),
                ...(familyInteraction === undefined ? {} : { familyInteraction }),
            },
        });
    }
    function textNode$2(context, id, x, y, text, options = {}) {
        return {
            type: 'text',
            ...nodeBase(id, { zIndex: options.zIndex ?? context.layer.zIndex + 3 }),
            x,
            y,
            text,
            fill: options.fill ?? context.theme.colors.text,
            fontFamily: context.theme.typography.fontFamily,
            fontSize: options.size ?? context.theme.typography.fontSize,
            fontWeight: options.weight ?? 600,
            align: options.align ?? 'center',
            baseline: options.baseline ?? 'middle',
            rotation: options.rotation ?? 0,
        };
    }
    function palette(context, index, count) {
        return categoricalColor(context.theme, index, Math.max(1, count));
    }
    function toPlot(context, point) {
        return {
            x: context.plot.x + point.x * context.plot.width,
            y: context.plot.y + point.y * context.plot.height,
        };
    }
    function pointOnCircle(cx, cy, radius, angle) {
        return { x: cx + Math.cos(angle) * radius, y: cy + Math.sin(angle) * radius };
    }
    function sampledArc(cx, cy, radius, start, end, segments = 32) {
        const count = Math.max(2, segments);
        return Array.from({ length: count + 1 }, (_, index) => pointOnCircle(cx, cy, radius, start + ((end - start) * index) / count));
    }
    function annularSector(cx, cy, innerRadius, outerRadius, start, end) {
        const segments = Math.max(4, Math.ceil((Math.abs(end - start) / TAU) * 64));
        return [
            ...sampledArc(cx, cy, outerRadius, start, end, segments),
            ...sampledArc(cx, cy, innerRadius, end, start, segments),
        ];
    }
    function quadratic(points, segments = 24) {
        if (points.length !== 3)
            return points;
        const [start, control, end] = points;
        return Array.from({ length: segments + 1 }, (_, index) => {
            const ratio = index / segments;
            const inverse = 1 - ratio;
            return {
                x: inverse * inverse * start.x + 2 * inverse * ratio * control.x + ratio * ratio * end.x,
                y: inverse * inverse * start.y + 2 * inverse * ratio * control.y + ratio * ratio * end.y,
            };
        });
    }
    function sourceRowsTooltip(indices) {
        return [...new Set(indices)].sort((left, right) => left - right).slice(0, 256);
    }
    function tokenOptions(context) {
        const options = context.layer.mark.options;
        return {
            case: optionString(options.case, ['lower', 'upper', 'preserve'], 'lower'),
            stopwords: optionStrings(options.stopwords),
            ngram: Math.floor(optionNumber(options.ngram, 1)),
            stemming: optionString(options.stemming, ['none', 'simple-en'], 'none'),
            ...(typeof options.locale === 'string' ? { locale: options.locale } : {}),
        };
    }
    function fieldValue(context, index, field) {
        return context.table.has(field) ? context.table.value(index, field) : null;
    }
    function parsePositions(value) {
        const positions = {};
        const object = optionObject(value);
        if (object === null)
            return positions;
        for (const [id, raw] of Object.entries(object)) {
            const position = optionObject(raw);
            if (position !== null &&
                typeof position.x === 'number' &&
                Number.isFinite(position.x) &&
                typeof position.y === 'number' &&
                Number.isFinite(position.y)) {
                positions[id] = { x: position.x, y: position.y };
            }
        }
        return positions;
    }
    const compileAdvancedTreeMark = (context) => {
        if (!hasAnyOption(context, [
            'layout',
            'mode',
            'root',
            'zoomTo',
            'collapsed',
            'query',
            'padding',
            'breadcrumbs',
        ]))
            return compileTreeMark(context);
        if (context.table.length === 0)
            return [];
        const { layer, table, plot, theme } = context;
        const idField = layer.mark.fields.id ?? layer.x.field;
        const parentField = layer.mark.fields.parent ?? 'parent';
        const labelField = layer.mark.fields.label ?? idField;
        const valueField = layer.mark.fields.value ?? layer.y.field;
        const data = [];
        const rows = new Map();
        for (let index = 0; index < table.length; index += 1) {
            const id = stringValue(fieldValue(context, index, idField));
            if (id === null || id === '')
                continue;
            const parent = stringValue(fieldValue(context, index, parentField));
            const label = stringValue(fieldValue(context, index, labelField)) ?? id;
            const value = numericValue(fieldValue(context, index, valueField)) ?? 1;
            data.push({
                id,
                parent: parent === null || parent === '' || parent === id ? null : parent,
                label,
                value,
            });
            rows.set(id, index);
        }
        if (data.length === 0)
            return [];
        const mode = optionString(layer.mark.options.layout ?? layer.mark.options.mode, ['circle-pack', 'dendrogram', 'radial-tree'], 'circle-pack');
        const authoredRoot = typeof layer.mark.options.root === 'string' ? layer.mark.options.root : undefined;
        const zoomTo = typeof layer.mark.options.zoomTo === 'string' ? layer.mark.options.zoomTo : null;
        const collapsed = new Set(optionStrings(layer.mark.options.collapsed));
        const autoExpanded = [];
        if (zoomTo !== null) {
            const parents = new Map(data.map(({ id, parent }) => [id, parent ?? null]));
            let current = zoomTo;
            const visited = new Set();
            while (current !== null && current !== undefined && !visited.has(current)) {
                visited.add(current);
                if (collapsed.delete(current))
                    autoExpanded.push(current);
                current = parents.get(current);
            }
        }
        const result = layoutHierarchy(data, {
            mode,
            ...(authoredRoot === undefined ? {} : { root: authoredRoot }),
            collapsed: [...collapsed],
            ...(typeof layer.mark.options.query === 'string' ? { query: layer.mark.options.query } : {}),
            padding: optionNumber(layer.mark.options.padding, 0.004),
        });
        let viewScale = 1;
        let viewCenter = { x: 0.5, y: 0.5 };
        if (zoomTo !== null) {
            const target = result.nodes.find(({ id }) => id === zoomTo);
            if (target === undefined)
                throw new GraflumeError('INVALID_SPEC', `Unknown or hidden hierarchy zoom target "${zoomTo}".`);
            const byId = new Map(result.nodes.map((node) => [node.id, node]));
            const descendants = result.nodes.filter((node) => {
                let current = node;
                const visited = new Set();
                while (current !== undefined && !visited.has(current.id)) {
                    if (current.id === target.id)
                        return true;
                    visited.add(current.id);
                    current = current.parent === null ? undefined : byId.get(current.parent);
                }
                return false;
            });
            const minimumX = Math.min(...descendants.map(({ x, radius }) => x - radius));
            const maximumX = Math.max(...descendants.map(({ x, radius }) => x + radius));
            const minimumY = Math.min(...descendants.map(({ y, radius }) => y - radius));
            const maximumY = Math.max(...descendants.map(({ y, radius }) => y + radius));
            viewCenter = { x: (minimumX + maximumX) / 2, y: (minimumY + maximumY) / 2 };
            viewScale = Math.max(1, Math.min(32, 0.84 / Math.max(0.01, maximumX - minimumX), 0.84 / Math.max(0.01, maximumY - minimumY)));
        }
        const positions = new Map(result.nodes.map((node) => [
            node.id,
            toPlot(context, {
                x: 0.5 + (node.x - viewCenter.x) * viewScale,
                y: 0.5 + (node.y - viewCenter.y) * viewScale,
            }),
        ]));
        const nodes = [];
        if (result.mode !== 'circle-pack') {
            result.links.forEach((link, index) => {
                const source = positions.get(link.source);
                const target = positions.get(link.target);
                if (source === undefined || target === undefined)
                    return;
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:hierarchy-link:${index}`, { zIndex: layer.zIndex - 1 }),
                    points: result.mode === 'dendrogram'
                        ? [source, { x: source.x, y: target.y }, target]
                        : [source, target],
                    closed: false,
                    stroke: colorWithOpacity(theme.colors.axis, 0.7),
                    lineWidth: 1.5,
                    lineCap: 'round',
                    lineJoin: 'round',
                });
            });
        }
        result.nodes
            .slice()
            .sort((left, right) => left.depth - right.depth)
            .forEach((item, index) => {
            const position = positions.get(item.id);
            const sourceRows = sourceRowsTooltip(data.flatMap((candidate) => {
                let current = candidate;
                const visited = new Set();
                while (current !== undefined && !visited.has(current.id)) {
                    if (current.id === item.id)
                        return [rows.get(candidate.id) ?? 0];
                    visited.add(current.id);
                    current =
                        current.parent === null || current.parent === undefined
                            ? undefined
                            : data.find(({ id }) => id === current?.parent);
                }
                return [];
            }));
            const sourceRow = sourceRows[0] ?? rows.get(item.id) ?? 0;
            const radius = Math.max(4, item.radius * Math.min(plot.width, plot.height) * viewScale);
            const color = palette(context, item.depth, Math.max(1, ...result.nodes.map(({ depth }) => depth + 1)));
            const tooltip = {
                kind: 'hierarchy-node',
                layout: result.mode,
                id: item.id,
                parent: item.parent,
                label: item.label,
                depth: item.depth,
                value: item.value,
                aggregate: item.aggregate,
                leaf: item.leaf,
                collapsed: item.collapsed,
                matched: item.matched,
                root: result.root,
                zoomTo,
                viewScale,
                autoExpanded,
                breadcrumbs: result.breadcrumbs,
                interaction: ['collapse', 'reroot', 'zoom', 'search'],
                sourceRowIndices: sourceRows,
            };
            nodes.push({
                type: 'circle',
                ...datumBase(context, `${layer.id}:hierarchy-node:${item.id}`, sourceRow, item.depth * 0.01, tooltip, {
                    kind: 'hierarchy-node',
                    id: item.id,
                    parent: item.parent,
                    root: result.root,
                    leaf: item.leaf,
                    collapsed: item.collapsed,
                }),
                cx: position.x,
                cy: position.y,
                radius,
                fill: colorWithOpacity(layer.mark.fill ?? color, result.mode === 'circle-pack' ? 0.25 : 0.9),
                stroke: item.matched ? theme.colors.focus : (layer.mark.stroke ?? color),
                lineWidth: item.matched ? 3 : item.collapsed ? 2.5 : (layer.mark.lineWidth ?? 1.5),
                ...(item.collapsed ? { dash: [4, 3] } : {}),
            });
            if (result.mode !== 'circle-pack' || radius >= 14 || item.id === result.root) {
                const label = textNode$2(context, `${layer.id}:hierarchy-label:${item.id}`, position.x, result.mode === 'circle-pack' ? position.y : position.y + radius + 9, item.label, {
                    fill: result.mode === 'circle-pack'
                        ? readableTextColor(color, '#ffffff', theme.colors.text)
                        : theme.colors.mutedText,
                    size: Math.max(9, theme.typography.fontSize - 1),
                    weight: item.matched ? 800 : 600,
                });
                nodes.push({
                    ...label,
                    ...datumBase(context, label.id, sourceRow, 2, tooltip, {
                        kind: 'hierarchy-node',
                        id: item.id,
                        parent: item.parent,
                        root: result.root,
                        leaf: item.leaf,
                        collapsed: item.collapsed,
                    }),
                });
            }
        });
        if (optionBoolean(layer.mark.options.breadcrumbs, true) && result.breadcrumbs.length > 0) {
            nodes.push(textNode$2(context, `${layer.id}:hierarchy-breadcrumbs`, plot.x + 4, plot.y + 4, result.breadcrumbs.join(' / '), {
                align: 'left',
                baseline: 'top',
                fill: theme.colors.mutedText,
                size: Math.max(9, theme.typography.fontSize - 2),
            }));
        }
        return nodes;
    };
    const compileAdvancedSankeyMark = (context) => {
        if (!hasAnyOption(context, [
            'alignment',
            'order',
            'linkSort',
            'linkOrder',
            'iterations',
            'cycle',
            'positions',
            'balanceTolerance',
            'pathStart',
            'pathDirection',
        ]))
            return compileSankeyMark(context);
        if (context.table.length === 0)
            return [];
        const { layer, table, plot, theme } = context;
        const sourceField = layer.mark.fields.source ?? layer.x.field;
        const targetField = layer.mark.fields.target ?? 'target';
        const valueField = layer.mark.fields.value ?? layer.y.field;
        const idField = layer.mark.fields.id;
        const edges = [];
        const edgeRows = new Map();
        for (let index = 0; index < table.length; index += 1) {
            const source = stringValue(fieldValue(context, index, sourceField));
            const target = stringValue(fieldValue(context, index, targetField));
            const value = numericValue(fieldValue(context, index, valueField));
            if (source === null || target === null || value === null || value < 0)
                continue;
            const id = idField === undefined
                ? `flow-${index}`
                : (stringValue(fieldValue(context, index, idField)) ?? `flow-${index}`);
            edges.push({ id, source, target, value });
            edgeRows.set(id, index);
        }
        if (edges.length === 0)
            return [];
        const result = layoutFlow(edges, {
            alignment: optionString(layer.mark.options.alignment, ['left', 'right', 'center', 'justify'], 'left'),
            order: optionString(layer.mark.options.order, ['input', 'ascending', 'descending'], 'input'),
            linkSort: optionString(layer.mark.options.linkSort, ['input', 'ascending', 'descending', 'authored'], 'input'),
            linkOrder: optionStrings(layer.mark.options.linkOrder),
            iterations: Math.floor(optionNumber(layer.mark.options.iterations, 8)),
            cycle: optionString(layer.mark.options.cycle, ['reject', 'allow'], 'reject'),
            positions: parsePositions(layer.mark.options.positions),
            balanceTolerance: optionNumber(layer.mark.options.balanceTolerance, 1e-9),
        });
        const pathStart = typeof layer.mark.options.pathStart === 'string' ? layer.mark.options.pathStart : undefined;
        const path = pathStart === undefined
            ? { nodes: [], links: [] }
            : traverseFlowPath(result, pathStart, optionString(layer.mark.options.pathDirection, ['upstream', 'downstream', 'both'], 'both'));
        const nodeRows = new Map();
        edges.forEach((edge) => {
            const index = edgeRows.get(edge.id ?? '') ?? 0;
            nodeRows.set(edge.source, [...(nodeRows.get(edge.source) ?? []), index]);
            nodeRows.set(edge.target, [...(nodeRows.get(edge.target) ?? []), index]);
        });
        const maxNode = Math.max(1, ...result.nodes.map(({ value }) => value));
        const nodes = [];
        result.links.forEach((link, index) => {
            const sourceRow = edgeRows.get(link.id) ?? index;
            const selected = path.links.includes(link.id);
            const points = link.path.map((point) => toPlot(context, point));
            const color = palette(context, link.sourceOrder, result.nodes.length);
            nodes.push({
                type: 'path',
                ...datumBase(context, `${layer.id}:flow-link:${link.id}`, sourceRow, -1, {
                    kind: 'flow-link',
                    id: link.id,
                    source: link.source,
                    target: link.target,
                    value: link.value,
                    sourceLinkOrder: link.sourceLinkOrder,
                    targetLinkOrder: link.targetLinkOrder,
                    linkSort: optionString(layer.mark.options.linkSort, ['input', 'ascending', 'descending', 'authored'], 'input'),
                    selectedPath: selected,
                    directed: true,
                    interaction: ['path', 'drag'],
                    sourceRowIndices: [sourceRow],
                }),
                points,
                closed: false,
                stroke: colorWithOpacity(color, selected ? 0.95 : 0.5),
                lineWidth: Math.max(2, link.thickness * 18),
                lineCap: 'round',
                lineJoin: 'round',
            });
        });
        const nodeWidth = Math.max(10, Math.min(18, plot.width * 0.035));
        result.nodes.forEach((item, index) => {
            const center = toPlot(context, item);
            const height = Math.max(12, (item.value / maxNode) * Math.min(70, plot.height * 0.24));
            const rows = sourceRowsTooltip(nodeRows.get(item.id) ?? [0]);
            const sourceRow = rows[0] ?? 0;
            const selected = path.nodes.includes(item.id);
            const color = palette(context, item.column, Math.max(1, ...result.nodes.map(({ column }) => column + 1)));
            const tooltip = {
                kind: 'flow-node',
                id: item.id,
                stage: item.column,
                order: item.order,
                input: item.input,
                output: item.output,
                value: item.value,
                balanced: item.balanced,
                draggable: true,
                selectedPath: selected,
                cycles: result.cycles.map((cycle) => cycle.join(' -> ')),
                imbalance: result.imbalances.find(({ id }) => id === item.id)?.difference ?? 0,
                interaction: ['drag', 'path'],
                sourceRowIndices: rows,
            };
            nodes.push({
                type: 'rect',
                ...datumBase(context, `${layer.id}:flow-node:${item.id}`, sourceRow, 1, tooltip, {
                    kind: 'flow-node',
                    id: item.id,
                    position: { x: item.x, y: item.y },
                    plot: { ...plot },
                }),
                x: center.x - nodeWidth / 2,
                y: center.y - height / 2,
                width: nodeWidth,
                height,
                fill: layer.mark.fill ?? color,
                stroke: selected ? theme.colors.focus : (layer.mark.stroke ?? theme.colors.background),
                lineWidth: selected ? 3 : (layer.mark.lineWidth ?? 1.5),
                cornerRadius: layer.mark.cornerRadius ?? 2,
            });
            const label = textNode$2(context, `${layer.id}:flow-label:${item.id}`, center.x + nodeWidth / 2 + 5, center.y, item.id, {
                align: 'left',
                fill: theme.colors.mutedText,
                size: Math.max(9, theme.typography.fontSize - 1),
            });
            nodes.push({ ...label, ...datumBase(context, label.id, sourceRow, 2, tooltip) });
        });
        return nodes;
    };
    function dataValueStrings(value) {
        if (Array.isArray(value))
            return value.filter((item) => typeof item === 'string' && item.trim() !== '');
        if (typeof value !== 'string')
            return [];
        return value
            .split(/[|,;]/u)
            .map((item) => item.trim())
            .filter((item) => item !== '');
    }
    function networkInputs(context) {
        const { layer, table } = context;
        const sourceField = layer.mark.fields.source ?? layer.x.field;
        const targetField = layer.mark.fields.target ?? layer.y.field;
        const nodeField = layer.mark.fields.node ?? layer.mark.fields.nodeId;
        const edgeIdField = layer.mark.fields.edgeId ?? layer.mark.fields.id;
        const nodeRows = new Map();
        const edgeRows = new Map();
        const labels = new Map();
        const nodes = new Map();
        const edges = [];
        const ensureNode = (id, index) => {
            if (!nodes.has(id))
                nodes.set(id, { id });
            if (!nodeRows.has(id))
                nodeRows.set(id, index);
            if (!labels.has(id))
                labels.set(id, id);
        };
        for (let index = 0; index < table.length; index += 1) {
            const source = stringValue(fieldValue(context, index, sourceField));
            const target = stringValue(fieldValue(context, index, targetField));
            if (source !== null && source !== '' && target !== null && target !== '') {
                ensureNode(source, index);
                ensureNode(target, index);
                const edgeId = edgeIdField === undefined
                    ? `edge-${index}`
                    : (stringValue(fieldValue(context, index, edgeIdField)) ?? `edge-${index}`);
                const sourcePortField = layer.mark.fields.sourcePort;
                const targetPortField = layer.mark.fields.targetPort;
                const directedField = layer.mark.fields.directed;
                const weightField = layer.mark.fields.weight ?? layer.mark.fields.value;
                const sourcePort = sourcePortField === undefined
                    ? null
                    : stringValue(fieldValue(context, index, sourcePortField));
                const targetPort = targetPortField === undefined
                    ? null
                    : stringValue(fieldValue(context, index, targetPortField));
                const directedRaw = directedField === undefined ? null : fieldValue(context, index, directedField);
                const weight = weightField === undefined
                    ? (numericValue(fieldValue(context, index, layer.y.field)) ?? 1)
                    : (numericValue(fieldValue(context, index, weightField)) ?? 1);
                edges.push({
                    id: edgeId,
                    source,
                    target,
                    weight,
                    ...(sourcePort === null || sourcePort === '' ? {} : { sourcePort }),
                    ...(targetPort === null || targetPort === '' ? {} : { targetPort }),
                    ...(typeof directedRaw === 'boolean' ? { directed: directedRaw } : {}),
                });
                edgeRows.set(edgeId, index);
            }
            const nodeId = nodeField === undefined
                ? source === null && target === null
                    ? stringValue(fieldValue(context, index, layer.x.field))
                    : null
                : stringValue(fieldValue(context, index, nodeField));
            if (nodeId === null || nodeId === '')
                continue;
            ensureNode(nodeId, index);
            nodeRows.set(nodeId, index);
            const current = nodes.get(nodeId);
            const parent = layer.mark.fields.parent === undefined
                ? null
                : stringValue(fieldValue(context, index, layer.mark.fields.parent));
            const group = layer.mark.fields.group === undefined
                ? null
                : stringValue(fieldValue(context, index, layer.mark.fields.group));
            const radius = layer.mark.fields.radius === undefined
                ? null
                : numericValue(fieldValue(context, index, layer.mark.fields.radius));
            const x = layer.mark.fields.nodeX === undefined
                ? null
                : numericValue(fieldValue(context, index, layer.mark.fields.nodeX));
            const y = layer.mark.fields.nodeY === undefined
                ? null
                : numericValue(fieldValue(context, index, layer.mark.fields.nodeY));
            const pinnedRaw = layer.mark.fields.pinned === undefined
                ? null
                : fieldValue(context, index, layer.mark.fields.pinned);
            const portsField = layer.mark.fields.ports ?? layer.mark.fields.port;
            const portValues = portsField === undefined ? [] : dataValueStrings(fieldValue(context, index, portsField));
            const portAngle = layer.mark.fields.portAngle === undefined
                ? null
                : numericValue(fieldValue(context, index, layer.mark.fields.portAngle));
            const label = layer.mark.fields.label === undefined
                ? nodeId
                : (stringValue(fieldValue(context, index, layer.mark.fields.label)) ?? nodeId);
            labels.set(nodeId, label);
            nodes.set(nodeId, {
                ...current,
                ...(parent === null || parent === '' || parent === nodeId ? {} : { parent }),
                ...(group === null || group === '' ? {} : { group }),
                ...(radius === null ? {} : { radius }),
                ...(x === null ? {} : { x }),
                ...(y === null ? {} : { y }),
                ...(typeof pinnedRaw === 'boolean' ? { pinned: pinnedRaw } : {}),
                ...(portValues.length === 0
                    ? {}
                    : {
                        ports: portValues.map((id) => (portAngle === null ? { id } : { id, angle: portAngle })),
                    }),
            });
            if (parent !== null && parent !== '' && parent !== nodeId)
                ensureNode(parent, index);
        }
        const authored = optionObject(layer.mark.options.positions);
        if (authored !== null) {
            for (const [id, value] of Object.entries(authored)) {
                const position = optionObject(value);
                const node = nodes.get(id);
                if (position === null || node === undefined)
                    continue;
                const x = typeof position.x === 'number' && Number.isFinite(position.x) ? position.x : undefined;
                const y = typeof position.y === 'number' && Number.isFinite(position.y) ? position.y : undefined;
                const pinned = typeof position.pinned === 'boolean' ? position.pinned : true;
                nodes.set(id, {
                    ...node,
                    ...(x === undefined ? {} : { x }),
                    ...(y === undefined ? {} : { y }),
                    ...(x === undefined || y === undefined ? {} : { pinned }),
                });
            }
        }
        optionStrings(layer.mark.options.pinned).forEach((id) => {
            const node = nodes.get(id);
            if (node !== undefined && node.x !== undefined && node.y !== undefined)
                nodes.set(id, { ...node, pinned: true });
        });
        return { nodes: [...nodes.values()], edges, nodeRows, edgeRows, labels };
    }
    function lassoPolygon(value) {
        if (!Array.isArray(value))
            return [];
        return value.flatMap((raw) => {
            const point = optionObject(raw);
            return point !== null &&
                typeof point.x === 'number' &&
                Number.isFinite(point.x) &&
                typeof point.y === 'number' &&
                Number.isFinite(point.y)
                ? [{ x: point.x, y: point.y }]
                : [];
        });
    }
    const compileAdvancedGraphMark = (context) => {
        if (!hasAnyOption(context, [
            'layout',
            'routing',
            'directed',
            'allowMultiedges',
            'allowSelfLoops',
            'collapsed',
            'iterations',
            'seed',
            'nodeSpacing',
            'positions',
            'pinned',
            'lasso',
        ]) &&
            context.layer.mark.fields.node === undefined &&
            context.layer.mark.fields.nodeId === undefined)
            return compileGraphMark(context);
        if (context.table.length === 0)
            return [];
        const { layer, plot, theme } = context;
        const input = networkInputs(context);
        if (input.nodes.length === 0)
            return [];
        const routing = optionString(layer.mark.options.routing, ['straight', 'quadratic', 'orthogonal'], 'quadratic');
        const result = layoutNetwork(input.nodes, input.edges, {
            layout: optionString(layer.mark.options.layout, ['force', 'radial', 'grid', 'dag'], 'force'),
            routing,
            directed: optionBoolean(layer.mark.options.directed),
            allowMultiedges: optionBoolean(layer.mark.options.allowMultiedges, true),
            allowSelfLoops: optionBoolean(layer.mark.options.allowSelfLoops, true),
            collapsed: optionStrings(layer.mark.options.collapsed),
            iterations: Math.floor(optionNumber(layer.mark.options.iterations, 120)),
            seed: optionNumber(layer.mark.options.seed, 1),
            nodeSpacing: optionNumber(layer.mark.options.nodeSpacing, 0.08),
        });
        const lasso = lassoPolygon(layer.mark.options.lasso);
        const selected = new Set(lasso.length >= 3 ? selectNetworkNodes(result, lasso) : []);
        const parentById = new Map(input.nodes.map(({ id, parent }) => [id, parent === undefined ? null : parent]));
        const collapsed = new Set(optionStrings(layer.mark.options.collapsed));
        const owner = (id) => {
            let current = id;
            const visited = new Set();
            while (!visited.has(current)) {
                visited.add(current);
                const parent = parentById.get(current);
                if (parent === null || parent === undefined)
                    return current;
                if (collapsed.has(parent))
                    return parent;
                current = parent;
            }
            return id;
        };
        const edgeSourceRows = (source, target, id) => sourceRowsTooltip(input.edges.flatMap((candidate) => {
            if ((id === undefined || candidate.id === id || id.startsWith('collapsed:')) &&
                owner(candidate.source) === source &&
                owner(candidate.target) === target)
                return [input.edgeRows.get(candidate.id ?? '') ?? 0];
            return [];
        }));
        const maxWeight = Math.max(1, ...result.edges.map(({ weight }) => weight));
        const nodes = [];
        result.edges.forEach((edge, index) => {
            const sourceRows = edgeSourceRows(edge.source, edge.target, edge.id);
            const sourceRow = sourceRows[0] ?? input.edgeRows.get(edge.id) ?? 0;
            const normalized = edge.points;
            const plotPoints = normalized.map((point) => toPlot(context, point));
            const points = routing === 'quadratic' && plotPoints.length === 3 ? quadratic(plotPoints) : plotPoints;
            const color = palette(context, index, result.edges.length);
            const tooltip = {
                kind: 'network-edge',
                id: edge.id,
                source: edge.source,
                target: edge.target,
                weight: edge.weight,
                directed: edge.directed,
                multiedge: edge.parallelCount > 1,
                parallelIndex: edge.parallelIndex,
                parallelCount: edge.parallelCount,
                selfLoop: edge.selfLoop,
                routing,
                sourcePort: edge.sourcePort,
                targetPort: edge.targetPort,
                interaction: ['path', 'lasso'],
                sourceRowIndices: sourceRows.length === 0 ? [sourceRow] : sourceRows,
            };
            nodes.push({
                type: 'path',
                ...datumBase(context, `${layer.id}:network-edge:${edge.id}`, sourceRow, -1, tooltip),
                points,
                closed: false,
                stroke: colorWithOpacity(layer.mark.stroke ?? color, 0.68),
                lineWidth: Math.max(1.2, (edge.weight / maxWeight) * 5),
                lineCap: 'round',
                lineJoin: 'round',
            });
            if (edge.directed && points.length >= 2) {
                const end = points.at(-1);
                const before = points.at(-2);
                const angle = Math.atan2(end.y - before.y, end.x - before.x);
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:network-arrow:${edge.id}`, { zIndex: layer.zIndex + 0.5 }),
                    points: [
                        end,
                        pointOnCircle(end.x, end.y, 7, angle + Math.PI * 0.8),
                        pointOnCircle(end.x, end.y, 7, angle - Math.PI * 0.8),
                    ],
                    closed: true,
                    fill: layer.mark.stroke ?? color,
                    lineWidth: 0,
                });
            }
        });
        result.nodes.forEach((item, index) => {
            const center = toPlot(context, item);
            const radius = Math.max(5, item.radius * Math.min(plot.width, plot.height));
            const sourceRows = sourceRowsTooltip([
                input.nodeRows.get(item.id) ?? 0,
                ...input.nodes.flatMap(({ id }) => owner(id) === item.id && id !== item.id ? [input.nodeRows.get(id) ?? 0] : []),
                ...input.edges.flatMap((edge) => owner(edge.source) === item.id || owner(edge.target) === item.id
                    ? [input.edgeRows.get(edge.id ?? '') ?? 0]
                    : []),
            ]);
            const sourceRow = sourceRows[0] ?? input.nodeRows.get(item.id) ?? 0;
            const color = palette(context, index, result.nodes.length);
            const tooltip = {
                kind: 'network-node',
                id: item.id,
                label: input.labels.get(item.id) ?? item.id,
                layout: result.layout,
                parent: item.parent,
                group: item.group,
                pinned: item.pinned,
                compound: item.compound,
                collapsed: item.collapsed,
                hiddenCount: item.hiddenCount,
                ports: item.ports.map(({ id }) => id),
                selected: selected.has(item.id),
                draggable: true,
                lassoEligible: true,
                cycles: result.cycles.map((cycle) => cycle.join(' -> ')),
                topologicalOrder: result.topologicalOrder,
                interaction: ['drag', 'pin', 'collapse', 'lasso'],
                sourceRowIndices: sourceRows,
            };
            nodes.push({
                type: 'circle',
                ...datumBase(context, `${layer.id}:network-node:${item.id}`, sourceRow, 1, tooltip, {
                    kind: 'network-node',
                    id: item.id,
                    position: { x: item.x, y: item.y },
                    plot: { ...plot },
                    pinned: item.pinned,
                    compound: item.compound,
                    collapsed: item.collapsed,
                }),
                cx: center.x,
                cy: center.y,
                radius,
                fill: colorWithOpacity(layer.mark.fill ?? color, item.compound ? 0.38 : 0.92),
                stroke: selected.has(item.id)
                    ? theme.colors.focus
                    : (layer.mark.stroke ?? theme.colors.background),
                lineWidth: selected.has(item.id) ? 3 : item.collapsed ? 2.5 : (layer.mark.lineWidth ?? 1.5),
                ...(item.collapsed ? { dash: [4, 3] } : {}),
            });
            item.ports.forEach((port) => {
                const position = toPlot(context, port);
                nodes.push({
                    type: 'circle',
                    ...datumBase(context, `${layer.id}:network-port:${item.id}:${port.id}`, sourceRow, 2, {
                        kind: 'network-port',
                        node: item.id,
                        port: port.id,
                        draggable: true,
                        sourceRowIndices: [sourceRow],
                    }),
                    cx: position.x,
                    cy: position.y,
                    radius: 3,
                    fill: theme.colors.background,
                    stroke: color,
                    lineWidth: 1.5,
                });
            });
            const label = textNode$2(context, `${layer.id}:network-label:${item.id}`, center.x, center.y + radius + 9, input.labels.get(item.id) ?? item.id, { fill: theme.colors.mutedText, size: Math.max(9, theme.typography.fontSize - 1) });
            nodes.push({ ...label, ...datumBase(context, label.id, sourceRow, 3, tooltip) });
        });
        return nodes;
    };
    function chordEdges(context) {
        const { layer, table } = context;
        const sourceField = layer.mark.fields.source ?? layer.x.field;
        const targetField = layer.mark.fields.target ?? layer.y.field;
        const valueField = layer.mark.fields.value ?? layer.mark.fields.weight;
        const edges = [];
        const rows = [];
        for (let index = 0; index < table.length; index += 1) {
            const source = stringValue(fieldValue(context, index, sourceField));
            const target = stringValue(fieldValue(context, index, targetField));
            const value = valueField === undefined
                ? numericValue(fieldValue(context, index, layer.y.field))
                : numericValue(fieldValue(context, index, valueField));
            if (source === null || target === null || value === null || value < 0)
                continue;
            edges.push({ source, target, value });
            rows.push(index);
        }
        return { edges, rows };
    }
    function chordInput(context) {
        const rawMatrix = context.layer.mark.options.matrix;
        if (Array.isArray(rawMatrix)) {
            if (!rawMatrix.every(Array.isArray))
                throw new GraflumeError('INVALID_DATA', 'Chord matrix rows must be arrays.', {
                    path: '$.mark.options.matrix',
                });
            const authoredIds = context.layer.mark.options.matrixIds;
            const ids = Array.isArray(authoredIds)
                ? authoredIds.map((id) => String(id))
                : rawMatrix.map((_row, index) => String(index));
            const source = {
                ids,
                matrix: rawMatrix,
            };
            const directed = optionBoolean(context.layer.mark.options.directed);
            const edges = chordMatrixToEdges(source, { directed });
            return { source, edges, rows: edges.map(() => 0) };
        }
        const input = chordEdges(context);
        return { source: input.edges, edges: input.edges, rows: input.rows };
    }
    const compileAdvancedChordMark = (context) => {
        if (!hasAnyOption(context, [
            'directed',
            'padAngle',
            'groupOrder',
            'subgroupOrder',
            'matrix',
            'matrixIds',
        ]))
            return compileChordMark(context);
        if (context.table.length === 0)
            return [];
        const { layer, plot, theme } = context;
        const input = chordInput(context);
        if (input.edges.length === 0 && Array.isArray(input.source))
            return [];
        const result = layoutChord(input.source, {
            directed: optionBoolean(layer.mark.options.directed),
            padAngle: optionNumber(layer.mark.options.padAngle, 0.02),
            groupOrder: optionString(layer.mark.options.groupOrder, ['input', 'ascending', 'descending'], 'input'),
            subgroupOrder: optionString(layer.mark.options.subgroupOrder, ['input', 'ascending', 'descending'], 'input'),
        });
        const cx = plot.x + plot.width / 2;
        const cy = plot.y + plot.height / 2;
        const outerRadius = Math.max(24, Math.min(plot.width, plot.height) * 0.42);
        const innerRadius = outerRadius * 0.78;
        const nodes = [];
        const availableRibbonRows = input.edges.map((edge, index) => ({
            edge,
            row: input.rows[index] ?? 0,
            used: false,
        }));
        result.ribbons.forEach((ribbon, index) => {
            const sourceAngle = (ribbon.sourceStartAngle + ribbon.sourceEndAngle) / 2;
            const targetAngle = (ribbon.targetStartAngle + ribbon.targetEndAngle) / 2;
            const source = pointOnCircle(cx, cy, innerRadius, sourceAngle);
            const target = pointOnCircle(cx, cy, innerRadius, targetAngle);
            const points = ribbon.selfLoop
                ? [
                    source,
                    pointOnCircle(cx, cy, innerRadius * 0.45, sourceAngle - 0.45),
                    pointOnCircle(cx, cy, innerRadius * 0.45, sourceAngle + 0.45),
                    source,
                ]
                : quadratic([source, { x: cx, y: cy }, target]);
            const provenance = availableRibbonRows.find((candidate) => !candidate.used &&
                candidate.edge.source === ribbon.source &&
                candidate.edge.target === ribbon.target &&
                candidate.edge.value === ribbon.value);
            if (provenance !== undefined)
                provenance.used = true;
            const sourceRow = provenance?.row ?? input.rows[index] ?? 0;
            const color = palette(context, index, result.ribbons.length);
            const tooltip = {
                kind: 'chord-ribbon',
                source: ribbon.source,
                target: ribbon.target,
                value: ribbon.value,
                directed: ribbon.directed,
                selfLoop: ribbon.selfLoop,
                sourceStartAngle: ribbon.sourceStartAngle,
                sourceEndAngle: ribbon.sourceEndAngle,
                targetStartAngle: ribbon.targetStartAngle,
                targetEndAngle: ribbon.targetEndAngle,
                sourceSubgroupOrder: ribbon.sourceSubgroupOrder,
                targetSubgroupOrder: ribbon.targetSubgroupOrder,
                subgroupOrder: optionString(layer.mark.options.subgroupOrder, ['input', 'ascending', 'descending'], 'input'),
                sourceRowIndices: [sourceRow],
            };
            nodes.push({
                type: 'path',
                ...datumBase(context, `${layer.id}:chord-ribbon:${index}`, sourceRow, -1, tooltip),
                points,
                closed: false,
                stroke: colorWithOpacity(color, 0.52),
                lineWidth: Math.max(2, Math.sqrt(ribbon.value) * 4),
                lineCap: 'round',
                lineJoin: 'round',
            });
            if (ribbon.directed && !ribbon.selfLoop) {
                const before = points.at(-2);
                const end = points.at(-1);
                const angle = Math.atan2(end.y - before.y, end.x - before.x);
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${layer.id}:chord-arrow:${index}`, { zIndex: layer.zIndex + 0.5 }),
                    points: [
                        end,
                        pointOnCircle(end.x, end.y, 7, angle + 2.45),
                        pointOnCircle(end.x, end.y, 7, angle - 2.45),
                    ],
                    closed: true,
                    fill: color,
                    lineWidth: 0,
                });
            }
        });
        result.groups.forEach((group, index) => {
            const relatedRows = input.edges.flatMap((edge, edgeIndex) => edge.source === group.id || edge.target === group.id ? [input.rows[edgeIndex] ?? 0] : []);
            const sourceRow = relatedRows[0] ?? 0;
            const color = palette(context, index, result.groups.length);
            const matrixRow = result.matrix[result.ids.indexOf(group.id)] ?? [];
            const tooltip = {
                kind: 'chord-group',
                id: group.id,
                value: group.value,
                inbound: group.inbound,
                outbound: group.outbound,
                matrix: matrixRow,
                groupOrder: optionString(layer.mark.options.groupOrder, ['input', 'ascending', 'descending'], 'input'),
                padAngle: optionNumber(layer.mark.options.padAngle, 0.02),
                sourceRowIndices: sourceRowsTooltip(relatedRows),
            };
            nodes.push({
                type: 'path',
                ...datumBase(context, `${layer.id}:chord-group:${group.id}`, sourceRow, 1, tooltip),
                points: annularSector(cx, cy, innerRadius, outerRadius, group.startAngle, group.endAngle),
                closed: true,
                fill: layer.mark.fill ?? color,
                stroke: layer.mark.stroke ?? theme.colors.background,
                lineWidth: layer.mark.lineWidth ?? 1.5,
                lineJoin: 'round',
            });
            const middle = (group.startAngle + group.endAngle) / 2;
            const label = pointOnCircle(cx, cy, outerRadius + 14, middle);
            nodes.push(textNode$2(context, `${layer.id}:chord-label:${group.id}`, label.x, label.y, group.id, {
                fill: theme.colors.mutedText,
                size: Math.max(9, theme.typography.fontSize - 1),
            }));
        });
        return nodes;
    };
    const compileAdvancedFunnelMark = (context) => {
        if (!hasAnyOption(context, ['neckWidth', 'neckHeight', 'labelGap', 'outsideLabels', 'semantics']) &&
            typeof context.layer.mark.options.sort !== 'string')
            return compileFunnelMark(context);
        if (context.table.length === 0)
            return [];
        const { layer, table, plot, theme } = context;
        const idField = layer.mark.fields.stage ?? layer.mark.fields.id ?? layer.x.field;
        const valueField = layer.mark.fields.value ?? layer.y.field;
        const orderField = layer.mark.fields.order;
        const labelField = layer.mark.fields.label;
        const input = [];
        const rows = new Map();
        for (let index = 0; index < table.length; index += 1) {
            const id = stringValue(fieldValue(context, index, idField));
            const value = numericValue(fieldValue(context, index, valueField));
            if (id === null || id === '' || value === null || value < 0)
                continue;
            const order = orderField === undefined ? null : numericValue(fieldValue(context, index, orderField));
            const label = labelField === undefined ? id : (stringValue(fieldValue(context, index, labelField)) ?? id);
            input.push({ id, value, label, ...(order === null ? {} : { order }) });
            rows.set(id, index);
        }
        if (input.length === 0)
            return [];
        const stages = funnelStages(input, {
            sort: optionString(layer.mark.options.sort, ['input', 'value-descending', 'order'], 'input'),
            neckWidth: optionNumber(layer.mark.options.neckWidth, 0.3),
            neckHeight: optionNumber(layer.mark.options.neckHeight, 0.25),
            labelGap: optionNumber(layer.mark.options.labelGap, 0.055),
        });
        const outside = optionBoolean(layer.mark.options.outsideLabels);
        const bodyWidth = plot.width * (outside ? 0.72 : 1);
        const cx = plot.x + bodyWidth / 2;
        const nodes = [];
        stages.forEach((stage, index) => {
            const sourceRows = sourceRowsTooltip([
                rows.get(stage.id) ?? 0,
                rows.get(stages[index - 1]?.id ?? stage.id) ?? 0,
                rows.get(stages[0]?.id ?? stage.id) ?? 0,
            ]);
            const sourceRow = sourceRows[0] ?? rows.get(stage.id) ?? 0;
            const y0 = plot.y + stage.y0 * plot.height;
            const y1 = plot.y + stage.y1 * plot.height;
            const topWidth = stage.topWidth * bodyWidth;
            const bottomWidth = stage.bottomWidth * bodyWidth;
            const fill = layer.mark.fill ?? palette(context, index, stages.length);
            const tooltip = {
                kind: 'funnel-stage',
                stage: stage.id,
                label: stage.label,
                value: stage.value,
                input: stage.input,
                output: stage.output,
                conversion: stage.conversion,
                dropoff: stage.dropoff,
                dropoffRate: stage.dropoffRate,
                cumulative: stage.cumulative,
                neckWidth: optionNumber(layer.mark.options.neckWidth, 0.3),
                neckHeight: optionNumber(layer.mark.options.neckHeight, 0.25),
                outsideLabel: outside,
                sourceRowIndices: sourceRows,
            };
            nodes.push({
                type: 'path',
                ...datumBase(context, `${layer.id}:funnel-stage:${stage.id}`, sourceRow, 0, tooltip),
                points: [
                    { x: cx - topWidth / 2, y: y0 + 1 },
                    { x: cx + topWidth / 2, y: y0 + 1 },
                    { x: cx + bottomWidth / 2, y: y1 - 1 },
                    { x: cx - bottomWidth / 2, y: y1 - 1 },
                ],
                closed: true,
                fill,
                stroke: layer.mark.stroke ?? theme.colors.background,
                lineWidth: layer.mark.lineWidth ?? 1.5,
                lineJoin: 'round',
            });
            const y = plot.y + stage.labelY * plot.height;
            const labelX = outside ? plot.x + bodyWidth + 12 : cx;
            if (outside) {
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:funnel-label-rule:${stage.id}`, { zIndex: layer.zIndex + 1 }),
                    x1: cx + Math.max(topWidth, bottomWidth) / 2,
                    y1: (y0 + y1) / 2,
                    x2: labelX - 4,
                    y2: y,
                    stroke: theme.colors.axis,
                    lineWidth: 1,
                });
            }
            const label = textNode$2(context, `${layer.id}:funnel-label:${stage.id}`, labelX, y, `${stage.label}  ${stage.value}`, {
                align: outside ? 'left' : 'center',
                fill: outside ? theme.colors.text : readableTextColor(fill, '#ffffff', '#0f172a'),
                size: Math.max(9, theme.typography.fontSize - 1),
                weight: 700,
            });
            nodes.push({ ...label, ...datumBase(context, label.id, sourceRow, 2, tooltip) });
        });
        return nodes;
    };
    function parallelAxes(context) {
        const authored = context.layer.mark.options.axes;
        if (Array.isArray(authored)) {
            return authored.flatMap((raw) => {
                const axis = optionObject(raw);
                if (axis === null || typeof axis.field !== 'string' || axis.field.trim() === '')
                    return [];
                const type = optionString(axis.type, ['linear', 'log', 'ordinal'], 'linear');
                const missing = optionString(axis.missing, ['gap', 'top', 'bottom', 'middle'], 'gap');
                const domain = Array.isArray(axis.domain)
                    ? axis.domain.filter((value) => typeof value === 'string' || (typeof value === 'number' && Number.isFinite(value)))
                    : undefined;
                return [
                    {
                        field: axis.field,
                        type,
                        invert: axis.invert === true,
                        missing,
                        ...(domain === undefined ? {} : { domain }),
                    },
                ];
            });
        }
        const fields = optionStrings(context.layer.mark.options.dimensions);
        const fallback = fields.length >= 2 ? fields : [context.layer.x.field, context.layer.y.field];
        return [...new Set(fallback)].map((field) => ({ field, type: 'linear', missing: 'gap' }));
    }
    function parallelBrushes(value) {
        if (!Array.isArray(value))
            return [];
        return value.flatMap((raw) => {
            const brush = optionObject(raw);
            if (brush === null || typeof brush.field !== 'string' || !Array.isArray(brush.extents))
                return [];
            const extents = brush.extents.flatMap((rawExtent) => {
                if (!Array.isArray(rawExtent) ||
                    rawExtent.length !== 2 ||
                    typeof rawExtent[0] !== 'number' ||
                    !Number.isFinite(rawExtent[0]) ||
                    typeof rawExtent[1] !== 'number' ||
                    !Number.isFinite(rawExtent[1]))
                    return [];
                return [[rawExtent[0], rawExtent[1]]];
            });
            return [{ field: brush.field, extents }];
        });
    }
    const compileAdvancedParallelMark = (context) => {
        if (!hasAnyOption(context, ['axes', 'brushes', 'combine']))
            return compileParallelMark(context);
        if (context.table.length === 0)
            return [];
        const { layer, table, plot, theme } = context;
        const axes = parallelAxes(context);
        const brushes = parallelBrushes(layer.mark.options.brushes);
        const combine = optionString(layer.mark.options.combine, ['union', 'intersection'], 'intersection');
        const rows = Array.from({ length: table.length }, (_, index) => table.row(index));
        const projection = projectParallelRows(rows, axes, brushes, combine);
        const xFor = (index) => plot.x + (index / Math.max(1, axes.length - 1)) * plot.width;
        const nodes = [];
        projection.axes.forEach((axis, index) => {
            const x = xFor(index);
            nodes.push({
                type: 'line',
                ...nodeBase(`${layer.id}:parallel-axis:${axis.field}`, { zIndex: layer.zIndex }),
                x1: x,
                y1: plot.y,
                x2: x,
                y2: plot.y + plot.height,
                stroke: theme.colors.axis,
                lineWidth: 1.25,
                lineCap: 'round',
            });
            nodes.push({
                type: 'rect',
                ...datumBase(context, `${layer.id}:parallel-axis-hit:${axis.field}`, 0, 0.25, {
                    kind: 'parallel-axis',
                    field: axis.field,
                    index,
                    count: axes.length,
                    invert: axis.invert === true,
                }, {
                    kind: 'parallel-axis',
                    field: axis.field,
                    index,
                    count: axes.length,
                    invert: axis.invert === true,
                    plot: { ...plot },
                }),
                x: x - 7,
                y: plot.y,
                width: 14,
                height: plot.height,
                fill: 'rgba(0, 0, 0, 0)',
                lineWidth: 0,
                cornerRadius: 0,
            });
            nodes.push(textNode$2(context, `${layer.id}:parallel-axis-label:${axis.field}`, x, plot.y - 7, `${axis.field}${axis.invert === true ? ' ↓' : ''}${axis.type === 'log' ? ' (log)' : ''}`, {
                baseline: 'bottom',
                fill: theme.colors.mutedText,
                size: Math.max(9, theme.typography.fontSize - 1),
                weight: 700,
            }));
            const brush = brushes.find(({ field }) => field === axis.field);
            brush?.extents.forEach((extent, extentIndex) => {
                const low = clamp$2(Math.min(...extent), 0, 1);
                const high = clamp$2(Math.max(...extent), 0, 1);
                nodes.push({
                    type: 'rect',
                    ...nodeBase(`${layer.id}:parallel-brush:${axis.field}:${extentIndex}`, {
                        zIndex: layer.zIndex + 0.5,
                        opacity: 0.22,
                    }),
                    x: x - 7,
                    y: plot.y + (1 - high) * plot.height,
                    width: 14,
                    height: Math.max(1, (high - low) * plot.height),
                    fill: theme.colors.focus,
                    stroke: theme.colors.focus,
                    lineWidth: 1,
                    cornerRadius: 2,
                });
            });
        });
        const colorField = layer.mark.fields.color ?? layer.mark.fields.group;
        const colorValues = colorField === undefined || !table.has(colorField) ? [] : table.unique(colorField);
        projection.rows.forEach((item) => {
            const segments = [];
            let current = [];
            axes.forEach((axis, axisIndex) => {
                const value = item.values[axis.field] ?? null;
                if (value === null) {
                    if (current.length >= 2)
                        segments.push(current);
                    current = [];
                    return;
                }
                current.push({ x: xFor(axisIndex), y: plot.y + (1 - value) * plot.height });
            });
            if (current.length >= 2)
                segments.push(current);
            const colorKey = colorField === undefined || !table.has(colorField)
                ? null
                : stringValue(table.value(item.index, colorField));
            const colorIndex = colorKey === null ? item.index : Math.max(0, colorValues.indexOf(colorKey));
            const color = palette(context, colorIndex, colorValues.length || table.length);
            const tooltip = {
                kind: 'parallel-row',
                selected: item.selected,
                axes: axes.map(({ field, type, invert, missing }) => `${field}:${type ?? 'linear'}:${invert === true ? 'inverted' : 'normal'}:${missing ?? 'gap'}`),
                projected: axes.map(({ field }) => `${field}=${item.values[field] ?? 'gap'}`),
                brushCombine: combine,
                brushes: brushes.map(({ field, extents }) => `${field}:${extents.map((extent) => extent.join('-')).join('|')}`),
                interaction: ['axis-reorder', 'axis-invert', 'axis-brush'],
                sourceRowIndices: [item.index],
            };
            segments.forEach((points, segmentIndex) => {
                nodes.push({
                    type: 'path',
                    ...datumBase(context, `${layer.id}:parallel-row:${item.index}:${segmentIndex}`, item.index, 1, tooltip),
                    points,
                    closed: false,
                    stroke: colorWithOpacity(layer.mark.stroke ?? color, item.selected ? 0.82 : 0.12),
                    lineWidth: item.selected ? (layer.mark.lineWidth ?? 1.8) : 1,
                    lineCap: 'round',
                    lineJoin: 'round',
                });
            });
        });
        return nodes;
    };
    function setRegionKey(sets) {
        return [...new Set(sets)].sort().join('\u0000');
    }
    function membershipRows(context) {
        const { layer, table } = context;
        const idField = layer.mark.fields.id ?? layer.x.field;
        const setsField = layer.mark.fields.sets ?? layer.mark.fields.memberships ?? 'sets';
        const sizeField = layer.mark.fields.size ?? layer.mark.fields.count;
        const membersField = layer.mark.fields.members;
        const regionRows = new Map();
        if (sizeField !== undefined) {
            const data = [];
            const rows = new Map();
            for (let index = 0; index < table.length; index += 1) {
                const sets = dataValueStrings(fieldValue(context, index, setsField));
                const size = numericValue(fieldValue(context, index, sizeField));
                if (size === null || size < 0)
                    continue;
                const members = membersField === undefined
                    ? []
                    : dataValueStrings(fieldValue(context, index, membersField));
                data.push({ sets, size, ...(members.length === 0 ? {} : { members }) });
                const key = setRegionKey(sets);
                rows.set(key, index);
                regionRows.set(key, [...(regionRows.get(key) ?? []), index]);
            }
            return { kind: 'intersection', data, rows, regionRows };
        }
        const data = [];
        const rows = new Map();
        for (let index = 0; index < table.length; index += 1) {
            const id = stringValue(fieldValue(context, index, idField));
            if (id === null || id === '')
                continue;
            const sets = dataValueStrings(fieldValue(context, index, setsField));
            data.push({ id, sets });
            rows.set(id, index);
            const key = setRegionKey(sets);
            regionRows.set(key, [...(regionRows.get(key) ?? []), index]);
        }
        return { kind: 'membership', data, rows, regionRows };
    }
    const compileAdvancedVennMark = (context) => {
        if (context.layer.mark.fields.sets === undefined &&
            context.layer.mark.fields.memberships === undefined &&
            context.layer.mark.fields.size === undefined &&
            context.layer.mark.fields.count === undefined &&
            !hasAnyOption(context, ['query', 'proportional', 'quality']))
            return compileVennMark(context);
        if (context.table.length === 0)
            return [];
        const { layer, plot, theme } = context;
        const membership = membershipRows(context);
        if (membership.data.length === 0)
            return [];
        const result = analyzeSets(membership.data);
        if (result.circles.length === 0)
            return [];
        const minX = Math.min(...result.circles.map(({ x, radius }) => x - radius));
        const maxX = Math.max(...result.circles.map(({ x, radius }) => x + radius));
        const minY = Math.min(...result.circles.map(({ y, radius }) => y - radius));
        const maxY = Math.max(...result.circles.map(({ y, radius }) => y + radius));
        const sourceWidth = Math.max(Number.EPSILON, maxX - minX);
        const sourceHeight = Math.max(Number.EPSILON, maxY - minY);
        const scale = Math.min((plot.width * 0.86) / sourceWidth, (plot.height * 0.8) / sourceHeight);
        const offsetX = plot.x + (plot.width - sourceWidth * scale) / 2 - minX * scale;
        const offsetY = plot.y + (plot.height - sourceHeight * scale) / 2 - minY * scale;
        const mappedCircles = result.circles.map((circle) => ({
            ...circle,
            x: offsetX + circle.x * scale,
            y: offsetY + circle.y * scale,
            radius: circle.radius * scale,
        }));
        const query = optionObject(layer.mark.options.query);
        const included = optionStrings(query?.included);
        const excluded = optionStrings(query?.excluded);
        const queriedIntersections = membership.kind === 'intersection'
            ? membership.data.filter(({ sets }) => included.every((set) => sets.includes(set)) &&
                excluded.every((set) => !sets.includes(set)))
            : [];
        const queryMembers = membership.kind === 'membership'
            ? querySetRegion(membership.data, included, excluded)
            : queriedIntersections.flatMap(({ members = [] }) => members);
        const querySize = membership.kind === 'membership'
            ? queryMembers.length
            : queriedIntersections.reduce((sum, { size }) => sum + size, 0);
        const nodes = [];
        mappedCircles.forEach((circle, index) => {
            const memberIds = membership.kind === 'membership'
                ? querySetRegion(membership.data, [circle.id])
                : membership.data.flatMap(({ sets, members = [] }) => sets.includes(circle.id) ? members : []);
            const rows = sourceRowsTooltip(membership.kind === 'membership'
                ? memberIds.map((id) => membership.rows.get(id) ?? 0)
                : membership.data.flatMap(({ sets }) => sets.includes(circle.id) ? (membership.regionRows.get(setRegionKey(sets)) ?? []) : []));
            const sourceRow = rows[0] ?? 0;
            const color = palette(context, index, mappedCircles.length);
            const tooltip = {
                kind: 'venn-set',
                set: circle.id,
                size: circle.size,
                members: memberIds,
                input: membership.kind,
                proportional: true,
                qualityStress: result.quality.stress,
                maximumRelativeError: result.quality.maximumRelativeError,
                hitRegion: hitSetRegion(result.circles, result.circles[index]),
                interaction: ['region-query', 'region-hit'],
                sourceRowIndices: rows,
            };
            nodes.push({
                type: 'circle',
                ...datumBase(context, `${layer.id}:venn-set:${circle.id}`, sourceRow, index * 0.01, tooltip),
                cx: circle.x,
                cy: circle.y,
                radius: circle.radius,
                fill: colorWithOpacity(layer.mark.fill ?? color, 0.3),
                stroke: layer.mark.stroke ?? color,
                lineWidth: layer.mark.lineWidth ?? 2,
            });
            nodes.push(textNode$2(context, `${layer.id}:venn-label:${circle.id}`, circle.x, circle.y - circle.radius + 14, `${circle.id} (${circle.size})`, {
                fill: theme.colors.text,
                size: Math.max(9, theme.typography.fontSize - 1),
                weight: 750,
            }));
        });
        result.intersections.forEach((intersection, index) => {
            if (intersection.sets.length === 0)
                return;
            const circles = intersection.sets.flatMap((id) => {
                const circle = mappedCircles.find((candidate) => candidate.id === id);
                return circle === undefined ? [] : [circle];
            });
            if (circles.length === 0)
                return;
            const x = circles.reduce((sum, circle) => sum + circle.x, 0) / circles.length;
            const y = circles.reduce((sum, circle) => sum + circle.y, 0) / circles.length;
            const rows = sourceRowsTooltip(membership.kind === 'membership'
                ? intersection.members.map((id) => membership.rows.get(id) ?? 0)
                : (membership.regionRows.get(setRegionKey(intersection.sets)) ?? []));
            const sourceRow = rows[0] ?? 0;
            const tooltip = {
                kind: 'venn-region',
                sets: intersection.sets,
                size: intersection.size,
                members: intersection.members,
                queryIncluded: included,
                queryExcluded: excluded,
                queryMembers,
                querySize,
                input: membership.kind,
                hitRegion: hitSetRegion(result.circles, {
                    x: circles.reduce((sum, circle) => sum + (circle.x - offsetX) / scale, 0) / circles.length,
                    y: circles.reduce((sum, circle) => sum + (circle.y - offsetY) / scale, 0) / circles.length,
                }),
                qualityStress: result.quality.stress,
                maximumRelativeError: result.quality.maximumRelativeError,
                interaction: ['region-query', 'region-hit'],
                sourceRowIndices: rows,
            };
            nodes.push({
                type: 'circle',
                ...datumBase(context, `${layer.id}:venn-region:${index}`, sourceRow, 2, tooltip),
                cx: x,
                cy: y,
                radius: Math.max(5, Math.min(12, 5 + intersection.size)),
                fill: included.length > 0 && included.every((id) => intersection.sets.includes(id))
                    ? theme.colors.focus
                    : theme.colors.background,
                stroke: theme.colors.text,
                lineWidth: 1.5,
            });
            nodes.push(textNode$2(context, `${layer.id}:venn-region-label:${index}`, x, y, String(intersection.size), {
                size: 9,
                weight: 750,
                fill: included.length > 0 && included.every((id) => intersection.sets.includes(id))
                    ? theme.colors.background
                    : theme.colors.text,
            }));
        });
        if (query !== null) {
            nodes.push(textNode$2(context, `${layer.id}:venn-query`, plot.x + 4, plot.y + 4, membership.kind === 'membership'
                ? `query: ${queryMembers.join(', ') || '∅'}`
                : `query count: ${querySize}`, {
                align: 'left',
                baseline: 'top',
                fill: theme.colors.mutedText,
                size: Math.max(9, theme.typography.fontSize - 2),
            }));
        }
        return nodes;
    };
    const compileAdvancedWordTreeMark = (context) => {
        const rootPhrase = context.layer.mark.options.rootPhrase;
        if (typeof rootPhrase !== 'string')
            return compileWordTreeMark(context);
        if (context.table.length === 0)
            return [];
        const { layer, table, plot, theme } = context;
        const textField = layer.mark.fields.text ?? layer.x.field;
        const texts = Array.from({ length: table.length }, (_, index) => stringValue(fieldValue(context, index, textField)) ?? '');
        const direction = optionString(layer.mark.options.direction, ['prefix', 'suffix', 'reverse'], 'prefix');
        const tree = buildWordTree(texts, rootPhrase, {
            ...tokenOptions(context),
            direction,
            minimumCount: Math.floor(optionNumber(layer.mark.options.minimumCount, 1)),
            maximumDepth: Math.floor(optionNumber(layer.mark.options.maximumDepth, 6)),
            maximumChildren: Math.floor(optionNumber(layer.mark.options.maximumChildren, 12)),
        });
        const byDepth = new Map();
        tree.forEach((item) => byDepth.set(item.depth, [...(byDepth.get(item.depth) ?? []), item]));
        const maximumDepth = Math.max(0, ...tree.map(({ depth }) => depth));
        const positions = new Map();
        byDepth.forEach((items, depth) => {
            items.forEach((item, index) => {
                positions.set(item.id, {
                    x: plot.x + ((depth + 0.5) / Math.max(1, maximumDepth + 1)) * plot.width,
                    y: plot.y + ((index + 1) / (items.length + 1)) * plot.height,
                });
            });
        });
        const parentFor = (item) => item.parent === null ? undefined : tree.find(({ id }) => id === item.parent);
        const nodes = [];
        const sourceTokens = texts.map((text) => [
            ...tokenizeWords(text, { ...tokenOptions(context), ngram: 1 }),
        ]);
        tree.forEach((item, index) => {
            const position = positions.get(item.id);
            const parent = parentFor(item);
            const parentPosition = parent === undefined ? undefined : positions.get(parent.id);
            if (parentPosition !== undefined) {
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${layer.id}:word-tree-link:${item.id}`, { zIndex: layer.zIndex - 1 }),
                    x1: parentPosition.x,
                    y1: parentPosition.y,
                    x2: position.x,
                    y2: position.y,
                    stroke: theme.colors.grid,
                    lineWidth: Math.max(1, Math.sqrt(item.count)),
                    lineCap: 'round',
                });
            }
            const phrase = item.phrase.split(' ');
            const sourceRows = sourceRowsTooltip(sourceTokens.flatMap((tokens, row) => {
                for (let offset = 0; offset <= tokens.length - phrase.length; offset += 1) {
                    if (phrase.every((value, tokenIndex) => tokens[offset + tokenIndex] === value))
                        return [row];
                }
                return [];
            }));
            const sourceRow = sourceRows[0] ?? 0;
            const color = palette(context, item.depth, maximumDepth + 1);
            const tooltip = {
                kind: 'word-tree-node',
                token: item.token,
                phrase: item.phrase,
                count: item.count,
                depth: item.depth,
                parent: parent?.id ?? null,
                direction,
                rootPhrase,
                minimumCount: Math.floor(optionNumber(layer.mark.options.minimumCount, 1)),
                maximumDepth: Math.floor(optionNumber(layer.mark.options.maximumDepth, 6)),
                maximumChildren: Math.floor(optionNumber(layer.mark.options.maximumChildren, 12)),
                sourceRowIndices: sourceRows,
            };
            const label = textNode$2(context, `${layer.id}:word-tree-node:${item.id}`, position.x, position.y, item.token, {
                fill: color,
                size: clamp$2(10 + Math.sqrt(item.count) * 3, 10, 28),
                weight: item.depth === 0 ? 800 : 650,
            });
            nodes.push({
                ...label,
                ...datumBase(context, label.id, sourceRow, 1 + index * 0.001, tooltip),
            });
        });
        return nodes;
    };
    const compileAdvancedWordCloudMark = (context) => {
        if (context.layer.mark.fields.text === undefined &&
            !hasAnyOption(context, [
                'tokenize',
                'case',
                'stopwords',
                'ngram',
                'stemming',
                'locale',
                'seed',
                'padding',
                'rotations',
                'minimumFrequency',
                'maximumWords',
            ]))
            return compileWordCloudMark(context);
        if (context.table.length === 0)
            return [];
        const { layer, table, plot } = context;
        const textField = layer.mark.fields.text ?? layer.x.field;
        const texts = Array.from({ length: table.length }, (_, index) => stringValue(fieldValue(context, index, textField)) ?? '');
        const token = tokenOptions(context);
        const rotations = optionNumbers(layer.mark.options.rotations);
        const placements = layoutWordCloud(texts, {
            ...token,
            width: plot.width,
            height: plot.height,
            seed: optionNumber(layer.mark.options.seed, 1),
            padding: optionNumber(layer.mark.options.padding, 2),
            rotations: rotations.length === 0 ? [0] : rotations,
            minimumFrequency: Math.floor(optionNumber(layer.mark.options.minimumFrequency, 1)),
            maximumWords: Math.min(context.performance.maxPointMarks, Math.floor(optionNumber(layer.mark.options.maximumWords, 200))),
        });
        return placements.map((placement, index) => {
            const sourceRows = sourceRowsTooltip(texts.flatMap((text, row) => tokenizeWords(text, token).includes(placement.word) ? [row] : []));
            const sourceRow = sourceRows[0] ?? 0;
            const tooltip = {
                kind: 'word-cloud-token',
                word: placement.word,
                frequency: placement.frequency,
                seed: optionNumber(layer.mark.options.seed, 1),
                padding: optionNumber(layer.mark.options.padding, 2),
                rotation: placement.rotation,
                ngram: token.ngram ?? 1,
                stemming: token.stemming ?? 'none',
                sourceRowIndices: sourceRows,
            };
            const node = textNode$2(context, `${layer.id}:word-cloud-token:${encodeURIComponent(placement.word)}`, plot.x + placement.x, plot.y + placement.y, placement.word, {
                fill: layer.mark.fill ?? palette(context, index, placements.length),
                size: placement.fontSize,
                weight: 520 + Math.round((placement.fontSize / 54) * 220),
                rotation: placement.rotation,
            });
            return { ...node, ...datumBase(context, node.id, sourceRow, index * 0.001, tooltip) };
        });
    };

    class EventEmitter {
        #listeners = new Map();
        on(type, listener) {
            let listeners = this.#listeners.get(type);
            if (listeners === undefined) {
                listeners = new Set();
                this.#listeners.set(type, listeners);
            }
            listeners.add(listener);
            return () => this.off(type, listener);
        }
        off(type, listener) {
            const listeners = this.#listeners.get(type);
            listeners?.delete(listener);
            if (listeners?.size === 0)
                this.#listeners.delete(type);
        }
        emit(type, event) {
            for (const listener of this.#listeners.get(type) ?? []) {
                listener(event);
            }
        }
        clear() {
            this.#listeners.clear();
        }
    }

    const absoluteMaximumCapacity = 1_000_000;
    function checkedCapacity(capacity) {
        if (!Number.isInteger(capacity) || capacity < 1 || capacity > absoluteMaximumCapacity) {
            throw new GraflumeError('INVALID_DATA', `Ring-buffer capacity must be an integer from 1 to ${absoluteMaximumCapacity}.`, { path: '$.streaming.retention.maxRows' });
        }
        return capacity;
    }
    /**
     * A fixed-capacity, allocation-stable ring used by streaming data paths.
     *
     * The container never grows beyond `capacity`. `push()` returns the evicted
     * value so callers can keep stable-key and transform caches synchronized.
     */
    class BoundedRingBuffer {
        #capacity;
        #slots;
        #start = 0;
        #length = 0;
        constructor(capacity, initial = []) {
            this.#capacity = checkedCapacity(capacity);
            this.#slots = Array(this.#capacity);
            const retained = initial.slice(-this.#capacity);
            for (const value of retained)
                this.push(value);
        }
        get capacity() {
            return this.#capacity;
        }
        get length() {
            return this.#length;
        }
        at(index) {
            const resolved = index < 0 ? this.#length + index : index;
            if (!Number.isInteger(resolved) || resolved < 0 || resolved >= this.#length)
                return undefined;
            return this.#slots[(this.#start + resolved) % this.#capacity];
        }
        set(index, value) {
            if (!Number.isInteger(index) || index < 0 || index >= this.#length) {
                throw new GraflumeError('INVALID_DATA', 'Ring-buffer index is outside retained rows.');
            }
            this.#slots[(this.#start + index) % this.#capacity] = value;
        }
        push(value) {
            if (this.#length < this.#capacity) {
                const slot = (this.#start + this.#length) % this.#capacity;
                this.#slots[slot] = value;
                this.#length += 1;
                return undefined;
            }
            const evicted = this.#slots[this.#start];
            this.#slots[this.#start] = value;
            this.#start = (this.#start + 1) % this.#capacity;
            return evicted;
        }
        pushMany(values) {
            const evicted = [];
            for (const value of values) {
                const removed = this.push(value);
                if (removed !== undefined)
                    evicted.push(removed);
            }
            return evicted;
        }
        shift() {
            if (this.#length === 0)
                return undefined;
            const value = this.#slots[this.#start];
            this.#slots[this.#start] = undefined;
            this.#start = (this.#start + 1) % this.#capacity;
            this.#length -= 1;
            if (this.#length === 0)
                this.#start = 0;
            return value;
        }
        clear() {
            this.#slots.fill(undefined);
            this.#start = 0;
            this.#length = 0;
        }
        replace(values) {
            this.clear();
            this.pushMany(values);
        }
        retain(predicate) {
            const retained = [];
            const removed = [];
            this.values().forEach((value, index) => {
                (predicate(value, index) ? retained : removed).push(value);
            });
            this.clear();
            this.pushMany(retained);
            return removed;
        }
        values() {
            return Array.from({ length: this.#length }, (_, index) => this.at(index));
        }
        clone() {
            return new BoundedRingBuffer(this.#capacity, this.values());
        }
    }
    function createBoundedRingBuffer(capacity, initial = []) {
        return new BoundedRingBuffer(capacity, initial);
    }

    const DEFAULT_MAX_ROWS$1 = 100_000;
    const ABSOLUTE_MAX_ROWS$1 = 1_000_000;
    const DEFAULT_MAX_BATCH_ROWS = 100_000;
    const DEFAULT_MAX_QUEUE_BATCHES$1 = 16;
    const DEFAULT_MAX_REPLAY_BATCHES = 128;
    const STREAMING_OPTION_KEYS = new Set([
        'key',
        'mode',
        'maxBatchRows',
        'retention',
        'eventTime',
        'queue',
        'replay',
        'runtime',
        'worker',
    ]);
    const RETENTION_KEYS = new Set(['maxRows', 'time']);
    const TIME_RETENTION_KEYS = new Set(['field', 'durationMs']);
    const EVENT_TIME_KEYS = new Set(['field', 'allowedLatenessMs', 'lateData']);
    const QUEUE_KEYS = new Set(['maxBatches', 'maxRows', 'overflow']);
    const REPLAY_KEYS = new Set(['maxBatches', 'maxRows']);
    const RUNTIME_KEYS = new Set([
        'schedule',
        'maxBatchesPerFrame',
        'overflow',
        'paused',
        'followLive',
        'history',
    ]);
    const RUNTIME_HISTORY_KEYS = new Set(['maxBatches', 'pageRows']);
    const WORKER_KEYS = new Set([
        'moduleURL',
        'name',
        'maxQueueBatches',
        'maxQueueRows',
        'maxInputRows',
        'maxBinaryBytes',
        'maxTransforms',
        'overflow',
        'engine',
    ]);
    const WORKER_ENGINE_KEYS = new Set(['type', 'adapter']);
    const UPDATE_KEYS = new Set(['mode', 'rows', 'watermark']);
    const REPLAY_ENVELOPE_KEYS = new Set(['version', 'options', 'initial', 'updates']);
    function closedObject$1(value, keys, path) {
        if (!isPlainObject(value)) {
            throw new GraflumeError('INVALID_SPEC', `${path} must be an object.`, { path });
        }
        const unknown = Object.keys(value).find((key) => !keys.has(key));
        if (unknown !== undefined) {
            throw new GraflumeError('INVALID_SPEC', `Unknown ${path} property "${unknown}".`, {
                path: `${path}.${unknown}`,
            });
        }
        return value;
    }
    function requiredSafeField(value, path) {
        if (typeof value !== 'string' || value.trim() === '') {
            throw new GraflumeError('INVALID_SPEC', `${path} must be a non-empty string.`, { path });
        }
        assertSafeKey(value, path);
    }
    function validateOptionsShape(value) {
        const options = closedObject$1(value, STREAMING_OPTION_KEYS, '$.streaming');
        requiredSafeField(options.key, '$.streaming.key');
        if (options.retention !== undefined) {
            const retention = closedObject$1(options.retention, RETENTION_KEYS, '$.streaming.retention');
            if (retention.time !== undefined) {
                const time = closedObject$1(retention.time, TIME_RETENTION_KEYS, '$.streaming.retention.time');
                requiredSafeField(time.field, '$.streaming.retention.time.field');
                if (time.durationMs === undefined) {
                    throw new GraflumeError('INVALID_SPEC', '$.streaming.retention.time.durationMs is required.', { path: '$.streaming.retention.time.durationMs' });
                }
            }
        }
        if (options.eventTime !== undefined) {
            const eventTime = closedObject$1(options.eventTime, EVENT_TIME_KEYS, '$.streaming.eventTime');
            requiredSafeField(eventTime.field, '$.streaming.eventTime.field');
        }
        if (options.queue !== undefined) {
            closedObject$1(options.queue, QUEUE_KEYS, '$.streaming.queue');
        }
        if (options.replay !== undefined) {
            closedObject$1(options.replay, REPLAY_KEYS, '$.streaming.replay');
        }
        if (options.runtime !== undefined) {
            const runtime = closedObject$1(options.runtime, RUNTIME_KEYS, '$.streaming.runtime');
            if (runtime.history !== undefined) {
                closedObject$1(runtime.history, RUNTIME_HISTORY_KEYS, '$.streaming.runtime.history');
            }
        }
        if (options.worker !== undefined) {
            const worker = closedObject$1(options.worker, WORKER_KEYS, '$.streaming.worker');
            if (worker.engine !== undefined) {
                closedObject$1(worker.engine, WORKER_ENGINE_KEYS, '$.streaming.worker.engine');
            }
        }
    }
    function validateUpdateShape(value) {
        const update = closedObject$1(value, UPDATE_KEYS, '$.update');
        if (!Array.isArray(update.rows)) {
            throw new GraflumeError('INVALID_DATA', '$.update.rows must be an array.', {
                path: '$.update.rows',
            });
        }
    }
    function boundedInteger$7(value, fallback, maximum, path) {
        const resolved = value ?? fallback;
        if (!Number.isInteger(resolved) || resolved < 1 || resolved > maximum) {
            throw new GraflumeError('INVALID_SPEC', `${path} must be an integer from 1 to ${maximum}.`, {
                path,
            });
        }
        return resolved;
    }
    function nonNegative(value, fallback, path) {
        const resolved = value ?? fallback;
        if (!Number.isFinite(resolved) || resolved < 0) {
            throw new GraflumeError('INVALID_SPEC', `${path} must be a finite non-negative number.`, {
                path,
            });
        }
        return resolved;
    }
    function normalizeOptions$2(options) {
        validateOptionsShape(options);
        if (!['append', 'upsert', 'replaceLast'].includes(options.mode ?? 'append')) {
            throw new GraflumeError('INVALID_SPEC', 'Streaming mode is not supported.', {
                path: '$.streaming.mode',
            });
        }
        const overflow = (options.queue?.overflow ?? 'reject');
        if (!['reject', 'drop-oldest', 'coalesce'].includes(overflow)) {
            throw new GraflumeError('INVALID_SPEC', 'Streaming queue overflow must be "reject", "drop-oldest", or "coalesce".', { path: '$.streaming.queue.overflow' });
        }
        const retentionRows = boundedInteger$7(options.retention?.maxRows, DEFAULT_MAX_ROWS$1, ABSOLUTE_MAX_ROWS$1, '$.streaming.retention.maxRows');
        const timeRetention = options.retention?.time === undefined
            ? false
            : {
                field: options.retention.time.field,
                durationMs: nonNegative(options.retention.time.durationMs, 0, '$.streaming.retention.time.durationMs'),
            };
        if (timeRetention !== false)
            assertSafeKey(timeRetention.field, 'streaming.retention.time.field');
        const eventTime = options.eventTime === undefined
            ? false
            : {
                field: options.eventTime.field,
                allowedLatenessMs: nonNegative(options.eventTime.allowedLatenessMs, 0, '$.streaming.eventTime.allowedLatenessMs'),
                lateData: options.eventTime.lateData ?? 'reject',
            };
        if (eventTime !== false) {
            assertSafeKey(eventTime.field, 'streaming.eventTime.field');
            if (!['reject', 'drop', 'accept'].includes(eventTime.lateData)) {
                throw new GraflumeError('INVALID_SPEC', 'Late-data policy is not supported.', {
                    path: '$.streaming.eventTime.lateData',
                });
            }
        }
        if (timeRetention !== false && eventTime === false) {
            throw new GraflumeError('INVALID_SPEC', 'Time retention requires an eventTime watermark contract.', { path: '$.streaming.retention.time' });
        }
        return {
            key: options.key,
            mode: options.mode ?? 'append',
            maxBatchRows: boundedInteger$7(options.maxBatchRows, DEFAULT_MAX_BATCH_ROWS, ABSOLUTE_MAX_ROWS$1, '$.streaming.maxBatchRows'),
            retentionRows,
            timeRetention,
            eventTime,
            maxQueueBatches: boundedInteger$7(options.queue?.maxBatches, DEFAULT_MAX_QUEUE_BATCHES$1, 1_024, '$.streaming.queue.maxBatches'),
            maxQueueRows: boundedInteger$7(options.queue?.maxRows, retentionRows, ABSOLUTE_MAX_ROWS$1, '$.streaming.queue.maxRows'),
            overflow: overflow,
            maxReplayBatches: boundedInteger$7(options.replay?.maxBatches, DEFAULT_MAX_REPLAY_BATCHES, 4_096, '$.streaming.replay.maxBatches'),
            maxReplayRows: boundedInteger$7(options.replay?.maxRows, DEFAULT_MAX_ROWS$1, ABSOLUTE_MAX_ROWS$1, '$.streaming.replay.maxRows'),
        };
    }
    function rowsFrom(input) {
        if (Array.isArray(input))
            return input.map(cloneRow);
        const table = DataTable.from(input);
        return Array.from({ length: table.length }, (_, index) => cloneRow(table.row(index)));
    }
    function cloneValue(value) {
        if (value instanceof Date)
            return new Date(value);
        return Array.isArray(value) ? [...value] : value;
    }
    function cloneRow(row) {
        if (!isPlainObject(row)) {
            throw new GraflumeError('INVALID_DATA', 'Incremental rows must be plain objects.', {
                path: '$.update.rows',
            });
        }
        const copy = Object.create(null);
        for (const [field, value] of Object.entries(row)) {
            assertSafeKey(field, `data.${field}`);
            copy[field] = cloneValue(value);
        }
        return copy;
    }
    function stableKey$1(value, path) {
        if (value instanceof Date && Number.isFinite(value.getTime()))
            return `date:${value.toISOString()}`;
        if (typeof value === 'number' && Number.isFinite(value))
            return `number:${value}`;
        if (typeof value === 'string' && value !== '')
            return `string:${value}`;
        if (typeof value === 'boolean')
            return `boolean:${value}`;
        throw new GraflumeError('INVALID_DATA', 'Streaming stable keys must be non-empty strings, finite numbers, booleans, or valid Dates.', { path });
    }
    function eventTimestamp(value, path) {
        const timestamp = value instanceof Date
            ? value.getTime()
            : typeof value === 'number'
                ? value
                : typeof value === 'string'
                    ? Date.parse(value)
                    : Number.NaN;
        if (!Number.isFinite(timestamp)) {
            throw new GraflumeError('INVALID_DATA', 'Event time must be a finite epoch number, Date, or parseable date string.', { path });
        }
        return timestamp;
    }
    function cloneUpdate$1(update) {
        return {
            ...(update.mode === undefined ? {} : { mode: update.mode }),
            rows: update.rows.map(cloneRow),
            ...(update.watermark === undefined ? {} : { watermark: update.watermark }),
        };
    }
    function cloneOptions(options) {
        return {
            key: options.key,
            ...(options.mode === undefined ? {} : { mode: options.mode }),
            ...(options.maxBatchRows === undefined ? {} : { maxBatchRows: options.maxBatchRows }),
            ...(options.retention === undefined
                ? {}
                : {
                    retention: {
                        ...(options.retention.maxRows === undefined
                            ? {}
                            : { maxRows: options.retention.maxRows }),
                        ...(options.retention.time === undefined
                            ? {}
                            : { time: { ...options.retention.time } }),
                    },
                }),
            ...(options.eventTime === undefined ? {} : { eventTime: { ...options.eventTime } }),
            ...(options.queue === undefined ? {} : { queue: { ...options.queue } }),
            ...(options.replay === undefined ? {} : { replay: { ...options.replay } }),
            ...(options.runtime === undefined
                ? {}
                : {
                    runtime: {
                        ...options.runtime,
                        ...(options.runtime.history === undefined
                            ? {}
                            : { history: { ...options.runtime.history } }),
                    },
                }),
            ...(options.worker === undefined
                ? {}
                : {
                    worker: {
                        ...options.worker,
                        ...(options.worker.engine === undefined
                            ? {}
                            : { engine: { ...options.worker.engine } }),
                    },
                }),
        };
    }
    /** Internal semantic comparison used before attaching replay state to a ChartSpec. */
    function incrementalContractsMatch(left, right) {
        return JSON.stringify(normalizeOptions$2(left)) === JSON.stringify(normalizeOptions$2(right));
    }
    class IncrementalDataStore {
        #options;
        #normalized;
        #initial;
        #rows;
        #watermark = null;
        #sequence = 0;
        #acceptedRows = 0;
        #droppedLateRows = 0;
        #evictedRows = 0;
        #provenance = [];
        #replay = [];
        #replayRows = 0;
        #replayTruncated = false;
        #queue = [];
        #queuedRows = 0;
        #queueDroppedBatches = 0;
        #queueCoalescedBatches = 0;
        #draining = false;
        constructor(input, options) {
            this.#normalized = normalizeOptions$2(options);
            this.#options = cloneOptions(options);
            const initial = rowsFrom(input);
            this.#assertUniqueKeys(initial, '$.data');
            this.#rows = new BoundedRingBuffer(this.#normalized.retentionRows, initial);
            this.#initial = this.#rows.values().map(cloneRow);
        }
        rows() {
            return this.#rows.values().map(cloneRow);
        }
        state() {
            return {
                version: 1,
                key: this.#normalized.key,
                rowCount: this.#rows.length,
                watermark: this.#watermark,
                sequence: this.#sequence,
                acceptedRows: this.#acceptedRows,
                droppedLateRows: this.#droppedLateRows,
                evictedRows: this.#evictedRows,
                queuedBatches: this.#queue.length,
                queuedRows: this.#queuedRows,
                queueDroppedBatches: this.#queueDroppedBatches,
                queueCoalescedBatches: this.#queueCoalescedBatches,
                replayBatches: this.#replay.length,
                replayRows: this.#replayRows,
                replayTruncated: this.#replayTruncated,
                provenance: this.#provenance.map((step) => ({ ...step })),
            };
        }
        apply(update) {
            validateUpdateShape(update);
            const mode = update.mode ?? this.#normalized.mode;
            if (!['append', 'upsert', 'replaceLast'].includes(mode)) {
                throw new GraflumeError('INVALID_DATA', `Incremental mode "${String(mode)}" is unsupported.`);
            }
            if (update.rows.length > this.#normalized.maxBatchRows) {
                throw new GraflumeError('INVALID_DATA', `Incremental batch has ${update.rows.length} rows; the deterministic limit is ${this.#normalized.maxBatchRows}.`);
            }
            if (update.watermark !== undefined &&
                (!Number.isFinite(update.watermark) ||
                    (this.#watermark !== null && update.watermark < this.#watermark))) {
                throw new GraflumeError('INVALID_DATA', 'Streaming watermarks must be finite and monotonic.');
            }
            const incoming = update.rows.map(cloneRow);
            this.#assertUniqueKeys(incoming, '$.update.rows');
            const eventTime = this.#normalized.eventTime;
            const timestamps = eventTime === false
                ? []
                : incoming.map((row, index) => eventTimestamp(ownValue(row, eventTime.field), `$.update.rows[${index}].${eventTime.field}`));
            const latenessWatermark = update.watermark === undefined
                ? this.#watermark
                : Math.max(this.#watermark ?? Number.NEGATIVE_INFINITY, update.watermark);
            let nextWatermark = latenessWatermark;
            if (eventTime !== false && timestamps.length > 0) {
                const maximumTimestamp = timestamps.reduce((maximum, value) => Math.max(maximum, value), Number.NEGATIVE_INFINITY);
                nextWatermark = Math.max(nextWatermark ?? Number.NEGATIVE_INFINITY, maximumTimestamp - eventTime.allowedLatenessMs);
            }
            if (update.watermark !== undefined) {
                nextWatermark = Math.max(nextWatermark ?? Number.NEGATIVE_INFINITY, update.watermark);
            }
            const accepted = [];
            let droppedLateRows = 0;
            incoming.forEach((row, index) => {
                const late = latenessWatermark !== null &&
                    timestamps[index] !== undefined &&
                    timestamps[index] < latenessWatermark;
                if (!late || eventTime === false || eventTime.lateData === 'accept') {
                    accepted.push(row);
                }
                else if (eventTime.lateData === 'drop') {
                    droppedLateRows += 1;
                }
                else {
                    throw new GraflumeError('INVALID_DATA', `Late event at row ${index} is behind watermark ${latenessWatermark}.`, { path: `$.update.rows[${index}].${eventTime.field}` });
                }
            });
            const nextRows = this.#rows.values().map(cloneRow);
            const indexByKey = this.#keyIndex(nextRows, '$.data');
            let insertedRows = 0;
            let updatedRows = 0;
            for (const [incomingIndex, row] of accepted.entries()) {
                const key = stableKey$1(ownValue(row, this.#normalized.key), `$.update.rows[${incomingIndex}].${this.#normalized.key}`);
                const existing = indexByKey.get(key);
                if (mode === 'append') {
                    if (existing !== undefined) {
                        throw new GraflumeError('INVALID_DATA', `Append would duplicate stable key "${key}".`);
                    }
                    indexByKey.set(key, nextRows.length);
                    nextRows.push(row);
                    insertedRows += 1;
                }
                else if (mode === 'upsert') {
                    if (existing === undefined) {
                        indexByKey.set(key, nextRows.length);
                        nextRows.push(row);
                        insertedRows += 1;
                    }
                    else {
                        nextRows[existing] = { ...nextRows[existing], ...row };
                        updatedRows += 1;
                    }
                }
                else {
                    const lastIndex = nextRows.length - 1;
                    if (existing === lastIndex && lastIndex >= 0) {
                        nextRows[lastIndex] = { ...nextRows[lastIndex], ...row };
                        updatedRows += 1;
                    }
                    else if (existing === undefined) {
                        indexByKey.set(key, nextRows.length);
                        nextRows.push(row);
                        insertedRows += 1;
                    }
                    else {
                        throw new GraflumeError('INVALID_DATA', `replaceLast key "${key}" exists before the final row; use upsert explicitly.`);
                    }
                }
            }
            let retained = nextRows;
            const timeRetention = this.#normalized.timeRetention;
            if (timeRetention !== false && nextWatermark !== null) {
                const threshold = nextWatermark - timeRetention.durationMs;
                retained = retained.filter((row) => eventTimestamp(ownValue(row, timeRetention.field), `$.data.${timeRetention.field}`) >= threshold);
            }
            if (retained.length > this.#normalized.retentionRows) {
                retained = retained.slice(-this.#normalized.retentionRows);
            }
            const evictedRows = nextRows.length - retained.length;
            this.#rows.replace(retained);
            this.#watermark = nextWatermark;
            this.#sequence += 1;
            this.#acceptedRows += accepted.length;
            this.#droppedLateRows += droppedLateRows;
            this.#evictedRows += evictedRows;
            const step = {
                sequence: this.#sequence,
                mode,
                inputRows: incoming.length,
                acceptedRows: accepted.length,
                insertedRows,
                updatedRows,
                droppedLateRows,
                evictedRows,
                outputRows: retained.length,
                watermark: this.#watermark,
            };
            this.#provenance.push(step);
            if (this.#provenance.length > this.#normalized.maxReplayBatches)
                this.#provenance.shift();
            this.#acceptedReplay(update);
            return { rows: this.rows(), state: this.state(), step: { ...step } };
        }
        enqueue(update) {
            let snapshot;
            try {
                validateUpdateShape(update);
                snapshot = cloneUpdate$1(update);
            }
            catch (error) {
                return Promise.reject(error);
            }
            return new Promise((resolve, reject) => {
                const over = () => this.#queue.length >= this.#normalized.maxQueueBatches ||
                    this.#queuedRows + snapshot.rows.length > this.#normalized.maxQueueRows;
                if (over() && this.#normalized.overflow === 'coalesce') {
                    const target = this.#queue.at(-1);
                    if (target !== undefined && this.#coalesceQueued(target, snapshot)) {
                        target.subscribers.push({ resolve, reject });
                        this.#queueCoalescedBatches += 1;
                        return;
                    }
                }
                if (over() && this.#normalized.overflow === 'drop-oldest') {
                    while (over() && this.#queue.length > 0) {
                        const dropped = this.#queue.shift();
                        this.#queuedRows -= dropped.update.rows.length;
                        this.#queueDroppedBatches += 1;
                        const error = new GraflumeError('INVALID_DATA', 'Incremental batch was dropped by explicit overflow policy.');
                        for (const subscriber of dropped.subscribers)
                            subscriber.reject(error);
                    }
                }
                if (over()) {
                    reject(new GraflumeError('INVALID_DATA', 'Incremental queue backpressure limit reached; the batch was rejected without mutation.'));
                    return;
                }
                this.#queue.push({ update: snapshot, subscribers: [{ resolve, reject }] });
                this.#queuedRows += snapshot.rows.length;
                if (!this.#draining)
                    queueMicrotask(() => this.#drain());
            });
        }
        exportReplay() {
            if (this.#replayTruncated) {
                throw new GraflumeError('INVALID_DATA', 'Replay history exceeded its bounded contract; export a fresh snapshot before replay.');
            }
            return {
                version: 1,
                options: cloneOptions(this.#options),
                initial: this.#initial.map(cloneRow),
                updates: this.#replay.map(cloneUpdate$1),
            };
        }
        static replay(replay) {
            const envelope = closedObject$1(replay, REPLAY_ENVELOPE_KEYS, '$.replay');
            if (replay.version !== 1) {
                throw new GraflumeError('INVALID_DATA', 'Incremental replay version is unsupported.');
            }
            if (!Array.isArray(envelope.initial) || !Array.isArray(envelope.updates)) {
                throw new GraflumeError('INVALID_DATA', 'Incremental replay rows and updates must be arrays.', {
                    path: '$.replay',
                });
            }
            const store = new IncrementalDataStore(replay.initial, replay.options);
            for (const update of replay.updates)
                store.apply(update);
            return store;
        }
        #assertUniqueKeys(rows, path) {
            this.#keyIndex(rows, path);
        }
        #keyIndex(rows, path) {
            const index = new Map();
            rows.forEach((row, rowIndex) => {
                const key = stableKey$1(ownValue(row, this.#normalized.key), `${path}[${rowIndex}].${this.#normalized.key}`);
                if (index.has(key)) {
                    throw new GraflumeError('INVALID_DATA', `Duplicate stable key "${key}".`, {
                        path: `${path}[${rowIndex}].${this.#normalized.key}`,
                    });
                }
                index.set(key, rowIndex);
            });
            return index;
        }
        #acceptedReplay(update) {
            const copy = cloneUpdate$1(update);
            this.#replay.push(copy);
            this.#replayRows += copy.rows.length;
            while (this.#replay.length > this.#normalized.maxReplayBatches ||
                this.#replayRows > this.#normalized.maxReplayRows) {
                const removed = this.#replay.shift();
                if (removed === undefined)
                    break;
                this.#replayRows -= removed.rows.length;
                this.#replayTruncated = true;
            }
        }
        #drain() {
            if (this.#draining)
                return;
            this.#draining = true;
            const next = () => {
                const item = this.#queue.shift();
                if (item === undefined) {
                    this.#draining = false;
                    return;
                }
                this.#queuedRows -= item.update.rows.length;
                try {
                    const result = this.apply(item.update);
                    for (const subscriber of item.subscribers)
                        subscriber.resolve(result);
                }
                catch (error) {
                    for (const subscriber of item.subscribers)
                        subscriber.reject(error);
                }
                queueMicrotask(next);
            };
            next();
        }
        #coalesceQueued(target, incoming) {
            const targetMode = target.update.mode ?? this.#normalized.mode;
            const incomingMode = incoming.mode ?? this.#normalized.mode;
            if (targetMode !== incomingMode)
                return false;
            let rows;
            if (targetMode === 'append') {
                rows = [...target.update.rows, ...incoming.rows];
            }
            else {
                const byKey = new Map();
                for (const [index, row] of [...target.update.rows, ...incoming.rows].entries()) {
                    byKey.set(stableKey$1(ownValue(row, this.#normalized.key), `$.update.rows[${index}].${this.#normalized.key}`), row);
                }
                rows = [...byKey.values()];
            }
            if (rows.length > this.#normalized.maxQueueRows)
                return false;
            this.#queuedRows += rows.length - target.update.rows.length;
            const watermark = Math.max(target.update.watermark ?? Number.NEGATIVE_INFINITY, incoming.watermark ?? Number.NEGATIVE_INFINITY);
            target.update = {
                mode: targetMode,
                rows: rows.map(cloneRow),
                ...(Number.isFinite(watermark) ? { watermark } : {}),
            };
            return true;
        }
    }
    function createIncrementalDataStore(input, options) {
        return new IncrementalDataStore(input, options);
    }
    function replayIncrementalData(replay) {
        return IncrementalDataStore.replay(replay);
    }

    const localTransformTypes = new Set(['filter', 'calculate']);
    function boundedTransforms(value) {
        const resolved = value ?? 128;
        if (!Number.isInteger(resolved) || resolved < 1 || resolved > 128) {
            throw new GraflumeError('INVALID_SPEC', '$.incrementalTransform.maxTransforms must be an integer from 1 to 128.');
        }
        return resolved;
    }
    function portableTransforms(transforms, maximum) {
        if (transforms.length > maximum) {
            throw new GraflumeError('INVALID_SPEC', `Incremental transform pipeline has ${transforms.length} stages; the deterministic limit is ${maximum}.`);
        }
        try {
            return structuredClone(transforms);
        }
        catch (error) {
            throw new GraflumeError('INVALID_SPEC', 'Incremental transforms must be structured-clone portable; functions are unsupported.', { cause: error });
        }
    }
    function stableKey(value, path) {
        if (value instanceof Date && Number.isFinite(value.getTime()))
            return `date:${value.toISOString()}`;
        if (typeof value === 'number' && Number.isFinite(value))
            return `number:${value}`;
        if (typeof value === 'string' && value !== '')
            return `string:${value}`;
        if (typeof value === 'boolean')
            return `boolean:${value}`;
        throw new GraflumeError('INVALID_DATA', 'Incremental transform keys must be non-empty strings, finite numbers, booleans, or valid Dates.', { path });
    }
    function canonical$1(value) {
        if (value instanceof Date)
            return `{"$date":${JSON.stringify(value.toISOString())}}`;
        if (Array.isArray(value))
            return `[${value.map(canonical$1).join(',')}]`;
        if (value !== null && typeof value === 'object') {
            return `{${Object.entries(value)
            .sort(([left], [right]) => left.localeCompare(right, 'en'))
            .map(([key, child]) => `${JSON.stringify(key)}:${canonical$1(child)}`)
            .join(',')}}`;
        }
        return JSON.stringify(value) ?? 'null';
    }
    function cloneRows$1(rows) {
        return structuredClone(rows);
    }
    function portableParameters(transform) {
        return structuredClone(transform);
    }
    function rowLocalLineage(inputRows, rows, rowSources, transforms, step) {
        return {
            sourceId: 'incremental-stream',
            sourceRows: inputRows,
            outputRows: rows.length,
            transforms: transforms.map((transform, index) => ({
                index,
                type: transform.type,
                inputRows,
                outputRows: rows.length,
                parameters: portableParameters(transform),
                detail: `Incremental cache: ${step.recomputedRows} recomputed, ${step.reusedRows} reused.`,
            })),
            rowSources,
            summary: `Incremental row-local transform cache recomputed ${step.recomputedRows} of ${inputRows} retained rows.`,
        };
    }
    /**
     * Stable-key transform cache.
     *
     * Function-free `filter`/`calculate` pipelines only recompute changed rows.
     * Order-dependent or aggregate stages use an explicit bounded full fallback,
     * preserving exact `executeTransforms` semantics.
     */
    class IncrementalTransformPipeline {
        #store;
        #key;
        #maxTransforms;
        #transforms;
        #transformFingerprint;
        #cache = new Map();
        #result;
        #sequence = 0;
        #recomputedRows = 0;
        #reusedRows = 0;
        #fullRecomputations = 0;
        #incrementalRecomputations = 0;
        #last;
        constructor(input, transforms, options) {
            this.#maxTransforms = boundedTransforms(options.maxTransforms);
            this.#transforms = portableTransforms(transforms, this.#maxTransforms);
            this.#transformFingerprint = canonical$1(this.#transforms);
            this.#key = options.streaming.key;
            this.#store = new IncrementalDataStore(input, options.streaming);
            const initialRows = this.#store.rows();
            this.#result = executeTransforms(initialRows, this.#transforms);
            this.#fullRecomputations = 1;
            this.#recomputedRows = initialRows.length;
            this.#last = {
                sequence: 0,
                mode: 'full',
                inputRows: initialRows.length,
                outputRows: this.#result.data.length,
                recomputedRows: initialRows.length,
                reusedRows: 0,
                removedRows: 0,
                reason: 'initial',
            };
            if (this.#isRowLocal())
                this.#rebuildLocalCache(initialRows);
        }
        apply(update) {
            const previousKeys = new Set(this.#cache.keys());
            const applied = this.#store.apply(update);
            const nextRows = applied.rows;
            const changed = [];
            let reusedRows = 0;
            if (this.#isRowLocal()) {
                nextRows.forEach((row, sourceIndex) => {
                    const key = this.#rowKey(row, `$.data[${sourceIndex}].${this.#key}`);
                    previousKeys.delete(key);
                    const fingerprint = canonical$1(row);
                    if (this.#cache.get(key)?.fingerprint === fingerprint)
                        reusedRows += 1;
                    else
                        changed.push({ key, row, sourceIndex });
                });
                for (const removed of previousKeys)
                    this.#cache.delete(removed);
                if (changed.length > 0) {
                    const transformed = executeTransforms(changed.map(({ row }) => row), this.#transforms);
                    const byChangedIndex = new Map();
                    transformed.data.forEach((row, outputIndex) => {
                        for (const changedIndex of transformed.lineage.rowSources[outputIndex] ?? []) {
                            const list = byChangedIndex.get(changedIndex) ?? [];
                            list.push(row);
                            byChangedIndex.set(changedIndex, list);
                        }
                    });
                    changed.forEach((entry, changedIndex) => {
                        this.#cache.set(entry.key, {
                            fingerprint: canonical$1(entry.row),
                            rows: cloneRows$1(byChangedIndex.get(changedIndex) ?? []),
                        });
                    });
                }
                const output = [];
                const sources = [];
                nextRows.forEach((row, sourceIndex) => {
                    const entry = this.#cache.get(this.#rowKey(row, `$.data[${sourceIndex}].${this.#key}`));
                    for (const transformed of entry?.rows ?? []) {
                        output.push(structuredClone(transformed));
                        sources.push([sourceIndex]);
                    }
                });
                this.#sequence += 1;
                const step = {
                    sequence: this.#sequence,
                    mode: 'incremental',
                    inputRows: nextRows.length,
                    outputRows: output.length,
                    recomputedRows: changed.length,
                    reusedRows,
                    removedRows: previousKeys.size,
                    reason: 'row-local',
                };
                this.#result = {
                    data: output,
                    lineage: rowLocalLineage(nextRows.length, output, sources, this.#transforms, step),
                };
                this.#incrementalRecomputations += 1;
                this.#recomputedRows += changed.length;
                this.#reusedRows += reusedRows;
                this.#last = step;
            }
            else {
                this.#full(nextRows, 'global-transform');
            }
            return {
                ...applied,
                transformed: this.result(),
                transformState: this.state(),
            };
        }
        setTransforms(transforms) {
            const next = portableTransforms(transforms, this.#maxTransforms);
            const fingerprint = canonical$1(next);
            if (fingerprint === this.#transformFingerprint)
                return this.result();
            this.#transforms = next;
            this.#transformFingerprint = fingerprint;
            this.#cache.clear();
            this.#full(this.#store.rows(), 'transform-change');
            if (this.#isRowLocal())
                this.#rebuildLocalCache(this.#store.rows());
            return this.result();
        }
        rows() {
            return this.#store.rows();
        }
        result() {
            return {
                data: cloneRows$1(this.#result.data),
                lineage: structuredClone(this.#result.lineage),
            };
        }
        state() {
            return {
                version: 1,
                sequence: this.#sequence,
                rowLocal: this.#isRowLocal(),
                inputRows: this.#last.inputRows,
                outputRows: this.#last.outputRows,
                recomputedRows: this.#recomputedRows,
                reusedRows: this.#reusedRows,
                fullRecomputations: this.#fullRecomputations,
                incrementalRecomputations: this.#incrementalRecomputations,
                last: { ...this.#last },
            };
        }
        #full(rows, reason) {
            this.#result = executeTransforms(rows, this.#transforms);
            this.#sequence += 1;
            this.#fullRecomputations += 1;
            this.#recomputedRows += rows.length;
            this.#last = {
                sequence: this.#sequence,
                mode: 'full',
                inputRows: rows.length,
                outputRows: this.#result.data.length,
                recomputedRows: rows.length,
                reusedRows: 0,
                removedRows: 0,
                reason,
            };
        }
        #rebuildLocalCache(rows) {
            this.#cache.clear();
            rows.forEach((row, index) => {
                const result = executeTransforms([row], this.#transforms);
                this.#cache.set(this.#rowKey(row, `$.data[${index}].${this.#key}`), {
                    fingerprint: canonical$1(row),
                    rows: cloneRows$1(result.data),
                });
            });
        }
        #rowKey(row, path) {
            return stableKey(ownValue(row, this.#key), path);
        }
        #isRowLocal() {
            return this.#transforms.every(({ type }) => localTransformTypes.has(type));
        }
    }
    function createIncrementalTransformPipeline(input, transforms, options) {
        return new IncrementalTransformPipeline(input, transforms, options);
    }
    const incrementalStackMaximumRows = 1_000_000;
    function boundedStackRows(value) {
        const resolved = value ?? 100_000;
        if (!Number.isInteger(resolved) || resolved < 1 || resolved > incrementalStackMaximumRows) {
            throw new GraflumeError('INVALID_SPEC', `Incremental stack maxRows must be an integer from 1 to ${incrementalStackMaximumRows}.`, { path: '$.incrementalStack.maxRows' });
        }
        return resolved;
    }
    function portableStackTransform(transform) {
        if (!isPlainObject(transform) || transform.type !== 'stack') {
            throw new GraflumeError('INVALID_SPEC', 'Incremental stack requires one stack transform.');
        }
        let portable;
        try {
            portable = structuredClone(transform);
        }
        catch (error) {
            throw new GraflumeError('INVALID_SPEC', 'Incremental stack transforms must be structured-clone portable.', { cause: error });
        }
        // The shared closed transform validator remains the source of truth. Empty
        // input validates structure without performing data-dependent work.
        executeTransforms([], [portable]);
        return portable;
    }
    function validateIncrementalStackRequest(request, maxRows) {
        if (!Array.isArray(request.input)) {
            throw new GraflumeError('INVALID_DATA', '$.incrementalStack.input must be a row array.');
        }
        if (typeof request.key !== 'string' || request.key.trim() === '') {
            throw new GraflumeError('INVALID_SPEC', '$.incrementalStack.key must be a non-empty field name.');
        }
        ownValue(Object.create(null), request.key);
        const previous = request.previous;
        if (previous === undefined)
            return;
        if (previous.version !== 1 ||
            previous.state.version !== 1 ||
            previous.state.portable !== true ||
            !Array.isArray(previous.input) ||
            !Array.isArray(previous.groups) ||
            !Array.isArray(previous.seriesOrder)) {
            throw new GraflumeError('INVALID_DATA', 'Incremental stack snapshot is malformed.');
        }
        const previousMaximum = boundedStackRows(previous.maxRows);
        if (previous.input.length > previousMaximum || previous.input.length > maxRows) {
            throw new GraflumeError('INVALID_DATA', 'Incremental stack snapshot exceeds maxRows.');
        }
        if (canonical$1(previous.transform) !== previous.transformFingerprint) {
            throw new GraflumeError('INVALID_DATA', 'Incremental stack snapshot transform fingerprint is invalid.');
        }
    }
    function stackValueKey(row, fields) {
        return canonical$1(fields.map((field) => {
            const value = ownValue(row, field);
            return value === undefined ? null : value;
        }));
    }
    function stableInputRows(rows, key, maxRows) {
        if (rows.length > maxRows) {
            throw new GraflumeError('INVALID_DATA', `Incremental stack input has ${rows.length} rows; the deterministic limit is ${maxRows}.`, { path: '$.incrementalStack.input' });
        }
        const cloned = cloneRows$1(rows);
        const byKey = new Map();
        cloned.forEach((row, index) => {
            const id = stableKey(ownValue(row, key), `$.incrementalStack.input[${index}].${key}`);
            if (byKey.has(id)) {
                throw new GraflumeError('INVALID_DATA', 'Incremental stack input keys must be unique.', {
                    path: `$.incrementalStack.input[${index}].${key}`,
                });
            }
            byKey.set(id, row);
        });
        return { rows: cloned, byKey };
    }
    function stackGroups(rows, keyField, transform, seriesOrder) {
        const grouped = new Map();
        rows.forEach((row, index) => {
            const groupKey = stackValueKey(row, transform.groupby);
            const group = grouped.get(groupKey) ?? { rows: [], sourceKeys: [] };
            group.rows.push(row);
            group.sourceKeys.push(stableKey(ownValue(row, keyField), `$.incrementalStack.input[${index}].${keyField}`));
            grouped.set(groupKey, group);
        });
        if ((transform.series ?? []).length === 0) {
            return [...grouped].map(([key, group]) => ({ key, ...group }));
        }
        const seriesRank = new Map(seriesOrder.map((key, index) => [key, index]));
        return [...grouped].map(([key, group]) => {
            const ordered = group.rows
                .map((row, index) => ({ row, sourceKey: group.sourceKeys[index], index }))
                .sort((left, right) => (seriesRank.get(stackValueKey(left.row, transform.series ?? [])) ??
                Number.MAX_SAFE_INTEGER) -
                (seriesRank.get(stackValueKey(right.row, transform.series ?? [])) ??
                    Number.MAX_SAFE_INTEGER) || left.index - right.index);
            return {
                key,
                rows: ordered.map(({ row }) => row),
                sourceKeys: ordered.map(({ sourceKey }) => sourceKey),
            };
        });
    }
    function observedSeriesOrder(rows, transform) {
        const fields = transform.series ?? [];
        if (fields.length === 0)
            return [];
        return [...new Set(rows.map((row) => stackValueKey(row, fields)))];
    }
    function sameStrings(left, right) {
        return left.length === right.length && left.every((value, index) => value === right[index]);
    }
    function stackGroupSnapshot(group, transform) {
        const result = executeTransforms(group.rows, [transform], {
            sourceId: 'incremental-stack-group',
        });
        return {
            key: group.key,
            output: cloneRows$1(result.data),
            outputSourceKeys: result.lineage.rowSources.map((sources) => sources.flatMap((sourceIndex) => group.sourceKeys[sourceIndex] ?? [])),
        };
    }
    function incrementalStackLineage(inputRows, output, rowSources, transform, step) {
        return {
            sourceId: 'incremental-stack',
            sourceRows: inputRows,
            outputRows: output.length,
            transforms: [
                {
                    index: 0,
                    type: 'stack',
                    inputRows,
                    outputRows: output.length,
                    parameters: portableParameters(transform),
                    aggregationCount: step.recomputedGroups.length + step.reusedGroups.length,
                    detail: `Incremental stack: ${step.recomputedGroups.length} groups recomputed, ${step.reusedGroups.length} groups reused (${step.reason}).`,
                },
            ],
            rowSources,
            summary: `Incremental stack recomputed ${step.recomputedRows} of ${inputRows} retained rows across ${step.recomputedGroups.length} affected groups; ${step.reusedRows} rows were reused.`,
        };
    }
    function stateAfterStackStep(previous, step, groupCount) {
        return {
            version: 1,
            portable: true,
            sequence: step.sequence,
            inputRows: step.inputRows,
            outputRows: step.outputRows,
            groupCount,
            fullRecomputations: (previous?.state.fullRecomputations ?? 0) + (step.mode === 'full' ? 1 : 0),
            incrementalRecomputations: (previous?.state.incrementalRecomputations ?? 0) + (step.mode === 'incremental' ? 1 : 0),
            recomputedRows: (previous?.state.recomputedRows ?? 0) + step.recomputedRows,
            reusedRows: (previous?.state.reusedRows ?? 0) + step.reusedRows,
            last: step,
        };
    }
    /**
     * Execute one stateless, structured-cloneable incremental stack transition.
     *
     * Zero/normalize/center stacks can be recomputed per affected `groupby`
     * bucket. Global series ordering and wiggle baselines deliberately fall back
     * to the exact full transform because a local update can move every bucket.
     */
    function executePortableIncrementalStack(request) {
        const maxRows = boundedStackRows(request.maxRows ?? request.previous?.maxRows);
        validateIncrementalStackRequest(request, maxRows);
        const transform = portableStackTransform(request.transform);
        const fingerprint = canonical$1(transform);
        const { rows: input, byKey } = stableInputRows(request.input, request.key, maxRows);
        const previous = request.previous;
        const previousCompatible = previous !== undefined &&
            previous.version === 1 &&
            previous.key === request.key &&
            previous.maxRows === maxRows &&
            previous.transformFingerprint === fingerprint;
        const previousByKey = new Map();
        if (previousCompatible) {
            previous.input.forEach((row, index) => {
                previousByKey.set(stableKey(ownValue(row, request.key), `$.incrementalStack.previous.input[${index}].${request.key}`), row);
            });
        }
        const changedKeys = new Set();
        let removedRows = 0;
        for (const [key, row] of byKey) {
            if (canonical$1(previousByKey.get(key)) !== canonical$1(row))
                changedKeys.add(key);
        }
        for (const key of previousByKey.keys()) {
            if (!byKey.has(key)) {
                changedKeys.add(key);
                removedRows += 1;
            }
        }
        const seriesOrder = observedSeriesOrder(input, transform);
        const groups = stackGroups(input, request.key, transform, seriesOrder);
        const groupKeys = groups.map(({ key }) => key);
        const affected = new Set();
        const groupForRow = (row) => stackValueKey(row, transform.groupby);
        for (const key of changedKeys) {
            const before = previousByKey.get(key);
            const after = byKey.get(key);
            if (before !== undefined)
                affected.add(groupForRow(before));
            if (after !== undefined)
                affected.add(groupForRow(after));
        }
        let reason = previousCompatible ? 'affected-groups' : 'initial';
        let full = !previousCompatible;
        if (previous !== undefined && !previousCompatible)
            reason = 'transform-change';
        if (!full && changedKeys.size === 0)
            reason = 'unchanged';
        if (!full && changedKeys.size > 0 && (transform.offset ?? 'zero') === 'wiggle') {
            full = true;
            reason = 'global-wiggle';
        }
        else if (!full &&
            changedKeys.size > 0 &&
            transform.order !== undefined &&
            transform.order !== 'input') {
            full = true;
            reason = 'global-order';
        }
        else if (!full && changedKeys.size > 0 && !sameStrings(previous.seriesOrder, seriesOrder)) {
            full = true;
            reason = 'series-order-change';
        }
        const previousGroups = new Map((previous?.groups ?? []).map((group) => [group.key, group]));
        let nextGroups = [];
        const recomputedGroups = [];
        const reusedGroups = [];
        let exactFull;
        if (full) {
            exactFull = executeTransforms(input, [transform], { sourceId: 'incremental-stack' });
            const fullGroups = new Map();
            exactFull.data.forEach((row, outputIndex) => {
                const groupKey = stackValueKey(row, transform.groupby);
                const group = fullGroups.get(groupKey) ?? { output: [], outputSourceKeys: [] };
                group.output.push(row);
                group.outputSourceKeys.push((exactFull.lineage.rowSources[outputIndex] ?? []).flatMap((sourceIndex) => {
                    const source = input[sourceIndex];
                    return source === undefined
                        ? []
                        : [
                            stableKey(ownValue(source, request.key), `$.incrementalStack.input[${sourceIndex}].${request.key}`),
                        ];
                }));
                fullGroups.set(groupKey, group);
            });
            nextGroups = groupKeys.map((key) => {
                const group = fullGroups.get(key) ?? { output: [], outputSourceKeys: [] };
                return { key, output: cloneRows$1(group.output), outputSourceKeys: group.outputSourceKeys };
            });
            recomputedGroups.push(...groupKeys);
        }
        else {
            for (const group of groups) {
                const cached = previousGroups.get(group.key);
                if (!affected.has(group.key) && cached !== undefined) {
                    nextGroups.push(structuredClone(cached));
                    reusedGroups.push(group.key);
                }
                else {
                    nextGroups.push(stackGroupSnapshot(group, transform));
                    recomputedGroups.push(group.key);
                }
            }
        }
        const inputIndex = new Map([...byKey.keys()].map((key, index) => [key, index]));
        const output = exactFull?.data ?? nextGroups.flatMap(({ output: rows }) => cloneRows$1(rows));
        const rowSources = exactFull?.lineage.rowSources ??
            nextGroups.flatMap(({ outputSourceKeys }) => outputSourceKeys.map((keys) => keys.flatMap((key) => inputIndex.get(key) ?? []).sort((left, right) => left - right)));
        const recomputedRows = full
            ? input.length
            : groups
                .filter(({ key }) => affected.has(key))
                .reduce((count, group) => count + group.rows.length, 0);
        const step = {
            sequence: (previous?.state.sequence ?? -1) + 1,
            mode: full ? 'full' : 'incremental',
            reason,
            inputRows: input.length,
            outputRows: output.length,
            changedRows: changedKeys.size,
            removedRows,
            recomputedRows,
            reusedRows: input.length - recomputedRows,
            recomputedGroups,
            reusedGroups,
            changedKeys: [...changedKeys],
        };
        const state = stateAfterStackStep(previous, step, groupKeys.length);
        const transformed = {
            data: output,
            lineage: incrementalStackLineage(input.length, output, rowSources, transform, step),
        };
        const snapshot = {
            version: 1,
            key: request.key,
            maxRows,
            transform,
            transformFingerprint: fingerprint,
            seriesOrder,
            input,
            groups: nextGroups,
            state,
        };
        // This is the same boundary used by the Worker protocol. Verify it before
        // exposing state so a non-portable datum can never silently enter replay.
        try {
            structuredClone(snapshot);
        }
        catch (error) {
            throw new GraflumeError('INVALID_DATA', 'Incremental stack state is not Worker-portable.', {
                cause: error,
            });
        }
        return { transformed, snapshot, state };
    }
    class IncrementalStackPipeline {
        #store;
        #key;
        #maxRows;
        #transform;
        #current;
        constructor(input, transform, options) {
            this.#store = new IncrementalDataStore(input, options.streaming);
            this.#key = options.streaming.key;
            this.#maxRows = boundedStackRows(options.maxRows ?? options.streaming.retention?.maxRows);
            this.#transform = portableStackTransform(transform);
            this.#current = executePortableIncrementalStack({
                key: this.#key,
                maxRows: this.#maxRows,
                input: this.#store.rows(),
                transform: this.#transform,
            });
        }
        apply(update) {
            const applied = this.#store.apply(update);
            this.#current = executePortableIncrementalStack({
                key: this.#key,
                maxRows: this.#maxRows,
                input: applied.rows,
                transform: this.#transform,
                previous: this.#current.snapshot,
            });
            return {
                ...applied,
                transformed: this.result(),
                stackSnapshot: this.snapshot(),
                stackState: this.state(),
            };
        }
        setTransform(transform) {
            this.#transform = portableStackTransform(transform);
            this.#current = executePortableIncrementalStack({
                key: this.#key,
                maxRows: this.#maxRows,
                input: this.#store.rows(),
                transform: this.#transform,
                previous: this.#current.snapshot,
            });
            return this.result();
        }
        result() {
            return structuredClone(this.#current.transformed);
        }
        snapshot() {
            return structuredClone(this.#current.snapshot);
        }
        state() {
            return structuredClone(this.#current.state);
        }
    }
    function createIncrementalStackPipeline(input, transform, options) {
        return new IncrementalStackPipeline(input, transform, options);
    }

    function boundedInteger$6(value, fallback, maximum, path) {
        const resolved = value ?? fallback;
        if (!Number.isInteger(resolved) || resolved < 1 || resolved > maximum) {
            throw new GraflumeError('INVALID_SPEC', `${path} must be an integer from 1 to ${maximum}.`, {
                path,
            });
        }
        return resolved;
    }
    function normalizeRuntimeSpec(spec) {
        if (spec === null || typeof spec !== 'object' || Array.isArray(spec)) {
            throw new GraflumeError('INVALID_SPEC', '$.streaming.runtime must be an object.');
        }
        const keys = new Set([
            'schedule',
            'maxBatchesPerFrame',
            'overflow',
            'paused',
            'followLive',
            'history',
        ]);
        const unknown = Object.keys(spec).find((key) => !keys.has(key));
        if (unknown !== undefined) {
            throw new GraflumeError('INVALID_SPEC', `Unknown streaming runtime property "${unknown}".`, {
                path: `$.streaming.runtime.${unknown}`,
            });
        }
        if (spec.schedule !== undefined && !['animation-frame', 'microtask'].includes(spec.schedule)) {
            throw new GraflumeError('INVALID_SPEC', 'Streaming schedule is unsupported.');
        }
        if (spec.overflow !== undefined &&
            !['reject', 'drop-oldest', 'coalesce'].includes(spec.overflow)) {
            throw new GraflumeError('INVALID_SPEC', 'Streaming overflow policy is unsupported.');
        }
        if (spec.paused !== undefined && typeof spec.paused !== 'boolean') {
            throw new GraflumeError('INVALID_SPEC', '$.streaming.runtime.paused must be boolean.');
        }
        if (spec.followLive !== undefined && typeof spec.followLive !== 'boolean') {
            throw new GraflumeError('INVALID_SPEC', '$.streaming.runtime.followLive must be boolean.');
        }
        if (spec.history !== undefined &&
            (spec.history === null || typeof spec.history !== 'object' || Array.isArray(spec.history))) {
            throw new GraflumeError('INVALID_SPEC', '$.streaming.runtime.history must be an object.');
        }
        if (spec.history !== undefined) {
            const historyUnknown = Object.keys(spec.history).find((key) => !['maxBatches', 'pageRows'].includes(key));
            if (historyUnknown !== undefined) {
                throw new GraflumeError('INVALID_SPEC', `Unknown streaming history property "${historyUnknown}".`);
            }
        }
        return {
            schedule: spec.schedule ?? 'animation-frame',
            maxBatchesPerFrame: boundedInteger$6(spec.maxBatchesPerFrame, 8, 1_024, '$.streaming.runtime.maxBatchesPerFrame'),
            overflow: spec.overflow ?? 'reject',
            paused: spec.paused ?? false,
            followLive: spec.followLive ?? true,
            historyBatches: boundedInteger$6(spec.history?.maxBatches, 256, 4_096, '$.streaming.runtime.history.maxBatches'),
            historyPageRows: boundedInteger$6(spec.history?.pageRows, 1_000, 100_000, '$.streaming.runtime.history.pageRows'),
        };
    }
    function defaultScheduler(mode) {
        if (mode === 'animation-frame' && typeof requestAnimationFrame === 'function') {
            return {
                request: (callback) => requestAnimationFrame(callback),
                cancel: (handle) => cancelAnimationFrame(handle),
            };
        }
        if (mode === 'microtask') {
            let sequence = 0;
            const cancelled = new Set();
            return {
                request(callback) {
                    sequence += 1;
                    const id = sequence;
                    queueMicrotask(() => {
                        if (!cancelled.delete(id))
                            callback(performance.now());
                    });
                    return id;
                },
                cancel(handle) {
                    cancelled.add(handle);
                },
            };
        }
        return {
            request: (callback) => setTimeout(() => callback(performance.now()), 16),
            cancel: (handle) => clearTimeout(handle),
        };
    }
    function cloneUpdate(update) {
        return structuredClone(update);
    }
    function updateRows(update) {
        if (!Array.isArray(update.rows)) {
            throw new GraflumeError('INVALID_DATA', '$.update.rows must be an array.');
        }
        return update.rows.length;
    }
    function coalesceStableKey(value, path) {
        if (value instanceof Date && Number.isFinite(value.getTime()))
            return `date:${value.toISOString()}`;
        if (typeof value === 'string' && value !== '')
            return `string:${value}`;
        if (typeof value === 'number' && Number.isFinite(value))
            return `number:${value}`;
        if (typeof value === 'boolean')
            return `boolean:${value}`;
        throw new GraflumeError('INVALID_DATA', 'Streaming coalesce keys must be portable scalars.', {
            path,
        });
    }
    function abortedError$1() {
        return new DOMException('The streaming update was cancelled.', 'AbortError');
    }
    /** RAF-coalesced controller around the stable-key incremental transform pipeline. */
    class IncrementalStreamRuntime {
        #pipeline;
        #streaming;
        #runtime;
        #scheduler;
        #history;
        #queue = [];
        #queuedRows = 0;
        #frame = null;
        #paused;
        #followLive;
        #visible = null;
        #latest = null;
        #appliedBatches = 0;
        #droppedBatches = 0;
        #coalescedBatches = 0;
        #cancelledBatches = 0;
        constructor(input, transforms, streaming, runtime = {}, scheduler) {
            this.#runtime = normalizeRuntimeSpec(runtime);
            this.#streaming = structuredClone(streaming);
            this.#pipeline = new IncrementalTransformPipeline(input, transforms, { streaming });
            this.#scheduler = scheduler ?? defaultScheduler(this.#runtime.schedule);
            this.#history = new BoundedRingBuffer(this.#runtime.historyBatches);
            this.#paused = this.#runtime.paused;
            this.#followLive = this.#runtime.followLive;
        }
        enqueue(update, options = {}) {
            if (options.signal?.aborted === true)
                return Promise.reject(abortedError$1());
            let snapshot;
            try {
                snapshot = cloneUpdate(update);
                updateRows(snapshot);
            }
            catch (error) {
                return Promise.reject(error);
            }
            return new Promise((resolve, reject) => {
                const subscriber = {
                    resolve,
                    reject,
                    ...(options.signal === undefined ? {} : { signal: options.signal }),
                };
                if (options.signal !== undefined) {
                    subscriber.abort = () => this.#cancelSubscriber(subscriber);
                    options.signal.addEventListener('abort', subscriber.abort, { once: true });
                }
                if (!this.#admit(snapshot, subscriber))
                    return;
                this.#schedule();
            });
        }
        pause() {
            this.#paused = true;
            if (this.#frame !== null) {
                this.#scheduler.cancel(this.#frame);
                this.#frame = null;
            }
        }
        resume() {
            if (!this.#paused)
                return;
            this.#paused = false;
            this.#schedule();
        }
        setFollowLive(follow) {
            this.#followLive = follow;
            if (follow && this.#latest !== null)
                this.#visible = this.#latest;
        }
        state() {
            return {
                version: 1,
                paused: this.#paused,
                followLive: this.#followLive,
                scheduled: this.#frame !== null,
                queuedBatches: this.#queue.length,
                queuedRows: this.#queuedRows,
                appliedBatches: this.#appliedBatches,
                droppedBatches: this.#droppedBatches,
                coalescedBatches: this.#coalescedBatches,
                cancelledBatches: this.#cancelledBatches,
                visibleSequence: this.#visible?.state.sequence ?? 0,
                liveSequence: this.#latest?.state.sequence ?? 0,
                historyBatches: this.#history.length,
            };
        }
        visible() {
            return this.#visible === null
                ? null
                : structuredClone(this.#visible);
        }
        latest() {
            return this.#latest === null
                ? null
                : structuredClone(this.#latest);
        }
        historyPage(cursor = 0) {
            if (!Number.isInteger(cursor) || cursor < 0) {
                throw new GraflumeError('INVALID_DATA', 'Streaming history cursor must be non-negative.');
            }
            const entries = this.#history.values().reverse();
            const page = [];
            let rowCount = 0;
            let index = cursor;
            while (index < entries.length) {
                const entry = entries[index];
                if (page.length > 0 && rowCount + entry.rows.length > this.#runtime.historyPageRows)
                    break;
                page.push(structuredClone(entry));
                rowCount += entry.rows.length;
                index += 1;
                if (rowCount >= this.#runtime.historyPageRows)
                    break;
            }
            return {
                version: 1,
                entries: page,
                nextCursor: index < entries.length ? index : null,
                retainedBatches: entries.length,
            };
        }
        destroy() {
            if (this.#frame !== null)
                this.#scheduler.cancel(this.#frame);
            this.#frame = null;
            const error = new GraflumeError('DESTROYED_CHART', 'Streaming runtime was destroyed.');
            for (const batch of this.#queue)
                this.#settle(batch, 'reject', error);
            this.#queue = [];
            this.#queuedRows = 0;
        }
        #admit(update, subscriber) {
            const maxBatches = this.#streaming.queue?.maxBatches ?? 16;
            const maxRows = this.#streaming.queue?.maxRows ?? 100_000;
            const rows = updateRows(update);
            const over = () => this.#queue.length >= maxBatches || this.#queuedRows + rows > maxRows;
            if (over() && this.#runtime.overflow === 'coalesce') {
                const last = this.#queue.at(-1);
                if (last !== undefined && this.#coalesce(last, update, maxRows)) {
                    last.subscribers.push(subscriber);
                    this.#coalescedBatches += 1;
                    return true;
                }
            }
            if (over() && this.#runtime.overflow === 'drop-oldest') {
                while (over() && this.#queue.length > 0) {
                    const dropped = this.#queue.shift();
                    this.#queuedRows -= updateRows(dropped.update);
                    this.#droppedBatches += 1;
                    this.#settle(dropped, 'reject', new GraflumeError('INVALID_DATA', 'Streaming batch was dropped by explicit overflow policy.'));
                }
            }
            if (over()) {
                subscriber.signal?.removeEventListener('abort', subscriber.abort);
                subscriber.reject(new GraflumeError('INVALID_DATA', 'Streaming queue backpressure limit reached; the batch was rejected without mutation.'));
                return false;
            }
            this.#queue.push({ update, subscribers: [subscriber] });
            this.#queuedRows += rows;
            return true;
        }
        #coalesce(batch, incoming, maximumRows) {
            const mode = incoming.mode ?? this.#streaming.mode ?? 'append';
            const batchMode = batch.update.mode ?? this.#streaming.mode ?? 'append';
            if (mode !== batchMode)
                return false;
            const key = this.#streaming.key;
            const combined = [...batch.update.rows, ...incoming.rows];
            if (mode === 'append') {
                if (combined.length > maximumRows)
                    return false;
                this.#queuedRows += incoming.rows.length;
                batch.update = {
                    mode,
                    rows: combined,
                    ...((incoming.watermark ?? batch.update.watermark) === undefined
                        ? {}
                        : {
                            watermark: Math.max(incoming.watermark ?? -Infinity, batch.update.watermark ?? -Infinity),
                        }),
                };
                return true;
            }
            const latest = new Map();
            for (const [index, row] of combined.entries()) {
                latest.set(coalesceStableKey(ownValue(row, key), `$.update.rows[${index}].${key}`), row);
            }
            const rows = [...latest.values()];
            if (rows.length > maximumRows)
                return false;
            this.#queuedRows += rows.length - batch.update.rows.length;
            batch.update = {
                mode,
                rows,
                ...((incoming.watermark ?? batch.update.watermark) === undefined
                    ? {}
                    : {
                        watermark: Math.max(incoming.watermark ?? -Infinity, batch.update.watermark ?? -Infinity),
                    }),
            };
            return true;
        }
        #schedule() {
            if (this.#paused || this.#frame !== null || this.#queue.length === 0)
                return;
            this.#frame = this.#scheduler.request(() => {
                this.#frame = null;
                this.#drainFrame();
            });
        }
        #drainFrame() {
            if (this.#paused)
                return;
            let count = 0;
            while (count < this.#runtime.maxBatchesPerFrame) {
                const batch = this.#queue.shift();
                if (batch === undefined)
                    break;
                this.#queuedRows -= updateRows(batch.update);
                if (batch.subscribers.length === 0)
                    continue;
                try {
                    const result = this.#pipeline.apply(batch.update);
                    this.#latest = result;
                    if (this.#followLive)
                        this.#visible = result;
                    this.#appliedBatches += 1;
                    this.#history.push({
                        sequence: result.state.sequence,
                        mode: batch.update.mode ?? this.#streaming.mode ?? 'append',
                        ...(batch.update.watermark === undefined ? {} : { watermark: batch.update.watermark }),
                        rows: structuredClone(batch.update.rows),
                    });
                    this.#settle(batch, 'resolve', result);
                }
                catch (error) {
                    this.#settle(batch, 'reject', error);
                }
                count += 1;
            }
            this.#schedule();
        }
        #cancelSubscriber(subscriber) {
            for (const batch of this.#queue) {
                const index = batch.subscribers.indexOf(subscriber);
                if (index < 0)
                    continue;
                batch.subscribers.splice(index, 1);
                subscriber.signal?.removeEventListener('abort', subscriber.abort);
                subscriber.reject(abortedError$1());
                this.#cancelledBatches += 1;
                if (batch.subscribers.length === 0) {
                    this.#queue = this.#queue.filter((candidate) => candidate !== batch);
                    this.#queuedRows -= updateRows(batch.update);
                }
                return;
            }
        }
        #settle(batch, action, value) {
            for (const subscriber of batch.subscribers) {
                if (subscriber.abort !== undefined) {
                    subscriber.signal?.removeEventListener('abort', subscriber.abort);
                }
                if (action === 'resolve')
                    subscriber.resolve(value);
                else
                    subscriber.reject(value);
            }
            batch.subscribers.splice(0);
        }
    }
    function createIncrementalStreamRuntime(input, transforms, streaming, runtime = {}, scheduler) {
        return new IncrementalStreamRuntime(input, transforms, streaming, runtime, scheduler);
    }
    function portableStreamData(input) {
        return Array.isArray(input)
            ? { kind: 'rows', rows: input }
            : { kind: 'columns', columns: input };
    }
    function resultSnapshot(result, expected) {
        if (result.action === 'close' || result.action !== expected) {
            throw new GraflumeError('INVALID_DATA', 'Worker stream retention result kind mismatch.');
        }
        return structuredClone(result.snapshot);
    }
    /**
     * Public session wrapper whose retained rows and watermark live inside the
     * Worker protocol rather than in the main-thread IncrementalStreamRuntime.
     */
    class WorkerStreamRetentionRuntime {
        #worker;
        #streamId;
        #latest;
        #closed = false;
        constructor(worker, streamId, snapshot) {
            this.#worker = worker;
            this.#streamId = streamId;
            this.#latest = structuredClone(snapshot);
        }
        static async create(worker, streamId, input, streaming, options = {}) {
            const result = await worker.streamRetention({
                streamId,
                action: {
                    type: 'initialize',
                    data: portableStreamData(input),
                    streaming,
                    ...(options.includeReplay === undefined ? {} : { includeReplay: options.includeReplay }),
                },
            }, options.signal === undefined ? {} : { signal: options.signal });
            return new WorkerStreamRetentionRuntime(worker, streamId, resultSnapshot(result, 'initialize'));
        }
        static async restore(worker, streamId, replay, options = {}) {
            const result = await worker.streamRetention({
                streamId,
                action: {
                    type: 'restore',
                    replay,
                    ...(options.includeReplay === undefined ? {} : { includeReplay: options.includeReplay }),
                },
            }, options.signal === undefined ? {} : { signal: options.signal });
            return new WorkerStreamRetentionRuntime(worker, streamId, resultSnapshot(result, 'restore'));
        }
        streamId() {
            return this.#streamId;
        }
        latest() {
            return structuredClone(this.#latest);
        }
        async enqueue(update, options = {}) {
            this.#assertOpen();
            const result = await this.#worker.streamRetention({
                streamId: this.#streamId,
                action: {
                    type: 'update',
                    update,
                    ...(options.includeReplay === undefined ? {} : { includeReplay: options.includeReplay }),
                },
            }, options.signal === undefined ? {} : { signal: options.signal });
            this.#latest = resultSnapshot(result, 'update');
            return this.latest();
        }
        async snapshot(options = {}) {
            this.#assertOpen();
            const result = await this.#worker.streamRetention({
                streamId: this.#streamId,
                action: {
                    type: 'snapshot',
                    ...(options.includeReplay === undefined ? {} : { includeReplay: options.includeReplay }),
                },
            }, options.signal === undefined ? {} : { signal: options.signal });
            this.#latest = resultSnapshot(result, 'snapshot');
            return this.latest();
        }
        async exportReplay(options = {}) {
            const snapshot = await this.snapshot({ ...options, includeReplay: true });
            if (snapshot.replayStatus !== 'available' || snapshot.replay === undefined) {
                throw new GraflumeError('INVALID_DATA', 'Worker stream replay is truncated; initialize a fresh bounded snapshot.');
            }
            return structuredClone(snapshot.replay);
        }
        async close(options = {}) {
            if (this.#closed)
                return;
            const result = await this.#worker.streamRetention({ streamId: this.#streamId, action: { type: 'close' } }, options.signal === undefined ? {} : { signal: options.signal });
            if (result.action !== 'close' || !result.closed) {
                throw new GraflumeError('INVALID_DATA', 'Worker stream close result kind mismatch.');
            }
            this.#closed = true;
        }
        #assertOpen() {
            if (this.#closed) {
                throw new GraflumeError('INVALID_DATA', 'Worker stream retention runtime is closed.');
            }
        }
    }
    function createWorkerStreamRetentionRuntime(worker, streamId, input, streaming, options = {}) {
        return WorkerStreamRetentionRuntime.create(worker, streamId, input, streaming, options);
    }
    function restoreWorkerStreamRetentionRuntime(worker, streamId, replay, options = {}) {
        return WorkerStreamRetentionRuntime.restore(worker, streamId, replay, options);
    }

    const absoluteMaximumRows = 1_000_000;
    function closedObject(value, allowed, path) {
        if (!isPlainObject(value)) {
            throw new GraflumeError('INVALID_SPEC', `${path} must be an object.`, { path });
        }
        const unknown = Object.keys(value).find((key) => !allowed.has(key));
        if (unknown !== undefined) {
            throw new GraflumeError('INVALID_SPEC', `Unknown ${path} property "${unknown}".`, {
                path: `${path}.${unknown}`,
            });
        }
        return value;
    }
    function boundedInteger$5(value, fallback, maximum, path) {
        const resolved = value ?? fallback;
        if (!Number.isInteger(resolved) || resolved < 1 || resolved > maximum) {
            throw new GraflumeError('INVALID_SPEC', `${path} must be an integer from 1 to ${maximum}.`, {
                path,
            });
        }
        return resolved;
    }
    function boundedNonNegativeInteger(value, fallback, maximum, path) {
        const resolved = value ?? fallback;
        if (!Number.isInteger(resolved) || resolved < 0 || resolved > maximum) {
            throw new GraflumeError('INVALID_SPEC', `${path} must be an integer from 0 to ${maximum}.`, {
                path,
            });
        }
        return resolved;
    }
    function field$2(value, path) {
        if (typeof value !== 'string' || value.trim() === '') {
            throw new GraflumeError('INVALID_SPEC', `${path} must be a non-empty field name.`, { path });
        }
        // ownValue() applies the shared unsafe-key guard.
        ownValue(Object.create(null), value);
        return value;
    }
    function normalizeOptions$1(options) {
        closedObject(options, new Set([
            'key',
            'category',
            'value',
            'weight',
            'aggregate',
            'direction',
            'maxRows',
            'windowRows',
            'overscanRows',
        ]), '$.barVirtualization.options');
        const maxRows = boundedInteger$5(options.maxRows, 100_000, absoluteMaximumRows, '$.barVirtualization.maxRows');
        const windowRows = boundedInteger$5(options.windowRows, 100, Math.min(maxRows, 10_000), '$.barVirtualization.windowRows');
        const overscanRows = boundedNonNegativeInteger(options.overscanRows, Math.min(20, windowRows), windowRows, '$.barVirtualization.overscanRows');
        if (options.aggregate !== undefined &&
            !['value', 'count', 'weighted-count'].includes(options.aggregate)) {
            throw new GraflumeError('INVALID_SPEC', '$.barVirtualization.aggregate is unsupported.');
        }
        if (options.direction !== undefined &&
            options.direction !== 'ascending' &&
            options.direction !== 'descending') {
            throw new GraflumeError('INVALID_SPEC', '$.barVirtualization.direction is unsupported.');
        }
        return {
            key: field$2(options.key, '$.barVirtualization.key'),
            category: field$2(options.category, '$.barVirtualization.category'),
            value: field$2(options.value, '$.barVirtualization.value'),
            ...(options.weight === undefined
                ? {}
                : { weight: field$2(options.weight, '$.barVirtualization.weight') }),
            aggregate: options.aggregate ?? 'value',
            direction: options.direction ?? 'descending',
            maxRows,
            windowRows,
            overscanRows,
        };
    }
    function validateAction(action) {
        const object = closedObject(action, new Set(['type', 'rows', 'command', 'direction', 'start']), '$.barVirtualization.action');
        if (!['replace', 'append', 'upsert', 'navigate', 'sort', 'window'].includes(String(object.type))) {
            throw new GraflumeError('INVALID_SPEC', '$.barVirtualization.action.type is unsupported.');
        }
        const expected = object.type === 'replace' || object.type === 'append' || object.type === 'upsert'
            ? new Set(['type', 'rows'])
            : object.type === 'navigate'
                ? new Set(['type', 'command'])
                : object.type === 'sort'
                    ? new Set(['type', 'direction'])
                    : new Set(['type', 'start']);
        const unexpected = Object.keys(object).find((key) => !expected.has(key));
        if (unexpected !== undefined) {
            throw new GraflumeError('INVALID_SPEC', `Property "${unexpected}" is not valid for bar virtualization action "${String(object.type)}".`);
        }
        if ((object.type === 'replace' || object.type === 'append' || object.type === 'upsert') &&
            !Array.isArray(object.rows)) {
            throw new GraflumeError('INVALID_DATA', '$.barVirtualization.action.rows must be an array.');
        }
        if (object.type === 'navigate' &&
            !['ArrowUp', 'ArrowDown', 'Home', 'End', 'PageUp', 'PageDown'].includes(String(object.command))) {
            throw new GraflumeError('INVALID_SPEC', '$.barVirtualization.action.command is unsupported.');
        }
        if (object.type === 'sort' &&
            object.direction !== 'ascending' &&
            object.direction !== 'descending') {
            throw new GraflumeError('INVALID_SPEC', '$.barVirtualization.action.direction is unsupported.');
        }
        if (object.type === 'window' &&
            (typeof object.start !== 'number' || !Number.isFinite(object.start))) {
            throw new GraflumeError('INVALID_SPEC', '$.barVirtualization.action.start must be finite.');
        }
    }
    function validatePreviousSnapshot(previous, options) {
        if (previous.version !== 1 ||
            previous.state.version !== 1 ||
            previous.state.portable !== true ||
            !Array.isArray(previous.retained) ||
            !Array.isArray(previous.ranked) ||
            !Array.isArray(previous.window.rows)) {
            throw new GraflumeError('INVALID_DATA', 'Bar virtualization snapshot is malformed.');
        }
        if (previous.retained.length > options.maxRows || previous.ranked.length > options.maxRows) {
            throw new GraflumeError('INVALID_DATA', 'Bar virtualization snapshot exceeds maxRows.');
        }
        if (previous.window.rows.length > options.windowRows + options.overscanRows * 2) {
            throw new GraflumeError('INVALID_DATA', 'Bar virtualization snapshot exceeds its materialized-row bound.');
        }
    }
    function scalarKey$1(value, path) {
        if (value instanceof Date && Number.isFinite(value.getTime()))
            return `date:${value.toISOString()}`;
        if (typeof value === 'string' && value !== '')
            return `string:${value}`;
        if (typeof value === 'number' && Number.isFinite(value))
            return `number:${value}`;
        if (typeof value === 'boolean')
            return `boolean:${value}`;
        throw new GraflumeError('INVALID_DATA', 'Virtual bar keys must be non-empty strings, finite numbers, booleans, or valid Dates.', { path });
    }
    function finite$4(value, path) {
        if (typeof value !== 'number' || !Number.isFinite(value)) {
            throw new GraflumeError('INVALID_DATA', `${path} must be a finite number.`, { path });
        }
        return value;
    }
    function canonical(value) {
        if (value instanceof Date)
            return `{"$date":${JSON.stringify(value.toISOString())}}`;
        if (Array.isArray(value))
            return `[${value.map(canonical).join(',')}]`;
        if (value !== null && typeof value === 'object') {
            return `{${Object.entries(value)
            .sort(([left], [right]) => left.localeCompare(right, 'en'))
            .map(([key, child]) => `${JSON.stringify(key)}:${canonical(child)}`)
            .join(',')}}`;
        }
        return JSON.stringify(value) ?? 'null';
    }
    function cloneRows(rows) {
        return structuredClone(rows);
    }
    function rowKey(row, options, index) {
        return scalarKey$1(ownValue(row, options.key), `$.barVirtualization.rows[${index}].${options.key}`);
    }
    function uniqueRows(rows, options) {
        const keys = new Set();
        return cloneRows(rows).map((row, index) => {
            const key = rowKey(row, options, index);
            if (keys.has(key)) {
                throw new GraflumeError('INVALID_DATA', 'Virtual bar update keys must be unique.', {
                    path: `$.barVirtualization.rows[${index}].${options.key}`,
                });
            }
            keys.add(key);
            return row;
        });
    }
    function retainedAfterAction(previous, options, action) {
        const before = previous?.retained ?? [];
        if (action.type === 'replace') {
            const incoming = uniqueRows(action.rows, options);
            const ring = new BoundedRingBuffer(options.maxRows, incoming);
            const next = ring.values();
            const beforeByKey = new Map(before.map((row, index) => [rowKey(row, options, index), row]));
            const reused = next.filter((row, index) => {
                const old = beforeByKey.get(rowKey(row, options, index));
                return old !== undefined && canonical(old) === canonical(row);
            }).length;
            return {
                rows: next,
                accepted: incoming.length,
                updated: 0,
                evicted: Math.max(0, incoming.length - options.maxRows),
                reused,
            };
        }
        if (action.type !== 'append' && action.type !== 'upsert') {
            return { rows: before, accepted: 0, updated: 0, evicted: 0, reused: before.length };
        }
        const incoming = uniqueRows(action.rows, options);
        const ring = new BoundedRingBuffer(options.maxRows, before);
        const indexes = new Map();
        ring.values().forEach((row, index) => indexes.set(rowKey(row, options, index), index));
        let updated = 0;
        let evicted = 0;
        for (let index = 0; index < incoming.length; index += 1) {
            const row = incoming[index];
            const key = rowKey(row, options, index);
            const existing = indexes.get(key);
            if (existing !== undefined) {
                if (action.type === 'append') {
                    throw new GraflumeError('INVALID_DATA', `Virtual bar append key ${key} already exists.`);
                }
                const merged = { ...ring.at(existing), ...row };
                ring.set(existing, merged);
                updated += 1;
                continue;
            }
            const removed = ring.push(row);
            if (removed !== undefined)
                evicted += 1;
            indexes.clear();
            ring
                .values()
                .forEach((retained, retainedIndex) => indexes.set(rowKey(retained, options, retainedIndex), retainedIndex));
        }
        const next = ring.values();
        const beforeByKey = new Map(before.map((row, index) => [rowKey(row, options, index), row]));
        const reused = next.filter((row, index) => {
            const old = beforeByKey.get(rowKey(row, options, index));
            return old !== undefined && canonical(old) === canonical(row);
        }).length;
        return { rows: next, accepted: incoming.length, updated, evicted, reused };
    }
    function rankedRows(retained, options, previous) {
        const previousRanks = Object.fromEntries((previous?.ranked ?? []).map(({ id, rank }) => [id, rank]));
        return rankBars(retained.map((row, index) => {
            const idValue = ownValue(row, options.key);
            const categoryValue = ownValue(row, options.category);
            return {
                id: scalarKey$1(idValue, `$.barVirtualization.retained[${index}].${options.key}`),
                category: categoryValue instanceof Date ? categoryValue.toISOString() : String(categoryValue ?? ''),
                value: finite$4(ownValue(row, options.value), `$.barVirtualization.retained[${index}].${options.value}`),
                ...(options.weight === undefined
                    ? {}
                    : {
                        weight: finite$4(ownValue(row, options.weight), `$.barVirtualization.retained[${index}].${options.weight}`),
                    }),
            };
        }), {
            aggregate: options.aggregate,
            direction: options.direction,
            previousRanks,
        });
    }
    function movedIndex(current, total, command, page) {
        if (total === 0)
            return -1;
        if (command === 'Home')
            return 0;
        if (command === 'End')
            return total - 1;
        const start = Math.max(0, current);
        const delta = command === 'ArrowUp' ? -1 : command === 'ArrowDown' ? 1 : command === 'PageUp' ? -page : page;
        return Math.max(0, Math.min(total - 1, start + delta));
    }
    function virtualWindow(ranked, options, activeIndex, authoredStart) {
        const total = ranked.length;
        if (total === 0) {
            return {
                totalRows: 0,
                viewportStart: 0,
                viewportEnd: 0,
                start: 0,
                end: 0,
                activeIndex: -1,
                activeId: null,
                rows: [],
            };
        }
        const maximumStart = Math.max(0, total - options.windowRows);
        let viewportStart = Math.max(0, Math.min(maximumStart, Math.floor(authoredStart)));
        if (activeIndex < viewportStart)
            viewportStart = activeIndex;
        if (activeIndex >= viewportStart + options.windowRows) {
            viewportStart = Math.min(maximumStart, activeIndex - options.windowRows + 1);
        }
        const viewportEnd = Math.min(total, viewportStart + options.windowRows);
        const start = Math.max(0, viewportStart - options.overscanRows);
        const end = Math.min(total, viewportEnd + options.overscanRows);
        return {
            totalRows: total,
            viewportStart,
            viewportEnd,
            start,
            end,
            activeIndex,
            activeId: ranked[activeIndex]?.id ?? null,
            rows: ranked.slice(start, end),
        };
    }
    /** Pure protocol payload reducer used identically on the main thread and in Workers. */
    function executePortableBarVirtualization(request) {
        validateAction(request.action);
        if (request.previous === undefined && request.options === undefined) {
            throw new GraflumeError('INVALID_SPEC', 'Initial bar virtualization requires portable options.');
        }
        const previous = request.previous;
        if (previous !== undefined && request.options !== undefined) {
            throw new GraflumeError('INVALID_SPEC', 'Bar virtualization options are immutable after initialization; use the sort action for direction changes.');
        }
        const options = normalizeOptions$1(request.options ??
            {
                ...previous.options,
                ...(request.action.type === 'sort' ? { direction: request.action.direction } : {}),
            });
        if (previous !== undefined)
            validatePreviousSnapshot(previous, options);
        const retention = retainedAfterAction(previous, options, request.action);
        const rerank = previous === undefined ||
            request.action.type === 'replace' ||
            request.action.type === 'append' ||
            request.action.type === 'upsert' ||
            request.action.type === 'sort';
        const ranked = rerank ? rankedRows(retention.rows, options, previous) : previous.ranked;
        const previousRanked = new Map((previous?.ranked ?? []).map((row) => [row.id, row]));
        const reusedRankRows = ranked.filter((row) => {
            const old = previousRanked.get(row.id);
            return old !== undefined && canonical(old) === canonical(row);
        }).length;
        const rankChanges = rerank
            ? ranked.flatMap((row) => row.previousRank !== null && row.rankChange !== null && row.rankChange !== 0
                ? [
                    {
                        id: row.id,
                        previousRank: row.previousRank,
                        rank: row.rank,
                        change: row.rankChange,
                    },
                ]
                : [])
            : [];
        const previousActiveId = previous?.window.activeId ?? null;
        let activeIndex = Math.max(0, previousActiveId === null ? 0 : ranked.findIndex(({ id }) => id === previousActiveId));
        if (ranked.length === 0)
            activeIndex = -1;
        if (request.action.type === 'navigate') {
            activeIndex = movedIndex(previous?.window.activeIndex ?? activeIndex, ranked.length, request.action.command, options.windowRows);
        }
        const authoredStart = request.action.type === 'window' ? request.action.start : (previous?.window.viewportStart ?? 0);
        const window = virtualWindow(ranked, options, activeIndex, authoredStart);
        const previousWindowIds = new Set((previous?.window.rows ?? []).map(({ id }) => id));
        const reusedWindowRows = window.rows.filter(({ id }) => previousWindowIds.has(id)).length;
        const action = previous === undefined ? 'initial' : request.action.type;
        const step = {
            sequence: (previous?.state.sequence ?? -1) + 1,
            action,
            retainedRows: retention.rows.length,
            acceptedRows: retention.accepted,
            updatedRows: retention.updated,
            evictedRows: retention.evicted,
            reusedRetainedRows: retention.reused,
            recomputedRanks: rerank ? ranked.length : 0,
            reusedRankRows,
            reusedWindowRows,
            materializedRows: window.rows.length,
            rankChanges,
        };
        const state = {
            version: 1,
            portable: true,
            sequence: step.sequence,
            retainedRows: retention.rows.length,
            rankedRows: ranked.length,
            materializedRows: window.rows.length,
            maxRows: options.maxRows,
            maxMaterializedRows: options.windowRows + options.overscanRows * 2,
            totalAcceptedRows: (previous?.state.totalAcceptedRows ?? 0) + retention.accepted,
            totalUpdatedRows: (previous?.state.totalUpdatedRows ?? 0) + retention.updated,
            totalEvictedRows: (previous?.state.totalEvictedRows ?? 0) + retention.evicted,
            last: step,
        };
        const snapshot = {
            version: 1,
            options,
            retained: cloneRows(retention.rows),
            ranked: structuredClone(ranked),
            window: structuredClone(window),
            state,
        };
        try {
            structuredClone(snapshot);
        }
        catch (error) {
            throw new GraflumeError('INVALID_DATA', 'Bar virtualization state is not Worker-portable.', {
                cause: error,
            });
        }
        return { snapshot, state, window: snapshot.window };
    }
    class BarVirtualizationController {
        #current;
        constructor(rows, options) {
            this.#current = executePortableBarVirtualization({
                options,
                action: { type: 'replace', rows },
            });
        }
        replace(rows) {
            return this.#transition({ type: 'replace', rows });
        }
        append(rows) {
            return this.#transition({ type: 'append', rows });
        }
        upsert(rows) {
            return this.#transition({ type: 'upsert', rows });
        }
        navigate(command) {
            return this.#transition({ type: 'navigate', command });
        }
        sort(direction) {
            return this.#transition({ type: 'sort', direction });
        }
        setWindowStart(start) {
            if (!Number.isFinite(start)) {
                throw new GraflumeError('INVALID_SPEC', 'Virtual bar window start must be finite.');
            }
            return this.#transition({ type: 'window', start });
        }
        snapshot() {
            return structuredClone(this.#current.snapshot);
        }
        state() {
            return structuredClone(this.#current.state);
        }
        window() {
            return structuredClone(this.#current.window);
        }
        #transition(action) {
            this.#current = executePortableBarVirtualization({
                previous: this.#current.snapshot,
                action,
            });
            return {
                snapshot: this.snapshot(),
                state: this.state(),
                window: this.window(),
            };
        }
    }
    function createBarVirtualizationController(rows, options) {
        return new BarVirtualizationController(rows, options);
    }

    function finite$3(value) {
        return typeof value === 'number' && Number.isFinite(value);
    }
    function averageState(state, name) {
        return (state.averages[name] ??= {
            queue: [],
            sum: 0,
            valid: 0,
            seed: [],
            previous: null,
        });
    }
    function queue(state, name) {
        return (state.queues[name] ??= []);
    }
    function rollingSum(state, name, value, period) {
        const rolling = averageState(state, name);
        if (finite$3(value)) {
            rolling.sum += value;
            rolling.valid += 1;
        }
        rolling.queue.push(value);
        if (rolling.queue.length > period) {
            const removed = rolling.queue.shift();
            if (finite$3(removed)) {
                rolling.sum -= removed;
                rolling.valid -= 1;
            }
        }
        return rolling.queue.length === period && rolling.valid === period ? rolling.sum : null;
    }
    function simpleAverage(state, name, value, period) {
        const sum = rollingSum(state, name, value, period);
        return finite$3(sum) ? sum / period : null;
    }
    function exponentialAverage(state, name, value, period) {
        const average = averageState(state, name);
        if (!finite$3(value)) {
            average.seed.length = 0;
            average.previous = null;
            return null;
        }
        if (average.previous === null) {
            average.seed.push(value);
            if (average.seed.length < period)
                return null;
            if (average.seed.length > period)
                average.seed.shift();
            average.previous = average.seed.reduce((sum, item) => sum + item, 0) / period;
        }
        else {
            const alpha = 2 / (period + 1);
            average.previous = value * alpha + average.previous * (1 - alpha);
        }
        return average.previous;
    }
    function wilderAverage(state, name, value, period) {
        const average = averageState(state, name);
        if (!finite$3(value)) {
            average.seed.length = 0;
            average.previous = null;
            return null;
        }
        if (average.previous === null) {
            average.seed.push(value);
            if (average.seed.length < period)
                return null;
            if (average.seed.length > period)
                average.seed.shift();
            average.previous = average.seed.reduce((sum, item) => sum + item, 0) / period;
        }
        else {
            average.previous = (average.previous * (period - 1) + value) / period;
        }
        return average.previous;
    }
    function boundedWindow(state, name, value, period) {
        const values = queue(state, name);
        values.push(value);
        if (values.length > period)
            values.shift();
        return values;
    }
    function weightedAverage(state, name, value, period) {
        const values = boundedWindow(state, name, value, period);
        if (values.length < period || values.some((item) => !finite$3(item)))
            return null;
        const denominator = (period * (period + 1)) / 2;
        return values.reduce((sum, item, index) => sum + item * (index + 1), 0) / denominator;
    }
    function rollingExtrema(state, name, value, period, mode) {
        const values = boundedWindow(state, name, value, period);
        if (values.length < period || values.some((item) => !finite$3(item)))
            return null;
        return mode === 'minimum' ? Math.min(...values) : Math.max(...values);
    }
    function rollingDeviation(state, name, value, average, period, squared) {
        const values = boundedWindow(state, name, value, period);
        if (!finite$3(average) || values.length < period || values.some((item) => !finite$3(item))) {
            return null;
        }
        const sum = values.reduce((total, item) => {
            const difference = item - average;
            return total + (squared ? difference ** 2 : Math.abs(difference));
        }, 0);
        return squared ? Math.sqrt(sum / period) : sum / period;
    }
    function regression(state, value, period) {
        const values = boundedWindow(state, 'regression', value, period);
        if (values.length < period || values.some((item) => !finite$3(item))) {
            return { value: null, slope: null, intercept: null, angle: null };
        }
        const sumX = (period * (period - 1)) / 2;
        const sumXX = (period * (period - 1) * (2 * period - 1)) / 6;
        const denominator = period * sumXX - sumX * sumX;
        const sumY = values.reduce((sum, item) => sum + item, 0);
        const sumXY = values.reduce((sum, item, x) => sum + x * item, 0);
        const slope = denominator === 0 ? 0 : (period * sumXY - sumX * sumY) / denominator;
        const intercept = (sumY - slope * sumX) / period;
        return {
            slope,
            intercept,
            angle: (Math.atan(slope) * 180) / Math.PI,
            value: intercept + slope * (period - 1),
        };
    }
    function pairDifference(left, right, percent = false) {
        if (!finite$3(left) || !finite$3(right) || (percent && right === 0))
            return null;
        return percent ? ((left - right) / right) * 100 : left - right;
    }
    function typical(row) {
        return finite$3(row.high) && finite$3(row.low) && finite$3(row.close)
            ? (row.high + row.low + row.close) / 3
            : null;
    }
    function trueRange(state, row) {
        const previousClose = state.nullable.previousClose;
        state.nullable.previousClose = row.close;
        if (!finite$3(row.high) || !finite$3(row.low))
            return null;
        return !finite$3(previousClose)
            ? row.high - row.low
            : Math.max(row.high - row.low, Math.abs(row.high - previousClose), Math.abs(row.low - previousClose));
    }
    function rollingRangePosition(state, prefix, row, period) {
        const high = rollingExtrema(state, `${prefix}:high`, row.high, period, 'maximum');
        const low = rollingExtrema(state, `${prefix}:low`, row.low, period, 'minimum');
        if (!finite$3(row.close) || !finite$3(high) || !finite$3(low))
            return null;
        return high === low ? 0 : ((row.close - low) / (high - low)) * 100;
    }
    function lagDifference(state, value, period, percent) {
        const values = queue(state, 'lag');
        const previous = values.length >= period ? values[values.length - period] : undefined;
        values.push(value);
        if (values.length > period)
            values.shift();
        return pairDifference(value, previous, percent);
    }
    function relativeStrength(state, value, period) {
        const previous = state.nullable.rsiPrevious;
        state.nullable.rsiPrevious = value;
        if (!finite$3(value) || !finite$3(previous)) {
            queue(state, 'rsi:gains').length = 0;
            queue(state, 'rsi:losses').length = 0;
            state.nullable.rsiAverageGain = null;
            state.nullable.rsiAverageLoss = null;
            return null;
        }
        const change = value - previous;
        const gain = Math.max(0, change);
        const loss = Math.max(0, -change);
        let averageGain = state.nullable.rsiAverageGain;
        let averageLoss = state.nullable.rsiAverageLoss;
        if (!finite$3(averageGain) || !finite$3(averageLoss)) {
            const gains = queue(state, 'rsi:gains');
            const losses = queue(state, 'rsi:losses');
            gains.push(gain);
            losses.push(loss);
            if (gains.length < period)
                return null;
            averageGain = gains.reduce((sum, item) => sum + item, 0) / period;
            averageLoss = losses.reduce((sum, item) => sum + item, 0) / period;
        }
        else {
            averageGain = (averageGain * (period - 1) + gain) / period;
            averageLoss = (averageLoss * (period - 1) + loss) / period;
        }
        state.nullable.rsiAverageGain = averageGain;
        state.nullable.rsiAverageLoss = averageLoss;
        return averageLoss === 0 ? 100 : 100 - 100 / (1 + averageGain / averageLoss);
    }
    function numeric(parameters, name, fallback) {
        return parameters[name] ?? fallback;
    }
    function createTechnicalIndicatorEngine(calculation) {
        return {
            version: 1,
            kind: calculation.capability.kind,
            index: 0,
            averages: {},
            queues: {},
            numbers: {},
            nullable: {},
            flags: {},
            pending: {},
        };
    }
    function legacyStep(state, row, parameters) {
        const value = row.value;
        const period = numeric(parameters, 'period', 14);
        const fastPeriod = numeric(parameters, 'fastPeriod', 12);
        const slowPeriod = numeric(parameters, 'slowPeriod', 26);
        const signalPeriod = numeric(parameters, 'signalPeriod', 9);
        switch (state.kind) {
            case 'sma':
                return { output: { value: simpleAverage(state, 'sma', value, period) }, patches: [] };
            case 'ema':
                return { output: { value: exponentialAverage(state, 'ema', value, period) }, patches: [] };
            case 'wma':
                return { output: { value: weightedAverage(state, 'wma', value, period) }, patches: [] };
            case 'dema': {
                const first = exponentialAverage(state, 'dema:1', value, period);
                const second = exponentialAverage(state, 'dema:2', first, period);
                return {
                    output: { value: finite$3(first) && finite$3(second) ? 2 * first - second : null },
                    patches: [],
                };
            }
            case 'tema':
            case 'trix': {
                const first = exponentialAverage(state, `${state.kind}:1`, value, period);
                const second = exponentialAverage(state, `${state.kind}:2`, first, period);
                const third = exponentialAverage(state, `${state.kind}:3`, second, period);
                if (state.kind === 'tema') {
                    return {
                        output: {
                            value: finite$3(first) && finite$3(second) && finite$3(third)
                                ? 3 * first - 3 * second + third
                                : null,
                        },
                        patches: [],
                    };
                }
                const previous = state.nullable.trixPrevious;
                state.nullable.trixPrevious = third;
                return { output: { value: pairDifference(third, previous, true) }, patches: [] };
            }
            case 'momentum':
                return { output: { value: lagDifference(state, value, period, false) }, patches: [] };
            case 'roc':
                return { output: { value: lagDifference(state, value, period, true) }, patches: [] };
            case 'rsi':
                return { output: { value: relativeStrength(state, value, period) }, patches: [] };
            case 'apo':
            case 'ppo':
            case 'macd': {
                const fast = exponentialAverage(state, `${state.kind}:fast`, value, fastPeriod);
                const slow = exponentialAverage(state, `${state.kind}:slow`, value, slowPeriod);
                const primary = pairDifference(fast, slow, state.kind === 'ppo');
                if (state.kind !== 'macd')
                    return { output: { value: primary }, patches: [] };
                const signal = exponentialAverage(state, 'macd:signal', primary, signalPeriod);
                return {
                    output: { value: primary, signal, histogram: pairDifference(primary, signal) },
                    patches: [],
                };
            }
            case 'disparityindex': {
                const average = simpleAverage(state, 'disparity', value, period);
                return {
                    output: {
                        value: finite$3(value) && finite$3(average) && average !== 0 ? (value / average - 1) * 100 : null,
                    },
                    patches: [],
                };
            }
            case 'linearregression':
            case 'linearregressionslope':
            case 'linearregressionintercept':
            case 'linearregressionangle': {
                const result = regression(state, value, period);
                const output = state.kind === 'linearregressionslope'
                    ? result.slope
                    : state.kind === 'linearregressionintercept'
                        ? result.intercept
                        : state.kind === 'linearregressionangle'
                            ? result.angle
                            : result.value;
                return { output: { value: output }, patches: [] };
            }
            default:
                return null;
        }
    }
    function moneyFlowVolume(row) {
        if (!finite$3(row.close) || !finite$3(row.high) || !finite$3(row.low) || !finite$3(row.volume)) {
            return null;
        }
        const multiplier = row.high === row.low
            ? 0
            : (row.close - row.low - (row.high - row.close)) / (row.high - row.low);
        return multiplier * row.volume;
    }
    function ohlcvStep(state, row, parameters) {
        const period = numeric(parameters, 'period', 14);
        switch (state.kind) {
            case 'abands': {
                const multiplier = numeric(parameters, 'multiplier', 4);
                const valid = finite$3(row.high) && finite$3(row.low) && row.high + row.low !== 0;
                const rawUpper = valid
                    ? row.high * (1 + (multiplier * (row.high - row.low)) / (row.high + row.low))
                    : null;
                const rawLower = valid
                    ? row.low * (1 - (multiplier * (row.high - row.low)) / (row.high + row.low))
                    : null;
                return {
                    output: {
                        value: simpleAverage(state, 'abands:middle', row.close, period),
                        lower: simpleAverage(state, 'abands:lower', rawLower, period),
                        upper: simpleAverage(state, 'abands:upper', rawUpper, period),
                    },
                    patches: [],
                };
            }
            case 'ao': {
                const median = finite$3(row.high) && finite$3(row.low) ? (row.high + row.low) / 2 : null;
                const fast = simpleAverage(state, 'ao:fast', median, parameters.fastPeriod);
                const slow = simpleAverage(state, 'ao:slow', median, parameters.slowPeriod);
                return { output: { value: pairDifference(fast, slow) }, patches: [] };
            }
            case 'aroon':
            case 'aroonoscillator': {
                const highs = boundedWindow(state, 'aroon:high', row.high, period + 1);
                const lows = boundedWindow(state, 'aroon:low', row.low, period + 1);
                let up = null;
                let down = null;
                if (highs.length === period + 1 &&
                    lows.length === period + 1 &&
                    highs.every(finite$3) &&
                    lows.every(finite$3)) {
                    let highIndex = 0;
                    let lowIndex = 0;
                    highs.forEach((item, index) => {
                        if (item >= highs[highIndex])
                            highIndex = index;
                    });
                    lows.forEach((item, index) => {
                        if (item <= lows[lowIndex])
                            lowIndex = index;
                    });
                    up = (highIndex / period) * 100;
                    down = (lowIndex / period) * 100;
                }
                return {
                    output: state.kind === 'aroon' ? { value: up, up, down } : { value: pairDifference(up, down) },
                    patches: [],
                };
            }
            case 'atr':
            case 'natr': {
                const range = trueRange(state, row);
                const average = wilderAverage(state, 'atr', range, period);
                return {
                    output: {
                        value: state.kind === 'natr'
                            ? finite$3(average) && finite$3(row.close) && row.close !== 0
                                ? (average / row.close) * 100
                                : null
                            : average,
                    },
                    patches: [],
                };
            }
            case 'bb': {
                const middle = simpleAverage(state, 'bb:average', row.value, period);
                const deviation = rollingDeviation(state, 'bb:deviation', row.value, middle, period, true);
                const multiplier = parameters.standardDeviations;
                return {
                    output: {
                        value: middle,
                        lower: finite$3(middle) && finite$3(deviation) ? middle - multiplier * deviation : null,
                        upper: finite$3(middle) && finite$3(deviation) ? middle + multiplier * deviation : null,
                    },
                    patches: [],
                };
            }
            case 'cci': {
                const price = typical(row);
                const average = simpleAverage(state, 'cci:average', price, period);
                const deviation = rollingDeviation(state, 'cci:deviation', price, average, period, false);
                return {
                    output: {
                        value: finite$3(price) && finite$3(average) && finite$3(deviation)
                            ? deviation === 0
                                ? 0
                                : (price - average) / (0.015 * deviation)
                            : null,
                    },
                    patches: [],
                };
            }
            case 'chaikin': {
                const flow = moneyFlowVolume(row);
                let line = null;
                if (finite$3(flow)) {
                    const cumulative = (state.numbers.chaikinCumulative ?? 0) + flow;
                    state.numbers.chaikinCumulative = cumulative;
                    line = cumulative;
                }
                const fast = exponentialAverage(state, 'chaikin:fast', line, parameters.fastPeriod);
                const slow = exponentialAverage(state, 'chaikin:slow', line, parameters.slowPeriod);
                return { output: { value: pairDifference(fast, slow) }, patches: [] };
            }
            case 'cmf': {
                const flow = moneyFlowVolume(row);
                const flowSum = rollingSum(state, 'cmf:flow', flow, period);
                const volumeSum = rollingSum(state, 'cmf:volume', row.volume, period);
                return {
                    output: {
                        value: finite$3(flowSum) && finite$3(volumeSum) && volumeSum !== 0 ? flowSum / volumeSum : null,
                    },
                    patches: [],
                };
            }
            case 'cmo': {
                const previous = state.nullable.cmoPrevious;
                state.nullable.cmoPrevious = row.value;
                const valid = finite$3(row.value) && finite$3(previous);
                const gain = valid ? Math.max(0, row.value - previous) : null;
                const loss = valid ? Math.max(0, previous - row.value) : null;
                const gains = rollingSum(state, 'cmo:gains', gain, period);
                const losses = rollingSum(state, 'cmo:losses', loss, period);
                return {
                    output: {
                        value: finite$3(gains) && finite$3(losses)
                            ? gains + losses === 0
                                ? 0
                                : ((gains - losses) / (gains + losses)) * 100
                            : null,
                    },
                    patches: [],
                };
            }
            case 'dmi': {
                const previousHigh = state.nullable.dmiPreviousHigh;
                const previousLow = state.nullable.dmiPreviousLow;
                const range = trueRange(state, row);
                let positiveMovement = null;
                let negativeMovement = null;
                if (state.index > 0 && [row.high, row.low, previousHigh, previousLow].every(finite$3)) {
                    const up = row.high - previousHigh;
                    const down = previousLow - row.low;
                    positiveMovement = up > down && up > 0 ? up : 0;
                    negativeMovement = down > up && down > 0 ? down : 0;
                }
                state.nullable.dmiPreviousHigh = row.high;
                state.nullable.dmiPreviousLow = row.low;
                // The one-shot implementation applies Wilder smoothing to slice(1).
                const averageRange = state.index === 0 ? null : wilderAverage(state, 'dmi:range', range, period);
                const averagePlus = state.index === 0 ? null : wilderAverage(state, 'dmi:plus', positiveMovement, period);
                const averageMinus = state.index === 0 ? null : wilderAverage(state, 'dmi:minus', negativeMovement, period);
                let plus = null;
                let minus = null;
                let dx = null;
                if (finite$3(averageRange) &&
                    finite$3(averagePlus) &&
                    finite$3(averageMinus) &&
                    averageRange !== 0) {
                    plus = (averagePlus / averageRange) * 100;
                    minus = (averageMinus / averageRange) * 100;
                    const sum = plus + minus;
                    dx = sum === 0 ? 0 : (Math.abs(plus - minus) / sum) * 100;
                }
                const value = wilderAverage(state, 'dmi:adx', dx, period);
                return { output: { value, plus, minus }, patches: [] };
            }
            case 'dpo': {
                const average = simpleAverage(state, 'dpo:average', row.value, period);
                const shift = Math.floor(period / 2) + 1;
                const values = queue(state, 'dpo:source');
                const source = values.length >= shift ? values[values.length - shift] : undefined;
                values.push(row.value);
                if (values.length > shift)
                    values.shift();
                return {
                    output: { value: finite$3(average) && finite$3(source) ? source - average : null },
                    patches: [],
                };
            }
            case 'ikh': {
                const conversionHigh = rollingExtrema(state, 'ikh:conversion:high', row.high, parameters.conversionPeriod, 'maximum');
                const conversionLow = rollingExtrema(state, 'ikh:conversion:low', row.low, parameters.conversionPeriod, 'minimum');
                const baseHigh = rollingExtrema(state, 'ikh:base:high', row.high, parameters.basePeriod, 'maximum');
                const baseLow = rollingExtrema(state, 'ikh:base:low', row.low, parameters.basePeriod, 'minimum');
                const spanHigh = rollingExtrema(state, 'ikh:span:high', row.high, parameters.spanPeriod, 'maximum');
                const spanLow = rollingExtrema(state, 'ikh:span:low', row.low, parameters.spanPeriod, 'minimum');
                const conversion = finite$3(conversionHigh) && finite$3(conversionLow)
                    ? (conversionHigh + conversionLow) / 2
                    : null;
                const base = finite$3(baseHigh) && finite$3(baseLow) ? (baseHigh + baseLow) / 2 : null;
                const spanA = finite$3(conversion) && finite$3(base) ? (conversion + base) / 2 : null;
                const spanB = finite$3(spanHigh) && finite$3(spanLow) ? (spanHigh + spanLow) / 2 : null;
                const pending = state.pending[String(state.index)] ?? {};
                delete state.pending[String(state.index)];
                const displacement = parameters.displacement;
                if (finite$3(spanA) && finite$3(spanB)) {
                    const target = String(state.index + displacement);
                    state.pending[target] = {
                        value: spanA,
                        lower: Math.min(spanA, spanB),
                        upper: Math.max(spanA, spanB),
                    };
                }
                if (displacement === 0 && finite$3(spanA) && finite$3(spanB)) {
                    return {
                        output: {
                            value: spanA,
                            lower: Math.min(spanA, spanB),
                            upper: Math.max(spanA, spanB),
                            conversion,
                            base,
                        },
                        patches: [],
                    };
                }
                return {
                    output: {
                        value: pending.value ?? null,
                        lower: pending.lower ?? null,
                        upper: pending.upper ?? null,
                        conversion,
                        base,
                    },
                    patches: [],
                };
            }
            case 'keltnerchannels': {
                const middle = exponentialAverage(state, 'keltner:middle', row.close, parameters.period);
                const range = wilderAverage(state, 'keltner:range', trueRange(state, row), parameters.atrPeriod);
                const multiplier = parameters.multiplier;
                return {
                    output: {
                        value: middle,
                        lower: finite$3(middle) && finite$3(range) ? middle - multiplier * range : null,
                        upper: finite$3(middle) && finite$3(range) ? middle + multiplier * range : null,
                    },
                    patches: [],
                };
            }
            case 'klinger': {
                const previousHigh = state.nullable.klingerPreviousHigh;
                const previousLow = state.nullable.klingerPreviousLow;
                const previousClose = state.nullable.klingerPreviousClose;
                let force = null;
                if (state.index > 0 &&
                    [row.high, row.low, row.close, row.volume, previousHigh, previousLow, previousClose].every(finite$3)) {
                    const previousTrend = state.numbers.klingerTrend ?? 0;
                    const currentSum = row.high + row.low + row.close;
                    const previousSum = previousHigh + previousLow + previousClose;
                    const trend = currentSum > previousSum ? 1 : currentSum < previousSum ? -1 : previousTrend || 1;
                    const movement = row.high - row.low;
                    const previousMovement = state.numbers.klingerMovement ?? 0;
                    const cumulativeMovement = trend === previousTrend
                        ? (state.numbers.klingerCumulative ?? 0) + movement
                        : previousMovement + movement;
                    force =
                        cumulativeMovement === 0
                            ? 0
                            : row.volume * Math.abs(2 * (movement / cumulativeMovement - 1)) * trend * 100;
                    state.numbers.klingerTrend = trend;
                    state.numbers.klingerMovement = movement;
                    state.numbers.klingerCumulative = cumulativeMovement;
                }
                else if (state.index > 0) {
                    state.numbers.klingerTrend = 0;
                    state.numbers.klingerMovement = 0;
                    state.numbers.klingerCumulative = 0;
                }
                state.nullable.klingerPreviousHigh = row.high;
                state.nullable.klingerPreviousLow = row.low;
                state.nullable.klingerPreviousClose = row.close;
                const fast = exponentialAverage(state, 'klinger:fast', force, parameters.fastPeriod);
                const slow = exponentialAverage(state, 'klinger:slow', force, parameters.slowPeriod);
                const value = pairDifference(fast, slow);
                const signal = exponentialAverage(state, 'klinger:signal', value, parameters.signalPeriod);
                return { output: { value, signal }, patches: [] };
            }
            case 'mfi': {
                const price = typical(row);
                const previous = state.nullable.mfiPrevious;
                state.nullable.mfiPrevious = price;
                const valid = finite$3(price) && finite$3(previous) && finite$3(row.volume);
                const flow = valid ? price * row.volume : null;
                const positive = valid ? (price > previous ? flow : 0) : null;
                const negative = valid ? (price < previous ? flow : 0) : null;
                const positiveSum = rollingSum(state, 'mfi:positive', positive, period);
                const negativeSum = rollingSum(state, 'mfi:negative', negative, period);
                let value = null;
                if (finite$3(positiveSum) && finite$3(negativeSum)) {
                    value =
                        negativeSum === 0
                            ? positiveSum === 0
                                ? 50
                                : 100
                            : 100 - 100 / (1 + positiveSum / negativeSum);
                }
                return { output: { value }, patches: [] };
            }
            case 'obv': {
                const previous = state.nullable.obvPrevious;
                let value = null;
                if (finite$3(row.close) && finite$3(row.volume)) {
                    let balance = state.numbers.obvBalance ?? 0;
                    if (state.index > 0 && finite$3(previous)) {
                        if (row.close > previous)
                            balance += row.volume;
                        else if (row.close < previous)
                            balance -= row.volume;
                    }
                    state.numbers.obvBalance = balance;
                    value = balance;
                }
                state.nullable.obvPrevious = row.close;
                return { output: { value }, patches: [] };
            }
            case 'pc': {
                const upper = rollingExtrema(state, 'pc:upper', row.high, period, 'maximum');
                const lower = rollingExtrema(state, 'pc:lower', row.low, period, 'minimum');
                return {
                    output: {
                        value: finite$3(upper) && finite$3(lower) ? (upper + lower) / 2 : null,
                        lower,
                        upper,
                    },
                    patches: [],
                };
            }
            case 'pivotpoints': {
                const previousHigh = state.nullable.pivotPreviousHigh;
                const previousLow = state.nullable.pivotPreviousLow;
                const previousClose = state.nullable.pivotPreviousClose;
                state.nullable.pivotPreviousHigh = row.high;
                state.nullable.pivotPreviousLow = row.low;
                state.nullable.pivotPreviousClose = row.close;
                if (!finite$3(previousHigh) || !finite$3(previousLow) || !finite$3(previousClose)) {
                    return { output: { value: null, support: null, resistance: null }, patches: [] };
                }
                const value = (previousHigh + previousLow + previousClose) / 3;
                return {
                    output: {
                        value,
                        support: 2 * value - previousHigh,
                        resistance: 2 * value - previousLow,
                    },
                    patches: [],
                };
            }
            case 'priceenvelopes': {
                const value = simpleAverage(state, 'envelope', row.value, period);
                const factor = parameters.envelopePercent / 100;
                return {
                    output: {
                        value,
                        lower: finite$3(value) ? value * (1 - factor) : null,
                        upper: finite$3(value) ? value * (1 + factor) : null,
                    },
                    patches: [],
                };
            }
            case 'psar': {
                if (state.index === 0) {
                    state.nullable.psarFirstHigh = row.high;
                    state.nullable.psarFirstLow = row.low;
                    return { output: { value: null }, patches: [] };
                }
                if (state.index === 1) {
                    const firstHigh = state.nullable.psarFirstHigh;
                    const firstLow = state.nullable.psarFirstLow;
                    if (![firstHigh, firstLow, row.high, row.low].every(finite$3)) {
                        state.flags.psarDisabled = true;
                        return { output: { value: null }, patches: [] };
                    }
                    const rising = (row.high + row.low) / 2 >= (firstHigh + firstLow) / 2;
                    state.flags.psarRising = rising;
                    state.numbers.psarExtreme = rising
                        ? Math.max(firstHigh, row.high)
                        : Math.min(firstLow, row.low);
                    state.numbers.psarValue = rising
                        ? Math.min(firstLow, row.low)
                        : Math.max(firstHigh, row.high);
                    state.numbers.psarFactor = parameters.acceleration;
                    boundedWindow(state, 'psar:high', firstHigh, 2);
                    boundedWindow(state, 'psar:low', firstLow, 2);
                    boundedWindow(state, 'psar:high', row.high, 2);
                    boundedWindow(state, 'psar:low', row.low, 2);
                    return { output: { value: state.numbers.psarValue }, patches: [] };
                }
                const previousHighs = queue(state, 'psar:high');
                const previousLows = queue(state, 'psar:low');
                if (state.flags.psarDisabled || !finite$3(row.high) || !finite$3(row.low)) {
                    boundedWindow(state, 'psar:high', row.high, 2);
                    boundedWindow(state, 'psar:low', row.low, 2);
                    return { output: { value: null }, patches: [] };
                }
                let rising = state.flags.psarRising ?? false;
                let extreme = state.numbers.psarExtreme;
                let sar = state.numbers.psarValue;
                let factor = state.numbers.psarFactor;
                sar += factor * (extreme - sar);
                if (rising) {
                    sar = Math.min(sar, previousLows[previousLows.length - 1] ?? sar, previousLows[previousLows.length - 2] ?? sar);
                    if (row.low < sar) {
                        rising = false;
                        sar = extreme;
                        extreme = row.low;
                        factor = parameters.acceleration;
                    }
                    else if (row.high > extreme) {
                        extreme = row.high;
                        factor = Math.min(parameters.maximumAcceleration, factor + parameters.acceleration);
                    }
                }
                else {
                    sar = Math.max(sar, previousHighs[previousHighs.length - 1] ?? sar, previousHighs[previousHighs.length - 2] ?? sar);
                    if (row.high > sar) {
                        rising = true;
                        sar = extreme;
                        extreme = row.high;
                        factor = parameters.acceleration;
                    }
                    else if (row.low < extreme) {
                        extreme = row.low;
                        factor = Math.min(parameters.maximumAcceleration, factor + parameters.acceleration);
                    }
                }
                state.flags.psarRising = rising;
                state.numbers.psarExtreme = extreme;
                state.numbers.psarValue = sar;
                state.numbers.psarFactor = factor;
                boundedWindow(state, 'psar:high', row.high, 2);
                boundedWindow(state, 'psar:low', row.low, 2);
                return { output: { value: sar }, patches: [] };
            }
            case 'slowstochastic':
            case 'stochastic': {
                const fast = rollingRangePosition(state, 'stochastic', row, period);
                const smoothK = state.kind === 'slowstochastic' ? parameters.smoothK : 1;
                const smoothD = state.kind === 'slowstochastic' ? parameters.smoothD : parameters.signalPeriod;
                const value = smoothK === 1 ? fast : simpleAverage(state, 'stochastic:k', fast, smoothK);
                const signal = smoothD === 1 ? value : simpleAverage(state, 'stochastic:d', value, smoothD);
                return { output: { value, signal }, patches: [] };
            }
            case 'supertrend': {
                const previousClose = state.nullable.supertrendPreviousClose;
                state.nullable.supertrendPreviousClose = row.close;
                const range = wilderAverage(state, 'supertrend:atr', trueRange(state, row), parameters.atrPeriod);
                if (!finite$3(row.high) || !finite$3(row.low) || !finite$3(row.close) || !finite$3(range)) {
                    return { output: { value: null, direction: null }, patches: [] };
                }
                const middle = (row.high + row.low) / 2;
                const basicUpper = middle + parameters.multiplier * range;
                const basicLower = middle - parameters.multiplier * range;
                let upper = state.nullable.supertrendUpper;
                let lower = state.nullable.supertrendLower;
                upper =
                    !finite$3(upper) || !finite$3(previousClose) || basicUpper < upper || previousClose > upper
                        ? basicUpper
                        : upper;
                lower =
                    !finite$3(lower) || !finite$3(previousClose) || basicLower > lower || previousClose < lower
                        ? basicLower
                        : lower;
                let direction = state.nullable.supertrendDirection;
                if (!finite$3(direction))
                    direction = row.close <= upper ? -1 : 1;
                else if (direction < 0 && row.close > upper)
                    direction = 1;
                else if (direction > 0 && row.close < lower)
                    direction = -1;
                state.nullable.supertrendUpper = upper;
                state.nullable.supertrendLower = lower;
                state.nullable.supertrendDirection = direction;
                return { output: { value: direction > 0 ? lower : upper, direction }, patches: [] };
            }
            case 'vwap': {
                const price = typical(row);
                let value = null;
                if (finite$3(price) && finite$3(row.volume)) {
                    const weighted = (state.numbers.vwapWeighted ?? 0) + price * row.volume;
                    const volume = (state.numbers.vwapVolume ?? 0) + row.volume;
                    state.numbers.vwapWeighted = weighted;
                    state.numbers.vwapVolume = volume;
                    value = volume === 0 ? null : weighted / volume;
                }
                return { output: { value }, patches: [] };
            }
            case 'williamsr': {
                const position = rollingRangePosition(state, 'williams', row, period);
                return { output: { value: finite$3(position) ? position - 100 : null }, patches: [] };
            }
            case 'zigzag': {
                const output = { value: null };
                const patches = [];
                if (!finite$3(row.value))
                    return { output, patches };
                if (!state.flags.zigzagStarted) {
                    state.flags.zigzagStarted = true;
                    state.numbers.zigzagCandidate = row.value;
                    state.numbers.zigzagCandidateIndex = state.index;
                    state.numbers.zigzagDirection = 0;
                    output.value = row.value;
                    return { output, patches };
                }
                let candidate = state.numbers.zigzagCandidate;
                let candidateIndex = state.numbers.zigzagCandidateIndex;
                let direction = state.numbers.zigzagDirection;
                const threshold = parameters.deviation / 100;
                if (direction >= 0) {
                    if (row.value >= candidate) {
                        patches.push({ index: candidateIndex, role: 'value', value: null });
                        candidate = row.value;
                        candidateIndex = state.index;
                        output.value = candidate;
                    }
                    else if (candidate !== 0 && (candidate - row.value) / Math.abs(candidate) >= threshold) {
                        direction = -1;
                        candidate = row.value;
                        candidateIndex = state.index;
                        output.value = candidate;
                    }
                }
                else if (direction <= 0) {
                    if (row.value <= candidate) {
                        patches.push({ index: candidateIndex, role: 'value', value: null });
                        candidate = row.value;
                        candidateIndex = state.index;
                        output.value = candidate;
                    }
                    else if (candidate !== 0 && (row.value - candidate) / Math.abs(candidate) >= threshold) {
                        direction = 1;
                        candidate = row.value;
                        candidateIndex = state.index;
                        output.value = candidate;
                    }
                }
                state.numbers.zigzagCandidate = candidate;
                state.numbers.zigzagCandidateIndex = candidateIndex;
                state.numbers.zigzagDirection = direction;
                return { output, patches };
            }
            default:
                throw new Error(`Unsupported incremental technical indicator: ${state.kind}`);
        }
    }
    function stepTechnicalIndicatorEngine(state, row, parameters) {
        const step = legacyStep(state, row, parameters) ?? ohlcvStep(state, row, parameters);
        state.index += 1;
        return step;
    }
    /** Incremental equivalent of the previous-completed-session pivot path. */
    function stepTechnicalIndicatorSessionPivot(state, row, boundary) {
        if (boundary && state.index > 0) {
            const previousHigh = state.nullable.sessionPivotHigh;
            const previousLow = state.nullable.sessionPivotLow;
            const previousClose = state.nullable.sessionPivotClose;
            if (finite$3(previousHigh) && finite$3(previousLow) && finite$3(previousClose)) {
                const pivot = (previousHigh + previousLow + previousClose) / 3;
                state.nullable.sessionPivotValue = pivot;
                state.nullable.sessionPivotSupport = 2 * pivot - previousHigh;
                state.nullable.sessionPivotResistance = 2 * pivot - previousLow;
            }
            else {
                state.nullable.sessionPivotValue = null;
                state.nullable.sessionPivotSupport = null;
                state.nullable.sessionPivotResistance = null;
            }
            state.nullable.sessionPivotHigh = null;
            state.nullable.sessionPivotLow = null;
            state.nullable.sessionPivotClose = null;
        }
        if (finite$3(row.high)) {
            state.nullable.sessionPivotHigh = finite$3(state.nullable.sessionPivotHigh)
                ? Math.max(state.nullable.sessionPivotHigh, row.high)
                : row.high;
        }
        if (finite$3(row.low)) {
            state.nullable.sessionPivotLow = finite$3(state.nullable.sessionPivotLow)
                ? Math.min(state.nullable.sessionPivotLow, row.low)
                : row.low;
        }
        state.nullable.sessionPivotClose = row.close;
        state.index += 1;
        return {
            output: {
                value: state.nullable.sessionPivotValue ?? null,
                support: state.nullable.sessionPivotSupport ?? null,
                resistance: state.nullable.sessionPivotResistance ?? null,
            },
            patches: [],
        };
    }
    function technicalIndicatorInputRow(input, index) {
        const clean = (value) => typeof value === 'number' && Number.isFinite(value) ? value : null;
        const value = clean(input.value?.[index] ?? input.close?.[index]);
        return {
            value,
            open: clean(input.open?.[index]),
            high: clean(input.high?.[index]),
            low: clean(input.low?.[index]),
            close: clean(input.close?.[index] ?? value),
            volume: clean(input.volume?.[index]),
        };
    }

    const DEFAULT_TECHNICAL_INDICATOR_MAX_ROWS = 4_096;
    const MAX_TECHNICAL_INDICATOR_ROWS = 100_000;
    const numericRoles = ['value', 'open', 'high', 'low', 'close', 'volume'];
    function resolveMaxRows(value) {
        const resolved = value ?? DEFAULT_TECHNICAL_INDICATOR_MAX_ROWS;
        if (!Number.isInteger(resolved) || resolved < 1 || resolved > MAX_TECHNICAL_INDICATOR_ROWS) {
            throw new GraflumeError('INVALID_SPEC', `maxRows must be an integer between 1 and ${MAX_TECHNICAL_INDICATOR_ROWS}.`, { path: '$.maxRows' });
        }
        return resolved;
    }
    function batchLength(batch) {
        const lengths = [
            ...numericRoles.map((role) => batch[role]?.length),
            batch.session?.length,
            batch.time?.length,
        ].filter((length) => length !== undefined);
        const length = lengths[0] ?? 0;
        if (lengths.some((candidate) => candidate !== length)) {
            throw new GraflumeError('INVALID_SPEC', 'Every technical-indicator append channel must have the same batch length.', { path: '$.append' });
        }
        return length;
    }
    function portableTime(values) {
        return values?.map((value) => (value instanceof Date ? value.toISOString() : value));
    }
    function appendChannel(previous, next, previousLength, appendLength, missing) {
        if (previous === undefined && next === undefined)
            return undefined;
        return [
            ...(previous ?? Array.from({ length: previousLength }).fill(missing)),
            ...(next ?? Array.from({ length: appendLength }).fill(missing)),
        ];
    }
    function appendInput$1(previous, next, previousLength, appendLength) {
        const output = {};
        numericRoles.forEach((role) => {
            const combined = appendChannel(previous[role], next[role], previousLength, appendLength, null);
            if (combined !== undefined)
                output[role] = combined;
        });
        const session = appendChannel(previous.session, next.session, previousLength, appendLength, null);
        if (session !== undefined)
            output.session = session;
        const time = appendChannel(previous.time, portableTime(next.time), previousLength, appendLength, null);
        if (time !== undefined)
            output.time = time;
        return output;
    }
    function assertMatchingSnapshot(spec, snapshot, maxRows) {
        if (snapshot.version !== 1 ||
            snapshot.spec.identifier !== spec.identifier ||
            snapshot.spec.maxRows !== maxRows ||
            JSON.stringify(snapshot.spec.options) !== JSON.stringify(spec.options ?? {})) {
            throw new GraflumeError('INVALID_SPEC', 'The incremental snapshot does not match the requested indicator specification.', { path: '$.previous.spec' });
        }
        if (snapshot.length > maxRows) {
            throw new GraflumeError('INVALID_SPEC', 'The incremental snapshot exceeds maxRows.', {
                path: '$.previous.length',
            });
        }
        if (!Number.isInteger(snapshot.length) || snapshot.length < 0) {
            throw new GraflumeError('INVALID_SPEC', 'The incremental snapshot length is invalid.', {
                path: '$.previous.length',
            });
        }
        const channelLengths = [
            ...numericRoles.map((role) => snapshot.input[role]?.length),
            snapshot.input.session?.length,
            snapshot.input.time?.length,
        ].filter((length) => length !== undefined);
        if (channelLengths.some((length) => length !== snapshot.length)) {
            throw new GraflumeError('INVALID_SPEC', 'Every incremental snapshot channel must match snapshot.length.', { path: '$.previous.input' });
        }
        if (snapshot.runtime !== undefined) {
            const runtime = snapshot.runtime;
            if (runtime.version !== 1 ||
                runtime.engine.version !== 1 ||
                runtime.engine.kind !== runtime.calculation.capability.kind ||
                !Number.isInteger(runtime.segmentStart) ||
                runtime.segmentStart < 0 ||
                runtime.segmentStart > snapshot.length ||
                !Number.isInteger(runtime.totalEvaluatedRows) ||
                runtime.totalEvaluatedRows < snapshot.length) {
                throw new GraflumeError('INVALID_SPEC', 'The incremental runtime snapshot is invalid.', {
                    path: '$.previous.runtime',
                });
            }
            if (runtime.calculation.capability.id !== resolveTechnicalIndicatorCapability(spec.identifier)?.id) {
                throw new GraflumeError('INVALID_SPEC', 'The incremental runtime capability does not match the requested indicator.', { path: '$.previous.runtime.calculation.capability' });
            }
            if (Object.values(runtime.calculation.outputs).some((series) => series.length !== snapshot.length)) {
                throw new GraflumeError('INVALID_SPEC', 'Every cached indicator output must match snapshot.length.', { path: '$.previous.runtime.calculation.outputs' });
            }
        }
    }
    function sessionConfig(calculation, options) {
        const supplied = options.session;
        const record = supplied !== null && typeof supplied === 'object' && !Array.isArray(supplied)
            ? supplied
            : {};
        return {
            mode: calculation.session.mode,
            reset: calculation.session.reset,
            gapMs: typeof record.gapMs === 'number' ? record.gapMs : 0,
        };
    }
    function sessionTimestamp(value, index) {
        const resolved = typeof value === 'number' ? value : Date.parse(value ?? '');
        if (!Number.isFinite(resolved)) {
            throw new GraflumeError('INVALID_SPEC', `Invalid session timestamp at row ${index}.`, {
                path: '$.mark.options.session.timeField',
            });
        }
        return resolved;
    }
    function sameSessionValue(left, right) {
        return Object.is(left, right) || JSON.stringify(left) === JSON.stringify(right);
    }
    function isSessionBoundary(input, index, config) {
        if (config.mode === 'none')
            return false;
        if (index === 0)
            return true;
        if (config.mode === 'field') {
            return !sameSessionValue(input.session?.[index - 1], input.session?.[index]);
        }
        const previous = sessionTimestamp(input.time?.[index - 1], index - 1);
        const current = sessionTimestamp(input.time?.[index], index);
        if (current < previous) {
            throw new GraflumeError('INVALID_SPEC', 'Session timestamps must be ordered from earliest to latest.', { path: '$.mark.options.session.timeField' });
        }
        return config.mode === 'utc-day'
            ? Math.floor(current / 86_400_000) !== Math.floor(previous / 86_400_000)
            : current - previous > config.gapMs;
    }
    function stepRuntime(runtime, calculation, input, index, boundary) {
        const hard = calculation.session.reset === 'hard' && calculation.session.mode !== 'none';
        const sessionPivot = calculation.capability.kind === 'pivotpoints' && hard;
        if (hard && boundary && index > 0 && !sessionPivot) {
            runtime.engine = createTechnicalIndicatorEngine(calculation);
            runtime.segmentStart = index;
        }
        const row = technicalIndicatorInputRow(input, index);
        return sessionPivot
            ? stepTechnicalIndicatorSessionPivot(runtime.engine, row, boundary)
            : stepTechnicalIndicatorEngine(runtime.engine, row, calculation.parameters);
    }
    function replayRuntime(calculation, input, length, options) {
        const runtime = {
            engine: createTechnicalIndicatorEngine(calculation),
            segmentStart: 0,
        };
        const config = sessionConfig(calculation, options);
        for (let index = 0; index < length; index += 1) {
            stepRuntime(runtime, calculation, input, index, isSessionBoundary(input, index, config));
        }
        return runtime;
    }
    function appendOutputSlots(calculation, added) {
        const output = {};
        calculation.capability.outputs.forEach((role) => {
            output[role] = [
                ...(calculation.outputs[role] ?? []),
                ...Array(added).fill(null),
            ];
        });
        Object.entries(calculation.outputs).forEach(([role, values]) => {
            output[role] ??= [...values, ...Array(added).fill(null)];
        });
        return output;
    }
    function appendIncrementally(previous, input, start, added, options) {
        const base = structuredClone(previous);
        const mutable = {
            engine: base.engine,
            segmentStart: base.segmentStart,
        };
        const output = appendOutputSlots(base.calculation, added);
        const boundaries = [...base.calculation.session.boundaries];
        const config = sessionConfig(base.calculation, options);
        const patched = new Set();
        for (let index = start; index < start + added; index += 1) {
            const boundary = isSessionBoundary(input, index, config);
            if (boundary && !boundaries.includes(index))
                boundaries.push(index);
            const step = stepRuntime(mutable, base.calculation, input, index, boundary);
            Object.entries(step.output).forEach(([role, value]) => {
                const series = output[role] ?? (output[role] = Array(start + added).fill(null));
                series[index] = value;
            });
            step.patches.forEach((patch) => {
                const target = mutable.segmentStart + patch.index;
                const series = output[patch.role];
                if (series === undefined || target < 0 || target >= index)
                    return;
                series[target] = patch.value;
                patched.add(target);
            });
        }
        const calculation = {
            ...base.calculation,
            outputs: output,
            warmUpRows: base.calculation.capability.kind === 'pivotpoints' && boundaries.length > 0
                ? (boundaries[1] ?? start + added)
                : base.calculation.warmUpRows,
            session: { ...base.calculation.session, boundaries },
        };
        return {
            runtime: {
                version: 1,
                calculation,
                engine: mutable.engine,
                segmentStart: mutable.segmentStart,
                totalEvaluatedRows: base.totalEvaluatedRows + added,
            },
            patchedPrefixRows: patched.size,
        };
    }
    /**
     * Pure, function-free incremental request suitable for structured cloning and
     * worker execution. State is explicit and bounded by maxRows.
     */
    function calculateTechnicalIndicatorIncremental(request) {
        const maxRows = resolveMaxRows(request.spec.maxRows);
        const options = structuredClone(request.spec.options ?? {});
        const previous = request.previous;
        if (previous !== undefined)
            assertMatchingSnapshot(request.spec, previous, maxRows);
        const previousLength = previous?.length ?? 0;
        const added = batchLength(request.append);
        const capability = resolveTechnicalIndicatorCapability(request.spec.identifier);
        if (added > 0 && capability !== null) {
            for (const role of capability.requiredInputs) {
                if (request.append[role] === undefined) {
                    throw new GraflumeError('INVALID_SPEC', `${capability.id} append requires the ${role} input channel.`, { path: `$.append.${role}` });
                }
            }
        }
        if (previousLength + added > maxRows) {
            throw new GraflumeError('INVALID_SPEC', `Technical-indicator state would exceed the configured maxRows (${maxRows}).`, { path: '$.append' });
        }
        const input = appendInput$1(previous?.input ?? {}, request.append, previousLength, added);
        let runtime;
        let diagnostics;
        if (previous?.runtime !== undefined) {
            const appended = appendIncrementally(previous.runtime, input, previousLength, added, options);
            runtime = appended.runtime;
            diagnostics = {
                strategy: 'incremental',
                evaluatedRows: added,
                recomputedPrefixRows: 0,
                patchedPrefixRows: appended.patchedPrefixRows,
            };
        }
        else {
            // Initial requests and legacy snapshots take one linear bootstrap pass.
            // Every subsequent append resumes from the rolling/cumulative checkpoints.
            const calculation = calculateTechnicalIndicator(request.spec.identifier, input, options);
            const replayed = replayRuntime(calculation, input, previousLength + added, options);
            runtime = {
                version: 1,
                calculation,
                engine: replayed.engine,
                segmentStart: replayed.segmentStart,
                totalEvaluatedRows: previousLength + added,
            };
            diagnostics = {
                strategy: previous === undefined ? 'bootstrap' : 'legacy-restore',
                evaluatedRows: previousLength + added,
                recomputedPrefixRows: previousLength,
                patchedPrefixRows: 0,
            };
        }
        const snapshot = {
            version: 1,
            spec: { identifier: request.spec.identifier, options, maxRows },
            input,
            length: previousLength + added,
            runtime,
        };
        return { snapshot, calculation: runtime.calculation, diagnostics };
    }
    class TechnicalIndicatorIncrementalCalculator {
        #spec;
        #snapshot;
        #calculation = null;
        constructor(spec, snapshot) {
            const maxRows = resolveMaxRows(spec.maxRows);
            this.#spec = {
                identifier: spec.identifier,
                options: structuredClone(spec.options ?? {}),
                maxRows,
            };
            if (snapshot !== undefined) {
                assertMatchingSnapshot(this.#spec, snapshot, maxRows);
                if (snapshot.runtime !== undefined) {
                    this.#snapshot = structuredClone(snapshot);
                    this.#calculation = this.#snapshot.runtime.calculation;
                }
                else {
                    const upgraded = calculateTechnicalIndicatorIncremental({
                        spec: this.#spec,
                        previous: snapshot,
                        append: {},
                    });
                    this.#snapshot = upgraded.snapshot;
                    this.#calculation = upgraded.calculation;
                }
            }
        }
        append(batch) {
            const result = calculateTechnicalIndicatorIncremental({
                spec: this.#spec,
                ...(this.#snapshot === undefined ? {} : { previous: this.#snapshot }),
                append: batch,
            });
            this.#snapshot = result.snapshot;
            this.#calculation = result.calculation;
            return result.calculation;
        }
        reset() {
            this.#snapshot = undefined;
            this.#calculation = null;
        }
        snapshot() {
            return this.#snapshot === undefined ? null : structuredClone(this.#snapshot);
        }
        result() {
            return this.#calculation;
        }
    }

    const workerRuntimeProtocolVersion = 2;
    const identifierPattern = /^[a-zA-Z0-9][a-zA-Z0-9_.:-]{0,95}$/;
    function boundedInteger$4(value, fallback, maximum, path) {
        const resolved = value ?? fallback;
        if (!Number.isInteger(resolved) || resolved < 1 || resolved > maximum) {
            throw new GraflumeError('INVALID_SPEC', `${path} must be an integer from 1 to ${maximum}.`, {
                path,
            });
        }
        return resolved;
    }
    function validateIdentifier(value, path) {
        if (typeof value !== 'string' || !identifierPattern.test(value)) {
            throw new GraflumeError('INVALID_SPEC', `${path} is not a portable adapter identifier.`, {
                path,
            });
        }
        return value;
    }
    function normalizeSpec(spec) {
        if (spec === null || typeof spec !== 'object' || Array.isArray(spec)) {
            throw new GraflumeError('INVALID_SPEC', '$.worker must be an object.');
        }
        const allowed = new Set([
            'moduleURL',
            'name',
            'maxQueueBatches',
            'maxQueueRows',
            'maxInputRows',
            'maxBinaryBytes',
            'maxTransforms',
            'overflow',
            'engine',
        ]);
        const unknown = Object.keys(spec).find((key) => !allowed.has(key));
        if (unknown !== undefined) {
            throw new GraflumeError('INVALID_SPEC', `Unknown worker property "${unknown}".`, {
                path: `$.worker.${unknown}`,
            });
        }
        if (typeof spec.moduleURL !== 'string' ||
            spec.moduleURL.trim() === '' ||
            spec.moduleURL.length > 2_048) {
            throw new GraflumeError('INVALID_SPEC', '$.worker.moduleURL must be a bounded non-empty URL string.');
        }
        if (spec.name !== undefined)
            validateIdentifier(spec.name, '$.worker.name');
        if (spec.overflow !== undefined &&
            !['reject', 'drop-oldest', 'coalesce'].includes(spec.overflow)) {
            throw new GraflumeError('INVALID_SPEC', '$.worker.overflow is unsupported.');
        }
        if (spec.engine !== undefined) {
            if (spec.engine === null || typeof spec.engine !== 'object' || Array.isArray(spec.engine)) {
                throw new GraflumeError('INVALID_SPEC', '$.worker.engine must be an object.');
            }
            const engineUnknown = Object.keys(spec.engine).find((key) => !['type', 'adapter'].includes(key));
            if (engineUnknown !== undefined) {
                throw new GraflumeError('INVALID_SPEC', `Unknown worker engine property "${engineUnknown}".`, { path: `$.worker.engine.${engineUnknown}` });
            }
            if (spec.engine.type === 'wasm')
                validateIdentifier(spec.engine.adapter, '$.worker.engine.adapter');
            else if (spec.engine.type !== 'javascript' || 'adapter' in spec.engine) {
                throw new GraflumeError('INVALID_SPEC', '$.worker.engine must be {type:"javascript"} or {type:"wasm",adapter}.');
            }
        }
        const maxInputRows = boundedInteger$4(spec.maxInputRows, 100_000, 1_000_000, '$.worker.maxInputRows');
        return {
            moduleURL: spec.moduleURL,
            ...(spec.name === undefined ? {} : { name: spec.name }),
            maxQueueBatches: boundedInteger$4(spec.maxQueueBatches, 8, 1_024, '$.worker.maxQueueBatches'),
            maxQueueRows: boundedInteger$4(spec.maxQueueRows, maxInputRows, 1_000_000, '$.worker.maxQueueRows'),
            maxInputRows,
            maxBinaryBytes: boundedInteger$4(spec.maxBinaryBytes, 64 * 1024 * 1024, 256 * 1024 * 1024, '$.worker.maxBinaryBytes'),
            maxTransforms: boundedInteger$4(spec.maxTransforms, 128, 128, '$.worker.maxTransforms'),
            overflow: spec.overflow ?? 'reject',
            ...(spec.engine === undefined ? {} : { engine: { ...spec.engine } }),
        };
    }
    function runtimeRows(data) {
        if (data === null || typeof data !== 'object') {
            throw new GraflumeError('INVALID_DATA', 'Worker data envelope must be an object.');
        }
        if (data.kind === 'rows') {
            if (!Array.isArray(data.rows)) {
                throw new GraflumeError('INVALID_DATA', 'Worker row data must be an array.');
            }
            return data.rows.length;
        }
        if (data.kind === 'binary') {
            if (!Number.isInteger(data.rowCount) || data.rowCount < 0) {
                throw new GraflumeError('INVALID_DATA', 'Worker binary rowCount must be non-negative.');
            }
            return data.rowCount;
        }
        if (data.kind === 'columns')
            return DataTable.from(data.columns).length;
        throw new GraflumeError('INVALID_DATA', 'Worker data envelope kind is unsupported.');
    }
    function dataEnvelope(input) {
        return Array.isArray(input)
            ? { kind: 'rows', rows: input }
            : { kind: 'columns', columns: input };
    }
    function indicatorRequestRows(request) {
        const lengths = [
            request.append.value?.length,
            request.append.open?.length,
            request.append.high?.length,
            request.append.low?.length,
            request.append.close?.length,
            request.append.volume?.length,
            request.append.session?.length,
            request.append.time?.length,
        ].filter((length) => length !== undefined);
        const appended = lengths[0] ?? 0;
        if (lengths.some((length) => length !== appended)) {
            throw new GraflumeError('INVALID_DATA', 'Worker technical-indicator append channels must have equal lengths.');
        }
        return (request.previous?.length ?? 0) + appended;
    }
    function incrementalStackRequestRows(request) {
        // Both snapshots describe one bounded logical dataset. Queue accounting uses
        // the larger retained view rather than double-counting unchanged identities.
        return Math.max(request.input.length, request.previous?.input.length ?? 0);
    }
    function barVirtualizationRequestRows(request) {
        const incoming = request.action.type === 'replace' ||
            request.action.type === 'append' ||
            request.action.type === 'upsert'
            ? request.action.rows.length
            : 0;
        return Math.max(request.previous?.retained.length ?? 0, incoming);
    }
    function closedRuntimeObject(value, allowed, path) {
        if (value === null || typeof value !== 'object' || Array.isArray(value)) {
            throw new GraflumeError('INVALID_DATA', `${path} must be an object.`, { path });
        }
        const record = value;
        const unknown = Object.keys(record).find((key) => !allowed.has(key));
        if (unknown !== undefined) {
            throw new GraflumeError('INVALID_DATA', `Unknown ${path} property "${unknown}".`, {
                path: `${path}.${unknown}`,
            });
        }
        return record;
    }
    function includeReplayValue(value) {
        if (value !== undefined && typeof value !== 'boolean') {
            throw new GraflumeError('INVALID_DATA', 'includeReplay must be boolean.', {
                path: '$.operation.request.action.includeReplay',
            });
        }
        return value === true;
    }
    function validateStreamRetentionRequest(request) {
        const envelope = closedRuntimeObject(request, new Set(['streamId', 'action']), '$.operation.request');
        validateIdentifier(envelope.streamId, '$.operation.request.streamId');
        const action = closedRuntimeObject(envelope.action, new Set(['type', 'data', 'streaming', 'replay', 'update', 'includeReplay']), '$.operation.request.action');
        const actionType = action.type;
        if (actionType === 'initialize') {
            closedRuntimeObject(action, new Set(['type', 'data', 'streaming', 'includeReplay']), '$.operation.request.action');
            includeReplayValue(action.includeReplay);
            if (action.streaming === null || typeof action.streaming !== 'object') {
                throw new GraflumeError('INVALID_DATA', 'Worker streaming contract must be an object.');
            }
            return runtimeRows(action.data);
        }
        if (actionType === 'restore') {
            closedRuntimeObject(action, new Set(['type', 'replay', 'includeReplay']), '$.operation.request.action');
            includeReplayValue(action.includeReplay);
            const replay = closedRuntimeObject(action.replay, new Set(['version', 'options', 'initial', 'updates']), '$.operation.request.action.replay');
            if (!Array.isArray(replay.initial) || !Array.isArray(replay.updates)) {
                throw new GraflumeError('INVALID_DATA', 'Worker stream replay arrays are required.');
            }
            return (replay.initial.length +
                replay.updates.reduce((total, update, index) => {
                    const item = closedRuntimeObject(update, new Set(['mode', 'rows', 'watermark']), `$.operation.request.action.replay.updates[${index}]`);
                    if (!Array.isArray(item.rows)) {
                        throw new GraflumeError('INVALID_DATA', 'Worker stream replay rows must be arrays.');
                    }
                    return total + item.rows.length;
                }, 0));
        }
        if (actionType === 'update') {
            closedRuntimeObject(action, new Set(['type', 'update', 'includeReplay']), '$.operation.request.action');
            includeReplayValue(action.includeReplay);
            const update = closedRuntimeObject(action.update, new Set(['mode', 'rows', 'watermark']), '$.operation.request.action.update');
            if (!Array.isArray(update.rows)) {
                throw new GraflumeError('INVALID_DATA', 'Worker stream update rows must be an array.');
            }
            return update.rows.length;
        }
        if (actionType === 'snapshot') {
            closedRuntimeObject(action, new Set(['type', 'includeReplay']), '$.operation.request.action');
            includeReplayValue(action.includeReplay);
            return 0;
        }
        if (actionType === 'close') {
            closedRuntimeObject(action, new Set(['type']), '$.operation.request.action');
            return 0;
        }
        throw new GraflumeError('INVALID_DATA', 'Worker stream retention action is unsupported.', {
            path: '$.operation.request.action.type',
        });
    }
    function streamRetentionData(operation) {
        return operation.request.action.type === 'initialize' ? operation.request.action.data : undefined;
    }
    function transferables$1(data, operation) {
        const output = new Set();
        if (data.kind === 'binary')
            output.add(data.buffer);
        if (data.kind === 'columns') {
            for (const column of Object.values(data.columns.columns)) {
                if (ArrayBuffer.isView(column) && column.buffer instanceof ArrayBuffer)
                    output.add(column.buffer);
            }
        }
        if (operation !== undefined &&
            typeof OffscreenCanvas === 'function' &&
            operation.canvas instanceof OffscreenCanvas) {
            output.add(operation.canvas);
        }
        return [...output];
    }
    function snapshotOperation(operation, transferOwnership) {
        const sourceTransfers = operation.kind === 'transform'
            ? transferables$1(operation.data)
            : operation.kind === 'render'
                ? transferables$1({ kind: 'rows'}, operation)
                : operation.kind === 'stream-retention'
                    ? (() => {
                        const data = streamRetentionData(operation);
                        return data === undefined ? [] : transferables$1(data);
                    })()
                    : [];
        try {
            const transfer = operation.kind === 'render' || transferOwnership;
            const snapshot = structuredClone(operation, transfer && sourceTransfers.length > 0 ? { transfer: sourceTransfers } : undefined);
            return {
                operation: snapshot,
                transfer: operation.kind === 'render' || transferOwnership
                    ? snapshot.kind === 'render'
                        ? transferables$1({ kind: 'rows', rows: [] }, snapshot)
                        : snapshot.kind === 'transform'
                            ? transferables$1(snapshot.data)
                            : snapshot.kind === 'stream-retention'
                                ? (() => {
                                    const data = streamRetentionData(snapshot);
                                    return data === undefined ? [] : transferables$1(data);
                                })()
                                : []
                    : [],
            };
        }
        catch (error) {
            throw new GraflumeError('INVALID_DATA', 'Worker operations must be structured-clone portable; functions are unsupported.', { cause: error });
        }
    }
    function abortedError() {
        return new DOMException('The Worker operation was cancelled.', 'AbortError');
    }
    function droppedError() {
        return new GraflumeError('INVALID_DATA', 'Worker request was dropped by explicit overflow policy.');
    }
    function response(value) {
        if (value === null || typeof value !== 'object')
            return false;
        const item = value;
        return (item.protocol === workerRuntimeProtocolVersion &&
            item.type === 'graflume:worker-result' &&
            typeof item.id === 'number' &&
            typeof item.ok === 'boolean');
    }
    function request(value) {
        if (value === null || typeof value !== 'object')
            return false;
        const item = value;
        return (item.protocol === workerRuntimeProtocolVersion &&
            typeof item.id === 'number' &&
            (item.type === 'graflume:worker-execute' || item.type === 'graflume:worker-cancel'));
    }
    function serializedError$1(error) {
        if (error instanceof GraflumeError) {
            return {
                name: error.name,
                message: error.message,
                code: error.code,
                ...(error.path === undefined ? {} : { path: error.path }),
            };
        }
        return {
            name: error instanceof Error ? error.name : 'Error',
            message: error instanceof Error ? error.message : String(error),
        };
    }
    function defaultWorkerFactory() {
        return {
            create(moduleURL, options) {
                if (typeof Worker !== 'function') {
                    throw new GraflumeError('UNSUPPORTED_RENDERER', 'This environment does not expose Worker; inject a WorkerRuntimeFactory.');
                }
                return new Worker(moduleURL, options);
            },
        };
    }
    /** Client with automatic module Worker construction, cancellation and explicit overflow. */
    class AutomaticWorkerRuntime {
        #port;
        #spec;
        #queue = [];
        #queuedRows = 0;
        #active = null;
        #nextId = 1;
        #completed = 0;
        #rejected = 0;
        #dropped = 0;
        #coalesced = 0;
        #cancelled = 0;
        #closed = false;
        #listener = (event) => {
            if (!response(event.data) || this.#active === null)
                return;
            if (event.data.id !== this.#active.request.id)
                return;
            const active = this.#active;
            this.#active = null;
            if (event.data.ok) {
                this.#completed += 1;
                this.#settle(active, 'resolve', event.data.result);
            }
            else {
                this.#rejected += active.subscribers.length;
                this.#settle(active, 'reject', event.data.error.name === 'AbortError'
                    ? abortedError()
                    : new GraflumeError('INVALID_DATA', event.data.error.message, {
                        ...(event.data.error.path === undefined ? {} : { path: event.data.error.path }),
                        details: { workerCode: event.data.error.code ?? event.data.error.name },
                    }));
            }
            this.#dispatch();
        };
        constructor(spec, factory = defaultWorkerFactory()) {
            this.#spec = normalizeSpec(spec);
            this.#port = factory.create(this.#spec.moduleURL, {
                type: 'module',
                ...(this.#spec.name === undefined ? {} : { name: this.#spec.name }),
            });
            this.#port.addEventListener('message', this.#listener);
        }
        transform(input, transforms, options = {}) {
            return this.execute({
                kind: 'transform',
                data: dataEnvelope(input),
                transforms,
                ...(this.#spec.engine === undefined ? {} : { engine: this.#spec.engine }),
            }, options).then((result) => {
                if (result.kind !== 'transform')
                    throw new GraflumeError('INVALID_DATA', 'Worker result kind mismatch.');
                return result.result;
            });
        }
        technicalIndicator(request, options = {}) {
            return this.execute({ kind: 'technical-indicator', request }, options).then((result) => {
                if (result.kind !== 'technical-indicator') {
                    throw new GraflumeError('INVALID_DATA', 'Worker result kind mismatch.');
                }
                return result.result;
            });
        }
        incrementalStack(request, options = {}) {
            return this.execute({ kind: 'incremental-stack', request }, options).then((result) => {
                if (result.kind !== 'incremental-stack') {
                    throw new GraflumeError('INVALID_DATA', 'Worker result kind mismatch.');
                }
                return result.result;
            });
        }
        barVirtualization(request, options = {}) {
            return this.execute({ kind: 'bar-virtualization', request }, options).then((result) => {
                if (result.kind !== 'bar-virtualization') {
                    throw new GraflumeError('INVALID_DATA', 'Worker result kind mismatch.');
                }
                return result.result;
            });
        }
        /** Executes one mutation or snapshot against a Worker-owned bounded streaming session. */
        streamRetention(request, options = {}) {
            return this.execute({ kind: 'stream-retention', request }, options).then((result) => {
                if (result.kind !== 'stream-retention') {
                    throw new GraflumeError('INVALID_DATA', 'Worker result kind mismatch.');
                }
                return result.result;
            });
        }
        transformBinary(data, transforms, engine = this.#spec.engine, options = {}) {
            return this.execute({
                kind: 'transform',
                data,
                transforms,
                ...(engine === undefined ? {} : { engine }),
            }, options).then((result) => {
                if (result.kind !== 'transform')
                    throw new GraflumeError('INVALID_DATA', 'Worker result kind mismatch.');
                return result.result;
            });
        }
        render(operation, options = {}) {
            return this.execute({ kind: 'render', ...operation }, options).then((result) => {
                if (result.kind !== 'render')
                    throw new GraflumeError('INVALID_DATA', 'Worker result kind mismatch.');
                return result.result;
            });
        }
        execute(operation, options = {}) {
            if (this.#closed)
                return Promise.reject(new GraflumeError('INVALID_DATA', 'Worker runtime is closed.'));
            if (options.signal?.aborted === true)
                return Promise.reject(abortedError());
            let snapshot;
            let rows = 0;
            try {
                if (options.coalesceKey !== undefined)
                    validateIdentifier(options.coalesceKey, '$.worker.coalesceKey');
                if (operation.kind === 'transform') {
                    rows = runtimeRows(operation.data);
                    if (rows > this.#spec.maxInputRows) {
                        throw new GraflumeError('INVALID_DATA', `Worker input has ${rows} rows; the deterministic limit is ${this.#spec.maxInputRows}.`);
                    }
                    if (operation.transforms.length > this.#spec.maxTransforms) {
                        throw new GraflumeError('INVALID_DATA', `Worker request has ${operation.transforms.length} transforms; the deterministic limit is ${this.#spec.maxTransforms}.`);
                    }
                    if (operation.data.kind === 'binary' &&
                        operation.data.buffer.byteLength > this.#spec.maxBinaryBytes) {
                        throw new GraflumeError('INVALID_DATA', 'Worker binary input exceeds the byte budget.');
                    }
                }
                else if (operation.kind === 'render') {
                    if (![operation.width, operation.height, operation.pixelRatio].every(Number.isFinite)) {
                        throw new GraflumeError('INVALID_DATA', 'Worker render viewport must be finite.');
                    }
                }
                else if (operation.kind === 'technical-indicator') {
                    rows = indicatorRequestRows(operation.request);
                    if (rows > this.#spec.maxInputRows) {
                        throw new GraflumeError('INVALID_DATA', `Worker technical-indicator state has ${rows} rows; the deterministic limit is ${this.#spec.maxInputRows}.`);
                    }
                }
                else if (operation.kind === 'incremental-stack') {
                    rows = incrementalStackRequestRows(operation.request);
                    if (rows > this.#spec.maxInputRows) {
                        throw new GraflumeError('INVALID_DATA', `Worker incremental-stack state has ${rows} rows; the deterministic limit is ${this.#spec.maxInputRows}.`);
                    }
                }
                else if (operation.kind === 'bar-virtualization') {
                    rows = barVirtualizationRequestRows(operation.request);
                    if (rows > this.#spec.maxInputRows) {
                        throw new GraflumeError('INVALID_DATA', `Worker bar-virtualization state has ${rows} rows; the deterministic limit is ${this.#spec.maxInputRows}.`);
                    }
                }
                else {
                    rows = validateStreamRetentionRequest(operation.request);
                    if (rows > this.#spec.maxInputRows) {
                        throw new GraflumeError('INVALID_DATA', `Worker stream-retention request has ${rows} rows; the deterministic limit is ${this.#spec.maxInputRows}.`);
                    }
                }
                snapshot = snapshotOperation(operation, options.transferOwnership === true);
            }
            catch (error) {
                this.#rejected += 1;
                return Promise.reject(error);
            }
            const pending = {
                request: {
                    protocol: workerRuntimeProtocolVersion,
                    type: 'graflume:worker-execute',
                    id: this.#nextId,
                    operation: snapshot.operation,
                },
                rows,
                transfer: snapshot.transfer,
                ...(options.coalesceKey === undefined ? {} : { coalesceKey: options.coalesceKey }),
            };
            this.#nextId += 1;
            return new Promise((resolve, reject) => {
                const subscriber = {
                    resolve,
                    reject,
                    ...(options.signal === undefined ? {} : { signal: options.signal }),
                };
                if (options.signal !== undefined) {
                    subscriber.abort = () => this.#cancelSubscriber(subscriber);
                    options.signal.addEventListener('abort', subscriber.abort, { once: true });
                }
                if (!this.#admit({ ...pending, subscribers: [subscriber] }))
                    return;
                this.#dispatch();
            });
        }
        state() {
            return {
                protocol: workerRuntimeProtocolVersion,
                queuedBatches: this.#queue.length,
                queuedRows: this.#queuedRows,
                inFlight: this.#active !== null,
                completed: this.#completed,
                rejected: this.#rejected,
                dropped: this.#dropped,
                coalesced: this.#coalesced,
                cancelled: this.#cancelled,
                closed: this.#closed,
            };
        }
        close(options = {}) {
            if (this.#closed)
                return;
            this.#closed = true;
            this.#port.removeEventListener('message', this.#listener);
            const error = new GraflumeError('INVALID_DATA', 'Worker runtime closed.');
            const rejected = (this.#active?.subscribers.length ?? 0) +
                this.#queue.reduce((count, pending) => count + pending.subscribers.length, 0);
            if (this.#active !== null) {
                this.#port.postMessage({
                    protocol: workerRuntimeProtocolVersion,
                    type: 'graflume:worker-cancel',
                    id: this.#active.request.id,
                });
                this.#settle(this.#active, 'reject', error);
            }
            for (const pending of this.#queue)
                this.#settle(pending, 'reject', error);
            this.#rejected += rejected;
            this.#active = null;
            this.#queue = [];
            this.#queuedRows = 0;
            if (options.terminate === true)
                this.#port.terminate?.();
        }
        #admit(pending) {
            const over = () => this.#queue.length + (this.#active === null ? 0 : 1) >= this.#spec.maxQueueBatches ||
                this.#queuedRows + (this.#active?.rows ?? 0) + pending.rows > this.#spec.maxQueueRows;
            if (over() && this.#spec.overflow === 'coalesce' && pending.coalesceKey !== undefined) {
                const existing = [...this.#queue]
                    .reverse()
                    .find(({ coalesceKey }) => coalesceKey === pending.coalesceKey);
                if (existing !== undefined) {
                    const nextQueuedRows = this.#queuedRows - existing.rows + pending.rows;
                    if (nextQueuedRows + (this.#active?.rows ?? 0) > this.#spec.maxQueueRows) {
                        this.#rejected += 1;
                        this.#settle(pending, 'reject', new GraflumeError('INVALID_DATA', 'Worker coalesced request exceeds the row budget.'));
                        return false;
                    }
                    this.#queuedRows += pending.rows - existing.rows;
                    existing.request = pending.request;
                    existing.rows = pending.rows;
                    existing.transfer = pending.transfer;
                    existing.subscribers.push(...pending.subscribers);
                    this.#coalesced += 1;
                    return true;
                }
            }
            if (over() && this.#spec.overflow === 'drop-oldest') {
                while (over() && this.#queue.length > 0) {
                    const dropped = this.#queue.shift();
                    this.#queuedRows -= dropped.rows;
                    this.#dropped += 1;
                    this.#settle(dropped, 'reject', droppedError());
                }
            }
            if (over()) {
                this.#rejected += 1;
                this.#settle(pending, 'reject', new GraflumeError('INVALID_DATA', 'Worker queue backpressure limit reached.'));
                return false;
            }
            this.#queue.push(pending);
            this.#queuedRows += pending.rows;
            return true;
        }
        #dispatch() {
            if (this.#closed || this.#active !== null)
                return;
            const pending = this.#queue.shift();
            if (pending === undefined)
                return;
            this.#queuedRows -= pending.rows;
            this.#active = pending;
            try {
                this.#port.postMessage(pending.request, pending.transfer);
            }
            catch (error) {
                this.#active = null;
                this.#rejected += pending.subscribers.length;
                this.#settle(pending, 'reject', error);
                this.#dispatch();
            }
        }
        #cancelSubscriber(subscriber) {
            const queued = this.#queue.find((pending) => pending.subscribers.includes(subscriber));
            if (queued !== undefined) {
                queued.subscribers.splice(queued.subscribers.indexOf(subscriber), 1);
                subscriber.signal?.removeEventListener('abort', subscriber.abort);
                subscriber.reject(abortedError());
                this.#cancelled += 1;
                if (queued.subscribers.length === 0) {
                    this.#queue = this.#queue.filter((pending) => pending !== queued);
                    this.#queuedRows -= queued.rows;
                }
                return;
            }
            if (this.#active?.subscribers.includes(subscriber) === true) {
                const active = this.#active;
                // One operation may have coalesced subscribers. Cancel the worker only
                // after all consumers have cancelled.
                active.subscribers.splice(active.subscribers.indexOf(subscriber), 1);
                subscriber.signal?.removeEventListener('abort', subscriber.abort);
                subscriber.reject(abortedError());
                this.#cancelled += 1;
                if (active.subscribers.length === 0) {
                    this.#port.postMessage({
                        protocol: workerRuntimeProtocolVersion,
                        type: 'graflume:worker-cancel',
                        id: active.request.id,
                    });
                    this.#active = null;
                    this.#dispatch();
                }
            }
        }
        #settle(pending, action, value) {
            for (const subscriber of pending.subscribers) {
                if (subscriber.abort !== undefined)
                    subscriber.signal?.removeEventListener('abort', subscriber.abort);
                if (action === 'resolve')
                    subscriber.resolve(value);
                else
                    subscriber.reject(value);
            }
            pending.subscribers.splice(0);
        }
    }
    function createAutomaticWorkerRuntime(spec, factory) {
        return new AutomaticWorkerRuntime(spec, factory);
    }
    /** Installs protocol v2 inside a WorkerGlobalScope-compatible injected scope. */
    function installWorkerRuntime(scope, options = {}) {
        const maxInputRows = boundedInteger$4(options.maxInputRows, 100_000, 1_000_000, '$.worker.maxInputRows');
        const maxBinaryBytes = boundedInteger$4(options.maxBinaryBytes, 64 * 1024 * 1024, 256 * 1024 * 1024, '$.worker.maxBinaryBytes');
        const maxTransforms = boundedInteger$4(options.maxTransforms, 128, 128, '$.worker.maxTransforms');
        const maxStreamingSessions = boundedInteger$4(options.maxStreamingSessions, 64, 1_024, '$.worker.maxStreamingSessions');
        const maxStreamingReplayBatches = boundedInteger$4(options.maxStreamingReplayBatches, 256, 4_096, '$.worker.maxStreamingReplayBatches');
        const maxStreamingReplayRows = boundedInteger$4(options.maxStreamingReplayRows, maxInputRows, 1_000_000, '$.worker.maxStreamingReplayRows');
        const binaries = new Map((options.binaryAdapters ?? []).map((adapter) => [
            validateIdentifier(adapter.id, '$.worker.binaryAdapter.id'),
            adapter,
        ]));
        const wasm = new Map((options.wasmAdapters ?? []).map((adapter) => [
            validateIdentifier(adapter.id, '$.worker.wasmAdapter.id'),
            adapter,
        ]));
        const renderers = new Map((options.renderers ?? []).map((renderer) => [
            validateIdentifier(renderer.id, '$.worker.renderer.id'),
            renderer,
        ]));
        const active = new Map();
        const streams = new Map();
        const decodeData = async (data, signal) => {
            let input;
            if (data.kind === 'rows')
                input = data.rows;
            else if (data.kind === 'columns')
                input = data.columns;
            else {
                if (data.buffer.byteLength > maxBinaryBytes) {
                    throw new GraflumeError('INVALID_DATA', 'Worker binary input exceeds the byte budget.');
                }
                const adapter = binaries.get(data.adapter);
                if (adapter === undefined) {
                    throw new GraflumeError('INVALID_DATA', `Binary adapter "${data.adapter}" is not registered.`);
                }
                if (!adapter.formats.includes(data.format)) {
                    throw new GraflumeError('INVALID_DATA', 'Binary adapter does not accept this format.');
                }
                input = await adapter.decode(data, signal);
            }
            if (signal.aborted)
                throw abortedError();
            if (DataTable.from(input).length > maxInputRows) {
                throw new GraflumeError('INVALID_DATA', 'Decoded Worker input exceeds the row budget.');
            }
            return input;
        };
        const assertStreamContractBudget = (streaming) => {
            const maxRows = streaming.retention?.maxRows ?? 100_000;
            if (typeof maxRows === 'number' && maxRows > maxInputRows) {
                throw new GraflumeError('INVALID_DATA', `Worker stream retention maxRows exceeds the installed ${maxInputRows}-row budget.`, { path: '$.streaming.retention.maxRows' });
            }
        };
        const streamSnapshot = (streamId, store, includeReplay) => {
            const state = store.state();
            if (!includeReplay) {
                return {
                    version: 1,
                    streamId,
                    rows: store.rows(),
                    state,
                    replayStatus: 'omitted',
                };
            }
            if (state.replayTruncated) {
                return {
                    version: 1,
                    streamId,
                    rows: store.rows(),
                    state,
                    replayStatus: 'truncated',
                };
            }
            return {
                version: 1,
                streamId,
                rows: store.rows(),
                state,
                replayStatus: 'available',
                replay: store.exportReplay(),
            };
        };
        const listener = (event) => {
            if (!request(event.data))
                return;
            if (event.data.type === 'graflume:worker-cancel') {
                active.get(event.data.id)?.abort();
                return;
            }
            const message = event.data;
            const controller = new AbortController();
            active.set(message.id, controller);
            void (async () => {
                let output;
                try {
                    const operation = message.operation;
                    let result;
                    if (operation.kind === 'transform') {
                        if (operation.transforms.length > maxTransforms) {
                            throw new GraflumeError('INVALID_DATA', 'Worker transform count exceeds the deterministic limit.');
                        }
                        const input = await decodeData(operation.data, controller.signal);
                        const engine = operation.engine ?? { type: 'javascript' };
                        const transformed = engine.type === 'javascript'
                            ? executeTransforms(input, operation.transforms)
                            : await (() => {
                                const adapter = wasm.get(engine.adapter);
                                if (adapter === undefined) {
                                    throw new GraflumeError('INVALID_DATA', `WASM adapter "${engine.adapter}" is not registered.`);
                                }
                                return adapter.execute(input, operation.transforms, controller.signal);
                            })();
                        if (controller.signal.aborted)
                            throw abortedError();
                        result = { kind: 'transform', result: transformed };
                    }
                    else if (operation.kind === 'render') {
                        const renderer = renderers.get(operation.renderer);
                        if (renderer === undefined) {
                            throw new GraflumeError('UNSUPPORTED_RENDERER', `Worker renderer "${operation.renderer}" is not registered.`);
                        }
                        if (![operation.width, operation.height, operation.pixelRatio].every(Number.isFinite) ||
                            operation.width < 1 ||
                            operation.height < 1 ||
                            operation.pixelRatio <= 0) {
                            throw new GraflumeError('INVALID_DATA', 'Worker render viewport is invalid.');
                        }
                        await renderer.render(operation.scene, operation.canvas, { width: operation.width, height: operation.height, pixelRatio: operation.pixelRatio }, controller.signal);
                        if (controller.signal.aborted)
                            throw abortedError();
                        result = {
                            kind: 'render',
                            result: {
                                renderer: renderer.id,
                                width: operation.width,
                                height: operation.height,
                                pixelRatio: operation.pixelRatio,
                            },
                        };
                    }
                    else if (operation.kind === 'technical-indicator') {
                        const rows = indicatorRequestRows(operation.request);
                        if (rows > maxInputRows) {
                            throw new GraflumeError('INVALID_DATA', 'Worker technical-indicator state exceeds the row budget.');
                        }
                        if (controller.signal.aborted)
                            throw abortedError();
                        const calculated = calculateTechnicalIndicatorIncremental(operation.request);
                        if (calculated.snapshot.length > maxInputRows) {
                            throw new GraflumeError('INVALID_DATA', 'Calculated technical-indicator state exceeds the row budget.');
                        }
                        if (controller.signal.aborted)
                            throw abortedError();
                        result = { kind: 'technical-indicator', result: calculated };
                    }
                    else if (operation.kind === 'incremental-stack') {
                        const rows = incrementalStackRequestRows(operation.request);
                        if (rows > maxInputRows) {
                            throw new GraflumeError('INVALID_DATA', 'Worker incremental-stack state exceeds the row budget.');
                        }
                        if (controller.signal.aborted)
                            throw abortedError();
                        const calculated = executePortableIncrementalStack(operation.request);
                        if (controller.signal.aborted)
                            throw abortedError();
                        result = { kind: 'incremental-stack', result: calculated };
                    }
                    else if (operation.kind === 'bar-virtualization') {
                        const rows = barVirtualizationRequestRows(operation.request);
                        if (rows > maxInputRows) {
                            throw new GraflumeError('INVALID_DATA', 'Worker bar-virtualization state exceeds the row budget.');
                        }
                        if (controller.signal.aborted)
                            throw abortedError();
                        const calculated = executePortableBarVirtualization(operation.request);
                        if (controller.signal.aborted)
                            throw abortedError();
                        result = { kind: 'bar-virtualization', result: calculated };
                    }
                    else {
                        const requestRows = validateStreamRetentionRequest(operation.request);
                        const { streamId, action } = operation.request;
                        if (action.type === 'restore') {
                            if (action.replay.updates.length > maxStreamingReplayBatches) {
                                throw new GraflumeError('INVALID_DATA', 'Worker stream replay exceeds the bounded batch budget.');
                            }
                            if (requestRows > maxStreamingReplayRows) {
                                throw new GraflumeError('INVALID_DATA', 'Worker stream replay exceeds the bounded row budget.');
                            }
                        }
                        else if (requestRows > maxInputRows) {
                            throw new GraflumeError('INVALID_DATA', 'Worker stream-retention request exceeds the row budget.');
                        }
                        if (controller.signal.aborted)
                            throw abortedError();
                        if (action.type === 'initialize') {
                            if (streams.has(streamId)) {
                                throw new GraflumeError('INVALID_DATA', `Worker stream "${streamId}" is already initialized.`);
                            }
                            if (streams.size >= maxStreamingSessions) {
                                throw new GraflumeError('INVALID_DATA', 'Worker streaming session budget is exhausted.');
                            }
                            assertStreamContractBudget(action.streaming);
                            const input = await decodeData(action.data, controller.signal);
                            if (controller.signal.aborted)
                                throw abortedError();
                            const store = new IncrementalDataStore(input, action.streaming);
                            streams.set(streamId, store);
                            result = {
                                kind: 'stream-retention',
                                result: {
                                    action: 'initialize',
                                    snapshot: streamSnapshot(streamId, store, includeReplayValue(action.includeReplay)),
                                },
                            };
                        }
                        else if (action.type === 'restore') {
                            if (streams.has(streamId)) {
                                throw new GraflumeError('INVALID_DATA', `Worker stream "${streamId}" is already initialized.`);
                            }
                            if (streams.size >= maxStreamingSessions) {
                                throw new GraflumeError('INVALID_DATA', 'Worker streaming session budget is exhausted.');
                            }
                            assertStreamContractBudget(action.replay.options);
                            const store = IncrementalDataStore.replay(action.replay);
                            if (controller.signal.aborted)
                                throw abortedError();
                            streams.set(streamId, store);
                            result = {
                                kind: 'stream-retention',
                                result: {
                                    action: 'restore',
                                    snapshot: streamSnapshot(streamId, store, includeReplayValue(action.includeReplay)),
                                },
                            };
                        }
                        else {
                            const store = streams.get(streamId);
                            if (store === undefined) {
                                throw new GraflumeError('INVALID_DATA', `Worker stream "${streamId}" is not initialized.`);
                            }
                            if (action.type === 'update') {
                                const updated = store.apply(action.update);
                                result = {
                                    kind: 'stream-retention',
                                    result: {
                                        action: 'update',
                                        step: updated.step,
                                        snapshot: streamSnapshot(streamId, store, includeReplayValue(action.includeReplay)),
                                    },
                                };
                            }
                            else if (action.type === 'snapshot') {
                                result = {
                                    kind: 'stream-retention',
                                    result: {
                                        action: 'snapshot',
                                        snapshot: streamSnapshot(streamId, store, includeReplayValue(action.includeReplay)),
                                    },
                                };
                            }
                            else {
                                streams.delete(streamId);
                                result = {
                                    kind: 'stream-retention',
                                    result: { action: 'close', streamId, closed: true },
                                };
                            }
                        }
                    }
                    output = {
                        protocol: workerRuntimeProtocolVersion,
                        type: 'graflume:worker-result',
                        id: message.id,
                        ok: true,
                        result,
                    };
                }
                catch (error) {
                    output = {
                        protocol: workerRuntimeProtocolVersion,
                        type: 'graflume:worker-result',
                        id: message.id,
                        ok: false,
                        error: serializedError$1(error),
                    };
                }
                finally {
                    active.delete(message.id);
                }
                scope.postMessage(output);
            })();
        };
        scope.addEventListener('message', listener);
        return () => {
            scope.removeEventListener('message', listener);
            for (const controller of active.values())
                controller.abort();
            active.clear();
            streams.clear();
        };
    }

    function samePoint(left, right) {
        return Math.abs(left.x - right.x) < 1e-9 && Math.abs(left.y - right.y) < 1e-9;
    }
    function defaultOrigin(context) {
        return Object.freeze({
            x: context.plot.x + context.plot.width / 2,
            y: context.plot.y + context.plot.height / 2,
        });
    }
    function startAnalyticKeyboardGesture(context, kind, origin = defaultOrigin(context)) {
        const point = clampPixelToPlot(context, origin);
        return Object.freeze({ kind, start: point, current: point, points: Object.freeze([point]) });
    }
    function moveAnalyticKeyboardGesture(context, gesture, direction, step) {
        const amount = Number.isFinite(step) && step > 0 ? step : 1;
        const delta = direction === 'left'
            ? { x: -amount, y: 0 }
            : direction === 'right'
                ? { x: amount, y: 0 }
                : direction === 'up'
                    ? { x: 0, y: -amount }
                    : { x: 0, y: amount };
        const current = clampPixelToPlot(context, {
            x: gesture.current.x + delta.x,
            y: gesture.current.y + delta.y,
        });
        return Object.freeze({ ...gesture, current });
    }
    function addAnalyticKeyboardVertex(gesture, maximumPoints) {
        if (gesture.kind !== 'lasso')
            return gesture;
        const last = gesture.points.at(-1);
        if (last !== undefined && samePoint(last, gesture.current))
            return gesture;
        if (gesture.points.length >= maximumPoints)
            return gesture;
        return Object.freeze({
            ...gesture,
            points: Object.freeze([...gesture.points, gesture.current]),
        });
    }
    function lassoPoints(gesture) {
        const last = gesture.points.at(-1);
        return last !== undefined && samePoint(last, gesture.current)
            ? gesture.points
            : [...gesture.points, gesture.current];
    }
    function previewAnalyticKeyboardSelection(context, gesture, config) {
        if (gesture.kind === 'axis') {
            return pixelAxisToSelection(context, config.axis, gesture.start, gesture.current);
        }
        if (gesture.kind === 'lasso') {
            const points = lassoPoints(gesture);
            if (points.length >= 3) {
                return pixelLassoToSelection(context, points, { x: config.xAxis, y: config.yAxis });
            }
            return pixelPointToDomain(context, gesture.current, {
                x: config.xAxis,
                y: config.yAxis,
            });
        }
        return pixelRectangleToSelection(context, gesture.start, gesture.current, {
            type: gesture.kind,
            xAxis: config.xAxis,
            yAxis: config.yAxis,
        });
    }
    function completeAnalyticKeyboardSelection(context, gesture, config) {
        const spanX = Math.abs(gesture.current.x - gesture.start.x);
        const spanY = Math.abs(gesture.current.y - gesture.start.y);
        if (gesture.kind === 'axis') {
            const span = cartesianAxisChannel(context, config.axis) === 'x' ? spanX : spanY;
            return span < config.minPixelSpan
                ? null
                : pixelAxisToSelection(context, config.axis, gesture.start, gesture.current);
        }
        if (gesture.kind === 'lasso') {
            const points = lassoPoints(gesture);
            return points.length < 3 || Math.max(spanX, spanY) < config.minPixelSpan
                ? null
                : pixelLassoToSelection(context, points, { x: config.xAxis, y: config.yAxis });
        }
        return Math.max(spanX, spanY) < config.minPixelSpan
            ? null
            : pixelRectangleToSelection(context, gesture.start, gesture.current, {
                type: gesture.kind,
                xAxis: config.xAxis,
                yAxis: config.yAxis,
            });
    }

    function boundedInteger$3(value, fallback, maximum, path) {
        const resolved = value ?? fallback;
        if (!Number.isInteger(resolved) || resolved < 1 || resolved > maximum) {
            throw new GraflumeError('INVALID_SPEC', `${path} must be an integer from 1 to ${maximum}.`, {
                path,
            });
        }
        return resolved;
    }
    function normalizeVirtualDataExplorerSpec(spec = {}) {
        if (spec === null || typeof spec !== 'object' || Array.isArray(spec)) {
            throw new GraflumeError('INVALID_SPEC', 'Accessibility explorer must be an object.', {
                path: '$.accessibility.explorer',
            });
        }
        const unknown = Object.keys(spec).find((key) => !['windowRows', 'overscanRows', 'rowHeight'].includes(key));
        if (unknown !== undefined) {
            throw new GraflumeError('INVALID_SPEC', `Unknown accessibility explorer property "${unknown}".`, { path: `$.accessibility.explorer.${unknown}` });
        }
        const overscanRows = spec.overscanRows ?? 6;
        if (!Number.isInteger(overscanRows) || overscanRows < 0 || overscanRows > 100) {
            throw new GraflumeError('INVALID_SPEC', '$.accessibility.explorer.overscanRows must be an integer from 0 to 100.', { path: '$.accessibility.explorer.overscanRows' });
        }
        return {
            windowRows: boundedInteger$3(spec.windowRows, 24, 5_000, '$.accessibility.explorer.windowRows'),
            overscanRows,
            rowHeight: boundedInteger$3(spec.rowHeight, 32, 256, '$.accessibility.explorer.rowHeight'),
        };
    }
    /** Pure window model; DOM scroll and focus are injected by the caller. */
    class VirtualDataExplorer {
        #spec;
        #rows = [];
        #viewportStart = 0;
        #activeIndex = null;
        constructor(spec = {}) {
            this.#spec = normalizeVirtualDataExplorerSpec(spec);
        }
        setRows(rows, preferredId) {
            if (rows.length > 100_000) {
                throw new GraflumeError('INVALID_DATA', 'The virtual data explorer accepts at most 100,000 bounded semantic rows.');
            }
            this.#rows = rows;
            const preferred = preferredId === undefined || preferredId === null
                ? -1
                : rows.findIndex(({ id }) => id === preferredId);
            const previous = this.#activeIndex ?? -1;
            this.#activeIndex =
                preferred >= 0
                    ? preferred
                    : rows.length === 0
                        ? null
                        : Math.min(Math.max(0, previous), rows.length - 1);
            this.#viewportStart = this.#boundedViewportStart(this.#viewportStart);
            if (this.#activeIndex !== null)
                this.#ensureVisible(this.#activeIndex);
            return this.window();
        }
        setScrollOffset(offsetPixels) {
            if (!Number.isFinite(offsetPixels) || offsetPixels < 0) {
                throw new GraflumeError('INVALID_DATA', 'Explorer scroll offset must be finite and non-negative.');
            }
            this.#viewportStart = this.#boundedViewportStart(Math.floor(offsetPixels / this.#spec.rowHeight));
            if (this.#rows.length > 0 &&
                (this.#activeIndex === null ||
                    this.#activeIndex < this.#viewportStart ||
                    this.#activeIndex >= this.#viewportStart + this.#spec.windowRows)) {
                this.#activeIndex = this.#viewportStart;
            }
            return this.window();
        }
        focusIndex(index) {
            if (!Number.isInteger(index) || index < 0 || index >= this.#rows.length) {
                throw new GraflumeError('INVALID_DATA', 'Explorer focus index is outside semantic rows.');
            }
            this.#activeIndex = index;
            this.#ensureVisible(index);
            return this.window();
        }
        focusId(id) {
            const index = this.#rows.findIndex((row) => row.id === id);
            if (index < 0)
                throw new GraflumeError('INVALID_DATA', `Semantic row "${id}" was not found.`);
            return this.focusIndex(index);
        }
        move(key) {
            if (this.#rows.length === 0)
                return this.window();
            const current = this.#activeIndex ?? 0;
            let next = current;
            switch (key) {
                case 'ArrowLeft':
                case 'ArrowUp':
                    next = Math.max(0, current - 1);
                    break;
                case 'ArrowRight':
                case 'ArrowDown':
                    next = Math.min(this.#rows.length - 1, current + 1);
                    break;
                case 'Home':
                    next = 0;
                    break;
                case 'End':
                    next = this.#rows.length - 1;
                    break;
                case 'PageUp':
                    next = Math.max(0, current - this.#spec.windowRows);
                    break;
                case 'PageDown':
                    next = Math.min(this.#rows.length - 1, current + this.#spec.windowRows);
                    break;
            }
            return this.focusIndex(next);
        }
        active() {
            return this.#activeIndex === null ? null : (this.#rows[this.#activeIndex] ?? null);
        }
        window() {
            const viewportEnd = Math.min(this.#rows.length, this.#viewportStart + this.#spec.windowRows);
            const start = Math.max(0, this.#viewportStart - this.#spec.overscanRows);
            const end = Math.min(this.#rows.length, viewportEnd + this.#spec.overscanRows);
            return {
                version: 1,
                totalRows: this.#rows.length,
                viewportStart: this.#viewportStart,
                viewportEnd,
                start,
                end,
                beforeRows: start,
                afterRows: this.#rows.length - end,
                beforePixels: start * this.#spec.rowHeight,
                afterPixels: (this.#rows.length - end) * this.#spec.rowHeight,
                activeIndex: this.#activeIndex,
                rows: this.#rows.slice(start, end),
            };
        }
        #boundedViewportStart(start) {
            return Math.max(0, Math.min(start, Math.max(0, this.#rows.length - this.#spec.windowRows)));
        }
        #ensureVisible(index) {
            if (index < this.#viewportStart)
                this.#viewportStart = index;
            else if (index >= this.#viewportStart + this.#spec.windowRows) {
                this.#viewportStart = index - this.#spec.windowRows + 1;
            }
            this.#viewportStart = this.#boundedViewportStart(this.#viewportStart);
        }
    }
    function createVirtualDataExplorer(spec = {}) {
        return new VirtualDataExplorer(spec);
    }

    function visuallyHidden(element) {
        element.style.position = 'absolute';
        element.style.width = '1px';
        element.style.height = '1px';
        element.style.padding = '0';
        element.style.margin = '-1px';
        element.style.overflow = 'hidden';
        element.style.clipPath = 'inset(50%)';
        element.style.whiteSpace = 'nowrap';
        element.style.border = '0';
    }
    function selected(mark, selectedKeys) {
        return selectedKeys.has(mark.id);
    }
    function rowFields(index) {
        const fields = [];
        for (const mark of index) {
            for (const field of Object.keys(mark.datum)) {
                if (!fields.includes(field))
                    fields.push(field);
                if (fields.length >= 12)
                    return fields;
            }
        }
        return fields;
    }
    function valueText(value) {
        if (value === null || value === undefined)
            return '\u2014';
        return value instanceof Date ? value.toISOString() : String(value);
    }
    class AccessibilityMirrorController {
        #explorer = null;
        #explorerSignature = '';
        #host = null;
        #surface = null;
        #mirror = null;
        #ring = null;
        #focusedId = null;
        #focusById = null;
        #scrollHandler = null;
        getFocusedId() {
            return this.#focusedId;
        }
        focusSemanticId(id) {
            return this.#focusById?.(id) ?? false;
        }
        sync(container, overlayHost, surface, index, spec, view, selectedKeys, actions) {
            if (spec.table === false && !spec.navigation && spec.linkedFocus === false) {
                this.destroy();
                return;
            }
            const host = spec.table === 'visible' ? container : overlayHost;
            const ownerDocument = host.ownerDocument;
            const focusedId = ownerDocument.activeElement?.dataset?.graflumeSemanticId ??
                this.#focusedId;
            if (this.#host !== host || this.#surface !== surface) {
                this.destroy();
                this.#host = host;
                this.#surface = surface;
                const mirror = ownerDocument.createElement('div');
                mirror.dataset.graflumeAccessibilityMirror = spec.table === 'visible' ? 'visible' : 'hidden';
                mirror.setAttribute('dir', 'auto');
                host.append(mirror);
                this.#mirror = mirror;
                const ring = overlayHost.ownerDocument.createElement('div');
                ring.dataset.graflumeSemanticFocus = 'true';
                ring.setAttribute('aria-hidden', 'true');
                ring.style.position = 'absolute';
                ring.style.zIndex = '19';
                ring.style.pointerEvents = 'none';
                ring.style.border = '3px solid #2563eb';
                ring.style.borderRadius = '5px';
                ring.style.boxShadow = '0 0 0 2px rgba(255,255,255,.9)';
                ring.hidden = true;
                overlayHost.append(ring);
                this.#ring = ring;
            }
            const mirror = this.#mirror;
            if (mirror === null)
                return;
            if (this.#scrollHandler !== null) {
                mirror.removeEventListener('scroll', this.#scrollHandler);
                this.#scrollHandler = null;
            }
            mirror.dataset.graflumeAccessibilityMirror = spec.table === 'visible' ? 'visible' : 'hidden';
            mirror.removeAttribute('style');
            if (spec.table !== 'visible')
                visuallyHidden(mirror);
            else {
                mirror.style.boxSizing = 'border-box';
                mirror.style.width = '100%';
                mirror.style.maxHeight = 'min(360px,50vh)';
                mirror.style.overflow = 'auto';
                mirror.style.marginTop = '12px';
                mirror.style.font =
                    '13px/1.45 ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif';
                mirror.style.color = '#111827';
                mirror.style.background = '#fff';
            }
            const summary = ownerDocument.createElement('p');
            summary.dataset.graflumeAccessibilitySummary = 'true';
            summary.textContent = spec.summary ?? `${index.length} chart marks`;
            if (spec.table === 'visible')
                summary.style.margin = '0 0 8px';
            const table = ownerDocument.createElement('table');
            table.dataset.graflumeAccessibilityTable = 'true';
            table.style.borderCollapse = 'collapse';
            table.style.width = '100%';
            const caption = ownerDocument.createElement('caption');
            caption.textContent = spec.summary ?? 'Chart data';
            visuallyHidden(caption);
            table.append(caption);
            const head = ownerDocument.createElement('thead');
            const headRow = ownerDocument.createElement('tr');
            const fields = rowFields(index);
            for (const label of ['Layer', ...fields]) {
                const cell = ownerDocument.createElement('th');
                cell.scope = 'col';
                cell.textContent = label;
                cell.style.textAlign = 'start';
                cell.style.padding = spec.table === 'visible' ? '6px 8px' : '0';
                headRow.append(cell);
            }
            head.append(headRow);
            table.append(head);
            const body = ownerDocument.createElement('tbody');
            table.setAttribute('role', 'grid');
            const navigableMarks = index.filter(({ visible }) => visible);
            table.setAttribute('aria-rowcount', String(navigableMarks.length + 1));
            const explorerSpec = spec.explorer === false
                ? { windowRows: Math.max(1, navigableMarks.length), overscanRows: 0, rowHeight: 32 }
                : spec.explorer;
            const explorerSignature = JSON.stringify(explorerSpec);
            if (this.#explorer === null || explorerSignature !== this.#explorerSignature) {
                this.#explorer = new VirtualDataExplorer(explorerSpec);
                this.#explorerSignature = explorerSignature;
            }
            const explorer = this.#explorer;
            let dataWindow = explorer.setRows(navigableMarks, focusedId);
            const navigationKeys = new Set([
                'ArrowLeft',
                'ArrowRight',
                'ArrowUp',
                'ArrowDown',
                'Home',
                'End',
                'PageUp',
                'PageDown',
            ]);
            let renderedRows = [];
            const spacer = (pixels, position) => {
                const row = ownerDocument.createElement('tr');
                row.dataset.graflumeVirtualSpacer = position;
                row.setAttribute('aria-hidden', 'true');
                const cell = ownerDocument.createElement('td');
                cell.colSpan = fields.length + 1;
                cell.style.height = `${pixels}px`;
                cell.style.padding = '0';
                cell.style.border = '0';
                row.append(cell);
                return row;
            };
            const renderWindow = (focusActive) => {
                const rows = [];
                if (dataWindow.beforePixels > 0)
                    rows.push(spacer(dataWindow.beforePixels, 'before'));
                dataWindow.rows.forEach((mark, windowIndex) => {
                    const absoluteIndex = dataWindow.start + windowIndex;
                    const row = ownerDocument.createElement('tr');
                    row.dataset.graflumeSemanticId = mark.id;
                    row.dataset.graflumeSemanticIndex = String(absoluteIndex);
                    row.setAttribute('aria-rowindex', String(absoluteIndex + 2));
                    row.setAttribute('aria-label', mark.label);
                    row.setAttribute('aria-selected', String(selected(mark, selectedKeys)));
                    row.tabIndex = spec.navigation && dataWindow.activeIndex === absoluteIndex ? 0 : -1;
                    const layerCell = ownerDocument.createElement('th');
                    layerCell.scope = 'row';
                    layerCell.textContent = mark.layerId;
                    layerCell.style.textAlign = 'start';
                    layerCell.style.padding = spec.table === 'visible' ? '6px 8px' : '0';
                    row.append(layerCell);
                    for (const field of fields) {
                        const cell = ownerDocument.createElement('td');
                        cell.textContent = valueText(mark.datum[field]);
                        cell.style.padding = spec.table === 'visible' ? '6px 8px' : '0';
                        row.append(cell);
                    }
                    row.addEventListener('focus', () => {
                        this.#focusedId = mark.id;
                        dataWindow = explorer.focusIndex(absoluteIndex);
                        for (const candidate of renderedRows) {
                            candidate.tabIndex = spec.navigation && candidate === row ? 0 : -1;
                        }
                        this.#positionRing(mark, view);
                        actions.focus(mark);
                    });
                    row.addEventListener('blur', () => {
                        queueMicrotask(() => {
                            const activeId = ownerDocument.activeElement?.dataset
                                ?.graflumeSemanticId;
                            if (activeId === undefined) {
                                if (this.#ring !== null)
                                    this.#ring.hidden = true;
                                actions.focus(null);
                            }
                        });
                    });
                    row.addEventListener('keydown', (event) => {
                        if (!spec.navigation)
                            return;
                        if (!('key' in event))
                            return;
                        if (event.key === 'Enter' || event.key === ' ') {
                            actions.toggle(mark);
                            event.preventDefault();
                            return;
                        }
                        if (event.key === 'Escape') {
                            actions.clear();
                            surface.focus();
                            if (this.#ring !== null)
                                this.#ring.hidden = true;
                            actions.focus(null);
                            event.preventDefault();
                            return;
                        }
                        if (!navigationKeys.has(event.key))
                            return;
                        dataWindow = explorer.move(event.key);
                        renderWindow(true);
                        event.preventDefault();
                    });
                    rows.push(row);
                });
                if (dataWindow.afterPixels > 0)
                    rows.push(spacer(dataWindow.afterPixels, 'after'));
                body.replaceChildren(...rows);
                renderedRows = rows.filter(({ dataset }) => dataset.graflumeSemanticId !== undefined);
                if (focusActive && dataWindow.activeIndex !== null) {
                    const target = renderedRows.find(({ dataset }) => dataset.graflumeSemanticIndex === String(dataWindow.activeIndex));
                    target?.focus();
                }
            };
            table.append(body);
            mirror.replaceChildren(summary, table);
            mirror.dataset.graflumeVirtualRows = String(navigableMarks.length);
            renderWindow(false);
            this.#focusById = (id) => {
                if (!navigableMarks.some((mark) => mark.id === id))
                    return false;
                dataWindow = explorer.focusId(id);
                renderWindow(true);
                return true;
            };
            if (focusedId !== null && focusedId !== undefined) {
                const restored = renderedRows.find(({ dataset }) => dataset.graflumeSemanticId === focusedId);
                restored?.focus();
            }
            if (spec.table === 'visible') {
                this.#scrollHandler = () => {
                    dataWindow = explorer.setScrollOffset(mirror.scrollTop);
                    renderWindow(false);
                };
                mirror.addEventListener('scroll', this.#scrollHandler);
            }
        }
        destroy() {
            if (this.#scrollHandler !== null) {
                this.#mirror?.removeEventListener('scroll', this.#scrollHandler);
                this.#scrollHandler = null;
            }
            this.#mirror?.remove();
            this.#ring?.remove();
            this.#mirror = null;
            this.#ring = null;
            this.#host = null;
            this.#surface = null;
            this.#focusedId = null;
            this.#focusById = null;
            this.#explorer = null;
            this.#explorerSignature = '';
        }
        #positionRing(mark, view) {
            const ring = this.#ring;
            if (ring === null)
                return;
            const left = mark.bounds.x * view.zoom + view.offsetX;
            const top = mark.bounds.y * view.zoom + view.offsetY;
            ring.style.left = `${left}px`;
            ring.style.top = `${top}px`;
            ring.style.width = `${Math.max(8, mark.bounds.width * view.zoom)}px`;
            ring.style.height = `${Math.max(8, mark.bounds.height * view.zoom)}px`;
            ring.hidden = !mark.visible;
        }
    }

    const groupPattern = /^[a-zA-Z0-9][a-zA-Z0-9_.:-]{0,95}$/;
    function boundedInteger$2(value, fallback, maximum, path) {
        const resolved = value ?? fallback;
        if (!Number.isInteger(resolved) || resolved < 1 || resolved > maximum) {
            throw new GraflumeError('INVALID_SPEC', `${path} must be an integer from 1 to ${maximum}.`, {
                path,
            });
        }
        return resolved;
    }
    function safeIdentity(value, path) {
        if (!groupPattern.test(value)) {
            throw new GraflumeError('INVALID_SPEC', `${path} must contain 1 to 96 portable identity characters.`, { path });
        }
        return value;
    }
    function focusKey(value, path) {
        if (value instanceof Date && Number.isFinite(value.getTime()))
            return `date:${value.toISOString()}`;
        if (typeof value === 'string' && value !== '')
            return `string:${value}`;
        if (typeof value === 'number' && Number.isFinite(value))
            return `number:${value}`;
        if (typeof value === 'boolean')
            return `boolean:${value}`;
        throw new GraflumeError('INVALID_DATA', 'Linked focus keys must be non-empty strings, finite numbers, booleans, or valid Dates.', { path });
    }
    function cloneTarget$1(target) {
        return { ...target };
    }
    /**
     * Bounded shared focus state for linked Canvas and GPU views.
     *
     * Specs and emitted state contain data only. Runtime listeners are registered
     * separately, so chart specifications remain JSON-serializable.
     */
    class SemanticFocusStore {
        #maxViews;
        #maxRowsPerView;
        #maxListeners;
        #views = new Map();
        #listeners = new Set();
        #focused = null;
        #matches = [];
        #revision = 0;
        constructor(options = {}) {
            this.#maxViews = boundedInteger$2(options.maxViews, 64, 1_024, '$.focus.maxViews');
            this.#maxRowsPerView = boundedInteger$2(options.maxRowsPerView, 5_000, 100_000, '$.focus.maxRowsPerView');
            this.#maxListeners = boundedInteger$2(options.maxListeners, 128, 4_096, '$.focus.maxListeners');
        }
        registerView(viewId, spec, index) {
            safeIdentity(viewId, '$.focus.viewId');
            safeIdentity(spec.group, '$.accessibility.linkedFocus.group');
            if (typeof spec.key !== 'string' || spec.key.trim() === '') {
                throw new GraflumeError('INVALID_SPEC', '$.accessibility.linkedFocus.key must be a non-empty field.', { path: '$.accessibility.linkedFocus.key' });
            }
            assertSafeKey(spec.key, '$.accessibility.linkedFocus.key');
            if (!this.#views.has(viewId) && this.#views.size >= this.#maxViews) {
                throw new GraflumeError('INVALID_DATA', 'Linked focus view limit reached.');
            }
            if (index.length > this.#maxRowsPerView) {
                throw new GraflumeError('INVALID_DATA', `Linked focus index has ${index.length} rows; the deterministic limit is ${this.#maxRowsPerView}.`);
            }
            const marksByKey = new Map();
            index.forEach((mark, rowIndex) => {
                const value = ownValue(mark.datum, spec.key);
                const key = focusKey(value, `$.semanticIndex[${rowIndex}].datum.${spec.key}`);
                // A single deterministic target per view prevents an ambiguous focus ring.
                if (!marksByKey.has(key) || (!marksByKey.get(key).visible && mark.visible)) {
                    marksByKey.set(key, mark);
                }
            });
            this.#views.set(viewId, { viewId, spec: { ...spec }, marksByKey });
            this.#reconcile('index');
            return () => {
                if (!this.#views.delete(viewId))
                    return;
                this.#reconcile('index');
            };
        }
        focus(viewId, mark) {
            const view = this.#views.get(viewId);
            if (view === undefined) {
                throw new GraflumeError('INVALID_DATA', `Linked focus view "${viewId}" is not registered.`);
            }
            const key = focusKey(ownValue(mark.datum, view.spec.key), `$.semanticIndex.datum.${view.spec.key}`);
            this.#focused = {
                group: view.spec.group,
                key,
                sourceViewId: viewId,
                semanticId: mark.id,
            };
            this.#reconcile('focus');
        }
        focusTarget(target) {
            safeIdentity(target.group, '$.focus.group');
            safeIdentity(target.sourceViewId, '$.focus.sourceViewId');
            if (target.key.length === 0 || target.key.length > 512) {
                throw new GraflumeError('INVALID_DATA', 'Linked focus key is empty or exceeds 512 characters.');
            }
            if (target.semanticId.length === 0 || target.semanticId.length > 512) {
                throw new GraflumeError('INVALID_DATA', 'Linked focus semantic identity is empty or exceeds 512 characters.');
            }
            this.#focused = cloneTarget$1(target);
            this.#reconcile('focus');
        }
        clear() {
            if (this.#focused === null && this.#matches.length === 0)
                return;
            this.#focused = null;
            this.#matches = [];
            this.#revision += 1;
            this.#emit('clear');
        }
        state() {
            return {
                version: 1,
                revision: this.#revision,
                focused: this.#focused === null ? null : cloneTarget$1(this.#focused),
                matches: this.#matches.map((match) => ({ ...match })),
                registeredViews: this.#views.size,
            };
        }
        subscribe(listener) {
            if (this.#listeners.size >= this.#maxListeners) {
                throw new GraflumeError('INVALID_DATA', 'Linked focus listener limit reached.');
            }
            this.#listeners.add(listener);
            return () => this.#listeners.delete(listener);
        }
        #reconcile(reason) {
            const focused = this.#focused;
            this.#matches =
                focused === null
                    ? []
                    : [...this.#views.values()]
                        .filter(({ spec }) => spec.group === focused.group)
                        .flatMap(({ viewId, marksByKey }) => {
                        const mark = marksByKey.get(focused.key);
                        return mark === undefined
                            ? []
                            : [
                                {
                                    viewId,
                                    semanticId: mark.id,
                                    layerId: mark.layerId,
                                    rowIndex: mark.rowIndex,
                                },
                            ];
                    })
                        .sort((left, right) => left.viewId.localeCompare(right.viewId, 'en'));
            this.#revision += 1;
            this.#emit(reason);
        }
        #emit(reason) {
            const change = { state: this.state(), reason };
            for (const listener of [...this.#listeners])
                listener(change);
        }
    }
    function createSemanticFocusStore(options = {}) {
        return new SemanticFocusStore(options);
    }
    /** Shared-by-default store used when linkedFocus is authored without an injected store. */
    const defaultSemanticFocusStore = new SemanticFocusStore();

    const ICON_PATHS = {
        'zoom-in': 'M10.5 4.5a6 6 0 1 0 0 12 6 6 0 0 0 0-12ZM10.5 8v5M8 10.5h5M15 15l4.5 4.5',
        'zoom-out': 'M10.5 4.5a6 6 0 1 0 0 12 6 6 0 0 0 0-12ZM8 10.5h5M15 15l4.5 4.5',
        reset: 'M5 5v5h5M6.4 8.2A7 7 0 1 1 5.8 16',
        fullscreen: 'M4.5 9V4.5H9M15 4.5h4.5V9M19.5 15v4.5H15M9 19.5H4.5V15',
        'exit-fullscreen': 'M9 4.5V9H4.5M19.5 9H15V4.5M15 19.5V15h4.5M4.5 15H9v4.5',
        export: 'M4.5 8h3l1.4-2h6.2l1.4 2h3v10.5h-15V8ZM12 10a3.25 3.25 0 1 0 0 6.5A3.25 3.25 0 0 0 12 10Z',
        annotations: 'M4.5 5.5h15v10h-9l-4 3v-3h-2v-10Z',
        'annotations-hidden': 'M4.5 5.5h15v10h-5M10.5 15.5l-4 3v-3h-2v-10h2M4 4l16 16',
        previous: 'M6 5v14M18 6.5 9.5 12l8.5 5.5Z',
        play: 'M8 5.5 18 12 8 18.5Z',
        pause: 'M8.5 6v12M15.5 6v12',
        next: 'M18 5v14M6 6.5l8.5 5.5L6 17.5Z',
        'playback-options': 'M4 7h16M4 12h16M4 17h16M8 5v4M15 10v4M11 15v4',
        loop: 'M17.5 7.5H8a4 4 0 0 0-4 4M16 4l3.5 3.5L16 11M6.5 16.5H16a4 4 0 0 0 4-4M8 20l-3.5-3.5L8 13',
    };
    const CONTROL_STYLES = `
.graflume-controls{position:absolute;z-index:30;top:6px;right:6px;display:block;max-width:calc(100% - 12px);color:#334155;font:600 12px/1 ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;direction:ltr;opacity:.78;transition:opacity 120ms ease;pointer-events:auto;user-select:none;-webkit-user-select:none}
.graflume-controls:hover,.graflume-controls:focus-within{opacity:1}
.graflume-controls__strip{box-sizing:border-box;display:flex;align-items:center;gap:0;width:max-content;max-width:100%;height:30px;padding:0;border:1px solid rgba(100,116,139,.3);border-radius:5px;background:rgba(248,250,252,.9);box-shadow:0 2px 8px rgba(15,23,42,.12);backdrop-filter:blur(4px);-webkit-backdrop-filter:blur(4px);overflow-x:auto;overflow-y:hidden;overscroll-behavior:contain;scrollbar-width:none}
.graflume-controls__strip::-webkit-scrollbar{display:none}
.graflume-controls__button{box-sizing:border-box;display:inline-grid;place-items:center;flex:0 0 28px;width:28px;height:28px;margin:0;padding:0;border:0;border-radius:3px;background:transparent;color:#475569;appearance:none;-webkit-appearance:none;cursor:pointer;touch-action:manipulation;transition:background-color 100ms ease,color 100ms ease,opacity 100ms ease}
.graflume-controls__button svg{display:block;width:16px;height:16px;overflow:visible;fill:none;stroke:currentColor;stroke-width:1.75;stroke-linecap:round;stroke-linejoin:round;pointer-events:none}
.graflume-controls__button:hover:not(:disabled){background:rgba(226,232,240,.9);color:#0f172a}
.graflume-controls__button:focus-visible,.graflume-controls__select:focus-visible,.graflume-controls__seek:focus-visible{outline:2px solid #2563eb;outline-offset:-2px}
.graflume-controls__button[aria-pressed="true"],.graflume-controls__button[aria-expanded="true"]{background:rgba(219,234,254,.92);color:#1d4ed8}
.graflume-controls__button:disabled{opacity:.28;cursor:default}
.graflume-controls__separator{display:block;flex:0 0 1px;width:1px;height:16px;margin:0 2px;background:rgba(100,116,139,.25)}
.graflume-controls__panel{position:absolute;z-index:1;top:calc(100% + 4px);right:0;box-sizing:border-box;display:grid;grid-template-columns:minmax(112px,1fr) auto;align-items:center;gap:7px;width:196px;padding:7px;border:1px solid rgba(100,116,139,.28);border-radius:6px;background:rgba(248,250,252,.96);box-shadow:0 5px 16px rgba(15,23,42,.16);backdrop-filter:blur(5px);-webkit-backdrop-filter:blur(5px)}
.graflume-controls__panel[hidden]{display:none}
.graflume-controls__frame{grid-column:1/-1;display:block;min-width:0;overflow:hidden;color:#475569;font-size:11px;line-height:1.25;text-overflow:ellipsis;white-space:nowrap}
.graflume-controls__seek{box-sizing:border-box;width:100%;height:20px;margin:0;accent-color:#2563eb;cursor:pointer}
.graflume-controls__select{box-sizing:border-box;height:28px;margin:0;padding:0 4px;border:1px solid rgba(100,116,139,.35);border-radius:4px;background:#fff;color:#334155;font:600 11px/1 ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;cursor:pointer}
.graflume-controls__panel .graflume-controls__button{border:1px solid rgba(100,116,139,.24);background:#fff}
.graflume-controls__visually-hidden{position:absolute!important;width:1px!important;height:1px!important;padding:0!important;margin:-1px!important;overflow:hidden!important;clip:rect(0,0,0,0)!important;white-space:nowrap!important;border:0!important}
@media (pointer:coarse),(max-width:560px){.graflume-controls{top:2px;right:2px;max-width:calc(100% - 4px);opacity:.9}.graflume-controls__strip{height:44px;border:0;box-shadow:0 0 0 1px rgba(100,116,139,.3),0 2px 8px rgba(15,23,42,.12)}.graflume-controls__button{flex-basis:44px;width:44px;height:44px}.graflume-controls__button svg{width:17px;height:17px}.graflume-controls__separator{height:20px;margin:0 1px}.graflume-controls__panel{width:min(236px,calc(100vw - 8px));padding:8px;gap:8px}.graflume-controls__panel .graflume-controls__button{width:44px;height:44px}.graflume-controls__select{height:44px;min-width:58px}}
@media (prefers-reduced-motion:reduce){.graflume-controls,.graflume-controls__button{transition:none}}
`;
    let panelSequence = 0;
    const styleRegistry = new WeakMap();
    function acquireStyles(document) {
        let entry = styleRegistry.get(document);
        if (entry === undefined) {
            const element = document.createElement('style');
            element.dataset.graflumeControlStyles = 'compact';
            element.textContent = CONTROL_STYLES;
            (document.head ?? document.documentElement).append(element);
            entry = { element, references: 0 };
            styleRegistry.set(document, entry);
        }
        entry.references += 1;
        let released = false;
        return () => {
            if (released)
                return;
            released = true;
            const current = styleRegistry.get(document);
            if (current === undefined)
                return;
            current.references -= 1;
            if (current.references > 0)
                return;
            current.element.remove();
            styleRegistry.delete(document);
        };
    }
    function icon(document, name) {
        const element = document.createElementNS('http://www.w3.org/2000/svg', 'svg');
        element.setAttribute('viewBox', '0 0 24 24');
        element.setAttribute('aria-hidden', 'true');
        element.setAttribute('focusable', 'false');
        const path = document.createElementNS('http://www.w3.org/2000/svg', 'path');
        path.setAttribute('d', ICON_PATHS[name]);
        element.append(path);
        return element;
    }
    function setIcon(element, name) {
        if (element.dataset.graflumeIcon === name)
            return;
        element.replaceChildren(icon(element.ownerDocument, name));
        element.dataset.graflumeIcon = name;
    }
    function button(document, control, iconName, label, action) {
        const element = document.createElement('button');
        element.type = 'button';
        element.className = 'graflume-controls__button';
        element.dataset.graflumeControl = control;
        setIcon(element, iconName);
        element.title = label;
        element.setAttribute('aria-label', label);
        element.addEventListener('click', action);
        return element;
    }
    function updateLabel(element, label) {
        element.title = label;
        element.setAttribute('aria-label', label);
    }
    function separator(document) {
        const element = document.createElement('span');
        element.className = 'graflume-controls__separator';
        element.setAttribute('aria-hidden', 'true');
        return element;
    }
    class ControlsController {
        #elements = null;
        #signature = '';
        #panelDocument = null;
        #releaseStyles = null;
        #handleDocumentPointerDown = (event) => {
            const root = this.#elements?.root;
            if (root === undefined || event.composedPath().includes(root))
                return;
            this.#setPlaybackOptionsOpen(false);
        };
        #handleDocumentKeyDown = (event) => {
            if (!('key' in event) || event.key !== 'Escape')
                return;
            const toggle = this.#elements?.playbackOptions;
            if (toggle?.getAttribute('aria-expanded') !== 'true')
                return;
            this.#setPlaybackOptionsOpen(false);
            toggle.focus();
        };
        sync(host, state, actions) {
            const signature = JSON.stringify({
                zoom: state.spec.zoom,
                reset: state.spec.reset,
                fullscreen: state.spec.fullscreen,
                export: state.spec.export,
                annotations: state.spec.annotations,
                annotationsAvailable: state.annotationsAvailable,
                playback: state.spec.playback,
                labels: state.spec.labels,
            });
            if (this.#elements === null || this.#signature !== signature) {
                this.destroy();
                this.#elements = this.#create(host.ownerDocument, state, actions);
                this.#signature = signature;
            }
            const elements = this.#elements;
            if (elements.root.parentElement !== host)
                host.append(elements.root);
            this.#update(elements, state);
        }
        destroy() {
            this.#detachPanelListeners();
            this.#elements?.root.remove();
            this.#releaseStyles?.();
            this.#releaseStyles = null;
            this.#elements = null;
            this.#signature = '';
        }
        #create(document, state, actions) {
            const spec = state.spec;
            const root = document.createElement('div');
            root.className = 'graflume-controls';
            root.dataset.graflumeControls = 'true';
            root.dataset.graflumeControlsDensity = 'compact';
            root.dataset.graflumeControlsPlacement = 'top-right';
            root.dataset.graflumeControlsOpen = 'false';
            root.setAttribute('role', 'toolbar');
            root.setAttribute('aria-label', spec.labels.controls);
            root.setAttribute('dir', 'ltr');
            root.addEventListener('pointerdown', (event) => event.stopPropagation());
            const strip = document.createElement('div');
            strip.className = 'graflume-controls__strip';
            strip.dataset.graflumeControlsStrip = 'true';
            root.append(strip);
            const elements = { root };
            const labels = spec.labels;
            const showAnnotations = spec.annotations && state.annotationsAvailable;
            if (spec.zoom) {
                elements.zoomOut = button(document, 'zoom-out', 'zoom-out', labels.zoomOut, actions.zoomOut);
                elements.zoomIn = button(document, 'zoom-in', 'zoom-in', labels.zoomIn, actions.zoomIn);
                strip.append(elements.zoomOut, elements.zoomIn);
            }
            if (spec.reset) {
                elements.reset = button(document, 'reset', 'reset', labels.reset, actions.reset);
                strip.append(elements.reset);
            }
            if ((spec.zoom || spec.reset) &&
                (spec.fullscreen || spec.export || showAnnotations || spec.playback)) {
                strip.append(separator(document));
            }
            if (spec.fullscreen) {
                elements.fullscreen = button(document, 'fullscreen', 'fullscreen', labels.enterFullscreen, actions.toggleFullscreen);
                strip.append(elements.fullscreen);
            }
            if (spec.export) {
                elements.exportPng = button(document, 'export-png', 'export', labels.exportPng, actions.exportPng);
                strip.append(elements.exportPng);
            }
            if (showAnnotations) {
                elements.annotations = button(document, 'annotations', 'annotations', labels.hideAnnotations, actions.toggleAnnotations);
                strip.append(elements.annotations);
            }
            if ((spec.fullscreen || spec.export || showAnnotations) && spec.playback)
                strip.append(separator(document));
            if (spec.playback) {
                const playbackOptionsLabel = `${labels.seek} · ${labels.speed} · ${labels.loop}`;
                elements.previousFrame = button(document, 'previous-frame', 'previous', labels.previousFrame, actions.previousFrame);
                elements.play = button(document, 'playback', 'play', labels.play, actions.togglePlayback);
                elements.nextFrame = button(document, 'next-frame', 'next', labels.nextFrame, actions.nextFrame);
                elements.playbackOptions = button(document, 'playback-options', 'playback-options', playbackOptionsLabel, () => this.#togglePlaybackOptions());
                const panelId = `graflume-playback-options-${++panelSequence}`;
                elements.playbackOptions.setAttribute('aria-controls', panelId);
                elements.playbackOptions.setAttribute('aria-expanded', 'false');
                elements.playbackOptions.setAttribute('aria-haspopup', 'dialog');
                strip.append(elements.previousFrame, elements.play, elements.nextFrame, elements.playbackOptions);
                const panel = document.createElement('div');
                panel.id = panelId;
                panel.className = 'graflume-controls__panel';
                panel.dataset.graflumePlaybackPanel = 'true';
                panel.setAttribute('role', 'dialog');
                panel.setAttribute('aria-label', playbackOptionsLabel);
                panel.hidden = true;
                elements.playbackPanel = panel;
                const frameOutput = document.createElement('output');
                frameOutput.className = 'graflume-controls__frame';
                frameOutput.title = labels.seek;
                frameOutput.setAttribute('aria-label', labels.seek);
                elements.frameOutput = frameOutput;
                const seek = document.createElement('input');
                seek.type = 'range';
                seek.min = '0';
                seek.step = '1';
                seek.className = 'graflume-controls__seek';
                seek.dataset.graflumeControl = 'playback-seek';
                seek.title = labels.seek;
                seek.setAttribute('aria-label', labels.seek);
                seek.addEventListener('input', () => actions.seek(Number(seek.value)));
                elements.seek = seek;
                const speed = document.createElement('select');
                speed.className = 'graflume-controls__select';
                speed.dataset.graflumeControl = 'playback-rate';
                speed.title = labels.speed;
                speed.setAttribute('aria-label', labels.speed);
                for (const rate of [0.25, 0.5, 1, 2, 4]) {
                    const option = document.createElement('option');
                    option.value = String(rate);
                    option.textContent = `${rate}×`;
                    speed.append(option);
                }
                speed.addEventListener('change', () => actions.setRate(Number(speed.value)));
                elements.speed = speed;
                elements.loop = button(document, 'playback-loop', 'loop', labels.loop, () => actions.setLoop(elements.loop?.getAttribute('aria-pressed') !== 'true'));
                panel.append(frameOutput, seek, speed, elements.loop);
                root.append(panel);
            }
            const status = document.createElement('span');
            status.className = 'graflume-controls__visually-hidden';
            status.setAttribute('aria-live', 'polite');
            status.setAttribute('aria-atomic', 'true');
            root.append(status);
            elements.status = status;
            this.#releaseStyles = acquireStyles(document);
            return elements;
        }
        #update(elements, state) {
            const labels = state.spec.labels;
            if (elements.zoomIn !== undefined) {
                elements.zoomIn.disabled = !state.navigationEnabled || state.zoom >= state.maxZoom;
            }
            if (elements.zoomOut !== undefined) {
                elements.zoomOut.disabled = !state.navigationEnabled || state.zoom <= state.minZoom;
            }
            if (elements.reset !== undefined) {
                elements.reset.disabled = !state.navigationEnabled || !state.viewDirty;
            }
            if (elements.fullscreen !== undefined) {
                elements.fullscreen.disabled = !state.fullscreenAvailable;
                updateLabel(elements.fullscreen, state.fullscreen ? labels.exitFullscreen : labels.enterFullscreen);
                setIcon(elements.fullscreen, state.fullscreen ? 'exit-fullscreen' : 'fullscreen');
                elements.fullscreen.setAttribute('aria-pressed', String(state.fullscreen));
            }
            if (elements.exportPng !== undefined)
                elements.exportPng.disabled = !state.exportAvailable;
            if (elements.annotations !== undefined) {
                elements.annotations.disabled = !state.annotationsAvailable;
                updateLabel(elements.annotations, state.annotationsVisible ? labels.hideAnnotations : labels.showAnnotations);
                setIcon(elements.annotations, state.annotationsVisible ? 'annotations' : 'annotations-hidden');
                elements.annotations.setAttribute('aria-pressed', String(state.annotationsVisible));
            }
            const playbackDisabled = !state.playbackEnabled || state.playbackRangeEnd - state.playbackRangeStart < 1;
            if (elements.previousFrame !== undefined)
                elements.previousFrame.disabled = playbackDisabled;
            if (elements.nextFrame !== undefined)
                elements.nextFrame.disabled = playbackDisabled;
            if (elements.play !== undefined) {
                elements.play.disabled = playbackDisabled;
                setIcon(elements.play, state.playing ? 'pause' : 'play');
                updateLabel(elements.play, state.playing ? labels.pause : labels.play);
                elements.play.setAttribute('aria-pressed', String(state.playing));
            }
            if (elements.playbackOptions !== undefined) {
                elements.playbackOptions.disabled = !state.playbackEnabled;
                if (!state.playbackEnabled)
                    this.#setPlaybackOptionsOpen(false);
            }
            if (elements.seek !== undefined) {
                elements.seek.disabled = !state.playbackEnabled || state.playbackLength === 0;
                elements.seek.min = String(state.playbackRangeStart);
                elements.seek.max = String(Math.max(state.playbackRangeStart, state.playbackRangeEnd));
                elements.seek.value = String(Math.max(0, state.playbackIndex));
                elements.seek.setAttribute('aria-valuetext', state.frameLabel);
            }
            if (elements.frameOutput !== undefined) {
                elements.frameOutput.textContent = state.frameLabel;
            }
            if (elements.speed !== undefined) {
                elements.speed.disabled = !state.playbackEnabled;
                const rate = String(state.playbackRate);
                if (![...elements.speed.options].some((option) => option.value === rate)) {
                    const option = document.createElement('option');
                    option.value = rate;
                    option.textContent = `${rate}×`;
                    elements.speed.append(option);
                }
                elements.speed.value = rate;
            }
            if (elements.loop !== undefined) {
                elements.loop.disabled = !state.playbackEnabled;
                elements.loop.setAttribute('aria-pressed', String(state.loop));
            }
            if (!state.playing || state.frameNamed) {
                elements.status.textContent = state.playbackEnabled
                    ? `${Math.round(state.zoom * 100)}%. ${state.frameLabel}`
                    : `${Math.round(state.zoom * 100)}%`;
            }
        }
        #togglePlaybackOptions() {
            const expanded = this.#elements?.playbackOptions?.getAttribute('aria-expanded') === 'true';
            this.#setPlaybackOptionsOpen(!expanded);
        }
        #setPlaybackOptionsOpen(open) {
            const elements = this.#elements;
            if (elements?.playbackOptions === undefined || elements.playbackPanel === undefined)
                return;
            elements.playbackOptions.setAttribute('aria-expanded', String(open));
            elements.playbackPanel.hidden = !open;
            elements.root.dataset.graflumeControlsOpen = String(open);
            this.#detachPanelListeners();
            if (!open)
                return;
            this.#panelDocument = elements.root.ownerDocument;
            this.#panelDocument.addEventListener('pointerdown', this.#handleDocumentPointerDown, true);
            this.#panelDocument.addEventListener('keydown', this.#handleDocumentKeyDown, true);
            elements.seek?.focus();
        }
        #detachPanelListeners() {
            this.#panelDocument?.removeEventListener('pointerdown', this.#handleDocumentPointerDown, true);
            this.#panelDocument?.removeEventListener('keydown', this.#handleDocumentKeyDown, true);
            this.#panelDocument = null;
        }
    }

    /**
     * A deterministic bounded uniform-grid index for screen-space geometry.
     *
     * Very large geometry is kept in a small overflow list instead of being
     * duplicated into an unbounded number of cells. Query results retain source
     * order so z-order hit testing is stable across indexed and linear paths.
     */
    class UniformSpatialIndex {
        #cellSize;
        #maximumCellsPerItem;
        #buckets = new Map();
        #overflow = [];
        #items = [];
        constructor(cellSize = 64, maximumCellsPerItem = 256) {
            this.#cellSize = Math.max(8, Math.trunc(cellSize));
            this.#maximumCellsPerItem = Math.max(1, Math.trunc(maximumCellsPerItem));
        }
        insert(item, bounds) {
            if (!Number.isFinite(bounds.x) ||
                !Number.isFinite(bounds.y) ||
                !Number.isFinite(bounds.width) ||
                !Number.isFinite(bounds.height)) {
                return;
            }
            const normalized = {
                x: Math.min(bounds.x, bounds.x + bounds.width),
                y: Math.min(bounds.y, bounds.y + bounds.height),
                width: Math.abs(bounds.width),
                height: Math.abs(bounds.height),
            };
            const indexed = {
                item,
                bounds: normalized,
                order: this.#items.length,
            };
            this.#items.push(indexed);
            const columns = this.#cellRange(normalized.x, normalized.x + normalized.width);
            const rows = this.#cellRange(normalized.y, normalized.y + normalized.height);
            if (columns.length * rows.length > this.#maximumCellsPerItem) {
                this.#overflow.push(indexed);
                return;
            }
            for (const column of columns) {
                for (const row of rows) {
                    const key = `${column}:${row}`;
                    const bucket = this.#buckets.get(key);
                    if (bucket === undefined)
                        this.#buckets.set(key, [indexed]);
                    else
                        bucket.push(indexed);
                }
            }
        }
        query(x, y, tolerance = 0) {
            if (!Number.isFinite(x) || !Number.isFinite(y) || !Number.isFinite(tolerance))
                return [];
            const radius = Math.max(0, tolerance);
            const columns = this.#cellRange(x - radius, x + radius);
            const rows = this.#cellRange(y - radius, y + radius);
            const matches = new Map();
            for (const column of columns) {
                for (const row of rows) {
                    for (const indexed of this.#buckets.get(`${column}:${row}`) ?? []) {
                        if (this.#intersectsQuery(indexed.bounds, x, y, radius)) {
                            matches.set(indexed.order, indexed);
                        }
                    }
                }
            }
            for (const indexed of this.#overflow) {
                if (this.#intersectsQuery(indexed.bounds, x, y, radius)) {
                    matches.set(indexed.order, indexed);
                }
            }
            return [...matches.values()]
                .sort((left, right) => left.order - right.order)
                .map(({ item }) => item);
        }
        stats() {
            return Object.freeze({
                itemCount: this.#items.length,
                indexedItemCount: this.#items.length - this.#overflow.length,
                overflowItemCount: this.#overflow.length,
                bucketCount: this.#buckets.size,
                cellSize: this.#cellSize,
            });
        }
        #cellRange(start, end) {
            const first = Math.floor(Math.min(start, end) / this.#cellSize);
            const last = Math.floor(Math.max(start, end) / this.#cellSize);
            return Array.from({ length: Math.max(1, last - first + 1) }, (_, index) => first + index);
        }
        #intersectsQuery(bounds, x, y, tolerance) {
            return (x + tolerance >= bounds.x &&
                x - tolerance <= bounds.x + bounds.width &&
                y + tolerance >= bounds.y &&
                y - tolerance <= bounds.y + bounds.height);
        }
    }

    function tooltipKind(candidate) {
        return candidate.node.datum?.tooltip?.kind;
    }
    function tooltipStrings(value) {
        if (!Array.isArray(value) || !value.every((entry) => typeof entry === 'string'))
            return null;
        return value;
    }
    /**
     * Resolve a Venn/Euler region from the actual overlapping set circles. Region
     * labels are deliberately small and must not be the hit geometry: a pointer
     * anywhere inside the exact visual membership combination resolves the
     * corresponding membership datum.
     */
    function hitTestVennRegion(candidates, x, y) {
        const setCircles = candidates.filter((candidate) => candidate.node.type === 'circle' &&
            tooltipKind(candidate) === 'venn-set' &&
            insideClips(candidate.clips, x, y));
        if (setCircles.length < 2 || setCircles.length > 3)
            return null;
        const included = setCircles
            .flatMap((candidate) => {
            const node = candidate.node;
            if (node.type !== 'circle' || Math.hypot(x - node.cx, y - node.cy) > node.radius)
                return [];
            const set = node.datum?.tooltip?.set;
            return typeof set === 'string' ? [set] : [];
        })
            .sort();
        if (included.length === 0)
            return null;
        const region = candidates.find((candidate) => {
            if (tooltipKind(candidate) !== 'venn-region' || candidate.node.datum === undefined)
                return false;
            const sets = tooltipStrings(candidate.node.datum.tooltip?.sets)?.slice().sort();
            return (sets !== undefined &&
                sets !== null &&
                sets.length === included.length &&
                sets.every((set, index) => set === included[index]));
        });
        if (region?.node.datum === undefined)
            return null;
        return {
            ...region.node.datum,
            nodeId: region.node.id,
            x,
            y,
            distance: 0,
        };
    }
    const hitCandidateCache = new WeakMap();
    function sceneHitCandidates(scene) {
        const cached = hitCandidateCache.get(scene);
        if (cached !== undefined)
            return cached;
        const candidates = [];
        const visit = (node, parentOpacity, clips) => {
            const opacity = parentOpacity * node.opacity;
            if (!node.visible || opacity <= 0)
                return;
            if (node.type === 'group') {
                const nextClips = node.clip === undefined ? clips : [...clips, node.clip];
                const children = [...node.children].sort((left, right) => left.zIndex - right.zIndex);
                for (const child of children)
                    visit(child, opacity, nextClips);
                return;
            }
            if (node.interactive === true && node.datum !== undefined)
                candidates.push({ node, clips });
        };
        visit(scene.root, 1, []);
        const index = new UniformSpatialIndex(64, 256);
        for (const candidate of candidates) {
            const bounds = sceneNodeBounds(candidate.node);
            if (bounds !== null)
                index.insert(candidate, bounds);
        }
        const result = Object.freeze({ candidates: Object.freeze(candidates), index });
        hitCandidateCache.set(scene, result);
        return result;
    }
    function insideClips(clips, x, y) {
        return clips.every((clip) => x >= clip.x && x <= clip.x + clip.width && y >= clip.y && y <= clip.y + clip.height);
    }
    function distanceToSegment(x, y, x1, y1, x2, y2) {
        const dx = x2 - x1;
        const dy = y2 - y1;
        if (dx === 0 && dy === 0)
            return Math.hypot(x - x1, y - y1);
        const t = Math.max(0, Math.min(1, ((x - x1) * dx + (y - y1) * dy) / (dx * dx + dy * dy)));
        return Math.hypot(x - (x1 + t * dx), y - (y1 + t * dy));
    }
    function pathDistance(node, x, y) {
        const paths = [node.points, ...(node.subpaths ?? [])];
        if (node.closed) {
            let inside = false;
            for (const points of paths) {
                if (points.length < 3)
                    continue;
                for (let index = 0, previous = points.length - 1; index < points.length; previous = index, index += 1) {
                    const currentPoint = points[index];
                    const previousPoint = points[previous];
                    if (currentPoint === undefined || previousPoint === undefined)
                        continue;
                    const crosses = currentPoint.y > y !== previousPoint.y > y &&
                        x <
                            ((previousPoint.x - currentPoint.x) * (y - currentPoint.y)) /
                                (previousPoint.y - currentPoint.y || Number.EPSILON) +
                                currentPoint.x;
                    if (crosses)
                        inside = !inside;
                }
            }
            if (inside)
                return 0;
        }
        let minimum = Number.POSITIVE_INFINITY;
        for (const points of paths) {
            const only = points[0];
            if (points.length === 1 && only !== undefined) {
                minimum = Math.min(minimum, Math.hypot(x - only.x, y - only.y));
                continue;
            }
            for (let index = 1; index < points.length; index += 1) {
                const first = points[index - 1];
                const second = points[index];
                if (first === undefined || second === undefined)
                    continue;
                minimum = Math.min(minimum, distanceToSegment(x, y, first.x, first.y, second.x, second.y));
            }
            if (node.closed && points.length > 1) {
                const first = points[0];
                const last = points[points.length - 1];
                if (first !== undefined && last !== undefined) {
                    minimum = Math.min(minimum, distanceToSegment(x, y, last.x, last.y, first.x, first.y));
                }
            }
        }
        return minimum;
    }
    function textDistance(node, x, y) {
        const angle = (-node.rotation * Math.PI) / 180;
        const translatedX = x - node.x;
        const translatedY = y - node.y;
        const localX = translatedX * Math.cos(angle) - translatedY * Math.sin(angle);
        const localY = translatedX * Math.sin(angle) + translatedY * Math.cos(angle);
        const width = Math.max(node.fontSize * 0.6, Array.from(node.text).length * node.fontSize * 0.6);
        const height = Math.max(1, node.fontSize * 1.2);
        const left = node.align === 'center'
            ? -width / 2
            : node.align === 'right' || node.align === 'end'
                ? -width
                : 0;
        const top = node.baseline === 'middle'
            ? -height / 2
            : node.baseline === 'bottom' || node.baseline === 'ideographic'
                ? -height
                : node.baseline === 'alphabetic'
                    ? -height * 0.8
                    : 0;
        const dx = Math.max(left - localX, 0, localX - (left + width));
        const dy = Math.max(top - localY, 0, localY - (top + height));
        return Math.hypot(dx, dy);
    }
    function nodeDistance(node, x, y) {
        switch (node.type) {
            case 'circle':
                return Math.max(0, Math.hypot(x - node.cx, y - node.cy) - node.radius);
            case 'rect': {
                const dx = Math.max(node.x - x, 0, x - (node.x + node.width));
                const dy = Math.max(node.y - y, 0, y - (node.y + node.height));
                return Math.hypot(dx, dy);
            }
            case 'line':
                return distanceToSegment(x, y, node.x1, node.y1, node.x2, node.y2);
            case 'path':
                return pathDistance(node, x, y);
            case 'text':
                return textDistance(node, x, y);
            case 'group':
                return Number.POSITIVE_INFINITY;
        }
    }
    function hitTestScene(scene, x, y, tolerance = 8) {
        const { candidates: allCandidates, index } = sceneHitCandidates(scene);
        const vennRegion = hitTestVennRegion(allCandidates, x, y);
        if (vennRegion !== null)
            return vennRegion;
        const candidates = index.query(x, y, tolerance);
        let best = null;
        for (let index = candidates.length - 1; index >= 0; index -= 1) {
            const candidate = candidates[index];
            if (candidate === undefined || !insideClips(candidate.clips, x, y))
                continue;
            const { node } = candidate;
            if (node.datum === undefined)
                continue;
            const distance = nodeDistance(node, x, y);
            if (distance > tolerance || (best !== null && distance >= best.distance))
                continue;
            best = {
                ...node.datum,
                nodeId: node.id,
                x,
                y,
                distance,
            };
        }
        return best;
    }

    const MAX_TABLE_FILTERS = 16;
    const MAX_TABLE_SORTS = 8;
    const MAX_TABLE_GROUP_FIELDS = 8;
    const MAX_TABLE_AGGREGATES = 16;
    const MAX_RUNTIME_POSITIONS = 2_048;
    const MAX_RUNTIME_COLLAPSED = 2_048;
    const MAX_NETWORK_LASSO_POINTS = 256;
    const MAX_TABLE_WINDOW = 10_000;
    function fail$1(path, message) {
        throw new GraflumeError('INVALID_SPEC', message, { path });
    }
    function field$1(value, path) {
        if (typeof value !== 'string' || value.trim() === '' || value.length > 128) {
            return fail$1(path, `${path} must be a non-empty field name of at most 128 characters.`);
        }
        if (value === '__proto__' || value === 'prototype' || value === 'constructor') {
            return fail$1(path, `${path} uses a forbidden object key.`);
        }
        return value;
    }
    function id(value, path) {
        if (typeof value !== 'string' || value.trim() === '' || value.length > 256) {
            return fail$1(path, `${path} must be a non-empty id of at most 256 characters.`);
        }
        return value;
    }
    function object$1(value) {
        return value !== null && typeof value === 'object' && !Array.isArray(value)
            ? value
            : null;
    }
    function primitive(value, path) {
        if (value === null ||
            typeof value === 'string' ||
            typeof value === 'boolean' ||
            (typeof value === 'number' && Number.isFinite(value))) {
            return value;
        }
        return fail$1(path, `${path} must be a finite JSON primitive.`);
    }
    function boundedInteger$1(value, fallback, maximum, path) {
        const resolved = value === undefined ? fallback : value;
        if (typeof resolved !== 'number' ||
            !Number.isInteger(resolved) ||
            resolved < 0 ||
            resolved > maximum) {
            return fail$1(path, `${path} must be an integer from 0 to ${maximum}.`);
        }
        return resolved;
    }
    function normalizeTableRuntimeState(value, fallback = {
        filters: [],
        sort: [],
        group: null,
        pivot: null,
        windowOffset: 0,
        windowLimit: 100,
        columnOffset: 0,
        columnLimit: 100,
    }) {
        const rawFilters = value.filters ?? fallback.filters;
        if (!Array.isArray(rawFilters) || rawFilters.length > MAX_TABLE_FILTERS) {
            return fail$1('$.filters', `Table runtime filters are limited to ${MAX_TABLE_FILTERS} entries.`);
        }
        const filters = rawFilters.map((candidate, index) => {
            const item = object$1(candidate);
            if (item === null)
                return fail$1(`$.filters[${index}]`, 'Table filters must be objects.');
            const operator = item.operator;
            if (operator !== 'equals' &&
                operator !== 'not-equals' &&
                operator !== 'contains' &&
                operator !== 'greater' &&
                operator !== 'greater-or-equal' &&
                operator !== 'less' &&
                operator !== 'less-or-equal') {
                return fail$1(`$.filters[${index}].operator`, 'Unknown table filter operator.');
            }
            const resolvedField = field$1(item.field, `$.filters[${index}].field`);
            const resolvedValue = primitive(item.value, `$.filters[${index}].value`);
            if (operator === 'greater' ||
                operator === 'greater-or-equal' ||
                operator === 'less' ||
                operator === 'less-or-equal') {
                if (typeof resolvedValue !== 'number') {
                    return fail$1(`$.filters[${index}].value`, 'Numeric table filters require a number.');
                }
                return { field: resolvedField, operator, value: resolvedValue };
            }
            return { field: resolvedField, operator, value: resolvedValue };
        });
        const rawSort = value.sort ?? fallback.sort;
        if (!Array.isArray(rawSort) || rawSort.length > MAX_TABLE_SORTS) {
            return fail$1('$.sort', `Table runtime sort is limited to ${MAX_TABLE_SORTS} fields.`);
        }
        const sort = rawSort.map((candidate, index) => {
            const item = object$1(candidate);
            if (item === null)
                return fail$1(`$.sort[${index}]`, 'Table sort entries must be objects.');
            const direction = item.direction ?? 'ascending';
            if (direction !== 'ascending' && direction !== 'descending') {
                return fail$1(`$.sort[${index}].direction`, 'Table sort direction is invalid.');
            }
            return { field: field$1(item.field, `$.sort[${index}].field`), direction };
        });
        const rawGroup = value.group === undefined ? fallback.group : value.group;
        let group = null;
        if (rawGroup !== null) {
            const candidate = object$1(rawGroup);
            if (candidate === null)
                return fail$1('$.group', 'Table group must be an object or null.');
            if (!Array.isArray(candidate.fields) || candidate.fields.length > MAX_TABLE_GROUP_FIELDS) {
                return fail$1('$.group.fields', `Table groups are limited to ${MAX_TABLE_GROUP_FIELDS} fields.`);
            }
            if (!Array.isArray(candidate.aggregates) ||
                candidate.aggregates.length > MAX_TABLE_AGGREGATES) {
                return fail$1('$.group.aggregates', `Table groups are limited to ${MAX_TABLE_AGGREGATES} aggregates.`);
            }
            group = {
                fields: candidate.fields.map((entry, index) => field$1(entry, `$.group.fields[${index}]`)),
                aggregates: candidate.aggregates.map((entry, index) => {
                    const aggregate = object$1(entry);
                    if (aggregate === null) {
                        return fail$1(`$.group.aggregates[${index}]`, 'Table aggregates must be objects.');
                    }
                    const op = aggregate.op;
                    if (op !== 'count' && op !== 'sum' && op !== 'mean' && op !== 'min' && op !== 'max') {
                        return fail$1(`$.group.aggregates[${index}].op`, 'Unknown table aggregate operation.');
                    }
                    return {
                        field: field$1(aggregate.field, `$.group.aggregates[${index}].field`),
                        op,
                        as: field$1(aggregate.as, `$.group.aggregates[${index}].as`),
                    };
                }),
            };
        }
        const rawPivot = value.pivot === undefined ? fallback.pivot : value.pivot;
        let pivot = null;
        if (rawPivot !== null) {
            const candidate = object$1(rawPivot);
            if (candidate === null)
                return fail$1('$.pivot', 'Table pivot must be an object or null.');
            const op = candidate.op ?? 'sum';
            if (op !== 'count' && op !== 'sum' && op !== 'mean') {
                return fail$1('$.pivot.op', 'Unknown table pivot operation.');
            }
            pivot = {
                row: field$1(candidate.row, '$.pivot.row'),
                column: field$1(candidate.column, '$.pivot.column'),
                value: field$1(candidate.value, '$.pivot.value'),
                op,
            };
        }
        if (group !== null && pivot !== null) {
            return fail$1('$.pivot', 'Runtime table group and pivot controls are mutually exclusive.');
        }
        return {
            filters,
            sort,
            group,
            pivot,
            windowOffset: boundedInteger$1(value.windowOffset, fallback.windowOffset, 100_000, '$.windowOffset'),
            windowLimit: boundedInteger$1(value.windowLimit, fallback.windowLimit, MAX_TABLE_WINDOW, '$.windowLimit'),
            columnOffset: boundedInteger$1(value.columnOffset, fallback.columnOffset, MAX_TABLE_WINDOW, '$.columnOffset'),
            columnLimit: boundedInteger$1(value.columnLimit, fallback.columnLimit, MAX_TABLE_WINDOW, '$.columnLimit'),
        };
    }
    function normalizePositions(value, path) {
        const source = object$1(value);
        if (source === null)
            return {};
        const entries = Object.entries(source);
        if (entries.length > MAX_RUNTIME_POSITIONS) {
            return fail$1(path, `Runtime positions are limited to ${MAX_RUNTIME_POSITIONS} nodes.`);
        }
        return Object.fromEntries(entries.map(([rawId, rawPosition]) => {
            const nodeId = id(rawId, `${path}.${rawId}`);
            const position = object$1(rawPosition);
            if (position === null)
                return fail$1(`${path}.${rawId}`, 'Node position must be an object.');
            const x = position.x;
            const y = position.y;
            if (typeof x !== 'number' ||
                !Number.isFinite(x) ||
                x < 0 ||
                x > 1 ||
                typeof y !== 'number' ||
                !Number.isFinite(y) ||
                y < 0 ||
                y > 1) {
                return fail$1(`${path}.${rawId}`, 'Node positions must use finite normalized x/y values.');
            }
            const pinned = position.pinned;
            if (pinned !== undefined && typeof pinned !== 'boolean') {
                return fail$1(`${path}.${rawId}.pinned`, 'Node pinned state must be boolean.');
            }
            return [nodeId, { x, y, ...(pinned === undefined ? {} : { pinned }) }];
        }));
    }
    function normalizeIds(value, path) {
        if (!Array.isArray(value))
            return [];
        if (value.length > MAX_RUNTIME_COLLAPSED) {
            return fail$1(path, `Runtime collapsed ids are limited to ${MAX_RUNTIME_COLLAPSED}.`);
        }
        const values = value.map((entry, index) => id(entry, `${path}[${index}]`));
        if (new Set(values).size !== values.length)
            return fail$1(path, 'Runtime ids must be unique.');
        return values;
    }
    function normalizeLasso(value) {
        if (!Array.isArray(value))
            return [];
        if (value.length !== 0 && value.length < 3) {
            return fail$1('$.lasso', 'Network lasso must be empty or contain at least three points.');
        }
        if (value.length > MAX_NETWORK_LASSO_POINTS) {
            return fail$1('$.lasso', `Network lasso is limited to ${MAX_NETWORK_LASSO_POINTS} points.`);
        }
        return value.map((entry, index) => {
            const point = object$1(entry);
            const x = point?.x;
            const y = point?.y;
            if (typeof x !== 'number' ||
                !Number.isFinite(x) ||
                x < 0 ||
                x > 1 ||
                typeof y !== 'number' ||
                !Number.isFinite(y) ||
                y < 0 ||
                y > 1) {
                return fail$1(`$.lasso[${index}]`, 'Lasso points must use finite normalized x/y values.');
            }
            return { x, y };
        });
    }
    function normalizeNetworkRuntimeState(value, fallback = { positions: {}, collapsed: [], lasso: [] }) {
        return {
            positions: normalizePositions(value.positions ?? fallback.positions, '$.positions'),
            collapsed: normalizeIds(value.collapsed ?? fallback.collapsed, '$.collapsed'),
            lasso: normalizeLasso(value.lasso ?? fallback.lasso),
        };
    }
    function normalizeFlowRuntimeState(value, fallback = { positions: {} }) {
        return { positions: normalizePositions(value.positions ?? fallback.positions, '$.positions') };
    }
    /** Converts a validated runtime state to an authored mark-options fragment. */
    function tableRuntimeOptions(state) {
        return {
            filters: state.filters,
            sort: state.sort,
            group: state.group,
            pivot: state.pivot,
            windowOffset: state.windowOffset,
            windowLimit: state.windowLimit,
            columnOffset: state.columnOffset,
            columnLimit: state.columnLimit,
        };
    }
    function networkRuntimeOptions(state) {
        return {
            positions: state.positions,
            collapsed: state.collapsed,
            lasso: state.lasso,
        };
    }
    function flowRuntimeOptions(state) {
        return { positions: state.positions };
    }

    const MAX_HIERARCHY_IDS = 2_048;
    const MAX_PARALLEL_AXES = 32;
    const MAX_PARALLEL_BRUSHES = 64;
    const MAX_MATRIX_ROWS = 100_000;
    const MAX_HEATMAP_KEYS = 4_096;
    function fail(path, message) {
        throw new GraflumeError('INVALID_SPEC', message, { path });
    }
    function object(value) {
        return value !== null && typeof value === 'object' && !Array.isArray(value)
            ? value
            : null;
    }
    function finite$2(value, path) {
        if (typeof value !== 'number' || !Number.isFinite(value)) {
            return fail(path, `${path} must be a finite number.`);
        }
        return value;
    }
    function field(value, path) {
        if (typeof value !== 'string' ||
            value.trim() === '' ||
            value.length > 128 ||
            value === '__proto__' ||
            value === 'prototype' ||
            value === 'constructor') {
            return fail(path, `${path} must be a safe non-empty field name of at most 128 characters.`);
        }
        return value;
    }
    function identifier(value, path) {
        if (typeof value !== 'string' || value.trim() === '' || value.length > 256) {
            return fail(path, `${path} must be a non-empty id of at most 256 characters.`);
        }
        return value;
    }
    function boundedString(value, path, maximum = 512) {
        if (typeof value !== 'string' || value.length > maximum) {
            return fail(path, `${path} must be a string of at most ${maximum} characters.`);
        }
        return value;
    }
    function normalizedExtent(value, path) {
        if (!Array.isArray(value) || value.length !== 2) {
            return fail(path, `${path} must contain two normalized numbers.`);
        }
        const first = finite$2(value[0], `${path}[0]`);
        const second = finite$2(value[1], `${path}[1]`);
        if (first < 0 || first > 1 || second < 0 || second > 1) {
            return fail(path, `${path} values must be inside the inclusive 0..1 range.`);
        }
        return [Math.min(first, second), Math.max(first, second)];
    }
    function uniqueIds(value, path, maximum) {
        if (!Array.isArray(value) || value.length > maximum) {
            return fail(path, `${path} must be an array with at most ${maximum} ids.`);
        }
        const ids = value.map((entry, index) => identifier(entry, `${path}[${index}]`));
        if (new Set(ids).size !== ids.length)
            return fail(path, `${path} ids must be unique.`);
        return ids;
    }
    function normalizeNavigatorRuntimeState(value, fallback, bounds) {
        const minimum = bounds === undefined ? Number.NEGATIVE_INFINITY : finite$2(bounds.minimum, '$.minimum');
        const maximum = bounds === undefined ? Number.POSITIVE_INFINITY : finite$2(bounds.maximum, '$.maximum');
        if (minimum >= maximum)
            return fail('$.maximum', 'Navigator maximum must be greater than minimum.');
        const start = Math.max(minimum, Math.min(maximum, finite$2(value.start ?? fallback.start, '$.start')));
        const end = Math.max(minimum, Math.min(maximum, finite$2(value.end ?? fallback.end, '$.end')));
        if (end <= start)
            return fail('$.end', 'Navigator end must be greater than start.');
        return { start, end };
    }
    function navigatorRuntimeOptions(family, state) {
        return family === 'candlestick'
            ? { navigatorStart: Math.floor(state.start), navigatorEnd: Math.ceil(state.end) }
            : { domain: [state.start, state.end] };
    }
    function normalizeHierarchyRuntimeState(value, fallback = {
        root: null,
        zoomTo: null,
        collapsed: [],
        query: '',
    }) {
        const rawRoot = value.root === undefined ? fallback.root : value.root;
        const rawZoom = value.zoomTo === undefined ? fallback.zoomTo : value.zoomTo;
        return {
            root: rawRoot === null ? null : identifier(rawRoot, '$.root'),
            zoomTo: rawZoom === null ? null : identifier(rawZoom, '$.zoomTo'),
            collapsed: uniqueIds(value.collapsed ?? fallback.collapsed, '$.collapsed', MAX_HIERARCHY_IDS),
            query: boundedString(value.query ?? fallback.query, '$.query'),
        };
    }
    function hierarchyRuntimeOptions(state) {
        return {
            root: state.root,
            zoomTo: state.zoomTo,
            collapsed: state.collapsed,
            query: state.query,
        };
    }
    function normalizeParallelAxis(value, index) {
        const axis = object(value);
        if (axis === null)
            return fail(`$.axes[${index}]`, 'Parallel axes must be objects.');
        const type = axis.type ?? 'linear';
        if (type !== 'linear' && type !== 'log' && type !== 'ordinal') {
            return fail(`$.axes[${index}].type`, 'Parallel axis type is invalid.');
        }
        const missing = axis.missing ?? 'gap';
        if (missing !== 'gap' && missing !== 'top' && missing !== 'bottom' && missing !== 'middle') {
            return fail(`$.axes[${index}].missing`, 'Parallel missing-value routing is invalid.');
        }
        let domain;
        if (axis.domain !== undefined) {
            if (!Array.isArray(axis.domain) || axis.domain.length === 0 || axis.domain.length > 4_096) {
                return fail(`$.axes[${index}].domain`, 'Parallel axis domain must contain 1..4096 values.');
            }
            domain = axis.domain.map((entry, domainIndex) => {
                if (typeof entry === 'string')
                    return entry;
                return finite$2(entry, `$.axes[${index}].domain[${domainIndex}]`);
            });
        }
        if (axis.invert !== undefined && typeof axis.invert !== 'boolean') {
            return fail(`$.axes[${index}].invert`, 'Parallel axis invert must be boolean.');
        }
        return {
            field: field(axis.field, `$.axes[${index}].field`),
            type,
            invert: axis.invert === true,
            missing,
            ...(domain === undefined ? {} : { domain }),
        };
    }
    function normalizeParallelBrush(value, index) {
        const brush = object(value);
        if (brush === null)
            return fail(`$.brushes[${index}]`, 'Parallel brushes must be objects.');
        if (!Array.isArray(brush.extents) || brush.extents.length === 0 || brush.extents.length > 16) {
            return fail(`$.brushes[${index}].extents`, 'A parallel brush needs 1..16 extents.');
        }
        return {
            field: field(brush.field, `$.brushes[${index}].field`),
            extents: brush.extents.map((extent, extentIndex) => normalizedExtent(extent, `$.brushes[${index}].extents[${extentIndex}]`)),
        };
    }
    function normalizeParallelRuntimeState(value, fallback) {
        const rawAxes = value.axes ?? fallback.axes;
        if (!Array.isArray(rawAxes) || rawAxes.length < 2 || rawAxes.length > MAX_PARALLEL_AXES) {
            return fail('$.axes', `Parallel runtime requires 2..${MAX_PARALLEL_AXES} axes.`);
        }
        const axes = rawAxes.map(normalizeParallelAxis);
        if (new Set(axes.map(({ field }) => field)).size !== axes.length) {
            return fail('$.axes', 'Parallel axis fields must be unique.');
        }
        const rawBrushes = value.brushes ?? fallback.brushes;
        if (!Array.isArray(rawBrushes) || rawBrushes.length > MAX_PARALLEL_BRUSHES) {
            return fail('$.brushes', `Parallel runtime accepts at most ${MAX_PARALLEL_BRUSHES} brushes.`);
        }
        const brushes = rawBrushes.map(normalizeParallelBrush);
        const fields = new Set(axes.map(({ field }) => field));
        if (brushes.some((brush) => !fields.has(brush.field))) {
            return fail('$.brushes', 'Every parallel brush field must reference a runtime axis.');
        }
        const combine = value.combine ?? fallback.combine;
        if (combine !== 'union' && combine !== 'intersection') {
            return fail('$.combine', 'Parallel brush combine must be union or intersection.');
        }
        return { axes, brushes, combine };
    }
    function parallelRuntimeOptions(state) {
        return {
            axes: state.axes,
            brushes: state.brushes,
            combine: state.combine,
        };
    }
    function reorderParallelAxis(state, fieldName, index) {
        const current = state.axes.findIndex(({ field }) => field === fieldName);
        if (current < 0)
            return fail('$.field', `Parallel axis "${fieldName}" was not found.`);
        if (!Number.isInteger(index) || index < 0 || index >= state.axes.length) {
            return fail('$.index', 'Parallel axis destination index is outside the axis list.');
        }
        const axes = [...state.axes];
        const [axis] = axes.splice(current, 1);
        axes.splice(index, 0, axis);
        return { ...state, axes };
    }
    function invertParallelAxis(state, fieldName, invert) {
        let found = false;
        const axes = state.axes.map((axis) => {
            if (axis.field !== fieldName)
                return axis;
            found = true;
            return { ...axis, invert: invert ?? !axis.invert };
        });
        if (!found)
            return fail('$.field', `Parallel axis "${fieldName}" was not found.`);
        return { ...state, axes };
    }
    function setParallelBrushExtents(state, fieldName, extents) {
        if (!state.axes.some(({ field }) => field === fieldName)) {
            return fail('$.field', `Parallel axis "${fieldName}" was not found.`);
        }
        const retained = state.brushes.filter(({ field }) => field !== fieldName);
        const brushes = extents.length === 0
            ? retained
            : [...retained, normalizeParallelBrush({ field: fieldName, extents }, retained.length)];
        return normalizeParallelRuntimeState({ brushes }, state);
    }
    function heatmapKeys(value, path) {
        if (!Array.isArray(value) || value.length > MAX_HEATMAP_KEYS) {
            return fail(path, `${path} must contain at most ${MAX_HEATMAP_KEYS} row or column keys.`);
        }
        return value.map((entry, index) => {
            if (typeof entry === 'string')
                return entry;
            return finite$2(entry, `${path}[${index}]`);
        });
    }
    function normalizeHeatmapRuntimeState(value, fallback = { rows: [], columns: [] }) {
        const rawValue = value.value === undefined ? fallback.value : value.value;
        let extent;
        if (rawValue !== undefined) {
            if (!Array.isArray(rawValue) || rawValue.length !== 2) {
                return fail('$.value', 'Heatmap value brush must contain two finite numbers.');
            }
            const first = finite$2(rawValue[0], '$.value[0]');
            const second = finite$2(rawValue[1], '$.value[1]');
            extent = [Math.min(first, second), Math.max(first, second)];
        }
        return {
            rows: heatmapKeys(value.rows ?? fallback.rows, '$.rows'),
            columns: heatmapKeys(value.columns ?? fallback.columns, '$.columns'),
            ...(extent === undefined ? {} : { value: extent }),
        };
    }
    function heatmapRuntimeOptions(state) {
        return { brush: state };
    }
    function normalizeScatterMatrixRuntimeState(value, fallback) {
        const normalizeDomain = (candidate, previous, path) => {
            const input = candidate ?? previous;
            if (!Array.isArray(input) || input.length !== 2) {
                return fail(path, `${path} must contain two finite numbers.`);
            }
            const first = finite$2(input[0], `${path}[0]`);
            const second = finite$2(input[1], `${path}[1]`);
            if (first === second)
                return fail(path, `${path} must have a non-zero span.`);
            return [Math.min(first, second), Math.max(first, second)];
        };
        const rawRows = value.selectedRows ?? fallback.selectedRows;
        if (!Array.isArray(rawRows) || rawRows.length > MAX_MATRIX_ROWS) {
            return fail('$.selectedRows', `Scatter-matrix selection is limited to ${MAX_MATRIX_ROWS} rows.`);
        }
        const selectedRows = rawRows.map((entry, index) => {
            if (!Number.isInteger(entry) || entry < 0) {
                return fail(`$.selectedRows[${index}]`, 'Selected row identities must be non-negative integers.');
            }
            return entry;
        });
        return {
            xField: field(value.xField ?? fallback.xField, '$.xField'),
            yField: field(value.yField ?? fallback.yField, '$.yField'),
            x: normalizeDomain(value.x, fallback.x, '$.x'),
            y: normalizeDomain(value.y, fallback.y, '$.y'),
            selectedRows: [...new Set(selectedRows)].sort((left, right) => left - right),
        };
    }
    function scatterMatrixRuntimeOptions(state) {
        return { linkedBrush: state };
    }
    /** Computes stable row identities from a cell-local numeric brush. */
    function selectScatterMatrixRows(rows, brush) {
        const [x0, x1] = brush.x;
        const [y0, y1] = brush.y;
        return rows.flatMap((row, rowIndex) => {
            const x = row[brush.xField];
            const y = row[brush.yField];
            return typeof x === 'number' &&
                Number.isFinite(x) &&
                typeof y === 'number' &&
                Number.isFinite(y) &&
                x >= x0 &&
                x <= x1 &&
                y >= y0 &&
                y <= y1
                ? [rowIndex]
                : [];
        });
    }
    /** Converts a pointer rectangle in one matrix cell into numeric data-domain extents. */
    function scatterMatrixPointerBrush(cell, start, end) {
        const mapX = (pixel) => cell.xDomain[0] +
            ((Math.max(cell.plot.x, Math.min(cell.plot.x + cell.plot.width, pixel)) - cell.plot.x) /
                Math.max(1, cell.plot.width)) *
                (cell.xDomain[1] - cell.xDomain[0]);
        const mapY = (pixel) => cell.yDomain[0] +
            (1 -
                (Math.max(cell.plot.y, Math.min(cell.plot.y + cell.plot.height, pixel)) - cell.plot.y) /
                    Math.max(1, cell.plot.height)) *
                (cell.yDomain[1] - cell.yDomain[0]);
        const x = [mapX(start.x), mapX(end.x)].sort((left, right) => left - right);
        const y = [mapY(start.y), mapY(end.y)].sort((left, right) => left - right);
        return { xField: cell.xField, yField: cell.yField, x, y };
    }
    /** Converts a navigator pointer delta to a clamped translated domain window. */
    function translateNavigatorWindow(interaction, deltaPixels) {
        const span = interaction.end - interaction.start;
        const delta = (deltaPixels / Math.max(1, interaction.plot.width)) *
            (interaction.maximum - interaction.minimum);
        const start = Math.max(interaction.minimum, Math.min(interaction.maximum - span, interaction.start + delta));
        return { start, end: start + span };
    }

    class LegendController {
        #host = null;
        #surface = null;
        sync(host, layout, spec, view, actions) {
            if (layout === null || spec === false || !spec.interactive) {
                this.destroy();
                return;
            }
            if (this.#host !== host) {
                this.destroy();
                this.#host = host;
                const surface = document.createElement('div');
                surface.dataset.graflumeLegendControls = 'true';
                surface.setAttribute('role', 'group');
                surface.setAttribute('aria-label', spec.title ?? 'Chart legend');
                surface.style.position = 'absolute';
                surface.style.inset = '0';
                surface.style.zIndex = '18';
                surface.style.pointerEvents = 'none';
                surface.style.overflow = 'hidden';
                host.append(surface);
                this.#surface = surface;
            }
            const surface = this.#surface;
            if (surface === null)
                return;
            const focusedId = document.activeElement?.dataset?.graflumeLegendItem;
            surface.setAttribute('aria-label', spec.title ?? 'Chart legend');
            surface.replaceChildren();
            let focusTarget = null;
            for (const entry of layout.entries) {
                if (!entry.toggleable)
                    continue;
                const button = document.createElement('button');
                button.type = 'button';
                button.dataset.graflumeLegendItem = entry.id;
                button.setAttribute('aria-pressed', String(entry.visible));
                const action = entry.visible ? spec.labels.hide : spec.labels.show;
                button.setAttribute('aria-label', `${action} ${entry.label}`);
                button.title = `${action} ${entry.label}`;
                button.textContent = entry.label;
                button.style.position = 'absolute';
                button.style.left = `${entry.bounds.x * view.zoom + view.offsetX}px`;
                button.style.top = `${entry.bounds.y * view.zoom + view.offsetY}px`;
                button.style.width = `${Math.max(24, entry.bounds.width * view.zoom)}px`;
                button.style.height = `${Math.max(24, entry.bounds.height * view.zoom)}px`;
                button.style.padding = '0';
                button.style.border = '1px solid transparent';
                button.style.borderRadius = '6px';
                button.style.background = 'transparent';
                button.style.color = 'transparent';
                button.style.fontSize = '1px';
                button.style.cursor = 'pointer';
                button.style.pointerEvents = 'auto';
                button.addEventListener('pointerenter', () => {
                    button.style.background = 'rgba(79,70,229,.08)';
                });
                button.addEventListener('pointerleave', () => {
                    button.style.background = 'transparent';
                });
                button.addEventListener('focus', () => {
                    button.style.outline = '2px solid #4f46e5';
                    button.style.outlineOffset = '1px';
                });
                button.addEventListener('blur', () => {
                    button.style.outline = 'none';
                });
                button.addEventListener('click', () => actions.setVisible(entry.id, !entry.visible));
                surface.append(button);
                if (entry.id === focusedId)
                    focusTarget = button;
            }
            focusTarget?.focus();
        }
        destroy() {
            this.#surface?.remove();
            this.#surface = null;
            this.#host = null;
        }
    }

    function cloneTarget(target) {
        return {
            ...target,
            ...(Array.isArray(target.rowIndex) ? { rowIndex: [...target.rowIndex] } : {}),
            ...(target.values === undefined ? {} : { values: [...target.values] }),
        };
    }
    function cloneMarkLabelPosition(position) {
        return { ...position, target: cloneTarget(position.target) };
    }
    function cloneMarkLabelPositions(positions) {
        return positions.map(cloneMarkLabelPosition);
    }
    function targetKey(target) {
        return JSON.stringify({
            type: 'datum',
            ...(target.layerId === undefined ? {} : { layerId: target.layerId }),
            ...(target.rowIndex === undefined
                ? {}
                : {
                    rowIndex: Array.isArray(target.rowIndex)
                        ? [...target.rowIndex].sort((left, right) => left - right)
                        : target.rowIndex,
                }),
            ...(target.field === undefined ? {} : { field: target.field }),
            ...(target.value === undefined ? {} : { value: target.value }),
            ...(target.values === undefined
                ? {}
                : {
                    values: [...target.values].sort((left, right) => JSON.stringify(left).localeCompare(JSON.stringify(right))),
                }),
        });
    }
    function positionsKey(positions) {
        return JSON.stringify(positions.map((position) => ({
            target: targetKey(position.target),
            offsetX: position.offsetX ?? 0,
            offsetY: position.offsetY ?? 0,
            hidden: position.hidden ?? false,
        })));
    }
    function setMarkLabelOffset(positions, target, offsetX, offsetY, hidden = false) {
        const key = targetKey(target);
        const next = positions.filter((position) => targetKey(position.target) !== key);
        if (offsetX === 0 && offsetY === 0 && !hidden)
            return next.map(cloneMarkLabelPosition);
        return [
            ...next.map(cloneMarkLabelPosition),
            {
                target: cloneTarget(target),
                ...(offsetX === 0 ? {} : { offsetX }),
                ...(offsetY === 0 ? {} : { offsetY }),
                ...(hidden ? { hidden: true } : {}),
            },
        ];
    }
    /** Bounded snapshot history shared by pointer, keyboard, and host-driven label edits. */
    class MarkLabelHistory {
        #positions = [];
        #undo = [];
        #redo = [];
        #limit = 50;
        constructor(positions = [], limit = 50) {
            this.reset(positions, limit);
        }
        reset(positions, limit = this.#limit) {
            this.#positions = cloneMarkLabelPositions(positions);
            this.#undo = [];
            this.#redo = [];
            this.#limit = Math.max(1, Math.trunc(limit));
        }
        positions() {
            return cloneMarkLabelPositions(this.#positions);
        }
        replace(positions) {
            const next = cloneMarkLabelPositions(positions);
            if (positionsKey(next) === positionsKey(this.#positions))
                return false;
            this.#pushUndo(this.#positions);
            this.#positions = next;
            this.#redo = [];
            return true;
        }
        preview(positions) {
            const next = cloneMarkLabelPositions(positions);
            if (positionsKey(next) === positionsKey(this.#positions))
                return false;
            this.#positions = next;
            return true;
        }
        commit(previous) {
            if (positionsKey(previous) === positionsKey(this.#positions))
                return false;
            this.#pushUndo(previous);
            this.#redo = [];
            return true;
        }
        restore(positions) {
            this.#positions = cloneMarkLabelPositions(positions);
        }
        undo() {
            const previous = this.#undo.pop();
            if (previous === undefined)
                return false;
            this.#redo.push(cloneMarkLabelPositions(this.#positions));
            this.#positions = cloneMarkLabelPositions(previous);
            return true;
        }
        redo() {
            const next = this.#redo.pop();
            if (next === undefined)
                return false;
            this.#pushUndo(this.#positions);
            this.#positions = cloneMarkLabelPositions(next);
            return true;
        }
        canUndo() {
            return this.#undo.length > 0;
        }
        canRedo() {
            return this.#redo.length > 0;
        }
        #pushUndo(positions) {
            this.#undo.push(cloneMarkLabelPositions(positions));
            if (this.#undo.length > this.#limit)
                this.#undo.splice(0, this.#undo.length - this.#limit);
        }
    }
    function hitTestMarkLabel(entries, x, y, tolerance = 5, activeId) {
        const hits = entries.filter(({ bounds }) => x >= bounds.x - tolerance &&
            x <= bounds.x + bounds.width + tolerance &&
            y >= bounds.y - tolerance &&
            y <= bounds.y + bounds.height + tolerance);
        if (hits.length === 0)
            return null;
        return (hits.find(({ id }) => id === activeId) ??
            [...hits].sort((left, right) => left.bounds.width * left.bounds.height - right.bounds.width * right.bounds.height)[0] ??
            null);
    }
    function shiftedBounds(entry, offsetX, offsetY) {
        return {
            ...entry.bounds,
            x: entry.bounds.x + offsetX - entry.offsetX,
            y: entry.bounds.y + offsetY - entry.offsetY,
        };
    }
    /** Apply deterministic grid, mark-anchor, and plot-edge snapping in scene pixels. */
    function snapMarkLabelOffset(options) {
        if (options.snap === false)
            return { offsetX: options.offsetX, offsetY: options.offsetY };
        const { entry, snap } = options;
        let offsetX = options.offsetX;
        let offsetY = options.offsetY;
        if (snap.grid !== false) {
            offsetX = Math.round(offsetX / snap.grid) * snap.grid;
            offsetY = Math.round(offsetY / snap.grid) * snap.grid;
        }
        if (snap.marks) {
            const center = { x: entry.baseCenter.x + offsetX, y: entry.baseCenter.y + offsetY };
            let bestX = null;
            let bestY = null;
            for (const candidate of options.entries) {
                if (candidate.id === entry.id)
                    continue;
                const distanceX = Math.abs(candidate.anchor.x - center.x);
                const distanceY = Math.abs(candidate.anchor.y - center.y);
                if (distanceX <= snap.distance && (bestX === null || distanceX < bestX.distance)) {
                    bestX = { distance: distanceX, value: candidate.anchor.x - entry.baseCenter.x };
                }
                if (distanceY <= snap.distance && (bestY === null || distanceY < bestY.distance)) {
                    bestY = { distance: distanceY, value: candidate.anchor.y - entry.baseCenter.y };
                }
            }
            if (bestX !== null)
                offsetX = bestX.value;
            if (bestY !== null)
                offsetY = bestY.value;
        }
        if (snap.plot) {
            let bounds = shiftedBounds(entry, offsetX, offsetY);
            const leftGap = bounds.x - options.plot.x;
            const rightGap = options.plot.x + options.plot.width - (bounds.x + bounds.width);
            const topGap = bounds.y - options.plot.y;
            const bottomGap = options.plot.y + options.plot.height - (bounds.y + bounds.height);
            if (Math.abs(leftGap) <= snap.distance)
                offsetX -= leftGap;
            else if (Math.abs(rightGap) <= snap.distance)
                offsetX += rightGap;
            if (Math.abs(topGap) <= snap.distance)
                offsetY -= topGap;
            else if (Math.abs(bottomGap) <= snap.distance)
                offsetY += bottomGap;
            bounds = shiftedBounds(entry, offsetX, offsetY);
            if (bounds.x < options.plot.x)
                offsetX += options.plot.x - bounds.x;
            if (bounds.x + bounds.width > options.plot.x + options.plot.width) {
                offsetX -= bounds.x + bounds.width - (options.plot.x + options.plot.width);
            }
            if (bounds.y < options.plot.y)
                offsetY += options.plot.y - bounds.y;
            if (bounds.y + bounds.height > options.plot.y + options.plot.height) {
                offsetY -= bounds.y + bounds.height - (options.plot.y + options.plot.height);
            }
        }
        return { offsetX, offsetY };
    }

    const identityInspectionView = {
        zoom: 1,
        offsetX: 0,
        offsetY: 0,
    };
    function finite$1(value, fallback) {
        return Number.isFinite(value) ? value : fallback;
    }
    function constrainInspectionView(input, bounds) {
        const minZoom = Math.max(1, finite$1(bounds.minZoom, 1));
        const maxZoom = Math.max(minZoom, finite$1(bounds.maxZoom, 6));
        const zoom = Math.max(minZoom, Math.min(maxZoom, finite$1(input.zoom, minZoom)));
        const width = Math.max(1, finite$1(bounds.width, 1));
        const height = Math.max(1, finite$1(bounds.height, 1));
        return {
            zoom,
            offsetX: Math.max(width - width * zoom, Math.min(0, finite$1(input.offsetX, 0))),
            offsetY: Math.max(height - height * zoom, Math.min(0, finite$1(input.offsetY, 0))),
        };
    }
    function zoomInspectionView(current, factor, anchor, bounds) {
        if (!Number.isFinite(factor) || factor <= 0)
            throw new RangeError('Zoom factor must be > 0.');
        const nextZoom = Math.max(bounds.minZoom, Math.min(bounds.maxZoom, current.zoom * factor));
        const ratio = nextZoom / current.zoom;
        return constrainInspectionView({
            zoom: nextZoom,
            offsetX: anchor.x - (anchor.x - current.offsetX) * ratio,
            offsetY: anchor.y - (anchor.y - current.offsetY) * ratio,
        }, bounds);
    }
    function panInspectionView(current, deltaX, deltaY, bounds) {
        if (!Number.isFinite(deltaX) || !Number.isFinite(deltaY)) {
            throw new RangeError('Pan deltas must be finite numbers.');
        }
        return constrainInspectionView({
            ...current,
            offsetX: current.offsetX + deltaX,
            offsetY: current.offsetY + deltaY,
        }, bounds);
    }
    function inverseInspectionPoint(view, point) {
        return {
            x: (point.x - view.offsetX) / view.zoom,
            y: (point.y - view.offsetY) / view.zoom,
        };
    }

    function playbackFrameKey(value) {
        if (value instanceof Date)
            return `date:${value.getTime()}`;
        return `${typeof value}:${String(value)}`;
    }
    function markType(mark) {
        return typeof mark === 'string' ? mark : mark?.type;
    }
    function tableRows(input) {
        if (Array.isArray(input))
            return input;
        const table = DataTable.from(input);
        return Array.from({ length: table.length }, (_, index) => table.row(index));
    }
    function matchingLayer(requestedLayerId, layerId) {
        return requestedLayerId === undefined || requestedLayerId === layerId;
    }
    function collectPlaybackFrames(spec, playback) {
        const frames = new Map();
        const add = (input) => {
            if (input === undefined)
                return;
            const table = DataTable.from(input);
            if (!table.has(playback.field))
                return;
            for (let index = 0; index < table.length; index += 1) {
                const value = table.value(index, playback.field);
                if (value === null || value === undefined)
                    continue;
                const key = playbackFrameKey(value);
                if (!frames.has(key))
                    frames.set(key, value);
            }
        };
        if (spec.layers === undefined) {
            if (matchingLayer(playback.layerId, 'layer-0'))
                add(spec.data);
        }
        else {
            spec.layers.forEach((layer, index) => {
                if (matchingLayer(playback.layerId, layer.id ?? `layer-${index}`)) {
                    add(layer.data ?? spec.data);
                }
            });
        }
        return [...frames.values()];
    }
    function resolvedRangeBound(reference, fallback, namedFrames, frameCount, path) {
        if (reference === undefined)
            return fallback;
        const index = typeof reference === 'string' ? namedFrames.get(reference)?.index : reference;
        if (index === undefined) {
            throw new GraflumeError('INVALID_SPEC', `Unknown playback frame name "${reference}".`, {
                path,
            });
        }
        if (!Number.isInteger(index) || index < 0 || index >= frameCount) {
            throw new GraflumeError('INVALID_SPEC', `Playback range index ${String(index)} is outside the available frame range.`, { path, details: { frameCount } });
        }
        return index;
    }
    /** Resolve portable names and inclusive range references against collected frame values. */
    function resolvePlaybackTimeline(frames, playback) {
        const frameIndices = new Map();
        frames.forEach((frame, index) => frameIndices.set(playbackFrameKey(frame), index));
        const namedFrames = [];
        const names = new Map();
        playback.namedFrames.forEach((namedFrame, authoredIndex) => {
            if (names.has(namedFrame.name)) {
                throw new GraflumeError('INVALID_SPEC', `Duplicate playback frame name "${namedFrame.name}".`, {
                    path: `$.interaction.playback.namedFrames[${authoredIndex}].name`,
                });
            }
            const index = frameIndices.get(playbackFrameKey(namedFrame.value));
            if (index === undefined) {
                throw new GraflumeError('INVALID_SPEC', `Named playback frame "${namedFrame.name}" does not match a collected frame value.`, {
                    path: `$.interaction.playback.namedFrames[${authoredIndex}].value`,
                    details: { value: namedFrame.value },
                });
            }
            const resolved = { ...namedFrame, index };
            namedFrames.push(resolved);
            names.set(namedFrame.name, resolved);
        });
        if (frames.length === 0) {
            if (playback.range !== false) {
                throw new GraflumeError('INVALID_SPEC', 'Playback range cannot be resolved because the selected source has no frames.', { path: '$.interaction.playback.range' });
            }
            return { namedFrames, range: { start: 0, end: -1 } };
        }
        const authoredRange = playback.range === false ? {} : playback.range;
        const start = resolvedRangeBound(authoredRange.start, 0, names, frames.length, '$.interaction.playback.range.start');
        const end = resolvedRangeBound(authoredRange.end, frames.length - 1, names, frames.length, '$.interaction.playback.range.end');
        if (start > end) {
            throw new GraflumeError('INVALID_SPEC', 'Playback range start must not exceed its end.', {
                path: '$.interaction.playback.range',
                details: { start, end },
            });
        }
        return { namedFrames, range: { start, end } };
    }
    function visibleFrameKeys(frames, index, playback, rangeStart) {
        const start = playback.mode === 'frame'
            ? index
            : playback.mode === 'window'
                ? Math.max(rangeStart, index - playback.windowSize + 1)
                : rangeStart;
        return new Set(frames.slice(start, index + 1).map(playbackFrameKey));
    }
    function filterInput(input, field, visible) {
        return tableRows(input).filter((row) => visible.has(playbackFrameKey(row[field])));
    }
    function withMotionFrame(mark, field, frame) {
        const source = typeof mark === 'string' ? { type: mark } : mark;
        const options = { ...source.options };
        if (frame === undefined)
            delete options.frame;
        else
            options.frame = frame;
        return {
            ...source,
            fields: { ...source.fields, time: field },
            options: options,
        };
    }
    function playbackSpec(base, playback, frames, index, rangeStart = 0) {
        const frame = frames[index];
        if (frame === undefined)
            return base;
        const visible = visibleFrameKeys(frames, index, playback, rangeStart);
        if (base.layers === undefined) {
            if (!matchingLayer(playback.layerId, 'layer-0') || base.mark === undefined)
                return base;
            const motion = markType(base.mark) === 'motion';
            const filter = playback.filter || (motion && playback.mode !== 'frame');
            return {
                ...base,
                ...(filter && base.data !== undefined
                    ? { data: filterInput(base.data, playback.field, visible) }
                    : {}),
                ...(motion
                    ? {
                        mark: withMotionFrame(base.mark, playback.field, playback.mode === 'frame' ? frame : undefined),
                    }
                    : {}),
            };
        }
        return {
            ...base,
            layers: base.layers.map((layer, layerIndex) => {
                const layerId = layer.id ?? `layer-${layerIndex}`;
                if (!matchingLayer(playback.layerId, layerId))
                    return layer;
                const source = layer.data ?? base.data;
                if (source === undefined || !DataTable.from(source).has(playback.field))
                    return layer;
                const motion = markType(layer.mark) === 'motion';
                const filter = playback.filter || (motion && playback.mode !== 'frame');
                return {
                    ...layer,
                    ...(filter ? { data: filterInput(source, playback.field, visible) } : {}),
                    ...(motion
                        ? {
                            mark: withMotionFrame(layer.mark, playback.field, playback.mode === 'frame' ? frame : undefined),
                        }
                        : {}),
                };
            }),
        };
    }

    let nextId = 0;
    function createId(prefix) {
        nextId += 1;
        return `${prefix}-${nextId.toString(36)}`;
    }

    const inferredFieldLimit = 8;
    const tooltipTextLimit = 240;
    function hasOwn(value, field) {
        return Object.prototype.hasOwnProperty.call(value, field);
    }
    function boundedText(value, limit = tooltipTextLimit) {
        const text = String(value);
        return text.length <= limit ? text : `${text.slice(0, Math.max(0, limit - 1))}…`;
    }
    function humanizeField(field) {
        return field
            .replace(/([a-z0-9])([A-Z])/g, '$1 $2')
            .replace(/[-_]+/g, ' ')
            .replace(/^\w/, (letter) => letter.toUpperCase());
    }
    function inferredFormat(field, layer) {
        const encoding = layer?.x.field === field ? layer.x : layer?.y.field === field ? layer.y : undefined;
        return encoding?.type === 'temporal' ? 'date' : 'auto';
    }
    function inferredFields(hit, spec) {
        const layer = spec.layers.find(({ id }) => id === hit.layerId);
        const fields = new Map();
        const add = (field, label) => {
            if (typeof field !== 'string' || field.length === 0 || fields.has(field))
                return;
            fields.set(field, {
                field,
                label: label ?? humanizeField(field),
                format: inferredFormat(field, layer),
                prefix: '',
                suffix: '',
            });
        };
        if (layer !== undefined) {
            add(layer.x.field, layer.x.title);
            add(layer.y.field, layer.y.title);
            for (const [channel, field] of Object.entries(layer.mark.fields))
                add(field, humanizeField(channel));
            for (const option of ['fields', 'dimensions', 'columns']) {
                const values = layer.mark.options[option];
                if (Array.isArray(values))
                    for (const field of values)
                        add(field);
            }
        }
        for (const field of Object.keys(hit.tooltip ?? {}))
            add(field);
        for (const field of Object.keys(hit.datum))
            add(field);
        return [...fields.values()].slice(0, inferredFieldLimit);
    }
    function finiteFractionDigits(value) {
        return value === undefined ? undefined : Math.max(0, Math.min(6, Math.trunc(value)));
    }
    function numberFormatter(locale, options) {
        try {
            return new Intl.NumberFormat(locale, options);
        }
        catch {
            return new Intl.NumberFormat(undefined, options);
        }
    }
    function dateFormatter(locale, options) {
        try {
            return new Intl.DateTimeFormat(locale, options);
        }
        catch {
            return new Intl.DateTimeFormat(undefined, options);
        }
    }
    function dateOnlyValue(value) {
        if (typeof value !== 'string')
            return null;
        const match = /^(\d{4})-(\d{2})-(\d{2})$/.exec(value);
        if (match === null)
            return null;
        const year = Number(match[1]);
        const month = Number(match[2]);
        const day = Number(match[3]);
        const date = new Date(Date.UTC(year, month - 1, day));
        return date.getUTCFullYear() === year &&
            date.getUTCMonth() === month - 1 &&
            date.getUTCDate() === day
            ? date
            : null;
    }
    function formatValue(value, field, locale) {
        if (value === null || value === undefined)
            return '—';
        const fractionDigits = finiteFractionDigits(field.fractionDigits);
        let formatted;
        if (field.format === 'date' || field.format === 'datetime') {
            const dateOnly = field.format === 'date' ? dateOnlyValue(value) : null;
            const date = dateOnly ?? (value instanceof Date ? value : new Date(String(value)));
            formatted = Number.isFinite(date.getTime())
                ? dateFormatter(locale, field.format === 'datetime'
                    ? { dateStyle: 'medium', timeStyle: 'short' }
                    : { dateStyle: 'medium', ...(dateOnly === null ? {} : { timeZone: 'UTC' }) }).format(date)
                : String(value);
        }
        else if (typeof value === 'number' && Number.isFinite(value)) {
            const options = field.format === 'percent'
                ? {
                    style: 'percent',
                    maximumFractionDigits: fractionDigits ?? 1,
                    ...(fractionDigits === undefined ? {} : { minimumFractionDigits: fractionDigits }),
                }
                : field.format === 'integer'
                    ? { maximumFractionDigits: 0 }
                    : {
                        maximumFractionDigits: fractionDigits ?? 3,
                        ...(fractionDigits === undefined ? {} : { minimumFractionDigits: fractionDigits }),
                    };
            formatted = numberFormatter(locale, options).format(value);
        }
        else {
            formatted = value instanceof Date ? value.toISOString() : boundedText(value);
        }
        return boundedText(`${field.prefix}${formatted}${field.suffix}`);
    }
    function datumValue(hit, field) {
        if (hit.tooltip !== undefined)
            return hit.tooltip[field];
        return hit.datum[field];
    }
    function hasDatumValue(hit, field) {
        return hit.tooltip === undefined ? hasOwn(hit.datum, field) : hasOwn(hit.tooltip, field);
    }
    function resolveTooltipContent(hit, spec) {
        const configured = spec.interaction.tooltip;
        if (configured === false)
            return { title: '', rows: [] };
        const layer = spec.layers.find(({ id }) => id === hit.layerId);
        const fields = configured.fields.length > 0 ? configured.fields : inferredFields(hit, spec);
        const rows = fields
            .filter((field) => hasDatumValue(hit, field.field))
            .map((field) => {
            const format = field.format === 'auto' && inferredFormat(field.field, layer) === 'date'
                ? 'date'
                : field.format;
            const resolvedField = format === field.format ? field : { ...field, format };
            return {
                field: field.field,
                label: boundedText(field.label, 80),
                value: formatValue(datumValue(hit, field.field), resolvedField, spec.locale),
            };
        });
        return {
            title: boundedText(configured.title ?? spec.title?.text ?? humanizeField(layer?.mark.type ?? 'Datum'), 120),
            rows,
        };
    }
    class TooltipController {
        #id = createId('graflume-tooltip');
        #element = null;
        #surface = null;
        #nodeId = '';
        show(content, hit, sourceEvent, surface, host) {
            this.showAt(content, hit, sourceEvent.clientX, sourceEvent.clientY, surface, host);
        }
        /** Show the same text-only tooltip for keyboard or other non-pointer semantic focus. */
        showAt(content, hit, clientX, clientY, surface, host) {
            if (content.rows.length === 0) {
                this.hide();
                return;
            }
            const element = this.#ensureElement(host);
            if (this.#nodeId !== hit.nodeId) {
                this.#nodeId = hit.nodeId;
                this.#renderContent(element, content);
            }
            this.#surface = surface;
            const describedBy = new Set((surface.getAttribute('aria-describedby') ?? '').split(/\s+/).filter(Boolean));
            describedBy.add(this.#id);
            surface.setAttribute('aria-describedby', [...describedBy].join(' '));
            element.hidden = false;
            this.#position(element, host, clientX, clientY);
        }
        hide() {
            this.#nodeId = '';
            if (this.#element !== null)
                this.#element.hidden = true;
            if (this.#surface !== null) {
                const describedBy = (this.#surface.getAttribute('aria-describedby') ?? '')
                    .split(/\s+/)
                    .filter((id) => id !== '' && id !== this.#id);
                if (describedBy.length === 0)
                    this.#surface.removeAttribute('aria-describedby');
                else
                    this.#surface.setAttribute('aria-describedby', describedBy.join(' '));
            }
            this.#surface = null;
        }
        destroy() {
            this.hide();
            this.#element?.remove();
            this.#element = null;
        }
        #ensureElement(host) {
            if (this.#element !== null) {
                if (this.#element.parentElement !== host)
                    host.append(this.#element);
                return this.#element;
            }
            const element = host.ownerDocument.createElement('div');
            element.id = this.#id;
            element.dataset.graflumeTooltip = 'true';
            element.setAttribute('role', 'tooltip');
            element.setAttribute('dir', 'auto');
            element.hidden = true;
            element.style.cssText =
                'position:absolute;z-index:20;max-width:min(280px,calc(100% - 24px));padding:10px 12px;pointer-events:none;color:var(--graflume-tooltip-color,#f8fafc);background:var(--graflume-tooltip-background,rgba(15,23,42,.96));border:1px solid var(--graflume-tooltip-border,rgba(148,163,184,.35));border-radius:10px;box-shadow:0 12px 30px rgba(15,23,42,.24);font:12px/1.45 ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;white-space:normal';
            host.append(element);
            this.#element = element;
            return element;
        }
        #renderContent(element, content) {
            const ownerDocument = element.ownerDocument;
            const title = ownerDocument.createElement('div');
            title.textContent = content.title;
            title.style.cssText = 'margin:0 0 6px;font-weight:700;color:#fff';
            const list = ownerDocument.createElement('dl');
            list.style.cssText =
                'display:grid;grid-template-columns:minmax(56px,auto) minmax(0,1fr);gap:3px 12px;margin:0';
            for (const row of content.rows) {
                const term = ownerDocument.createElement('dt');
                term.textContent = row.label;
                term.style.cssText = 'margin:0;color:#cbd5e1';
                const detail = ownerDocument.createElement('dd');
                detail.textContent = row.value;
                detail.style.cssText =
                    'margin:0;text-align:end;font-weight:650;color:#fff;overflow-wrap:anywhere';
                list.append(term, detail);
            }
            element.replaceChildren(title, list);
        }
        #position(element, host, clientX, clientY) {
            const margin = 8;
            const offset = 12;
            const hostBounds = host.getBoundingClientRect();
            const bounds = element.getBoundingClientRect();
            const localX = clientX - hostBounds.left;
            const localY = clientY - hostBounds.top;
            let left = localX + offset;
            let top = localY + offset;
            if (left + bounds.width + margin > hostBounds.width)
                left = localX - bounds.width - offset;
            if (top + bounds.height + margin > hostBounds.height)
                top = localY - bounds.height - offset;
            element.style.left = `${Math.max(margin, Math.min(left, hostBounds.width - bounds.width - margin))}px`;
            element.style.top = `${Math.max(margin, Math.min(top, hostBounds.height - bounds.height - margin))}px`;
        }
    }

    function clamp$1(value, minimum, maximum) {
        return Math.max(minimum, Math.min(maximum, value));
    }
    function mix(from, to, progress) {
        return from + (to - from) * progress;
    }
    function easeSceneProgress(progress, easing) {
        if (!Number.isFinite(progress))
            throw new RangeError('Scene transition progress must be finite.');
        const value = clamp$1(progress, 0, 1);
        return easing === 'ease-in-out' ? value * value * (3 - 2 * value) : value;
    }
    function mixRect(from, to, progress) {
        return {
            x: mix(from.x, to.x, progress),
            y: mix(from.y, to.y, progress),
            width: mix(from.width, to.width, progress),
            height: mix(from.height, to.height, progress),
        };
    }
    function mixPoints(from, to, progress) {
        return to.map((point, index) => {
            const previous = from[index];
            return {
                x: mix(previous.x, point.x, progress),
                y: mix(previous.y, point.y, progress),
            };
        });
    }
    function parseHexColor(input) {
        const value = input.slice(1);
        if (![3, 4, 6, 8].includes(value.length) || !/^[0-9a-f]+$/i.test(value))
            return null;
        const expanded = value.length <= 4 ? [...value].map((character) => `${character}${character}`).join('') : value;
        const red = Number.parseInt(expanded.slice(0, 2), 16);
        const green = Number.parseInt(expanded.slice(2, 4), 16);
        const blue = Number.parseInt(expanded.slice(4, 6), 16);
        const alpha = expanded.length === 8 ? Number.parseInt(expanded.slice(6, 8), 16) / 255 : 1;
        return { red, green, blue, alpha };
    }
    function parseRgbChannel(input) {
        const value = input.trim();
        const percentage = value.endsWith('%');
        const parsed = Number.parseFloat(percentage ? value.slice(0, -1) : value);
        if (!Number.isFinite(parsed))
            return null;
        return clamp$1(percentage ? (parsed / 100) * 255 : parsed, 0, 255);
    }
    function parseAlpha(input) {
        const value = input.trim();
        const percentage = value.endsWith('%');
        const parsed = Number.parseFloat(percentage ? value.slice(0, -1) : value);
        if (!Number.isFinite(parsed))
            return null;
        return clamp$1(percentage ? parsed / 100 : parsed, 0, 1);
    }
    function parseFunctionalColor(input) {
        const match = /^rgba?\((.*)\)$/i.exec(input.trim());
        if (match === null)
            return null;
        const parts = match[1]
            .trim()
            .replace(/\s*\/\s*/, ' ')
            .split(/[\s,]+/);
        if (parts.length !== 3 && parts.length !== 4)
            return null;
        const red = parseRgbChannel(parts[0]);
        const green = parseRgbChannel(parts[1]);
        const blue = parseRgbChannel(parts[2]);
        const alpha = parts[3] === undefined ? 1 : parseAlpha(parts[3]);
        if (red === null || green === null || blue === null || alpha === null)
            return null;
        return { red, green, blue, alpha };
    }
    function parseColor(input) {
        const value = input.trim();
        if (value.toLowerCase() === 'transparent')
            return { red: 0, green: 0, blue: 0, alpha: 0 };
        if (value.startsWith('#'))
            return parseHexColor(value);
        return parseFunctionalColor(value);
    }
    function colorCompatible(from, to) {
        if (from === to)
            return true;
        if (from === undefined || to === undefined)
            return false;
        return parseColor(from) !== null && parseColor(to) !== null;
    }
    function mixColor(from, to, progress) {
        if (from === to)
            return to;
        const left = parseColor(from);
        const right = parseColor(to);
        const red = Math.round(mix(left.red, right.red, progress));
        const green = Math.round(mix(left.green, right.green, progress));
        const blue = Math.round(mix(left.blue, right.blue, progress));
        const alpha = Math.round(mix(left.alpha, right.alpha, progress) * 1_000) / 1_000;
        return `rgba(${red}, ${green}, ${blue}, ${alpha})`;
    }
    function mixOptionalColor(from, to, progress) {
        if (from === undefined || to === undefined)
            return to;
        return mixColor(from, to, progress);
    }
    function normalizedDash(input) {
        return input ?? [];
    }
    function dashCompatible(from, to) {
        return normalizedDash(from).length === normalizedDash(to).length;
    }
    function mixDash(from, to, progress) {
        const left = normalizedDash(from);
        const right = normalizedDash(to);
        if (right.length === 0)
            return undefined;
        return right.map((value, index) => mix(left[index], value, progress));
    }
    function effectiveOpacity(node) {
        return node.visible ? node.opacity : 0;
    }
    function baseTransition(from, to, progress) {
        const opacity = mix(effectiveOpacity(from), effectiveOpacity(to), progress);
        return {
            id: to.id,
            zIndex: to.zIndex,
            opacity,
            visible: opacity > 0,
        };
    }
    function scalarKey(value) {
        if (value instanceof Date)
            return `date:${value.getTime()}`;
        if (value === null)
            return 'null';
        if (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean') {
            return `${typeof value}:${String(value)}`;
        }
        return null;
    }
    function stableDatumIdentity(node, keyField) {
        const datum = node.datum;
        if (keyField !== undefined && datum !== undefined) {
            const key = scalarKey(datum.datum[keyField] ?? datum.tooltip?.[keyField]);
            if (key !== null) {
                return `datum:${node.type}:${datum.layerId}:${key}`;
            }
        }
        return null;
    }
    function nodeIdentity(node, keyField) {
        return stableDatumIdentity(node, keyField) ?? `node:${node.type}:${node.id}`;
    }
    function stableDatumRoleCandidates(node) {
        if (node.datum === undefined)
            return new Set();
        const parts = node.id.split(':');
        const rowIndex = String(node.datum.rowIndex);
        const roles = new Set();
        for (let index = 0; index < parts.length; index += 1) {
            if (parts[index] !== rowIndex)
                continue;
            const candidate = [...parts];
            candidate[index] = '@datum';
            roles.add(candidate.join(':'));
        }
        return roles;
    }
    function sameStableDatumRole(from, to) {
        if (from.id === to.id)
            return true;
        const left = stableDatumRoleCandidates(from);
        const right = stableDatumRoleCandidates(to);
        return [...left].some((role) => right.has(role));
    }
    function samePathTopology(from, to) {
        if (from.closed !== to.closed || from.points.length !== to.points.length)
            return false;
        const left = from.subpaths ?? [];
        const right = to.subpaths ?? [];
        return (left.length === right.length &&
            left.every((subpath, index) => subpath.length === right[index].length));
    }
    function nodeCompatible(from, to) {
        if (from.type !== to.type)
            return false;
        switch (to.type) {
            case 'group': {
                const previous = from;
                return (previous.clip === undefined) === (to.clip === undefined);
            }
            case 'line': {
                const previous = from;
                return (previous.lineCap === to.lineCap &&
                    colorCompatible(previous.stroke, to.stroke) &&
                    dashCompatible(previous.dash, to.dash));
            }
            case 'path': {
                const previous = from;
                return (samePathTopology(previous, to) &&
                    previous.fillRule === to.fillRule &&
                    previous.lineCap === to.lineCap &&
                    previous.lineJoin === to.lineJoin &&
                    colorCompatible(previous.fill, to.fill) &&
                    colorCompatible(previous.stroke, to.stroke) &&
                    dashCompatible(previous.dash, to.dash));
            }
            case 'rect': {
                const previous = from;
                return (colorCompatible(previous.fill, to.fill) &&
                    colorCompatible(previous.stroke, to.stroke) &&
                    dashCompatible(previous.dash, to.dash));
            }
            case 'circle': {
                const previous = from;
                return colorCompatible(previous.fill, to.fill) && colorCompatible(previous.stroke, to.stroke);
            }
            case 'text': {
                const previous = from;
                return (previous.text === to.text &&
                    previous.fontFamily === to.fontFamily &&
                    previous.fontWeight === to.fontWeight &&
                    previous.fontStyle === to.fontStyle &&
                    previous.align === to.align &&
                    previous.baseline === to.baseline &&
                    colorCompatible(previous.fill, to.fill));
            }
        }
    }
    function interpolateCompatibleNode(from, to, progress, options) {
        switch (to.type) {
            case 'group': {
                const previous = from;
                return {
                    ...to,
                    ...baseTransition(previous, to, progress),
                    children: mergeChildren(previous.children, to.children, progress, options),
                    ...(previous.clip === undefined || to.clip === undefined
                        ? {}
                        : { clip: mixRect(previous.clip, to.clip, progress) }),
                };
            }
            case 'line': {
                const previous = from;
                const dash = mixDash(previous.dash, to.dash, progress);
                return {
                    ...to,
                    ...baseTransition(previous, to, progress),
                    x1: mix(previous.x1, to.x1, progress),
                    y1: mix(previous.y1, to.y1, progress),
                    x2: mix(previous.x2, to.x2, progress),
                    y2: mix(previous.y2, to.y2, progress),
                    stroke: mixColor(previous.stroke, to.stroke, progress),
                    lineWidth: mix(previous.lineWidth, to.lineWidth, progress),
                    ...(dash === undefined ? {} : { dash }),
                };
            }
            case 'path': {
                const previous = from;
                const leftSubpaths = previous.subpaths ?? [];
                const rightSubpaths = to.subpaths ?? [];
                const dash = mixDash(previous.dash, to.dash, progress);
                const fill = mixOptionalColor(previous.fill, to.fill, progress);
                const stroke = mixOptionalColor(previous.stroke, to.stroke, progress);
                return {
                    ...to,
                    ...baseTransition(previous, to, progress),
                    points: mixPoints(previous.points, to.points, progress),
                    ...(rightSubpaths.length === 0
                        ? {}
                        : {
                            subpaths: rightSubpaths.map((subpath, index) => mixPoints(leftSubpaths[index], subpath, progress)),
                        }),
                    lineWidth: mix(previous.lineWidth, to.lineWidth, progress),
                    ...(fill === undefined ? {} : { fill }),
                    ...(stroke === undefined ? {} : { stroke }),
                    ...(dash === undefined ? {} : { dash }),
                };
            }
            case 'rect': {
                const previous = from;
                const dash = mixDash(previous.dash, to.dash, progress);
                const fill = mixOptionalColor(previous.fill, to.fill, progress);
                const stroke = mixOptionalColor(previous.stroke, to.stroke, progress);
                return {
                    ...to,
                    ...baseTransition(previous, to, progress),
                    x: mix(previous.x, to.x, progress),
                    y: mix(previous.y, to.y, progress),
                    width: mix(previous.width, to.width, progress),
                    height: mix(previous.height, to.height, progress),
                    lineWidth: mix(previous.lineWidth, to.lineWidth, progress),
                    cornerRadius: mix(previous.cornerRadius, to.cornerRadius, progress),
                    ...(fill === undefined ? {} : { fill }),
                    ...(stroke === undefined ? {} : { stroke }),
                    ...(dash === undefined ? {} : { dash }),
                };
            }
            case 'circle': {
                const previous = from;
                const fill = mixOptionalColor(previous.fill, to.fill, progress);
                const stroke = mixOptionalColor(previous.stroke, to.stroke, progress);
                return {
                    ...to,
                    ...baseTransition(previous, to, progress),
                    cx: mix(previous.cx, to.cx, progress),
                    cy: mix(previous.cy, to.cy, progress),
                    radius: mix(previous.radius, to.radius, progress),
                    lineWidth: mix(previous.lineWidth, to.lineWidth, progress),
                    ...(fill === undefined ? {} : { fill }),
                    ...(stroke === undefined ? {} : { stroke }),
                };
            }
            case 'text': {
                const previous = from;
                return {
                    ...to,
                    ...baseTransition(previous, to, progress),
                    x: mix(previous.x, to.x, progress),
                    y: mix(previous.y, to.y, progress),
                    fill: mixColor(previous.fill, to.fill, progress),
                    fontSize: mix(previous.fontSize, to.fontSize, progress),
                    rotation: mix(previous.rotation, to.rotation, progress),
                };
            }
        }
    }
    function fadeNode(node, opacity, idPrefix) {
        const id = idPrefix === undefined ? node.id : `${idPrefix}:${node.id}`;
        if (node.type === 'group') {
            return {
                ...node,
                id,
                opacity: effectiveOpacity(node) * opacity,
                visible: node.visible && opacity > 0,
                children: idPrefix === undefined
                    ? node.children
                    : node.children.map((child) => fadeNode(child, 1, idPrefix)),
            };
        }
        return {
            ...node,
            id,
            opacity: effectiveOpacity(node) * opacity,
            visible: node.visible && opacity > 0,
        };
    }
    function mergeChildren(from, to, progress, options) {
        const previousByIdentity = new Map();
        for (const node of from) {
            const identity = nodeIdentity(node, options.keyField);
            const candidates = previousByIdentity.get(identity) ?? [];
            candidates.push(node);
            previousByIdentity.set(identity, candidates);
        }
        let exitSequence = 0;
        const output = [];
        for (const node of to) {
            const identity = nodeIdentity(node, options.keyField);
            const candidates = previousByIdentity.get(identity);
            const stableIdentity = stableDatumIdentity(node, options.keyField) !== null;
            const candidateIndex = candidates === undefined
                ? -1
                : stableIdentity
                    ? candidates.findIndex((candidate) => sameStableDatumRole(candidate, node))
                    : 0;
            const previous = candidateIndex < 0 ? undefined : candidates?.splice(candidateIndex, 1)[0];
            if (candidates !== undefined && candidates.length === 0)
                previousByIdentity.delete(identity);
            if (previous === undefined) {
                output.push(fadeNode(node, progress));
                continue;
            }
            if (nodeCompatible(previous, node)) {
                output.push(interpolateCompatibleNode(previous, node, progress, options));
                continue;
            }
            exitSequence += 1;
            output.push(fadeNode(previous, 1 - progress, `transition-exit-${exitSequence}`), fadeNode(node, progress));
        }
        for (const candidates of previousByIdentity.values()) {
            for (const node of candidates) {
                exitSequence += 1;
                output.push(fadeNode(node, 1 - progress, `transition-exit-${exitSequence}`));
            }
        }
        return output;
    }
    /**
     * Create a transient render-only Scene between two compiled endpoint Scenes.
     * The endpoint Scenes are never mutated and their data/accessibility contracts
     * remain authoritative; incompatible nodes safely crossfade.
     */
    function interpolateScene(from, to, progress, options = {}) {
        if (!Number.isFinite(progress))
            throw new RangeError('Scene transition progress must be finite.');
        if (progress <= 0)
            return from;
        if (progress >= 1)
            return to;
        if (from.root.type !== 'group' || to.root.type !== 'group')
            return to;
        const background = colorCompatible(from.background, to.background)
            ? mixColor(from.background, to.background, progress)
            : to.background;
        return {
            ...to,
            background,
            root: interpolateCompatibleNode(from.root, to.root, progress, options),
        };
    }

    class RenderScheduler {
        #handle = null;
        schedule(task) {
            this.cancel();
            if (typeof requestAnimationFrame === 'function') {
                this.#handle = requestAnimationFrame(() => {
                    this.#handle = null;
                    task();
                });
            }
            else {
                this.#handle = setTimeout(() => {
                    this.#handle = null;
                    task();
                }, 0);
            }
        }
        cancel() {
            if (this.#handle === null)
                return;
            if (typeof this.#handle === 'number' && typeof cancelAnimationFrame === 'function') {
                cancelAnimationFrame(this.#handle);
            }
            else {
                clearTimeout(this.#handle);
            }
            this.#handle = null;
        }
    }

    const PLAYBACK_TRANSITION_INTERVAL_GAP = 1;
    let canvasSemanticViewSequence = 0;
    function effectivePlaybackTransitionDuration(playback) {
        if (playback.transition === false)
            return 0;
        // Keep every automatic frame boundary authoritative. A long requested tween
        // is shortened just below the frame interval so it cannot become the input
        // Scene for the following automatic transition.
        return Math.min(playback.transition.duration, Math.max(1, playback.interval - PLAYBACK_TRANSITION_INTERVAL_GAP));
    }
    function resolveTarget(target) {
        if (typeof target !== 'string')
            return target;
        if (typeof document === 'undefined') {
            throw new GraflumeError('MISSING_TARGET', 'A DOM target is required to create a chart.');
        }
        const element = document.querySelector(target);
        if (element === null) {
            throw new GraflumeError('MISSING_TARGET', `Chart target "${target}" was not found.`);
        }
        return element;
    }
    function dataRows(input) {
        if (Array.isArray(input))
            return input;
        const table = DataTable.from(input);
        return Array.from({ length: table.length }, (_, index) => table.row(index));
    }
    function appendInput(input, rows) {
        return [...dataRows(input), ...rows];
    }
    function sameView(left, right) {
        return (Math.abs(left.zoom - right.zoom) < 1e-9 &&
            Math.abs(left.offsetX - right.offsetX) < 1e-6 &&
            Math.abs(left.offsetY - right.offsetY) < 1e-6);
    }
    function sameDomainView(left, right) {
        return JSON.stringify(left) === JSON.stringify(right);
    }
    function pointDistance(left, right) {
        return Math.hypot(left.x - right.x, left.y - right.y);
    }
    function pointCenter(left, right) {
        return { x: (left.x + right.x) / 2, y: (left.y + right.y) / 2 };
    }
    function formatPlaybackFrame(value) {
        if (value instanceof Date)
            return value.toISOString();
        if (Array.isArray(value))
            return value.map(String).join(', ');
        return String(value ?? '');
    }
    function configuredFrame(spec) {
        const markFrame = (mark) => {
            if (typeof mark !== 'object' || mark === null)
                return undefined;
            const value = mark.options?.frame;
            return typeof value === 'string' || typeof value === 'number' || value === null
                ? value
                : undefined;
        };
        const shorthand = markFrame(spec.mark);
        if (shorthand !== undefined)
            return shorthand;
        for (const layer of spec.layers ?? []) {
            const frame = markFrame(layer.mark);
            if (frame !== undefined)
                return frame;
        }
        return undefined;
    }
    function cloneDatumTarget(target) {
        return {
            ...target,
            ...(Array.isArray(target.rowIndex) ? { rowIndex: [...target.rowIndex] } : {}),
            ...(target.values === undefined ? {} : { values: [...target.values] }),
        };
    }
    function cloneDecorationTarget(target) {
        switch (target.type) {
            case 'datum':
                return cloneDatumTarget(target);
            case 'range':
                return {
                    type: 'range',
                    ...(target.x === undefined ? {} : { x: { ...target.x } }),
                    ...(target.y === undefined ? {} : { y: { ...target.y } }),
                };
            case 'layer':
            case 'plot':
                return { ...target };
        }
    }
    function cloneAnnotation(annotation) {
        return {
            ...annotation,
            target: cloneDecorationTarget(annotation.target),
            ...(typeof annotation.connector === 'object'
                ? { connector: { ...annotation.connector, dash: [...(annotation.connector.dash ?? [])] } }
                : {}),
            ...(annotation.style === undefined ? {} : { style: { ...annotation.style } }),
        };
    }
    function markWithOptions(mark, options) {
        return typeof mark === 'string'
            ? { type: mark, options }
            : { ...mark, options: { ...mark.options, ...options } };
    }
    function cloneTableRuntimeState(state) {
        return {
            filters: state.filters.map((filter) => ({ ...filter })),
            sort: state.sort.map((sort) => ({ ...sort })),
            group: state.group === null
                ? null
                : {
                    fields: [...state.group.fields],
                    aggregates: state.group.aggregates.map((aggregate) => ({ ...aggregate })),
                },
            pivot: state.pivot === null ? null : { ...state.pivot },
            windowOffset: state.windowOffset,
            windowLimit: state.windowLimit,
            columnOffset: state.columnOffset,
            columnLimit: state.columnLimit,
        };
    }
    function cloneNetworkRuntimeState(state) {
        return {
            positions: Object.fromEntries(Object.entries(state.positions).map(([id, position]) => [id, { ...position }])),
            collapsed: [...state.collapsed],
            lasso: state.lasso.map((point) => ({ ...point })),
        };
    }
    function cloneFlowRuntimeState(state) {
        return {
            positions: Object.fromEntries(Object.entries(state.positions).map(([id, position]) => [id, { ...position }])),
        };
    }
    function cloneNavigatorRuntimeState(state) {
        return { ...state };
    }
    function cloneHierarchyRuntimeState(state) {
        return { ...state, collapsed: [...state.collapsed] };
    }
    function cloneParallelRuntimeState(state) {
        return {
            axes: state.axes.map((axis) => ({
                ...axis,
                ...(axis.domain === undefined ? {} : { domain: [...axis.domain] }),
            })),
            brushes: state.brushes.map((brush) => ({
                field: brush.field,
                extents: brush.extents.map((extent) => [...extent]),
            })),
            combine: state.combine,
        };
    }
    function cloneHeatmapRuntimeState(state) {
        return {
            rows: [...state.rows],
            columns: [...state.columns],
            ...(state.value === undefined ? {} : { value: [...state.value] }),
        };
    }
    function cloneScatterMatrixRuntimeState(state) {
        return {
            ...state,
            x: [...state.x],
            y: [...state.y],
            selectedRows: [...state.selectedRows],
        };
    }
    function familySceneEntries(root) {
        const entries = [];
        const visit = (node) => {
            if (node.type === 'group') {
                node.children.forEach(visit);
                return;
            }
            if (node.datum?.familyInteraction !== undefined) {
                entries.push({
                    ...node.datum,
                    nodeId: node.id,
                    familyInteraction: node.datum.familyInteraction,
                });
            }
        };
        visit(root);
        return entries;
    }
    function normalizedPoint(point, plot) {
        return {
            x: Math.max(0, Math.min(1, (point.x - plot.x) / Math.max(1, plot.width))),
            y: Math.max(0, Math.min(1, (point.y - plot.y) / Math.max(1, plot.height))),
        };
    }
    function annotationId(annotation, index) {
        return annotation.id ?? `annotation-${index}`;
    }
    function selectionKey(target) {
        const values = target.field === undefined
            ? null
            : [...(target.values ?? (target.value === undefined ? [] : [target.value]))].sort((left, right) => JSON.stringify(left).localeCompare(JSON.stringify(right)));
        const rows = target.rowIndex === undefined
            ? null
            : (Array.isArray(target.rowIndex) ? [...target.rowIndex] : [target.rowIndex]).sort((left, right) => left - right);
        return JSON.stringify([target.layerId ?? null, rows, target.field ?? null, values]);
    }
    class Chart {
        #target;
        #registry;
        #events = new EventEmitter();
        #scheduler = new RenderScheduler();
        #tooltip = new TooltipController();
        #controls = new ControlsController();
        #legend = new LegendController();
        #accessibility = new AccessibilityMirrorController();
        #options;
        #activePointers = new Map();
        #incrementalStores = new Map();
        #streamRuntimes = new Map();
        #semanticViewId;
        #spec;
        #renderer = null;
        #rendererName = null;
        #result = null;
        #destroyed = false;
        #resizeObserver = null;
        #windowResizeListener = null;
        #manualWidth;
        #manualHeight;
        #eventSurface = null;
        #surfaceTouchAction = null;
        #surfaceTabIndex = null;
        #surfaceCursor = null;
        #surfaceAriaKeyShortcuts = null;
        #view = identityInspectionView;
        #dragPrevious = null;
        #pinchStart = null;
        #dragDistance = 0;
        #suppressClick = false;
        #playback = false;
        #playbackFrames = [];
        #playbackIndex = 0;
        #playbackRate = 1;
        #playbackLoop = false;
        #playbackDirection = 'forward';
        #playbackRange = { start: 0, end: -1 };
        #playbackNamedFrames = [];
        #playing = false;
        #playbackCancel = null;
        #playbackTimestamp = null;
        #sceneTransition = null;
        #displayScene = null;
        #reducedMotion = null;
        #fullscreen = false;
        #hiddenLegendItems = new Set();
        #selection = [];
        #analyticSelection = new AnalyticSelectionStore();
        #domainView = emptyDomainViewState();
        #analyticGesture = null;
        #analyticKeyboardGesture = null;
        #domainGesture = null;
        #annotations = [];
        #annotationsVisible = true;
        #annotationHistory = new AnnotationAuthoringHistory();
        #activeAnnotationId = null;
        #annotationGesture = null;
        #markLabels = new MarkLabelHistory();
        #activeMarkLabelId = null;
        #markLabelGesture = null;
        #tableRuntime = new Map();
        #networkRuntime = new Map();
        #flowRuntime = new Map();
        #navigatorRuntime = new Map();
        #hierarchyRuntime = new Map();
        #parallelRuntime = new Map();
        #heatmapRuntime = new Map();
        #scatterMatrixRuntime = new Map();
        #familyFocus = null;
        #familyGesture = null;
        #technicalCrosshairValue = null;
        #markLabelLive = null;
        #markLabelLiveHost = null;
        #selectionLive = null;
        #selectionLiveHost = null;
        #selectionLiveTimer = null;
        #selectionLiveUpdatedAt = 0;
        #linkedFocusUnregister = null;
        #linkedFocusUnsubscribe = null;
        #applyingLinkedFocus = false;
        #linkedViewUnregister = null;
        #applyingLinkedViewState = false;
        #lastPublishedSemanticId = null;
        #preserveStreamRuntimes = false;
        #workerRuntime = null;
        #pointerMoveListener = (event) => {
            if (!(event instanceof PointerEvent))
                return;
            const crosshairChanged = this.#updateTechnicalCrosshair(event, false);
            const navigating = this.#handlePointerMove(event);
            if (crosshairChanged && !navigating)
                this.render();
            if (!navigating && this.#result?.spec.interaction.hover !== false) {
                this.#emitPointer('hover', event);
            }
        };
        #pointerDownListener = (event) => {
            if (event instanceof PointerEvent)
                this.#handlePointerDown(event);
        };
        #pointerUpListener = (event) => {
            if (event instanceof PointerEvent)
                this.#handlePointerEnd(event);
        };
        #pointerCancelListener = (event) => {
            if (!(event instanceof PointerEvent))
                return;
            this.#handlePointerEnd(event, true);
            this.#clearTechnicalCrosshair();
            this.#tooltip.hide();
            if (this.#result?.spec.interaction.hover !== false) {
                this.#events.emit('hover', { chart: this, hit: null, sourceEvent: event });
            }
        };
        #clickListener = (event) => {
            if (!(event instanceof PointerEvent))
                return;
            if (this.#suppressClick) {
                this.#suppressClick = false;
                return;
            }
            const interaction = this.#result?.spec.interaction;
            if (interaction?.click !== false || interaction?.selection !== false) {
                this.#emitPointer('click', event);
            }
        };
        #pointerLeaveListener = (event) => {
            if (!(event instanceof PointerEvent))
                return;
            this.#clearTechnicalCrosshair();
            this.#tooltip.hide();
            if (this.#result?.spec.interaction.hover !== false) {
                this.#events.emit('hover', { chart: this, hit: null, sourceEvent: event });
            }
        };
        #wheelListener = (event) => {
            if (event instanceof WheelEvent)
                this.#handleWheel(event);
        };
        #keyDownListener = (event) => {
            if (event instanceof KeyboardEvent)
                this.#handleKeyDown(event);
        };
        #visibilityListener = () => {
            if (typeof document !== 'undefined' && document.hidden)
                this.pause();
        };
        #reducedMotionListener = (event) => {
            if (event.matches)
                this.pause();
        };
        #fullscreenListener = () => {
            const active = this.#isOwnFullscreen();
            if (active === this.#fullscreen)
                return;
            this.#fullscreen = active;
            try {
                this.render();
                const scene = this.#result?.scene;
                if (scene !== undefined) {
                    this.#events.emit('resize', { chart: this, width: scene.width, height: scene.height });
                }
            }
            catch (error) {
                this.#events.emit('error', { chart: this, error });
            }
            this.#events.emit('fullscreenchange', { chart: this, active });
        };
        constructor(target, spec, registry, options = {}) {
            this.#target = resolveTarget(target);
            canvasSemanticViewSequence += 1;
            this.#semanticViewId = `canvas-view-${canvasSemanticViewSequence}`;
            this.#spec = spec;
            this.#registry = registry;
            this.#options = options;
            this.#manualWidth = options.width;
            this.#manualHeight = options.height;
            const normalized = normalizeSpec$1(spec);
            this.#annotations = normalized.annotations.map((annotation, index) => ({
                ...cloneAnnotation(annotation),
                id: annotationId(annotation, index),
            }));
            this.#annotationHistory.reset(this.#annotations);
            this.#resetMarkLabels(normalized);
            this.#configureInteraction(normalized, true);
            const linkedState = options.linkedViewStore?.get();
            if (linkedState !== undefined) {
                this.#analyticSelection.set(linkedState.analyticSelection);
                this.#domainView = linkedState.domainView;
                if (normalized.interaction.selection !== false &&
                    normalized.interaction.selection.kind === 'point') {
                    this.#selection = linkedState.analyticSelection.selections.flatMap((candidate) => candidate.type === 'point' && candidate.target !== undefined
                        ? [cloneDatumTarget(candidate.target)]
                        : []);
                }
            }
            try {
                this.#configureEnvironmentListeners();
                this.render();
                this.#linkedViewUnregister =
                    options.linkedViewStore?.register(this.#semanticViewId, (change) => this.#applyLinkedViewState(change)) ?? null;
                this.#configureResizeObserver();
                this.#startAutoplay();
            }
            catch (error) {
                try {
                    this.destroy();
                }
                catch {
                    // Preserve the original constructor failure after best-effort cleanup.
                }
                throw error;
            }
        }
        on(type, listener) {
            this.#assertAlive();
            return this.#events.on(type, listener);
        }
        off(type, listener) {
            this.#events.off(type, listener);
        }
        getSpec() {
            return this.#spec;
        }
        getScene() {
            return this.#result?.scene ?? null;
        }
        domainToPixel(axis, value, viewId) {
            this.#assertAlive();
            const view = this.#coordinateView(viewId);
            const pixel = domainToPixel(view.coordinates, axis, value);
            return (pixel + (cartesianAxisChannel(view.coordinates, axis) === 'x' ? view.offsetX : view.offsetY));
        }
        pixelToDomain(axis, pixel, viewId) {
            this.#assertAlive();
            const view = this.#coordinateView(viewId);
            const local = pixel - (cartesianAxisChannel(view.coordinates, axis) === 'x' ? view.offsetX : view.offsetY);
            return pixelToDomain(view.coordinates, axis, local);
        }
        getCoordinateViewIds() {
            this.#assertAlive();
            return this.#result?.coordinateViews.map(({ id }) => id) ?? [];
        }
        #coordinateView(viewId) {
            const result = this.#result;
            if (result === null)
                throw new GraflumeError('INVALID_SPEC', 'The chart is not rendered.');
            const view = viewId === undefined
                ? result.coordinateViews[0]
                : result.coordinateViews.find(({ id }) => id === viewId);
            if (view === undefined) {
                throw new GraflumeError('INVALID_SPEC', viewId === undefined
                    ? 'The chart has no Cartesian coordinate view.'
                    : `Coordinate view "${viewId}" was not found.`);
            }
            return view;
        }
        #coordinateAt(point) {
            const view = this.#result?.coordinateViews.find(({ bounds }) => point.x >= bounds.x &&
                point.x <= bounds.x + bounds.width &&
                point.y >= bounds.y &&
                point.y <= bounds.y + bounds.height);
            return view === undefined
                ? null
                : {
                    view,
                    point: { x: point.x - view.offsetX, y: point.y - view.offsetY },
                };
        }
        #localPoint(viewId, point) {
            const view = this.#coordinateView(viewId);
            return { x: point.x - view.offsetX, y: point.y - view.offsetY };
        }
        getSemanticIndex() {
            return this.#result?.scene.semanticIndex ?? [];
        }
        toAccessibleRows(maxRows) {
            return toAccessibleRows(this.getSemanticIndex(), maxRows ?? this.#result?.spec.accessibility.maxRows ?? 0);
        }
        getAccessibilityState() {
            const accessibility = this.#result?.spec.accessibility;
            const focusedId = this.#accessibility.getFocusedId();
            return {
                enabled: accessibility !== undefined &&
                    (accessibility.table !== false ||
                        accessibility.navigation ||
                        accessibility.linkedFocus !== false),
                table: accessibility?.table ?? false,
                navigation: accessibility?.navigation ?? false,
                rowCount: this.getSemanticIndex().length,
                ...(focusedId === null ? {} : { focusedId }),
            };
        }
        getFamilyFocus() {
            this.#assertAlive();
            return this.#familyFocus === null ? null : { ...this.#familyFocus };
        }
        focusPieSlice(layerId, id) {
            this.#assertAlive();
            const entry = this.#familyEntries('pie-slice').find((candidate) => candidate.layerId === layerId && candidate.familyInteraction.id === id);
            if (entry === undefined) {
                throw new GraflumeError('INVALID_SPEC', `Pie slice "${id}" was not found in layer "${layerId}".`);
            }
            return this.#setFamilyFocus({ kind: 'pie-slice', layerId, id }, 'programmatic');
        }
        focusTableCell(layerId, row, column) {
            this.#assertAlive();
            if (!Number.isInteger(row) || row < 0 || !Number.isInteger(column) || column < 0) {
                throw new GraflumeError('INVALID_SPEC', 'Table cell row and column must be non-negative integers.');
            }
            const entries = this.#familyEntries('table-cell').filter((candidate) => candidate.layerId === layerId);
            const first = entries[0]?.familyInteraction;
            if (first === undefined ||
                first.kind !== 'table-cell' ||
                row >= first.rows ||
                column >= first.columns) {
                throw new GraflumeError('INVALID_SPEC', `Table cell (${row}, ${column}) is outside layer "${layerId}".`);
            }
            let state = this.getTableRuntimeState(layerId);
            const frozenRows = this.#tableFrozenRows(layerId);
            const frozenColumns = this.#tableFrozenColumns(layerId);
            let nextState = state;
            if (row >= frozenRows &&
                (row < state.windowOffset || row >= state.windowOffset + state.windowLimit)) {
                const offset = Math.max(0, row - Math.max(0, state.windowLimit - 1));
                nextState = normalizeTableRuntimeState({ windowOffset: offset }, nextState);
            }
            if (column >= frozenColumns &&
                (column < state.columnOffset || column >= state.columnOffset + state.columnLimit)) {
                const offset = Math.max(0, column - Math.max(0, state.columnLimit - 1));
                nextState = normalizeTableRuntimeState({ columnOffset: offset }, nextState);
            }
            if (nextState !== state) {
                this.#setTableRuntimeState(layerId, nextState, 'programmatic', false);
                state = nextState;
            }
            const field = this.#familyEntries('table-cell').find((candidate) => candidate.layerId === layerId &&
                candidate.familyInteraction.row === row &&
                candidate.familyInteraction.column === column)?.familyInteraction;
            if (field === undefined || field.kind !== 'table-cell') {
                throw new GraflumeError('INVALID_SPEC', `Table column ${column} was not found.`);
            }
            return this.#setFamilyFocus({ kind: 'table-cell', layerId, row, column, field: field.field }, 'programmatic');
        }
        clearFamilyFocus() {
            this.#assertAlive();
            if (this.#familyFocus === null)
                return this;
            this.#familyFocus = null;
            this.render();
            this.#events.emit('familyfocuschange', { chart: this, state: null, reason: 'clear' });
            return this;
        }
        getTableRuntimeState(layerId) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['table']);
            const stored = this.#tableRuntime.get(layerId);
            if (stored !== undefined)
                return cloneTableRuntimeState(stored);
            const options = this.#layerOptions(layerId);
            return normalizeTableRuntimeState({
                filters: Array.isArray(options.filters)
                    ? options.filters
                    : [],
                sort: Array.isArray(options.sort)
                    ? options.sort
                    : [],
                group: options.group !== undefined ? options.group : null,
                pivot: options.pivot !== undefined ? options.pivot : null,
                windowOffset: typeof options.windowOffset === 'number' ? options.windowOffset : 0,
                windowLimit: typeof options.windowLimit === 'number' ? options.windowLimit : 100,
                columnOffset: typeof options.columnOffset === 'number' ? options.columnOffset : 0,
                columnLimit: typeof options.columnLimit === 'number' ? options.columnLimit : 100,
            });
        }
        setTableRuntimeState(layerId, state) {
            const current = this.getTableRuntimeState(layerId);
            return this.#setTableRuntimeState(layerId, normalizeTableRuntimeState(state, current), 'programmatic');
        }
        setTableFilters(layerId, filters) {
            return this.setTableRuntimeState(layerId, { filters, windowOffset: 0 });
        }
        setTableSort(layerId, sort) {
            return this.setTableRuntimeState(layerId, { sort, windowOffset: 0 });
        }
        setTableGroup(layerId, group) {
            return this.setTableRuntimeState(layerId, {
                group,
                ...(group === null ? {} : { pivot: null }),
                windowOffset: 0,
            });
        }
        setTablePivot(layerId, pivot) {
            return this.setTableRuntimeState(layerId, {
                pivot,
                ...(pivot === null ? {} : { group: null }),
                windowOffset: 0,
            });
        }
        resetTableRuntime(layerId) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['table']);
            if (!this.#tableRuntime.delete(layerId))
                return this;
            this.render();
            this.#events.emit('tablechange', {
                chart: this,
                layerId,
                state: this.getTableRuntimeState(layerId),
                reason: 'reset',
            });
            return this;
        }
        getNetworkRuntimeState(layerId) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['graph']);
            const stored = this.#networkRuntime.get(layerId);
            if (stored !== undefined)
                return cloneNetworkRuntimeState(stored);
            const options = this.#layerOptions(layerId);
            return normalizeNetworkRuntimeState({
                positions: options.positions !== null &&
                    typeof options.positions === 'object' &&
                    !Array.isArray(options.positions)
                    ? options.positions
                    : {},
                collapsed: Array.isArray(options.collapsed)
                    ? options.collapsed
                    : [],
                lasso: Array.isArray(options.lasso) ? options.lasso : [],
            });
        }
        moveNetworkNode(layerId, id, position, pin = true) {
            const current = this.getNetworkRuntimeState(layerId);
            this.#requireFamilyNode(layerId, id, 'network-node');
            return this.#setNetworkRuntimeState(layerId, normalizeNetworkRuntimeState({ positions: { ...current.positions, [id]: { ...position, pinned: pin } } }, current), 'programmatic');
        }
        setNetworkNodePinned(layerId, id, pinned) {
            const current = this.getNetworkRuntimeState(layerId);
            const entry = this.#requireFamilyNode(layerId, id, 'network-node').familyInteraction;
            if (entry.kind !== 'network-node')
                return this;
            const position = current.positions[id] ?? entry.position;
            return this.#setNetworkRuntimeState(layerId, normalizeNetworkRuntimeState({ positions: { ...current.positions, [id]: { ...position, pinned } } }, current), 'programmatic');
        }
        setNetworkNodeCollapsed(layerId, id, collapsed) {
            const current = this.getNetworkRuntimeState(layerId);
            const entry = this.#requireFamilyNode(layerId, id, 'network-node').familyInteraction;
            if (entry.kind !== 'network-node' || (!entry.compound && collapsed)) {
                throw new GraflumeError('INVALID_SPEC', `Network node "${id}" is not collapsible.`);
            }
            const values = new Set(current.collapsed);
            if (collapsed)
                values.add(id);
            else
                values.delete(id);
            return this.#setNetworkRuntimeState(layerId, normalizeNetworkRuntimeState({ collapsed: [...values] }, current), 'programmatic');
        }
        setNetworkLasso(layerId, lasso) {
            const current = this.getNetworkRuntimeState(layerId);
            return this.#setNetworkRuntimeState(layerId, normalizeNetworkRuntimeState({ lasso }, current), 'programmatic');
        }
        resetNetworkRuntime(layerId) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['graph']);
            if (!this.#networkRuntime.delete(layerId))
                return this;
            this.render();
            this.#events.emit('networkchange', {
                chart: this,
                layerId,
                state: this.getNetworkRuntimeState(layerId),
                reason: 'reset',
            });
            return this;
        }
        getFlowRuntimeState(layerId) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['sankey']);
            const stored = this.#flowRuntime.get(layerId);
            if (stored !== undefined)
                return cloneFlowRuntimeState(stored);
            const options = this.#layerOptions(layerId);
            return normalizeFlowRuntimeState({
                positions: options.positions !== null &&
                    typeof options.positions === 'object' &&
                    !Array.isArray(options.positions)
                    ? options.positions
                    : {},
            });
        }
        moveFlowNode(layerId, id, position) {
            const current = this.getFlowRuntimeState(layerId);
            this.#requireFamilyNode(layerId, id, 'flow-node');
            return this.#setFlowRuntimeState(layerId, normalizeFlowRuntimeState({ positions: { ...current.positions, [id]: { ...position } } }, current), 'programmatic');
        }
        resetFlowRuntime(layerId) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['sankey']);
            if (!this.#flowRuntime.delete(layerId))
                return this;
            this.render();
            this.#events.emit('flowchange', {
                chart: this,
                layerId,
                state: this.getFlowRuntimeState(layerId),
                reason: 'reset',
            });
            return this;
        }
        getNavigatorWindow(layerId) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['candlestick', 'timeline']);
            const stored = this.#navigatorRuntime.get(layerId);
            if (stored !== undefined)
                return cloneNavigatorRuntimeState(stored.state);
            const interaction = this.#navigatorInteraction(layerId);
            return { start: interaction.start, end: interaction.end };
        }
        setNavigatorWindow(layerId, state) {
            const interaction = this.#navigatorInteraction(layerId);
            const current = this.getNavigatorWindow(layerId);
            return this.#setNavigatorRuntimeState(layerId, interaction.family, normalizeNavigatorRuntimeState(state, current, {
                minimum: interaction.minimum,
                maximum: interaction.maximum,
            }), 'programmatic');
        }
        resetNavigatorWindow(layerId) {
            this.#assertAlive();
            const layer = this.#requireFamilyLayer(layerId, ['candlestick', 'timeline']);
            if (!this.#navigatorRuntime.delete(layerId))
                return this;
            this.render();
            this.#events.emit('navigatorchange', {
                chart: this,
                layerId,
                family: layer.mark.type,
                state: this.getNavigatorWindow(layerId),
                reason: 'reset',
            });
            return this;
        }
        getHierarchyRuntimeState(layerId) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['tree']);
            const stored = this.#hierarchyRuntime.get(layerId);
            if (stored !== undefined)
                return cloneHierarchyRuntimeState(stored);
            const options = this.#layerOptions(layerId);
            return normalizeHierarchyRuntimeState({
                root: typeof options.root === 'string' ? options.root : null,
                zoomTo: typeof options.zoomTo === 'string' ? options.zoomTo : null,
                collapsed: Array.isArray(options.collapsed)
                    ? options.collapsed
                    : [],
                query: typeof options.query === 'string' ? options.query : '',
            });
        }
        setHierarchyRuntimeState(layerId, state) {
            const current = this.getHierarchyRuntimeState(layerId);
            return this.#setHierarchyRuntimeState(layerId, normalizeHierarchyRuntimeState(state, current), 'programmatic');
        }
        setHierarchyNodeCollapsed(layerId, id, collapsed) {
            const current = this.getHierarchyRuntimeState(layerId);
            const values = new Set(current.collapsed);
            if (collapsed)
                values.add(id);
            else
                values.delete(id);
            return this.#setHierarchyRuntimeState(layerId, normalizeHierarchyRuntimeState({ collapsed: [...values] }, current), 'programmatic');
        }
        rerootHierarchy(layerId, id) {
            return this.setHierarchyRuntimeState(layerId, { root: id, zoomTo: null });
        }
        zoomHierarchy(layerId, id) {
            return this.setHierarchyRuntimeState(layerId, { zoomTo: id });
        }
        setHierarchyQuery(layerId, query) {
            return this.setHierarchyRuntimeState(layerId, { query });
        }
        resetHierarchyRuntime(layerId) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['tree']);
            if (!this.#hierarchyRuntime.delete(layerId))
                return this;
            this.render();
            this.#events.emit('hierarchychange', {
                chart: this,
                layerId,
                state: this.getHierarchyRuntimeState(layerId),
                reason: 'reset',
            });
            return this;
        }
        getParallelRuntimeState(layerId) {
            this.#assertAlive();
            const layer = this.#requireFamilyLayer(layerId, ['parallel']);
            const stored = this.#parallelRuntime.get(layerId);
            if (stored !== undefined)
                return cloneParallelRuntimeState(stored);
            const options = layer.mark.options;
            const rawAxes = Array.isArray(options.axes)
                ? options.axes
                : Array.isArray(options.dimensions)
                    ? options.dimensions.map((field) => ({ field }))
                    : [{ field: layer.x.field }, { field: layer.y.field }];
            const axes = rawAxes.flatMap((raw) => {
                if (typeof raw === 'string') {
                    return [{ field: raw, type: 'linear', invert: false, missing: 'gap' }];
                }
                if (raw === null || typeof raw !== 'object' || Array.isArray(raw))
                    return [];
                const axis = raw;
                if (typeof axis.field !== 'string')
                    return [];
                const type = axis.type === 'log' || axis.type === 'ordinal' ? axis.type : 'linear';
                const missing = axis.missing === 'top' || axis.missing === 'bottom' || axis.missing === 'middle'
                    ? axis.missing
                    : 'gap';
                return [
                    {
                        field: axis.field,
                        type,
                        invert: axis.invert === true,
                        missing,
                        ...(Array.isArray(axis.domain)
                            ? { domain: axis.domain }
                            : {}),
                    },
                ];
            });
            return normalizeParallelRuntimeState({
                axes,
                brushes: Array.isArray(options.brushes)
                    ? options.brushes
                    : [],
                combine: options.combine === 'union' ? 'union' : 'intersection',
            }, { axes, brushes: [], combine: 'intersection' });
        }
        setParallelRuntimeState(layerId, state) {
            const current = this.getParallelRuntimeState(layerId);
            return this.#setParallelRuntimeState(layerId, normalizeParallelRuntimeState(state, current), 'programmatic');
        }
        reorderParallelAxis(layerId, field, index) {
            return this.#setParallelRuntimeState(layerId, reorderParallelAxis(this.getParallelRuntimeState(layerId), field, index), 'programmatic');
        }
        invertParallelAxis(layerId, field, invert) {
            return this.#setParallelRuntimeState(layerId, invertParallelAxis(this.getParallelRuntimeState(layerId), field, invert), 'programmatic');
        }
        setParallelBrush(layerId, field, extents) {
            return this.#setParallelRuntimeState(layerId, setParallelBrushExtents(this.getParallelRuntimeState(layerId), field, extents), 'programmatic');
        }
        resetParallelRuntime(layerId) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['parallel']);
            if (!this.#parallelRuntime.delete(layerId))
                return this;
            this.render();
            this.#events.emit('parallelchange', {
                chart: this,
                layerId,
                state: this.getParallelRuntimeState(layerId),
                reason: 'reset',
            });
            return this;
        }
        getHeatmapBrush(layerId) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['heatmap']);
            const stored = this.#heatmapRuntime.get(layerId);
            if (stored !== undefined)
                return cloneHeatmapRuntimeState(stored);
            const brush = this.#layerOptions(layerId).brush;
            return normalizeHeatmapRuntimeState(brush !== null && typeof brush === 'object' && !Array.isArray(brush)
                ? brush
                : {});
        }
        setHeatmapBrush(layerId, state) {
            const current = this.getHeatmapBrush(layerId);
            return this.#setHeatmapRuntimeState(layerId, normalizeHeatmapRuntimeState(state, current), 'programmatic');
        }
        resetHeatmapBrush(layerId) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['heatmap']);
            if (!this.#heatmapRuntime.delete(layerId))
                return this;
            this.render();
            this.#events.emit('heatmapchange', {
                chart: this,
                layerId,
                state: this.getHeatmapBrush(layerId),
                reason: 'reset',
            });
            return this;
        }
        getScatterMatrixBrush(layerId) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['scatter-matrix']);
            const stored = this.#scatterMatrixRuntime.get(layerId);
            if (stored !== undefined)
                return cloneScatterMatrixRuntimeState(stored);
            const cell = this.#familyEntries('scatter-matrix-cell').find((entry) => entry.layerId === layerId)?.familyInteraction;
            if (cell === undefined)
                return null;
            const fallback = {
                xField: cell.xField,
                yField: cell.yField,
                x: [...cell.xDomain],
                y: [...cell.yDomain],
                selectedRows: [],
            };
            const linkedBrush = this.#layerOptions(layerId).linkedBrush;
            return normalizeScatterMatrixRuntimeState(linkedBrush !== null && typeof linkedBrush === 'object' && !Array.isArray(linkedBrush)
                ? linkedBrush
                : {}, fallback);
        }
        setScatterMatrixBrush(layerId, state) {
            const current = this.getScatterMatrixBrush(layerId);
            if (current === null) {
                throw new GraflumeError('INVALID_SPEC', `Layer "${layerId}" has no interactive scatter-matrix cell.`);
            }
            const normalized = normalizeScatterMatrixRuntimeState(state, current);
            const selectedRows = state.selectedRows === undefined &&
                (state.x !== undefined ||
                    state.y !== undefined ||
                    state.xField !== undefined ||
                    state.yField !== undefined)
                ? selectScatterMatrixRows(this.#familyLayerRows(layerId), normalized)
                : normalized.selectedRows;
            return this.#setScatterMatrixRuntimeState(layerId, { ...normalized, selectedRows }, 'programmatic');
        }
        resetScatterMatrixBrush(layerId) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['scatter-matrix']);
            if (!this.#scatterMatrixRuntime.delete(layerId))
                return this;
            this.render();
            this.#events.emit('scattermatrixchange', {
                chart: this,
                layerId,
                state: this.getScatterMatrixBrush(layerId),
                reason: 'reset',
            });
            return this;
        }
        #familyEntries(kind) {
            const scene = this.#result?.scene;
            if (scene === undefined)
                return [];
            return familySceneEntries(scene.root).filter((entry) => entry.familyInteraction.kind === kind);
        }
        #navigatorInteraction(layerId) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['candlestick', 'timeline']);
            const interaction = this.#familyEntries('navigator-window').find((entry) => entry.layerId === layerId)?.familyInteraction;
            if (interaction === undefined) {
                throw new GraflumeError('INVALID_SPEC', `Layer "${layerId}" does not have an enabled navigator window.`);
            }
            return interaction;
        }
        #familyLayerRows(layerId) {
            const layer = this.#result?.spec.layers.find((candidate) => candidate.id === layerId);
            if (layer === undefined) {
                throw new GraflumeError('INVALID_SPEC', `Layer "${layerId}" was not found.`);
            }
            return dataRows(layer.data);
        }
        #requireFamilyLayer(layerId, types) {
            const layer = this.#result?.spec.layers.find((candidate) => candidate.id === layerId);
            if (layer === undefined || !types.includes(layer.mark.type)) {
                throw new GraflumeError('INVALID_SPEC', `Layer "${layerId}" is not an interactive ${types.join('/')} layer.`);
            }
            return layer;
        }
        #layerOptions(layerId) {
            const layer = this.#result?.spec.layers.find((candidate) => candidate.id === layerId);
            if (layer === undefined) {
                throw new GraflumeError('INVALID_SPEC', `Layer "${layerId}" was not found.`);
            }
            return layer.mark.options;
        }
        #tableFrozenRows(layerId) {
            const value = this.#layerOptions(layerId).frozenRows;
            return typeof value === 'number' && Number.isFinite(value) ? Math.max(0, Math.floor(value)) : 0;
        }
        #tableFrozenColumns(layerId) {
            const value = this.#layerOptions(layerId).frozenColumns;
            return typeof value === 'number' && Number.isFinite(value) ? Math.max(0, Math.floor(value)) : 0;
        }
        #requireFamilyNode(layerId, id, kind) {
            const root = this.#result?.scene.root;
            const entry = root === undefined
                ? undefined
                : familySceneEntries(root).find((candidate) => candidate.layerId === layerId &&
                    candidate.familyInteraction.kind === kind &&
                    candidate.familyInteraction.id === id);
            if (entry === undefined) {
                throw new GraflumeError('INVALID_SPEC', `${kind === 'network-node' ? 'Network' : 'Flow'} node "${id}" was not found in layer "${layerId}".`);
            }
            return entry;
        }
        #setFamilyFocus(state, reason) {
            if (JSON.stringify(this.#familyFocus) === JSON.stringify(state))
                return this;
            const previous = this.#familyFocus;
            this.#familyFocus = { ...state };
            try {
                this.render();
            }
            catch (error) {
                this.#familyFocus = previous;
                throw error;
            }
            this.#events.emit('familyfocuschange', {
                chart: this,
                state: { ...state },
                reason,
            });
            return this;
        }
        #setTableRuntimeState(layerId, state, reason, emit = true) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['table']);
            const previous = this.#tableRuntime.get(layerId);
            this.#tableRuntime.set(layerId, cloneTableRuntimeState(state));
            try {
                this.render();
            }
            catch (error) {
                if (previous === undefined)
                    this.#tableRuntime.delete(layerId);
                else
                    this.#tableRuntime.set(layerId, previous);
                throw error;
            }
            if (emit) {
                this.#events.emit('tablechange', {
                    chart: this,
                    layerId,
                    state: cloneTableRuntimeState(state),
                    reason,
                });
            }
            return this;
        }
        #setNetworkRuntimeState(layerId, state, reason, emit = true) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['graph']);
            const previous = this.#networkRuntime.get(layerId);
            this.#networkRuntime.set(layerId, cloneNetworkRuntimeState(state));
            try {
                this.render();
            }
            catch (error) {
                if (previous === undefined)
                    this.#networkRuntime.delete(layerId);
                else
                    this.#networkRuntime.set(layerId, previous);
                throw error;
            }
            if (emit) {
                this.#events.emit('networkchange', {
                    chart: this,
                    layerId,
                    state: cloneNetworkRuntimeState(state),
                    reason,
                });
            }
            return this;
        }
        #setFlowRuntimeState(layerId, state, reason, emit = true) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['sankey']);
            const previous = this.#flowRuntime.get(layerId);
            this.#flowRuntime.set(layerId, cloneFlowRuntimeState(state));
            try {
                this.render();
            }
            catch (error) {
                if (previous === undefined)
                    this.#flowRuntime.delete(layerId);
                else
                    this.#flowRuntime.set(layerId, previous);
                throw error;
            }
            if (emit) {
                this.#events.emit('flowchange', {
                    chart: this,
                    layerId,
                    state: cloneFlowRuntimeState(state),
                    reason,
                });
            }
            return this;
        }
        #setNavigatorRuntimeState(layerId, family, state, reason, emit = true) {
            this.#assertAlive();
            const interaction = this.#navigatorInteraction(layerId);
            if (interaction.family !== family) {
                throw new GraflumeError('INVALID_SPEC', `Navigator family mismatch for layer "${layerId}".`);
            }
            const previous = this.#navigatorRuntime.get(layerId);
            this.#navigatorRuntime.set(layerId, { family, state: cloneNavigatorRuntimeState(state) });
            try {
                this.render();
            }
            catch (error) {
                if (previous === undefined)
                    this.#navigatorRuntime.delete(layerId);
                else
                    this.#navigatorRuntime.set(layerId, previous);
                throw error;
            }
            if (emit) {
                this.#events.emit('navigatorchange', {
                    chart: this,
                    layerId,
                    family,
                    state: cloneNavigatorRuntimeState(state),
                    reason,
                });
            }
            return this;
        }
        #setHierarchyRuntimeState(layerId, state, reason, emit = true) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['tree']);
            const previous = this.#hierarchyRuntime.get(layerId);
            this.#hierarchyRuntime.set(layerId, cloneHierarchyRuntimeState(state));
            try {
                this.render();
            }
            catch (error) {
                if (previous === undefined)
                    this.#hierarchyRuntime.delete(layerId);
                else
                    this.#hierarchyRuntime.set(layerId, previous);
                throw error;
            }
            if (emit) {
                this.#events.emit('hierarchychange', {
                    chart: this,
                    layerId,
                    state: cloneHierarchyRuntimeState(state),
                    reason,
                });
            }
            return this;
        }
        #setParallelRuntimeState(layerId, state, reason, emit = true) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['parallel']);
            const previous = this.#parallelRuntime.get(layerId);
            this.#parallelRuntime.set(layerId, cloneParallelRuntimeState(state));
            try {
                this.render();
            }
            catch (error) {
                if (previous === undefined)
                    this.#parallelRuntime.delete(layerId);
                else
                    this.#parallelRuntime.set(layerId, previous);
                throw error;
            }
            if (emit) {
                this.#events.emit('parallelchange', {
                    chart: this,
                    layerId,
                    state: cloneParallelRuntimeState(state),
                    reason,
                });
            }
            return this;
        }
        #setHeatmapRuntimeState(layerId, state, reason, emit = true) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['heatmap']);
            const previous = this.#heatmapRuntime.get(layerId);
            this.#heatmapRuntime.set(layerId, cloneHeatmapRuntimeState(state));
            try {
                this.render();
            }
            catch (error) {
                if (previous === undefined)
                    this.#heatmapRuntime.delete(layerId);
                else
                    this.#heatmapRuntime.set(layerId, previous);
                throw error;
            }
            if (emit) {
                this.#events.emit('heatmapchange', {
                    chart: this,
                    layerId,
                    state: cloneHeatmapRuntimeState(state),
                    reason,
                });
            }
            return this;
        }
        #setScatterMatrixRuntimeState(layerId, state, reason, emit = true) {
            this.#assertAlive();
            this.#requireFamilyLayer(layerId, ['scatter-matrix']);
            const previous = this.#scatterMatrixRuntime.get(layerId);
            this.#scatterMatrixRuntime.set(layerId, cloneScatterMatrixRuntimeState(state));
            try {
                this.render();
            }
            catch (error) {
                if (previous === undefined)
                    this.#scatterMatrixRuntime.delete(layerId);
                else
                    this.#scatterMatrixRuntime.set(layerId, previous);
                throw error;
            }
            if (emit) {
                this.#events.emit('scattermatrixchange', {
                    chart: this,
                    layerId,
                    state: cloneScatterMatrixRuntimeState(state),
                    reason,
                });
            }
            return this;
        }
        #familyOptions(layerId) {
            const options = {};
            const table = this.#tableRuntime.get(layerId);
            if (table !== undefined)
                Object.assign(options, tableRuntimeOptions(table));
            const network = this.#networkRuntime.get(layerId);
            if (network !== undefined)
                Object.assign(options, networkRuntimeOptions(network));
            const flow = this.#flowRuntime.get(layerId);
            if (flow !== undefined)
                Object.assign(options, flowRuntimeOptions(flow));
            const navigator = this.#navigatorRuntime.get(layerId);
            if (navigator !== undefined) {
                Object.assign(options, navigatorRuntimeOptions(navigator.family, navigator.state));
            }
            const hierarchy = this.#hierarchyRuntime.get(layerId);
            if (hierarchy !== undefined)
                Object.assign(options, hierarchyRuntimeOptions(hierarchy));
            const parallel = this.#parallelRuntime.get(layerId);
            if (parallel !== undefined)
                Object.assign(options, parallelRuntimeOptions(parallel));
            const heatmap = this.#heatmapRuntime.get(layerId);
            if (heatmap !== undefined)
                Object.assign(options, heatmapRuntimeOptions(heatmap));
            const scatterMatrix = this.#scatterMatrixRuntime.get(layerId);
            if (scatterMatrix !== undefined) {
                Object.assign(options, scatterMatrixRuntimeOptions(scatterMatrix));
            }
            if (this.#familyFocus?.layerId === layerId) {
                if (this.#familyFocus.kind === 'pie-slice') {
                    options.runtimeFocusedSlice = this.#familyFocus.id;
                }
                else {
                    options.runtimeFocusedCell = {
                        row: this.#familyFocus.row,
                        column: this.#familyFocus.column,
                    };
                }
            }
            return options;
        }
        #familyRuntimeSpec(spec) {
            const topOptions = this.#familyOptions('layer-0');
            const layers = spec.layers?.map((layer, index) => {
                const layerId = layer.id ?? `layer-${index}`;
                const options = this.#familyOptions(layerId);
                return Object.keys(options).length === 0
                    ? layer
                    : { ...layer, mark: markWithOptions(layer.mark, options) };
            });
            return {
                ...spec,
                ...(spec.mark === undefined || Object.keys(topOptions).length === 0
                    ? {}
                    : { mark: markWithOptions(spec.mark, topOptions) }),
                ...(layers === undefined ? {} : { layers }),
            };
        }
        getViewState() {
            return { ...this.#view, enabled: this.#navigation() !== false };
        }
        getPlaybackState() {
            const frame = this.#playbackFrames[this.#playbackIndex];
            const name = this.#playbackNamedFrames.find((namedFrame) => namedFrame.index === this.#playbackIndex)?.name;
            const hasRange = this.#playbackRange.end >= this.#playbackRange.start;
            return {
                enabled: this.#playback !== false,
                frames: this.#playbackFrames,
                index: this.#playbackIndex,
                ...(frame === undefined ? {} : { frame }),
                playing: this.#playing,
                rate: this.#playbackRate,
                loop: this.#playbackLoop,
                mode: this.#playback === false ? 'frame' : this.#playback.mode,
                direction: this.#playbackDirection,
                ...(hasRange
                    ? {
                        range: {
                            start: this.#playbackRange.start,
                            end: this.#playbackRange.end,
                            startFrame: this.#playbackFrames[this.#playbackRange.start],
                            endFrame: this.#playbackFrames[this.#playbackRange.end],
                        },
                    }
                    : {}),
                namedFrames: this.#playbackNamedFrames,
                ...(name === undefined ? {} : { name }),
                label: name ?? formatPlaybackFrame(frame),
            };
        }
        getLegendState() {
            const layout = this.#result === null ? null : sceneLegendLayout(this.#result.scene);
            return {
                enabled: layout !== null,
                items: layout?.entries.map((entry) => ({
                    id: entry.id,
                    label: entry.label,
                    color: entry.color,
                    visible: entry.visible,
                    toggleable: entry.toggleable,
                    ...(entry.layerId === undefined ? {} : { layerId: entry.layerId }),
                    ...(entry.value === undefined ? {} : { value: entry.value }),
                })) ?? [],
            };
        }
        setLegendItemVisible(id, visible) {
            return this.#setLegendItemVisible(id, visible, 'programmatic');
        }
        #setLegendItemVisible(id, visible, reason) {
            this.#assertAlive();
            const item = this.getLegendState().items.find((candidate) => candidate.id === id);
            if (item === undefined) {
                throw new GraflumeError('INVALID_SPEC', `Legend item "${id}" was not found.`);
            }
            if (!item.toggleable) {
                throw new GraflumeError('INVALID_SPEC', `Legend item "${id}" is not toggleable.`);
            }
            if (item.visible === visible)
                return this;
            if (visible)
                this.#hiddenLegendItems.delete(id);
            else
                this.#hiddenLegendItems.add(id);
            this.render();
            this.#events.emit('legendchange', {
                chart: this,
                state: this.getLegendState(),
                reason,
            });
            return this;
        }
        resetLegend() {
            this.#assertAlive();
            if (this.#hiddenLegendItems.size === 0)
                return this;
            this.#hiddenLegendItems.clear();
            this.render();
            this.#events.emit('legendchange', {
                chart: this,
                state: this.getLegendState(),
                reason: 'reset',
            });
            return this;
        }
        getSelection() {
            return {
                enabled: this.#result !== null && this.#result.spec.interaction.selection !== false,
                items: this.#selection.map(cloneDatumTarget),
            };
        }
        setSelection(items) {
            this.#assertAlive();
            const configuredSelection = this.#result?.spec.interaction.selection;
            if (configuredSelection === false) {
                throw new GraflumeError('INVALID_SPEC', 'Enable interaction.selection before setting selection state.');
            }
            if (configuredSelection !== undefined && configuredSelection.kind !== 'point') {
                throw new GraflumeError('INVALID_SPEC', 'setSelection() is the point/datum facade; use setAnalyticSelection() for domain geometry.');
            }
            normalizeSpec$1({
                ...this.#spec,
                // Replace authored highlights while validating selection targets so
                // their count and IDs cannot interfere with transient selection state.
                highlights: items.map((target) => ({ target })),
            });
            const selection = this.#result?.spec.interaction.selection;
            if (selection !== undefined &&
                selection !== false &&
                selection.mode === 'single' &&
                items.length > 1)
                throw new GraflumeError('INVALID_SPEC', 'Single selection mode accepts at most one target.');
            const next = items.map(cloneDatumTarget);
            const keys = next.map(selectionKey);
            if (new Set(keys).size !== keys.length)
                throw new GraflumeError('INVALID_SPEC', 'Selection targets must be unique.');
            if (next.length === this.#selection.length &&
                next.every((target, index) => selectionKey(target) === selectionKey(this.#selection[index])))
                return this;
            this.#selection = next;
            this.#syncAnalyticPointTargets();
            this.render();
            this.#emitSelection('programmatic');
            this.#emitAnalyticSelection('programmatic');
            return this;
        }
        clearSelection() {
            this.#assertAlive();
            if (this.#selection.length === 0)
                return this;
            this.#selection = [];
            if (this.#result?.spec.interaction.selection !== false)
                this.#analyticSelection.clear();
            this.render();
            this.#emitSelection('clear');
            this.#emitAnalyticSelection('clear');
            return this;
        }
        getAnalyticSelection() {
            this.#assertAlive();
            return this.#analyticSelection.get();
        }
        setAnalyticSelection(state) {
            this.#assertAlive();
            const selection = this.#requireAnalyticSelection();
            const next = normalizeAnalyticSelectionState(state);
            this.#validateAnalyticStateForConfig(next, selection);
            const previous = this.#analyticSelection.get();
            const previousPointSelection = this.#selection;
            this.#analyticSelection.set(next);
            if (selection.kind === 'point') {
                this.#selection = next.selections.flatMap((candidate) => candidate.type === 'point' && candidate.target !== undefined
                    ? [cloneDatumTarget(candidate.target)]
                    : []);
            }
            else
                this.#selection = [];
            try {
                this.render();
            }
            catch (error) {
                this.#analyticSelection.set(previous);
                this.#selection = previousPointSelection;
                throw error;
            }
            this.#emitAnalyticSelection('programmatic');
            if (selection.kind === 'point')
                this.#emitSelection('programmatic');
            return this;
        }
        applyAnalyticSelection(selection, update = 'replace') {
            this.#assertAlive();
            const config = this.#requireAnalyticSelection();
            const previous = this.#analyticSelection.get();
            const previousPointSelection = this.#selection;
            const next = this.#analyticSelection.apply(selection, update);
            try {
                this.#validateAnalyticStateForConfig(next, config);
                if (config.kind === 'point') {
                    this.#selection = next.selections.flatMap((candidate) => candidate.type === 'point' && candidate.target !== undefined
                        ? [cloneDatumTarget(candidate.target)]
                        : []);
                }
                else
                    this.#selection = [];
                this.render();
            }
            catch (error) {
                this.#analyticSelection.set(previous);
                this.#selection = previousPointSelection;
                throw error;
            }
            this.#emitAnalyticSelection('programmatic');
            if (config.kind === 'point')
                this.#emitSelection('programmatic');
            return this;
        }
        clearAnalyticSelection() {
            this.#assertAlive();
            if (this.#analyticSelection.get().selections.length === 0)
                return this;
            this.#analyticSelection.clear();
            const selection = this.#result?.spec.interaction.selection;
            const pointSelection = selection !== undefined && selection !== false && selection.kind === 'point';
            if (pointSelection) {
                this.#selection = [];
            }
            this.render();
            this.#emitAnalyticSelection('clear');
            if (pointSelection)
                this.#emitSelection('clear');
            return this;
        }
        getAnnotations() {
            return this.#annotations.map(cloneAnnotation);
        }
        getAnnotationsVisible() {
            this.#assertAlive();
            return this.#annotationsVisible;
        }
        setAnnotationsVisible(visible) {
            return this.#setAnnotationsVisible(visible, 'programmatic');
        }
        toggleAnnotations() {
            this.#assertAlive();
            return this.#setAnnotationsVisible(!this.#annotationsVisible, 'toggle');
        }
        setAnnotations(annotations) {
            this.#assertAlive();
            const resolved = annotations.map((annotation, index) => ({
                ...cloneAnnotation(annotation),
                id: annotationId(annotation, index),
            }));
            normalizeSpec$1({ ...this.#spec, annotations: resolved });
            this.#annotationHistory.replace(resolved);
            this.#annotations = [...this.#annotationHistory.annotations()];
            this.render();
            this.#emitAnnotations('set');
            return this;
        }
        addAnnotation(annotation) {
            this.#assertAlive();
            const id = annotation.id ?? `annotation-runtime-${Date.now()}-${this.#annotations.length}`;
            if (this.#annotations.some((candidate) => candidate.id === id)) {
                throw new GraflumeError('INVALID_SPEC', `Annotation "${id}" already exists.`);
            }
            const next = [...this.#annotations, { ...cloneAnnotation(annotation), id }];
            normalizeSpec$1({ ...this.#spec, annotations: next });
            this.#annotationHistory.replace(next);
            this.#annotations = [...this.#annotationHistory.annotations()];
            this.render();
            this.#emitAnnotations('add', id);
            return id;
        }
        updateAnnotation(id, patch) {
            this.#assertAlive();
            const index = this.#annotations.findIndex((annotation) => annotation.id === id);
            if (index < 0)
                throw new GraflumeError('INVALID_SPEC', `Annotation "${id}" was not found.`);
            const current = this.#annotations[index];
            const updated = cloneAnnotation({ ...current, ...patch, id });
            const next = this.#annotations.map((annotation, candidate) => candidate === index ? updated : annotation);
            normalizeSpec$1({ ...this.#spec, annotations: next });
            this.#annotationHistory.replace(next);
            this.#annotations = [...this.#annotationHistory.annotations()];
            this.render();
            this.#emitAnnotations('update', id);
            return this;
        }
        removeAnnotation(id) {
            this.#assertAlive();
            const next = this.#annotations.filter((annotation) => annotation.id !== id);
            if (next.length === this.#annotations.length)
                return false;
            this.#annotationHistory.replace(next);
            this.#annotations = [...this.#annotationHistory.annotations()];
            if (this.#activeAnnotationId === id)
                this.#activeAnnotationId = null;
            this.render();
            this.#emitAnnotations('remove', id);
            return true;
        }
        getAnnotationAuthoringState() {
            this.#assertAlive();
            return {
                annotations: this.getAnnotations(),
                handles: this.#result?.scene.metadata.annotations?.entries.map((entry) => ({
                    ...entry,
                    bounds: { ...entry.bounds },
                    targetBounds: { ...entry.targetBounds },
                })) ?? [],
                canUndo: this.#annotationHistory.canUndo(),
                canRedo: this.#annotationHistory.canRedo(),
                ...(this.#activeAnnotationId === null ? {} : { activeId: this.#activeAnnotationId }),
            };
        }
        selectAnnotation(id) {
            this.#assertAlive();
            if (id !== null && !this.#annotations.some((annotation) => annotation.id === id)) {
                throw new GraflumeError('INVALID_SPEC', `Annotation "${id}" was not found.`);
            }
            if (id === this.#activeAnnotationId)
                return this;
            this.#activeAnnotationId = id;
            this.render();
            this.#emitAnnotations('select', id ?? undefined);
            return this;
        }
        editAnnotationWithKeyboard(id, key, options = {}) {
            this.#assertAlive();
            const annotation = this.#annotations.find((candidate) => candidate.id === id);
            if (annotation === undefined) {
                throw new GraflumeError('INVALID_SPEC', `Annotation "${id}" was not found.`);
            }
            const nextAnnotation = editAnnotationByKeyboard({ annotation, key, ...options });
            const next = this.#annotations.map((candidate) => candidate.id === id ? nextAnnotation : candidate);
            normalizeSpec$1({ ...this.#spec, annotations: next });
            if (!this.#annotationHistory.replace(next))
                return this;
            this.#annotations = [...this.#annotationHistory.annotations()];
            this.#activeAnnotationId = id;
            this.render();
            this.#emitAnnotations('keyboard', id);
            return this;
        }
        undoAnnotationEdit() {
            this.#assertAlive();
            if (!this.#annotationHistory.undo())
                return false;
            this.#annotations = [...this.#annotationHistory.annotations()];
            this.render();
            this.#emitAnnotations('undo', this.#activeAnnotationId ?? undefined);
            return true;
        }
        redoAnnotationEdit() {
            this.#assertAlive();
            if (!this.#annotationHistory.redo())
                return false;
            this.#annotations = [...this.#annotationHistory.annotations()];
            this.render();
            this.#emitAnnotations('redo', this.#activeAnnotationId ?? undefined);
            return true;
        }
        getMarkLabelState() {
            this.#assertAlive();
            const metadata = this.#result?.scene.metadata.markLabels;
            const labels = metadata?.entries.map((entry) => ({
                ...entry,
                target: cloneDatumTarget(entry.target),
                anchor: { ...entry.anchor },
                baseCenter: { ...entry.baseCenter },
                bounds: { ...entry.bounds },
            })) ?? [];
            return {
                enabled: metadata !== undefined,
                authoring: this.#markLabelAuthoring() !== false,
                labels,
                positions: this.#markLabels.positions(),
                canUndo: this.#markLabels.canUndo(),
                canRedo: this.#markLabels.canRedo(),
                ...(this.#activeMarkLabelId === null ? {} : { activeId: this.#activeMarkLabelId }),
            };
        }
        getMarkLabelPositions() {
            this.#assertAlive();
            return this.#markLabels.positions();
        }
        setMarkLabelPositions(positions) {
            this.#assertAlive();
            normalizeSpec$1(this.#specWithMarkLabelPositions(positions));
            if (!this.#markLabels.replace(positions))
                return this;
            this.render();
            this.#emitMarkLabels('set');
            return this;
        }
        setMarkLabelPosition(id, offsetX, offsetY) {
            this.#assertAlive();
            if (!Number.isFinite(offsetX) || !Number.isFinite(offsetY)) {
                throw new RangeError('Mark label offsets must be finite.');
            }
            if (Math.abs(offsetX) > 10_000 || Math.abs(offsetY) > 10_000) {
                throw new RangeError('Mark label offsets must stay within -10000..10000 pixels.');
            }
            const entry = this.#requireMarkLabel(id);
            const next = setMarkLabelOffset(this.#markLabels.positions(), entry.target, offsetX, offsetY);
            if (!this.#markLabels.replace(next))
                return this;
            this.#activeMarkLabelId = id;
            this.render();
            this.#emitMarkLabels('programmatic', id);
            return this;
        }
        selectMarkLabel(id) {
            this.#assertAlive();
            if (id !== null)
                this.#requireMarkLabel(id);
            if (id === this.#activeMarkLabelId)
                return this;
            this.#activeMarkLabelId = id;
            this.render();
            this.#emitMarkLabels('select', id ?? undefined);
            return this;
        }
        resetMarkLabelPositions() {
            this.#assertAlive();
            const configured = normalizeSpec$1(this.#spec).markLabels;
            const positions = configured === false ? [] : configured.positions;
            if (!this.#markLabels.replace(positions))
                return this;
            this.render();
            this.#emitMarkLabels('reset');
            return this;
        }
        undoMarkLabelEdit() {
            this.#assertAlive();
            if (!this.#markLabels.undo())
                return false;
            this.render();
            this.#emitMarkLabels('undo', this.#activeMarkLabelId ?? undefined);
            return true;
        }
        redoMarkLabelEdit() {
            this.#assertAlive();
            if (!this.#markLabels.redo())
                return false;
            this.render();
            this.#emitMarkLabels('redo', this.#activeMarkLabelId ?? undefined);
            return true;
        }
        setSpec(spec) {
            this.#assertAlive();
            const normalized = normalizeSpec$1(spec);
            this.pause();
            this.#spec = spec;
            this.#incrementalStores.clear();
            if (!this.#preserveStreamRuntimes) {
                for (const runtime of this.#streamRuntimes.values())
                    runtime.destroy();
                this.#streamRuntimes.clear();
                this.#workerRuntime?.close();
                this.#workerRuntime = null;
            }
            this.#annotations = normalized.annotations.map((annotation, index) => ({
                ...cloneAnnotation(annotation),
                id: annotationId(annotation, index),
            }));
            this.#annotationHistory.reset(this.#annotations);
            this.#activeAnnotationId = null;
            this.#selection = [];
            this.#analyticSelection.clear();
            this.#tableRuntime.clear();
            this.#networkRuntime.clear();
            this.#flowRuntime.clear();
            this.#navigatorRuntime.clear();
            this.#hierarchyRuntime.clear();
            this.#parallelRuntime.clear();
            this.#heatmapRuntime.clear();
            this.#scatterMatrixRuntime.clear();
            this.#familyFocus = null;
            this.#familyGesture = null;
            this.#technicalCrosshairValue = null;
            this.#domainView = emptyDomainViewState();
            this.#annotationsVisible = true;
            this.#hiddenLegendItems.clear();
            this.#configureInteraction(normalized, true);
            this.#resetMarkLabels(normalized);
            this.render();
            this.#configureResizeObserver();
            this.#emitPlayback('spec');
            this.#events.emit('legendchange', {
                chart: this,
                state: this.getLegendState(),
                reason: 'spec',
            });
            this.#emitSelection('spec');
            this.#emitAnalyticSelection('spec');
            this.#emitDomainView('spec');
            this.#emitAnnotations('spec');
            this.#emitAnnotationVisibility('spec');
            this.#emitMarkLabels('spec');
            this.#events.emit('familyfocuschange', { chart: this, state: null, reason: 'spec' });
            for (const layerId of new Set(this.#familyEntries('table-cell').map(({ layerId }) => layerId))) {
                this.#events.emit('tablechange', {
                    chart: this,
                    layerId,
                    state: this.getTableRuntimeState(layerId),
                    reason: 'spec',
                });
            }
            for (const layerId of new Set(this.#familyEntries('network-node').map(({ layerId }) => layerId))) {
                this.#events.emit('networkchange', {
                    chart: this,
                    layerId,
                    state: this.getNetworkRuntimeState(layerId),
                    reason: 'spec',
                });
            }
            for (const layerId of new Set(this.#familyEntries('flow-node').map(({ layerId }) => layerId))) {
                this.#events.emit('flowchange', {
                    chart: this,
                    layerId,
                    state: this.getFlowRuntimeState(layerId),
                    reason: 'spec',
                });
            }
            for (const layerId of new Set(this.#familyEntries('navigator-window').map(({ layerId }) => layerId))) {
                const interaction = this.#navigatorInteraction(layerId);
                this.#events.emit('navigatorchange', {
                    chart: this,
                    layerId,
                    family: interaction.family,
                    state: this.getNavigatorWindow(layerId),
                    reason: 'spec',
                });
            }
            for (const layerId of new Set(this.#familyEntries('hierarchy-node').map(({ layerId }) => layerId))) {
                this.#events.emit('hierarchychange', {
                    chart: this,
                    layerId,
                    state: this.getHierarchyRuntimeState(layerId),
                    reason: 'spec',
                });
            }
            for (const layerId of new Set(this.#familyEntries('parallel-axis').map(({ layerId }) => layerId))) {
                this.#events.emit('parallelchange', {
                    chart: this,
                    layerId,
                    state: this.getParallelRuntimeState(layerId),
                    reason: 'spec',
                });
            }
            for (const layerId of new Set(this.#familyEntries('heatmap-cell').map(({ layerId }) => layerId))) {
                this.#events.emit('heatmapchange', {
                    chart: this,
                    layerId,
                    state: this.getHeatmapBrush(layerId),
                    reason: 'spec',
                });
            }
            for (const layerId of new Set(this.#familyEntries('scatter-matrix-cell').map(({ layerId }) => layerId))) {
                const state = this.getScatterMatrixBrush(layerId);
                if (state !== null) {
                    this.#events.emit('scattermatrixchange', {
                        chart: this,
                        layerId,
                        state,
                        reason: 'spec',
                    });
                }
            }
            this.#startAutoplay();
            return this;
        }
        setData(data, layerId) {
            this.#assertAlive();
            if (layerId === undefined) {
                if (this.#spec.data !== undefined || this.#spec.layers === undefined) {
                    return this.setSpec({ ...this.#spec, data });
                }
                if (this.#spec.layers.length === 1) {
                    const onlyLayer = this.#spec.layers[0];
                    if (onlyLayer !== undefined) {
                        return this.setSpec({ ...this.#spec, layers: [{ ...onlyLayer, data }] });
                    }
                }
                throw new GraflumeError('INVALID_DATA', 'Specify layerId when replacing data in a multi-layer chart.');
            }
            let matched = false;
            const layers = this.#spec.layers?.map((layer, index) => {
                if ((layer.id ?? `layer-${index}`) !== layerId)
                    return layer;
                matched = true;
                return { ...layer, data };
            });
            if (!matched || layers === undefined) {
                throw new GraflumeError('INVALID_DATA', `Layer "${layerId}" was not found.`);
            }
            return this.setSpec({ ...this.#spec, layers });
        }
        appendData(rows, layerId) {
            this.#assertAlive();
            if (rows.length === 0)
                return this;
            if (this.#spec.streaming !== undefined) {
                return this.updateData({ mode: 'append', rows }, layerId);
            }
            if (layerId === undefined && this.#spec.data !== undefined) {
                return this.setSpec({ ...this.#spec, data: appendInput(this.#spec.data, rows) });
            }
            if (this.#spec.layers === undefined) {
                throw new GraflumeError('INVALID_DATA', 'The chart has no layer data to append to.');
            }
            const targetLayerId = layerId ??
                (this.#spec.layers.length === 1 ? (this.#spec.layers[0]?.id ?? 'layer-0') : undefined);
            if (targetLayerId === undefined) {
                throw new GraflumeError('INVALID_DATA', 'Specify layerId when appending to a multi-layer chart.');
            }
            let matched = false;
            const layers = this.#spec.layers.map((layer, index) => {
                if ((layer.id ?? `layer-${index}`) !== targetLayerId)
                    return layer;
                const source = layer.data ?? this.#spec.data;
                if (source === undefined) {
                    throw new GraflumeError('INVALID_DATA', `Layer "${targetLayerId}" has no data source.`);
                }
                matched = true;
                return { ...layer, data: appendInput(source, rows) };
            });
            if (!matched)
                throw new GraflumeError('INVALID_DATA', `Layer "${targetLayerId}" was not found.`);
            return this.setSpec({ ...this.#spec, layers });
        }
        /** Apply a bounded stable-key mutation configured by ChartSpec.streaming. */
        updateData(update, layerId) {
            this.#assertAlive();
            const streaming = this.#spec.streaming;
            if (streaming === undefined) {
                throw new GraflumeError('INVALID_DATA', 'updateData requires an explicit ChartSpec.streaming contract.');
            }
            const target = this.#streamingTarget(layerId);
            const store = this.#incrementalStores.get(target.id) ?? new IncrementalDataStore(target.source, streaming);
            const result = store.apply(update);
            this.setData(result.rows, target.layerId);
            this.#incrementalStores.set(target.id, store);
            return this;
        }
        upsertData(rows, layerId, watermark) {
            return this.updateData({ mode: 'upsert', rows, ...(watermark === undefined ? {} : { watermark }) }, layerId);
        }
        replaceLastData(rows, layerId, watermark) {
            return this.updateData({ mode: 'replaceLast', rows, ...(watermark === undefined ? {} : { watermark }) }, layerId);
        }
        /** Queue an authored streaming mutation through the bounded frame runtime. */
        async enqueueData(update, layerId, options = {}) {
            this.#assertAlive();
            const runtime = this.#streamRuntime(layerId);
            const result = await runtime.enqueue(update, options);
            this.#assertAlive();
            const visible = runtime.visible();
            if (visible !== null && visible.state.sequence === result.state.sequence) {
                this.#applyStreamingRows(visible.rows, layerId);
            }
            return this;
        }
        pauseStreaming(layerId) {
            this.#assertAlive();
            this.#streamRuntime(layerId).pause();
            return this;
        }
        resumeStreaming(layerId) {
            this.#assertAlive();
            this.#streamRuntime(layerId).resume();
            return this;
        }
        setStreamingFollowLive(follow, layerId) {
            this.#assertAlive();
            if (typeof follow !== 'boolean') {
                throw new GraflumeError('INVALID_DATA', 'Streaming follow-live state must be boolean.');
            }
            const runtime = this.#streamRuntime(layerId);
            runtime.setFollowLive(follow);
            const visible = runtime.visible();
            if (follow && visible !== null)
                this.#applyStreamingRows(visible.rows, layerId);
            return this;
        }
        getStreamRuntimeState(layerId) {
            this.#assertAlive();
            const target = this.#streamingTarget(layerId, false);
            return target === null ? null : (this.#streamRuntimes.get(target.id)?.state() ?? null);
        }
        getStreamingHistoryPage(cursor = 0, layerId) {
            this.#assertAlive();
            return this.#streamRuntime(layerId).historyPage(cursor);
        }
        /** Lazily constructs the module Worker declared by ChartSpec.streaming.worker. */
        getWorkerRuntime() {
            this.#assertAlive();
            const spec = this.#spec.streaming?.worker;
            if (spec === undefined) {
                throw new GraflumeError('INVALID_SPEC', 'getWorkerRuntime requires ChartSpec.streaming.worker.');
            }
            this.#workerRuntime ??= new AutomaticWorkerRuntime(spec, this.#options.workerFactory);
            return this.#workerRuntime;
        }
        getStreamingState(layerId) {
            this.#assertAlive();
            const target = this.#streamingTarget(layerId, false);
            return target === null ? null : (this.#incrementalStores.get(target.id)?.state() ?? null);
        }
        exportStreamingReplay(layerId) {
            this.#assertAlive();
            const target = this.#streamingTarget(layerId);
            const store = this.#incrementalStores.get(target.id);
            if (store === undefined) {
                throw new GraflumeError('INVALID_DATA', 'No incremental updates are available to replay.');
            }
            return store.exportReplay();
        }
        replayData(replay, layerId) {
            this.#assertAlive();
            const target = this.#streamingTarget(layerId);
            const streaming = this.#spec.streaming;
            if (streaming === undefined || !incrementalContractsMatch(replay.options, streaming)) {
                throw new GraflumeError('INVALID_DATA', 'Replay options do not match the current ChartSpec.streaming contract.');
            }
            const store = IncrementalDataStore.replay(replay);
            this.setData(store.rows(), target.layerId);
            this.#incrementalStores.set(target.id, store);
            return this;
        }
        resize(width, height) {
            this.#assertAlive();
            this.#manualWidth = width;
            this.#manualHeight = height;
            this.render();
            const scene = this.#result?.scene;
            if (scene !== undefined) {
                this.#events.emit('resize', { chart: this, width: scene.width, height: scene.height });
                this.#emitView('resize');
            }
            return this;
        }
        zoomBy(factor, anchor) {
            this.#assertAlive();
            const navigation = this.#requireNavigation();
            const scene = this.#result?.scene;
            if (scene === undefined)
                return this;
            this.#setView(zoomInspectionView(this.#view, factor, anchor ?? { x: scene.width / 2, y: scene.height / 2 }, {
                width: scene.width,
                height: scene.height,
                minZoom: navigation.minZoom,
                maxZoom: navigation.maxZoom,
            }), 'zoom');
            return this;
        }
        panBy(deltaX, deltaY) {
            this.#assertAlive();
            const navigation = this.#requireNavigation();
            const scene = this.#result?.scene;
            if (scene === undefined)
                return this;
            this.#setView(panInspectionView(this.#view, deltaX, deltaY, {
                width: scene.width,
                height: scene.height,
                minZoom: navigation.minZoom,
                maxZoom: navigation.maxZoom,
            }), 'pan');
            return this;
        }
        resetView() {
            this.#assertAlive();
            const navigation = this.#requireNavigation();
            const scene = this.#result?.scene;
            if (scene === undefined)
                return this;
            this.#setView(constrainInspectionView(identityInspectionView, {
                width: scene.width,
                height: scene.height,
                minZoom: navigation.minZoom,
                maxZoom: navigation.maxZoom,
            }), 'reset');
            return this;
        }
        getDomainViewState() {
            this.#assertAlive();
            return this.#domainView;
        }
        setDomainViewState(state) {
            this.#assertAlive();
            const navigation = this.#requireDomainNavigation();
            const next = normalizeDomainViewState(state);
            for (const axis of Object.keys(next.axes)) {
                if (!navigation.axes.includes(axis)) {
                    throw new GraflumeError('INVALID_SPEC', `Axis "${axis}" is not enabled by interaction.domainNavigation.axes.`);
                }
            }
            return this.#setDomainView(next, 'programmatic');
        }
        zoomDomainBy(factor, anchor, axes, viewId) {
            this.#assertAlive();
            const navigation = this.#requireDomainNavigation();
            const result = this.#result;
            if (result === null)
                return this;
            const located = anchor === undefined || viewId !== undefined ? null : this.#coordinateAt(anchor);
            const view = this.#coordinateView(viewId ?? located?.view.id);
            const selectedAxes = this.#domainAxes(navigation, axes);
            const point = anchor === undefined
                ? {
                    x: view.coordinates.plot.x + view.coordinates.plot.width / 2,
                    y: view.coordinates.plot.y + view.coordinates.plot.height / 2,
                }
                : (located?.point ?? this.#localPoint(view.id, anchor));
            let next = this.#domainView;
            for (const axis of selectedAxes) {
                next = zoomDomainAtPixel(next, view.coordinates, axis, factor, cartesianAxisChannel(view.coordinates, axis) === 'x' ? point.x : point.y, navigation.maxZoom);
            }
            return this.#setDomainView(next, 'zoom');
        }
        panDomainBy(deltaX, deltaY, axes, viewId) {
            this.#assertAlive();
            const navigation = this.#requireDomainNavigation();
            const result = this.#result;
            if (result === null)
                return this;
            const view = this.#coordinateView(viewId);
            let next = this.#domainView;
            for (const axis of this.#domainAxes(navigation, axes)) {
                next = panDomainByPixels(next, view.coordinates, axis, cartesianAxisChannel(view.coordinates, axis) === 'x' ? deltaX : deltaY);
            }
            return this.#setDomainView(next, 'pan');
        }
        resetDomainView() {
            this.#assertAlive();
            this.#requireDomainNavigation();
            return this.#setDomainView(emptyDomainViewState(), 'reset');
        }
        #resolvePlaybackReference(reference, fallback, bound) {
            if (reference === undefined)
                return fallback;
            if (typeof reference === 'string') {
                const named = this.#playbackNamedFrames.find((frame) => frame.name === reference);
                if (named === undefined) {
                    throw new GraflumeError('INVALID_SPEC', `Unknown playback frame name "${reference}".`, {
                        path: bound === 'target'
                            ? '$.interaction.playback.namedFrames'
                            : `$.interaction.playback.range.${bound}`,
                    });
                }
                return named.index;
            }
            if (!Number.isFinite(reference) || !Number.isInteger(reference)) {
                throw new RangeError('Playback frame index must be a finite integer.');
            }
            return reference;
        }
        play(from) {
            this.#assertAlive();
            if (this.#playback === false || this.#playbackFrames.length === 0)
                return this;
            if (from !== undefined)
                this.seek(from);
            const rangeLength = this.#playbackRange.end - this.#playbackRange.start + 1;
            if (rangeLength <= 1 || this.#playing)
                return this;
            if (typeof document !== 'undefined' && document.hidden)
                return this;
            const terminal = this.#playbackDirection === 'forward' ? this.#playbackRange.end : this.#playbackRange.start;
            if (from === undefined && this.#playbackIndex === terminal) {
                const previousIndex = this.#playbackIndex;
                const restart = this.#playbackDirection === 'forward' ? this.#playbackRange.start : this.#playbackRange.end;
                this.#renderPlaybackIndex(restart);
                this.#emitPlayback('seek', previousIndex);
            }
            this.#playing = true;
            this.#playbackTimestamp = null;
            this.#schedulePlaybackFrame();
            this.#emitPlayback('play');
            this.#syncControls();
            return this;
        }
        pause() {
            if (this.#destroyed || (!this.#playing && this.#sceneTransition === null))
                return this;
            this.#stopPlayback(true);
            return this;
        }
        step(delta = 1) {
            this.#assertAlive();
            if (!Number.isFinite(delta))
                throw new RangeError('Playback step must be finite.');
            if (this.#playback === false || this.#playbackFrames.length === 0)
                return this;
            const start = this.#playbackRange.start;
            const end = this.#playbackRange.end;
            const length = end - start + 1;
            let next = this.#playbackIndex + Math.trunc(delta);
            if (this.#playbackLoop)
                next = start + ((((next - start) % length) + length) % length);
            else
                next = Math.max(start, Math.min(end, next));
            if (next === this.#playbackIndex) {
                if (this.#playing && !this.#playbackLoop)
                    this.#stopPlayback(true);
                else if (this.#sceneTransition !== null)
                    this.#finishSceneTransition();
                return this;
            }
            const previousIndex = this.#playbackIndex;
            this.#renderPlaybackIndex(next);
            this.#emitPlayback('step', previousIndex);
            const terminal = this.#playbackDirection === 'forward' ? end : start;
            if (this.#playing && !this.#playbackLoop && next === terminal) {
                this.#stopPlayback(false);
            }
            return this;
        }
        seek(target) {
            this.#assertAlive();
            if (this.#playback === false || this.#playbackFrames.length === 0)
                return this;
            const resolved = this.#resolvePlaybackReference(target, this.#playbackRange.start, 'target');
            const named = typeof target === 'string';
            if (named && (resolved < this.#playbackRange.start || resolved > this.#playbackRange.end)) {
                throw new RangeError(`Named playback frame "${target}" is outside the active range.`);
            }
            const next = Math.max(this.#playbackRange.start, Math.min(this.#playbackRange.end, Math.trunc(resolved)));
            if (next === this.#playbackIndex) {
                if (this.#sceneTransition !== null)
                    this.#finishSceneTransition();
                return this;
            }
            const previousIndex = this.#playbackIndex;
            this.#renderPlaybackIndex(next);
            this.#emitPlayback('seek', previousIndex);
            return this;
        }
        setPlaybackDirection(direction) {
            this.#assertAlive();
            if (direction !== 'forward' && direction !== 'reverse') {
                throw new TypeError('Playback direction must be "forward" or "reverse".');
            }
            if (this.#playback === false || this.#playbackDirection === direction)
                return this;
            this.#playbackDirection = direction;
            this.#playbackTimestamp = null;
            this.#emitPlayback('direction');
            this.#syncControls();
            return this;
        }
        setPlaybackRange(range) {
            this.#assertAlive();
            if (this.#playback === false)
                return this;
            if (range !== undefined &&
                (typeof range !== 'object' || range === null || Array.isArray(range))) {
                throw new TypeError('Playback range must be an object.');
            }
            if (this.#playbackFrames.length === 0) {
                if (range !== undefined) {
                    throw new RangeError('Playback range cannot be set without available frames.');
                }
                return this;
            }
            const start = this.#resolvePlaybackReference(range?.start, 0, 'start');
            const end = this.#resolvePlaybackReference(range?.end, this.#playbackFrames.length - 1, 'end');
            if (start < 0 ||
                start >= this.#playbackFrames.length ||
                end < 0 ||
                end >= this.#playbackFrames.length) {
                throw new RangeError('Playback range must stay within the available frame indices.');
            }
            if (start > end)
                throw new RangeError('Playback range start must not exceed its end.');
            if (start === this.#playbackRange.start && end === this.#playbackRange.end)
                return this;
            const previousIndex = this.#playbackIndex;
            this.#playbackRange = { start, end };
            this.#playbackTimestamp = null;
            this.#renderPlaybackIndex(Math.max(start, Math.min(end, previousIndex)));
            this.#emitPlayback('range', previousIndex);
            return this;
        }
        setPlaybackRate(rate) {
            this.#assertAlive();
            if (!Number.isFinite(rate) || rate < 0.1 || rate > 16) {
                throw new RangeError('Playback rate must be from 0.1 to 16.');
            }
            if (this.#playback === false || this.#playbackRate === rate)
                return this;
            this.#playbackRate = rate;
            this.#emitPlayback('rate');
            this.#syncControls();
            return this;
        }
        setPlaybackLoop(loop) {
            this.#assertAlive();
            if (typeof loop !== 'boolean')
                throw new TypeError('Playback loop must be a boolean.');
            if (this.#playback === false || this.#playbackLoop === loop)
                return this;
            this.#playbackLoop = loop;
            this.#emitPlayback('loop');
            this.#syncControls();
            return this;
        }
        async toggleFullscreen() {
            this.#assertAlive();
            const host = this.#renderer?.overlayHost?.();
            if (host === null ||
                host === undefined ||
                typeof document === 'undefined' ||
                typeof host.requestFullscreen !== 'function' ||
                typeof document.exitFullscreen !== 'function') {
                throw new GraflumeError('UNSUPPORTED_RENDERER', 'Fullscreen is unavailable for the active chart surface.');
            }
            try {
                if (document.fullscreenElement === host)
                    await document.exitFullscreen();
                else {
                    if (document.fullscreenElement !== null)
                        await document.exitFullscreen();
                    await host.requestFullscreen();
                }
            }
            catch (error) {
                this.#events.emit('error', { chart: this, error });
                throw error;
            }
        }
        scheduleRender() {
            this.#assertAlive();
            this.#scheduler.schedule(() => {
                try {
                    this.render();
                }
                catch (error) {
                    this.#events.emit('error', { chart: this, error });
                }
            });
        }
        render() {
            this.#assertAlive();
            this.#cancelSceneTransition();
            return this.#renderEndpoint();
        }
        #renderEndpoint() {
            const dimensions = this.#measure();
            const familyRuntimeSpec = this.#familyRuntimeSpec(this.#spec);
            const playbackSpecInput = this.#playback === false
                ? familyRuntimeSpec
                : playbackSpec(familyRuntimeSpec, this.#playback, this.#playbackFrames, this.#playbackIndex, this.#playbackRange.start);
            // Fullscreen sizing is transient: it must override fixed chart dimensions
            // without mutating the caller's portable base spec.
            const effectiveSpec = this.#fullscreen
                ? { ...playbackSpecInput, width: 'container', height: 'container' }
                : playbackSpecInput;
            const analyticSelectionDraft = this.#analyticSelectionDraft();
            const result = compileWithRegistry(effectiveSpec, this.#registry, dimensions, {
                hiddenLegendItemIds: this.#hiddenLegendItems,
                annotations: this.#annotations,
                annotationsVisible: this.#annotationsVisible,
                ...(this.#activeAnnotationId === null
                    ? {}
                    : { activeAnnotationId: this.#activeAnnotationId }),
                markLabelPositions: this.#markLabels.positions(),
                ...(this.#activeMarkLabelId === null ? {} : { activeMarkLabelId: this.#activeMarkLabelId }),
                selection: this.#selection,
                analyticSelection: this.#analyticSelection.get(),
                ...(analyticSelectionDraft === undefined ? {} : { analyticSelectionDraft }),
                domainView: this.#domainView,
                ...(this.#technicalCrosshairValue === null
                    ? {}
                    : { technicalCrosshairValue: this.#technicalCrosshairValue }),
            });
            this.#validateAnalyticCapabilities(result);
            const factory = this.#registry.resolveRenderer(result.spec.renderer);
            const pixelRatio = this.#pixelRatio();
            const rendererChanged = this.#renderer === null || this.#rendererName !== factory.name;
            if (rendererChanged) {
                this.#detachSurfaceEvents();
                this.#controls.destroy();
                this.#legend.destroy();
                this.#accessibility.destroy();
                this.#destroyMarkLabelLive();
                this.#destroySelectionLive();
                this.#renderer?.destroy();
                this.#renderer = factory.create();
                this.#rendererName = factory.name;
                this.#renderer.mount(this.#target, {
                    width: result.scene.width,
                    height: result.scene.height,
                    pixelRatio,
                    ariaLabel: result.scene.accessibility.label,
                    ...(result.scene.accessibility.description === undefined
                        ? {}
                        : { ariaDescription: result.scene.accessibility.description }),
                });
            }
            else {
                const renderer = this.#renderer;
                if (renderer === null) {
                    throw new GraflumeError('UNSUPPORTED_RENDERER', 'The active renderer is unavailable.');
                }
                renderer.resize(result.scene.width, result.scene.height, pixelRatio);
                const surface = renderer.surface();
                surface?.setAttribute('aria-label', result.scene.accessibility.label);
                if (result.scene.accessibility.description === undefined) {
                    surface?.removeAttribute('aria-description');
                }
                else {
                    surface?.setAttribute('aria-description', result.scene.accessibility.description);
                }
            }
            this.#result = result;
            this.#constrainViewToScene();
            const renderer = this.#renderer;
            if (renderer === null) {
                throw new GraflumeError('UNSUPPORTED_RENDERER', 'The active renderer is unavailable.');
            }
            renderer.setInspectionView?.(this.#view);
            renderer.render(result.scene);
            this.#displayScene = result.scene;
            this.#syncSurfaceEvents();
            this.#syncControls();
            this.#syncLegend();
            this.#syncAccessibilityMirror();
            this.#syncMarkLabelAccessibility();
            this.#syncSelectionAccessibility();
            this.#events.emit('render', { chart: this, scene: result.scene });
            return this;
        }
        #validateAnalyticCapabilities(result) {
            const requireAxis = (view, axis, feature, continuous) => {
                const scale = view.coordinates.axes[axis];
                const viewLabel = view.label === '' ? view.id : `${view.id} (${view.label})`;
                if (scale === undefined) {
                    throw new GraflumeError('INCOMPATIBLE_SCALE', `${feature} requires resolved axis "${axis}" in coordinate view "${viewLabel}".`, { path: `$.axes.${axis}` });
                }
                if (scale.invert === undefined &&
                    (continuous || (scale.kind !== 'band' && scale.kind !== 'point'))) {
                    throw new GraflumeError('INCOMPATIBLE_SCALE', continuous
                        ? `${feature} requires an invertible continuous axis; "${axis}" in coordinate view "${viewLabel}" uses "${scale.kind}".`
                        : `${feature} requires an invertible continuous, band, or point axis; "${axis}" in coordinate view "${viewLabel}" uses "${scale.kind}".`, { path: `$.axes.${axis}` });
                }
            };
            const navigation = result.spec.interaction.domainNavigation;
            if (navigation !== false) {
                for (const view of result.coordinateViews) {
                    for (const axis of navigation.axes)
                        requireAxis(view, axis, 'Domain navigation', false);
                }
            }
            const selection = result.spec.interaction.selection;
            if (selection === false) {
                if (this.#analyticSelection.get().selections.length > 0) {
                    throw new GraflumeError('INVALID_SPEC', 'Analytic selection state requires interaction.selection.');
                }
                return;
            }
            this.#validateAnalyticStateForConfig(this.#analyticSelection.get(), selection);
            for (const view of result.coordinateViews) {
                if (selection.kind === 'axis') {
                    requireAxis(view, selection.axis, 'Axis selection', false);
                }
                else if (selection.kind === 'interval' ||
                    selection.kind === 'rectangle' ||
                    selection.kind === 'lasso') {
                    const continuous = selection.kind === 'lasso';
                    requireAxis(view, selection.xAxis, `${selection.kind} selection`, continuous);
                    requireAxis(view, selection.yAxis, `${selection.kind} selection`, continuous);
                }
            }
        }
        toDataURL(type, quality) {
            this.#assertAlive();
            if (this.#renderer?.toDataURL === undefined) {
                throw new GraflumeError('UNSUPPORTED_RENDERER', 'The active renderer cannot export a data URL.');
            }
            return this.#renderer.toDataURL(type, quality);
        }
        destroy() {
            if (this.#destroyed)
                return;
            const exitFullscreen = this.#isOwnFullscreen();
            this.#playing = false;
            this.#sceneTransition = null;
            this.#cancelPlaybackFrame();
            this.#scheduler.cancel();
            this.#resizeObserver?.disconnect();
            this.#resizeObserver = null;
            if (this.#windowResizeListener !== null && typeof window !== 'undefined') {
                window.removeEventListener('resize', this.#windowResizeListener);
            }
            this.#windowResizeListener = null;
            this.#detachEnvironmentListeners();
            if (exitFullscreen &&
                typeof document !== 'undefined' &&
                typeof document.exitFullscreen === 'function') {
                void document.exitFullscreen().catch(() => undefined);
            }
            this.#fullscreen = false;
            this.#detachSurfaceEvents();
            this.#controls.destroy();
            this.#legend.destroy();
            this.#accessibility.destroy();
            this.#linkedFocusUnregister?.();
            this.#linkedFocusUnsubscribe?.();
            this.#linkedViewUnregister?.();
            this.#linkedFocusUnregister = null;
            this.#linkedFocusUnsubscribe = null;
            this.#linkedViewUnregister = null;
            for (const runtime of this.#streamRuntimes.values())
                runtime.destroy();
            this.#streamRuntimes.clear();
            this.#workerRuntime?.close({ terminate: true });
            this.#workerRuntime = null;
            this.#destroyMarkLabelLive();
            this.#destroySelectionLive();
            this.#tooltip.destroy();
            this.#renderer?.destroy();
            this.#renderer = null;
            this.#result = null;
            this.#displayScene = null;
            this.#events.clear();
            this.#destroyed = true;
        }
        #configureInteraction(spec, reset) {
            this.#cancelActiveGesture();
            const navigation = spec.interaction.navigation;
            if (navigation === false)
                this.#view = identityInspectionView;
            else if (reset)
                this.#view = { zoom: navigation.minZoom, offsetX: 0, offsetY: 0 };
            if (reset)
                this.#domainView = emptyDomainViewState();
            this.#playback = spec.interaction.playback;
            if (this.#playback === false) {
                this.#playbackFrames = [];
                this.#playbackIndex = 0;
                this.#playbackRate = 1;
                this.#playbackLoop = false;
                this.#playbackDirection = 'forward';
                this.#playbackRange = { start: 0, end: -1 };
                this.#playbackNamedFrames = [];
                return;
            }
            this.#playbackFrames = collectPlaybackFrames(this.#spec, this.#playback);
            const timeline = resolvePlaybackTimeline(this.#playbackFrames, this.#playback);
            this.#playbackRate = this.#playback.rate;
            this.#playbackLoop = this.#playback.loop;
            this.#playbackDirection = this.#playback.direction;
            this.#playbackRange = timeline.range;
            this.#playbackNamedFrames = timeline.namedFrames;
            const initial = configuredFrame(this.#spec);
            const initialIndex = initial === undefined
                ? -1
                : this.#playbackFrames.findIndex((frame) => playbackFrameKey(frame) === playbackFrameKey(initial));
            const initialInRange = initialIndex >= this.#playbackRange.start && initialIndex <= this.#playbackRange.end;
            const directionalStart = this.#playbackDirection === 'forward' ? this.#playbackRange.start : this.#playbackRange.end;
            this.#playbackIndex = initialInRange
                ? initialIndex
                : this.#playback.autoplay || this.#playback.mode === 'frame'
                    ? Math.max(0, directionalStart)
                    : Math.max(0, this.#playbackRange.end);
        }
        #navigation() {
            return this.#result?.spec.interaction.navigation ?? false;
        }
        #domainNavigation() {
            return this.#result?.spec.interaction.domainNavigation ?? false;
        }
        #requireDomainNavigation() {
            const navigation = this.#domainNavigation();
            if (navigation === false) {
                throw new GraflumeError('INVALID_SPEC', 'Enable interaction.domainNavigation before changing data domains.');
            }
            return navigation;
        }
        #domainAxes(navigation, requested) {
            const axes = requested ?? navigation.axes;
            if (axes.length === 0 || new Set(axes).size !== axes.length) {
                throw new GraflumeError('INVALID_SPEC', 'Domain navigation axes must be non-empty and unique.');
            }
            for (const axis of axes) {
                if (!navigation.axes.includes(axis)) {
                    throw new GraflumeError('INVALID_SPEC', `Axis "${axis}" is not enabled by interaction.domainNavigation.axes.`);
                }
            }
            return axes;
        }
        #setDomainView(state, reason) {
            const next = normalizeDomainViewState(state);
            if (sameDomainView(next, this.#domainView))
                return this;
            const previous = this.#domainView;
            this.#domainView = next;
            this.#tooltip.hide();
            try {
                this.render();
            }
            catch (error) {
                this.#domainView = previous;
                throw error;
            }
            this.#emitDomainView(reason);
            return this;
        }
        #emitDomainView(reason) {
            this.#events.emit('domainviewchange', { chart: this, state: this.#domainView, reason });
            if (!this.#applyingLinkedViewState) {
                this.#options.linkedViewStore?.setDomainView(this.#domainView, this.#semanticViewId);
            }
        }
        #requireNavigation() {
            const navigation = this.#navigation();
            if (navigation === false) {
                throw new GraflumeError('INVALID_SPEC', 'Enable interaction.navigation before changing the inspection viewport.');
            }
            if (this.#renderer?.capabilities.inspectionViewport !== true ||
                this.#renderer.setInspectionView === undefined) {
                throw new GraflumeError('UNSUPPORTED_RENDERER', 'The active renderer does not support inspection viewport navigation.');
            }
            return navigation;
        }
        #viewBounds(navigation) {
            const scene = this.#result?.scene;
            return scene === undefined
                ? null
                : {
                    width: scene.width,
                    height: scene.height,
                    minZoom: navigation.minZoom,
                    maxZoom: navigation.maxZoom,
                };
        }
        #constrainViewToScene() {
            const navigation = this.#navigation();
            if (navigation === false) {
                this.#view = identityInspectionView;
                return;
            }
            const bounds = this.#viewBounds(navigation);
            if (bounds !== null)
                this.#view = constrainInspectionView(this.#view, bounds);
        }
        #setView(view, reason) {
            if (sameView(this.#view, view))
                return;
            this.#view = view;
            this.#tooltip.hide();
            const scene = this.#displayScene ?? this.#result?.scene;
            if (scene !== undefined) {
                this.#renderer?.setInspectionView?.(this.#view);
                this.#renderer?.render(scene);
            }
            this.#syncSurfaceConfiguration();
            this.#syncControls();
            this.#syncLegend();
            this.#syncAccessibilityMirror();
            this.#emitView(reason);
        }
        #emitView(reason) {
            this.#events.emit('viewchange', { chart: this, view: this.getViewState(), reason });
        }
        #emitPlayback(reason, previousIndex) {
            const state = this.getPlaybackState();
            this.#events.emit('playbackchange', {
                chart: this,
                state,
                reason,
            });
            if (reason === 'spec' ||
                (previousIndex !== undefined && previousIndex !== this.#playbackIndex)) {
                this.#events.emit('playbackframechange', {
                    chart: this,
                    state,
                    reason: reason === 'step' || reason === 'seek' || reason === 'range' ? reason : 'spec',
                    ...(previousIndex === undefined ? {} : { previousIndex }),
                    label: state.label,
                });
            }
        }
        #renderPlaybackIndex(index) {
            const from = this.#displayScene ?? this.#result?.scene ?? null;
            const previousRenderer = this.#renderer;
            this.#cancelSceneTransition();
            this.#playbackIndex = index;
            this.#renderEndpoint();
            const playback = this.#playback;
            const to = this.#result?.scene;
            const renderer = this.#renderer;
            if (playback === false ||
                playback.transition === false ||
                this.#reducedMotion?.matches === true ||
                from === null ||
                to === undefined ||
                renderer === null ||
                renderer !== previousRenderer ||
                from.width !== to.width ||
                from.height !== to.height) {
                return;
            }
            this.#sceneTransition = {
                from,
                to,
                duration: effectivePlaybackTransitionDuration(playback),
                easing: playback.transition.easing,
                keyField: playback.key,
                elapsed: 0,
                previousTimestamp: this.#playing && this.#playbackTimestamp !== null ? this.#playbackTimestamp : null,
            };
            this.#displayScene = from;
            this.#tooltip.hide();
            renderer.render(from);
            this.#schedulePlaybackFrame();
        }
        #advanceSceneTransition(timestamp) {
            const transition = this.#sceneTransition;
            if (transition === null)
                return;
            if (transition.previousTimestamp === null) {
                transition.previousTimestamp = timestamp;
                return;
            }
            transition.elapsed +=
                Math.max(0, timestamp - transition.previousTimestamp) * this.#playbackRate;
            transition.previousTimestamp = timestamp;
            const progress = transition.elapsed / transition.duration;
            if (progress >= 1) {
                this.#finishSceneTransition();
                return;
            }
            const scene = interpolateScene(transition.from, transition.to, easeSceneProgress(progress, transition.easing), transition.keyField === undefined ? {} : { keyField: transition.keyField });
            this.#renderer?.render(scene);
            this.#displayScene = scene;
        }
        #cancelSceneTransition() {
            if (this.#sceneTransition === null)
                return;
            this.#sceneTransition = null;
            this.#tooltip.hide();
            if (!this.#playing)
                this.#cancelScheduledPlaybackFrame();
        }
        #finishSceneTransition() {
            const transition = this.#sceneTransition;
            if (transition === null)
                return;
            this.#sceneTransition = null;
            this.#renderer?.render(transition.to);
            this.#displayScene = transition.to;
            this.#tooltip.hide();
            if (!this.#playing)
                this.#cancelScheduledPlaybackFrame();
        }
        #stopPlayback(settleTransition) {
            const wasPlaying = this.#playing;
            this.#playing = false;
            this.#playbackTimestamp = null;
            if (settleTransition)
                this.#finishSceneTransition();
            if (this.#sceneTransition === null)
                this.#cancelScheduledPlaybackFrame();
            else
                this.#schedulePlaybackFrame();
            if (!wasPlaying)
                return;
            this.#emitPlayback('pause');
            this.#syncControls();
        }
        #startAutoplay() {
            if (this.#playback !== false &&
                this.#playback.autoplay &&
                this.#reducedMotion?.matches !== true) {
                this.play();
            }
        }
        #schedulePlaybackFrame() {
            if (this.#playbackCancel !== null || (!this.#playing && this.#sceneTransition === null)) {
                return;
            }
            const run = (timestamp) => {
                this.#playbackCancel = null;
                if (this.#destroyed)
                    return;
                this.#advanceSceneTransition(timestamp);
                if (this.#playing && this.#playback !== false) {
                    if (this.#playbackTimestamp === null)
                        this.#playbackTimestamp = timestamp;
                    const duration = this.#playback.interval / this.#playbackRate;
                    if (timestamp - this.#playbackTimestamp >= duration) {
                        this.#playbackTimestamp = timestamp;
                        // Sparse RAF delivery can skip past the effective tween duration.
                        // Settle before advancing so transient crossfade/exit nodes never
                        // accumulate as inputs to the next automatic frame.
                        this.#finishSceneTransition();
                        this.step(this.#playbackDirection === 'forward' ? 1 : -1);
                    }
                }
                if (this.#playing || this.#sceneTransition !== null)
                    this.#schedulePlaybackFrame();
            };
            if (typeof requestAnimationFrame === 'function') {
                const handle = requestAnimationFrame(run);
                this.#playbackCancel = () => cancelAnimationFrame(handle);
            }
            else {
                const handle = setTimeout(() => run(typeof performance === 'undefined' ? Date.now() : performance.now()), 16);
                this.#playbackCancel = () => clearTimeout(handle);
            }
        }
        #cancelPlaybackFrame() {
            this.#cancelScheduledPlaybackFrame();
            this.#playbackTimestamp = null;
        }
        #cancelScheduledPlaybackFrame() {
            this.#playbackCancel?.();
            this.#playbackCancel = null;
        }
        #configureEnvironmentListeners() {
            if (typeof document !== 'undefined') {
                document.addEventListener('visibilitychange', this.#visibilityListener);
                document.addEventListener('fullscreenchange', this.#fullscreenListener);
            }
            if (typeof window !== 'undefined' && typeof window.matchMedia === 'function') {
                this.#reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)');
                this.#reducedMotion.addEventListener?.('change', this.#reducedMotionListener);
            }
        }
        #detachEnvironmentListeners() {
            if (typeof document !== 'undefined') {
                document.removeEventListener('visibilitychange', this.#visibilityListener);
                document.removeEventListener('fullscreenchange', this.#fullscreenListener);
            }
            this.#reducedMotion?.removeEventListener?.('change', this.#reducedMotionListener);
            this.#reducedMotion = null;
        }
        #measure() {
            const fullscreenHost = this.#renderer?.overlayHost?.();
            if (this.#fullscreen && fullscreenHost !== null && fullscreenHost !== undefined) {
                const bounds = fullscreenHost.getBoundingClientRect();
                const width = bounds.width || (typeof window === 'undefined' ? 640 : window.innerWidth);
                const height = bounds.height || (typeof window === 'undefined' ? 400 : window.innerHeight);
                return { width: Math.max(1, width), height: Math.max(1, height) };
            }
            const width = this.#manualWidth ??
                (typeof this.#spec.width === 'number' ? this.#spec.width : this.#target.clientWidth || 640);
            const height = this.#manualHeight ??
                (typeof this.#spec.height === 'number'
                    ? this.#spec.height
                    : this.#target.clientHeight || 400);
            return { width: Math.max(1, width), height: Math.max(1, height) };
        }
        #pixelRatio() {
            const ratio = this.#options.pixelRatio ??
                (typeof window === 'undefined' ? 1 : window.devicePixelRatio || 1);
            return Math.max(1, Math.min(3, ratio));
        }
        #configureResizeObserver() {
            this.#resizeObserver?.disconnect();
            this.#resizeObserver = null;
            if (this.#windowResizeListener !== null && typeof window !== 'undefined') {
                window.removeEventListener('resize', this.#windowResizeListener);
                this.#windowResizeListener = null;
            }
            if (this.#options.autoResize === false)
                return;
            const responsive = this.#spec.width !== undefined ? this.#spec.width === 'container' : true;
            if (!responsive)
                return;
            if (typeof ResizeObserver === 'function') {
                let previousWidth = this.#target.clientWidth;
                let previousHeight = this.#target.clientHeight;
                this.#resizeObserver = new ResizeObserver(() => {
                    const width = this.#target.clientWidth;
                    const height = this.#target.clientHeight;
                    if (width === previousWidth && height === previousHeight)
                        return;
                    previousWidth = width;
                    previousHeight = height;
                    this.scheduleRender();
                });
                this.#resizeObserver.observe(this.#target);
            }
            else if (typeof window !== 'undefined') {
                this.#windowResizeListener = () => this.scheduleRender();
                window.addEventListener('resize', this.#windowResizeListener, { passive: true });
            }
        }
        #syncSurfaceEvents() {
            const surface = this.#renderer?.surface() ?? null;
            if (surface !== this.#eventSurface) {
                this.#detachSurfaceEvents();
                if (surface === null)
                    return;
                this.#eventSurface = surface;
                this.#surfaceTouchAction = surface.style.touchAction;
                this.#surfaceTabIndex = surface.getAttribute('tabindex');
                this.#surfaceCursor = surface.style.cursor;
                this.#surfaceAriaKeyShortcuts = surface.getAttribute('aria-keyshortcuts');
                surface.addEventListener('pointermove', this.#pointerMoveListener, { passive: false });
                surface.addEventListener('pointerdown', this.#pointerDownListener, { passive: false });
                surface.addEventListener('pointerup', this.#pointerUpListener, { passive: false });
                surface.addEventListener('pointercancel', this.#pointerCancelListener, { passive: false });
                surface.addEventListener('pointerleave', this.#pointerLeaveListener, { passive: true });
                surface.addEventListener('click', this.#clickListener, { passive: true });
                surface.addEventListener('wheel', this.#wheelListener, { passive: false });
                surface.addEventListener('keydown', this.#keyDownListener);
            }
            this.#syncSurfaceConfiguration();
        }
        #syncSurfaceConfiguration() {
            const surface = this.#eventSurface;
            if (surface === null)
                return;
            const navigation = this.#navigation();
            const domainNavigation = this.#domainNavigation();
            const selection = this.#result?.spec.interaction.selection;
            const markLabelAuthoring = this.#markLabelAuthoring();
            const annotationAuthoring = (this.#result?.scene.metadata.annotations?.entries.length ?? 0) > 0;
            const familyKeyboard = this.#familyEntries('pie-slice').length > 0 || this.#familyEntries('table-cell').length > 0;
            const familyPointer = this.#familyEntries('network-node').length > 0 ||
                this.#familyEntries('flow-node').length > 0 ||
                this.#familyEntries('navigator-window').length > 0 ||
                this.#familyEntries('hierarchy-node').length > 0 ||
                this.#familyEntries('parallel-axis').length > 0 ||
                this.#familyEntries('heatmap-cell').length > 0 ||
                this.#familyEntries('scatter-matrix-cell').length > 0;
            const analyticDrag = selection !== undefined && selection !== false && selection.kind !== 'point';
            const analyticKeyboard = analyticDrag && selection.keyboard;
            const inspecting = this.#view.zoom > 1 || this.#view.offsetX !== 0 || this.#view.offsetY !== 0;
            surface.style.touchAction =
                annotationAuthoring ||
                    familyPointer ||
                    (markLabelAuthoring !== false && markLabelAuthoring.pointer) ||
                    analyticDrag ||
                    (domainNavigation !== false && domainNavigation.drag)
                    ? 'none'
                    : navigation !== false && (navigation.drag || navigation.pinch)
                        ? inspecting
                            ? 'none'
                            : 'pan-y'
                        : (this.#surfaceTouchAction ?? '');
            surface.style.cursor =
                annotationAuthoring && this.#activeAnnotationId !== null
                    ? 'move'
                    : markLabelAuthoring !== false && markLabelAuthoring.pointer
                        ? 'move'
                        : familyPointer
                            ? 'grab'
                            : analyticDrag
                                ? 'crosshair'
                                : domainNavigation !== false && domainNavigation.drag
                                    ? 'grab'
                                    : navigation !== false && navigation.drag
                                        ? 'grab'
                                        : (this.#surfaceCursor ?? '');
            if ((navigation !== false && navigation.keyboard) ||
                (domainNavigation !== false && domainNavigation.keyboard) ||
                annotationAuthoring ||
                (markLabelAuthoring !== false && markLabelAuthoring.keyboard) ||
                analyticKeyboard ||
                familyKeyboard ||
                this.#result?.spec.accessibility.navigation === true ||
                (selection !== undefined && selection !== false && selection.clearOnEscape))
                surface.tabIndex = 0;
            else if (this.#surfaceTabIndex === null)
                surface.removeAttribute('tabindex');
            else
                surface.setAttribute('tabindex', this.#surfaceTabIndex);
            if (annotationAuthoring ||
                (markLabelAuthoring !== false && markLabelAuthoring.keyboard) ||
                analyticKeyboard ||
                familyKeyboard) {
                const shortcuts = [
                    'Enter',
                    'Shift+Enter',
                    'ArrowUp',
                    'ArrowDown',
                    'ArrowLeft',
                    'ArrowRight',
                    'Escape',
                ];
                if (annotationAuthoring || (markLabelAuthoring !== false && markLabelAuthoring.keyboard)) {
                    shortcuts.push('Alt+ArrowLeft', 'Alt+ArrowRight', 'Control+Z', 'Control+Y', 'Meta+Z', 'Meta+Y');
                }
                if (analyticKeyboard)
                    shortcuts.push('S', 'Space');
                if (familyKeyboard)
                    shortcuts.push('Home', 'End', 'PageUp', 'PageDown', 'Space');
                surface.setAttribute('aria-keyshortcuts', [...new Set(shortcuts)].join(' '));
            }
            else if (this.#surfaceAriaKeyShortcuts === null)
                surface.removeAttribute('aria-keyshortcuts');
            else
                surface.setAttribute('aria-keyshortcuts', this.#surfaceAriaKeyShortcuts);
        }
        #detachSurfaceEvents() {
            const surface = this.#eventSurface;
            if (surface === null)
                return;
            this.#cancelActiveGesture();
            this.#tooltip.hide();
            surface.removeEventListener('pointermove', this.#pointerMoveListener);
            surface.removeEventListener('pointerdown', this.#pointerDownListener);
            surface.removeEventListener('pointerup', this.#pointerUpListener);
            surface.removeEventListener('pointercancel', this.#pointerCancelListener);
            surface.removeEventListener('pointerleave', this.#pointerLeaveListener);
            surface.removeEventListener('click', this.#clickListener);
            surface.removeEventListener('wheel', this.#wheelListener);
            surface.removeEventListener('keydown', this.#keyDownListener);
            surface.style.touchAction = this.#surfaceTouchAction ?? '';
            surface.style.cursor = this.#surfaceCursor ?? '';
            if (this.#surfaceTabIndex === null)
                surface.removeAttribute('tabindex');
            else
                surface.setAttribute('tabindex', this.#surfaceTabIndex);
            if (this.#surfaceAriaKeyShortcuts === null)
                surface.removeAttribute('aria-keyshortcuts');
            else
                surface.setAttribute('aria-keyshortcuts', this.#surfaceAriaKeyShortcuts);
            this.#eventSurface = null;
            this.#surfaceTouchAction = null;
            this.#surfaceTabIndex = null;
            this.#surfaceCursor = null;
            this.#surfaceAriaKeyShortcuts = null;
        }
        #cancelActiveGesture() {
            const surface = this.#eventSurface;
            if (surface !== null) {
                const pointerIds = new Set(this.#activePointers.keys());
                if (this.#analyticGesture !== null)
                    pointerIds.add(this.#analyticGesture.pointerId);
                if (this.#domainGesture !== null)
                    pointerIds.add(this.#domainGesture.pointerId);
                if (this.#annotationGesture !== null)
                    pointerIds.add(this.#annotationGesture.pointerId);
                if (this.#markLabelGesture !== null)
                    pointerIds.add(this.#markLabelGesture.pointerId);
                if (this.#familyGesture !== null)
                    pointerIds.add(this.#familyGesture.pointerId);
                for (const pointerId of pointerIds) {
                    if (surface.hasPointerCapture?.(pointerId))
                        surface.releasePointerCapture?.(pointerId);
                }
                surface.style.cursor = this.#surfaceCursor ?? '';
            }
            this.#activePointers.clear();
            if (this.#annotationGesture !== null) {
                this.#annotationHistory.restore(this.#annotationGesture.previous);
                this.#annotations = [...this.#annotationHistory.annotations()];
            }
            this.#annotationGesture = null;
            if (this.#markLabelGesture !== null)
                this.#markLabels.restore(this.#markLabelGesture.previous);
            this.#markLabelGesture = null;
            if (this.#familyGesture?.kind === 'network-lasso') {
                this.#networkRuntime.set(this.#familyGesture.layerId, cloneNetworkRuntimeState(this.#familyGesture.previous));
            }
            else if (this.#familyGesture?.kind === 'network-node' &&
                this.#familyGesture.previousNetwork !== undefined) {
                this.#networkRuntime.set(this.#familyGesture.layerId, cloneNetworkRuntimeState(this.#familyGesture.previousNetwork));
            }
            else if (this.#familyGesture?.kind === 'flow-node' &&
                this.#familyGesture.previousFlow !== undefined) {
                this.#flowRuntime.set(this.#familyGesture.layerId, cloneFlowRuntimeState(this.#familyGesture.previousFlow));
            }
            else if (this.#familyGesture?.kind === 'navigator-window') {
                this.#navigatorRuntime.set(this.#familyGesture.layerId, {
                    family: this.#familyGesture.interaction.family,
                    state: cloneNavigatorRuntimeState(this.#familyGesture.previous),
                });
            }
            else if (this.#familyGesture?.kind === 'parallel-brush') {
                this.#parallelRuntime.set(this.#familyGesture.layerId, cloneParallelRuntimeState(this.#familyGesture.previous));
            }
            else if (this.#familyGesture?.kind === 'heatmap-brush') {
                this.#heatmapRuntime.set(this.#familyGesture.layerId, cloneHeatmapRuntimeState(this.#familyGesture.previous));
            }
            else if (this.#familyGesture?.kind === 'scatter-matrix-brush') {
                if (this.#familyGesture.previous === null) {
                    this.#scatterMatrixRuntime.delete(this.#familyGesture.layerId);
                }
                else {
                    this.#scatterMatrixRuntime.set(this.#familyGesture.layerId, cloneScatterMatrixRuntimeState(this.#familyGesture.previous));
                }
            }
            this.#familyGesture = null;
            this.#analyticGesture = null;
            this.#analyticKeyboardGesture = null;
            this.#domainGesture = null;
            this.#dragPrevious = null;
            this.#pinchStart = null;
            this.#dragDistance = 0;
            this.#suppressClick = false;
        }
        #surfacePoint(event) {
            const surface = this.#eventSurface;
            const scene = this.#result?.scene;
            if (surface === null || scene === undefined)
                return null;
            const bounds = surface.getBoundingClientRect();
            return {
                x: ((event.clientX - bounds.left) / Math.max(1, bounds.width)) * scene.width,
                y: ((event.clientY - bounds.top) / Math.max(1, bounds.height)) * scene.height,
            };
        }
        #updateTechnicalCrosshair(event, render = true) {
            const scene = this.#result?.scene;
            const surfacePoint = this.#surfacePoint(event);
            if (scene === undefined || surfacePoint === null)
                return false;
            const scenePoint = inverseInspectionPoint(this.#view, surfacePoint);
            const panel = scene.metadata.technicalIndicatorPanels?.find(({ bounds }) => scenePoint.x >= bounds.x &&
                scenePoint.x <= bounds.x + bounds.width &&
                scenePoint.y >= bounds.y &&
                scenePoint.y <= bounds.y + bounds.height);
            if (panel === undefined)
                return this.#clearTechnicalCrosshair(render);
            const coordinate = this.#coordinateAt(scenePoint);
            if (coordinate === null)
                return this.#clearTechnicalCrosshair(render);
            let value;
            try {
                value = pixelToDomain(coordinate.view.coordinates, 'x', coordinate.point.x);
            }
            catch {
                return this.#clearTechnicalCrosshair(render);
            }
            if (typeof value === 'number' && !Number.isFinite(value)) {
                return this.#clearTechnicalCrosshair(render);
            }
            if (this.#technicalCrosshairValue === value)
                return false;
            this.#technicalCrosshairValue = value;
            if (render)
                this.render();
            return true;
        }
        #clearTechnicalCrosshair(render = true) {
            if (this.#technicalCrosshairValue === null)
                return false;
            this.#technicalCrosshairValue = null;
            if (render)
                this.render();
            return true;
        }
        #handleWheel(event) {
            const domainNavigation = this.#domainNavigation();
            if (domainNavigation !== false &&
                domainNavigation.wheel !== 'off' &&
                (domainNavigation.wheel !== 'modifier' || event.ctrlKey || event.metaKey)) {
                const point = this.#surfacePoint(event);
                if (point === null)
                    return;
                event.preventDefault();
                this.zoomDomainBy(Math.exp(-event.deltaY * 0.002), point);
                return;
            }
            const navigation = this.#navigation();
            if (navigation === false ||
                navigation.wheel === 'off' ||
                (navigation.wheel === 'modifier' && !event.ctrlKey && !event.metaKey)) {
                return;
            }
            const point = this.#surfacePoint(event);
            if (point === null)
                return;
            event.preventDefault();
            this.zoomBy(Math.exp(-event.deltaY * 0.002), point);
        }
        #familyHit(point) {
            const scene = this.#result?.scene;
            if (scene === undefined)
                return null;
            const local = inverseInspectionPoint(this.#view, point);
            const hit = hitTestScene(scene, local.x, local.y, 8 / this.#view.zoom);
            return hit?.familyInteraction === undefined ? null : hit;
        }
        #nearestFamilyNode(scenePoint, maximumDistance = 24) {
            const root = this.#result?.scene.root;
            if (root === undefined)
                return null;
            return (familySceneEntries(root)
                .flatMap((entry) => {
                const interaction = entry.familyInteraction;
                if (interaction.kind !== 'network-node' && interaction.kind !== 'flow-node')
                    return [];
                const center = {
                    x: interaction.plot.x + interaction.position.x * interaction.plot.width,
                    y: interaction.plot.y + interaction.position.y * interaction.plot.height,
                };
                return [{ entry, distance: pointDistance(center, scenePoint) }];
            })
                .filter(({ distance }) => distance <= maximumDistance)
                .sort((left, right) => left.distance - right.distance)[0] ?? null);
        }
        #familyRegionAt(scenePoint) {
            const root = this.#result?.scene.root;
            if (root === undefined)
                return null;
            const entries = familySceneEntries(root);
            for (let index = entries.length - 1; index >= 0; index -= 1) {
                const entry = entries[index];
                if (entry === undefined)
                    continue;
                const interaction = entry.familyInteraction;
                if (interaction.kind !== 'navigator-window' &&
                    interaction.kind !== 'parallel-axis' &&
                    interaction.kind !== 'scatter-matrix-cell') {
                    continue;
                }
                const { plot } = interaction;
                if (interaction.kind === 'parallel-axis') {
                    const axisX = plot.x + (interaction.index / Math.max(1, interaction.count - 1)) * plot.width;
                    if (Math.abs(scenePoint.x - axisX) <= 9 &&
                        scenePoint.y >= plot.y &&
                        scenePoint.y <= plot.y + plot.height) {
                        return entry;
                    }
                    continue;
                }
                if (interaction.kind === 'navigator-window') {
                    const span = Math.max(Number.EPSILON, interaction.maximum - interaction.minimum);
                    const x = plot.x + ((interaction.start - interaction.minimum) / span) * plot.width;
                    const width = ((interaction.end - interaction.start) / span) * plot.width;
                    if (scenePoint.x >= x &&
                        scenePoint.x <= x + width &&
                        scenePoint.y >= plot.y &&
                        scenePoint.y <= plot.y + plot.height) {
                        return entry;
                    }
                    continue;
                }
                if (scenePoint.x >= plot.x &&
                    scenePoint.x <= plot.x + plot.width &&
                    scenePoint.y >= plot.y &&
                    scenePoint.y <= plot.y + plot.height) {
                    return entry;
                }
            }
            return null;
        }
        #handleFamilyPointerDown(event, point) {
            const scenePoint = inverseInspectionPoint(this.#view, point);
            const hit = this.#familyHit(point);
            const directTarget = hit ?? this.#familyRegionAt(scenePoint);
            const directInteraction = directTarget?.familyInteraction;
            if (directTarget !== null && directInteraction?.kind === 'navigator-window') {
                this.#familyGesture = {
                    kind: 'navigator-window',
                    pointerId: event.pointerId,
                    layerId: directTarget.layerId,
                    interaction: { ...directInteraction, plot: { ...directInteraction.plot } },
                    start: { ...scenePoint },
                    previous: this.getNavigatorWindow(directTarget.layerId),
                };
            }
            else if (directTarget !== null &&
                directInteraction?.kind === 'parallel-axis' &&
                event.shiftKey) {
                this.#familyGesture = {
                    kind: 'parallel-brush',
                    pointerId: event.pointerId,
                    layerId: directTarget.layerId,
                    field: directInteraction.field,
                    plot: { ...directInteraction.plot },
                    start: { ...scenePoint },
                    previous: this.getParallelRuntimeState(directTarget.layerId),
                };
            }
            else if (directTarget !== null && directInteraction?.kind === 'heatmap-cell') {
                this.#familyGesture = {
                    kind: 'heatmap-brush',
                    pointerId: event.pointerId,
                    layerId: directTarget.layerId,
                    start: { ...directInteraction },
                    previous: this.getHeatmapBrush(directTarget.layerId),
                };
            }
            else if (directTarget !== null && directInteraction?.kind === 'scatter-matrix-cell') {
                this.#familyGesture = {
                    kind: 'scatter-matrix-brush',
                    pointerId: event.pointerId,
                    layerId: directTarget.layerId,
                    interaction: {
                        ...directInteraction,
                        plot: { ...directInteraction.plot },
                        xDomain: [...directInteraction.xDomain],
                        yDomain: [...directInteraction.yDomain],
                    },
                    start: { ...scenePoint },
                    previous: this.getScatterMatrixBrush(directTarget.layerId),
                };
            }
            if (this.#familyGesture !== null) {
                this.#dragDistance = 0;
                this.#suppressClick = false;
                this.#eventSurface?.setPointerCapture?.(event.pointerId);
                this.#eventSurface?.style.setProperty('cursor', this.#familyGesture.kind === 'navigator-window' ? 'grabbing' : 'crosshair');
                event.preventDefault();
                return true;
            }
            if (event.shiftKey) {
                const network = this.#familyEntries('network-node').find(({ familyInteraction }) => {
                    const { plot } = familyInteraction;
                    return (scenePoint.x >= plot.x &&
                        scenePoint.x <= plot.x + plot.width &&
                        scenePoint.y >= plot.y &&
                        scenePoint.y <= plot.y + plot.height);
                });
                if (network !== undefined) {
                    const previous = this.getNetworkRuntimeState(network.layerId);
                    this.#familyGesture = {
                        kind: 'network-lasso',
                        pointerId: event.pointerId,
                        layerId: network.layerId,
                        plot: { ...network.familyInteraction.plot },
                        points: [{ ...scenePoint }],
                        previous,
                    };
                    this.#dragDistance = 0;
                    this.#suppressClick = false;
                    this.#eventSurface?.setPointerCapture?.(event.pointerId);
                    this.#eventSurface?.style.setProperty('cursor', 'crosshair');
                    event.preventDefault();
                    return true;
                }
            }
            const fallback = this.#nearestFamilyNode(scenePoint)?.entry;
            const target = directInteraction?.kind === 'network-node' || directInteraction?.kind === 'flow-node'
                ? hit
                : (fallback ?? null);
            const interaction = target?.familyInteraction;
            if (target === null ||
                interaction === undefined ||
                (interaction.kind !== 'network-node' && interaction.kind !== 'flow-node')) {
                return false;
            }
            this.#familyGesture = {
                kind: interaction.kind,
                pointerId: event.pointerId,
                layerId: target.layerId,
                id: interaction.id,
                plot: { ...interaction.plot },
                start: { ...scenePoint },
                ...(interaction.kind === 'network-node'
                    ? { previousNetwork: this.getNetworkRuntimeState(target.layerId) }
                    : { previousFlow: this.getFlowRuntimeState(target.layerId) }),
            };
            this.#dragDistance = 0;
            this.#suppressClick = false;
            this.#eventSurface?.setPointerCapture?.(event.pointerId);
            this.#eventSurface?.style.setProperty('cursor', 'grabbing');
            event.preventDefault();
            return true;
        }
        #handleFamilyPointerMove(event) {
            const gesture = this.#familyGesture;
            if (gesture === null || gesture.pointerId !== event.pointerId)
                return false;
            const surfacePoint = this.#surfacePoint(event);
            if (surfacePoint === null)
                return false;
            const scenePoint = inverseInspectionPoint(this.#view, surfacePoint);
            if (gesture.kind === 'network-lasso') {
                const previous = gesture.points.at(-1);
                if (previous === undefined || pointDistance(previous, scenePoint) >= 2) {
                    if (gesture.points.length < 256)
                        gesture.points.push({ ...scenePoint });
                }
                this.#dragDistance = pointDistance(gesture.points[0] ?? scenePoint, scenePoint);
                if (gesture.points.length >= 3) {
                    const lasso = gesture.points.map((point) => normalizedPoint(point, gesture.plot));
                    const current = this.getNetworkRuntimeState(gesture.layerId);
                    this.#setNetworkRuntimeState(gesture.layerId, normalizeNetworkRuntimeState({ lasso }, current), 'pointer', false);
                }
            }
            else if (gesture.kind === 'network-node' || gesture.kind === 'flow-node') {
                const position = normalizedPoint(scenePoint, gesture.plot);
                this.#dragDistance = pointDistance(gesture.start, scenePoint);
                if (gesture.kind === 'network-node') {
                    const current = this.getNetworkRuntimeState(gesture.layerId);
                    this.#setNetworkRuntimeState(gesture.layerId, normalizeNetworkRuntimeState({
                        positions: {
                            ...current.positions,
                            [gesture.id]: { ...position, pinned: true },
                        },
                    }, current), 'pointer', false);
                }
                else {
                    const current = this.getFlowRuntimeState(gesture.layerId);
                    this.#setFlowRuntimeState(gesture.layerId, normalizeFlowRuntimeState({ positions: { ...current.positions, [gesture.id]: position } }, current), 'pointer', false);
                }
            }
            else if (gesture.kind === 'navigator-window') {
                this.#dragDistance = pointDistance(gesture.start, scenePoint);
                const state = translateNavigatorWindow(gesture.interaction, scenePoint.x - gesture.start.x);
                this.#setNavigatorRuntimeState(gesture.layerId, gesture.interaction.family, state, 'pointer', false);
            }
            else if (gesture.kind === 'parallel-brush') {
                this.#dragDistance = pointDistance(gesture.start, scenePoint);
                const normalizeY = (value) => 1 - Math.max(0, Math.min(1, (value - gesture.plot.y) / Math.max(1, gesture.plot.height)));
                const extent = [normalizeY(gesture.start.y), normalizeY(scenePoint.y)].sort((left, right) => left - right);
                this.#setParallelRuntimeState(gesture.layerId, setParallelBrushExtents(gesture.previous, gesture.field, [extent]), 'pointer', false);
            }
            else if (gesture.kind === 'heatmap-brush') {
                this.#dragDistance = Math.max(this.#dragDistance, 3);
                const current = this.#familyHit(surfacePoint)?.familyInteraction;
                const end = current?.kind === 'heatmap-cell' ? current : gesture.start;
                const rowMinimum = Math.min(gesture.start.rowIndex, end.rowIndex);
                const rowMaximum = Math.max(gesture.start.rowIndex, end.rowIndex);
                const columnMinimum = Math.min(gesture.start.columnIndex, end.columnIndex);
                const columnMaximum = Math.max(gesture.start.columnIndex, end.columnIndex);
                const entries = this.#familyEntries('heatmap-cell').filter((entry) => entry.layerId === gesture.layerId);
                const rows = [
                    ...new Map(entries
                        .filter(({ familyInteraction }) => familyInteraction.rowIndex >= rowMinimum &&
                        familyInteraction.rowIndex <= rowMaximum)
                        .map(({ familyInteraction }) => [familyInteraction.rowIndex, familyInteraction.row])).entries(),
                ]
                    .sort(([left], [right]) => left - right)
                    .map(([, value]) => value);
                const columns = [
                    ...new Map(entries
                        .filter(({ familyInteraction }) => familyInteraction.columnIndex >= columnMinimum &&
                        familyInteraction.columnIndex <= columnMaximum)
                        .map(({ familyInteraction }) => [
                        familyInteraction.columnIndex,
                        familyInteraction.column,
                    ])).entries(),
                ]
                    .sort(([left], [right]) => left - right)
                    .map(([, value]) => value);
                this.#setHeatmapRuntimeState(gesture.layerId, normalizeHeatmapRuntimeState({ rows, columns }, gesture.previous), 'pointer', false);
            }
            else if (gesture.kind === 'scatter-matrix-brush') {
                this.#dragDistance = pointDistance(gesture.start, scenePoint);
                const brush = scatterMatrixPointerBrush(gesture.interaction, gesture.start, scenePoint);
                const selectedRows = selectScatterMatrixRows(this.#familyLayerRows(gesture.layerId), brush);
                const state = normalizeScatterMatrixRuntimeState({ ...brush, selectedRows }, gesture.previous ?? { ...brush, selectedRows: [] });
                this.#setScatterMatrixRuntimeState(gesture.layerId, state, 'pointer', false);
            }
            if (this.#dragDistance > 2)
                this.#suppressClick = true;
            event.preventDefault();
            return true;
        }
        #handleFamilyPointerEnd(event, cancelled) {
            const gesture = this.#familyGesture;
            if (gesture === null || gesture.pointerId !== event.pointerId)
                return false;
            this.#familyGesture = null;
            if (this.#eventSurface?.hasPointerCapture?.(event.pointerId)) {
                this.#eventSurface.releasePointerCapture?.(event.pointerId);
            }
            this.#eventSurface?.style.setProperty('cursor', this.#surfaceCursor ?? '');
            if (cancelled) {
                if (gesture.kind === 'network-lasso') {
                    this.#networkRuntime.set(gesture.layerId, cloneNetworkRuntimeState(gesture.previous));
                }
                else if (gesture.kind === 'network-node' && gesture.previousNetwork !== undefined) {
                    this.#networkRuntime.set(gesture.layerId, cloneNetworkRuntimeState(gesture.previousNetwork));
                }
                else if (gesture.kind === 'flow-node' && gesture.previousFlow !== undefined) {
                    this.#flowRuntime.set(gesture.layerId, cloneFlowRuntimeState(gesture.previousFlow));
                }
                else if (gesture.kind === 'navigator-window') {
                    this.#navigatorRuntime.set(gesture.layerId, {
                        family: gesture.interaction.family,
                        state: cloneNavigatorRuntimeState(gesture.previous),
                    });
                }
                else if (gesture.kind === 'parallel-brush') {
                    this.#parallelRuntime.set(gesture.layerId, cloneParallelRuntimeState(gesture.previous));
                }
                else if (gesture.kind === 'heatmap-brush') {
                    this.#heatmapRuntime.set(gesture.layerId, cloneHeatmapRuntimeState(gesture.previous));
                }
                else if (gesture.kind === 'scatter-matrix-brush') {
                    if (gesture.previous === null)
                        this.#scatterMatrixRuntime.delete(gesture.layerId);
                    else {
                        this.#scatterMatrixRuntime.set(gesture.layerId, cloneScatterMatrixRuntimeState(gesture.previous));
                    }
                }
                this.render();
                return true;
            }
            if (gesture.kind === 'network-lasso' || gesture.kind === 'network-node') {
                const state = this.getNetworkRuntimeState(gesture.layerId);
                this.#events.emit('networkchange', {
                    chart: this,
                    layerId: gesture.layerId,
                    state,
                    reason: 'pointer',
                });
            }
            else if (gesture.kind === 'flow-node') {
                const state = this.getFlowRuntimeState(gesture.layerId);
                this.#events.emit('flowchange', {
                    chart: this,
                    layerId: gesture.layerId,
                    state,
                    reason: 'pointer',
                });
            }
            else if (gesture.kind === 'navigator-window') {
                this.#events.emit('navigatorchange', {
                    chart: this,
                    layerId: gesture.layerId,
                    family: gesture.interaction.family,
                    state: this.getNavigatorWindow(gesture.layerId),
                    reason: 'pointer',
                });
            }
            else if (gesture.kind === 'parallel-brush') {
                this.#events.emit('parallelchange', {
                    chart: this,
                    layerId: gesture.layerId,
                    state: this.getParallelRuntimeState(gesture.layerId),
                    reason: 'pointer',
                });
            }
            else if (gesture.kind === 'heatmap-brush') {
                this.#events.emit('heatmapchange', {
                    chart: this,
                    layerId: gesture.layerId,
                    state: this.getHeatmapBrush(gesture.layerId),
                    reason: 'pointer',
                });
            }
            else {
                const state = this.getScatterMatrixBrush(gesture.layerId);
                if (state !== null) {
                    this.#events.emit('scattermatrixchange', {
                        chart: this,
                        layerId: gesture.layerId,
                        state,
                        reason: 'pointer',
                    });
                }
            }
            this.#suppressClick =
                this.#suppressClick || gesture.kind === 'network-lasso' || this.#dragDistance > 2;
            return true;
        }
        #handlePointerDown(event) {
            if (event.pointerType === 'mouse' && event.button !== 0)
                return;
            const point = this.#surfacePoint(event);
            if (point === null)
                return;
            const annotationMetadata = this.#result?.scene.metadata.annotations;
            if (this.#annotationsVisible && annotationMetadata !== undefined) {
                const scenePoint = inverseInspectionPoint(this.#view, point);
                const candidates = [...annotationMetadata.entries].reverse();
                const entry = candidates.find(({ bounds }) => hitTestAnnotationHandle(bounds, scenePoint.x, scenePoint.y, 7));
                if (entry !== undefined) {
                    const annotation = this.#annotations.find(({ id }) => id === entry.id);
                    const tested = hitTestAnnotationHandle(entry.bounds, scenePoint.x, scenePoint.y, 7);
                    const handle = tested !== null && (entry.resizable || tested === 'move') ? tested : 'move';
                    if (annotation !== undefined) {
                        this.#activeAnnotationId = entry.id;
                        this.#annotationGesture = {
                            pointerId: event.pointerId,
                            id: entry.id,
                            handle,
                            start: scenePoint,
                            bounds: { ...entry.bounds },
                            annotation: cloneAnnotation(annotation),
                            previous: this.#annotationHistory.annotations(),
                        };
                        this.#suppressClick = false;
                        this.#dragDistance = 0;
                        this.#eventSurface?.setPointerCapture?.(event.pointerId);
                        this.render();
                        this.#eventSurface?.style.setProperty('cursor', handle === 'move' ? 'grabbing' : 'nwse-resize');
                        event.preventDefault();
                        return;
                    }
                }
            }
            const authoring = this.#markLabelAuthoring();
            const labelMetadata = this.#result?.scene.metadata.markLabels;
            if (authoring !== false && authoring.pointer && labelMetadata !== undefined) {
                const scenePoint = inverseInspectionPoint(this.#view, point);
                const entry = hitTestMarkLabel(labelMetadata.entries, scenePoint.x, scenePoint.y, 6, this.#activeMarkLabelId ?? undefined);
                if (entry !== null && entry.editable) {
                    const previous = this.#markLabels.positions();
                    this.#activeMarkLabelId = entry.id;
                    this.#markLabelGesture = {
                        pointerId: event.pointerId,
                        id: entry.id,
                        target: cloneDatumTarget(entry.target),
                        start: scenePoint,
                        startOffsetX: entry.offsetX,
                        startOffsetY: entry.offsetY,
                        entry: {
                            ...entry,
                            target: cloneDatumTarget(entry.target),
                            anchor: { ...entry.anchor },
                            baseCenter: { ...entry.baseCenter },
                            bounds: { ...entry.bounds },
                        },
                        entries: labelMetadata.entries,
                        plot: { ...labelMetadata.plot },
                        previous,
                    };
                    this.#suppressClick = false;
                    this.#dragDistance = 0;
                    this.#eventSurface?.setPointerCapture?.(event.pointerId);
                    this.render();
                    this.#eventSurface?.style.setProperty('cursor', 'grabbing');
                    event.preventDefault();
                    return;
                }
            }
            if (this.#handleFamilyPointerDown(event, point))
                return;
            const selection = this.#result?.spec.interaction.selection;
            if (selection !== undefined &&
                selection !== false &&
                selection.kind !== 'point' &&
                this.#analyticGesture === null) {
                const coordinate = this.#coordinateAt(point);
                if (coordinate === null)
                    return;
                this.#analyticGesture = {
                    pointerId: event.pointerId,
                    viewId: coordinate.view.id,
                    start: coordinate.point,
                    current: coordinate.point,
                    points: [coordinate.point],
                };
                this.#suppressClick = false;
                this.#dragDistance = 0;
                this.#eventSurface?.setPointerCapture?.(event.pointerId);
                event.preventDefault();
                return;
            }
            const domainNavigation = this.#domainNavigation();
            if (domainNavigation !== false && domainNavigation.drag && this.#domainGesture === null) {
                const coordinate = this.#coordinateAt(point);
                if (coordinate === null)
                    return;
                this.#domainGesture = {
                    pointerId: event.pointerId,
                    viewId: coordinate.view.id,
                    previous: coordinate.point,
                };
                this.#suppressClick = false;
                this.#dragDistance = 0;
                this.#eventSurface?.setPointerCapture?.(event.pointerId);
                this.#eventSurface?.style.setProperty('cursor', 'grabbing');
                event.preventDefault();
                return;
            }
            const navigation = this.#navigation();
            if (navigation === false)
                return;
            if (!navigation.drag && !(navigation.pinch && event.pointerType === 'touch'))
                return;
            if (this.#activePointers.size === 0) {
                this.#suppressClick = false;
                this.#dragDistance = 0;
            }
            this.#activePointers.set(event.pointerId, { ...point, pointerType: event.pointerType });
            this.#eventSurface?.setPointerCapture?.(event.pointerId);
            this.#dragPrevious = point;
            this.#pinchStart = this.#pinchSnapshot(navigation);
            if (navigation.drag)
                this.#eventSurface?.style.setProperty('cursor', 'grabbing');
            event.preventDefault();
        }
        #handlePointerMove(event) {
            if (this.#annotationGesture?.pointerId === event.pointerId) {
                const point = this.#surfacePoint(event);
                if (point === null)
                    return false;
                const scenePoint = inverseInspectionPoint(this.#view, point);
                const gesture = this.#annotationGesture;
                const edited = editAnnotationByPointer({
                    annotation: gesture.annotation,
                    handle: gesture.handle,
                    deltaX: scenePoint.x - gesture.start.x,
                    deltaY: scenePoint.y - gesture.start.y,
                    bounds: gesture.bounds,
                    grid: event.shiftKey ? 10 : false,
                });
                const next = this.#annotations.map((annotation) => annotation.id === gesture.id ? edited : annotation);
                this.#dragDistance = Math.hypot(scenePoint.x - gesture.start.x, scenePoint.y - gesture.start.y);
                if (this.#annotationHistory.preview(next)) {
                    this.#annotations = [...this.#annotationHistory.annotations()];
                    this.render();
                }
                if (this.#dragDistance > 2)
                    this.#suppressClick = true;
                event.preventDefault();
                return true;
            }
            if (this.#markLabelGesture?.pointerId === event.pointerId) {
                const point = this.#surfacePoint(event);
                const authoring = this.#markLabelAuthoring();
                if (point === null || authoring === false)
                    return false;
                const scenePoint = inverseInspectionPoint(this.#view, point);
                const gesture = this.#markLabelGesture;
                const desired = snapMarkLabelOffset({
                    entry: gesture.entry,
                    entries: gesture.entries,
                    plot: gesture.plot,
                    offsetX: gesture.startOffsetX + scenePoint.x - gesture.start.x,
                    offsetY: gesture.startOffsetY + scenePoint.y - gesture.start.y,
                    snap: authoring.snap,
                });
                const next = setMarkLabelOffset(this.#markLabels.positions(), gesture.target, desired.offsetX, desired.offsetY);
                this.#dragDistance = Math.hypot(scenePoint.x - gesture.start.x, scenePoint.y - gesture.start.y);
                if (this.#markLabels.preview(next))
                    this.render();
                if (this.#dragDistance > 2)
                    this.#suppressClick = true;
                event.preventDefault();
                return true;
            }
            if (this.#handleFamilyPointerMove(event))
                return true;
            if (this.#analyticGesture?.pointerId === event.pointerId) {
                const gesture = this.#analyticGesture;
                const surfacePoint = this.#surfacePoint(event);
                if (surfacePoint === null)
                    return false;
                const point = this.#localPoint(gesture.viewId, surfacePoint);
                this.#dragDistance += pointDistance(gesture.current, point);
                gesture.current = point;
                const selection = this.#result?.spec.interaction.selection;
                if (selection !== undefined && selection !== false && selection.kind === 'lasso') {
                    const last = gesture.points.at(-1);
                    if (gesture.points.length < selection.maxLassoPoints &&
                        (last === undefined || pointDistance(last, point) >= 2)) {
                        gesture.points.push(point);
                    }
                }
                if (this.#dragDistance > 4)
                    this.#suppressClick = true;
                event.preventDefault();
                return true;
            }
            if (this.#domainGesture?.pointerId === event.pointerId) {
                const gesture = this.#domainGesture;
                const surfacePoint = this.#surfacePoint(event);
                if (surfacePoint === null)
                    return false;
                const point = this.#localPoint(gesture.viewId, surfacePoint);
                const previous = gesture.previous;
                const deltaX = point.x - previous.x;
                const deltaY = point.y - previous.y;
                this.#dragDistance += Math.hypot(deltaX, deltaY);
                gesture.previous = point;
                this.panDomainBy(deltaX, deltaY, undefined, gesture.viewId);
                if (this.#dragDistance > 4)
                    this.#suppressClick = true;
                event.preventDefault();
                return true;
            }
            const navigation = this.#navigation();
            if (navigation === false || !this.#activePointers.has(event.pointerId))
                return false;
            const point = this.#surfacePoint(event);
            if (point === null)
                return false;
            const previous = this.#activePointers.get(event.pointerId);
            this.#activePointers.set(event.pointerId, { ...point, pointerType: event.pointerType });
            if (navigation.pinch && this.#activePointers.size >= 2) {
                if (this.#pinchStart === null)
                    this.#pinchStart = this.#pinchSnapshot(navigation);
                const [first, second] = [...this.#activePointers.values()];
                const start = this.#pinchStart;
                if (first !== undefined && second !== undefined && start !== null && start.distance > 0) {
                    const center = pointCenter(first, second);
                    const distance = pointDistance(first, second);
                    const scene = this.#result?.scene;
                    if (scene !== undefined) {
                        const zoom = Math.max(navigation.minZoom, Math.min(navigation.maxZoom, start.view.zoom * (distance / start.distance)));
                        const local = inverseInspectionPoint(start.view, start.center);
                        this.#setView(constrainInspectionView({
                            zoom,
                            offsetX: center.x - local.x * zoom,
                            offsetY: center.y - local.y * zoom,
                        }, {
                            width: scene.width,
                            height: scene.height,
                            minZoom: navigation.minZoom,
                            maxZoom: navigation.maxZoom,
                        }), 'zoom');
                    }
                }
            }
            else if (navigation.drag && this.#dragPrevious !== null) {
                const deltaX = point.x - this.#dragPrevious.x;
                const deltaY = point.y - this.#dragPrevious.y;
                this.#dragDistance += Math.hypot(deltaX, deltaY);
                this.panBy(deltaX, deltaY);
                this.#dragPrevious = point;
            }
            else if (previous !== undefined)
                this.#dragDistance += pointDistance(previous, point);
            if (this.#dragDistance > 4 || this.#activePointers.size > 1)
                this.#suppressClick = true;
            event.preventDefault();
            return true;
        }
        #handlePointerEnd(event, cancelled = false) {
            if (this.#handleFamilyPointerEnd(event, cancelled))
                return;
            if (this.#annotationGesture?.pointerId === event.pointerId) {
                const gesture = this.#annotationGesture;
                this.#annotationGesture = null;
                if (this.#eventSurface?.hasPointerCapture?.(event.pointerId)) {
                    this.#eventSurface.releasePointerCapture?.(event.pointerId);
                }
                this.#eventSurface?.style.setProperty('cursor', 'move');
                if (cancelled) {
                    this.#annotationHistory.restore(gesture.previous);
                    this.#annotations = [...this.#annotationHistory.annotations()];
                    this.render();
                }
                else if (this.#annotationHistory.commit(gesture.previous)) {
                    this.#suppressClick = true;
                    this.#emitAnnotations('pointer', gesture.id);
                }
                else
                    this.#emitAnnotations('select', gesture.id);
                return;
            }
            if (this.#markLabelGesture?.pointerId === event.pointerId) {
                const gesture = this.#markLabelGesture;
                this.#markLabelGesture = null;
                if (this.#eventSurface?.hasPointerCapture?.(event.pointerId)) {
                    this.#eventSurface.releasePointerCapture?.(event.pointerId);
                }
                this.#eventSurface?.style.setProperty('cursor', 'move');
                if (cancelled) {
                    this.#markLabels.restore(gesture.previous);
                    this.render();
                }
                else if (this.#markLabels.commit(gesture.previous)) {
                    this.#suppressClick = true;
                    this.#emitMarkLabels('pointer', gesture.id);
                }
                else
                    this.#emitMarkLabels('select', gesture.id);
                return;
            }
            if (this.#analyticGesture?.pointerId === event.pointerId) {
                const gesture = this.#analyticGesture;
                const point = this.#surfacePoint(event);
                if (point !== null)
                    gesture.current = this.#localPoint(gesture.viewId, point);
                this.#analyticGesture = null;
                if (this.#eventSurface?.hasPointerCapture?.(event.pointerId)) {
                    this.#eventSurface.releasePointerCapture?.(event.pointerId);
                }
                this.#suppressClick = true;
                if (!cancelled) {
                    try {
                        this.#completeAnalyticGesture(gesture);
                    }
                    catch (error) {
                        this.#events.emit('error', { chart: this, error });
                    }
                }
                return;
            }
            if (this.#domainGesture?.pointerId === event.pointerId) {
                this.#domainGesture = null;
                if (this.#eventSurface?.hasPointerCapture?.(event.pointerId)) {
                    this.#eventSurface.releasePointerCapture?.(event.pointerId);
                }
                this.#eventSurface?.style.setProperty('cursor', this.#domainNavigation() !== false ? 'grab' : (this.#surfaceCursor ?? ''));
                return;
            }
            if (!this.#activePointers.has(event.pointerId))
                return;
            this.#activePointers.delete(event.pointerId);
            if (this.#eventSurface?.hasPointerCapture?.(event.pointerId)) {
                this.#eventSurface.releasePointerCapture?.(event.pointerId);
            }
            const remaining = [...this.#activePointers.values()];
            this.#dragPrevious = remaining[0] ?? null;
            this.#pinchStart = null;
            const navigation = this.#navigation();
            if (remaining.length >= 2 && navigation !== false)
                this.#pinchStart = this.#pinchSnapshot(navigation);
            if (remaining.length === 0) {
                this.#eventSurface?.style.setProperty('cursor', navigation !== false && navigation.drag ? 'grab' : (this.#surfaceCursor ?? ''));
            }
        }
        #completeAnalyticGesture(gesture) {
            const result = this.#result;
            const selection = result?.spec.interaction.selection;
            if (result === null || result === undefined || selection === undefined || selection === false)
                return;
            const coordinates = this.#coordinateView(gesture.viewId).coordinates;
            const spanX = Math.abs(gesture.current.x - gesture.start.x);
            const spanY = Math.abs(gesture.current.y - gesture.start.y);
            let resolved;
            if (selection.kind === 'axis') {
                const span = cartesianAxisChannel(coordinates, selection.axis) === 'x' ? spanX : spanY;
                if (span < selection.minPixelSpan)
                    return;
                resolved = pixelAxisToSelection(coordinates, selection.axis, gesture.start, gesture.current);
            }
            else if (selection.kind === 'lasso') {
                const last = gesture.points.at(-1);
                if (last === undefined || pointDistance(last, gesture.current) >= 2) {
                    gesture.points.push(gesture.current);
                }
                if (gesture.points.length < 3 || Math.max(spanX, spanY) < selection.minPixelSpan) {
                    return;
                }
                resolved = pixelLassoToSelection(coordinates, gesture.points, {
                    x: selection.xAxis,
                    y: selection.yAxis,
                });
            }
            else if (selection.kind === 'interval' || selection.kind === 'rectangle') {
                if (Math.max(spanX, spanY) < selection.minPixelSpan)
                    return;
                resolved = pixelRectangleToSelection(coordinates, gesture.start, gesture.current, {
                    type: selection.kind,
                    xAxis: selection.xAxis,
                    yAxis: selection.yAxis,
                });
            }
            else
                return;
            this.#applyAnalyticSelection(resolved, selection, 'pointer');
        }
        #applyAnalyticSelection(resolved, selection, reason) {
            const previous = this.#analyticSelection.get();
            if (selection.mode === 'single') {
                this.#analyticSelection.set({
                    version: analyticSelectionVersion,
                    combine: selection.combine,
                    selections: [resolved],
                });
            }
            else
                this.#analyticSelection.apply(resolved, selection.combine);
            try {
                this.#validateAnalyticStateForConfig(this.#analyticSelection.get(), selection);
                this.render();
            }
            catch (error) {
                this.#analyticSelection.set(previous);
                throw error;
            }
            this.#emitAnalyticSelection(reason);
        }
        #pinchSnapshot(navigation) {
            if (!navigation.pinch || this.#activePointers.size < 2)
                return null;
            const [first, second] = [...this.#activePointers.values()];
            if (first === undefined || second === undefined)
                return null;
            return {
                distance: pointDistance(first, second),
                center: pointCenter(first, second),
                view: this.#view,
            };
        }
        #analyticSelectionDraft() {
            const active = this.#analyticKeyboardGesture;
            const selection = this.#result?.spec.interaction.selection;
            if (active === null || selection === undefined || selection === false)
                return undefined;
            return previewAnalyticKeyboardSelection(this.#coordinateView(active.viewId).coordinates, active.gesture, selection);
        }
        #handleAnalyticSelectionKeyDown(event) {
            const result = this.#result;
            const selection = result?.spec.interaction.selection;
            if (result === null ||
                result === undefined ||
                selection === undefined ||
                selection === false ||
                selection.kind === 'point' ||
                !selection.keyboard) {
                return false;
            }
            const active = this.#analyticKeyboardGesture;
            if (active === null) {
                if (event.key.toLowerCase() !== 's' || event.ctrlKey || event.metaKey || event.altKey) {
                    return false;
                }
                const focusedId = this.#accessibility.getFocusedId();
                const focused = focusedId === null
                    ? undefined
                    : result.scene.semanticIndex.find(({ id }) => id === focusedId);
                const view = (focused === undefined
                    ? undefined
                    : result.coordinateViews.find(({ id }) => id === focused.viewId)) ??
                    result.coordinateViews[0];
                if (view === undefined)
                    return false;
                const origin = focused === undefined || focused.viewId !== view.id
                    ? undefined
                    : {
                        x: focused.bounds.x + focused.bounds.width / 2 - view.offsetX,
                        y: focused.bounds.y + focused.bounds.height / 2 - view.offsetY,
                    };
                this.#analyticKeyboardGesture = {
                    viewId: view.id,
                    gesture: startAnalyticKeyboardGesture(view.coordinates, selection.kind, origin),
                };
                this.render();
                event.preventDefault();
                return true;
            }
            if (event.key === 'Escape') {
                this.#analyticKeyboardGesture = null;
                this.render();
                event.preventDefault();
                return true;
            }
            const view = this.#coordinateView(active.viewId);
            const direction = event.key === 'ArrowLeft'
                ? 'left'
                : event.key === 'ArrowRight'
                    ? 'right'
                    : event.key === 'ArrowUp'
                        ? 'up'
                        : event.key === 'ArrowDown'
                            ? 'down'
                            : null;
            if (direction !== null) {
                this.#analyticKeyboardGesture = {
                    viewId: active.viewId,
                    gesture: moveAnalyticKeyboardGesture(view.coordinates, active.gesture, direction, selection.keyboardStep * (event.shiftKey ? 10 : 1)),
                };
                this.render();
                event.preventDefault();
                return true;
            }
            if ((event.key === ' ' || event.key === 'Spacebar') && selection.kind === 'lasso') {
                this.#analyticKeyboardGesture = {
                    viewId: active.viewId,
                    gesture: addAnalyticKeyboardVertex(active.gesture, selection.maxLassoPoints),
                };
                this.render();
                event.preventDefault();
                return true;
            }
            if (event.key === 'Enter') {
                const resolved = completeAnalyticKeyboardSelection(view.coordinates, active.gesture, selection);
                if (resolved !== null) {
                    this.#analyticKeyboardGesture = null;
                    this.#applyAnalyticSelection(resolved, selection, 'keyboard');
                }
                event.preventDefault();
                return true;
            }
            return false;
        }
        #handleFamilyKeyDown(event) {
            const navigationKeys = new Set([
                'ArrowLeft',
                'ArrowRight',
                'ArrowUp',
                'ArrowDown',
                'Home',
                'End',
                'PageUp',
                'PageDown',
            ]);
            if (event.key === 'Escape' && this.#familyFocus !== null) {
                this.clearFamilyFocus();
                event.preventDefault();
                return true;
            }
            const pieEntries = this.#familyEntries('pie-slice');
            const tableEntries = this.#familyEntries('table-cell');
            if (pieEntries.length === 0 && tableEntries.length === 0)
                return false;
            let focus = this.#familyFocus;
            if (focus?.kind === 'pie-slice') {
                const activeFocus = focus;
                if (!pieEntries.some((entry) => entry.layerId === activeFocus.layerId && entry.familyInteraction.id === activeFocus.id)) {
                    focus = null;
                }
            }
            else if (focus?.kind === 'table-cell') {
                if (!tableEntries.some((entry) => entry.layerId === focus?.layerId))
                    focus = null;
            }
            if (event.key === 'Enter' || event.key === ' ' || event.key === 'Spacebar') {
                if (focus === null)
                    return false;
                const entry = focus.kind === 'pie-slice'
                    ? pieEntries.find((candidate) => candidate.layerId === focus.layerId && candidate.familyInteraction.id === focus.id)
                    : tableEntries.find((candidate) => candidate.layerId === focus.layerId &&
                        candidate.familyInteraction.row === focus.row &&
                        candidate.familyInteraction.column === focus.column);
                if (entry !== undefined) {
                    this.#applyClickSelection({ ...entry, x: 0, y: 0, distance: 0 }, 'keyboard', 'keyboard');
                }
                event.preventDefault();
                return true;
            }
            if (!navigationKeys.has(event.key))
                return false;
            if (focus === null) {
                const firstPie = pieEntries[0];
                const firstTable = [...tableEntries].sort((left, right) => left.familyInteraction.row - right.familyInteraction.row ||
                    left.familyInteraction.column - right.familyInteraction.column)[0];
                if (firstPie !== undefined) {
                    this.#setFamilyFocus({
                        kind: 'pie-slice',
                        layerId: firstPie.layerId,
                        id: firstPie.familyInteraction.id,
                    }, 'keyboard');
                }
                else if (firstTable !== undefined) {
                    this.#setFamilyFocus({
                        kind: 'table-cell',
                        layerId: firstTable.layerId,
                        row: firstTable.familyInteraction.row,
                        column: firstTable.familyInteraction.column,
                        field: firstTable.familyInteraction.field,
                    }, 'keyboard');
                }
                event.preventDefault();
                return true;
            }
            if (focus.kind === 'pie-slice') {
                const entries = pieEntries
                    .filter(({ layerId }) => layerId === focus?.layerId)
                    .sort((left, right) => left.familyInteraction.index - right.familyInteraction.index);
                const direction = event.key === 'Home'
                    ? 'first'
                    : event.key === 'End'
                        ? 'last'
                        : event.key === 'ArrowLeft' || event.key === 'ArrowUp' || event.key === 'PageUp'
                            ? 'previous'
                            : 'next';
                const id = nextPieSlice(entries.map(({ familyInteraction }) => ({ id: familyInteraction.id })), focus.id, direction);
                if (id !== null) {
                    this.#setFamilyFocus({ kind: 'pie-slice', layerId: focus.layerId, id }, 'keyboard');
                }
                event.preventDefault();
                return true;
            }
            const layerEntries = tableEntries.filter(({ layerId }) => layerId === focus?.layerId);
            const first = layerEntries[0]?.familyInteraction;
            if (first === undefined || first.kind !== 'table-cell')
                return false;
            const next = moveTableCell({ row: focus.row, column: focus.column }, event.key, { rows: first.rows, columns: first.columns, pageSize: Math.max(1, first.windowLimit) });
            let state = this.getTableRuntimeState(focus.layerId);
            const frozenRows = this.#tableFrozenRows(focus.layerId);
            const frozenColumns = this.#tableFrozenColumns(focus.layerId);
            let stateChanged = false;
            if (next.row >= frozenRows &&
                (next.row < state.windowOffset || next.row >= state.windowOffset + state.windowLimit)) {
                const windowOffset = next.row < state.windowOffset
                    ? next.row
                    : Math.max(0, next.row - Math.max(0, state.windowLimit - 1));
                state = normalizeTableRuntimeState({ windowOffset }, state);
                stateChanged = true;
            }
            if (next.column >= frozenColumns &&
                (next.column < state.columnOffset || next.column >= state.columnOffset + state.columnLimit)) {
                const columnOffset = next.column < state.columnOffset
                    ? next.column
                    : Math.max(0, next.column - Math.max(0, state.columnLimit - 1));
                state = normalizeTableRuntimeState({ columnOffset }, state);
                stateChanged = true;
            }
            if (stateChanged)
                this.#setTableRuntimeState(focus.layerId, state, 'keyboard');
            const refreshed = this.#familyEntries('table-cell').find((entry) => entry.layerId === focus.layerId &&
                entry.familyInteraction.row === next.row &&
                entry.familyInteraction.column === next.column);
            const field = refreshed?.familyInteraction.field ?? focus.field;
            this.#setFamilyFocus({ kind: 'table-cell', layerId: focus.layerId, row: next.row, column: next.column, field }, 'keyboard');
            event.preventDefault();
            return true;
        }
        #handleKeyDown(event) {
            if (this.#handleAnalyticSelectionKeyDown(event))
                return;
            if (this.#handleAnnotationKeyDown(event))
                return;
            if (this.#handleMarkLabelKeyDown(event))
                return;
            if (this.#handleFamilyKeyDown(event))
                return;
            const selection = this.#result?.spec.interaction.selection;
            if (event.key === 'Escape' &&
                selection !== undefined &&
                selection !== false &&
                selection.clearOnEscape &&
                (this.#selection.length > 0 || this.#analyticSelection.get().selections.length > 0)) {
                if (selection.kind === 'point')
                    this.clearSelection();
                else
                    this.clearAnalyticSelection();
                event.preventDefault();
                return;
            }
            const domainNavigation = this.#domainNavigation();
            if (domainNavigation !== false && domainNavigation.keyboard) {
                const before = this.#domainView;
                let handled = true;
                switch (event.key) {
                    case '+':
                    case '=':
                        this.zoomDomainBy(1.25);
                        break;
                    case '-':
                    case '_':
                        this.zoomDomainBy(0.8);
                        break;
                    case 'ArrowLeft':
                        this.panDomainBy(24, 0);
                        break;
                    case 'ArrowRight':
                        this.panDomainBy(-24, 0);
                        break;
                    case 'ArrowUp':
                        this.panDomainBy(0, 24);
                        break;
                    case 'ArrowDown':
                        this.panDomainBy(0, -24);
                        break;
                    case '0':
                    case 'Home':
                        this.resetDomainView();
                        break;
                    default:
                        handled = false;
                }
                if (handled && !sameDomainView(before, this.#domainView))
                    event.preventDefault();
                if (handled)
                    return;
            }
            const navigation = this.#navigation();
            if (navigation === false || !navigation.keyboard)
                return;
            const before = this.#view;
            let handled = true;
            switch (event.key) {
                case '+':
                case '=':
                    this.zoomBy(1.25);
                    break;
                case '-':
                case '_':
                    this.zoomBy(0.8);
                    break;
                case 'ArrowLeft':
                    this.panBy(24, 0);
                    break;
                case 'ArrowRight':
                    this.panBy(-24, 0);
                    break;
                case 'ArrowUp':
                    this.panBy(0, 24);
                    break;
                case 'ArrowDown':
                    this.panBy(0, -24);
                    break;
                case '0':
                case 'Home':
                    this.resetView();
                    break;
                default:
                    handled = false;
            }
            if (handled && !sameView(before, this.#view))
                event.preventDefault();
        }
        #handleAnnotationKeyDown(event) {
            const entries = this.#result?.scene.metadata.annotations?.entries ?? [];
            if (entries.length === 0)
                return false;
            const command = event.ctrlKey || event.metaKey;
            const key = event.key.toLowerCase();
            if (this.#activeAnnotationId !== null && command && key === 'z') {
                const changed = event.shiftKey ? this.redoAnnotationEdit() : this.undoAnnotationEdit();
                if (changed)
                    event.preventDefault();
                return changed;
            }
            if (this.#activeAnnotationId !== null && command && key === 'y') {
                const changed = this.redoAnnotationEdit();
                if (changed)
                    event.preventDefault();
                return changed;
            }
            if (event.key === 'Enter') {
                const current = entries.findIndex(({ id }) => id === this.#activeAnnotationId);
                const direction = event.shiftKey ? -1 : 1;
                const index = current < 0
                    ? event.shiftKey
                        ? entries.length - 1
                        : 0
                    : (current + direction + entries.length) % entries.length;
                const entry = entries[index];
                if (entry !== undefined) {
                    this.selectAnnotation(entry.id);
                    event.preventDefault();
                    return true;
                }
            }
            if (event.key === 'Escape' && this.#activeAnnotationId !== null) {
                this.selectAnnotation(null);
                event.preventDefault();
                return true;
            }
            if (this.#activeAnnotationId !== null &&
                (event.key === 'ArrowLeft' ||
                    event.key === 'ArrowRight' ||
                    event.key === 'ArrowUp' ||
                    event.key === 'ArrowDown')) {
                this.editAnnotationWithKeyboard(this.#activeAnnotationId, event.key, {
                    step: 1,
                    coarse: event.shiftKey,
                    resize: event.altKey,
                });
                event.preventDefault();
                return true;
            }
            return false;
        }
        #handleMarkLabelKeyDown(event) {
            const authoring = this.#markLabelAuthoring();
            const metadata = this.#result?.scene.metadata.markLabels;
            if (authoring === false || !authoring.keyboard || metadata === undefined)
                return false;
            const command = event.ctrlKey || event.metaKey;
            const key = event.key.toLowerCase();
            if (command && key === 'z') {
                const changed = event.shiftKey ? this.redoMarkLabelEdit() : this.undoMarkLabelEdit();
                if (changed)
                    event.preventDefault();
                return changed;
            }
            if (command && key === 'y') {
                const changed = this.redoMarkLabelEdit();
                if (changed)
                    event.preventDefault();
                return changed;
            }
            if (event.key === 'Enter' && metadata.entries.length > 0) {
                const current = metadata.entries.findIndex(({ id }) => id === this.#activeMarkLabelId);
                const direction = event.shiftKey ? -1 : 1;
                const index = current < 0
                    ? event.shiftKey
                        ? metadata.entries.length - 1
                        : 0
                    : (current + direction + metadata.entries.length) % metadata.entries.length;
                const entry = metadata.entries[index];
                if (entry !== undefined) {
                    this.#activeMarkLabelId = entry.id;
                    this.render();
                    this.#emitMarkLabels('select', entry.id);
                    event.preventDefault();
                    return true;
                }
            }
            if (event.key === 'Escape' && this.#activeMarkLabelId !== null) {
                const previous = this.#activeMarkLabelId;
                this.#activeMarkLabelId = null;
                this.render();
                this.#emitMarkLabels('select', previous);
                event.preventDefault();
                return true;
            }
            const entry = metadata.entries.find(({ id }) => id === this.#activeMarkLabelId);
            if (entry === undefined)
                return false;
            const step = authoring.step * (event.shiftKey ? 10 : 1);
            let deltaX = 0;
            let deltaY = 0;
            switch (event.key) {
                case 'ArrowLeft':
                    deltaX = -step;
                    break;
                case 'ArrowRight':
                    deltaX = step;
                    break;
                case 'ArrowUp':
                    deltaY = -step;
                    break;
                case 'ArrowDown':
                    deltaY = step;
                    break;
                default:
                    return false;
            }
            const snapped = snapMarkLabelOffset({
                entry,
                entries: metadata.entries,
                plot: metadata.plot,
                offsetX: entry.offsetX + deltaX,
                offsetY: entry.offsetY + deltaY,
                snap: authoring.snap,
            });
            const next = setMarkLabelOffset(this.#markLabels.positions(), entry.target, snapped.offsetX, snapped.offsetY);
            if (this.#markLabels.replace(next)) {
                this.render();
                this.#emitMarkLabels('keyboard', entry.id);
            }
            event.preventDefault();
            return true;
        }
        #emitPointer(type, sourceEvent) {
            const result = this.#result;
            if (this.#sceneTransition !== null) {
                this.#tooltip.hide();
                if (type === 'hover' || result?.spec.interaction.click !== false) {
                    this.#events.emit(type, { chart: this, hit: null, sourceEvent });
                }
                return;
            }
            const screen = this.#surfacePoint(sourceEvent);
            const surface = this.#renderer?.surface();
            if (result === null || screen === null || surface === null || surface === undefined)
                return;
            const scene = result.scene;
            const local = inverseInspectionPoint(this.#view, screen);
            const markLocal = scene.metadata.hitTestingEnabled
                ? hitTestScene(scene, local.x, local.y, 8 / this.#view.zoom)
                : null;
            const regionFamily = markLocal?.familyInteraction === undefined ? this.#familyRegionAt(local) : null;
            const nearestFamily = markLocal?.familyInteraction === undefined && regionFamily === null
                ? this.#nearestFamilyNode(local)
                : null;
            const familyLocal = markLocal?.familyInteraction !== undefined
                ? markLocal
                : regionFamily !== null
                    ? { ...regionFamily, x: local.x, y: local.y, distance: 0 }
                    : nearestFamily === null
                        ? null
                        : {
                            ...nearestFamily.entry,
                            x: local.x,
                            y: local.y,
                            distance: nearestFamily.distance,
                        };
            const tooltipSpec = result.spec.interaction.tooltip;
            const tooltipLocal = type === 'hover' &&
                tooltipSpec !== false &&
                tooltipSpec.trigger === 'axis' &&
                markLocal === null
                ? hitTestAxisTooltip(scene, local.x, local.y)
                : markLocal;
            const screenHit = (hit) => hit === null
                ? null
                : { ...hit, x: screen.x, y: screen.y, distance: hit.distance * this.#view.zoom };
            const markHit = screenHit(markLocal);
            const tooltipHit = screenHit(tooltipLocal);
            if (type === 'hover' && tooltipSpec !== false) {
                if (tooltipHit === null)
                    this.#tooltip.hide();
                else
                    this.#tooltip.show(resolveTooltipContent(tooltipHit, result.spec), tooltipHit, sourceEvent, surface, this.#renderer?.overlayHost?.() ?? surface.parentElement ?? surface);
            }
            if (type === 'click' && !this.#handleFamilyClick(familyLocal, sourceEvent)) {
                this.#applyClickSelection(markLocal);
            }
            if (type === 'hover' || result.spec.interaction.click !== false) {
                this.#events.emit(type, { chart: this, hit: markHit, sourceEvent });
            }
        }
        #handleFamilyClick(hit, event) {
            const interaction = hit?.familyInteraction;
            if (hit === null || interaction === undefined)
                return false;
            if (interaction.kind === 'pie-slice') {
                this.#setFamilyFocus({ kind: 'pie-slice', layerId: hit.layerId, id: interaction.id }, 'pointer');
                return false;
            }
            if (interaction.kind === 'table-cell') {
                this.#setFamilyFocus({
                    kind: 'table-cell',
                    layerId: hit.layerId,
                    row: interaction.row,
                    column: interaction.column,
                    field: interaction.field,
                }, 'pointer');
                return false;
            }
            if (interaction.kind === 'table-header') {
                const current = this.getTableRuntimeState(hit.layerId);
                const existing = current.sort.find(({ field }) => field === interaction.field);
                const nextDirection = existing === undefined
                    ? 'ascending'
                    : existing.direction === 'ascending'
                        ? 'descending'
                        : null;
                const retained = event.shiftKey
                    ? current.sort.filter(({ field }) => field !== interaction.field)
                    : [];
                const sort = nextDirection === null
                    ? retained
                    : [...retained, { field: interaction.field, direction: nextDirection }];
                this.#setTableRuntimeState(hit.layerId, normalizeTableRuntimeState({ sort, windowOffset: 0 }, current), 'pointer');
                return true;
            }
            if (interaction.kind === 'network-node') {
                const toggleCollapse = (event.altKey || event.detail >= 2) && interaction.compound;
                const togglePin = event.ctrlKey || event.metaKey;
                if (!toggleCollapse && !togglePin)
                    return false;
                const current = this.getNetworkRuntimeState(hit.layerId);
                let next = current;
                if (toggleCollapse) {
                    const collapsed = new Set(current.collapsed);
                    if (collapsed.has(interaction.id))
                        collapsed.delete(interaction.id);
                    else
                        collapsed.add(interaction.id);
                    next = normalizeNetworkRuntimeState({ collapsed: [...collapsed] }, next);
                }
                if (togglePin) {
                    const position = next.positions[interaction.id] ?? interaction.position;
                    next = normalizeNetworkRuntimeState({
                        positions: {
                            ...next.positions,
                            [interaction.id]: { ...position, pinned: !interaction.pinned },
                        },
                    }, next);
                }
                this.#setNetworkRuntimeState(hit.layerId, next, 'pointer');
                return true;
            }
            if (interaction.kind === 'hierarchy-node') {
                const current = this.getHierarchyRuntimeState(hit.layerId);
                if (event.shiftKey) {
                    this.#setHierarchyRuntimeState(hit.layerId, normalizeHierarchyRuntimeState({ root: interaction.id, zoomTo: null }, current), 'pointer');
                }
                else if (event.altKey) {
                    this.#setHierarchyRuntimeState(hit.layerId, normalizeHierarchyRuntimeState({ zoomTo: interaction.id }, current), 'pointer');
                }
                else if (!interaction.leaf || current.collapsed.includes(interaction.id)) {
                    const collapsed = new Set(current.collapsed);
                    if (collapsed.has(interaction.id))
                        collapsed.delete(interaction.id);
                    else
                        collapsed.add(interaction.id);
                    this.#setHierarchyRuntimeState(hit.layerId, normalizeHierarchyRuntimeState({ collapsed: [...collapsed] }, current), 'pointer');
                }
                else
                    return false;
                return true;
            }
            if (interaction.kind === 'parallel-axis' && (event.altKey || event.detail >= 2)) {
                this.#setParallelRuntimeState(hit.layerId, invertParallelAxis(this.getParallelRuntimeState(hit.layerId), interaction.field), 'pointer');
                return true;
            }
            return false;
        }
        #applyClickSelection(hit, reason = 'click', analyticReason = 'pointer') {
            const selection = this.#result?.spec.interaction.selection;
            if (selection === undefined || selection === false)
                return;
            if (selection.kind !== 'point')
                return;
            if (hit === null) {
                if (selection.clearOnBackground && this.#selection.length > 0) {
                    this.#selection = [];
                    this.#analyticSelection.clear(selection.combine);
                    this.render();
                    this.#emitSelection(reason);
                    this.#emitAnalyticSelection(analyticReason);
                }
                return;
            }
            const keyValue = selection.key === undefined
                ? undefined
                : (hit.tooltip?.[selection.key] ?? hit.datum[selection.key]);
            const portableKey = keyValue === null ||
                typeof keyValue === 'string' ||
                typeof keyValue === 'boolean' ||
                (typeof keyValue === 'number' && Number.isFinite(keyValue))
                ? keyValue
                : undefined;
            const target = selection.key !== undefined && portableKey !== undefined
                ? {
                    type: 'datum',
                    ...(selection.linked ? {} : { layerId: hit.layerId }),
                    field: selection.key,
                    value: portableKey,
                }
                : { type: 'datum', layerId: hit.layerId, rowIndex: hit.rowIndex };
            const key = selectionKey(target);
            const existing = this.#selection.findIndex((candidate) => selectionKey(candidate) === key);
            const before = this.#selection.map(selectionKey).join('|');
            if (selection.mode === 'single') {
                this.#selection = existing >= 0 && selection.toggle ? [] : [target];
            }
            else if (existing >= 0 && selection.toggle) {
                this.#selection = this.#selection.filter((_, index) => index !== existing);
            }
            else if (existing < 0) {
                this.#selection = [...this.#selection, target];
            }
            if (before === this.#selection.map(selectionKey).join('|'))
                return;
            this.#syncAnalyticPointTargets();
            this.render();
            this.#emitSelection(reason);
            this.#emitAnalyticSelection(analyticReason);
        }
        #emitSelection(reason) {
            this.#syncSelectionAccessibility();
            this.#events.emit('selectionchange', { chart: this, state: this.getSelection(), reason });
        }
        #requireAnalyticSelection() {
            const selection = this.#result?.spec.interaction.selection;
            if (selection === undefined || selection === false) {
                throw new GraflumeError('INVALID_SPEC', 'Enable interaction.selection before changing analytic selection state.');
            }
            return selection;
        }
        #validateAnalyticStateForConfig(state, config) {
            if (state.selections.length > config.maxSelections) {
                throw new GraflumeError('INVALID_SPEC', `Analytic selection exceeds the configured ${config.maxSelections} selection bound.`);
            }
            if (config.mode === 'single' && state.selections.length > 1) {
                throw new GraflumeError('INVALID_SPEC', 'Single selection mode accepts at most one selection.');
            }
            for (const selection of state.selections) {
                if (selection.type !== config.kind) {
                    throw new GraflumeError('INVALID_SPEC', `Selection type "${selection.type}" does not match configured kind "${config.kind}".`);
                }
                if (selection.type === 'lasso' && selection.points.length > config.maxLassoPoints) {
                    throw new GraflumeError('INVALID_SPEC', `Lasso selection exceeds the configured ${config.maxLassoPoints} point bound.`);
                }
            }
        }
        #syncAnalyticPointTargets() {
            const config = this.#result?.spec.interaction.selection;
            if (config === undefined || config === false || config.kind !== 'point')
                return;
            this.#analyticSelection.set({
                version: analyticSelectionVersion,
                combine: config.combine,
                selections: this.#selection.map((target) => ({ type: 'point', target })),
            });
        }
        #emitAnalyticSelection(reason) {
            this.#syncSelectionAccessibility();
            const state = this.#analyticSelection.get();
            this.#events.emit('analyticselectionchange', {
                chart: this,
                state,
                reason,
            });
            if (!this.#applyingLinkedViewState) {
                this.#options.linkedViewStore?.setAnalyticSelection(state, this.#semanticViewId);
            }
        }
        #applyLinkedViewState(change) {
            if (this.#applyingLinkedViewState || change.sourceViewId === this.#semanticViewId)
                return;
            const previousSelection = this.#analyticSelection.get();
            const previousTargets = this.#selection;
            const previousDomain = this.#domainView;
            this.#applyingLinkedViewState = true;
            try {
                if (change.changed === 'analytic-selection' || change.changed === 'both') {
                    const config = this.#result?.spec.interaction.selection;
                    if (config === undefined || config === false) {
                        if (change.state.analyticSelection.selections.length > 0) {
                            throw new GraflumeError('INVALID_SPEC', 'Linked analytic selection requires interaction.selection in every view.');
                        }
                    }
                    else {
                        this.#validateAnalyticStateForConfig(change.state.analyticSelection, config);
                        this.#analyticSelection.set(change.state.analyticSelection);
                        if (config.kind === 'point') {
                            this.#selection = change.state.analyticSelection.selections.flatMap((candidate) => candidate.type === 'point' && candidate.target !== undefined
                                ? [cloneDatumTarget(candidate.target)]
                                : []);
                        }
                        else
                            this.#selection = [];
                    }
                }
                if (change.changed === 'domain-view' || change.changed === 'both') {
                    const navigation = this.#domainNavigation();
                    const next = normalizeDomainViewState(change.state.domainView);
                    if (navigation === false && Object.keys(next.axes).length > 0) {
                        throw new GraflumeError('INVALID_SPEC', 'Linked domain state requires interaction.domainNavigation in every view.');
                    }
                    if (navigation !== false) {
                        for (const axis of Object.keys(next.axes)) {
                            if (!navigation.axes.includes(axis)) {
                                throw new GraflumeError('INVALID_SPEC', `Linked domain axis "${axis}" is not enabled in this view.`);
                            }
                        }
                    }
                    this.#domainView = next;
                }
                this.render();
                if (change.changed === 'analytic-selection' || change.changed === 'both') {
                    this.#emitAnalyticSelection('linked');
                    const selection = this.#result?.spec.interaction.selection;
                    if (selection !== undefined && selection !== false && selection.kind === 'point') {
                        this.#emitSelection('programmatic');
                    }
                }
                if (change.changed === 'domain-view' || change.changed === 'both') {
                    this.#emitDomainView('linked');
                }
            }
            catch (error) {
                this.#analyticSelection.set(previousSelection);
                this.#selection = previousTargets;
                this.#domainView = previousDomain;
                this.#events.emit('error', { chart: this, error });
            }
            finally {
                this.#applyingLinkedViewState = false;
            }
        }
        #emitAnnotations(reason, id) {
            this.#events.emit('annotationchange', {
                chart: this,
                annotations: this.getAnnotations(),
                reason,
                ...(id === undefined ? {} : { id }),
            });
        }
        #markLabelAuthoring() {
            const labels = this.#result?.spec.markLabels;
            return labels === undefined || labels === false ? false : labels.authoring;
        }
        #resetMarkLabels(spec) {
            const labels = spec.markLabels;
            const historyLimit = labels === false || labels.authoring === false ? 50 : labels.authoring.historyLimit;
            this.#markLabels.reset(labels === false ? [] : labels.positions, historyLimit);
            this.#activeMarkLabelId = null;
            this.#markLabelGesture = null;
        }
        #specWithMarkLabelPositions(positions) {
            const labels = this.#spec.markLabels;
            if (labels === undefined || labels === false) {
                throw new GraflumeError('INVALID_SPEC', 'Enable markLabels before setting authored label positions.');
            }
            return {
                ...this.#spec,
                markLabels: {
                    ...(labels === true ? {} : labels),
                    positions: cloneMarkLabelPositions(positions),
                },
            };
        }
        #requireMarkLabel(id) {
            const entry = this.#result?.scene.metadata.markLabels?.entries.find((candidate) => candidate.id === id);
            if (entry === undefined) {
                throw new GraflumeError('INVALID_SPEC', `Mark label "${id}" was not found.`);
            }
            return entry;
        }
        #emitMarkLabels(reason, id) {
            const state = this.getMarkLabelState();
            this.#events.emit('marklabelchange', {
                chart: this,
                state,
                reason,
                ...(id === undefined ? {} : { id }),
            });
            const entry = id === undefined ? undefined : state.labels.find((candidate) => candidate.id === id);
            const status = reason === 'undo'
                ? 'Label movement undone.'
                : reason === 'redo'
                    ? 'Label movement redone.'
                    : reason === 'reset'
                        ? 'Label positions reset.'
                        : entry === undefined
                            ? `Label authoring ${reason}.`
                            : `${entry.text}: horizontal offset ${entry.offsetX}, vertical offset ${entry.offsetY}.`;
            if (this.#markLabelLive !== null)
                this.#markLabelLive.textContent = status;
        }
        #syncMarkLabelAccessibility() {
            const host = this.#renderer?.overlayHost?.();
            const authoring = this.#markLabelAuthoring();
            if (host === null || host === undefined || authoring === false) {
                this.#destroyMarkLabelLive();
                return;
            }
            if (this.#markLabelLiveHost === host && this.#markLabelLive !== null)
                return;
            this.#destroyMarkLabelLive();
            const live = host.ownerDocument.createElement('div');
            live.dataset.graflumeMarkLabelStatus = 'true';
            live.setAttribute('role', 'status');
            live.setAttribute('aria-live', 'polite');
            live.style.position = 'absolute';
            live.style.width = '1px';
            live.style.height = '1px';
            live.style.overflow = 'hidden';
            live.style.clipPath = 'inset(50%)';
            host.append(live);
            this.#markLabelLive = live;
            this.#markLabelLiveHost = host;
        }
        #destroyMarkLabelLive() {
            this.#markLabelLive?.remove();
            this.#markLabelLive = null;
            this.#markLabelLiveHost = null;
        }
        #setAnnotationsVisible(visible, reason) {
            this.#assertAlive();
            if (typeof visible !== 'boolean')
                throw new TypeError('Annotation visibility must be a boolean.');
            if (visible === this.#annotationsVisible)
                return this;
            this.#annotationsVisible = visible;
            this.render();
            this.#emitAnnotationVisibility(reason);
            return this;
        }
        #emitAnnotationVisibility(reason) {
            this.#events.emit('annotationvisibilitychange', {
                chart: this,
                visible: this.#annotationsVisible,
                reason,
            });
        }
        #isOwnFullscreen() {
            if (typeof document === 'undefined')
                return false;
            const host = this.#renderer?.overlayHost?.();
            return host !== null && host !== undefined && document.fullscreenElement === host;
        }
        #fullscreenAvailable() {
            const host = this.#renderer?.overlayHost?.();
            return (host !== null &&
                host !== undefined &&
                typeof document !== 'undefined' &&
                typeof host.requestFullscreen === 'function' &&
                typeof document.exitFullscreen === 'function');
        }
        #syncControls() {
            const controls = this.#result?.spec.interaction.controls;
            const host = this.#renderer?.overlayHost?.();
            if (controls === false || controls === undefined || host === null || host === undefined) {
                this.#controls.destroy();
                return;
            }
            const navigation = this.#navigation();
            const domainNavigation = this.#domainNavigation();
            const domainZoom = domainNavigation === false
                ? 1
                : Math.max(...domainNavigation.axes.map((axis) => {
                    const window = domainAxisWindow(this.#domainView, axis);
                    return 1 / (window.end - window.start);
                }));
            const playbackState = this.getPlaybackState();
            const state = {
                spec: controls,
                navigationEnabled: domainNavigation !== false ||
                    (navigation !== false &&
                        this.#renderer?.capabilities.inspectionViewport === true &&
                        this.#renderer.setInspectionView !== undefined),
                viewDirty: domainNavigation !== false
                    ? !domainViewIsIdentity(this.#domainView)
                    : navigation !== false &&
                        !sameView(this.#view, { zoom: navigation.minZoom, offsetX: 0, offsetY: 0 }),
                zoom: domainNavigation === false ? this.#view.zoom : domainZoom,
                minZoom: domainNavigation === false ? (navigation === false ? 1 : navigation.minZoom) : 1,
                maxZoom: domainNavigation === false
                    ? navigation === false
                        ? 1
                        : navigation.maxZoom
                    : domainNavigation.maxZoom,
                fullscreenAvailable: this.#fullscreenAvailable(),
                fullscreen: this.#fullscreen,
                exportAvailable: this.#renderer?.toDataURL !== undefined &&
                    this.#renderer.capabilities.exportFormats.includes('image/png'),
                annotationsAvailable: this.#annotations.length > 0,
                annotationsVisible: this.#annotationsVisible,
                playbackEnabled: playbackState.enabled,
                playbackIndex: playbackState.index,
                playbackLength: playbackState.frames.length,
                playbackRangeStart: playbackState.range?.start ?? 0,
                playbackRangeEnd: playbackState.range?.end ?? -1,
                playing: playbackState.playing,
                playbackRate: playbackState.rate,
                loop: playbackState.loop,
                frameLabel: playbackState.label,
                frameNamed: playbackState.name !== undefined,
            };
            const actions = {
                zoomIn: () => (domainNavigation === false ? this.zoomBy(1.25) : this.zoomDomainBy(1.25)),
                zoomOut: () => (domainNavigation === false ? this.zoomBy(0.8) : this.zoomDomainBy(0.8)),
                reset: () => (domainNavigation === false ? this.resetView() : this.resetDomainView()),
                toggleFullscreen: () => void this.toggleFullscreen().catch(() => undefined),
                exportPng: () => this.#exportPng(),
                toggleAnnotations: () => this.toggleAnnotations(),
                previousFrame: () => this.step(-1),
                togglePlayback: () => (this.#playing ? this.pause() : this.play()),
                nextFrame: () => this.step(1),
                seek: (index) => this.seek(index),
                setRate: (rate) => this.setPlaybackRate(rate),
                setLoop: (loop) => this.setPlaybackLoop(loop),
            };
            this.#controls.sync(host, state, actions);
        }
        #syncLegend() {
            const result = this.#result;
            const host = this.#renderer?.overlayHost?.();
            if (result === null || host === null || host === undefined) {
                this.#legend.destroy();
                return;
            }
            this.#legend.sync(host, sceneLegendLayout(result.scene), result.spec.legend, this.#view, {
                setVisible: (id, visible) => this.#setLegendItemVisible(id, visible, 'toggle'),
            });
        }
        #semanticSelected(mark, target) {
            if (target.layerId !== undefined && target.layerId !== mark.layerId)
                return false;
            if (target.rowIndex !== undefined) {
                const rows = Array.isArray(target.rowIndex) ? target.rowIndex : [target.rowIndex];
                if (!rows.includes(mark.rowIndex))
                    return false;
            }
            if (target.field !== undefined) {
                const value = mark.datum[target.field];
                const values = target.values ?? (target.value === undefined ? [] : [target.value]);
                if (!values.some((candidate) => Object.is(candidate, value)))
                    return false;
            }
            return target.rowIndex !== undefined || target.field !== undefined;
        }
        #semanticSelectionTarget(mark) {
            const selection = this.#result?.spec.interaction.selection;
            if (selection !== undefined && selection !== false && selection.key !== undefined) {
                const value = mark.datum[selection.key];
                if (value === null ||
                    typeof value === 'string' ||
                    typeof value === 'boolean' ||
                    (typeof value === 'number' && Number.isFinite(value))) {
                    return { type: 'datum', layerId: mark.layerId, field: selection.key, value };
                }
            }
            return { type: 'datum', layerId: mark.layerId, rowIndex: mark.rowIndex };
        }
        #toggleSemanticSelection(mark) {
            const selection = this.#result?.spec.interaction.selection;
            if (selection === undefined || selection === false)
                return;
            if (selection.kind !== 'point')
                return;
            const target = this.#semanticSelectionTarget(mark);
            const key = selectionKey(target);
            const existing = this.#selection.findIndex((candidate) => selectionKey(candidate) === key);
            if (selection.mode === 'single') {
                this.setSelection(existing >= 0 && selection.toggle ? [] : [target]);
                return;
            }
            if (existing >= 0 && selection.toggle) {
                this.setSelection(this.#selection.filter((_, index) => index !== existing));
            }
            else if (existing < 0)
                this.setSelection([...this.#selection, target]);
        }
        #focusSemanticMark(mark) {
            if (mark === null) {
                this.#tooltip.hide();
                return;
            }
            const result = this.#result;
            const surface = this.#renderer?.surface();
            const host = this.#renderer?.overlayHost?.();
            if (result === null ||
                result.spec.interaction.tooltip === false ||
                surface === null ||
                surface === undefined ||
                host === null ||
                host === undefined)
                return;
            const x = (mark.bounds.x + mark.bounds.width / 2) * this.#view.zoom + this.#view.offsetX;
            const y = (mark.bounds.y + mark.bounds.height / 2) * this.#view.zoom + this.#view.offsetY;
            const hostBounds = host.getBoundingClientRect();
            const hit = {
                layerId: mark.layerId,
                rowIndex: mark.rowIndex,
                datum: mark.datum,
                nodeId: mark.id,
                x,
                y,
                distance: 0,
            };
            this.#tooltip.showAt(resolveTooltipContent(hit, result.spec), hit, hostBounds.left + x, hostBounds.top + y, surface, host);
        }
        #syncAccessibilityMirror() {
            const result = this.#result;
            const surface = this.#renderer?.surface();
            const host = this.#renderer?.overlayHost?.();
            if (result === null ||
                surface === null ||
                surface === undefined ||
                host === null ||
                host === undefined) {
                this.#accessibility.destroy();
                this.#linkedFocusUnregister?.();
                this.#linkedFocusUnsubscribe?.();
                this.#linkedFocusUnregister = null;
                this.#linkedFocusUnsubscribe = null;
                return;
            }
            const selectedIds = new Set(result.scene.semanticIndex
                .filter((mark) => this.#selection.some((target) => this.#semanticSelected(mark, target)))
                .map(({ id }) => id));
            this.#accessibility.sync(this.#target, host, surface, result.scene.semanticIndex, result.spec.accessibility, this.#view, selectedIds, {
                toggle: (mark) => this.#toggleSemanticSelection(mark),
                clear: () => this.clearSelection(),
                focus: (mark) => {
                    this.#focusSemanticMark(mark);
                    this.#publishLinkedFocus(mark);
                },
            });
            this.#syncLinkedFocus();
        }
        #focusStore() {
            return this.#options.focusStore ?? defaultSemanticFocusStore;
        }
        #syncLinkedFocus() {
            this.#linkedFocusUnregister?.();
            this.#linkedFocusUnsubscribe?.();
            this.#linkedFocusUnregister = null;
            this.#linkedFocusUnsubscribe = null;
            this.#lastPublishedSemanticId = null;
            const result = this.#result;
            const linked = result?.spec.accessibility.linkedFocus;
            if (result === null || result === undefined || linked === undefined || linked === false)
                return;
            const index = result.scene.semanticIndex.filter(({ datum }) => {
                const value = datum[linked.key];
                return ((typeof value === 'string' && value !== '') ||
                    typeof value === 'boolean' ||
                    (typeof value === 'number' && Number.isFinite(value)) ||
                    (value instanceof Date && Number.isFinite(value.getTime())));
            });
            const store = this.#focusStore();
            this.#linkedFocusUnregister = store.registerView(this.#semanticViewId, linked, index);
            this.#linkedFocusUnsubscribe = store.subscribe((change) => this.#applyLinkedFocus(change));
            this.#applyLinkedFocus({ state: store.state(), reason: 'index' });
        }
        #applyLinkedFocus(change) {
            const linked = this.#result?.spec.accessibility.linkedFocus;
            if (linked === undefined || linked === false || change.state.focused?.group !== linked.group)
                return;
            const match = change.state.matches.find(({ viewId }) => viewId === this.#semanticViewId);
            if (match === undefined || this.#accessibility.getFocusedId() === match.semanticId)
                return;
            this.#applyingLinkedFocus = true;
            try {
                this.#accessibility.focusSemanticId(match.semanticId);
            }
            finally {
                this.#applyingLinkedFocus = false;
            }
        }
        #publishLinkedFocus(mark) {
            const linked = this.#result?.spec.accessibility.linkedFocus;
            if (linked === undefined || linked === false || this.#applyingLinkedFocus)
                return;
            const store = this.#focusStore();
            if (mark === null) {
                if (store.state().focused?.sourceViewId === this.#semanticViewId)
                    store.clear();
                this.#lastPublishedSemanticId = null;
                return;
            }
            if (this.#lastPublishedSemanticId === mark.id)
                return;
            const value = mark.datum[linked.key];
            if (!((typeof value === 'string' && value !== '') ||
                typeof value === 'boolean' ||
                (typeof value === 'number' && Number.isFinite(value)) ||
                (value instanceof Date && Number.isFinite(value.getTime()))))
                return;
            this.#lastPublishedSemanticId = mark.id;
            store.focus(this.#semanticViewId, mark);
        }
        #syncSelectionAccessibility() {
            const host = this.#renderer?.overlayHost?.();
            const selection = this.#result?.spec.interaction.selection;
            const live = this.#result?.spec.accessibility.live;
            if (host === null ||
                host === undefined ||
                selection === undefined ||
                selection === false ||
                live === undefined ||
                live === false) {
                this.#destroySelectionLive();
                return;
            }
            if (this.#selectionLiveHost !== host) {
                this.#destroySelectionLive();
                const live = host.ownerDocument.createElement('div');
                live.dataset.graflumeSelectionStatus = 'true';
                live.setAttribute('role', 'status');
                live.setAttribute('aria-live', 'polite');
                live.style.position = 'absolute';
                live.style.width = '1px';
                live.style.height = '1px';
                live.style.overflow = 'hidden';
                live.style.clipPath = 'inset(50%)';
                host.append(live);
                this.#selectionLive = live;
                this.#selectionLiveHost = host;
            }
            if (this.#selectionLive !== null) {
                const selectedCount = selection.kind === 'point'
                    ? this.#selection.length
                    : this.#analyticSelection.get().selections.length;
                const summary = `${selection.ariaLabel}: ${selectedCount}`;
                if (this.#selectionLive.textContent === summary)
                    return;
                const throttleMs = live.throttleMs;
                const elapsed = Date.now() - this.#selectionLiveUpdatedAt;
                const update = () => {
                    if (this.#selectionLive !== null)
                        this.#selectionLive.textContent = summary;
                    this.#selectionLiveUpdatedAt = Date.now();
                    this.#selectionLiveTimer = null;
                };
                if (this.#selectionLiveUpdatedAt === 0 || elapsed >= throttleMs)
                    update();
                else {
                    if (this.#selectionLiveTimer !== null)
                        clearTimeout(this.#selectionLiveTimer);
                    this.#selectionLiveTimer = setTimeout(update, throttleMs - elapsed);
                }
            }
        }
        #destroySelectionLive() {
            if (this.#selectionLiveTimer !== null)
                clearTimeout(this.#selectionLiveTimer);
            this.#selectionLiveTimer = null;
            this.#selectionLiveUpdatedAt = 0;
            this.#selectionLive?.remove();
            this.#selectionLive = null;
            this.#selectionLiveHost = null;
        }
        #exportPng() {
            if (typeof document === 'undefined')
                return;
            try {
                const link = document.createElement('a');
                link.href = this.toDataURL('image/png');
                link.download = 'graflume-chart.png';
                link.hidden = true;
                document.body.append(link);
                link.click();
                link.remove();
            }
            catch (error) {
                this.#events.emit('error', { chart: this, error });
            }
        }
        #streamRuntime(layerId) {
            const streaming = this.#spec.streaming;
            if (streaming === undefined) {
                throw new GraflumeError('INVALID_DATA', 'A streaming runtime requires an explicit ChartSpec.streaming contract.');
            }
            const target = this.#streamingTarget(layerId);
            const existing = this.#streamRuntimes.get(target.id);
            if (existing !== undefined)
                return existing;
            const layerTransforms = target.layerId === undefined
                ? []
                : (this.#spec.layers?.find((layer, index) => (layer.id ?? `layer-${index}`) === target.layerId)?.transform ?? []);
            const transforms = [
                ...(this.#spec.transform ?? []),
                ...layerTransforms,
            ];
            const runtime = new IncrementalStreamRuntime(target.source, transforms, streaming, streaming.runtime, this.#options.streamScheduler);
            this.#streamRuntimes.set(target.id, runtime);
            return runtime;
        }
        #applyStreamingRows(rows, layerId) {
            this.#preserveStreamRuntimes = true;
            try {
                this.setData(rows, layerId);
            }
            finally {
                this.#preserveStreamRuntimes = false;
            }
        }
        #streamingTarget(layerId, required = true) {
            if (this.#spec.streaming === undefined) {
                if (!required)
                    return null;
                throw new GraflumeError('INVALID_DATA', 'An explicit ChartSpec.streaming contract is required.');
            }
            if (layerId === undefined && this.#spec.data !== undefined) {
                return { id: '$chart', source: this.#spec.data };
            }
            const layers = this.#spec.layers;
            if (layers === undefined) {
                if (!required)
                    return null;
                throw new GraflumeError('INVALID_DATA', 'The chart has no layer data to update.');
            }
            const targetLayerId = layerId ?? (layers.length === 1 ? (layers[0]?.id ?? 'layer-0') : undefined);
            if (targetLayerId === undefined) {
                throw new GraflumeError('INVALID_DATA', 'Specify layerId when updating a multi-layer chart.');
            }
            const index = layers.findIndex((layer, candidate) => (layer.id ?? `layer-${candidate}`) === targetLayerId);
            const layer = layers[index];
            const source = layer?.data ?? this.#spec.data;
            if (layer === undefined || source === undefined) {
                if (!required)
                    return null;
                throw new GraflumeError('INVALID_DATA', `Layer "${targetLayerId}" has no data source.`);
            }
            return { id: `layer:${targetLayerId}`, source, layerId: targetLayerId };
        }
        #assertAlive() {
            if (this.#destroyed) {
                throw new GraflumeError('DESTROYED_CHART', 'This chart instance has been destroyed.');
            }
        }
    }

    function finite(value, path) {
        if (typeof value !== 'number' || !Number.isFinite(value)) {
            throw new GraflumeError('INVALID_DATA', `${path} must be a finite number.`, { path });
        }
        return value;
    }
    function clamp(value, minimum, maximum) {
        return Math.max(minimum, Math.min(maximum, value));
    }
    function longitude(value, path) {
        const resolved = finite(value, path);
        if (resolved < -540 || resolved > 540)
            throw new GraflumeError('INVALID_DATA', `${path} is outside the supported longitude range.`);
        return resolved;
    }
    function latitude(value, path) {
        const resolved = finite(value, path);
        if (resolved < -90 || resolved > 90)
            throw new GraflumeError('INVALID_DATA', `${path} must be from -90 to 90.`);
        return resolved;
    }
    function wrapLongitude(value) {
        const finiteValue = longitude(value, '$.longitude');
        return ((((finiteValue + 180) % 360) + 360) % 360) - 180;
    }
    function position(value, path) {
        if (!Array.isArray(value) || value.length < 2)
            throw new GraflumeError('INVALID_DATA', `${path} must be [longitude, latitude].`, { path });
        return [longitude(value[0], `${path}[0]`), latitude(value[1], `${path}[1]`)];
    }
    function positions(value, depth, path) {
        if (depth === 0)
            return position(value, path);
        if (!Array.isArray(value))
            throw new GraflumeError('INVALID_DATA', `${path} must be an array.`, { path });
        return value.map((entry, index) => positions(entry, depth - 1, `${path}[${index}]`));
    }
    function normalizeGeometry(value, path) {
        if (typeof value !== 'object' || value === null || Array.isArray(value))
            throw new GraflumeError('INVALID_DATA', `${path} must be a GeoJSON geometry.`);
        const candidate = value;
        if (candidate.type === 'GeometryCollection') {
            if (!Array.isArray(candidate.geometries))
                throw new GraflumeError('INVALID_DATA', `${path}.geometries must be an array.`, {
                    path: `${path}.geometries`,
                });
            return {
                type: 'GeometryCollection',
                geometries: candidate.geometries.map((geometry, index) => normalizeGeometry(geometry, `${path}.geometries[${index}]`)),
            };
        }
        const depth = new Map([
            ['Point', 0],
            ['MultiPoint', 1],
            ['LineString', 1],
            ['MultiLineString', 2],
            ['Polygon', 2],
            ['MultiPolygon', 3],
        ]).get(String(candidate.type));
        if (depth === undefined)
            throw new GraflumeError('INVALID_DATA', `${path}.type is unsupported.`);
        return {
            type: candidate.type,
            coordinates: positions(candidate.coordinates, depth, `${path}.coordinates`),
        };
    }
    /** Validates GeoJSON and returns a closed, normalized FeatureCollection. */
    function normalizeGeoJson(value) {
        if (typeof value !== 'object' || value === null || Array.isArray(value))
            throw new GraflumeError('INVALID_DATA', 'GeoJSON must be an object.');
        const candidate = value;
        const inputs = candidate.type === 'FeatureCollection'
            ? Array.isArray(candidate.features)
                ? candidate.features
                : (() => {
                    throw new GraflumeError('INVALID_DATA', '$.features must be an array.');
                })()
            : [
                candidate.type === 'Feature'
                    ? candidate
                    : { type: 'Feature', geometry: candidate, properties: null },
            ];
        const features = inputs.map((entry, index) => {
            if (typeof entry !== 'object' || entry === null || Array.isArray(entry))
                throw new GraflumeError('INVALID_DATA', `$.features[${index}] must be an object.`);
            const feature = entry;
            if (feature.type !== 'Feature')
                throw new GraflumeError('INVALID_DATA', `$.features[${index}].type must be Feature.`);
            const properties = feature.properties == null
                ? null
                : typeof feature.properties === 'object' && !Array.isArray(feature.properties)
                    ? feature.properties
                    : (() => {
                        throw new GraflumeError('INVALID_DATA', `$.features[${index}].properties must be an object or null.`);
                    })();
            return {
                type: 'Feature',
                ...(typeof feature.id === 'string' || typeof feature.id === 'number'
                    ? { id: feature.id }
                    : {}),
                properties,
                geometry: feature.geometry === null
                    ? null
                    : normalizeGeometry(feature.geometry, `$.features[${index}].geometry`),
            };
        });
        return { type: 'FeatureCollection', features };
    }
    function decodedArcs(topology) {
        const transform = topology.transform;
        if (transform !== undefined) {
            transform.scale.forEach((value, index) => finite(value, `$.transform.scale[${index}]`));
            transform.translate.forEach((value, index) => finite(value, `$.transform.translate[${index}]`));
        }
        return topology.arcs.map((arc, arcIndex) => {
            let x = 0;
            let y = 0;
            return arc.map((entry, pointIndex) => {
                if (!Array.isArray(entry) || entry.length < 2)
                    throw new GraflumeError('INVALID_DATA', `$.arcs[${arcIndex}][${pointIndex}] must be a pair.`);
                if (transform === undefined)
                    return position(entry, `$.arcs[${arcIndex}][${pointIndex}]`);
                x += finite(entry[0], `$.arcs[${arcIndex}][${pointIndex}][0]`);
                y += finite(entry[1], `$.arcs[${arcIndex}][${pointIndex}][1]`);
                return position([
                    x * transform.scale[0] + transform.translate[0],
                    y * transform.scale[1] + transform.translate[1],
                ], `$.arcs[${arcIndex}][${pointIndex}]`);
            });
        });
    }
    function topoArc(reference, arcs) {
        const index = reference < 0 ? ~reference : reference;
        const arc = arcs[index];
        if (arc === undefined)
            throw new GraflumeError('INVALID_DATA', `Topology references unknown arc ${reference}.`);
        return reference < 0 ? [...arc].reverse() : [...arc];
    }
    function joinTopoArcs(references, arcs, path) {
        if (!Array.isArray(references))
            throw new GraflumeError('INVALID_DATA', `${path} must be an arc array.`);
        const output = [];
        references.forEach((reference, index) => {
            if (!Number.isInteger(reference))
                throw new GraflumeError('INVALID_DATA', `${path}[${index}] must be an integer.`);
            const arc = topoArc(reference, arcs);
            output.push(...(output.length === 0 ? arc : arc.slice(1)));
        });
        return output;
    }
    function topologyGeometry(object, arcs, path) {
        if (object.type === 'GeometryCollection') {
            if (!Array.isArray(object.geometries))
                throw new GraflumeError('INVALID_DATA', `${path}.geometries must be an array.`);
            return object.geometries.flatMap((geometry, index) => topologyGeometry(geometry, arcs, `${path}.geometries[${index}]`));
        }
        let geometry;
        if (object.type === 'Point')
            geometry = { type: 'Point', coordinates: position(object.coordinates, `${path}.coordinates`) };
        else if (object.type === 'MultiPoint')
            geometry = {
                type: 'MultiPoint',
                coordinates: positions(object.coordinates, 1, `${path}.coordinates`),
            };
        else if (object.type === 'LineString')
            geometry = { type: 'LineString', coordinates: joinTopoArcs(object.arcs, arcs, `${path}.arcs`) };
        else if (object.type === 'MultiLineString') {
            if (!Array.isArray(object.arcs))
                throw new GraflumeError('INVALID_DATA', `${path}.arcs must be an array.`);
            geometry = {
                type: 'MultiLineString',
                coordinates: object.arcs.map((references, index) => joinTopoArcs(references, arcs, `${path}.arcs[${index}]`)),
            };
        }
        else if (object.type === 'Polygon') {
            if (!Array.isArray(object.arcs))
                throw new GraflumeError('INVALID_DATA', `${path}.arcs must be an array.`);
            geometry = {
                type: 'Polygon',
                coordinates: object.arcs.map((references, index) => joinTopoArcs(references, arcs, `${path}.arcs[${index}]`)),
            };
        }
        else if (object.type === 'MultiPolygon') {
            if (!Array.isArray(object.arcs))
                throw new GraflumeError('INVALID_DATA', `${path}.arcs must be an array.`);
            geometry = {
                type: 'MultiPolygon',
                coordinates: object.arcs.map((polygon, polygonIndex) => {
                    if (!Array.isArray(polygon))
                        throw new GraflumeError('INVALID_DATA', `${path}.arcs[${polygonIndex}] must be an array.`);
                    return polygon.map((references, ringIndex) => joinTopoArcs(references, arcs, `${path}.arcs[${polygonIndex}][${ringIndex}]`));
                }),
            };
        }
        else
            throw new GraflumeError('INVALID_DATA', `${path}.type is unsupported.`);
        return [
            {
                type: 'Feature',
                ...(object.id === undefined ? {} : { id: object.id }),
                properties: object.properties ?? null,
                geometry,
            },
        ];
    }
    /** Decodes delta/arcs/reversed-arcs TopoJSON into normalized GeoJSON features. */
    function topologyToGeoJson(topology, objectName) {
        if (topology.type !== 'Topology')
            throw new GraflumeError('INVALID_DATA', 'Topology type must be Topology.');
        const arcs = decodedArcs(topology);
        const names = objectName === undefined ? Object.keys(topology.objects) : [objectName];
        const features = names.flatMap((name) => {
            const object = topology.objects[name];
            if (object === undefined)
                throw new GraflumeError('INVALID_SPEC', `Unknown topology object "${name}".`);
            return topologyGeometry(object, arcs, `$.objects.${name}`);
        });
        return normalizeGeoJson({ type: 'FeatureCollection', features });
    }
    /** Closes and snapshots the mutable input used by the persistent map runtime. */
    function normalizeMapProjection(value = {}, path = '$.projection') {
        if (value === null || typeof value !== 'object' || Array.isArray(value))
            throw new GraflumeError('INVALID_SPEC', `${path} must be an object.`, { path });
        const unknown = Object.keys(value).find((key) => !['name', 'rotate', 'clip'].includes(key));
        if (unknown !== undefined)
            throw new GraflumeError('INVALID_SPEC', `Unknown map projection property "${unknown}".`, {
                path: `${path}.${unknown}`,
            });
        const name = value.name ?? 'equirectangular';
        if (!['equirectangular', 'mercator', 'orthographic'].includes(name))
            throw new GraflumeError('INVALID_SPEC', `${path}.name is unsupported.`, {
                path: `${path}.name`,
            });
        let rotate;
        if (value.rotate !== undefined) {
            if (!Array.isArray(value.rotate) || value.rotate.length !== 2)
                throw new GraflumeError('INVALID_SPEC', `${path}.rotate must be [longitude, latitude].`, {
                    path: `${path}.rotate`,
                });
            rotate = Object.freeze([
                longitude(value.rotate[0], `${path}.rotate[0]`),
                latitude(value.rotate[1], `${path}.rotate[1]`),
            ]);
        }
        let clip;
        if (value.clip !== undefined) {
            if (!Array.isArray(value.clip) || value.clip.length !== 4)
                throw new GraflumeError('INVALID_SPEC', `${path}.clip must be [west, south, east, north].`, {
                    path: `${path}.clip`,
                });
            const west = longitude(value.clip[0], `${path}.clip[0]`);
            const south = latitude(value.clip[1], `${path}.clip[1]`);
            const east = longitude(value.clip[2], `${path}.clip[2]`);
            const north = latitude(value.clip[3], `${path}.clip[3]`);
            if (west > east || south > north)
                throw new GraflumeError('INVALID_SPEC', `${path}.clip bounds must be ordered.`, {
                    path: `${path}.clip`,
                });
            clip = Object.freeze([west, south, east, north]);
        }
        return Object.freeze({
            name,
            ...(rotate === undefined ? {} : { rotate }),
            ...(clip === undefined ? {} : { clip }),
        });
    }
    const ANTIMERIDIAN_EPSILON = 1e-9;
    const WORLD_CLIP = Object.freeze([-180, -90, 180, 90]);
    const ORTHOGRAPHIC_HORIZON_CLIP = Object.freeze([-90, -90, 90, 90]);
    function projectionPosition(point, rotate) {
        return [
            wrapLongitude(longitude(point[0], '$.point[0]') + (rotate?.[0] ?? 0)),
            clamp(latitude(point[1], '$.point[1]') + (rotate?.[1] ?? 0), -90, 90),
        ];
    }
    function effectiveGeographicClip(clip) {
        const west = Math.max(-180, clip[0]);
        const south = Math.max(-90, clip[1]);
        const east = Math.min(180, clip[2]);
        const north = Math.min(90, clip[3]);
        return west > east || south > north ? null : [west, south, east, north];
    }
    function effectiveProjectionClip(projection) {
        const base = projection.name === 'orthographic' ? ORTHOGRAPHIC_HORIZON_CLIP : WORLD_CLIP;
        const authored = projection.clip ?? WORLD_CLIP;
        return effectiveGeographicClip([
            Math.max(base[0], authored[0]),
            Math.max(base[1], authored[1]),
            Math.min(base[2], authored[2]),
            Math.min(base[3], authored[3]),
        ]);
    }
    function sameGeographicPosition(left, right) {
        return Math.abs(left[0] - right[0]) <= 1e-10 && Math.abs(left[1] - right[1]) <= 1e-10;
    }
    function appendGeographicPosition(output, point) {
        if (output.length === 0 || !sameGeographicPosition(output[output.length - 1], point))
            output.push(point);
    }
    function projectionSafeLongitude(value) {
        return Math.abs(value - 180) <= 1e-10 ? 180 - ANTIMERIDIAN_EPSILON : value;
    }
    /**
     * Splits a wrapped geographic line at the antimeridian. Returned positions have already had
     * projection rotation applied, so callers must project them without applying rotation again.
     */
    function splitAntimeridianLine(points) {
        if (points.length === 0)
            return [];
        const paths = [];
        let current = [points[0]];
        for (let index = 1; index < points.length; index += 1) {
            const previous = points[index - 1];
            const next = points[index];
            const delta = next[0] - previous[0];
            if (Math.abs(delta) <= 180) {
                appendGeographicPosition(current, next);
                continue;
            }
            const crossesEast = delta < -180;
            const unwrappedNext = next[0] + (crossesEast ? 360 : -360);
            const boundary = crossesEast ? 180 : -180;
            const denominator = unwrappedNext - previous[0];
            const t = denominator === 0 ? 0 : (boundary - previous[0]) / denominator;
            const latitudeAtBoundary = previous[1] + (next[1] - previous[1]) * t;
            appendGeographicPosition(current, [
                crossesEast ? 180 - ANTIMERIDIAN_EPSILON : -180,
                latitudeAtBoundary,
            ]);
            if (current.length > 1)
                paths.push(current);
            current = [[crossesEast ? -180 : 180 - ANTIMERIDIAN_EPSILON, latitudeAtBoundary]];
            appendGeographicPosition(current, next);
        }
        if (current.length > 1)
            paths.push(current);
        return paths;
    }
    function liangBarskySegment(start, end, clip) {
        const dx = end[0] - start[0];
        const dy = end[1] - start[1];
        const p = [-dx, dx, -dy, dy];
        const q = [start[0] - clip[0], clip[2] - start[0], start[1] - clip[1], clip[3] - start[1]];
        let enter = 0;
        let exit = 1;
        for (let index = 0; index < 4; index += 1) {
            const direction = p[index];
            const distance = q[index];
            if (Math.abs(direction) <= Number.EPSILON) {
                if (distance < 0)
                    return null;
                continue;
            }
            const ratio = distance / direction;
            if (direction < 0)
                enter = Math.max(enter, ratio);
            else
                exit = Math.min(exit, ratio);
            if (enter > exit)
                return null;
        }
        const interpolate = (amount) => [
            projectionSafeLongitude(start[0] + dx * amount),
            start[1] + dy * amount,
        ];
        return [interpolate(enter), interpolate(exit)];
    }
    function clipLinePath(points, clip) {
        const paths = [];
        let current = [];
        const flush = () => {
            if (current.length > 1)
                paths.push(current);
            current = [];
        };
        for (let index = 1; index < points.length; index += 1) {
            const segment = liangBarskySegment(points[index - 1], points[index], clip);
            if (segment === null) {
                flush();
                continue;
            }
            if (current.length > 0 && !sameGeographicPosition(current[current.length - 1], segment[0]))
                flush();
            appendGeographicPosition(current, segment[0]);
            appendGeographicPosition(current, segment[1]);
        }
        flush();
        return paths;
    }
    /**
     * Applies projection rotation/wrap, antimeridian splitting, and rectangular Liang-Barsky
     * clipping. Orthographic paths are intersected with the visible hemisphere before projection,
     * so a segment crossing the horizon keeps its exact boundary point instead of disappearing.
     * Boundary intersections are retained and disjoint visible runs remain subpaths.
     */
    function clipMapLine(points, options = {}) {
        const projection = normalizeMapProjection(options);
        const split = splitAntimeridianLine(points.map((point) => projectionPosition(point, projection.rotate)));
        if (projection.clip === undefined && projection.name !== 'orthographic')
            return split;
        const clip = effectiveProjectionClip(projection);
        return clip === null ? [] : split.flatMap((path) => clipLinePath(path, clip));
    }
    function clipPolygonBoundary(input, boundary) {
        if (input.length === 0)
            return [];
        const output = [];
        let start = input[input.length - 1];
        let startInside = boundary.inside(start);
        for (const end of input) {
            const endInside = boundary.inside(end);
            if (endInside) {
                if (!startInside)
                    appendGeographicPosition(output, boundary.intersect(start, end));
                appendGeographicPosition(output, end);
            }
            else if (startInside)
                appendGeographicPosition(output, boundary.intersect(start, end));
            start = end;
            startInside = endInside;
        }
        if (output.length > 1 && sameGeographicPosition(output[0], output[output.length - 1]))
            output.pop();
        return output;
    }
    function sutherlandHodgmanRing(ring, clip) {
        const verticalIntersection = (longitude) => (start, end) => {
            const amount = (longitude - start[0]) / (end[0] - start[0]);
            return [projectionSafeLongitude(longitude), start[1] + (end[1] - start[1]) * amount];
        };
        const horizontalIntersection = (latitude) => (start, end) => {
            const amount = (latitude - start[1]) / (end[1] - start[1]);
            return [projectionSafeLongitude(start[0] + (end[0] - start[0]) * amount), latitude];
        };
        const boundaries = [
            { inside: ([lon]) => lon >= clip[0], intersect: verticalIntersection(clip[0]) },
            { inside: ([lon]) => lon <= clip[2], intersect: verticalIntersection(clip[2]) },
            { inside: ([, lat]) => lat >= clip[1], intersect: horizontalIntersection(clip[1]) },
            { inside: ([, lat]) => lat <= clip[3], intersect: horizontalIntersection(clip[3]) },
        ];
        return boundaries.reduce(clipPolygonBoundary, [...ring]);
    }
    function unwrapPolygonRing(points) {
        if (points.length === 0)
            return [];
        const output = [points[0]];
        for (let index = 1; index < points.length; index += 1) {
            const point = points[index];
            const previous = output[output.length - 1];
            let longitude = point[0];
            while (longitude - previous[0] > 180)
                longitude -= 360;
            while (longitude - previous[0] < -180)
                longitude += 360;
            appendGeographicPosition(output, [longitude, point[1]]);
        }
        if (output.length > 1 && sameGeographicPosition(output[0], output[output.length - 1]))
            output.pop();
        return output;
    }
    /**
     * Applies projection rotation/wrap and Sutherland-Hodgman clipping to a polygon ring. A ring
     * crossing the antimeridian can return two scene-ready rings, one beside each world edge.
     */
    function clipMapPolygonRing(points, options = {}) {
        const projection = normalizeMapProjection(options);
        const clip = effectiveProjectionClip(projection);
        if (clip === null)
            return [];
        const transformed = points.map((point) => projectionPosition(point, projection.rotate));
        const ring = unwrapPolygonRing(transformed);
        if (ring.length < 3)
            return [];
        const longitudes = ring.map(([lon]) => lon);
        const minimumLongitude = Math.min(...longitudes);
        const maximumLongitude = Math.max(...longitudes);
        const minimumShift = Math.ceil((clip[0] - maximumLongitude) / 360);
        const maximumShift = Math.floor((clip[2] - minimumLongitude) / 360);
        const output = [];
        const keys = new Set();
        for (let shift = minimumShift; shift <= maximumShift; shift += 1) {
            const shifted = ring.map(([lon, lat]) => [lon + shift * 360, lat]);
            const clipped = sutherlandHodgmanRing(shifted, clip);
            if (clipped.length < 3)
                continue;
            const normalized = clipped.map(([lon, lat]) => [projectionSafeLongitude(lon), lat]);
            const key = normalized.map(([lon, lat]) => `${lon.toFixed(9)},${lat.toFixed(9)}`).join(';');
            if (!keys.has(key)) {
                keys.add(key);
                output.push(normalized);
            }
        }
        return output;
    }
    /** Projects longitude/latitude with rotation, clipping and back-face visibility. */
    function projectMapPosition(point, options = {}) {
        const projection = normalizeMapProjection(options);
        let [lon, lat] = projectionPosition(point, projection.rotate);
        const clip = projection.clip;
        const insideClip = clip === undefined || (lon >= clip[0] && lon <= clip[2] && lat >= clip[1] && lat <= clip[3]);
        const name = projection.name;
        if (name === 'mercator') {
            lat = clamp(lat, -85.05112878, 85.05112878);
            return {
                x: (lon + 180) / 360,
                y: (1 - Math.log(Math.tan(Math.PI / 4 + (lat * Math.PI) / 360)) / Math.PI) / 2,
                visible: insideClip,
            };
        }
        if (name === 'orthographic') {
            const lambda = (lon * Math.PI) / 180;
            const phi = (lat * Math.PI) / 180;
            const cosine = Math.cos(phi) * Math.cos(lambda);
            return {
                x: 0.5 + Math.cos(phi) * Math.sin(lambda) * 0.5,
                y: 0.5 - Math.sin(phi) * 0.5,
                visible: insideClip && cosine >= 0,
            };
        }
        return { x: (lon + 180) / 360, y: (90 - lat) / 180, visible: insideClip };
    }
    function geometryPositions(geometry) {
        if (geometry.type === 'GeometryCollection')
            return geometry.geometries.flatMap(geometryPositions);
        const depth = new Map([
            ['Point', 0],
            ['MultiPoint', 1],
            ['LineString', 1],
            ['MultiLineString', 2],
            ['Polygon', 2],
            ['MultiPolygon', 3],
        ]).get(geometry.type);
        const flatten = (value, current) => current === 0
            ? [value]
            : value.flatMap((entry) => flatten(entry, current - 1));
        return flatten(geometry.coordinates, depth);
    }
    /** Computes geographic bounds, choosing the shorter dateline-wrapped span. */
    function mapBounds(collection) {
        const points = collection.features.flatMap(({ geometry }) => geometry === null ? [] : geometryPositions(geometry));
        if (points.length === 0)
            throw new GraflumeError('INVALID_DATA', 'Cannot fit an empty map source.');
        const latitudes = points.map(([, lat]) => lat);
        const longitudes = points.map(([lon]) => wrapLongitude(lon)).sort((a, b) => a - b);
        let largestGap = -1;
        let gapIndex = 0;
        for (let index = 0; index < longitudes.length; index += 1) {
            const current = longitudes[index];
            const next = index + 1 < longitudes.length ? longitudes[index + 1] : longitudes[0] + 360;
            if (next - current > largestGap) {
                largestGap = next - current;
                gapIndex = index;
            }
        }
        const west = longitudes[(gapIndex + 1) % longitudes.length];
        let east = longitudes[gapIndex];
        if (east < west)
            east += 360;
        return { west, south: Math.min(...latitudes), east, north: Math.max(...latitudes) };
    }
    /** Fits bounds into a viewport with padding using an explicit projection. */
    function fitMapBounds(bounds, viewport, padding = 20, projection = 'mercator') {
        const width = finite(viewport.width, '$.viewport.width');
        const height = finite(viewport.height, '$.viewport.height');
        if (width <= padding * 2 || height <= padding * 2)
            throw new GraflumeError('INVALID_SPEC', 'Map viewport must exceed its padding.');
        const west = longitude(bounds.west, '$.bounds.west');
        const east = longitude(bounds.east, '$.bounds.east');
        const south = latitude(bounds.south, '$.bounds.south');
        const north = latitude(bounds.north, '$.bounds.north');
        if (west > east || south > north)
            throw new GraflumeError('INVALID_SPEC', '$.bounds must be ordered.');
        const longitudeSpan = Math.min(360, east - west);
        const centerLon = wrapLongitude((west + east) / 2);
        const centerLat = (south + north) / 2;
        const latitudeProjection = [
            projectMapPosition([0, south], { name: projection }),
            projectMapPosition([0, north], { name: projection }),
        ];
        const spanX = Math.max(1e-9, projection === 'orthographic'
            ? longitudeSpan >= 180
                ? 1
                : Math.abs(projectMapPosition([longitudeSpan / 2, centerLat], { name: projection }).x -
                    projectMapPosition([-longitudeSpan / 2, centerLat], { name: projection }).x)
            : longitudeSpan / 360);
        const spanY = Math.max(1e-9, Math.abs(latitudeProjection[1].y - latitudeProjection[0].y));
        const scale = Math.min((width - padding * 2) / (width * spanX), (height - padding * 2) / (height * spanY));
        return {
            center: [centerLon, centerLat],
            zoom: Math.log2(Math.max(scale, Number.EPSILON)),
            bounds,
        };
    }
    /** Great-circle route interpolation with dateline-safe spherical linear interpolation. */
    function geodesicPath(start, end, segments = 64) {
        const count = Math.floor(finite(segments, '$.segments'));
        if (count < 1 || count > 10_000)
            throw new GraflumeError('INVALID_SPEC', '$.segments must be from 1 to 10000.');
        const vector = ([lon, lat]) => {
            const lambda = (lon * Math.PI) / 180;
            const phi = (lat * Math.PI) / 180;
            return [
                Math.cos(phi) * Math.cos(lambda),
                Math.cos(phi) * Math.sin(lambda),
                Math.sin(phi),
            ];
        };
        const a = vector(start);
        const b = vector(end);
        const omega = Math.acos(clamp(a[0] * b[0] + a[1] * b[1] + a[2] * b[2], -1, 1));
        return Array.from({ length: count + 1 }, (_, index) => {
            const t = index / count;
            const denominator = Math.sin(omega);
            const left = omega < 1e-12 ? 1 - t : Math.sin((1 - t) * omega) / denominator;
            const right = omega < 1e-12 ? t : Math.sin(t * omega) / denominator;
            const x = a[0] * left + b[0] * right;
            const y = a[1] * left + b[1] * right;
            const z = a[2] * left + b[2] * right;
            return [
                wrapLongitude((Math.atan2(y, x) * 180) / Math.PI),
                (Math.atan2(z, Math.hypot(x, y)) * 180) / Math.PI,
            ];
        });
    }
    /** Generates longitude/latitude graticules as ordinary map line features. */
    function mapGraticule(step = [30, 30]) {
        const lonStep = finite(step[0], '$.step[0]');
        const latStep = finite(step[1], '$.step[1]');
        if (lonStep <= 0 || latStep <= 0)
            throw new GraflumeError('INVALID_SPEC', 'Graticule steps must be positive.');
        const features = [];
        for (let lon = -180; lon <= 180; lon += lonStep)
            features.push({
                type: 'Feature',
                properties: { kind: 'longitude', value: lon },
                geometry: {
                    type: 'LineString',
                    coordinates: Array.from({ length: 181 }, (_, index) => [lon, -90 + index]),
                },
            });
        for (let lat = -90 + latStep; lat < 90; lat += latStep)
            features.push({
                type: 'Feature',
                properties: { kind: 'latitude', value: lat },
                geometry: {
                    type: 'LineString',
                    coordinates: Array.from({ length: 361 }, (_, index) => [-180 + index, lat]),
                },
            });
        return { type: 'FeatureCollection', features };
    }
    /** Closed source/layer lifecycle with explicit ordering and visibility. */
    class MapLayerRegistry {
        #sources = new Map();
        #layers = [];
        #revision = 0;
        addSource(id, source) {
            const normalized = id.trim();
            if (normalized === '' || this.#sources.has(normalized))
                throw new GraflumeError('INVALID_SPEC', `Map source "${id}" is empty or already registered.`);
            this.#sources.set(normalized, source);
            this.#revision += 1;
        }
        removeSource(id) {
            if (this.#layers.some(({ source }) => source === id))
                throw new GraflumeError('INVALID_SPEC', `Map source "${id}" is still used by a layer.`);
            if (!this.#sources.delete(id))
                throw new GraflumeError('INVALID_SPEC', `Unknown map source "${id}".`);
            this.#revision += 1;
        }
        addLayer(layer, before) {
            if (this.#layers.some(({ id }) => id === layer.id))
                throw new GraflumeError('INVALID_SPEC', `Duplicate map layer "${layer.id}".`);
            if (!this.#sources.has(layer.source))
                throw new GraflumeError('INVALID_SPEC', `Unknown map source "${layer.source}".`);
            const index = before === undefined
                ? this.#layers.length
                : this.#layers.findIndex(({ id }) => id === before);
            if (index < 0)
                throw new GraflumeError('INVALID_SPEC', `Unknown before-layer "${before}".`);
            this.#layers.splice(index, 0, Object.freeze({ ...layer }));
            this.#revision += 1;
        }
        removeLayer(id) {
            const index = this.#layers.findIndex((layer) => layer.id === id);
            if (index < 0)
                throw new GraflumeError('INVALID_SPEC', `Unknown map layer "${id}".`);
            this.#layers.splice(index, 1);
            this.#revision += 1;
        }
        setVisibility(id, visible) {
            const index = this.#layers.findIndex((layer) => layer.id === id);
            if (index < 0)
                throw new GraflumeError('INVALID_SPEC', `Unknown map layer "${id}".`);
            this.#layers[index] = Object.freeze({ ...this.#layers[index], visible });
            this.#revision += 1;
        }
        source(id) {
            return this.#sources.get(id) ?? null;
        }
        layer(id) {
            return this.#layers.find((layer) => layer.id === id) ?? null;
        }
        snapshot() {
            return Object.freeze({
                revision: this.#revision,
                sources: Object.freeze([...this.#sources.keys()]),
                layers: Object.freeze(this.#layers.map((layer) => Object.freeze({ ...layer }))),
                attributions: Object.freeze([
                    ...new Set(this.#layers.flatMap(({ attribution }) => attribution === undefined ? [] : [attribution])),
                ]),
            });
        }
    }
    function normalizeTile(tile) {
        const z = Math.floor(finite(tile.z, '$.tile.z'));
        const x = Math.floor(finite(tile.x, '$.tile.x'));
        const y = Math.floor(finite(tile.y, '$.tile.y'));
        if (z < 0 || z > 24)
            throw new GraflumeError('INVALID_SPEC', 'Tile zoom must be from 0 to 24.');
        const count = 2 ** z;
        if (y < 0 || y >= count)
            throw new GraflumeError('INVALID_SPEC', 'Tile y is outside the zoom pyramid.');
        return { z, x: ((x % count) + count) % count, y };
    }
    function tileUrl(source, tile) {
        const normalized = normalizeTile(tile);
        if (!source.template.includes('{z}') ||
            !source.template.includes('{x}') ||
            !source.template.includes('{y}'))
            throw new GraflumeError('INVALID_SPEC', 'Tile template must contain {z}, {x}, and {y}.');
        const subdomains = source.subdomains ?? [];
        const subdomain = subdomains.length === 0 ? '' : subdomains[(normalized.x + normalized.y) % subdomains.length];
        return source.template
            .replaceAll('{z}', String(normalized.z))
            .replaceAll('{x}', String(normalized.x))
            .replaceAll('{y}', String(normalized.y))
            .replaceAll('{s}', subdomain);
    }
    /** Provider-backed tile loader with request deduplication, abort, bounded LRU cache, expiry and attribution. */
    class MapTileManager {
        #source;
        #fetcher;
        #maximumEntries;
        #maximumConcurrent;
        #cache = new Map();
        #pending = new Map();
        #controllers = new Map();
        #destroyed = false;
        constructor(source, fetcher, maximumEntries = 128, maximumConcurrent = 8) {
            if (source.attribution.trim() === '')
                throw new GraflumeError('INVALID_SPEC', 'Provider-backed tiles require attribution.');
            this.#source = Object.freeze({ ...source });
            this.#fetcher = fetcher;
            this.#maximumEntries = Math.floor(finite(maximumEntries, '$.maximumEntries'));
            if (this.#maximumEntries < 1 || this.#maximumEntries > 4_096)
                throw new GraflumeError('INVALID_SPEC', '$.maximumEntries must be from 1 to 4096.');
            this.#maximumConcurrent = Math.floor(finite(maximumConcurrent, '$.maximumConcurrent'));
            if (this.#maximumConcurrent < 1 || this.#maximumConcurrent > 64)
                throw new GraflumeError('INVALID_SPEC', '$.maximumConcurrent must be from 1 to 64.');
        }
        get attribution() {
            return this.#source.attribution;
        }
        async load(tile, signal) {
            if (this.#destroyed)
                throw new GraflumeError('INVALID_DATA', 'Map tile manager has been destroyed.');
            const url = tileUrl(this.#source, tile);
            const cached = this.#cache.get(url);
            if (cached !== undefined && (cached.expiresAt === undefined || cached.expiresAt > Date.now())) {
                this.#cache.delete(url);
                this.#cache.set(url, cached);
                return cached;
            }
            this.#cache.delete(url);
            const existing = this.#pending.get(url);
            if (existing !== undefined)
                return existing;
            const controller = new AbortController();
            const abort = () => controller.abort(signal?.reason);
            if (signal?.aborted === true)
                controller.abort(signal.reason);
            else
                signal?.addEventListener('abort', abort, { once: true });
            const request = this.#fetcher(url, controller.signal)
                .then((response) => {
                if (!(response.bytes instanceof Uint8Array) ||
                    response.bytes.length === 0 ||
                    response.mimeType.trim() === '')
                    throw new GraflumeError('INVALID_DATA', 'Tile provider returned an invalid response.');
                const frozen = Object.freeze({ ...response, bytes: response.bytes.slice() });
                this.#cache.set(url, frozen);
                while (this.#cache.size > this.#maximumEntries)
                    this.#cache.delete(this.#cache.keys().next().value);
                return frozen;
            })
                .finally(() => {
                signal?.removeEventListener('abort', abort);
                this.#pending.delete(url);
                this.#controllers.delete(url);
            });
            this.#pending.set(url, request);
            this.#controllers.set(url, controller);
            return request;
        }
        /** Loads a bounded tile set with stable output order and explicit concurrency. */
        async loadMany(tiles, signal) {
            if (tiles.length > this.#maximumEntries * 4)
                throw new GraflumeError('INVALID_SPEC', `Tile batch has ${tiles.length} entries; the bounded limit is ${this.#maximumEntries * 4}.`);
            const output = new Array(tiles.length);
            let cursor = 0;
            const worker = async () => {
                while (cursor < tiles.length) {
                    const index = cursor;
                    cursor += 1;
                    output[index] = await this.load(tiles[index], signal);
                }
            };
            await Promise.all(Array.from({ length: Math.min(this.#maximumConcurrent, tiles.length) }, () => worker()));
            return Object.freeze(output);
        }
        clear() {
            this.#cache.clear();
        }
        /** Aborts every provider request and permanently releases this manager. */
        destroy(reason = new DOMException('Map tile manager destroyed.', 'AbortError')) {
            if (this.#destroyed)
                return;
            this.#destroyed = true;
            for (const controller of this.#controllers.values())
                controller.abort(reason);
            this.#controllers.clear();
            this.#pending.clear();
            this.#cache.clear();
        }
        state() {
            return Object.freeze({
                cached: this.#cache.size,
                pending: this.#pending.size,
                maximumEntries: this.#maximumEntries,
                maximumConcurrent: this.#maximumConcurrent,
                destroyed: this.#destroyed,
            });
        }
    }
    /** Default browser/server fetch adapter with cache expiry derived from response headers. */
    const fetchMapTile = async (url, signal) => {
        if (typeof fetch !== 'function')
            throw new GraflumeError('UNSUPPORTED_RENDERER', 'Provider-backed tiles require fetch or an injected TileFetcher.');
        const response = await fetch(url, { signal });
        if (!response.ok)
            throw new GraflumeError('INVALID_DATA', `Tile provider returned HTTP ${response.status}.`);
        const bytes = new Uint8Array(await response.arrayBuffer());
        const mimeType = response.headers.get('content-type')?.split(';', 1)[0]?.trim() ?? '';
        const cacheControl = response.headers.get('cache-control') ?? '';
        const maxAge = /(?:^|,)\s*max-age=(\d+)/i.exec(cacheControl)?.[1];
        return {
            bytes,
            mimeType,
            ...(maxAge === undefined ? {} : { expiresAt: Date.now() + Number(maxAge) * 1_000 }),
        };
    };
    function createMapTileManager(source, options = {}) {
        return new MapTileManager(source, options.fetcher ?? fetchMapTile, options.maximumEntries, options.maximumConcurrent);
    }
    function cloneMapProjection(projection) {
        return Object.freeze({
            name: projection.name,
            ...(projection.rotate === undefined
                ? {}
                : { rotate: Object.freeze([...projection.rotate]) }),
            ...(projection.clip === undefined
                ? {}
                : {
                    clip: Object.freeze([...projection.clip]),
                }),
        });
    }
    /**
     * Persistent source/layer runtime that binds ordered layer state to real
     * provider requests. It is renderer-neutral and can be shared by custom map
     * controls, server prefetchers, or a Canvas chart lifecycle.
     */
    class MapRuntime {
        layers = new MapLayerRegistry();
        #options;
        #managers = new Map();
        #projection;
        #revision = 0;
        #destroyed = false;
        constructor(options = {}) {
            const { projection, ...tileOptions } = options;
            this.#options = { ...tileOptions };
            this.#projection = normalizeMapProjection(projection);
        }
        addSource(id, source) {
            this.#assertAlive();
            this.layers.addSource(id, source);
            this.#revision += 1;
            return this;
        }
        removeSource(id) {
            this.#assertAlive();
            this.layers.removeSource(id);
            this.#managers.get(id)?.destroy();
            this.#managers.delete(id);
            this.#revision += 1;
            return this;
        }
        addLayer(layer, before) {
            this.#assertAlive();
            this.layers.addLayer(layer, before);
            this.#revision += 1;
            return this;
        }
        removeLayer(id) {
            this.#assertAlive();
            this.layers.removeLayer(id);
            this.#revision += 1;
            return this;
        }
        setVisibility(id, visible) {
            this.#assertAlive();
            this.layers.setVisibility(id, visible);
            this.#revision += 1;
            return this;
        }
        getProjection() {
            return cloneMapProjection(this.#projection);
        }
        setProjection(projection) {
            this.#assertAlive();
            const normalized = normalizeMapProjection(projection);
            if (JSON.stringify(normalized) !== JSON.stringify(this.#projection)) {
                this.#projection = normalized;
                this.#revision += 1;
            }
            return this;
        }
        project(point) {
            this.#assertAlive();
            return projectMapPosition(point, this.#projection);
        }
        async loadLayerTiles(layerId, tiles, signal) {
            this.#assertAlive();
            const layer = this.layers.layer(layerId);
            if (layer === null)
                throw new GraflumeError('INVALID_SPEC', `Unknown map layer "${layerId}".`);
            if (layer.visible === false)
                return Object.freeze([]);
            const source = this.layers.source(layer.source);
            if (source === null || !('template' in source))
                throw new GraflumeError('INVALID_SPEC', `Map layer "${layerId}" is not backed by a tile provider.`);
            let manager = this.#managers.get(layer.source);
            if (manager === undefined) {
                manager = createMapTileManager(source, this.#options);
                this.#managers.set(layer.source, manager);
            }
            return manager.loadMany(tiles, signal);
        }
        snapshot() {
            return Object.freeze({
                destroyed: this.#destroyed,
                revision: this.#revision,
                projection: cloneMapProjection(this.#projection),
                registry: this.layers.snapshot(),
                providers: Object.freeze(Object.fromEntries([...this.#managers].map(([id, manager]) => [id, manager.state()]))),
            });
        }
        destroy(reason) {
            if (this.#destroyed)
                return;
            this.#destroyed = true;
            for (const manager of this.#managers.values())
                manager.destroy(reason);
            this.#managers.clear();
        }
        #assertAlive() {
            if (this.#destroyed)
                throw new GraflumeError('INVALID_DATA', 'Map runtime has been destroyed.');
        }
    }
    function createMapRuntime(options = {}) {
        return new MapRuntime(options);
    }

    function stringOption$1(context, name) {
        const value = context.layer.mark.options[name];
        return typeof value === 'string' ? value : undefined;
    }
    function numberOption$1(context, name) {
        const value = context.layer.mark.options[name];
        return typeof value === 'number' && Number.isFinite(value) ? value : undefined;
    }
    function booleanOption$1(context, name) {
        const value = context.layer.mark.options[name];
        return typeof value === 'boolean' ? value : undefined;
    }
    function numberArray(value, length) {
        return Array.isArray(value) &&
            value.length === length &&
            value.every((entry) => typeof entry === 'number' && Number.isFinite(entry))
            ? [...value]
            : undefined;
    }
    function sourceCollection(context) {
        const geojson = context.layer.mark.options.geojson;
        if (geojson !== undefined)
            return normalizeGeoJson(geojson);
        const topology = context.layer.mark.options.topojson;
        if (topology !== undefined) {
            const object = stringOption$1(context, 'topologyObject');
            return topologyToGeoJson(topology, object);
        }
        return null;
    }
    function projectionName(context) {
        const value = stringOption$1(context, 'projection');
        return value === 'mercator' || value === 'orthographic' ? value : 'equirectangular';
    }
    function textNode$1(context, id, x, y, text, align = 'left') {
        return {
            type: 'text',
            ...nodeBase(id, { zIndex: context.layer.zIndex + 10 }),
            x,
            y,
            text,
            fill: context.theme.colors.mutedText,
            fontFamily: context.theme.typography.fontFamily,
            fontSize: 9,
            fontWeight: 500,
            align,
            baseline: 'bottom',
            rotation: 0,
        };
    }
    function geometryPaths(geometry) {
        if (geometry.type === 'GeometryCollection')
            return geometry.geometries.flatMap(geometryPaths);
        if (geometry.type === 'Point')
            return [{ kind: 'point', points: [geometry.coordinates] }];
        if (geometry.type === 'MultiPoint')
            return geometry.coordinates.map((point) => ({
                kind: 'point',
                points: [point],
            }));
        if (geometry.type === 'LineString')
            return [{ kind: 'line', points: geometry.coordinates }];
        if (geometry.type === 'MultiLineString')
            return geometry.coordinates.map((points) => ({
                kind: 'line',
                points,
            }));
        const polygons = geometry.type === 'Polygon'
            ? [geometry.coordinates]
            : geometry.coordinates;
        return polygons.flatMap((rings) => {
            const outer = rings[0];
            return outer === undefined ? [] : [{ kind: 'polygon', outer, holes: rings.slice(1) }];
        });
    }
    function tileSourceOption(context) {
        const value = context.layer.mark.options.tileSource;
        if (value === null || typeof value !== 'object' || Array.isArray(value))
            return null;
        const template = Reflect.get(value, 'template');
        const attribution = Reflect.get(value, 'attribution');
        if (typeof template !== 'string' || typeof attribution !== 'string')
            return null;
        const type = Reflect.get(value, 'type');
        const subdomains = Reflect.get(value, 'subdomains');
        const tileSize = Reflect.get(value, 'tileSize');
        return {
            type: type === 'vector' ? 'vector' : 'raster',
            template,
            attribution,
            ...(Array.isArray(subdomains) && subdomains.every((entry) => typeof entry === 'string')
                ? { subdomains: [...subdomains] }
                : {}),
            ...(tileSize === 512 ? { tileSize } : tileSize === 256 ? { tileSize } : {}),
        };
    }
    function tileCoordinates(context) {
        const value = context.layer.mark.options.tiles;
        if (!Array.isArray(value))
            return [];
        return value.flatMap((entry) => {
            if (entry === null || typeof entry !== 'object' || Array.isArray(entry))
                return [];
            const z = Reflect.get(entry, 'z');
            const x = Reflect.get(entry, 'x');
            const y = Reflect.get(entry, 'y');
            return [z, x, y].every((part) => typeof part === 'number' && Number.isInteger(part))
                ? [{ z: z, x: x, y: y }]
                : [];
        });
    }
    function propertyValue(feature, field) {
        return field === undefined || feature.properties === null || feature.properties === undefined
            ? undefined
            : feature.properties[field];
    }
    function densifyGeodesicLine(points, segments) {
        if (points.length < 2)
            return points;
        return points.slice(1).flatMap((point, index) => {
            const segment = geodesicPath(points[index], point, segments);
            return index === 0 ? segment : segment.slice(1);
        });
    }
    /** GeoJSON/TopoJSON map compiler with lifecycle, fit/clip/projection, geodesics, and provider tiles. */
    const compileAdvancedMapMark = (context) => {
        const collection = sourceCollection(context);
        const tileSource = tileSourceOption(context);
        if (collection === null && tileSource === null)
            return compileMapMark(context);
        const registry = new MapLayerRegistry();
        if (collection !== null)
            registry.addSource('features', collection);
        if (tileSource !== null)
            registry.addSource('provider', tileSource);
        if (tileSource !== null)
            registry.addLayer({
                id: 'provider',
                source: 'provider',
                type: 'raster',
                attribution: tileSource.attribution,
            });
        const attribution = stringOption$1(context, 'attribution');
        if (collection !== null)
            registry.addLayer({
                id: 'features',
                source: 'features',
                type: 'fill',
                visible: true,
                ...(attribution === undefined ? {} : { attribution }),
            });
        const lifecycle = registry.snapshot();
        const projection = projectionName(context);
        const rotate = numberArray(context.layer.mark.options.rotate, 2);
        const clip = numberArray(context.layer.mark.options.clip, 4);
        const rotation = rotate === undefined ? undefined : [rotate[0], rotate[1]];
        const geographicClip = clip === undefined ? undefined : [clip[0], clip[1], clip[2], clip[3]];
        const projectionOptions = {
            name: projection,
            ...(rotation === undefined ? {} : { rotate: rotation }),
            ...(geographicClip === undefined ? {} : { clip: geographicClip }),
        };
        const preparedProjectionOptions = {
            name: projection,
            ...(geographicClip === undefined ? {} : { clip: geographicClip }),
        };
        const padding = numberOption$1(context, 'fitPadding') ?? 18;
        const fitEnabled = booleanOption$1(context, 'fit') !== false;
        const fitted = collection === null || !fitEnabled
            ? null
            : fitMapBounds(mapBounds(collection), { width: context.plot.width, height: context.plot.height }, padding, projection);
        const centerProjected = projectMapPosition(fitted?.center ?? [0, 0], projectionOptions);
        const zoom = fitEnabled ? 2 ** (fitted?.zoom ?? 0) : 1;
        const mapProjected = (projected) => {
            if (!projected.visible)
                return null;
            let horizontalOffset = projected.x - centerProjected.x;
            if (projection !== 'orthographic') {
                if (horizontalOffset > 0.5)
                    horizontalOffset -= 1;
                else if (horizontalOffset < -0.5)
                    horizontalOffset += 1;
            }
            return {
                x: context.plot.x + context.plot.width / 2 + horizontalOffset * context.plot.width * zoom,
                y: context.plot.y +
                    context.plot.height / 2 +
                    (projected.y - centerProjected.y) * context.plot.height * zoom,
            };
        };
        const map = (position) => mapProjected(projectMapPosition(position, projectionOptions));
        const mapPrepared = (position) => mapProjected(projectMapPosition(position, preparedProjectionOptions));
        const projectOpenPaths = (paths) => {
            const output = [];
            for (const path of paths) {
                let current = [];
                const flush = () => {
                    if (current.length > 1)
                        output.push(current);
                    current = [];
                };
                for (const position of path) {
                    const point = mapPrepared(position);
                    if (point === null)
                        flush();
                    else
                        current.push(point);
                }
                flush();
            }
            return output;
        };
        const projectClosedPaths = (paths) => paths.flatMap((path) => {
            const projected = path.flatMap((position) => {
                const point = mapPrepared(position);
                return point === null ? [] : [point];
            });
            return projected.length < 3 ? [] : [projected];
        });
        const nodes = [];
        if (booleanOption$1(context, 'graticule') === true) {
            const step = numberArray(context.layer.mark.options.graticuleStep, 2) ?? [30, 30];
            mapGraticule([step[0], step[1]]).features.forEach((feature, index) => {
                if (feature.geometry === null)
                    return;
                geometryPaths(feature.geometry).forEach((path, pathIndex) => {
                    if (path.kind !== 'line')
                        return;
                    const projectedPaths = projectOpenPaths(clipMapLine(path.points, projectionOptions));
                    const points = projectedPaths[0];
                    if (points === undefined)
                        return;
                    nodes.push({
                        type: 'path',
                        ...nodeBase(`${context.layer.id}:graticule:${index}:${pathIndex}`, {
                            zIndex: context.layer.zIndex - 4,
                            opacity: 0.5,
                        }),
                        points,
                        ...(projectedPaths.length > 1 ? { subpaths: projectedPaths.slice(1) } : {}),
                        closed: false,
                        stroke: context.theme.colors.axis,
                        lineWidth: 0.6,
                        dash: [3, 3],
                    });
                });
            });
        }
        if (tileSource !== null) {
            const tiles = tileCoordinates(context);
            const count = Math.max(1, Math.ceil(Math.sqrt(tiles.length)));
            tiles.forEach((tile, index) => {
                const column = index % count;
                const row = Math.floor(index / count);
                const width = context.plot.width / count;
                const height = context.plot.height / Math.max(1, Math.ceil(tiles.length / count));
                const url = tileUrl(tileSource, tile);
                nodes.push({
                    type: 'rect',
                    ...nodeBase(`${context.layer.id}:provider-tile:${tile.z}:${tile.x}:${tile.y}`, {
                        zIndex: context.layer.zIndex - 3,
                        opacity: 1,
                        interactive: context.performance.enableHitTesting,
                        datum: {
                            layerId: context.layer.id,
                            rowIndex: index,
                            datum: { z: tile.z, x: tile.x, y: tile.y, url, attribution: tileSource.attribution },
                            tooltip: {
                                provider: tileSource.type,
                                z: tile.z,
                                x: tile.x,
                                y: tile.y,
                                url,
                                attribution: tileSource.attribution,
                                lifecycle: 'provider request',
                            },
                        },
                    }),
                    x: context.plot.x + column * width,
                    y: context.plot.y + row * height,
                    width,
                    height,
                    providerTile: { source: tileSource, tile },
                    fill: context.theme.colors.surface,
                    stroke: context.theme.colors.axis,
                    lineWidth: 0.5,
                    cornerRadius: 0,
                });
            });
        }
        const colorField = context.layer.mark.fields.color;
        collection?.features.forEach((feature, featureIndex) => {
            if (feature.geometry === null)
                return;
            const value = propertyValue(feature, colorField);
            const colorIndex = typeof value === 'number' ? Math.abs(Math.floor(value)) : featureIndex;
            const color = context.layer.mark.fill ??
                categoricalColor(context.theme, colorIndex, collection.features.length);
            geometryPaths(feature.geometry).forEach((path, pathIndex) => {
                const geodesicApplied = booleanOption$1(context, 'geodesic') === true &&
                    path.kind === 'line' &&
                    path.points.length >= 2;
                const sourcePoints = geodesicApplied
                    ? densifyGeodesicLine(path.points, numberOption$1(context, 'geodesicSegments') ?? 64)
                    : path.kind === 'polygon'
                        ? path.outer
                        : path.points;
                const datum = {
                    layerId: context.layer.id,
                    rowIndex: featureIndex,
                    datum: {
                        featureIndex,
                        geometry: feature.geometry?.type ?? 'null',
                        properties: JSON.stringify(feature.properties),
                    },
                    tooltip: {
                        featureIndex,
                        geometry: feature.geometry?.type ?? 'null',
                        properties: JSON.stringify(feature.properties),
                        projection,
                        fitCenter: fitted?.center.join(', ') ?? 'none',
                        fitZoom: fitted?.zoom ?? 0,
                        clipped: clip !== undefined,
                        geodesic: geodesicApplied,
                        source: 'features',
                        layer: 'features',
                        holes: path.kind === 'polygon' ? path.holes.length : 0,
                    },
                };
                if (path.kind === 'point') {
                    const projected = map(sourcePoints[0]);
                    if (projected === null)
                        return;
                    nodes.push({
                        type: 'circle',
                        ...nodeBase(`${context.layer.id}:map-feature:${featureIndex}:${pathIndex}`, {
                            zIndex: context.layer.zIndex + 2,
                            opacity: context.layer.mark.opacity,
                            interactive: context.performance.enableHitTesting,
                            datum,
                        }),
                        cx: projected.x,
                        cy: projected.y,
                        radius: context.layer.mark.radius ?? 5,
                        fill: color,
                        stroke: context.theme.colors.background,
                        lineWidth: 1,
                    });
                }
                else if (path.kind === 'line') {
                    const projectedPaths = projectOpenPaths(clipMapLine(sourcePoints, projectionOptions));
                    const projected = projectedPaths[0];
                    if (projected === undefined)
                        return;
                    nodes.push({
                        type: 'path',
                        ...nodeBase(`${context.layer.id}:map-feature:${featureIndex}:${pathIndex}`, {
                            zIndex: context.layer.zIndex + 1,
                            opacity: context.layer.mark.opacity,
                            interactive: context.performance.enableHitTesting,
                            datum,
                        }),
                        points: projected,
                        ...(projectedPaths.length > 1 ? { subpaths: projectedPaths.slice(1) } : {}),
                        closed: false,
                        stroke: context.layer.mark.stroke ?? color,
                        lineWidth: context.layer.mark.lineWidth ?? 1.8,
                        lineCap: 'round',
                        lineJoin: 'round',
                    });
                }
                else {
                    const outerPaths = projectClosedPaths(clipMapPolygonRing(sourcePoints, projectionOptions));
                    const projected = outerPaths[0];
                    if (projected === undefined)
                        return;
                    const holePaths = path.holes.flatMap((hole) => projectClosedPaths(clipMapPolygonRing(hole, projectionOptions)));
                    const subpaths = [...outerPaths.slice(1), ...holePaths];
                    nodes.push({
                        type: 'path',
                        ...nodeBase(`${context.layer.id}:map-feature:${featureIndex}:${pathIndex}`, {
                            zIndex: context.layer.zIndex + 1,
                            opacity: context.layer.mark.opacity,
                            interactive: context.performance.enableHitTesting,
                            datum: {
                                ...datum,
                                datum: { ...datum.datum, renderedRings: 1 + subpaths.length },
                                tooltip: { ...datum.tooltip, renderedRings: 1 + subpaths.length },
                            },
                        }),
                        points: projected,
                        ...(subpaths.length > 0 ? { subpaths } : {}),
                        closed: true,
                        fill: colorWithOpacity(color, 0.55),
                        fillRule: 'evenodd',
                        stroke: context.layer.mark.stroke ?? color,
                        lineWidth: context.layer.mark.lineWidth ?? 1,
                        lineCap: 'round',
                        lineJoin: 'round',
                    });
                }
            });
        });
        lifecycle.attributions.forEach((attribution, index) => nodes.push(textNode$1(context, `${context.layer.id}:map-attribution:${index}`, context.plot.x + context.plot.width - 4, context.plot.y + context.plot.height - 3 - index * 11, attribution, 'right')));
        return nodes;
    };

    function stringOption(context, name) {
        const value = context.layer.mark.options[name];
        return typeof value === 'string' ? value : undefined;
    }
    function numberOption(context, name) {
        const value = context.layer.mark.options[name];
        return typeof value === 'number' && Number.isFinite(value) ? value : undefined;
    }
    function booleanOption(context, name) {
        const value = context.layer.mark.options[name];
        return typeof value === 'boolean' ? value : undefined;
    }
    function numberArrayOption(context, name) {
        const value = context.layer.mark.options[name];
        return Array.isArray(value) &&
            value.every((entry) => typeof entry === 'number' && Number.isFinite(entry))
            ? [...value]
            : undefined;
    }
    function stringArrayOption(context, name) {
        const value = context.layer.mark.options[name];
        return Array.isArray(value) && value.every((entry) => typeof entry === 'string')
            ? [...value]
            : undefined;
    }
    function rows(context) {
        return Array.from({ length: context.table.length }, (_, rowIndex) => context.table.row(rowIndex));
    }
    function textNode(context, id, x, y, text, options = {}) {
        return {
            type: 'text',
            ...nodeBase(id, { zIndex: context.layer.zIndex + 4 }),
            x,
            y,
            text,
            fill: options.fill ?? context.theme.colors.text,
            fontFamily: context.theme.typography.fontFamily,
            fontSize: options.size ?? context.theme.typography.fontSize,
            fontWeight: options.weight ?? 600,
            align: options.align ?? 'center',
            baseline: 'middle',
            rotation: 0,
        };
    }
    /** Bar compiler with exact weighted counts, deterministic ranking, and rank-change provenance. */
    const compileRankedBarMark = (context) => {
        const aggregate = stringOption(context, 'aggregate');
        const ranked = booleanOption(context, 'rank') === true || aggregate !== undefined;
        if (!ranked)
            return compileBarMark(context);
        const categoryField = context.layer.mark.fields.category ?? context.layer.x.field;
        const valueField = context.layer.mark.fields.value ?? context.layer.y.field;
        const weightField = context.layer.mark.fields.weight ?? 'weight';
        const idField = context.layer.mark.fields.id ?? 'id';
        const previous = context.layer.mark.options.previousRanks;
        const previousRanks = previous !== null && typeof previous === 'object' && !Array.isArray(previous)
            ? Object.fromEntries(Object.entries(previous).flatMap(([key, value]) => typeof value === 'number' && Number.isFinite(value) ? [[key, value]] : []))
            : undefined;
        const data = Array.from({ length: context.table.length }, (_, rowIndex) => {
            const rawId = context.table.has(idField) ? context.table.value(rowIndex, idField) : undefined;
            const rawCategory = context.table.value(rowIndex, categoryField);
            return {
                id: rawId === null || rawId === undefined ? `row-${rowIndex}` : String(rawId),
                category: rawCategory === null || rawCategory === undefined ? `row-${rowIndex}` : String(rawCategory),
                value: numericDataValue(context.table.value(rowIndex, valueField)) ?? 0,
                weight: context.table.has(weightField)
                    ? (numericDataValue(context.table.value(rowIndex, weightField)) ?? 0)
                    : 1,
            };
        });
        const direction = stringOption(context, 'sortDirection') === 'ascending' ? 'ascending' : 'descending';
        const rankedRows = rankBars(data, {
            aggregate: aggregate === 'count' || aggregate === 'weighted-count' || aggregate === 'value'
                ? aggregate
                : 'value',
            ...(previousRanks === undefined ? {} : { previousRanks }),
            direction,
        });
        const { plot, layer, theme, performance } = context;
        const maximum = Math.max(0, ...rankedRows.map(({ value }) => value));
        const minimum = Math.min(0, ...rankedRows.map(({ value }) => value));
        const span = Math.max(Number.EPSILON, maximum - minimum);
        const limit = Math.min(rankedRows.length, context.performance.maxBarMarks);
        const displayed = rankedRows.slice(0, limit);
        const nodes = [];
        displayed.forEach((row, index) => {
            const color = layer.mark.fill ?? categoricalColor(theme, index, displayed.length);
            const ratio = (row.value - minimum) / span;
            const base = nodeBase(`${layer.id}:ranked-bar:${row.id}`, {
                zIndex: layer.zIndex,
                opacity: layer.mark.opacity,
                interactive: performance.enableHitTesting,
                datum: {
                    layerId: layer.id,
                    rowIndex: index,
                    datum: {
                        id: row.id,
                        rank: row.rank,
                        previousRank: row.previousRank,
                        rankChange: row.rankChange,
                        value: row.value,
                        sourceIds: [...row.sourceIds],
                    },
                    tooltip: {
                        category: row.id,
                        value: row.value,
                        rank: row.rank,
                        previousRank: row.previousRank,
                        rankChange: row.rankChange,
                        sourceIds: row.sourceIds.join(', '),
                    },
                },
            });
            if (layer.mark.orientation === 'horizontal') {
                const slot = plot.height / Math.max(1, displayed.length);
                const zero = plot.x + ((0 - minimum) / span) * plot.width;
                const end = plot.x + ratio * plot.width;
                nodes.push({
                    type: 'rect',
                    ...base,
                    x: Math.min(zero, end),
                    y: plot.y + index * slot + slot * 0.12,
                    width: Math.max(0.5, Math.abs(end - zero)),
                    height: Math.max(1, slot * 0.76),
                    fill: color,
                    lineWidth: 0,
                    cornerRadius: layer.mark.cornerRadius ?? theme.mark.barRadius,
                });
                nodes.push(textNode(context, `${layer.id}:rank-label:${row.id}`, plot.x + 4, plot.y + (index + 0.5) * slot, `#${row.rank} ${row.id}`, {
                    align: 'left',
                    size: Math.min(12, slot * 0.36),
                    fill: readableTextColor(color, '#ffffff', '#0f172a'),
                }));
            }
            else {
                const slot = plot.width / Math.max(1, displayed.length);
                const zero = plot.y + plot.height - ((0 - minimum) / span) * plot.height;
                const end = plot.y + plot.height - ratio * plot.height;
                nodes.push({
                    type: 'rect',
                    ...base,
                    x: plot.x + index * slot + slot * 0.12,
                    y: Math.min(zero, end),
                    width: Math.max(1, slot * 0.76),
                    height: Math.max(0.5, Math.abs(end - zero)),
                    fill: color,
                    lineWidth: 0,
                    cornerRadius: layer.mark.cornerRadius ?? theme.mark.barRadius,
                });
                nodes.push(textNode(context, `${layer.id}:rank-label:${row.id}`, plot.x + (index + 0.5) * slot, Math.min(zero, end) - 8, `#${row.rank}`, {
                    size: Math.min(12, slot * 0.28),
                }));
            }
        });
        return nodes;
    };
    /** Bubble compiler with absolute-area scaling, explicit zero/negative policy, and a shared size guide. */
    const compileAreaBubbleMark = (context) => {
        const sizeField = context.layer.mark.fields.size;
        if (sizeField === undefined || !context.table.has(sizeField))
            return compileBubbleMark(context);
        const raw = Array.from({ length: context.table.length }, (_, rowIndex) => ({
            rowIndex,
            value: numericDataValue(context.table.value(rowIndex, sizeField)),
        })).filter((entry) => entry.value !== null);
        const negative = stringOption(context, 'negativeSize');
        const zero = stringOption(context, 'zeroSize');
        const values = raw.flatMap(({ value }) => value < 0 && negative === 'hide'
            ? []
            : [value < 0 && negative === 'absolute' ? Math.abs(value) : value]);
        const domain = values.length === 0
            ? [0, 1]
            : [Math.max(0, Math.min(...values)), Math.max(0, Math.max(...values))];
        const radiusOptions = {
            minRadius: numberOption(context, 'minRadius') ?? context.layer.mark.radius ?? 3,
            maxRadius: numberOption(context, 'maxRadius') ?? 26,
            zero: zero === 'hide' || zero === 'zero' ? zero : 'minimum',
            negative: negative === 'absolute' || negative === 'hide' ? negative : 'reject',
        };
        const nodes = [];
        raw.forEach(({ rowIndex, value }) => {
            const xInput = scaleInput(context.table.value(rowIndex, context.layer.x.field));
            const yInput = scaleInput(context.table.value(rowIndex, context.layer.y.field));
            if (xInput === null || yInput === null)
                return;
            const radius = areaRadius(value, domain, radiusOptions);
            if (radius === null)
                return;
            const cx = context.xScale.map(xInput);
            const cy = context.yScale.map(yInput);
            if (!Number.isFinite(cx) || !Number.isFinite(cy))
                return;
            nodes.push({
                type: 'circle',
                ...nodeBase(`${context.layer.id}:bubble:${rowIndex}`, {
                    zIndex: context.layer.zIndex,
                    opacity: context.layer.mark.opacity,
                    interactive: context.performance.enableHitTesting,
                    datum: {
                        layerId: context.layer.id,
                        rowIndex,
                        datum: context.table.row(rowIndex),
                        tooltip: {
                            ...context.table.row(rowIndex),
                            sizeValue: value,
                            radius,
                            area: Math.PI * radius * radius,
                        },
                    },
                }),
                cx,
                cy,
                radius,
                fill: context.layer.mark.fill ?? colorWithOpacity(context.color, 0.72),
                stroke: context.layer.mark.stroke ?? context.color,
                lineWidth: context.layer.mark.lineWidth ?? 1.5,
            });
        });
        if (booleanOption(context, 'sizeGuide') !== false) {
            const guide = areaSizeGuide(domain, {
                ...radiusOptions,
                count: numberOption(context, 'sizeGuideCount') ?? 3,
            });
            const right = context.plot.x + context.plot.width - 8;
            let cursor = context.plot.y + 10;
            guide
                .slice()
                .reverse()
                .forEach((entry, index) => {
                cursor += entry.radius;
                nodes.push({
                    type: 'circle',
                    ...nodeBase(`${context.layer.id}:size-guide:${index}`, {
                        zIndex: context.layer.zIndex + 3,
                    }),
                    cx: right - entry.radius,
                    cy: cursor,
                    radius: entry.radius,
                    fill: colorWithOpacity(context.theme.colors.background, 0.64),
                    stroke: context.theme.colors.mutedText,
                    lineWidth: 1,
                });
                nodes.push(textNode(context, `${context.layer.id}:size-guide-label:${index}`, right - entry.radius * 2 - 5, cursor, String(Number(entry.value.toPrecision(4))), { align: 'right', size: 10 }));
                cursor += entry.radius + 5;
            });
        }
        return nodes;
    };
    /** Calendar compiler with year/month/week/day ranges, locale week starts, zones, and missing semantics. */
    const compileAdvancedCalendarMark = (context) => {
        const modeValue = stringOption(context, 'mode');
        const missingValue = stringOption(context, 'missing');
        const mode = modeValue === 'month' || modeValue === 'week' || modeValue === 'day' ? modeValue : 'year';
        const missing = missingValue === 'zero' || missingValue === 'explicit' ? missingValue : 'gap';
        const dateField = context.layer.mark.fields.date ?? context.layer.x.field;
        const valueField = context.layer.mark.fields.value ?? context.layer.y.field;
        const explicitWeekStart = numberOption(context, 'weekStart');
        const timeZone = stringOption(context, 'timeZone') ?? 'UTC';
        const calendarLocale = {
            ...(context.locale === undefined ? {} : { locale: context.locale }),
            ...(explicitWeekStart === undefined ? {} : { weekStart: explicitWeekStart }),
            timeZone,
        };
        let cells;
        let weekStart;
        let weekdayLabels;
        try {
            weekStart = calendarWeekStart(calendarLocale);
            weekdayLabels = calendarWeekdayLabels(calendarLocale);
            cells = calendarCells(rows(context), {
                dateField,
                valueField,
                mode,
                ...calendarLocale,
                missing,
                ...(context.layer.mark.options.start === undefined
                    ? {}
                    : { start: context.layer.mark.options.start }),
                ...(context.layer.mark.options.end === undefined
                    ? {}
                    : { end: context.layer.mark.options.end }),
            });
        }
        catch (error) {
            if (booleanOption(context, 'strictCalendar') === true ||
                context.locale !== undefined ||
                explicitWeekStart !== undefined ||
                context.layer.mark.options.timeZone !== undefined) {
                throw error;
            }
            return compileCalendarMark(context);
        }
        if (cells.length === 0)
            return [];
        const valueExtent = cells.flatMap(({ value }) => (value === null ? [] : [value]));
        const minimum = Math.min(...valueExtent, 0);
        const maximum = Math.max(...valueExtent, 1);
        const columns = Math.max(1, Math.max(...cells.map(({ weekIndex }) => weekIndex)) + 1);
        const labelFontSize = Math.max(8, Math.min(11, context.theme.typography.fontSize * 0.8, context.plot.height / 10));
        const longestLabel = Math.max(...weekdayLabels.map(({ label }) => Array.from(label).length));
        const desiredHeaderWidth = longestLabel * labelFontSize * 0.62 + 9;
        const headerWidth = Math.min(desiredHeaderWidth, Math.max(0, context.plot.width - columns * 0.5));
        const gridX = context.plot.x + headerWidth;
        const gridWidth = Math.max(0.5, context.plot.width - headerWidth);
        const cellWidth = gridWidth / columns;
        const cellHeight = context.plot.height / 7;
        const nodes = weekdayLabels.map(({ weekday, label }, row) => textNode(context, `${context.layer.id}:calendar-weekday:${weekday}`, context.plot.x + Math.max(4, headerWidth - 4), context.plot.y + (row + 0.5) * cellHeight, label, {
            align: 'right',
            size: labelFontSize,
            fill: context.theme.colors.mutedText,
            weight: 600,
        }));
        cells.forEach((cell, index) => {
            const row = (((cell.weekday - weekStart) % 7) + 7) % 7;
            const ratio = cell.value === null
                ? 0
                : (cell.value - minimum) / Math.max(Number.EPSILON, maximum - minimum);
            const fill = cell.value === null
                ? missing === 'explicit'
                    ? colorWithOpacity(context.theme.colors.mutedText, 0.2)
                    : context.theme.colors.surface
                : cell.value === 0
                    ? colorWithOpacity(context.color, 0.14)
                    : colorWithOpacity(context.color, 0.2 + ratio * 0.75);
            const x = gridX + cell.weekIndex * cellWidth + 0.7;
            const y = context.plot.y + row * cellHeight + 0.7;
            nodes.push({
                type: 'rect',
                ...nodeBase(`${context.layer.id}:calendar:${cell.key}`, {
                    zIndex: context.layer.zIndex,
                    opacity: context.layer.mark.opacity,
                    interactive: context.performance.enableHitTesting,
                    datum: {
                        layerId: context.layer.id,
                        rowIndex: cell.sourceRows[0] ?? index,
                        datum: {
                            date: cell.date,
                            value: cell.value,
                            leapDay: cell.leapDay,
                            monthBoundary: cell.monthBoundary,
                            sourceRows: [...cell.sourceRows],
                            locale: context.locale ?? 'en',
                            timeZone,
                            weekStart,
                            weekday: cell.weekday,
                        },
                        tooltip: {
                            date: cell.date,
                            value: cell.value,
                            mode,
                            leapDay: cell.leapDay,
                            monthBoundary: cell.monthBoundary,
                            missing: cell.value === null,
                            sourceRows: cell.sourceRows.join(', '),
                            locale: context.locale ?? 'en',
                            timeZone,
                            weekStart,
                            weekday: cell.weekday,
                        },
                    },
                }),
                x,
                y,
                width: Math.max(0.5, cellWidth - 1.4),
                height: Math.max(0.5, cellHeight - 1.4),
                fill,
                stroke: cell.monthBoundary ? context.theme.colors.text : context.theme.colors.background,
                lineWidth: cell.monthBoundary ? 1.6 : 0.6,
                cornerRadius: context.layer.mark.cornerRadius ?? 1.5,
            });
            if (cell.value === null && missing === 'explicit') {
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${context.layer.id}:calendar-missing:${cell.key}`, {
                        zIndex: context.layer.zIndex + 1,
                    }),
                    x1: x,
                    y1: y,
                    x2: x + Math.max(0.5, cellWidth - 1.4),
                    y2: y + Math.max(0.5, cellHeight - 1.4),
                    stroke: context.theme.colors.mutedText,
                    lineWidth: 0.7,
                });
            }
        });
        return nodes;
    };
    function observedInterval(context) {
        const times = Array.from({ length: context.table.length }, (_, index) => numericDataValue(context.table.value(index, context.layer.x.field), true))
            .filter((value) => value !== null)
            .sort((a, b) => a - b);
        const gaps = times
            .slice(1)
            .map((value, index) => value - times[index])
            .filter((value) => value > 0);
        return Math.max(1, Math.min(...gaps, 86_400_000));
    }
    /** OHLC compiler with aggregation, session calendar, extended-hours policy, gapless axis, and navigator. */
    const compileAdvancedCandlestickMark = (context) => {
        const hasAdvanced = [
            'aggregateIntervalMs',
            'timeZone',
            'sessionStartMinute',
            'sessionEndMinute',
            'tradingDays',
            'excludedDates',
            'includedDates',
            'extendedHours',
            'navigator',
            'navigatorStart',
            'navigatorEnd',
        ].some((name) => context.layer.mark.options[name] !== undefined);
        if (!hasAdvanced)
            return compileCandlestickMark(context);
        const extendedValue = stringOption(context, 'extendedHours');
        const extendedHours = extendedValue === 'exclude' || extendedValue === 'separate' ? extendedValue : 'include';
        const startMinute = numberOption(context, 'sessionStartMinute') ?? 0;
        const endMinute = numberOption(context, 'sessionEndMinute') ?? 1_440;
        const buckets = aggregateOhlc(rows(context), {
            timeField: context.layer.x.field,
            openField: context.layer.mark.fields.open ?? 'open',
            highField: context.layer.mark.fields.high ?? 'high',
            lowField: context.layer.mark.fields.low ?? 'low',
            closeField: context.layer.mark.fields.close ?? context.layer.y.field,
            ...(context.layer.mark.fields.volume === undefined
                ? {}
                : { volumeField: context.layer.mark.fields.volume }),
            intervalMs: numberOption(context, 'aggregateIntervalMs') ?? observedInterval(context),
            timeZone: stringOption(context, 'timeZone') ?? 'UTC',
            session: { startMinute, endMinute },
            ...(numberArrayOption(context, 'tradingDays') === undefined
                ? {}
                : { tradingDays: numberArrayOption(context, 'tradingDays') }),
            ...(stringArrayOption(context, 'excludedDates') === undefined
                ? {}
                : { excludedDates: stringArrayOption(context, 'excludedDates') }),
            ...(stringArrayOption(context, 'includedDates') === undefined
                ? {}
                : { includedDates: stringArrayOption(context, 'includedDates') }),
            extendedHours,
        });
        if (buckets.length === 0)
            return [];
        const [from, to] = navigatorWindow(buckets.length, numberOption(context, 'navigatorStart') ?? 0, numberOption(context, 'navigatorEnd') ?? buckets.length);
        let visible = buckets.slice(from, to);
        if (context.xScale.bandwidth > 0) {
            const domain = context.xScale.domain();
            if (domain.length === 2 && typeof domain[0] === 'number' && typeof domain[1] === 'number') {
                const minimumTime = Math.min(domain[0], domain[1]);
                const maximumTime = Math.max(domain[0], domain[1]);
                const withinScale = visible.filter(({ time }) => time >= minimumTime && time <= maximumTime);
                if (withinScale.length > 0)
                    visible = withinScale;
            }
        }
        if (visible.length === 0)
            return [];
        const minimum = Math.min(...visible.map(({ low }) => low));
        const maximum = Math.max(...visible.map(({ high }) => high));
        const span = Math.max(Number.EPSILON, maximum - minimum);
        const navigatorHeight = booleanOption(context, 'navigator') === false ? 0 : Math.min(28, context.plot.height * 0.13);
        const chartHeight = context.plot.height - navigatorHeight - (navigatorHeight > 0 ? 5 : 0);
        const width = context.xScale.bandwidth || context.plot.width / visible.length;
        const mapY = (value) => context.plot.y + chartHeight - ((value - minimum) / span) * chartHeight;
        const nodes = [];
        visible.forEach((bucket, index) => {
            const x = context.xScale.map(bucket.time);
            const open = mapY(bucket.open);
            const close = mapY(bucket.close);
            const high = mapY(bucket.high);
            const low = mapY(bucket.low);
            const rising = bucket.close >= bucket.open;
            const color = rising
                ? categoricalColor(context.theme, 1, 4)
                : categoricalColor(context.theme, 3, 4);
            const opacity = bucket.extended && extendedHours === 'separate'
                ? context.layer.mark.opacity * 0.45
                : context.layer.mark.opacity;
            nodes.push({
                type: 'line',
                ...nodeBase(`${context.layer.id}:ohlc-wick:${bucket.tradingIndex}`, {
                    zIndex: context.layer.zIndex,
                    opacity,
                }),
                x1: x,
                y1: high,
                x2: x,
                y2: low,
                stroke: color,
                lineWidth: 1.3,
            });
            nodes.push({
                type: 'rect',
                ...nodeBase(`${context.layer.id}:ohlc:${bucket.tradingIndex}`, {
                    zIndex: context.layer.zIndex + 1,
                    opacity,
                    interactive: context.performance.enableHitTesting,
                    datum: {
                        layerId: context.layer.id,
                        rowIndex: bucket.sourceRows.at(-1) ?? index,
                        datum: { ...bucket, sourceRows: [...bucket.sourceRows] },
                        tooltip: {
                            time: bucket.time,
                            tradingIndex: bucket.tradingIndex,
                            open: bucket.open,
                            high: bucket.high,
                            low: bucket.low,
                            close: bucket.close,
                            volume: bucket.volume,
                            extended: bucket.extended,
                            sourceRows: bucket.sourceRows.join(', '),
                        },
                    },
                }),
                x: x - width * 0.31,
                y: Math.min(open, close),
                width: Math.max(1.5, width * 0.62),
                height: Math.max(1.5, Math.abs(open - close)),
                fill: context.layer.mark.fill ?? colorWithOpacity(color, 0.72),
                stroke: context.layer.mark.stroke ?? color,
                lineWidth: context.layer.mark.lineWidth ?? 1.2,
                cornerRadius: context.layer.mark.cornerRadius ?? 1,
            });
        });
        if (navigatorHeight > 0) {
            const y = context.plot.y + context.plot.height - navigatorHeight;
            nodes.push({
                type: 'rect',
                ...nodeBase(`${context.layer.id}:navigator-track`, { zIndex: context.layer.zIndex + 3 }),
                x: context.plot.x,
                y,
                width: context.plot.width,
                height: navigatorHeight,
                fill: context.theme.colors.surface,
                stroke: context.theme.colors.axis,
                lineWidth: 1,
                cornerRadius: 2,
            });
            nodes.push({
                type: 'rect',
                ...nodeBase(`${context.layer.id}:navigator-window`, {
                    zIndex: context.layer.zIndex + 4,
                    interactive: true,
                    datum: {
                        layerId: context.layer.id,
                        rowIndex: from,
                        datum: { start: from, end: to, total: buckets.length },
                        tooltip: { start: from, end: to, total: buckets.length },
                        familyInteraction: {
                            kind: 'navigator-window',
                            family: 'candlestick',
                            minimum: 0,
                            maximum: buckets.length,
                            start: from,
                            end: to,
                            plot: {
                                x: context.plot.x,
                                y,
                                width: context.plot.width,
                                height: navigatorHeight,
                            },
                        },
                    },
                }),
                x: context.plot.x + (from / buckets.length) * context.plot.width,
                y: y + 2,
                width: Math.max(4, ((to - from) / buckets.length) * context.plot.width),
                height: Math.max(2, navigatorHeight - 4),
                fill: colorWithOpacity(context.theme.colors.focus, 0.28),
                stroke: context.theme.colors.focus,
                lineWidth: 1.2,
                cornerRadius: 2,
            });
        }
        return nodes;
    };
    /** Difference compiler with aligned baselines, policy values, and exact interpolated crossings. */
    const compileAdvancedDifferenceMark = (context) => {
        const policyValue = stringOption(context, 'policy');
        const policy = policyValue === 'relative' || policyValue === 'percent' ? policyValue : 'absolute';
        const baselineField = context.layer.mark.fields.baseline ?? context.layer.mark.fields.old ?? 'old';
        const comparisonField = context.layer.mark.fields.comparison ?? context.layer.mark.fields.new ?? context.layer.y.field;
        const seriesField = context.layer.mark.fields.series;
        const valueField = context.layer.mark.fields.value;
        const baselineSeries = stringOption(context, 'baselineSeries');
        const comparisonSeries = stringOption(context, 'comparisonSeries');
        const longForm = seriesField !== undefined &&
            valueField !== undefined &&
            baselineSeries !== undefined &&
            comparisonSeries !== undefined;
        if (!longForm && (!context.table.has(baselineField) || !context.table.has(comparisonField)))
            return compileDiffMark(context);
        const unmatchedValue = stringOption(context, 'unmatched');
        const unmatched = unmatchedValue === 'drop' || unmatchedValue === 'zero' ? unmatchedValue : 'error';
        const zeroBaselineValue = stringOption(context, 'zeroBaseline');
        const zeroBaseline = zeroBaselineValue === 'skip' || zeroBaselineValue === 'absolute' ? zeroBaselineValue : 'error';
        const points = differenceSeries(rows(context), {
            keyField: context.layer.x.field,
            ...(longForm
                ? {
                    seriesField,
                    valueField,
                    baselineSeries,
                    comparisonSeries,
                    unmatched,
                }
                : { baselineField, comparisonField }),
            policy,
            zeroBaseline,
            interpolateCrossings: booleanOption(context, 'interpolateCrossings') !== false,
            ...(context.layer.x.type === undefined ? {} : { keyType: context.layer.x.type }),
        });
        const nodes = [];
        points.forEach((point, index) => {
            const next = points[index + 1];
            const x = context.xScale.map(scaleInput(point.key) ?? index);
            const baseline = context.yScale.map(point.baseline);
            const comparison = context.yScale.map(point.comparison);
            if (next !== undefined && next.continuousFromPrevious) {
                const nx = context.xScale.map(scaleInput(next.key) ?? index + 1);
                const nb = context.yScale.map(next.baseline);
                const nc = context.yScale.map(next.comparison);
                const positive = (point.difference + next.difference) / 2 >= 0;
                nodes.push({
                    type: 'path',
                    ...nodeBase(`${context.layer.id}:difference-area:${index}`, {
                        zIndex: context.layer.zIndex,
                        opacity: context.layer.mark.opacity,
                    }),
                    points: [
                        { x, y: baseline },
                        { x, y: comparison },
                        { x: nx, y: nc },
                        { x: nx, y: nb },
                    ],
                    closed: true,
                    fill: colorWithOpacity(categoricalColor(context.theme, positive ? 1 : 3, 4), 0.45),
                    lineWidth: 0,
                });
            }
            nodes.push({
                type: 'circle',
                ...nodeBase(`${context.layer.id}:difference:${index}`, {
                    zIndex: context.layer.zIndex + 2,
                    interactive: context.performance.enableHitTesting,
                    datum: {
                        layerId: context.layer.id,
                        rowIndex: point.sourceRows[0] ?? index,
                        datum: {
                            key: point.key,
                            baseline: point.baseline,
                            comparison: point.comparison,
                            difference: point.difference,
                            crossing: point.crossing ?? false,
                            sourceRows: [...point.sourceRows],
                        },
                        tooltip: {
                            key: point.key,
                            baseline: point.baseline,
                            comparison: point.comparison,
                            difference: point.difference,
                            policy,
                            unmatched: longForm ? unmatched : null,
                            zeroBaseline,
                            crossing: point.crossing ?? false,
                            sourceRows: point.sourceRows.join(', '),
                        },
                    },
                }),
                cx: x,
                cy: comparison,
                radius: point.crossing === true ? 3.5 : 2.2,
                fill: point.crossing === true ? context.theme.colors.focus : context.color,
                stroke: context.theme.colors.background,
                lineWidth: 1,
            });
        });
        return nodes;
    };
    /** Raw-data interval compiler for CI/PI/SE/SD/IQR/HDI with horizontal and vertical provenance. */
    const compileEstimatedIntervalMark = (context) => {
        const rawKind = stringOption(context, 'kind');
        const estimatorEnabled = booleanOption(context, 'rawEstimator') === true || rawKind !== undefined;
        if (!estimatorEnabled)
            return compileIntervalMark(context);
        const kind = rawKind === 'PI' ||
            rawKind === 'SE' ||
            rawKind === 'SD' ||
            rawKind === 'IQR' ||
            rawKind === 'HDI'
            ? rawKind
            : 'CI';
        const orientation = stringOption(context, 'orientation') === 'horizontal' ? 'horizontal' : 'vertical';
        const categoryField = orientation === 'vertical' ? context.layer.x.field : context.layer.y.field;
        const valueField = orientation === 'vertical' ? context.layer.y.field : context.layer.x.field;
        const groups = new Map();
        for (let rowIndex = 0; rowIndex < context.table.length; rowIndex += 1) {
            const rawKey = context.table.value(rowIndex, categoryField);
            const value = numericDataValue(context.table.value(rowIndex, valueField));
            if (value === null ||
                rawKey === null ||
                rawKey === undefined ||
                typeof rawKey === 'boolean' ||
                (typeof rawKey === 'object' && !(rawKey instanceof Date)))
                continue;
            const key = rawKey;
            const id = key instanceof Date ? key.toISOString() : String(key);
            const group = groups.get(id) ?? { key, values: [], sourceRows: [] };
            group.values.push(value);
            group.sourceRows.push(rowIndex);
            groups.set(id, group);
        }
        const nodes = [];
        [...groups.values()].forEach((group, groupIndex) => {
            const interval = estimateInterval(group.values, {
                kind,
                confidence: numberOption(context, 'confidence') ?? 0.95,
                estimator: stringOption(context, 'estimator') === 'median' ? 'median' : 'mean',
                orientation,
            });
            if (interval === null)
                return;
            const categoryScale = orientation === 'vertical' ? context.xScale : context.yScale;
            const valueScale = orientation === 'vertical' ? context.yScale : context.xScale;
            const category = categoryScale.map(group.key);
            const low = valueScale.map(interval.low);
            const high = valueScale.map(interval.high);
            const estimate = valueScale.map(interval.estimate);
            const cap = categoryScale instanceof BandScale ? categoryScale.bandwidth * 0.25 : 7;
            const base = `${context.layer.id}:estimated-interval:${groupIndex}`;
            const common = {
                stroke: context.layer.mark.stroke ?? context.color,
                lineWidth: context.layer.mark.lineWidth ?? 2,
                lineCap: context.theme.mark.lineCap ?? 'round',
            };
            const range = orientation === 'vertical'
                ? {
                    type: 'line',
                    ...nodeBase(`${base}:range`, { zIndex: context.layer.zIndex }),
                    x1: category,
                    x2: category,
                    y1: high,
                    y2: low,
                    ...common,
                }
                : {
                    type: 'line',
                    ...nodeBase(`${base}:range`, { zIndex: context.layer.zIndex }),
                    x1: low,
                    x2: high,
                    y1: category,
                    y2: category,
                    ...common,
                };
            nodes.push(range);
            for (const [name, coordinate] of [
                ['low', low],
                ['high', high],
            ]) {
                nodes.push(orientation === 'vertical'
                    ? {
                        type: 'line',
                        ...nodeBase(`${base}:${name}`, { zIndex: context.layer.zIndex }),
                        x1: category - cap,
                        x2: category + cap,
                        y1: coordinate,
                        y2: coordinate,
                        ...common,
                    }
                    : {
                        type: 'line',
                        ...nodeBase(`${base}:${name}`, { zIndex: context.layer.zIndex }),
                        x1: coordinate,
                        x2: coordinate,
                        y1: category - cap,
                        y2: category + cap,
                        ...common,
                    });
            }
            nodes.push({
                type: 'circle',
                ...nodeBase(`${base}:estimate`, {
                    zIndex: context.layer.zIndex + 1,
                    interactive: context.performance.enableHitTesting,
                    datum: {
                        layerId: context.layer.id,
                        rowIndex: group.sourceRows[0] ?? groupIndex,
                        datum: {
                            category: group.key,
                            estimate: interval.estimate,
                            low: interval.low,
                            high: interval.high,
                            kind,
                            sourceRows: [...group.sourceRows],
                        },
                        tooltip: {
                            category: group.key,
                            estimate: interval.estimate,
                            low: interval.low,
                            high: interval.high,
                            kind,
                            confidence: interval.confidence,
                            summary: interval.summary,
                            sourceRows: group.sourceRows.join(', '),
                        },
                    },
                }),
                cx: orientation === 'vertical' ? category : estimate,
                cy: orientation === 'vertical' ? estimate : category,
                radius: context.layer.mark.radius ?? 4,
                fill: context.layer.mark.fill ?? context.theme.colors.background,
                stroke: context.layer.mark.stroke ?? context.color,
                lineWidth: 2,
            });
        });
        return nodes;
    };
    /** Line compiler with explicit duplicate aggregation and implicit-sort policy. */
    const compileOrderedLineMark = (context) => {
        const duplicateValue = stringOption(context, 'duplicates');
        const sortValue = stringOption(context, 'sortPolicy');
        if (duplicateValue === undefined && sortValue === undefined)
            return compileLineMark(context);
        const duplicates = duplicateValue === 'first' ||
            duplicateValue === 'last' ||
            duplicateValue === 'sum' ||
            duplicateValue === 'mean'
            ? duplicateValue
            : 'error';
        const sort = sortValue === 'ascending' || sortValue === 'descending' || sortValue === 'error'
            ? sortValue
            : 'preserve';
        const points = prepareOrderedSeries(rows(context), {
            keyField: context.layer.x.field,
            valueField: context.layer.y.field,
            duplicates,
            sort,
            ...(context.layer.x.type === undefined ? {} : { keyType: context.layer.x.type }),
        });
        const scenePoints = points.flatMap((point) => {
            const key = scaleInput(point.key);
            if (key === null)
                return [];
            return [{ x: context.xScale.map(key), y: context.yScale.map(point.value) }];
        });
        if (scenePoints.length < 2)
            return [];
        const nodes = [
            {
                type: 'path',
                ...nodeBase(`${context.layer.id}:ordered-line`, {
                    zIndex: context.layer.zIndex,
                    opacity: context.layer.mark.opacity,
                }),
                points: scenePoints,
                closed: false,
                stroke: context.layer.mark.stroke ?? context.color,
                lineWidth: context.layer.mark.lineWidth ?? context.theme.mark.lineWidth,
                lineCap: context.theme.mark.lineCap ?? 'round',
                lineJoin: context.theme.mark.lineJoin ?? 'round',
            },
        ];
        points.forEach((point, index) => {
            const key = scaleInput(point.key);
            if (key === null)
                return;
            nodes.push({
                type: 'circle',
                ...nodeBase(`${context.layer.id}:ordered-line-point:${index}`, {
                    zIndex: context.layer.zIndex + 1,
                    interactive: context.performance.enableHitTesting,
                    datum: {
                        layerId: context.layer.id,
                        rowIndex: point.sourceRows[0] ?? index,
                        datum: { key: point.key, value: point.value, sourceRows: [...point.sourceRows] },
                        tooltip: {
                            key: point.key,
                            value: point.value,
                            duplicates,
                            sort,
                            sourceRows: point.sourceRows.join(', '),
                        },
                    },
                }),
                cx: context.xScale.map(key),
                cy: context.yScale.map(point.value),
                radius: context.layer.mark.radius ?? 3,
                fill: context.layer.mark.fill ?? context.color,
                stroke: context.theme.colors.background,
                lineWidth: 1,
            });
        });
        return nodes;
    };
    /** Waterfall compiler with explicit relative/absolute/subtotal/total semantics. */
    const compileSemanticWaterfallMark = (context) => {
        const kindField = context.layer.mark.fields.kind ?? 'kind';
        if (!context.table.has(kindField) && booleanOption(context, 'explicitSemantics') !== true)
            return compileWaterfallMark(context);
        const values = Array.from({ length: context.table.length }, (_, rowIndex) => {
            const rawKind = context.table.has(kindField)
                ? context.table.value(rowIndex, kindField)
                : 'relative';
            const kind = rawKind === 'absolute' || rawKind === 'subtotal' || rawKind === 'total'
                ? rawKind
                : 'relative';
            const rawLabel = context.table.value(rowIndex, context.layer.x.field);
            return {
                value: numericDataValue(context.table.value(rowIndex, context.layer.y.field)) ?? 0,
                kind,
                label: rawLabel === null || rawLabel === undefined ? `step-${rowIndex}` : String(rawLabel),
            };
        });
        const steps = waterfallSteps(values);
        const minimum = Math.min(0, ...steps.flatMap(({ start, end }) => [start, end]));
        const maximum = Math.max(0, ...steps.flatMap(({ start, end }) => [start, end]));
        const span = Math.max(Number.EPSILON, maximum - minimum);
        const width = context.plot.width / Math.max(1, steps.length);
        const mapY = (value) => context.plot.y + context.plot.height - ((value - minimum) / span) * context.plot.height;
        const nodes = [];
        steps.forEach((step, index) => {
            const start = mapY(step.start);
            const end = mapY(step.end);
            const color = step.kind === 'subtotal' || step.kind === 'total'
                ? context.theme.colors.focus
                : categoricalColor(context.theme, step.change >= 0 ? 1 : 3, 4);
            nodes.push({
                type: 'rect',
                ...nodeBase(`${context.layer.id}:semantic-waterfall:${index}`, {
                    zIndex: context.layer.zIndex,
                    interactive: context.performance.enableHitTesting,
                    datum: {
                        layerId: context.layer.id,
                        rowIndex: index,
                        datum: { ...step },
                        tooltip: {
                            label: step.label ?? '',
                            kind: step.kind,
                            value: step.value,
                            start: step.start,
                            end: step.end,
                            change: step.change,
                        },
                    },
                }),
                x: context.plot.x + index * width + width * 0.13,
                y: Math.min(start, end),
                width: Math.max(1, width * 0.74),
                height: Math.max(1, Math.abs(start - end)),
                fill: context.layer.mark.fill ?? colorWithOpacity(color, 0.78),
                stroke: context.layer.mark.stroke ?? color,
                lineWidth: context.layer.mark.lineWidth ?? 1,
                cornerRadius: context.layer.mark.cornerRadius ?? context.theme.mark.barRadius,
            });
            if (index < steps.length - 1) {
                const connectorY = mapY(step.end);
                nodes.push({
                    type: 'line',
                    ...nodeBase(`${context.layer.id}:semantic-waterfall-connector:${index}`, {
                        zIndex: context.layer.zIndex - 1,
                    }),
                    x1: context.plot.x + (index + 0.87) * width,
                    x2: context.plot.x + (index + 1.13) * width,
                    y1: connectorY,
                    y2: connectorY,
                    stroke: context.theme.colors.axis,
                    lineWidth: 1,
                    dash: [3, 3],
                });
            }
        });
        return nodes;
    };

    const capabilities$2 = {
        vector: false,
        gpu: false,
        worker: false,
        exportFormats: ['image/png', 'image/jpeg', 'image/webp'],
        inspectionViewport: true,
    };
    const maximumProviderTileRequests = 8;
    const maximumDecodedProviderTiles = 128;
    function tileSourceKey(source) {
        return JSON.stringify([
            source.type,
            source.template,
            source.attribution,
            source.minimumZoom ?? null,
            source.maximumZoom ?? null,
            source.tileSize ?? null,
            source.subdomains ?? [],
        ]);
    }
    function providerTileKey(source, tile) {
        return `${tileSourceKey(source)}\u0000${tileUrl(source, tile)}`;
    }
    function collectVisibleProviderTiles(node, output) {
        if (!node.visible || node.opacity <= 0)
            return;
        if (node.type === 'rect' && node.providerTile !== undefined) {
            const request = node.providerTile;
            output.set(providerTileKey(request.source, request.tile), request);
        }
        if (node.type === 'group') {
            for (const child of node.children)
                collectVisibleProviderTiles(child, output);
        }
    }
    async function decodeProviderTile(response) {
        if (!response.mimeType.toLowerCase().startsWith('image/'))
            return {};
        const blob = new Blob([Uint8Array.from(response.bytes).buffer], { type: response.mimeType });
        if (typeof createImageBitmap === 'function') {
            const image = await createImageBitmap(blob);
            return { image, dispose: () => image.close() };
        }
        if (typeof Image === 'undefined' ||
            typeof URL === 'undefined' ||
            typeof URL.createObjectURL !== 'function')
            throw new Error('Provider raster tiles require createImageBitmap or Image support.');
        const objectUrl = URL.createObjectURL(blob);
        const image = new Image();
        try {
            await new Promise((resolve, reject) => {
                image.addEventListener('load', () => resolve(), { once: true });
                image.addEventListener('error', () => reject(new Error('Provider raster tile decode failed.')), {
                    once: true,
                });
                image.src = objectUrl;
            });
            return { image, dispose: () => URL.revokeObjectURL(objectUrl) };
        }
        catch (error) {
            URL.revokeObjectURL(objectUrl);
            throw error;
        }
    }
    function roundedRectPath(context, x, y, width, height, radius) {
        const resolvedRadius = Math.max(0, Math.min(radius, Math.abs(width) / 2, Math.abs(height) / 2));
        context.moveTo(x + resolvedRadius, y);
        context.lineTo(x + width - resolvedRadius, y);
        context.quadraticCurveTo(x + width, y, x + width, y + resolvedRadius);
        context.lineTo(x + width, y + height - resolvedRadius);
        context.quadraticCurveTo(x + width, y + height, x + width - resolvedRadius, y + height);
        context.lineTo(x + resolvedRadius, y + height);
        context.quadraticCurveTo(x, y + height, x, y + height - resolvedRadius);
        context.lineTo(x, y + resolvedRadius);
        context.quadraticCurveTo(x, y, x + resolvedRadius, y);
    }
    class CanvasRenderer {
        name = 'canvas';
        capabilities = capabilities$2;
        #root = null;
        #canvas = null;
        #context = null;
        #width = 0;
        #height = 0;
        #pixelRatio = 1;
        #inspectionView = { zoom: 1, offsetX: 0, offsetY: 0 };
        #tileManagers = new Map();
        #tileImages = new Map();
        #tileQueue = [];
        #activeTileRequests = 0;
        #tileGeneration = 0;
        #visibleTileKeys = new Set();
        #lastScene = null;
        #destroyed = false;
        mount(target, options) {
            if (this.#root !== null)
                this.destroy();
            this.#destroyed = false;
            const root = document.createElement('div');
            root.dataset.graflumeRoot = 'true';
            root.style.position = 'relative';
            root.style.width = '100%';
            root.style.height = '100%';
            root.style.overflow = 'hidden';
            const canvas = document.createElement('canvas');
            canvas.dataset.graflumeSurface = 'canvas';
            canvas.style.display = 'block';
            canvas.style.width = `${options.width}px`;
            canvas.style.height = `${options.height}px`;
            canvas.setAttribute('role', 'img');
            canvas.setAttribute('aria-label', options.ariaLabel);
            if (options.ariaDescription !== undefined) {
                canvas.setAttribute('aria-description', options.ariaDescription);
            }
            const context = canvas.getContext('2d');
            if (context === null)
                throw new Error('Canvas 2D context is unavailable.');
            root.append(canvas);
            target.append(root);
            this.#root = root;
            this.#canvas = canvas;
            this.#context = context;
            this.resize(options.width, options.height, options.pixelRatio);
        }
        resize(width, height, pixelRatio) {
            if (this.#canvas === null || this.#context === null)
                return;
            this.#width = Math.max(1, width);
            this.#height = Math.max(1, height);
            this.#pixelRatio = Math.max(1, pixelRatio);
            this.#canvas.width = Math.round(this.#width * this.#pixelRatio);
            this.#canvas.height = Math.round(this.#height * this.#pixelRatio);
            this.#canvas.style.width = `${this.#width}px`;
            this.#canvas.style.height = `${this.#height}px`;
            this.#context.setTransform(this.#pixelRatio, 0, 0, this.#pixelRatio, 0, 0);
        }
        render(scene) {
            const context = this.#context;
            if (context === null)
                return;
            this.#lastScene = scene;
            this.#reconcileProviderTiles(scene.root);
            this.#paint(scene);
        }
        #paint(scene) {
            const context = this.#context;
            if (context === null)
                return;
            context.save();
            context.setTransform(this.#pixelRatio, 0, 0, this.#pixelRatio, 0, 0);
            context.clearRect(0, 0, this.#width, this.#height);
            context.fillStyle = scene.background;
            context.fillRect(0, 0, scene.width, scene.height);
            if (this.#inspectionView.zoom !== 1 ||
                this.#inspectionView.offsetX !== 0 ||
                this.#inspectionView.offsetY !== 0) {
                context.translate(this.#inspectionView.offsetX, this.#inspectionView.offsetY);
                context.scale(this.#inspectionView.zoom, this.#inspectionView.zoom);
            }
            this.#drawNode(context, scene.root);
            context.restore();
        }
        surface() {
            return this.#canvas;
        }
        overlayHost() {
            return this.#root;
        }
        setInspectionView(transform) {
            this.#inspectionView = transform;
        }
        toDataURL(type = 'image/png', quality) {
            if (this.#canvas === null)
                throw new Error('Renderer is not mounted.');
            return this.#canvas.toDataURL(type, quality);
        }
        /** Observable provider state for diagnostics and deterministic host readiness checks. */
        providerTileState() {
            const states = [...this.#tileImages.values()];
            return Object.freeze({
                sources: this.#tileManagers.size,
                loading: states.filter(({ status }) => status === 'queued' || status === 'loading').length,
                ready: states.filter(({ status }) => status === 'ready').length,
                failed: states.filter(({ status }) => status === 'failed').length,
            });
        }
        destroy() {
            this.#destroyed = true;
            this.#tileGeneration += 1;
            for (const state of this.#tileImages.values()) {
                if (state.status === 'queued' || state.status === 'loading') {
                    state.controller.abort(new DOMException('Canvas provider tile destroyed.', 'AbortError'));
                }
            }
            for (const manager of this.#tileManagers.values())
                manager.destroy();
            this.#tileManagers.clear();
            for (const state of this.#tileImages.values()) {
                if (state.status === 'ready')
                    state.dispose?.();
            }
            this.#tileImages.clear();
            this.#tileQueue = [];
            this.#activeTileRequests = 0;
            this.#visibleTileKeys.clear();
            this.#lastScene = null;
            this.#root?.remove();
            this.#root = null;
            this.#canvas = null;
            this.#context = null;
            this.#inspectionView = { zoom: 1, offsetX: 0, offsetY: 0 };
        }
        #drawNode(context, node) {
            if (!node.visible || node.opacity <= 0)
                return;
            context.save();
            context.globalAlpha *= node.opacity;
            switch (node.type) {
                case 'group':
                    this.#drawGroup(context, node);
                    break;
                case 'line':
                    this.#drawLine(context, node);
                    break;
                case 'path':
                    this.#drawPath(context, node);
                    break;
                case 'rect':
                    this.#drawRect(context, node);
                    break;
                case 'circle':
                    this.#drawCircle(context, node);
                    break;
                case 'text':
                    this.#drawText(context, node);
                    break;
            }
            context.restore();
        }
        #drawGroup(context, node) {
            if (node.clip !== undefined) {
                context.beginPath();
                context.rect(node.clip.x, node.clip.y, node.clip.width, node.clip.height);
                context.clip();
            }
            const children = [...node.children].sort((left, right) => left.zIndex - right.zIndex);
            for (const child of children)
                this.#drawNode(context, child);
        }
        #drawLine(context, node) {
            context.beginPath();
            context.moveTo(node.x1, node.y1);
            context.lineTo(node.x2, node.y2);
            context.strokeStyle = node.stroke;
            context.lineWidth = node.lineWidth;
            context.lineCap = node.lineCap ?? 'butt';
            context.setLineDash(node.dash === undefined ? [] : [...node.dash]);
            context.stroke();
        }
        #drawPath(context, node) {
            const paths = [node.points, ...(node.subpaths ?? [])].filter((points) => points.length > 0);
            if (paths.length === 0)
                return;
            context.beginPath();
            for (const points of paths) {
                const first = points[0];
                if (first === undefined)
                    continue;
                context.moveTo(first.x, first.y);
                for (let index = 1; index < points.length; index += 1) {
                    const point = points[index];
                    if (point !== undefined)
                        context.lineTo(point.x, point.y);
                }
                if (node.closed)
                    context.closePath();
            }
            context.setLineDash(node.dash === undefined ? [] : [...node.dash]);
            context.lineCap = node.lineCap ?? 'round';
            context.lineJoin = node.lineJoin ?? 'round';
            if (node.fill !== undefined) {
                context.fillStyle = node.fill;
                context.fill(node.fillRule ?? 'nonzero');
            }
            if (node.stroke !== undefined && node.lineWidth > 0) {
                context.strokeStyle = node.stroke;
                context.lineWidth = node.lineWidth;
                context.stroke();
            }
        }
        #drawRect(context, node) {
            context.beginPath();
            roundedRectPath(context, node.x, node.y, node.width, node.height, node.cornerRadius);
            context.closePath();
            context.setLineDash(node.dash === undefined ? [] : [...node.dash]);
            if (node.fill !== undefined) {
                context.fillStyle = node.fill;
                context.fill();
            }
            if (node.providerTile !== undefined) {
                const image = this.#providerTileImage(node.providerTile.source, node.providerTile.tile);
                if (image !== undefined)
                    context.drawImage(image, node.x, node.y, node.width, node.height);
            }
            if (node.stroke !== undefined && node.lineWidth > 0) {
                context.strokeStyle = node.stroke;
                context.lineWidth = node.lineWidth;
                context.stroke();
            }
        }
        #providerTileImage(source, tile) {
            const key = providerTileKey(source, tile);
            const current = this.#tileImages.get(key);
            if (current?.status === 'ready') {
                this.#tileImages.delete(key);
                this.#tileImages.set(key, current);
                return current.image;
            }
            return undefined;
        }
        #reconcileProviderTiles(root) {
            const visible = new Map();
            collectVisibleProviderTiles(root, visible);
            this.#visibleTileKeys = new Set(visible.keys());
            for (const [key, state] of this.#tileImages) {
                if (visible.has(key))
                    continue;
                if (state.status === 'queued' || state.status === 'loading') {
                    state.controller.abort(new DOMException('Canvas provider tile left the visible scene.', 'AbortError'));
                    this.#tileImages.delete(key);
                }
                else if (state.status === 'failed') {
                    this.#tileImages.delete(key);
                }
            }
            this.#tileQueue = this.#tileQueue.filter((key) => {
                const state = this.#tileImages.get(key);
                return visible.has(key) && state?.status === 'queued';
            });
            for (const [key, request] of visible) {
                const current = this.#tileImages.get(key);
                if (current?.status === 'ready') {
                    this.#tileImages.delete(key);
                    this.#tileImages.set(key, current);
                }
                else if (current === undefined) {
                    const controller = new AbortController();
                    this.#tileImages.set(key, { status: 'queued', request, controller });
                    this.#tileQueue.push(key);
                }
            }
            this.#drainProviderTileQueue();
        }
        #drainProviderTileQueue() {
            while (!this.#destroyed &&
                this.#activeTileRequests < maximumProviderTileRequests &&
                this.#tileQueue.length > 0) {
                const key = this.#tileQueue.shift();
                const queued = this.#tileImages.get(key);
                if (queued?.status !== 'queued' ||
                    queued.controller.signal.aborted ||
                    !this.#visibleTileKeys.has(key)) {
                    if (queued?.status === 'queued')
                        this.#tileImages.delete(key);
                    continue;
                }
                const sourceKey = tileSourceKey(queued.request.source);
                let manager = this.#tileManagers.get(sourceKey);
                if (manager === undefined) {
                    manager = new MapTileManager(queued.request.source, fetchMapTile);
                    this.#tileManagers.set(sourceKey, manager);
                }
                const generation = this.#tileGeneration;
                const controller = queued.controller;
                const request = queued.request;
                this.#tileImages.set(key, { ...queued, status: 'loading' });
                this.#activeTileRequests += 1;
                void manager
                    .load(request.tile, controller.signal)
                    .then(decodeProviderTile)
                    .then((decoded) => {
                    const current = this.#tileImages.get(key);
                    if (this.#destroyed ||
                        generation !== this.#tileGeneration ||
                        controller.signal.aborted ||
                        !this.#visibleTileKeys.has(key) ||
                        current?.status !== 'loading' ||
                        current.controller !== controller) {
                        decoded.dispose?.();
                        return;
                    }
                    this.#tileImages.delete(key);
                    this.#tileImages.set(key, { status: 'ready', ...decoded });
                    this.#evictDecodedProviderTiles();
                    if (this.#lastScene !== null)
                        this.#paint(this.#lastScene);
                })
                    .catch(() => {
                    const current = this.#tileImages.get(key);
                    if (!this.#destroyed &&
                        generation === this.#tileGeneration &&
                        current?.status === 'loading' &&
                        current.controller === controller) {
                        if (controller.signal.aborted || !this.#visibleTileKeys.has(key))
                            this.#tileImages.delete(key);
                        else
                            this.#tileImages.set(key, { status: 'failed' });
                    }
                })
                    .finally(() => {
                    if (generation !== this.#tileGeneration)
                        return;
                    this.#activeTileRequests = Math.max(0, this.#activeTileRequests - 1);
                    this.#drainProviderTileQueue();
                });
            }
        }
        #evictDecodedProviderTiles() {
            let ready = [...this.#tileImages.values()].filter(({ status }) => status === 'ready').length;
            if (ready <= maximumDecodedProviderTiles)
                return;
            for (const [key, state] of this.#tileImages) {
                if (ready <= maximumDecodedProviderTiles)
                    break;
                if (state.status !== 'ready')
                    continue;
                this.#tileImages.delete(key);
                state.dispose?.();
                ready -= 1;
            }
        }
        #drawCircle(context, node) {
            context.beginPath();
            context.arc(node.cx, node.cy, node.radius, 0, Math.PI * 2);
            context.setLineDash(node.dash === undefined ? [] : [...node.dash]);
            if (node.fill !== undefined) {
                context.fillStyle = node.fill;
                context.fill();
            }
            if (node.stroke !== undefined && node.lineWidth > 0) {
                context.strokeStyle = node.stroke;
                context.lineWidth = node.lineWidth;
                context.stroke();
            }
        }
        #drawText(context, node) {
            context.translate(node.x, node.y);
            context.rotate((node.rotation * Math.PI) / 180);
            context.fillStyle = node.fill;
            const fontStyle = node.fontStyle === undefined ? '' : `${node.fontStyle} `;
            context.font = `${fontStyle}${node.fontWeight} ${node.fontSize}px ${node.fontFamily}`;
            context.textAlign = node.align;
            context.textBaseline = node.baseline;
            context.fillText(node.text, 0, 0);
        }
    }
    const canvasRendererFactory = {
        name: 'canvas',
        capabilities: capabilities$2,
        create: () => new CanvasRenderer(),
    };

    const capabilities$1 = {
        vector: false,
        gpu: true,
        worker: false,
        exportFormats: ['image/png', 'image/jpeg', 'image/webp'],
        inspectionViewport: true,
    };
    function pointGeometry(node, opacity) {
        if (node.fill === undefined)
            return null;
        if (node.type === 'circle') {
            return { x: node.cx, y: node.cy, radius: node.radius, shape: 0, fill: node.fill, opacity };
        }
        if (node.type === 'rect') {
            return {
                x: node.x + node.width / 2,
                y: node.y + node.height / 2,
                radius: Math.max(node.width, node.height) / 2,
                shape: 1,
                fill: node.fill,
                opacity,
            };
        }
        if (node.points.length < 3)
            return null;
        const minimumX = Math.min(...node.points.map(({ x }) => x));
        const maximumX = Math.max(...node.points.map(({ x }) => x));
        const minimumY = Math.min(...node.points.map(({ y }) => y));
        const maximumY = Math.max(...node.points.map(({ y }) => y));
        return {
            x: (minimumX + maximumX) / 2,
            y: (minimumY + maximumY) / 2,
            radius: Math.max(maximumX - minimumX, maximumY - minimumY) / 2,
            shape: node.points.length === 3 ? 2 : node.points.length === 12 ? 4 : 3,
            fill: node.fill,
            opacity,
        };
    }
    function isScatterPointNode(node) {
        return (/(^|:)point:\d+$/.test(node.id) &&
            (node.type === 'circle' || node.type === 'rect' || node.type === 'path'));
    }
    function scatterPoints(root) {
        const points = [];
        const visit = (node, parentOpacity) => {
            if (!node.visible || node.opacity <= 0)
                return;
            const opacity = parentOpacity * node.opacity;
            if (node.type === 'group') {
                node.children.forEach((child) => visit(child, opacity));
                return;
            }
            if (!isScatterPointNode(node))
                return;
            const geometry = pointGeometry(node, opacity);
            if (geometry !== null)
                points.push(geometry);
        };
        visit(root, 1);
        return points;
    }
    function suppressScatterPoints(node) {
        if (node.type === 'group') {
            return { ...node, children: node.children.map(suppressScatterPoints) };
        }
        return isScatterPointNode(node) ? { ...node, visible: false } : node;
    }
    function sceneWithoutScatterPoints(scene) {
        return { ...scene, root: suppressScatterPoints(scene.root) };
    }
    function colorChannels(color) {
        const hex = color.trim().match(/^#([0-9a-f]{3,8})$/i)?.[1];
        if (hex !== undefined) {
            const expanded = hex.length === 3 || hex.length === 4
                ? [...hex].map((channel) => `${channel}${channel}`).join('')
                : hex;
            if (expanded.length === 6 || expanded.length === 8) {
                return [
                    Number.parseInt(expanded.slice(0, 2), 16) / 255,
                    Number.parseInt(expanded.slice(2, 4), 16) / 255,
                    Number.parseInt(expanded.slice(4, 6), 16) / 255,
                    expanded.length === 8 ? Number.parseInt(expanded.slice(6, 8), 16) / 255 : 1,
                ];
            }
        }
        const rgb = color
            .trim()
            .match(/^rgba?\(\s*([\d.]+)\s*,\s*([\d.]+)\s*,\s*([\d.]+)(?:\s*,\s*([\d.]+))?\s*\)$/i);
        if (rgb !== null) {
            return [
                Math.max(0, Math.min(255, Number(rgb[1]))) / 255,
                Math.max(0, Math.min(255, Number(rgb[2]))) / 255,
                Math.max(0, Math.min(255, Number(rgb[3]))) / 255,
                Math.max(0, Math.min(1, rgb[4] === undefined ? 1 : Number(rgb[4]))),
            ];
        }
        return null;
    }
    function compileShader(gl, type, source) {
        const shader = gl.createShader(type);
        if (shader === null)
            return null;
        gl.shaderSource(shader, source);
        gl.compileShader(shader);
        if (!gl.getShaderParameter(shader, gl.COMPILE_STATUS)) {
            gl.deleteShader(shader);
            return null;
        }
        return shader;
    }
    function createProgram(gl) {
        const vertex = compileShader(gl, gl.VERTEX_SHADER, `
attribute vec2 a_position;
attribute vec4 a_color;
attribute float a_size;
attribute float a_shape;
uniform vec2 u_resolution;
varying vec4 v_color;
varying float v_shape;
void main() {
  vec2 clip = (a_position / u_resolution) * 2.0 - 1.0;
  gl_Position = vec4(clip.x, -clip.y, 0.0, 1.0);
  gl_PointSize = a_size;
  v_color = a_color;
  v_shape = a_shape;
}`);
        const fragment = compileShader(gl, gl.FRAGMENT_SHADER, `
precision mediump float;
varying vec4 v_color;
varying float v_shape;
void main() {
  vec2 centered = gl_PointCoord - vec2(0.5);
  if (v_shape < 0.5 && dot(centered, centered) > 0.25) discard;
  if (v_shape > 1.5 && v_shape < 2.5) {
    float top = gl_PointCoord.y;
    float halfWidth = min(top, 1.0 - top * 0.5);
    if (abs(centered.x) > halfWidth) discard;
  }
  if (v_shape > 2.5 && v_shape < 3.5 && abs(centered.x) + abs(centered.y) > 0.5) discard;
  if (v_shape > 3.5 && abs(centered.x) > 0.17 && abs(centered.y) > 0.17) discard;
  gl_FragColor = v_color;
}`);
        if (vertex === null || fragment === null)
            return null;
        const program = gl.createProgram();
        if (program === null)
            return null;
        gl.attachShader(program, vertex);
        gl.attachShader(program, fragment);
        gl.linkProgram(program);
        gl.deleteShader(vertex);
        gl.deleteShader(fragment);
        if (!gl.getProgramParameter(program, gl.LINK_STATUS)) {
            gl.deleteProgram(program);
            return null;
        }
        return program;
    }
    /** Hybrid renderer: axes/labels remain Canvas 2D while scatter points are actual WebGL points. */
    class ScatterWebGLRenderer {
        name = 'webgl';
        capabilities = capabilities$1;
        #canvas = new CanvasRenderer();
        #surface = null;
        #gl = null;
        #program = null;
        #width = 1;
        #height = 1;
        #pixelRatio = 1;
        #view = { zoom: 1, offsetX: 0, offsetY: 0 };
        #lastScene = null;
        #state = {
            renderer: 'canvas',
            pointCount: 0,
            reason: 'empty',
        };
        mount(target, options) {
            if (this.#surface !== null || this.#gl !== null)
                this.destroy();
            this.#canvas.mount(target, options);
            const host = this.#canvas.overlayHost();
            if (host === null)
                return;
            const surface = host.ownerDocument.createElement('canvas');
            surface.dataset.graflumeScatterSurface = 'webgl';
            surface.setAttribute('aria-hidden', 'true');
            surface.style.position = 'absolute';
            surface.style.inset = '0';
            surface.style.pointerEvents = 'none';
            surface.style.width = `${options.width}px`;
            surface.style.height = `${options.height}px`;
            host.append(surface);
            const context = surface.getContext('webgl2', {
                alpha: true,
                antialias: true,
                depth: true,
                premultipliedAlpha: true,
            }) ??
                surface.getContext('webgl', {
                    alpha: true,
                    antialias: true,
                    depth: true,
                    premultipliedAlpha: true,
                });
            this.#surface = surface;
            this.#gl = context;
            this.#program = context === null ? null : createProgram(context);
            this.resize(options.width, options.height, options.pixelRatio);
        }
        resize(width, height, pixelRatio) {
            this.#width = Math.max(1, width);
            this.#height = Math.max(1, height);
            this.#pixelRatio = Math.max(1, pixelRatio);
            this.#canvas.resize(this.#width, this.#height, this.#pixelRatio);
            if (this.#surface !== null) {
                this.#surface.width = Math.round(this.#width * this.#pixelRatio);
                this.#surface.height = Math.round(this.#height * this.#pixelRatio);
                this.#surface.style.width = `${this.#width}px`;
                this.#surface.style.height = `${this.#height}px`;
            }
        }
        render(scene) {
            this.#lastScene = scene;
            const points = scatterPoints(scene.root);
            const rendered = points.length > 0 && this.#drawPoints(points);
            if (!rendered)
                this.#clearPoints();
            this.#canvas.render(rendered ? sceneWithoutScatterPoints(scene) : scene);
            this.#state = {
                renderer: rendered ? 'webgl' : 'canvas',
                pointCount: points.length,
                reason: rendered ? 'gpu' : points.length === 0 ? 'empty' : 'unavailable',
            };
        }
        surface() {
            return this.#canvas.surface();
        }
        overlayHost() {
            return this.#canvas.overlayHost();
        }
        setInspectionView(transform) {
            this.#view = { ...transform };
            this.#canvas.setInspectionView(transform);
            if (this.#lastScene !== null)
                this.render(this.#lastScene);
        }
        toDataURL(type = 'image/png', quality) {
            const base = this.#canvas.surface();
            const overlay = this.#surface;
            if (base === null || overlay === null || typeof base.getContext !== 'function') {
                return this.#canvas.toDataURL(type, quality);
            }
            const composed = base.ownerDocument.createElement('canvas');
            composed.width = base.width;
            composed.height = base.height;
            const context = composed.getContext('2d');
            if (context === null)
                return this.#canvas.toDataURL(type, quality);
            context.drawImage(base, 0, 0);
            context.drawImage(overlay, 0, 0);
            return composed.toDataURL(type, quality);
        }
        dispatchState() {
            return Object.freeze({ ...this.#state });
        }
        destroy() {
            const gl = this.#gl;
            if (gl !== null && this.#program !== null)
                gl.deleteProgram(this.#program);
            this.#program = null;
            this.#gl = null;
            this.#surface?.remove();
            this.#surface = null;
            this.#canvas.destroy();
            this.#lastScene = null;
            this.#state = { renderer: 'canvas', pointCount: 0, reason: 'destroyed' };
        }
        #drawPoints(points) {
            const gl = this.#gl;
            const program = this.#program;
            if (gl === null || program === null || this.#surface === null)
                return false;
            const colorValues = points.map(({ fill }) => colorChannels(fill));
            if (colorValues.some((color) => color === null))
                return false;
            const positions = new Float32Array(points.length * 2);
            const colors = new Float32Array(points.length * 4);
            const sizes = new Float32Array(points.length);
            const shapes = new Float32Array(points.length);
            points.forEach((point, index) => {
                positions[index * 2] = point.x * this.#view.zoom + this.#view.offsetX;
                positions[index * 2 + 1] = point.y * this.#view.zoom + this.#view.offsetY;
                const color = colorValues[index];
                colors.set([color[0], color[1], color[2], color[3] * point.opacity], index * 4);
                sizes[index] = Math.max(1, point.radius * 2 * this.#view.zoom * this.#pixelRatio);
                shapes[index] = point.shape;
            });
            const bind = (name, values, size) => {
                const location = gl.getAttribLocation(program, name);
                if (location < 0)
                    return null;
                const buffer = gl.createBuffer();
                if (buffer === null)
                    return null;
                gl.bindBuffer(gl.ARRAY_BUFFER, buffer);
                gl.bufferData(gl.ARRAY_BUFFER, values, gl.STREAM_DRAW);
                gl.enableVertexAttribArray(location);
                gl.vertexAttribPointer(location, size, gl.FLOAT, false, 0, 0);
                return buffer;
            };
            gl.viewport(0, 0, this.#surface.width, this.#surface.height);
            gl.clearColor(0, 0, 0, 0);
            gl.clear(gl.COLOR_BUFFER_BIT | gl.DEPTH_BUFFER_BIT);
            gl.useProgram(program);
            const position = bind('a_position', positions, 2);
            const color = bind('a_color', colors, 4);
            const size = bind('a_size', sizes, 1);
            const shape = bind('a_shape', shapes, 1);
            if (position === null || color === null || size === null || shape === null) {
                for (const buffer of [position, color, size, shape])
                    if (buffer !== null)
                        gl.deleteBuffer(buffer);
                return false;
            }
            const resolution = gl.getUniformLocation(program, 'u_resolution');
            gl.uniform2f(resolution, this.#width, this.#height);
            gl.enable(gl.BLEND);
            gl.blendFunc(gl.SRC_ALPHA, gl.ONE_MINUS_SRC_ALPHA);
            gl.enable(gl.DEPTH_TEST);
            gl.depthFunc(gl.LEQUAL);
            gl.drawArrays(gl.POINTS, 0, points.length);
            for (const buffer of [position, color, size, shape])
                gl.deleteBuffer(buffer);
            return true;
        }
        #clearPoints() {
            const gl = this.#gl;
            if (gl === null || this.#surface === null)
                return;
            gl.viewport(0, 0, this.#surface.width, this.#surface.height);
            gl.clearColor(0, 0, 0, 0);
            gl.clear(gl.COLOR_BUFFER_BIT | gl.DEPTH_BUFFER_BIT);
        }
    }
    const scatterWebGLRendererFactory = {
        name: 'webgl',
        capabilities: capabilities$1,
        create: () => new ScatterWebGLRenderer(),
    };

    const pluginApiVersion = '0.1';

    class ThemeRegistry {
        #themes = new Map();
        constructor() {
            for (const entry of builtInThemeCatalog)
                this.register(entry.tokens);
        }
        register(theme) {
            if (theme.name.trim() === '') {
                throw new GraflumeError('INVALID_SPEC', 'Theme name must not be empty.', {
                    path: '$.theme.name',
                });
            }
            this.#themes.set(theme.name, theme);
        }
        has(name) {
            return this.#themes.has(name);
        }
        get(name) {
            const theme = this.#themes.get(name);
            if (theme === undefined) {
                throw new GraflumeError('INVALID_SPEC', `Unknown theme "${name}".`, {
                    path: '$.theme',
                    details: { availableThemes: this.names() },
                });
            }
            return theme;
        }
        names() {
            return [...this.#themes.keys()].sort();
        }
        resolve(input) {
            if (typeof input === 'string')
                return this.get(input);
            const baseName = input.extends ?? defaultThemeId;
            const { extends: _extends, ...overrides } = input;
            const merged = deepMerge(this.get(baseName), overrides);
            return {
                ...merged,
                name: merged.name || `custom:${baseName}`,
            };
        }
    }

    class RuntimeRegistry {
        themes = new ThemeRegistry();
        /** Built-in and host-registered formatter ids used by compiled table marks. */
        tableFormatters = createTableFormatterRegistry();
        #marks = new Map();
        #renderers = new Map();
        #plugins = new Map();
        registerMark(type, compiler) {
            const normalized = type.trim().toLowerCase();
            if (normalized === '')
                throw new Error('Mark type must not be empty.');
            this.#marks.set(normalized, compiler);
        }
        mark(type) {
            const compiler = this.#marks.get(type.trim().toLowerCase());
            if (compiler === undefined) {
                throw new GraflumeError('UNSUPPORTED_MARK', `Unsupported mark type "${type}".`, {
                    path: '$.layers[].mark.type',
                    details: { availableMarks: this.markNames() },
                });
            }
            return compiler;
        }
        markNames() {
            return [...this.#marks.keys()].sort();
        }
        registerRenderer(factory) {
            this.#renderers.set(factory.name, factory);
        }
        renderer(name) {
            const factory = this.#renderers.get(name);
            if (factory === undefined) {
                throw new GraflumeError('UNSUPPORTED_RENDERER', `Unsupported renderer "${name}".`, {
                    path: '$.renderer',
                    details: { availableRenderers: this.rendererNames() },
                });
            }
            return factory;
        }
        resolveRenderer(preference) {
            if (preference === 'auto')
                return this.renderer('canvas');
            return this.renderer(preference);
        }
        rendererNames() {
            return [...this.#renderers.keys()].sort();
        }
        registerTheme(theme) {
            this.themes.register(theme);
        }
        registerTableFormatter(id, formatter) {
            this.tableFormatters.register(id, formatter);
        }
        use(plugin) {
            if (this.#plugins.has(plugin.name))
                return;
            if (plugin.apiVersion !== undefined && plugin.apiVersion !== pluginApiVersion) {
                throw new GraflumeError('INVALID_SPEC', `Plugin "${plugin.name}" requires API ${plugin.apiVersion}; runtime is ${pluginApiVersion}.`);
            }
            const context = {
                apiVersion: pluginApiVersion,
                registerMark: (type, compiler) => this.registerMark(type, compiler),
                registerRenderer: (factory) => this.registerRenderer(factory),
                registerTheme: (theme) => this.registerTheme(theme),
            };
            plugin.install(context);
            this.#plugins.set(plugin.name, plugin.version);
        }
        capabilities() {
            return {
                marks: this.markNames(),
                renderers: this.rendererNames(),
                themes: this.themes.names(),
                tableFormatters: [...this.tableFormatters.ids()].sort(),
                plugins: [...this.#plugins.keys()].sort(),
            };
        }
    }

    function createDefaultRegistry() {
        const registry = new RuntimeRegistry();
        registry.registerRenderer(canvasRendererFactory);
        registry.registerRenderer(scatterWebGLRendererFactory);
        registry.registerMark('line', compileOrderedLineMark);
        registry.registerMark('bar', compileRankedBarMark);
        registry.registerMark('point', compilePointMark);
        registry.registerMark('area', compileAreaMark);
        registry.registerMark('annotation', compileAnnotationMark);
        registry.registerMark('bubble', compileAreaBubbleMark);
        registry.registerMark('calendar', compileAdvancedCalendarMark);
        registry.registerMark('candlestick', compileAdvancedCandlestickMark);
        registry.registerMark('diff', compileAdvancedDifferenceMark);
        registry.registerMark('gantt', compileGanttMark);
        registry.registerMark('gauge', compileAdvancedGaugeMark);
        registry.registerMark('geo', compileGeoMark);
        registry.registerMark('histogram', compileHistogramMark);
        registry.registerMark('distribution', compileAnalyticalDistributionMark);
        registry.registerMark('interval', compileEstimatedIntervalMark);
        registry.registerMark('map', compileAdvancedMapMark);
        registry.registerMark('motion', compileBubbleMark);
        registry.registerMark('org', compileOrgMark);
        registry.registerMark('pie', compileAdvancedPieMark);
        registry.registerMark('sankey', compileAdvancedSankeyMark);
        registry.registerMark('stepped-area', compileSteppedAreaMark);
        registry.registerMark('table', compileAdvancedTableMark);
        registry.registerMark('timeline', compileAdvancedTimelineMark);
        registry.registerMark('treemap', compileTreemapMark);
        registry.registerMark('trendline', compileTrendlineMark);
        registry.registerMark('vega', compileVegaMark);
        registry.registerMark('waterfall', compileSemanticWaterfallMark);
        registry.registerMark('word-tree', compileAdvancedWordTreeMark);
        return registry;
    }
    createDefaultRegistry();

    /** Executable long-form series layout shared by Area, Bar, and Theme river. */
    const seriesStackCapability = {
        id: 'series-stack',
        status: 'supported',
        marks: ['area', 'bar', 'theme-river'],
        seriesField: 'mark.fields.series',
        option: 'mark.options.stack',
        modes: seriesStackModes,
        offsets: ['zero', 'normalize', 'expand', 'center', 'silhouette', 'wiggle'],
        orders: ['input', 'ascending', 'descending', 'sumAscending', 'sumDescending', 'insideOut'],
        signedBaseline: true,
        totals: ['absolute', 'positive', 'negative', 'net'],
        tooltipLineage: true,
        datumHitTargets: true,
        encoding: {
            bar: ['color', 'fill', 'stroke', 'opacity', 'strokeWidth', 'strokeDash', 'order', 'tooltip'],
            areaAndThemeRiver: [
                'color',
                'fill',
                'stroke',
                'opacity',
                'strokeWidth',
                'strokeDash',
                'order',
                'detail',
            ],
            areaPointOnly: ['size', 'radius', 'tooltip'],
            positionalConflicts: {
                bar: ['x2', 'y2'],
                area: ['y2'],
                themeRiver: ['y2'],
            },
        },
    };
    /**
     * Runtime truth for the historical tiled-map compatibility name. Graflume has
     * no provider-backed tile lifecycle, network loader, cache, or attribution API.
     */
    const tiledMapCapability = {
        id: 'tiled-map',
        status: 'deprecated',
        behavior: 'embedded-basemap-alias',
        preferredFamily: 'map',
        basemap: 'natural-earth-embedded',
        tileLifecycle: false,
        networkRequests: false,
    };
    /** Executable Canvas boundary for serializable analytic interaction state. */
    const analyticInteractionCapability = {
        id: 'analytic-interaction',
        status: 'supported',
        stateVersion: 1,
        selections: ['point', 'interval', 'rectangle', 'axis', 'lasso'],
        combine: ['union', 'intersection'],
        inputs: {
            pointer: true,
            touch: true,
            keyboardEscapeClear: true,
            keyboardGeometryAuthoring: true,
        },
        coordinates: {
            domainPixelRoundTrip: true,
            continuous: true,
            pointNearest: true,
            bandNearest: true,
            categoricalBrush: true,
            nonInvertibleGeometry: false,
        },
        domainNavigation: {
            cartesianContinuous: true,
            wheel: true,
            pointerDrag: true,
            keyboard: true,
            categorical: true,
            spatial: false,
            multiViewLinkedStore: true,
        },
        filtering: {
            selectionDriven: true,
            retainsScaleDomains: true,
            lineage: true,
        },
        composition: {
            coordinateViews: true,
            selectionShared: true,
            domainViewShared: true,
            viewAddressable: true,
        },
    };
    /** The two canonical surfaces are separate from the 45 named preset functions. */
    const technicalIndicatorCanonicalSurfaces = [
        {
            id: 'technicalIndicator',
            kind: 'quick-api',
            entryPoint: 'graflume/complete',
        },
        {
            id: 'indicator',
            kind: 'portable-mark',
            entryPoint: 'ChartSpec',
        },
    ];
    /** 45 named presets plus two canonical surfaces: 47 public entry points. */
    const technicalIndicatorPublicEntryPointCount = {
        canonicalSurfaces: technicalIndicatorCanonicalSurfaces.length,
        namedPresets: technicalIndicatorCapabilities.length,
        total: technicalIndicatorCanonicalSurfaces.length + technicalIndicatorCapabilities.length,
    };
    const computedTechnicalIndicatorCount = technicalIndicatorCapabilities.filter(({ support }) => support === 'computed').length;
    /** Machine-readable calculation boundary; never infers support from a public name. */
    const technicalIndicatorRuntimeCapability = {
        id: 'technical-indicator',
        status: 'supported',
        publicEntryPoints: {
            canonical: technicalIndicatorCanonicalSurfaces,
            ...technicalIndicatorPublicEntryPointCount,
        },
        calculations: {
            computed: computedTechnicalIndicatorCount,
            precomputedRequired: technicalIndicatorCapabilities.length - computedTechnicalIndicatorCount,
            warmUpPolicy: 'null',
            sessionReset: ['hard', 'carry'],
            incremental: { bounded: true, workerProtocol: 2 },
            synchronizedCrosshair: { axis: 'x', sharedDomain: true },
        },
        presets: technicalIndicatorCapabilities,
    };
    /** Exact public runtime claims embedded in the generated integration catalog. */
    const runtimeCapabilities = {
        analyticInteraction: analyticInteractionCapability,
        seriesStack: seriesStackCapability,
        technicalIndicators: technicalIndicatorRuntimeCapability,
        tiledMap: tiledMapCapability,
    };

    const transformWorkerProtocolVersion = 1;
    const DEFAULT_MAX_QUEUE_BATCHES = 8;
    const DEFAULT_MAX_ROWS = 100_000;
    const ABSOLUTE_MAX_ROWS = 1_000_000;
    const DEFAULT_MAX_TRANSFORMS = 128;
    const ADAPTER_OPTION_KEYS = new Set([
        'maxQueueBatches',
        'maxQueueRows',
        'maxInputRows',
        'maxTransforms',
    ]);
    const EXECUTE_OPTION_KEYS = new Set(['transferOwnership']);
    function assertClosedOptions(value, allowed, path) {
        if (!isPlainObject(value)) {
            throw new GraflumeError('INVALID_DATA', `${path} must be an object.`, { path });
        }
        const unknown = Object.keys(value).find((key) => !allowed.has(key));
        if (unknown !== undefined) {
            throw new GraflumeError('INVALID_DATA', `Unknown ${path} property "${unknown}".`, {
                path: `${path}.${unknown}`,
            });
        }
    }
    function boundedInteger(value, fallback, maximum, path) {
        const resolved = value ?? fallback;
        if (!Number.isInteger(resolved) || resolved < 1 || resolved > maximum) {
            throw new GraflumeError('INVALID_DATA', `${path} must be an integer from 1 to ${maximum}.`, {
                path,
            });
        }
        return resolved;
    }
    function normalizeOptions(options) {
        assertClosedOptions(options, ADAPTER_OPTION_KEYS, '$.worker');
        const maxInputRows = boundedInteger(options.maxInputRows, DEFAULT_MAX_ROWS, ABSOLUTE_MAX_ROWS, 'worker.maxInputRows');
        return {
            maxQueueBatches: boundedInteger(options.maxQueueBatches, DEFAULT_MAX_QUEUE_BATCHES, 1_024, 'worker.maxQueueBatches'),
            maxQueueRows: boundedInteger(options.maxQueueRows, maxInputRows, ABSOLUTE_MAX_ROWS, 'worker.maxQueueRows'),
            maxInputRows,
            maxTransforms: boundedInteger(options.maxTransforms, DEFAULT_MAX_TRANSFORMS, DEFAULT_MAX_TRANSFORMS, 'worker.maxTransforms'),
        };
    }
    function rowCount(data) {
        return DataTable.from(data).length;
    }
    function transferables(data) {
        if (Array.isArray(data))
            return [];
        const columnar = data;
        const buffers = new Set();
        for (const column of Object.values(columnar.columns)) {
            if (ArrayBuffer.isView(column) && column.buffer instanceof ArrayBuffer) {
                buffers.add(column.buffer);
            }
        }
        return [...buffers];
    }
    function snapshotRequest(data, transforms, transferOwnership) {
        try {
            const transformSnapshot = structuredClone(transforms);
            const sourceTransfers = transferOwnership ? transferables(data) : [];
            const dataSnapshot = structuredClone(data, sourceTransfers.length === 0 ? undefined : { transfer: sourceTransfers });
            return {
                data: dataSnapshot,
                transforms: transformSnapshot,
                transfer: transferOwnership ? transferables(dataSnapshot) : [],
            };
        }
        catch (error) {
            throw new GraflumeError('INVALID_DATA', 'Worker data and transforms must be structured-clone portable; functions are unsupported.', { details: { cause: error instanceof Error ? error.message : String(error) } });
        }
    }
    function isResponse(value) {
        if (value === null || typeof value !== 'object')
            return false;
        const response = value;
        return (response.protocol === transformWorkerProtocolVersion &&
            response.type === 'graflume:transform-result' &&
            typeof response.id === 'number' &&
            typeof response.ok === 'boolean');
    }
    function isRequest(value) {
        if (value === null || typeof value !== 'object')
            return false;
        const request = value;
        return (request.protocol === transformWorkerProtocolVersion &&
            request.type === 'graflume:transform' &&
            typeof request.id === 'number' &&
            request.data !== undefined &&
            Array.isArray(request.transforms));
    }
    function serializedError(error) {
        if (error instanceof GraflumeError) {
            return {
                name: error.name,
                message: error.message,
                code: error.code,
                ...(error.path === undefined ? {} : { path: error.path }),
            };
        }
        return {
            name: error instanceof Error ? error.name : 'Error',
            message: error instanceof Error ? error.message : String(error),
        };
    }
    class TransformWorkerAdapter {
        #port;
        #options;
        #queue = [];
        #queuedRows = 0;
        #active = null;
        #nextId = 1;
        #completed = 0;
        #rejected = 0;
        #closed = false;
        #messageListener = (event) => {
            if (!isResponse(event.data) || this.#active === null)
                return;
            if (event.data.id !== this.#active.request.id)
                return;
            const active = this.#active;
            this.#active = null;
            if (event.data.ok) {
                this.#completed += 1;
                active.resolve(event.data.result);
            }
            else {
                this.#rejected += 1;
                active.reject(new GraflumeError('INVALID_DATA', event.data.error.message, {
                    ...(event.data.error.path === undefined ? {} : { path: event.data.error.path }),
                    details: { workerCode: event.data.error.code ?? event.data.error.name },
                }));
            }
            this.#dispatch();
        };
        constructor(port, options = {}) {
            this.#port = port;
            this.#options = normalizeOptions(options);
            this.#port.addEventListener('message', this.#messageListener);
        }
        execute(data, transforms, options = {}) {
            if (this.#closed) {
                return this.#rejectRequest(new GraflumeError('INVALID_DATA', 'Transform worker adapter is closed.'));
            }
            try {
                assertClosedOptions(options, EXECUTE_OPTION_KEYS, '$.worker.execute');
                if (options.transferOwnership !== undefined &&
                    typeof options.transferOwnership !== 'boolean') {
                    throw new GraflumeError('INVALID_DATA', '$.worker.execute.transferOwnership must be boolean.', { path: '$.worker.execute.transferOwnership' });
                }
            }
            catch (error) {
                return this.#rejectRequest(error);
            }
            const rows = rowCount(data);
            if (rows > this.#options.maxInputRows) {
                return this.#rejectRequest(new GraflumeError('INVALID_DATA', `Worker input has ${rows} rows; the deterministic limit is ${this.#options.maxInputRows}.`));
            }
            if (transforms.length > this.#options.maxTransforms) {
                return this.#rejectRequest(new GraflumeError('INVALID_DATA', `Worker request has ${transforms.length} transforms; the deterministic limit is ${this.#options.maxTransforms}.`));
            }
            if (this.#queue.length + (this.#active === null ? 0 : 1) >= this.#options.maxQueueBatches ||
                this.#queuedRows + (this.#active?.rows ?? 0) + rows > this.#options.maxQueueRows) {
                return this.#rejectRequest(new GraflumeError('INVALID_DATA', 'Transform worker backpressure limit reached; the request was rejected without dispatch.'));
            }
            let snapshot;
            try {
                snapshot = snapshotRequest(data, transforms, options.transferOwnership === true);
            }
            catch (error) {
                return this.#rejectRequest(error);
            }
            const request = {
                protocol: transformWorkerProtocolVersion,
                type: 'graflume:transform',
                id: this.#nextId,
                data: snapshot.data,
                transforms: snapshot.transforms,
            };
            this.#nextId += 1;
            return new Promise((resolve, reject) => {
                this.#queue.push({
                    request,
                    rows,
                    transfer: snapshot.transfer,
                    resolve,
                    reject,
                });
                this.#queuedRows += rows;
                this.#dispatch();
            });
        }
        state() {
            return {
                protocol: transformWorkerProtocolVersion,
                queuedBatches: this.#queue.length,
                queuedRows: this.#queuedRows,
                inFlight: this.#active !== null,
                completed: this.#completed,
                rejected: this.#rejected,
                closed: this.#closed,
            };
        }
        close(options = {}) {
            if (this.#closed)
                return;
            this.#closed = true;
            this.#port.removeEventListener('message', this.#messageListener);
            const error = new GraflumeError('INVALID_DATA', 'Transform worker adapter closed.');
            this.#rejected += (this.#active === null ? 0 : 1) + this.#queue.length;
            this.#active?.reject(error);
            this.#active = null;
            for (const item of this.#queue)
                item.reject(error);
            this.#queue = [];
            this.#queuedRows = 0;
            if (options.terminate === true)
                this.#port.terminate?.();
        }
        #rejectRequest(error) {
            this.#rejected += 1;
            return Promise.reject(error);
        }
        #dispatch() {
            if (this.#closed || this.#active !== null)
                return;
            const next = this.#queue.shift();
            if (next === undefined)
                return;
            this.#queuedRows -= next.rows;
            this.#active = next;
            try {
                this.#port.postMessage(next.request, next.transfer);
            }
            catch (error) {
                this.#active = null;
                this.#rejected += 1;
                next.reject(error);
                this.#dispatch();
            }
        }
    }
    function createTransformWorkerAdapter(port, options = {}) {
        return new TransformWorkerAdapter(port, options);
    }
    function installTransformWorker(scope, options = {}) {
        const normalized = normalizeOptions(options);
        const listener = (event) => {
            if (!isRequest(event.data))
                return;
            const request = event.data;
            let response;
            try {
                const rows = rowCount(request.data);
                if (rows > normalized.maxInputRows) {
                    throw new GraflumeError('INVALID_DATA', `Worker input has ${rows} rows; the deterministic limit is ${normalized.maxInputRows}.`);
                }
                if (request.transforms.length > normalized.maxTransforms) {
                    throw new GraflumeError('INVALID_DATA', `Worker request has ${request.transforms.length} transforms; the deterministic limit is ${normalized.maxTransforms}.`);
                }
                response = {
                    protocol: transformWorkerProtocolVersion,
                    type: 'graflume:transform-result',
                    id: request.id,
                    ok: true,
                    result: executeTransforms(request.data, request.transforms),
                };
            }
            catch (error) {
                response = {
                    protocol: transformWorkerProtocolVersion,
                    type: 'graflume:transform-result',
                    id: request.id,
                    ok: false,
                    error: serializedError(error),
                };
            }
            scope.postMessage(response);
        };
        scope.addEventListener('message', listener);
        return () => scope.removeEventListener('message', listener);
    }

    const linkedViewStateVersion = 1;
    const maximumLinkedAnalyticViews = 128;
    function invalid(message) {
        throw new GraflumeError('INVALID_SPEC', message, { path: '$.linkedViewStore' });
    }
    function viewId(value) {
        if (typeof value !== 'string' ||
            value.length === 0 ||
            value.length > 4096 ||
            value === '__proto__' ||
            value === 'prototype' ||
            value === 'constructor') {
            invalid('Linked analytic view IDs must be safe non-empty bounded strings.');
        }
        return value;
    }
    function normalizeLinkedViewState(input) {
        if (typeof input !== 'object' || input === null || Array.isArray(input)) {
            invalid('Linked view state must be an object.');
        }
        const unknown = Object.keys(input).filter((key) => key !== 'version' && key !== 'analyticSelection' && key !== 'domainView');
        if (unknown.length > 0)
            invalid(`Linked view state contains unknown key "${unknown[0]}".`);
        if (input.version !== linkedViewStateVersion) {
            invalid(`Linked view state.version must be ${linkedViewStateVersion}.`);
        }
        return Object.freeze({
            version: linkedViewStateVersion,
            analyticSelection: normalizeAnalyticSelectionState(input.analyticSelection),
            domainView: normalizeDomainViewState(input.domainView),
        });
    }
    function emptyLinkedViewState() {
        return normalizeLinkedViewState({
            version: linkedViewStateVersion,
            analyticSelection: emptyAnalyticSelectionState(),
            domainView: emptyDomainViewState(),
        });
    }
    function same(left, right) {
        return JSON.stringify(left) === JSON.stringify(right);
    }
    /**
     * Runtime-only bounded state bus for independently created Canvas views. A
     * composed chart already shares one internal state; injecting this store adds
     * the same deterministic selection/domain link across Chart instances.
     */
    class LinkedViewStateStore {
        #state;
        #listeners = new Map();
        constructor(initial = emptyLinkedViewState()) {
            this.#state = normalizeLinkedViewState(initial);
        }
        get() {
            return this.#state;
        }
        register(view, listener) {
            const id = viewId(view);
            if (typeof listener !== 'function')
                invalid('Linked view listener must be a function.');
            if (!this.#listeners.has(id) && this.#listeners.size >= maximumLinkedAnalyticViews) {
                invalid(`Linked view store exceeds the ${maximumLinkedAnalyticViews} view bound.`);
            }
            this.#listeners.set(id, listener);
            return () => {
                if (this.#listeners.get(id) === listener)
                    this.#listeners.delete(id);
            };
        }
        set(state, sourceViewId, changed = 'both') {
            const source = viewId(sourceViewId);
            const next = normalizeLinkedViewState(state);
            if (same(next, this.#state))
                return this.#state;
            this.#state = next;
            const change = Object.freeze({ state: next, sourceViewId: source, changed });
            for (const [id, listener] of [...this.#listeners]) {
                if (id !== source)
                    listener(change);
            }
            return next;
        }
        setAnalyticSelection(analyticSelection, sourceViewId) {
            return this.set({ ...this.#state, analyticSelection }, sourceViewId, 'analytic-selection');
        }
        setDomainView(domainView, sourceViewId) {
            return this.set({ ...this.#state, domainView }, sourceViewId, 'domain-view');
        }
    }

    const additionalMarkCompilers = [
        ['radar', compileRadarMark],
        ['polar', compileAdvancedPolarMark],
        ['tree', compileAdvancedTreeMark],
        ['graph', compileAdvancedGraphMark],
        ['chord', compileAdvancedChordMark],
        ['funnel', compileAdvancedFunnelMark],
        ['parallel', compileAdvancedParallelMark],
        ['boxplot', compileBoxplotMark],
        ['image', compileAnalyticalImageMark],
        ['ternary', compileAnalyticalTernaryMark],
        ['smith', compileAnalyticalSmithMark],
        ['scatter-matrix', compileAnalyticalScatterMatrixMark],
        ['carpet', compileAnalyticalCarpetMark],
        ['effect-scatter', compileEffectScatterMark],
        ['lines', compileLinesMark],
        ['heatmap', compileAnalyticalHeatmapMark],
        ['pictorial-bar', compilePictorialBarMark],
        ['theme-river', compileThemeRiverMark],
        ['sunburst', compileSunburstMark],
        ['custom', compileCustomMark],
        ['arc-diagram', compileArcDiagramMark],
        ['range', compileRangeMark],
        ['smooth', compileSmoothMark],
        ['distribution', compileAnalyticalDistributionMark],
        ['bullet', compileBulletMark],
        ['contour', compileAdvancedContourMark],
        ['cylinder', compileCylinderMark],
        ['item', compileAnalyticalItemMark],
        ['lollipop', compileLollipopMark],
        ['packed-bubble', compilePackedBubbleMark],
        ['pareto', compileParetoMark],
        ['polygon', compilePolygonMark],
        ['pyramid', compilePyramidMark],
        ['scatter-3d', compileScatter3dMark],
        ['solid-gauge', compileSolidGaugeMark],
        ['tilemap', compileTilemapMark],
        ['variable-pie', compileVariablePieMark],
        ['variwide', compileVariwideMark],
        ['vector', compileAdvancedVectorMark],
        ['venn', compileAdvancedVennMark],
        ['wind-barb', compileWindBarbMark],
        ['word-cloud', compileAdvancedWordCloudMark],
        ['indicator', compileIndicatorMark],
        ['flags', compileFlagsMark],
        ['financial', compileFinancialMark],
        ['point-figure', compilePointFigureMark],
        ['renko', compilePriceBlocksMark],
        ['volume-profile', compileAdvancedVolumeProfileMark],
        ['geo-flow', compileGeoFlowMark],
        ['geo-heatmap', compileGeoHeatmapMark],
        ['geo-line', compileGeoLineMark],
        ['tiled-map', compileTiledMapMark],
    ];
    function installAdditionalMarks(registry) {
        for (const [type, compiler] of additionalMarkCompilers)
            registry.registerMark(type, compiler);
    }
    function createCompleteRegistry() {
        const registry = createDefaultRegistry();
        installAdditionalMarks(registry);
        return registry;
    }
    const completeRegistry = createCompleteRegistry();
    function registerAdditionalMarks(registry = completeRegistry) {
        installAdditionalMarks(registry);
    }
    function create(target, spec, options) {
        return new Chart(target, spec, completeRegistry, options);
    }
    function compile(spec, options) {
        return compileWithRegistry(spec, completeRegistry, options);
    }
    function registerTheme(theme) {
        completeRegistry.registerTheme(theme);
    }
    function registerRenderer(factory) {
        completeRegistry.registerRenderer(factory);
    }
    function registerMark(type, compiler) {
        completeRegistry.registerMark(type, compiler);
    }
    function use(plugin) {
        completeRegistry.use(plugin);
    }
    function capabilities() {
        return completeRegistry.capabilities();
    }
    const createRegistry = createCompleteRegistry;
    function line(target, data, options) {
        return quickChart(create, 'line', target, data, options);
    }
    function bar(target, data, options) {
        return quickChart(create, 'bar', target, data, options);
    }
    function point(target, data, options) {
        return quickChart(create, 'point', target, data, options);
    }
    function scatter(target, data, options) {
        return quickScatter(create, target, data, options);
    }
    function area(target, data, options) {
        return quickChart(create, 'area', target, data, options);
    }
    function specialized(type, target, data, options, markDefaults = {}) {
        return quickChart(create, type, target, data, {
            ...options,
            mark: {
                ...markDefaults,
                ...options.mark,
                fields: { ...markDefaults.fields, ...options.mark?.fields },
                options: { ...markDefaults.options, ...options.mark?.options },
            },
        });
    }
    function annotation(target, data, options) {
        return specialized('annotation', target, data, options, { point: true });
    }
    function annotatedTimeline(target, data, options) {
        return annotation(target, data, options);
    }
    function horizontalBar(target, data, options) {
        return specialized('bar', target, data, options, { orientation: 'horizontal' });
    }
    function column(target, data, options) {
        return specialized('bar', target, data, options, { orientation: 'vertical' });
    }
    function bubble(target, data, options) {
        return specialized('bubble', target, data, options);
    }
    function calendar(target, data, options) {
        return specialized('calendar', target, data, options);
    }
    function candlestick(target, data, options) {
        return specialized('candlestick', target, data, options);
    }
    function combo(target, data, options) {
        return quickCombo(create, target, data, options);
    }
    function diff(target, data, options) {
        return specialized('diff', target, data, options);
    }
    function pie(target, data, options) {
        return specialized('pie', target, data, options);
    }
    function donut(target, data, options) {
        return specialized('pie', target, data, options, { options: { innerRadius: 0.56 } });
    }
    function gantt(target, data, options) {
        return specialized('gantt', target, data, options);
    }
    function gauge(target, data, options) {
        return specialized('gauge', target, data, options);
    }
    function gaugeNumber(target, data, options) {
        return specialized('gauge', target, data, options, { options: { mode: 'number' } });
    }
    function gaugeDelta(target, data, options) {
        return specialized('gauge', target, data, options, {
            fields: { reference: 'reference' },
            options: { mode: 'delta' },
        });
    }
    function gaugeBullet(target, data, options) {
        return specialized('gauge', target, data, options, {
            fields: { target: 'target' },
            options: { mode: 'bullet' },
        });
    }
    function geo(target, data, options) {
        return specialized('geo', target, data, options);
    }
    function histogram(target, data, options) {
        return specialized('histogram', target, data, options);
    }
    /** Canonical API for histogram, empirical, KDE, box, violin, curve, and bivariate modes. */
    function distribution(target, data, options) {
        return specialized('distribution', target, data, options, { options: { mode: 'histogram' } });
    }
    function violin(target, data, options) {
        return specialized('distribution', target, data, options, { options: { mode: 'violin' } });
    }
    function ecdf(target, data, options) {
        return specialized('distribution', target, data, options, { options: { mode: 'ecdf' } });
    }
    function ccdf(target, data, options) {
        return specialized('distribution', target, data, options, { options: { mode: 'ccdf' } });
    }
    function kde(target, data, options) {
        return specialized('distribution', target, data, options, { options: { mode: 'kde' } });
    }
    function histogram2d(target, data, options) {
        return specialized('distribution', target, data, options, {
            options: { mode: 'histogram-2d' },
        });
    }
    function histogram2dContour(target, data, options) {
        return specialized('distribution', target, data, options, {
            options: { mode: 'histogram-2d-contour' },
        });
    }
    function intervals(target, data, options) {
        return specialized('interval', target, data, options);
    }
    const interval = intervals;
    function map(target, data, options) {
        return specialized('map', target, data, options);
    }
    function motion(target, data, options) {
        return specialized('motion', target, data, options);
    }
    function org(target, data, options) {
        return specialized('org', target, data, options);
    }
    function sankey(target, data, options) {
        return specialized('sankey', target, data, options);
    }
    function steppedArea(target, data, options) {
        return specialized('stepped-area', target, data, options);
    }
    function table(target, data, options) {
        return specialized('table', target, data, options);
    }
    function timeline(target, data, options) {
        return specialized('timeline', target, data, options);
    }
    function treemap(target, data, options) {
        return specialized('treemap', target, data, options);
    }
    function icicle(target, data, options) {
        return specialized('treemap', target, data, options, {
            fields: { parent: 'parent' },
            options: { mode: 'icicle' },
        });
    }
    function trendline(target, data, options) {
        return specialized('trendline', target, data, options);
    }
    function vegaChart(target, data, options) {
        return specialized('vega', target, data, options);
    }
    function waterfall(target, data, options) {
        return specialized('waterfall', target, data, options);
    }
    function wordTree(target, data, options) {
        return specialized('word-tree', target, data, options);
    }
    function additional(type, target, data, options) {
        return quickChart(create, type, target, data, options);
    }
    function radar(target, data, options) {
        return additional('radar', target, data, options);
    }
    function polar(target, data, options) {
        return additional('polar', target, data, options);
    }
    function polarLine(target, data, options) {
        return additional('polar', target, data, mergeDefaults({ options: { mode: 'line' } }, options));
    }
    function polarScatter(target, data, options) {
        return additional('polar', target, data, mergeDefaults({ options: { mode: 'scatter' } }, options));
    }
    function polarBar(target, data, options) {
        return additional('polar', target, data, mergeDefaults({ options: { mode: 'bar' } }, options));
    }
    function image(target, data, options) {
        return additional('image', target, data, options);
    }
    function ternary(target, data, options) {
        return additional('ternary', target, data, options);
    }
    function smith(target, data, options) {
        return additional('smith', target, data, options);
    }
    function scatterMatrix(target, data, options) {
        return additional('scatter-matrix', target, data, options);
    }
    function carpet(target, data, options) {
        return additional('carpet', target, data, options);
    }
    function carpetScatter(target, data, options) {
        return additional('carpet', target, data, mergeDefaults({ options: { mode: 'scatter' } }, options));
    }
    function carpetContour(target, data, options) {
        return additional('carpet', target, data, mergeDefaults({ options: { mode: 'contour' } }, options));
    }
    function tree(target, data, options) {
        return additional('tree', target, data, options);
    }
    function graph(target, data, options) {
        return additional('graph', target, data, options);
    }
    function selectedMode(options) {
        const mode = options.mark?.options?.mode;
        return typeof mode === 'string' ? mode : undefined;
    }
    /** One family API for node-link, arc, and connection-line layouts. */
    function network(target, data, options) {
        const mode = selectedMode(options);
        if (mode === 'arc')
            return arcDiagram(target, data, options);
        if (mode === 'connections')
            return lines(target, data, options);
        return graph(target, data, options);
    }
    function chord(target, data, options) {
        return additional('chord', target, data, options);
    }
    function funnel(target, data, options) {
        return additional('funnel', target, data, options);
    }
    function funnelArea(target, data, options) {
        return additional('funnel', target, data, mergeDefaults({ options: { mode: 'area' } }, options));
    }
    function parallel(target, data, options) {
        return additional('parallel', target, data, options);
    }
    function parallelCategories(target, data, options) {
        return additional('parallel', target, data, mergeDefaults({ options: { mode: 'categories' } }, options));
    }
    function boxplot(target, data, options) {
        return additional('boxplot', target, data, options);
    }
    function effectScatter(target, data, options) {
        return additional('effect-scatter', target, data, options);
    }
    function lines(target, data, options) {
        return additional('lines', target, data, options);
    }
    function heatmap(target, data, options) {
        return additional('heatmap', target, data, options);
    }
    function pictorialBar(target, data, options) {
        return additional('pictorial-bar', target, data, options);
    }
    function themeRiver(target, data, options) {
        return additional('theme-river', target, data, options);
    }
    function sunburst(target, data, options) {
        return additional('sunburst', target, data, options);
    }
    function custom(target, data, options) {
        return additional('custom', target, data, options);
    }
    function mergeDefaults(defaults, options) {
        return {
            ...options,
            mark: {
                ...defaults,
                ...options.mark,
                fields: { ...defaults.fields, ...options.mark?.fields },
                options: { ...defaults.options, ...options.mark?.options },
            },
        };
    }
    function makeSeriesQuick(type, defaults = {}) {
        return (target, data, options) => additional(type, target, data, mergeDefaults(defaults, options));
    }
    function makeIndicatorQuick(kind, defaults = {}) {
        return makeSeriesQuick('indicator', {
            ...defaults,
            options: { kind, ...defaults.options },
        });
    }
    const arcDiagram = makeSeriesQuick('arc-diagram', {
        fields: { target: 'target', value: 'value' },
    });
    const areaRange = makeSeriesQuick('range', {
        fields: { low: 'low', high: 'high' },
        options: { mode: 'area' },
    });
    const areaSpline = makeSeriesQuick('smooth', { options: { area: true } });
    const areaSplineRange = makeSeriesQuick('range', {
        fields: { low: 'low', high: 'high' },
        options: { mode: 'area', smooth: true },
    });
    const bellCurve = makeSeriesQuick('distribution', { options: { mode: 'curve' } });
    const bullet = makeSeriesQuick('bullet', { fields: { target: 'target' } });
    const columnPyramid = makeSeriesQuick('pyramid', { options: { variant: 'column' } });
    const columnRange = makeSeriesQuick('range', {
        fields: { low: 'low', high: 'high' },
        options: { mode: 'column' },
    });
    const contour = makeSeriesQuick('contour', { fields: { value: 'value' } });
    const cylinder = makeSeriesQuick('cylinder');
    const dependencyWheel = chord;
    const dumbbell = makeSeriesQuick('range', {
        fields: { low: 'low', high: 'high' },
        options: { mode: 'dumbbell' },
    });
    const errorBar = intervals;
    const funnel3d = makeSeriesQuick('pyramid', { options: { variant: 'funnel-3d' } });
    const itemChart = makeSeriesQuick('item');
    const lollipop = makeSeriesQuick('lollipop');
    const networkGraph = graph;
    const organizationNetwork = org;
    const packedBubble = makeSeriesQuick('packed-bubble');
    const pareto = makeSeriesQuick('pareto');
    const pictorialColumn = pictorialBar;
    const polygon = makeSeriesQuick('polygon');
    const pyramid = makeSeriesQuick('pyramid', { options: { variant: 'pyramid' } });
    const pyramid3d = makeSeriesQuick('pyramid', { options: { variant: 'pyramid-3d' } });
    const scatter3d = makeSeriesQuick('scatter-3d', { fields: { z: 'z' } });
    const solidGauge = makeSeriesQuick('solid-gauge');
    const spline = makeSeriesQuick('smooth');
    const streamgraph = makeSeriesQuick('theme-river', {
        fields: { series: 'series' },
        options: {
            stack: { mode: 'streamgraph', offset: 'wiggle', order: 'insideOut' },
        },
    });
    const tileMap = makeSeriesQuick('tilemap', { fields: { value: 'value' } });
    const treeGraph = tree;
    const variablePie = makeSeriesQuick('variable-pie', { fields: { radius: 'radius' } });
    const variableWidth = makeSeriesQuick('variwide', { fields: { width: 'width' } });
    const vector = makeSeriesQuick('vector', {
        fields: { direction: 'direction', magnitude: 'magnitude' },
    });
    const venn = makeSeriesQuick('venn');
    const windBarb = makeSeriesQuick('wind-barb', {
        fields: { speed: 'speed', direction: 'direction' },
    });
    const wordCloud = makeSeriesQuick('word-cloud');
    const xRange = timeline;
    /** One family API for arrow and wind-barb vector glyphs. */
    function vectorField(target, data, options) {
        return selectedMode(options) === 'wind-barb'
            ? windBarb(target, data, options)
            : vector(target, data, options);
    }
    const accelerationBands = makeIndicatorQuick('abands', {
        fields: { lower: 'lower', middle: 'value', upper: 'upper' },
    });
    const awesomeOscillator = makeIndicatorQuick('ao');
    const absolutePriceOscillator = makeIndicatorQuick('apo');
    const aroon = makeIndicatorQuick('aroon', {
        options: { fields: ['up', 'down'] },
    });
    const aroonOscillator = makeIndicatorQuick('aroonoscillator');
    const averageTrueRange = makeIndicatorQuick('atr');
    const volatilityBands = makeIndicatorQuick('bb', {
        fields: { lower: 'lower', middle: 'value', upper: 'upper' },
    });
    const commodityChannelIndex = makeIndicatorQuick('cci');
    const chaikinOscillator = makeIndicatorQuick('chaikin');
    const chaikinMoneyFlow = makeIndicatorQuick('cmf');
    const chandeMomentumOscillator = makeIndicatorQuick('cmo');
    const doubleExponentialMovingAverage = makeIndicatorQuick('dema');
    const disparityIndex = makeIndicatorQuick('disparityindex');
    const directionalMovementIndex = makeIndicatorQuick('dmi', {
        options: { fields: ['plus', 'minus', 'value'] },
    });
    const detrendedPriceOscillator = makeIndicatorQuick('dpo');
    const exponentialMovingAverage = makeIndicatorQuick('ema');
    const eventFlags = makeSeriesQuick('flags', { fields: { title: 'title' } });
    const heikinAshi = makeSeriesQuick('financial', {
        fields: { open: 'open', high: 'high', low: 'low', close: 'close' },
        options: { kind: 'heikin-ashi' },
    });
    const highLowClose = makeSeriesQuick('financial', {
        fields: { high: 'high', low: 'low', close: 'close' },
        options: { kind: 'hlc' },
    });
    const hollowCandlestick = makeSeriesQuick('financial', {
        fields: { open: 'open', high: 'high', low: 'low', close: 'close' },
        options: { kind: 'hollow-candlestick' },
    });
    const ichimokuCloud = makeIndicatorQuick('ikh', {
        fields: { lower: 'lower', middle: 'value', upper: 'upper' },
        options: { fields: ['conversion', 'base', 'value'] },
    });
    const keltnerChannels = makeIndicatorQuick('keltnerchannels', {
        fields: { lower: 'lower', middle: 'value', upper: 'upper' },
    });
    const klingerOscillator = makeIndicatorQuick('klinger', {
        options: { fields: ['value', 'signal'] },
    });
    const linearRegression = makeIndicatorQuick('linearregression');
    const linearRegressionAngle = makeIndicatorQuick('linearregressionangle');
    const linearRegressionIntercept = makeIndicatorQuick('linearregressionintercept');
    const linearRegressionSlope = makeIndicatorQuick('linearregressionslope');
    const movingAverageConvergenceDivergence = makeIndicatorQuick('macd', {
        options: { fields: ['value', 'signal'] },
    });
    const moneyFlowIndex = makeIndicatorQuick('mfi');
    const momentumIndicator = makeIndicatorQuick('momentum');
    const normalizedAverageTrueRange = makeIndicatorQuick('natr');
    const onBalanceVolume = makeIndicatorQuick('obv');
    const openHighLowClose = makeSeriesQuick('financial', {
        fields: { open: 'open', high: 'high', low: 'low', close: 'close' },
        options: { kind: 'ohlc' },
    });
    const priceChannel = makeIndicatorQuick('pc', {
        fields: { lower: 'lower', middle: 'value', upper: 'upper' },
    });
    const pivotPoints = makeIndicatorQuick('pivotpoints', {
        options: { fields: ['support', 'value', 'resistance'] },
    });
    const pointAndFigure = makeSeriesQuick('point-figure');
    const percentagePriceOscillator = makeIndicatorQuick('ppo');
    const priceEnvelopes = makeIndicatorQuick('priceenvelopes', {
        fields: { lower: 'lower', middle: 'value', upper: 'upper' },
    });
    const parabolicStopAndReverse = makeIndicatorQuick('psar');
    const renko = makeSeriesQuick('renko');
    const kagi = makeSeriesQuick('renko', { options: { mode: 'kagi' } });
    const threeLineBreak = makeSeriesQuick('renko', {
        options: { mode: 'three-line-break' },
    });
    const rangeBars = makeSeriesQuick('renko', { options: { mode: 'range-bars' } });
    const rateOfChange = makeIndicatorQuick('roc');
    const relativeStrengthIndex = makeIndicatorQuick('rsi');
    const slowStochastic = makeIndicatorQuick('slowstochastic', {
        options: { fields: ['value', 'signal'] },
    });
    const simpleMovingAverage = makeIndicatorQuick('sma');
    const stochastic = makeIndicatorQuick('stochastic', {
        options: { fields: ['value', 'signal'] },
    });
    const supertrend = makeIndicatorQuick('supertrend');
    const tripleExponentialMovingAverage = makeIndicatorQuick('tema');
    const tripleExponentialAverageOscillator = makeIndicatorQuick('trix');
    const volumeByPrice = makeSeriesQuick('volume-profile', {
        fields: { price: 'price', volume: 'volume' },
    });
    const volumeWeightedAveragePrice = makeIndicatorQuick('vwap');
    const williamsRange = makeIndicatorQuick('williamsr');
    const weightedMovingAverage = makeIndicatorQuick('wma');
    const zigzag = makeIndicatorQuick('zigzag');
    /** One family API for all indicator presets; select one with mark.options.kind. */
    const technicalIndicator = makeSeriesQuick('indicator', {
        options: { kind: 'sma' },
    });
    /** One family API for discrete price-block layouts. */
    function priceBlocks(target, data, options) {
        return selectedMode(options) === 'point-and-figure'
            ? pointAndFigure(target, data, options)
            : renko(target, data, options);
    }
    /** Canonical name for the volume-by-price preset. */
    const volumeProfile = volumeByPrice;
    const flowMap = makeSeriesQuick('geo-flow', {
        fields: { longitude2: 'longitude2', latitude2: 'latitude2', value: 'value' },
    });
    const geoHeatmap = makeSeriesQuick('geo-heatmap', { fields: { value: 'value' } });
    const mapBubble = map;
    const mapLine = makeSeriesQuick('geo-line', {
        fields: { longitude2: 'longitude2', latitude2: 'latitude2', value: 'value' },
    });
    const mapPoint = map;
    const tiledMap = makeSeriesQuick('tiled-map');
    const fullCatalog = [
        ...chartTypeCatalog,
        ...additionalChartTypeCatalog,
        ...seriesChartTypeCatalog,
    ];
    /** All historical names as presets mapped onto the consolidated family catalog. */
    const fullVariantCatalog = [
        ...chartVariantCatalog,
        ...additionalChartVariantCatalog,
        ...seriesChartVariantCatalog,
    ];

    exports.AnalyticSelectionStore = AnalyticSelectionStore;
    exports.AutomaticWorkerRuntime = AutomaticWorkerRuntime;
    exports.BarVirtualizationController = BarVirtualizationController;
    exports.BoundedRingBuffer = BoundedRingBuffer;
    exports.CanvasRenderer = CanvasRenderer;
    exports.Chart = Chart;
    exports.DEFAULT_TECHNICAL_INDICATOR_MAX_ROWS = DEFAULT_TECHNICAL_INDICATOR_MAX_ROWS;
    exports.DataTable = DataTable;
    exports.GraflumeError = GraflumeError;
    exports.IncrementalDataStore = IncrementalDataStore;
    exports.IncrementalStackPipeline = IncrementalStackPipeline;
    exports.IncrementalStreamRuntime = IncrementalStreamRuntime;
    exports.IncrementalTransformPipeline = IncrementalTransformPipeline;
    exports.LinkedViewStateStore = LinkedViewStateStore;
    exports.MAX_TECHNICAL_INDICATOR_ROWS = MAX_TECHNICAL_INDICATOR_ROWS;
    exports.MapLayerRegistry = MapLayerRegistry;
    exports.MapRuntime = MapRuntime;
    exports.MapTileManager = MapTileManager;
    exports.MarkLabelHistory = MarkLabelHistory;
    exports.RuntimeRegistry = RuntimeRegistry;
    exports.ScatterWebGLRenderer = ScatterWebGLRenderer;
    exports.SemanticFocusStore = SemanticFocusStore;
    exports.TableFormatterRegistry = TableFormatterRegistry;
    exports.TechnicalIndicatorIncrementalCalculator = TechnicalIndicatorIncrementalCalculator;
    exports.TransformDataflow = TransformDataflow;
    exports.TransformWorkerAdapter = TransformWorkerAdapter;
    exports.VirtualDataExplorer = VirtualDataExplorer;
    exports.WorkerStreamRetentionRuntime = WorkerStreamRetentionRuntime;
    exports.absolutePriceOscillator = absolutePriceOscillator;
    exports.accelerationBands = accelerationBands;
    exports.addAnalyticKeyboardVertex = addAnalyticKeyboardVertex;
    exports.additionalChartTypeCatalog = additionalChartTypeCatalog;
    exports.additionalChartVariantCatalog = additionalChartVariantCatalog;
    exports.aggregateOhlc = aggregateOhlc;
    exports.analyticInteractionCapability = analyticInteractionCapability;
    exports.analyticSelectionKey = analyticSelectionKey;
    exports.analyticSelectionMatches = analyticSelectionMatches;
    exports.analyticSelectionPredicate = analyticSelectionPredicate;
    exports.analyticSelectionVersion = analyticSelectionVersion;
    exports.analyzeSets = analyzeSets;
    exports.annotatedTimeline = annotatedTimeline;
    exports.annotation = annotation;
    exports.arcDiagram = arcDiagram;
    exports.area = area;
    exports.areaRadius = areaRadius;
    exports.areaRange = areaRange;
    exports.areaSizeGuide = areaSizeGuide;
    exports.areaSpline = areaSpline;
    exports.areaSplineRange = areaSplineRange;
    exports.aroon = aroon;
    exports.aroonOscillator = aroonOscillator;
    exports.assertValidSpec = assertValidSpec;
    exports.automaticScatterWebGLThreshold = automaticScatterWebGLThreshold;
    exports.averageTrueRange = averageTrueRange;
    exports.awesomeOscillator = awesomeOscillator;
    exports.bar = bar;
    exports.bellCurve = bellCurve;
    exports.boxplot = boxplot;
    exports.brushHeatmap = brushHeatmap;
    exports.bubble = bubble;
    exports.buildHeatmapMatrix = buildHeatmapMatrix;
    exports.buildTableModel = buildTableModel;
    exports.buildWordTree = buildWordTree;
    exports.builtInThemeCatalog = builtInThemeCatalog;
    exports.bullet = bullet;
    exports.calculateTechnicalIndicator = calculateTechnicalIndicator;
    exports.calculateTechnicalIndicatorIncremental = calculateTechnicalIndicatorIncremental;
    exports.calendar = calendar;
    exports.calendarCells = calendarCells;
    exports.calendarWeekStart = calendarWeekStart;
    exports.calendarWeekdayLabels = calendarWeekdayLabels;
    exports.candlestick = candlestick;
    exports.canvasRendererFactory = canvasRendererFactory;
    exports.capabilities = capabilities;
    exports.carpet = carpet;
    exports.carpetContour = carpetContour;
    exports.carpetScatter = carpetScatter;
    exports.cartesianAxisChannel = cartesianAxisChannel;
    exports.ccdf = ccdf;
    exports.chaikinMoneyFlow = chaikinMoneyFlow;
    exports.chaikinOscillator = chaikinOscillator;
    exports.chandeMomentumOscillator = chandeMomentumOscillator;
    exports.chartTypeCatalog = chartTypeCatalog;
    exports.chartVariantCatalog = chartVariantCatalog;
    exports.chord = chord;
    exports.chordEdgesToMatrix = chordEdgesToMatrix;
    exports.chordMatrixToEdges = chordMatrixToEdges;
    exports.clampPixelToPlot = clampPixelToPlot;
    exports.cloneMarkLabelPosition = cloneMarkLabelPosition;
    exports.cloneMarkLabelPositions = cloneMarkLabelPositions;
    exports.colorScaleTypes = colorScaleTypes;
    exports.column = column;
    exports.columnPyramid = columnPyramid;
    exports.columnRange = columnRange;
    exports.combo = combo;
    exports.commodityChannelIndex = commodityChannelIndex;
    exports.compile = compile;
    exports.completeAnalyticKeyboardSelection = completeAnalyticKeyboardSelection;
    exports.compositionOperators = compositionOperators;
    exports.contour = contour;
    exports.create = create;
    exports.createAutomaticWorkerRuntime = createAutomaticWorkerRuntime;
    exports.createBarVirtualizationController = createBarVirtualizationController;
    exports.createBoundedRingBuffer = createBoundedRingBuffer;
    exports.createColorScale = createColorScale;
    exports.createCompleteRegistry = createCompleteRegistry;
    exports.createIncrementalDataStore = createIncrementalDataStore;
    exports.createIncrementalStackPipeline = createIncrementalStackPipeline;
    exports.createIncrementalStreamRuntime = createIncrementalStreamRuntime;
    exports.createIncrementalTransformPipeline = createIncrementalTransformPipeline;
    exports.createMapRuntime = createMapRuntime;
    exports.createMapTileManager = createMapTileManager;
    exports.createPositionScale = createPositionScale;
    exports.createRegistry = createRegistry;
    exports.createSemanticFocusStore = createSemanticFocusStore;
    exports.createTableFormatterRegistry = createTableFormatterRegistry;
    exports.createTransformDataflow = createTransformDataflow;
    exports.createTransformWorkerAdapter = createTransformWorkerAdapter;
    exports.createVirtualDataExplorer = createVirtualDataExplorer;
    exports.createWorkerStreamRetentionRuntime = createWorkerStreamRetentionRuntime;
    exports.curveNames = curveNames;
    exports.curveRegistry = curveRegistry;
    exports.custom = custom;
    exports.cylinder = cylinder;
    exports.dataInputRowCount = dataInputRowCount;
    exports.defaultSemanticFocusStore = defaultSemanticFocusStore;
    exports.defaultThemeId = defaultThemeId;
    exports.dependencyWheel = dependencyWheel;
    exports.detrendedPriceOscillator = detrendedPriceOscillator;
    exports.diff = diff;
    exports.differenceSeries = differenceSeries;
    exports.directionalMovementIndex = directionalMovementIndex;
    exports.disparityIndex = disparityIndex;
    exports.distribution = distribution;
    exports.domainAxisWindow = domainAxisWindow;
    exports.domainForAxisWindow = domainForAxisWindow;
    exports.domainPointToPixel = domainPointToPixel;
    exports.domainToPixel = domainToPixel;
    exports.domainViewIsIdentity = domainViewIsIdentity;
    exports.domainViewVersion = domainViewVersion;
    exports.donut = donut;
    exports.doubleExponentialMovingAverage = doubleExponentialMovingAverage;
    exports.dumbbell = dumbbell;
    exports.ecdf = ecdf;
    exports.effectScatter = effectScatter;
    exports.emptyAnalyticSelectionState = emptyAnalyticSelectionState;
    exports.emptyDomainViewState = emptyDomainViewState;
    exports.emptyLinkedViewState = emptyLinkedViewState;
    exports.errorBar = errorBar;
    exports.estimateInterval = estimateInterval;
    exports.evaluateTransformExpression = evaluateTransformExpression;
    exports.eventFlags = eventFlags;
    exports.executePortableBarVirtualization = executePortableBarVirtualization;
    exports.executePortableIncrementalStack = executePortableIncrementalStack;
    exports.executeTransformBranch = executeTransformBranch;
    exports.executeTransformDataflow = executeTransformDataflow;
    exports.executeTransforms = executeTransforms;
    exports.executeTransformsWithNamedLineage = executeTransformsWithNamedLineage;
    exports.exponentialMovingAverage = exponentialMovingAverage;
    exports.fetchMapTile = fetchMapTile;
    exports.fitMapBounds = fitMapBounds;
    exports.flowMap = flowMap;
    exports.flowRuntimeOptions = flowRuntimeOptions;
    exports.fullCatalog = fullCatalog;
    exports.fullVariantCatalog = fullVariantCatalog;
    exports.funnel = funnel;
    exports.funnel3d = funnel3d;
    exports.funnelArea = funnelArea;
    exports.funnelStages = funnelStages;
    exports.gantt = gantt;
    exports.gauge = gauge;
    exports.gaugeBullet = gaugeBullet;
    exports.gaugeDelta = gaugeDelta;
    exports.gaugeModel = gaugeModel;
    exports.gaugeNumber = gaugeNumber;
    exports.geo = geo;
    exports.geoHeatmap = geoHeatmap;
    exports.geodesicPath = geodesicPath;
    exports.graflumeDark = graflumeDark;
    exports.graflumeGgplot = graflumeGgplot;
    exports.graflumeLight = graflumeLight;
    exports.graflumeMatplotlib = graflumeMatplotlib;
    exports.graflumeRBase = graflumeRBase;
    exports.graph = graph;
    exports.heatmap = heatmap;
    exports.heatmapRuntimeOptions = heatmapRuntimeOptions;
    exports.heikinAshi = heikinAshi;
    exports.hierarchyRuntimeOptions = hierarchyRuntimeOptions;
    exports.highLowClose = highLowClose;
    exports.histogram = histogram;
    exports.histogram2d = histogram2d;
    exports.histogram2dContour = histogram2dContour;
    exports.hitSetRegion = hitSetRegion;
    exports.hitTestMarkLabel = hitTestMarkLabel;
    exports.hitTestScene = hitTestScene;
    exports.hollowCandlestick = hollowCandlestick;
    exports.horizontalBar = horizontalBar;
    exports.ichimokuCloud = ichimokuCloud;
    exports.icicle = icicle;
    exports.image = image;
    exports.installTransformWorker = installTransformWorker;
    exports.installWorkerRuntime = installWorkerRuntime;
    exports.interpolateCurve = interpolateCurve;
    exports.interval = interval;
    exports.intervals = intervals;
    exports.invertParallelAxis = invertParallelAxis;
    exports.isNamedDataReference = isNamedDataReference;
    exports.itemChart = itemChart;
    exports.kagi = kagi;
    exports.kde = kde;
    exports.keltnerChannels = keltnerChannels;
    exports.klingerOscillator = klingerOscillator;
    exports.layoutChord = layoutChord;
    exports.layoutFlow = layoutFlow;
    exports.layoutHierarchy = layoutHierarchy;
    exports.layoutItems = layoutItems;
    exports.layoutNetwork = layoutNetwork;
    exports.layoutPie = layoutPie;
    exports.layoutPolar = layoutPolar;
    exports.layoutTimeline = layoutTimeline;
    exports.layoutWordCloud = layoutWordCloud;
    exports.line = line;
    exports.linearRegression = linearRegression;
    exports.linearRegressionAngle = linearRegressionAngle;
    exports.linearRegressionIntercept = linearRegressionIntercept;
    exports.linearRegressionSlope = linearRegressionSlope;
    exports.lines = lines;
    exports.linkedViewStateVersion = linkedViewStateVersion;
    exports.lollipop = lollipop;
    exports.map = map;
    exports.mapBounds = mapBounds;
    exports.mapBubble = mapBubble;
    exports.mapGraticule = mapGraticule;
    exports.mapLine = mapLine;
    exports.mapPoint = mapPoint;
    exports.mapRasterColor = mapRasterColor;
    exports.materializeSpecDataflow = materializeSpecDataflow;
    exports.maximumAnalyticSelections = maximumAnalyticSelections;
    exports.maximumCategoricalSelectionValues = maximumCategoricalSelectionValues;
    exports.maximumCompositionDepth = maximumCompositionDepth;
    exports.maximumCompositionLayers = maximumCompositionLayers;
    exports.maximumCompositionViews = maximumCompositionViews;
    exports.maximumLassoPoints = maximumLassoPoints;
    exports.maximumLayerCompositionChildren = maximumLayerCompositionChildren;
    exports.maximumLinkedAnalyticViews = maximumLinkedAnalyticViews;
    exports.momentumIndicator = momentumIndicator;
    exports.moneyFlowIndex = moneyFlowIndex;
    exports.motion = motion;
    exports.moveAnalyticKeyboardGesture = moveAnalyticKeyboardGesture;
    exports.moveNetworkNode = moveNetworkNode;
    exports.moveTableCell = moveTableCell;
    exports.movingAverageConvergenceDivergence = movingAverageConvergenceDivergence;
    exports.namedDataLineage = namedDataLineage;
    exports.navigatorRuntimeOptions = navigatorRuntimeOptions;
    exports.navigatorWindow = navigatorWindow;
    exports.network = network;
    exports.networkGraph = networkGraph;
    exports.networkRuntimeOptions = networkRuntimeOptions;
    exports.nextPieSlice = nextPieSlice;
    exports.normalizeAnalyticSelectionState = normalizeAnalyticSelectionState;
    exports.normalizeDomainViewState = normalizeDomainViewState;
    exports.normalizeFlowRuntimeState = normalizeFlowRuntimeState;
    exports.normalizeGeoJson = normalizeGeoJson;
    exports.normalizeHeatmapRuntimeState = normalizeHeatmapRuntimeState;
    exports.normalizeHierarchyRuntimeState = normalizeHierarchyRuntimeState;
    exports.normalizeLinkedViewState = normalizeLinkedViewState;
    exports.normalizeNavigatorRuntimeState = normalizeNavigatorRuntimeState;
    exports.normalizeNetworkRuntimeState = normalizeNetworkRuntimeState;
    exports.normalizeParallelRuntimeState = normalizeParallelRuntimeState;
    exports.normalizeScatterMatrixRuntimeState = normalizeScatterMatrixRuntimeState;
    exports.normalizeSpec = normalizeSpec$1;
    exports.normalizeTableRuntimeState = normalizeTableRuntimeState;
    exports.normalizeVirtualDataExplorerSpec = normalizeVirtualDataExplorerSpec;
    exports.normalizedAverageTrueRange = normalizedAverageTrueRange;
    exports.onBalanceVolume = onBalanceVolume;
    exports.openHighLowClose = openHighLowClose;
    exports.org = org;
    exports.organizationNetwork = organizationNetwork;
    exports.packedBubble = packedBubble;
    exports.panDomainAxisWindow = panDomainAxisWindow;
    exports.panDomainByPixels = panDomainByPixels;
    exports.parabolicStopAndReverse = parabolicStopAndReverse;
    exports.parallel = parallel;
    exports.parallelCategories = parallelCategories;
    exports.parallelRuntimeOptions = parallelRuntimeOptions;
    exports.pareto = pareto;
    exports.percentagePriceOscillator = percentagePriceOscillator;
    exports.pictorialBar = pictorialBar;
    exports.pictorialColumn = pictorialColumn;
    exports.pie = pie;
    exports.pivotPoints = pivotPoints;
    exports.pixelAxisToSelection = pixelAxisToSelection;
    exports.pixelLassoToSelection = pixelLassoToSelection;
    exports.pixelPointToDomain = pixelPointToDomain;
    exports.pixelRectangleToSelection = pixelRectangleToSelection;
    exports.pixelToDomain = pixelToDomain;
    exports.pluginApiVersion = pluginApiVersion;
    exports.point = point;
    exports.pointAndFigure = pointAndFigure;
    exports.polar = polar;
    exports.polarBar = polarBar;
    exports.polarLine = polarLine;
    exports.polarScatter = polarScatter;
    exports.polygon = polygon;
    exports.positionScaleTypes = positionScaleTypes;
    exports.prepareOrderedSeries = prepareOrderedSeries;
    exports.previewAnalyticKeyboardSelection = previewAnalyticKeyboardSelection;
    exports.priceBlocks = priceBlocks;
    exports.priceChannel = priceChannel;
    exports.priceEnvelopes = priceEnvelopes;
    exports.projectCarpet = projectCarpet;
    exports.projectMapPosition = projectMapPosition;
    exports.projectParallelRows = projectParallelRows;
    exports.projectSmith = projectSmith;
    exports.projectTernary = projectTernary;
    exports.pyramid = pyramid;
    exports.pyramid3d = pyramid3d;
    exports.querySetRegion = querySetRegion;
    exports.quickScatter = quickScatter;
    exports.radar = radar;
    exports.rangeBars = rangeBars;
    exports.rankBars = rankBars;
    exports.rateOfChange = rateOfChange;
    exports.registerAdditionalMarks = registerAdditionalMarks;
    exports.registerMark = registerMark;
    exports.registerRenderer = registerRenderer;
    exports.registerTheme = registerTheme;
    exports.relativeStrengthIndex = relativeStrengthIndex;
    exports.renko = renko;
    exports.reorderParallelAxis = reorderParallelAxis;
    exports.replayIncrementalData = replayIncrementalData;
    exports.resolveScatterRendererDispatch = resolveScatterRendererDispatch;
    exports.resolveSeriesType = resolveSeriesType;
    exports.resolveTechnicalIndicatorCapability = resolveTechnicalIndicatorCapability;
    exports.resolveTechnicalIndicatorPresentation = resolveTechnicalIndicatorPresentation;
    exports.restoreWorkerStreamRetentionRuntime = restoreWorkerStreamRetentionRuntime;
    exports.rugStrip = rugStrip;
    exports.runtimeCapabilities = runtimeCapabilities;
    exports.sampleRaster = sampleRaster;
    exports.sankey = sankey;
    exports.scatter = scatter;
    exports.scatter3d = scatter3d;
    exports.scatterMatrix = scatterMatrix;
    exports.scatterMatrixPlan = scatterMatrixPlan;
    exports.scatterMatrixPointerBrush = scatterMatrixPointerBrush;
    exports.scatterMatrixRuntimeOptions = scatterMatrixRuntimeOptions;
    exports.scatterWebGLRendererFactory = scatterWebGLRendererFactory;
    exports.selectNetworkNodes = selectNetworkNodes;
    exports.selectScatterMatrixRows = selectScatterMatrixRows;
    exports.selectionToPixels = selectionToPixels;
    exports.seriesChartTypeCatalog = seriesChartTypeCatalog;
    exports.seriesChartVariantCatalog = seriesChartVariantCatalog;
    exports.seriesCompatibilityCatalog = seriesCompatibilityCatalog;
    exports.seriesCompatibilityIds = seriesCompatibilityIds;
    exports.seriesStackCapability = seriesStackCapability;
    exports.seriesStackModes = seriesStackModes;
    exports.setMarkLabelOffset = setMarkLabelOffset;
    exports.setParallelBrushExtents = setParallelBrushExtents;
    exports.sharedHistogramBins = sharedHistogramBins;
    exports.simpleMovingAverage = simpleMovingAverage;
    exports.slowStochastic = slowStochastic;
    exports.smith = smith;
    exports.snapMarkLabelOffset = snapMarkLabelOffset;
    exports.solidGauge = solidGauge;
    exports.specVersion = specVersion;
    exports.spline = spline;
    exports.startAnalyticKeyboardGesture = startAnalyticKeyboardGesture;
    exports.steppedArea = steppedArea;
    exports.stochastic = stochastic;
    exports.streamgraph = streamgraph;
    exports.sunburst = sunburst;
    exports.supertrend = supertrend;
    exports.table = table;
    exports.tableRuntimeOptions = tableRuntimeOptions;
    exports.technicalIndicator = technicalIndicator;
    exports.technicalIndicatorCanonicalSurfaces = technicalIndicatorCanonicalSurfaces;
    exports.technicalIndicatorCapabilities = technicalIndicatorCapabilities;
    exports.technicalIndicatorPresetIds = technicalIndicatorPresetIds;
    exports.technicalIndicatorPublicEntryPointCount = technicalIndicatorPublicEntryPointCount;
    exports.technicalIndicatorRuntimeCapability = technicalIndicatorRuntimeCapability;
    exports.ternary = ternary;
    exports.themeRiver = themeRiver;
    exports.threeLineBreak = threeLineBreak;
    exports.tileMap = tileMap;
    exports.tileUrl = tileUrl;
    exports.tiledMap = tiledMap;
    exports.tiledMapCapability = tiledMapCapability;
    exports.timeline = timeline;
    exports.toAccessibleRows = toAccessibleRows;
    exports.tokenizeWords = tokenizeWords;
    exports.topologyToGeoJson = topologyToGeoJson;
    exports.transformWorkerProtocolVersion = transformWorkerProtocolVersion;
    exports.translateNavigatorWindow = translateNavigatorWindow;
    exports.traverseFlowPath = traverseFlowPath;
    exports.tree = tree;
    exports.treeGraph = treeGraph;
    exports.treemap = treemap;
    exports.trendline = trendline;
    exports.tripleExponentialAverageOscillator = tripleExponentialAverageOscillator;
    exports.tripleExponentialMovingAverage = tripleExponentialMovingAverage;
    exports.use = use;
    exports.validateSpec = validateSpec;
    exports.variablePie = variablePie;
    exports.variableWidth = variableWidth;
    exports.vector = vector;
    exports.vectorField = vectorField;
    exports.vegaChart = vegaChart;
    exports.venn = venn;
    exports.version = version;
    exports.violin = violin;
    exports.volatilityBands = volatilityBands;
    exports.volumeByPrice = volumeByPrice;
    exports.volumeProfile = volumeProfile;
    exports.volumeWeightedAveragePrice = volumeWeightedAveragePrice;
    exports.waterfall = waterfall;
    exports.waterfallSteps = waterfallSteps;
    exports.weightedBoxSummary = weightedBoxSummary;
    exports.weightedCount = weightedCount;
    exports.weightedMovingAverage = weightedMovingAverage;
    exports.williamsRange = williamsRange;
    exports.windBarb = windBarb;
    exports.wordCloud = wordCloud;
    exports.wordTree = wordTree;
    exports.workerRuntimeProtocolVersion = workerRuntimeProtocolVersion;
    exports.wrapLongitude = wrapLongitude;
    exports.xRange = xRange;
    exports.zigzag = zigzag;
    exports.zoomDomainAtPixel = zoomDomainAtPixel;
    exports.zoomDomainAxisWindow = zoomDomainAxisWindow;

    return exports;

})({});
//# sourceMappingURL=graflume.complete.global.js.map
